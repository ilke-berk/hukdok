# G027 — CaseGroup sayfası + kalan kullanılmayan ui/ bileşenleri

- **Bant:** frontend
- **Bağımlı:** G026
- **Dosya kapsamı:** frontend/src/pages/CaseGroup.tsx, frontend/src/components/ui/** (kalan ölüler), frontend/src/components/** (yalnız CaseGroup'a bağlı ölü parçalar), frontend/src/App.tsx (yalnız rota kaydı), frontend/package.json
- **Dokunma:** frontend/src/hooks/useCases.ts (`getCaseGroup*` fonksiyonları — bkz. aşağıdaki not), backend/**

## Hedef

Plan FAZ C'nin kalanı.

**1. `CaseGroup` sayfası (666 satır) — tamamlanmamış özellik iskeleti.** Bağlı olduğu backend
uçları (`/api/case-groups/{id}`, `/api/cases/{id}/group`) **hiç var olmamış** — git geçmişinde
de yok. Yani sayfa çalıştığı gün bile 404 alıyordu.

**2. Kalan kullanılmayan `ui/` bileşenleri (~1.900 satır).** G026'dan sonra yeniden tara:
knip ya da `ts-prune` çıktısını **elden geçir**, her adayı import aramasıyla teyit et.
Bilinen adaylardan biri `ui/chart.tsx` (recharts'ı import ediyor, kendisinin ithal edeni yok).

**3. Sahipsiz kalan npm bağımlılıkları.** G026 + bu görevin silmeleri sonrası `frontend/src`
içinde hiç import edilmeyen paketler (`recharts` gibi) kaldırılır. G021 dört paketi zaten
sildi; bu, onun devamı.

> **`useCases.ts` NOTU:** `getCaseGroup*` fonksiyonları CaseGroup sayfasının tek tüketicisiydi.
> Sayfa silinince onlar da ölür. AMA `useCases.ts` bu görevin "Dokunma" listesinde çünkü gece
> boyunca G019/G020 orayı değiştirdi ve hub dosya. **Karar:** ölü fonksiyonları sil ama
> `useCases.ts`'te başka HİÇBİR şeye dokunma; diff'te yalnız o fonksiyonlar görünmeli.
> Şüphedeysen bırak ve raporda "izlenecek" olarak yaz.

> **PAZARLIKSIZ KURAL:** `getattr`/string-dispatch/dinamik import taraması yapılmadan hiçbir
> sembol ölü sayılmaz. Bu görev dosyasındaki listeye güvenme — kendin doğrula.
> Rota kaydı (`App.tsx`) üzerinden lazy import var mı, ayrıca kontrol et.

## Kabul kriterleri

- [ ] **Beklenen silme beyanı:** başta liste + satır sayısı, sonda gerçekleşenle karşılaştırma
- [ ] `pages/CaseGroup.tsx` silindi (**666 satır**) ve `App.tsx`'teki rota kaydı da kaldırıldı
- [ ] Uygulamada CaseGroup'a giden **hiçbir link/yönlendirme kalmadı** (arama ile kanıtlı) —
      kalırsa kullanıcı ölü rotaya tıklar
- [ ] Kalan ölü `ui/` bileşenleri **yeniden taranarak** silindi; her biri için import araması
      kanıtı raporda. Şüpheli olan BIRAKILDI ve gerekçesi yazıldı
- [ ] Sahipsiz npm bağımlılıkları kaldırıldı; `package-lock.json` `npm install` ile üretildi
      (elle düzenlenmedi). `npm audit` önce/sonra raporda
- [ ] `useCases.ts` diff'i **yalnız** `getCaseGroup*` fonksiyonlarını içeriyor (`git diff` kanıtı)
- [ ] **Net satır deltası NEGATİF**
- [ ] `npm --prefix frontend run build` başarılı; bundle boyutu önce/sonra raporda
- [ ] `npx tsc --noEmit` temiz; mevcut **317 test** yeşil
- [ ] `npm --prefix frontend run lint` yeni HATA üretmiyor

## Doğrulama

- `npm --prefix frontend install`
- `npm --prefix frontend test`
- `npm --prefix frontend run build`
- `npx --prefix frontend tsc --noEmit`

## Rapor

### 1. Beklenen silme beyanı (uygulamadan ÖNCE ilan edildi)

| # | Hedef | Beklenen satır |
| --- | --- | --- |
| A | `pages/CaseGroup.tsx` (dosya) | 666 |
| B | Ölü `ui/` bileşenleri (23 dosya) | 1.942 |
| C | `App.tsx` — import + rota kaydı | 2 |
| D | `useCases.ts` — `getCaseGroup` + `getCaseGroupByCase` + return kayıtları | ~19 |
| E | `Sidebar.tsx` — `/case-groups` prefix eşleşmesi | satır içi (net 0) |
| F | `package.json` — sahipsiz `dependencies` | 25 paket |

**Gerçekleşen** (`git diff --numstat`): A 666 ✓ · B 1.942 ✓ · C 2 ✓ · D 19 ✓ · E 1-/1+ ✓ ·
F 25 paket (26-/1+, son satırın sondaki virgülü) ✓. **Sapma yok.**

### 2. Ölü kod tespiti — yöntem ve kanıt

Görev dosyasındaki listeye güvenilmedi; kapanış (reachability) analizi sıfırdan kuruldu:

1. `frontend` genelinde `components/ui` geçen TÜM dosyalar tarandı (`node_modules`/`dist` hariç).
   Yalnız `components.json` (shadcn alias, dosya listesi değil) ve `README_FRONTEND.md` kod dışı.
2. Tüm `from "@/components/ui/<x>"` import'ları çıkarıldı → **ui dışından** ithal edilenler kök
   kümesi oldu; ui-içi kenarlar (`alert-dialog→button`, `command→dialog`, `toaster→toast`,
   `form→label`, `calendar→button`, `carousel→button`, `pagination→button`,
   `toggle-group→toggle`) ile geçişli kapanış alındı.
3. **Dinamik erişim taraması** (pazarlıksız kural): `import(` → yalnız `"sonner"` (npm paketi,
   `ui/sonner` DEĞİL) ve `"@/lib/api"`. `import.meta.glob` / `require(` → **hiç yok**.
   Göreli ui import'u (`./ui/x`, `../ui/x`) → **hiç yok**. Vite `resolve.alias` yalnız `@`.
   Yani TS/ESM'de import dışı sembol erişimi mümkün değil; statik kapanış eksiksiz.
4. **Çapraz doğrulama:** ölü adayların JSX kullanımı ayrıca arandı
   (`<Accordion|Alert|AspectRatio|...`). Tüm isabetler `AlertDialog*` (canlı) ya da lucide
   ikonları (`CalendarPlus`, `CalendarDays`, `SlidersHorizontal`) çıktı — ölü bileşen kullanımı yok.
5. Test dosyaları da tarandı: hiçbir test ölü bileşeni ya da `CaseGroup`'u ithal etmiyor.

**Canlı kalan 23 ui dosyası:** alert-dialog, badge, button, card, checkbox, collapsible, command,
dialog, dropdown-menu, input, label, popover, select, skeleton, sonner, switch, table, tabs,
textarea, toast, toaster, tooltip, use-toast.

**Silinen 23 ui dosyası** (hepsinin ithal edeni sıfır): accordion 52, alert 43, aspect-ratio 5,
avatar 38, breadcrumb 90, calendar 54, carousel 224, chart 303, context-menu 178, drawer 87,
form 129, hover-card 27, input-otp 61, menubar 207, navigation-menu 120, pagination 81,
progress 23, radio-group 36, resizable 37, scroll-area 38, slider 23, toggle-group 49, toggle 37.
`toggle`, tek tüketicisi `toggle-group` de ölü olduğu için birlikte düştü. **Şüpheli bırakılan
aday olmadı** — her adayın ithal-eden sayısı kesin sıfırdı, belirsizlik kalmadı.

### 3. CaseGroup — backend uçları gerçekten hiç yok

`grep -rn 'case_group' backend/` → **hiç eşleşme yok.** Sayfa `/api/case-groups/{id}` ve
`/api/cases/{id}/group` çağırıyordu; ikisi de hiç var olmamış. Görev dosyasındaki tespit teyitli.

**Ölü rotaya giden yol kalmadı** (kanıt: `grep -rn 'case-groups\|CaseGroup\|caseGroup' src/`):
- `App.tsx` import + `<Route path="/case-groups/:groupId">` → silindi.
- `Sidebar.tsx:34` `p.startsWith("/case-groups")` → kaldırıldı (rota yokken ölü eşleşme).
- Kalan TEK isabet: `CaseDetails.tsx:68` yalnızca bir **yorum satırı**
  (`// Tip badge renkleri (CaseGroup ile tutarlı)`) — link/yönlendirme değil, kapsam dışı (aş. NOT).

### 4. npm bağımlılıkları

Silinen 25 `dependencies` — kaynağı ölü ui bileşenleri (20):
`@radix-ui/react-{accordion,aspect-ratio,avatar,context-menu,hover-card,menubar,navigation-menu,progress,radio-group,scroll-area,slider,toggle,toggle-group}`,
`embla-carousel-react` (carousel), `input-otp`, `react-day-picker` (calendar),
`react-hook-form` (form), `react-resizable-panels` (resizable), `recharts` (chart), `vaul` (drawer).

Ek olarak **zaten sahipsiz** 5 paket (G026 kalıntısı / hiç kullanılmamış — `frontend` genelinde,
config dosyaları dahil, tek referansı yoktu): `@hookform/resolvers`, `@radix-ui/react-separator`
(karşılığı `separator.tsx` zaten yok), `axios` (API katmanı `fetch` kullanıyor), `date-fns`
(tek tüketicisi `react-day-picker` idi), `zod`.

`package-lock.json` **elle düzenlenmedi**, `npm install` üretti → `removed 84 packages`.

`npm audit`: **20 açık (7 orta, 13 yüksek) → 16 açık (6 orta, 10 yüksek).** Düşüşün tamamı
`axios` zincirinden (axios + follow-redirects + form-data).

### 5. Ölçümler (önce → sonra)

| Ölçüm | Önce | Sonra | Delta |
| --- | --- | --- | --- |
| Bundle JS | 1.320,60 kB (gzip 362,01) | 1.300,13 kB (gzip 358,93) | **−20,47 kB** (gzip −3,08) |
| Bundle CSS | 91,98 kB (gzip 16,77) | 79,01 kB (gzip 14,82) | **−12,97 kB** (gzip −1,95) |
| Transform edilen modül | 1.988 | 1.987 | −1 |
| `npm audit` | 20 (7 orta / 13 yüksek) | 16 (6 orta / 10 yüksek) | −4 |
| node_modules paketi | 494 | 410 | −84 |
| `ui/` dosya sayısı | 46 | 23 | −23 |

**Net satır deltası: −3.837** (13 ekleme / 3.850 silme; `src/` payı −2.629, `package.json` −25,
`package-lock.json` −1.183). **NEGATİF** ✓

> Modül sayısının yalnız 1 düşmesi beklenen davranış: ölü `ui/` bileşenlerinin zaten ithal edeni
> yoktu, Rollup onları hiç grafiğe almıyordu. Bundle'daki asıl kazanç CSS'te — Tailwind artık
> silinen dosyaları taramadığı için üretilen sınıf kümesi küçüldü. Yani kazanım bundle'dan çok
> **bakım yükü + bağımlılık yüzeyi + güvenlik açığı** tarafında.

### 6. Doğrulama sonuçları

| Kapı | Sonuç |
| --- | --- |
| `npm install` | ✅ removed 84 packages, audited 410 |
| `npx tsc --noEmit` | ✅ **temiz** (çıktı yok) |
| `npm test` (vitest) | ✅ **317 passed (317)** / 25 dosya — taban ile birebir aynı |
| `npm run build` | ✅ 4,70 s'de başarılı |
| `npm run lint` | ✅ **0 hata**, 22 uyarı (hepsi dokunulmamış dosyalarda: `useCaseIntake`, `useDashboardView`, `usePageTitle`, `Index`, `NewCase`, `NewClient` — mevcut `react-hooks`/`react-refresh` uyarıları) |
| Dokunulan dosyalarda lint | ✅ `App.tsx` + `useCases.ts` + `Sidebar.tsx` → tamamen temiz |

Bant kuralına uyuldu: worktree'deyim, **`docker compose` komutu koşulmadı**; testler host'ta
vitest ile koştu.

### 7. Alınan kararlar + gerekçeleri

- **`useCases.ts` "Dokunma" istisnası dar tutuldu.** Görev dosyasının izin verdiği tek istisna
  kullanıldı: diff'te *yalnız* `getCaseGroup`/`getCaseGroupByCase` blokları ve return'deki iki
  kaydı var (yukarıdaki `git diff` kanıtı). Hub dosyada başka hiçbir şeye dokunulmadı ki
  G019/G020 ile çakışma doğmasın.
- **`Sidebar.tsx` kapsam içi sayıldı.** Dosya kapsamı `components/**`'ı "yalnız CaseGroup'a bağlı
  ölü parçalar" için açıyor; `p.startsWith("/case-groups")` rota silinince tanımı gereği ölü.
- **Yalnız `dependencies` temizlendi, `devDependencies` bırakıldı.** Görevin kuralı "`frontend/src`
  içinde ithal edilmeyen paketler"; bu ölçüt build araçlarına (vite, typescript, eslint) doğası
  gereği uymaz, dolayısıyla runtime bağımlılıklarına aittir. Sahipsiz görünen devDep'ler aşağıda
  NOT olarak bırakıldı — alakasız riski bu commit'e katmamak için.
- **Şüpheli bileşen bırakılmadı** çünkü şüphe kalmadı: her adayın ithal-eden sayısı sıfırdı ve
  dinamik erişim yolu (glob/require/dinamik import/göreli import) topluca elendi.

### 8. İzlenecekler / NOT'lar (kapsam dışı — düzeltilmedi)

- **NOT:** `frontend/README_FRONTEND.md:8` "49 hazır komponent `src/components/ui/` altında"
  diyor; gerçek sayı bu görevden ÖNCE 46, SONRA **23**. Sayı G026 ile zaten yanlıştı. Dosya bu
  görevin kapsam listesinde yok, o yüzden dokunulmadı — ayrı bir doküman görevine değer.
- **NOT:** `frontend/src/pages/CaseDetails.tsx` iki ölü kalıntı taşıyor: `case_group_id?: number;`
  (satır 52 — backend'de `case_group` diye bir şey yok, hiç dolmayacak opsiyonel alan) ve satır
  68'deki `(CaseGroup ile tutarlı)` yorumu. `pages/CaseDetails.tsx` kapsam listesinde olmadığı
  için dokunulmadı. Fonksiyonel etkisi yok (alan hiçbir yerde okunmuyor).
- **NOT:** sahipsiz görünen `devDependencies`: `@types/plist`, `@tailwindcss/typography`
  (tailwind.config.ts `plugins`'inde kayıtlı değil), `cross-env` (hiçbir npm script'inde yok).
  Üçü de `frontend` genelinde tek referansa sahip değil. Silmeleri bu görevin silme hikâyesiyle
  ilgisiz olduğu için ertelendi.
- **İzlenecek:** bundle hâlâ 1,30 MB tek parça; Vite "chunks larger than 500 kB" uyarısı sürüyor.
  Kod bölme (route bazlı `lazy`) ayrı bir iş — bu görevin konusu değil.
