# G152 — `status` kesim-sonrası koruma: kullanıcı değişikliği paketle ezilmez + `DEGISIKLIK_OZETI` "Veri kesim tarihi"

- **Bant:** backend
- **Bağımlı:** G151 (aynı `hukdok_aktarim.py`)
- **Dosya kapsamı:** backend/scripts/hukdok_aktarim.py (`_kart_alanlarini_yaz` `:1613-1640`; `status` alanı `:755`),
  backend/services/teslim_kutusu.py (`onceki_teslim_adi_oku` `:832-859` yanına `kesim_tarihi_oku`; defter kolonu
  YOK — değer koşuya parametre geçer), backend/managers/case_manager.py (yalnız `update_case` tarihçe imzası
  `:1010-1027`: `changed_by`/`source` doldurulur), backend/tests/test_g152_status_koruma.py (YENİ)
- **Dokunma:** backend/models.py, backend/database.py (kolon/migrasyon YOK), routes/**, frontend/**

## Test değiştirme İZNİ (baştan)

**İZİNLİ:** `tests/test_cases_update.py`'de `case_history` satırlarının `changed_by`/`source` alanını NULL bekleyen
beklentiler (varsa). **KİLİT:** `test_g064_aktarim_cekirdek.py`, `test_g107_teslim_kutusu.py`, diğerleri.

## Hedef

Plan 08.09 §1.5 **P2** (kullanıcı kararı 08.09: "paket kazanır"ın tek istisnası). Ekibin veri kesimi 30.07.2026;
o tarihten sonra bizde MAHZEN'e alınan kartı paket `AKTIF → DERDEST` ile **geri açar** (`DURUM_ESLEMESI` `:542`,
`status` `:755`, üzerine yazma `:1614-1622`). Ekip de "Durum'da HukuDok'un güncel kaydı esas" diyor.

## Yapılacaklar

1. **Kesim tarihi kaynağı:** `DEGISIKLIK_OZETI` sayfasında "Veri kesim tarihi: 30.07.2026" satırı (`onceki_teslim_adi_oku`
   deseniyle `kesim_tarihi_oku`); yoksa paket adındaki tarih (`HUKDOK_TESLIM_PAKETI_2026-09-04` → 2026-09-04) ile
   **uyarı**; o da yoksa kural devre dışı + WARNING. `hukdok_aktarim.aktarimi_kos`'a `kesim_tarihi` parametresi;
   teslim hattı (`teslim_kutusu`) ve CLI (`--kesim-tarihi`) geçer.
2. **Koruma kuralı:** `status` alanında paket değeri mevcut değerden farklıysa: `case_history`'de `field_name='status'`,
   `changed_at > kesim_tarihi` ve `source` **aktarım imzası taşımayan** (`AKTARIM_SOURCE_PREFIX` ile başlamayan,
   `case_manager._is_aktarim_kaydi` ikizi) bir kayıt varsa → **yazılmaz**, satır raporuna "status korundu (kullanıcı
   dd.mm.yyyy)" ve `sonuc.status_korunan` sayacı. Yoksa mevcut davranış (paket yazar).
3. **Elle yol imzası:** `update_case` tarihçesi (`case_manager.py:1019-1027`) `changed_by=<kullanıcı>` ve
   `source="panel"` yazar (bugün NULL). Takip paneli `status` değiştiriyorsa (`update_case_tracking`) aynı imza.
   `tracked_fields` genişletilmez (kapsam dışı).
4. **Testler:** kesim tarihi okuma (sayfa / ad / yok); kullanıcı kaydı kesim sonrası → korunur + rapor; kesim öncesi →
   paket yazar; aktarım imzalı kayıt → paket yazar; kural yalnız `status` (başka alan etkilenmez); `update_case`
   imzası dolu.
5. Rapor: lokalde 04.09 paketi + kesim 30.07 ile koşu → `status_korunan` sayısı (lokal kopyada kullanıcı değişikliği
   yoksa 0 beklenir; sentetik testle kanıt).

## Kabul kriterleri

- [ ] Kesim tarihi üç kaynaktan sırayla okunur; hiçbiri yoksa kural kapalı + WARNING (test).
- [ ] Kesim sonrası kullanıcı `status` değişikliği paketle ezilmez, rapor + sayaç (test); aktarım/kesim öncesi kayıtta paket yazar.
- [ ] `update_case` tarihçe satırı `changed_by`/`source` dolu (test).
- [ ] Defter/migrasyon değişmedi; ruff + mypy temiz; pytest özet satırı.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-08, gece işçisi)

### Yapılanlar

1. **Kesim tarihi kaynağı** (`services/teslim_kutusu.py`): `kesim_tarihi_oku(ws)` — `onceki_teslim_adi_oku`
   ile aynı arama deseni; ikisi ortak `_ozet_etiket_degeri` ayrıştırıcısına alındı (G107 sözleşmesi test ile
   korunuyor). Değer `hukdok_aktarim._tarih` ile çözülür (dd.mm.yyyy / yyyy-mm-dd / tarih hücresi); bozuk
   değer paketi reddetmez → None + WARNING. `kesim_tarihi_bul(yol, dosya_adi)` üç kaynağı sırayla dener:
   özet sayfası (INFO) → paket adındaki ISO tarih (`_PAKET_ADI_TARIHI`, WARNING: teslim günü kesim sayıldı)
   → None. `_aktarimi_calistir` bunu `aktarimi_kos(kesim_tarihi=...)` olarak geçer — **defter kolonu YOK**,
   değer koşuya parametredir (görev tanımı).
2. **Koruma kuralı** (`scripts/hukdok_aktarim.py`): `kesim_sonrasi_kullanici_kaydi(db, case_id, kesim)` —
   `_is_aktarim_kaydi`nin ters yönlü ikizi (`startswith(..., autoescape=True)`; `source IS NULL` de kullanıcı
   sayılır — dünkü elle yolun imzasız hâli). `_kart_alanlarini_yaz` yalnız `status`ta ve paket değeri farklıysa
   bakar; kayıt varsa yazmaz, `korunanlar`a `("status", "status korundu (kullanıcı dd.mm.yyyy)")` düşer.
   `_satiri_isle` bunu `sonuc.status_korunan` sayacına ve satır raporuna **`KORUNDU`** türüyle yazar
   (HATA değil: çıkış kodu ve teslim kapısının hata oranı etkilenmez; sessizce de yutulmaz — her koşuda
   yeniden sayılır, belgeli aşama gibi). `aktarimi_kos(kesim_tarihi=None)` → kural kapalı, TEK WARNING.
   `ozet_metni`ne "status korunan" satırı; CLI `--kesim-tarihi` (verilmezse teslim hattıyla AYNI arayıcı,
   `_cli_kesim_tarihi`; bozuk açık değer `AktarimHatasi` → `CIKIS_GIRDI`).
3. **Elle yol imzası** (`managers/case_manager.py`): `PANEL_SOURCE = "panel"`. `update_case(..., *, changed_by=None)`
   tarihçeyi `changed_by=<kullanıcı> or "panel"`, `source="panel"` ile yazar (dün ikisi NULL). `update_case_tracking`
   `status` değişince aynı imzayla `case_history` yazar (yalnız `status` — G073 "takip paneli tarihçe yazmaz"
   kilidi öteki alanlar için aynen duruyor, testle yeniden kilitlendi).

### Kararlar + gerekçeleri

- **Eşik = kesim GÜNÜNÜN başı (TR 00:00)**, "changed_at > kesim_tarihi" değil: ekibin fotoğrafı günün hangi
  saatinde alındı bilinmez; o gün yapılan kullanıcı değişikliği de korunur (koruyucu taraf). Test: 30.07 öğlen kaydı korunur.
- **`source IS NULL` kullanıcı sayılır**: bugüne kadarki panel değişiklikleri imzasız; onları aktarım sanmak
  kuralı prod'daki gerçek kayıtlar için boşa çıkarırdı. SQL'de `NOT LIKE` NULL'u dışladığı için `or_(is_(None), ~startswith)`.
- **`KORUNDU` ayrı tür**: G104'ün alan-düzeyi atlamaları `HATA` türündedir; korunan status hata değildir
  (kapı `hata_orani`nı ve `cikis_kodu`nu şişirmemeli). `hatalar` filtresi `tur == "HATA"` olduğu için otomatik dışarıda.
- **`changed_by` yedeği "panel"**: `routes/cases.py:299` `update_case`e kullanıcıyı geçirmiyor ve routes/** kapsam
  DIŞI. Kural ayrımı `source`tan yapıldığı için doğruluğu etkilemez; `changed_by`nin NULL kalmaması için yedek.
  İzlenecek: route'a tek satır `changed_by=user.get("name") or user.get("preferred_username")`
  (`api_update_case_tracking`in deseni) — ayrı görev.
- **Ruff F811 için `# noqa` KULLANILMADI** (G076/G151 kullanmıştı; işçi sözleşmesi susturma yasak): fixture'lar
  `g064.db_env` nitelik atamasıyla takıldı — pytest için aynı fixture, ruff için yeniden tanım değil.
- Kuru koşu / uygulama yolunda `kesim_tarihi_bul` paketi ikinci kez açar (yapı doğrulaması ayrı aşamada, ayrı
  oturumda). Bir openpyxl read_only açılışı; deftere kolon açmaktan (görev: YOK) ucuz.

### Test sonuçları

- `backend/tests/test_g152_status_koruma.py` (YENİ, 30 test): kesim tarihi ayrıştırma (8 param + bozuk değer WARNING),
  `onceki_teslim_adi_oku` regresyon, `kesim_tarihi_bul` üç kaynak, CLI çözümü; kural: kesim sonrası imzasız kayıt
  korunur + rapor + sayaç + özet satırı, kesim günü korunur, kesim öncesi paket yazar, aktarım imzalı paket yazar,
  LIKE jokeri (`HUKDOKxTESLIM`) kanmaz, kural kapalı + TEK WARNING, aynı değer sayılmaz, ikinci koşu (sayaç 1/1,
  tarihçe büyümez), kuru koşu, teslim hattı parametreyi geçer (monkeypatch), sözleşme; `update_case` imzası
  (kullanıcılı / yedekli), `update_case_tracking` status imzası + öteki alan tarihçesiz, uçtan uca panel → paket.
- Kırmızı-yeşil: eski kodda `aktarimi_kos(kesim_tarihi=...)` TypeError, `update_case` tarihçesi NULL, `kesim_tarihi_oku` yok.
- Konteynerde: **pytest 2899 passed, 3 skipped** (önce 2869) · **ruff temiz** · **mypy temiz (61 dosya)**.
- Mevcut test DEĞİŞTİRİLMEDİ (`test_cases_update.py`de tarihçe beklentisi yoktu — izin kullanılmadı).

### Lokal koşu (görev madde 5)

04.09 paketi (`HUKDOK_TESLIM_PAKETI_2026-09-04.xlsx`, masaüstü) lokal DB'de kuru koşu, `--kesim-tarihi 30.07.2026`:
8409 okunan / 8386 işlenen / alan değişikliği 0 / **status korunan 0** / envanter DENK (paket 05.09'da lokale
gerçekten yazılmıştı; `alan değişikliği 0` ile tutarlı). Beklenen 0'ın kaynağı DB'den doğrulandı: kesim sonrası
`status` tarihçesinde kullanıcı imzalı satır **0**, aktarım imzalı 149 (`HUKDOK_TESLIM_tam_teslim.xlsx`).
Paketin `DEGISIKLIK_OZETI`'nde "Veri kesim tarihi" satırı YOK → arayıcı paket adından 2026-09-04'ü WARNING ile aldı
(tasarlandığı gibi). Kanıt sentetik testlerde.

### İzlenecekler

- Veri ekibinden `DEGISIKLIK_OZETI`'ne **"Veri kesim tarihi: dd.mm.yyyy"** satırı istenir (SOZLESME.md güncellemesi
  plan #13 docs görevinde; bu görevin kapsamında değildi). Satır gelmezse paket adındaki teslim günü kesim sayılır —
  gerçek kesimden GEÇ bir eşik: 30.07–04.09 arası kullanıcı değişiklikleri bu durumda korunmaz.
- `routes/cases.py` `api_update_case` → `update_case(..., changed_by=<kullanıcı>)` tek satır (kapsam dışıydı).
- NOT (kapsam dışı, düzeltilmedi): `.claude/settings.local.json` (M) ve `.claude/launch.json` (??) oturum başında
  kirliydi — harness dosyaları, önceki görevlerdeki (G035/G037/G050…) gibi dokunulmadı, commit'e girmedi.
