# G191 — Bağlantı ve sunucu ayarları: idle-in-transaction / lock timeout, Postgres compose parametreleri, pg_stat_statements

- **Bant:** backend
- **Bağımlı:** G189, G194
- **Dosya kapsamı:** `backend/database.py` (`_build_connect_args` + hata-toleranslı `CREATE EXTENSION IF NOT EXISTS pg_stat_statements`), `backend/migrate.py` (env muafiyeti), `docker-compose.yml` (postgres `command:`), `.env.example`, `backend/tests/test_faz*_connect_args*.py` ya da yeni `test_g191_baglanti_ayarlari.py`, `backend/tests/test_faz3_e_hardening.py` (YALNIZ satır 129 bekçi metni), `docs/mimari/deploy-ve-altyapi.md`
- **Dokunma:** `deploy.sh`, `infra/**` (değişmez; yalnız doküman notu), `backend/managers/case_manager.py`, `backend/tests/test_faz3_e_hardening.py` satır 109-122 (iki birim test değişmez)

> **Kullanıcı kararı 14.09 (BLOKE sonrası):**
> 1. **Test değiştirme izni VAR — dar kapsamlı:** `test_faz3_e_hardening.py:129` bekçisi yeni çağrı metnine
>    güncellenir: `"connect_args=_build_connect_args(DB_STATEMENT_TIMEOUT_MS, idle_ms=DB_IDLE_TX_TIMEOUT_MS, lock_ms=DB_LOCK_TIMEOUT_MS)"`.
>    Bekçinin amacı (motor sabitleri connect_args'tan alır) korunur. `idle_ms`/`lock_ms` varsayılanı `0` olur → satır
>    109-122'deki iki birim test DEĞİŞMEDEN yeşil kalır; o testlere dokunmak gerekirse yine BLOKE.
> 2. **`services/teslim_cevap.py:487-545` uzun transaction riski önce G194 ile kapatılır** — bu görev G194'e bağımlıdır.

Kaynak: `docs/arsiv/performans-denetimi-2026-09-14.md` D5, D6 (kanıt: bağlantı options'ta yalnız
`statement_timeout=30000`; sunucuda `idle_in_transaction_session_timeout=0`, `lock_timeout=0`;
compose'da Postgres conf yok → `shared_buffers=128MB`, `effective_cache_size=4GB` (mem_limit
512m ile tutarsız), `random_page_cost=4`, `pg_stat_statements` yok).

## Hedef
1. **D5** `_build_connect_args`: `-c idle_in_transaction_session_timeout=<ms> -c lock_timeout=<ms>`
   eklenir; env `DB_IDLE_TX_TIMEOUT_MS` (varsayılan 60000) ve `DB_LOCK_TIMEOUT_MS` (varsayılan
   5000); `0` = kapalı. `migrate.py` `DB_STATEMENT_TIMEOUT_MS=0` desenini iki yeni env için de
   uygular (backfill UPDATE'ler kilit bekleyebilir). Gece aktarım script'i (`hukdok_aktarim`,
   tek transaction ~93 sn, D13) idle DEĞİL aktif olduğu için etkilenmez — testle kanıtla
   (uzun süren tek ifade idle sayılmaz).
2. **D6** compose `postgres.command:` → `postgres -c effective_cache_size=384MB -c
   random_page_cost=1.1 -c shared_preload_libraries=pg_stat_statements -c track_io_timing=on`;
   `shared_buffers`/`work_mem`/`max_connections` varsayılanda kalır (havuz 2×30=60 < 100).
   `.env.example`'a yeni env'ler; deploy notu: `.env`/compose değişikliği `restart` ile gelmez,
   `docker compose up -d` recreate gerekir (bilinen tuzak).
3. `pg_stat_statements` extension'ı `database.py`'de `pg_trgm` ile aynı hata-toleranslı blokta
   `CREATE EXTENSION IF NOT EXISTS` (preload yoksa hata → WARNING, uygulama ayakta).

## Kabul kriterleri
- [x] Birim test: `_build_connect_args(30000, idle_ms=60000, lock_ms=5000)` → options `-c statement_timeout=30000 -c idle_in_transaction_session_timeout=60000 -c lock_timeout=5000`; `0` verilen parametre options'ta YOK.
- [x] `dbtest`: bağlantıda `SHOW idle_in_transaction_session_timeout` = env değeri; `migrate.py` yolundan açılan bağlantıda (env 0) = 0.
- [x] `dbtest`: iki bağlantıyla kilit senaryosu — biri satırı kilitler, diğeri `lock_timeout=200ms` ile `UPDATE` dener → `QueryCanceled`/`LockNotAvailable` 1 sn içinde döner (havuz rehin alınmaz).
- [x] Lokal stack recreate sonrası `docker compose exec -T postgres psql -U hukudok_user -d hukudok -c "SHOW effective_cache_size; SHOW shared_preload_libraries;"` beklenen değerleri verir; `SELECT count(*) FROM pg_stat_statements` çalışır (rapora).
- [x] `docs/mimari/deploy-ve-altyapi.md`: yeni env'ler, compose parametreleri, "prod'da `up -d` recreate şart" ve pg_stat_statements'ın en pahalı 10 sorgu için nasıl okunacağı (tek sorgu örneği).
- [x] Bellek: `effective_cache_size` yalnız planlayıcı ipucu, RSS artırmaz — doküman bunu ve 512m limitle ilişkisini açıklar (2026-07-29 OOM dersleri).

## Doğrulama
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- `docker compose up -d` (recreate) → `SHOW` çıktıları rapora; `/healthz` 200

## Rapor

### İkinci koşu — DURUM: TAMAM (2026-09-14, işçi oturumu, kullanıcı kararı sonrası; G194 bf62085 main'de)

#### Yapılanlar
- **D5 `backend/database.py`:** `DB_IDLE_TX_TIMEOUT_MS` (varsayılan 60000) ve `DB_LOCK_TIMEOUT_MS`
  (varsayılan 5000) env'leri eklendi. İmza: `_build_connect_args(statement_timeout_ms, idle_ms=0, lock_ms=0)`.
  Seçenek sırası sabit (statement → idle → lock), `0`/negatif değer options'a girmez, hepsi 0 ise
  `options` anahtarı yok. Motor çağrısı kullanıcı kararındaki metinle birebir:
  `connect_args=_build_connect_args(DB_STATEMENT_TIMEOUT_MS, idle_ms=DB_IDLE_TX_TIMEOUT_MS, lock_ms=DB_LOCK_TIMEOUT_MS)`.
- **`backend/migrate.py`:** `DB_STATEMENT_TIMEOUT_MS=0` desenine `DB_IDLE_TX_TIMEOUT_MS=0` ve
  `DB_LOCK_TIMEOUT_MS=0` eklendi.
- **pg_stat_statements:** `database.py::_ensure_pg_stat_statements(conn)` yeni yardımcı.
  `check_and_migrate_tables` içinde pg_trgm bloğunun hemen ardından çağrılır. Adımlar: `CREATE
  EXTENSION IF NOT EXISTS`, ardından görünümü okuyarak yoklama. Yoklama şart, çünkü uzantı preload
  olmadan da yaratılabilir. Hata → rollback + TEK WARNING + `False`, uygulama ayakta.
- **D6 `docker-compose.yml`:** `postgres.command: postgres -c effective_cache_size=384MB -c
  random_page_cost=1.1 -c shared_preload_libraries=pg_stat_statements -c track_io_timing=on`,
  gerekçe yorumuyla. `shared_buffers`/`work_mem`/`max_connections` varsayılanda.
- **`.env.example`:** "Veritabani baglanti sinirlari" bölümü (üç `DB_*` env'i, varsayılanlarıyla yorumlu).
- **`backend/tests/test_faz3_e_hardening.py`:** YALNIZ satır 129 bekçi metni, kullanıcı kararındaki
  çağrı metnine güncellendi. Satır 109-122'deki iki birim test değişmedi ve yeşil (varsayılanlar 0).
- **`docs/mimari/deploy-ve-altyapi.md` §13:** env tablosu, migrate muafiyeti, idle ile uzun tek
  ifade farkı, script'ler için `-e DB_LOCK_TIMEOUT_MS=0` yolu, compose parametre tablosu, bellek/512m
  açıklaması, `up -d` recreate şartı, en pahalı 10 sorgu örneği. Başlığa doğrulama satırı eklendi.

#### Yeni testler — `backend/tests/test_g191_baglanti_ayarlari.py` (16 test, hepsi eski kodda kırmızı)
- Birim: üç seçenekli options; 4 parametrik "0 verilen seçenek yok"; hepsi 0 → `{"connect_timeout": 5}`;
  kaynakta env varsayılanları; migrate'in üç env'i 0'laması; `_ensure_pg_stat_statements` preload yok
  → WARNING×1 + False + rollback; başarılı yol; migrasyonda çağrı bekçisi.
- dbtest (gerçek Postgres, scratch DB'ler `test_migration_path._scratch_database` ile):
  * Ayrı süreçte `import database`, env 25000/45678/3456 → `pg_settings` aynı değerler. Aynı env ile
    önce `import migrate` → üçü `"0"` (migrate'in env'i ezdiği görülür).
  * Kilit: A satırı `FOR UPDATE` tutar. B (`lock_ms=200`, tek yuvalı havuz, `pool_timeout=1`)
    `UPDATE` dener → `OperationalError`, `pgcode == "55P03"`, < 1 sn. Ardından aynı havuzdan
    `SELECT 1` hemen alınır. Kilit kalkınca B yazabilir (v=1).
  * Idle (`idle_ms=300`): 1 sn'lik tek `pg_sleep` + ikinci ifade + commit başarılı (aktif ifade
    idle sayılmaz; gece aktarımı kanıtı). Transaction açıkken 1,5 sn ifadesiz bekleme → `OperationalError`.
  * pg_stat_statements: dönüş değeri `SHOW shared_preload_libraries` ile tutarlı. Preload'lu
    sunucuda `count(*)` okunur. Her iki yolda bağlantı kullanılabilir kalır. Skip'e düşmez, iki
    ortamda da deterministik.
- Compose/.env.example için pytest testi YAZILMADI: konteynerde repo kökü mount'lu değil, yeni
  `skipif` işaretlemek yasak. Canlı `SHOW` ile doğrulandı (aşağıda).

#### Doğrulama (konteynerde)
- `python -m pytest` → **3517 passed, 3 skipped** (193 sn). Hedefli koşu (g191 + faz3_e +
  migration_path + index_envanteri) → 92 passed, 0 skip (dbtest'ler gerçekten koştu).
- `python -m mypy` → Success: no issues found in 62 source files.
- `python -m ruff check .` → **6 hata, hepsi `calibration-data/_g179/ek3_cevapla.py`** (B905, E702×5).
  Bu dosya git'te yok (`git ls-files backend/calibration-data` = 0); konteynere mount'lu kalibrasyon
  verisi dizini, bu görevle ilgisiz. `ruff check . --exclude calibration-data` → All checks passed!
- `docker compose up -d` → yalnız `hukudok-postgres` recreate oldu. `docker compose restart backend`
  ile entrypoint'in migrate.py'si yeniden koştu. Log: `"pg_stat_statements hazır"` →
  `"✅ Database ready!"`. `/healthz` → 200.
- Recreate öncesi: `effective_cache_size=4GB`, `shared_preload_libraries=` (boş),
  `random_page_cost=4`, `track_io_timing=off`, `idle_in_transaction_session_timeout=0`, `lock_timeout=0`.
- Recreate sonrası `SHOW`: `effective_cache_size=384MB`, `shared_preload_libraries=pg_stat_statements`,
  `random_page_cost=1.1`, `track_io_timing=on`, `shared_buffers=128MB`, `max_connections=100`.
- `pg_extension`: pg_trgm 1.6, pg_stat_statements 1.10. `SELECT count(*) FROM pg_stat_statements` = 164.
  Dokümandaki en pahalı 10 sorgu örneği koşuldu ve satır döndü.
- Bellek: `docker stats` → hukudok-postgres 53.04 MiB / 512 MiB (recreate sonrası, lokal).

#### Kararlar ve gerekçeler
- `_ensure_pg_stat_statements` ayrı fonksiyon oldu: pg_trgm'le aynı hata-toleranslı bölgede, ama kendi
  try'ı var (pg_trgm'in hatası uzantı adımını atlatmasın). Birim testlenebilirlik de bunu gerektirdi.
  Log düzeyi WARNING (gözlem aracı, nihai uygulama hatası değil — log sözleşmesi). pg_trgm'in mevcut
  `logger.error`'ına dokunulmadı.
- `pg_shmem_allocations` pg_stat_statements için yalnız başlık boyutunu (2944 + 128 bayt) gösteriyor.
  Hash girdilerinin gerçek boyutu bu görünümden okunamadığı için dokümana MB iddiası yazılmadı; yalnız
  `pg_stat_statements.max=5000` ve ölçülen konteyner belleği yazıldı.

#### İzlenecekler (prod — insan adımı)
- Deploy `up -d` postgres'i recreate eder (birkaç saniyelik DB kesintisi). Uzantı sonraki backend
  açılışındaki migrasyonda kurulur. Deploy sonrası §13'teki `SHOW` kontrolü koşulmalı.
- `lock_timeout=5 sn` davranış değişikliği: önceden kilitli satırın arkasındaki istek 30 sn'ye kadar
  bekleyip statement_timeout ile düşüyordu, artık 5 sn'de `LockNotAvailable` alır. 04:00 TR gece aktarımı
  (engine'den bağlanır, `_statement_timeout_yukselt` yalnız statement_timeout'u yükseltir) bir kilide
  5 sn'den uzun takılırsa koşu düşer. Gece trafiği düşük olduğu için risk düşük, ama ilk gece turları
  loglarında `lock timeout` aranmalı.
- NOT (kapsam dışı, düzeltilmedi): `scripts/hukdok_aktarim.py` idle/lock sınırlarını koşu için
  yükseltmiyor. Gerekirse `_statement_timeout_yukselt` benzeri bir `lock_timeout` ayarı ayrı görev olabilir.
- Çalışma ağacında görev dışı kirli dosyalar vardı (`.claude/settings.local.json` M,
  `.claude/launch.json` ??). Koşucu yapılandırması; dokunulmadı, commit'e girmedi.

### Gece kaydı — DURUM: BLOKE — test değiştirmek gerekti (2026-09-14, işçi oturumu; kod yazılmadı, commit yok)

#### Sebep

Hedef 1 (D5) motorun bağlantılarına `idle_in_transaction_session_timeout` + `lock_timeout`
göndermeyi, kabul kriteri 1 de `_build_connect_args(30000, idle_ms=60000, lock_ms=5000)`
imzasını istiyor. Mevcut üç test bunu birlikte mekanik olarak KİLİTLİYOR
(`backend/tests/test_faz3_e_hardening.py`, bu oturumda koşuldu: 4 passed, hepsi yeşil):

- `test_engine_created_with_timeouts` (satır 125-129): kaynak metinde
  `"connect_args=_build_connect_args(DB_STATEMENT_TIMEOUT_MS)" in src` — create_engine
  çağrısının METNİNİ kapanış parantezine kadar sabitliyor. `idle_ms=`/`lock_ms=` eklenen her
  çağrı bu alt dizgiyi bozar.
- `test_build_connect_args_with_statement_timeout` (109-114): tek argümanlı
  `_build_connect_args(30000)` → `options == "-c statement_timeout=30000"` (tam eşitlik).
- `test_build_connect_args_zero_disables_statement_timeout` (117-122):
  `_build_connect_args(0) == {"connect_timeout": 5}` (tam eşitlik).

Çelişki: motor çağrısı metni `_build_connect_args(DB_STATEMENT_TIMEOUT_MS)` olarak KALMALI
(bekçi) ve aynı tek argümanlı çağrı yalnız `statement_timeout` üretmeli (iki birim test);
ama hedef, motorun bu çağrıdan idle/lock da almasını istiyor. Aynı ifadeden iki farklı çıktı
beklenemez. Kilidi korumanın tek yolu idle/lock'u motora başka bir kanaldan (ör. SQLAlchemy
`do_connect` olayıyla `cparams["options"]`'a ekleme) sokmaktı — bu, bir kaynak-metin
bekçisini atlatmak için yapılandırmayı create_engine'den gizlemek demek (sahte yeşilin ters
yüzü); denenmedi. `PGOPTIONS`/URL `?options=` da `connect_args["options"]` verildiğinde
yok sayılır/ezilir — uygulanabilir değil.

Diğer kalemler (D6 compose `command:`, `pg_stat_statements` extension bloğu, `.env.example`,
doküman) tek başına yapılabilirdi; ama görev tek iş = tek commit ve kabul kriterleri
bütün hâlinde — yarım görev commit'lenmedi.

#### Başka kilit taraması (kod yazılmadan, okuyarak)

- `test_faz3_e_hardening.py::test_migrate_sets_statement_timeout_exemption` yalnız
  `DB_STATEMENT_TIMEOUT_MS == "0"` bakar — `migrate.py`'ye iki yeni env eklemek onu bozmaz.
- `test_faz2_logging.py::test_compose_sets_json_log_format` compose'da yalnız
  `LOG_FORMAT=json` arar — postgres `command:` eklemek engel değil.
- `test_g147_teslim_sharepoint_kimligi.py:612-616` `.env.example`'da yalnız
  `TESLIM_SHAREPOINT_*` varlığına bakar — yeni `DB_*` satırları engel değil.
- `test_perf_olcum.py:133` ayar adlarını ölçüm listesinde arar, değerlere değil — engel yok.

#### Sabah kararı için öneri (insan)

1. `test_faz3_e_hardening.py:129` bekçisini yeni çağrıya güncelle:
   `"connect_args=_build_connect_args(DB_STATEMENT_TIMEOUT_MS, idle_ms=DB_IDLE_TX_TIMEOUT_MS, lock_ms=DB_LOCK_TIMEOUT_MS)"`
   (bekçinin amacı — motor sabitleri connect_args'tan alır — korunur, gevşetme değil).
   Satır 109-122'deki iki birim test DEĞİŞMEDEN kalabilir: yeni parametrelerin varsayılanı 0
   olursa tek argümanlı çağrı bugünkü çıktıyı verir.
2. Görev dosyasının "Dosya kapsamı"na `backend/tests/test_faz3_e_hardening.py (yalnız satır
   129 bekçi metni)` satırını ekle; sonra G191'i kuyruğa geri koy.

#### NOT (kapsam dışı bulgu — düzeltilmedi)

`idle_in_transaction_session_timeout=60 s` açıldığında AÇIK transaction içinde uzun DB-dışı
iş yapan yollar yeni bir hata moduna girer (sunucu oturumu keser, sonraki commit
`OperationalError`). Taranan çekirdek yollar oturumu uzun işten ÖNCE kapatıyor
(`upload_queue._attempt_upload`, `conversion_retry._process_one`, `case_intake` merge
bağlamı, `routes/reports.py` asistan akışı; aktarımda `xlsx_oku` transaction'dan önce).
İstisna: `services/teslim_cevap.py:487-545` — `eslesme_csv_uret(db=session)` sorgusundan
sonra SharePoint yükleme döngüsü aynı açık transaction'da koşar, `session.commit()` en
sonda. Yüklemeler (uploader iç retry'ı dahil) toplamda 60 sn'yi aşarsa commit düşer,
`cevap_yuklendi` yazılmaz, ertesi gece yeniden dener (dosyalar üzerine yazılır — zararsız
ama gürültülü). G191 yeniden koşulmadan önce ya bu yol yükleme öncesi `session.commit()`
alacak şekilde düzeltilmeli ya da risk bilinçli kabul edilmeli.
