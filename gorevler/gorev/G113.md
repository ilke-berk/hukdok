# G113 — Silinen_Föyler / Kapsam_Dışı sayfaları: föy kapsam işareti

- **Bant:** backend
- **Bağımlı:** G112
- **Dosya kapsamı:** backend/models.py (`CaseFoy`), backend/database.py (migration),
  backend/scripts/hukdok_aktarim.py, backend/managers/case_manager.py (kart çıktısına föy
  işareti), backend/schemas.py, backend/tests/test_g113_kapsam_disi_foy.py (YENİ),
  backend/tests/test_g063_case_foys.py (YALNIZ kolon kümesi kilidi — 03.09 sabahı
  kullanıcı kararıyla ÖNCEDEN güncellendi; HEAD'de bu test kolonlar gelene kadar KIRMIZI,
  G113'ün ilk yeşili onu kapatır)
- **Dokunma:** backend/routes/cases.py (serializer üzerinden kendiliğinden gelir; route'a
  dokunma), backend/services/case_relations_auto.py (ilişki hesabına kapsam dışı föy
  KATILMAMALI — yalnız filtre ekle, algoritmaya dokunma)

## Hedef

Plan §4. Veri ekibi kapsamdan çıkardığı föyleri (`Silinen_Föyler`: mükerrer/hatalı açılış;
`Kapsam_Dışı`: malpraktis dışı) gerekçe + tarihle ayrı sayfalarda gönderiyor (54 sütun + "…
Gerekçesi" + "Tarih"). Bugün bu föyler bizde `case_foys`'ta canlı görünüyor ve TKU ilişki
katmanına giriyor. Föy **silinmez** (kart ve belgeler dokunulmaz — belge koruma şartı), yalnız
işaretlenir; kart panelinde "kapsam dışı föy" bilgisi görünür.

## SÖZLEŞME

- `case_foys.kapsam_durumu` VARCHAR(20) NULL: `SILINDI` | `KAPSAM_DISI` (NULL = kapsamda).
- `case_foys.kapsam_gerekcesi` VARCHAR NULL, `case_foys.kapsam_tarihi` DATE NULL.
- Migration `("columns", "case_foys", {...})`; index gerekmiyor (G041 kuralı: gerekirse ayrı
  `("index", …)`).
- Aktarım: iki sayfa okunur (`SistemNo`, "Silinme Gerekçesi" / "Kapsam Dışı Gerekçesi",
  "Tarih"); SistemNo `case_foys`'ta varsa üç alan yazılır (idempotent: aynı değer → 0
  değişiklik; `case_history` satırı kart düzeyinde "föy kapsam dışı: <gerekçe>"). Bizde
  olmayan SistemNo → ATLANDI (rapor). Sayfa yoksa hata değil.
- **Geri dönüş:** föy `Sheet`'te yeniden görünürse (aynı SistemNo ana sayfada) işaret
  NULL'a çekilir + tarihçe ("kapsama geri alındı"). Sayfa AD listesi tek kaynak: `Sheet`.
- Kart alanı yazımında kapsam dışı föy **kardeş föy uzlaşısına KATILMAZ** (D9): `kart_alan_celiskileri`
  ve künye çelişki toplayıcısı bu föyleri atlar — kapsam dışı föyün eski değeri kartı
  "çelişkili" göstermesin.
- `case_relations_auto`: `kapsam_durumu IS NULL` filtresi; kapsam dışı föy TKU grubuna
  ilişki üretmez.
- Kart çıktısı (`get_case`): `foyler[]` listesindeki her föye `kapsam_durumu`,
  `kapsam_gerekcesi`, `kapsam_tarihi` eklenir (frontend rozeti sonraki tur; bu görevde UI yok).

## Kabul kriterleri

- [ ] Migration: üç kolon geliyor, ikinci koşu idempotent (3-ortam kuralı).
- [ ] Aktarım: sentetik pakette `Silinen_Föyler`'deki SistemNo işaretleniyor (gerekçe + tarih),
      kart ve belge sayıları DEĞİŞMİYOR (belge envanteri denk); ikinci koşu 0 değişiklik.
- [ ] Geri dönüş: föy sonraki pakette `Sheet`'e dönünce işaret NULL + tarihçe.
- [ ] Kapsam dışı föy çelişki raporuna girmiyor (iki föylü kart, biri kapsam dışı, künye
      farklı → çelişki 0).
- [ ] `case_relations_auto` kapsam dışı föy için ilişki üretmiyor (mevcut ilişki testleri
      deseniyle).
- [ ] `get_case` föy listesinde üç alan var; kapsamda föyde NULL.
- [ ] Docstring "YAZILAN kart alanları" bölümü + `KART_ALANLARI` kilidi korunarak güncellendi.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM — 2026-09-03 (işçi oturumu, tur 1 yeşil)

### Yapılanlar

- `backend/models.py` — `CaseFoy`'a `kapsam_durumu` VARCHAR(20) / `kapsam_gerekcesi`
  VARCHAR / `kapsam_tarihi` DATE (üçü NULL, DEFAULT yok); docstring'e kapsam işareti
  maddesi. `test_g063_case_foys.py::test_model_ve_kolonlar_gorev_taslagina_uygun`
  (03.09 sabahı güncellenen kilit) kendiliğinden yeşile döndü — test DEĞİŞTİRİLMEDİ.
- `backend/database.py` — madde 40: `("columns", "case_foys", {...})` op'u (index yok,
  CHECK yok; gerekçe op yorumunda). Lokal Postgres'te `python migrate.py` iki kez
  koşuldu: ilk koşu "Added kapsam_durumu/kapsam_gerekcesi/kapsam_tarihi to case_foys",
  ikinci koşu hiçbir kolon eklemedi (idempotent).
- `backend/scripts/hukdok_aktarim.py` — kapsam katmanı: `KAPSAM_SAYFALARI`
  (`Silinen_Föyler`→`SILINDI`, `Kapsam_Dışı`→`KAPSAM_DISI`), `kapsam_kayitlarini_oku`
  (sayfa adı ve gerekçe başlığı aksan/alt çizgi toleranslı; sayfa yoksa boş; SistemNo
  sütunu yoksa `AktarimHatasi`; çözümlenemeyen tarih föyü düşürmez → None + WARNING;
  mükerrer SistemNo'da ilk kayıt kazanır), `kapsam_isaretlerini_yaz` (işaretleme
  idempotent — aynı üçlü → ne UPDATE ne tarihçe; bizde olmayan → ATLANDI rapor satırı,
  koşu kırmızı değil; geri dönüş: ana sayfada BAŞARIYLA işlenen ve paketin kapsam
  sayfalarında olmayan işaretli föy NULL'a çekilir, tarihçe "kapsama geri alındı").
  Tarihçe kart düzeyinde: `case_history.field_name = "case_foys.kapsam_durumu"`,
  `new_value = "<SistemNo>: <durum> — föy kapsam dışı: <gerekçe>"`. D9: paketin kapsam
  sayfasındaki SistemNo `kart_alan_celiskileri`'ne ve künye toplayıcısına GİRMEZ;
  `_satiri_isle` bu satır için yalnız föy kimliğini yazar (kart alanı/avukat/taraf
  atlanır) — uzlaşıya girmeyen föyün kartı yazması salınımı geri getirirdi; aşama
  satırları da süzülür. `AktarimSonucu`'na `kapsam_isaretlenen/geri_alinan/atlanan`,
  özet metnine "kapsam işareti" satırı. Docstring "YAZILAN kart alanları" bölümüne
  "Kapsam sayfaları (G113)" alt bölümü; `KART_ALANLARI` kilidine dokunulmadı.
- `backend/services/case_relations_auto.py` — YALNIZ filtre: `_tku_kumesi` ve
  `_tku_eslesmeleri` föy sorgularına `CaseFoy.kapsam_durumu IS NULL`; algoritma ve
  legacy `cases.tku_no` okuması aynen. ("Dokunma" satırındaki "yalnız filtre ekle"
  izni bu şekilde yorumlandı; sözleşme ve kabul kriteri filtreyi açıkça istiyor.)
- `backend/managers/case_manager.py` — `get_case` çıktısına `foyler[]`
  (`_foy_row_dict`: id, sistem_no, tku_no, hasar_no, source, case_party_id,
  kapsam_durumu, kapsam_gerekcesi, kapsam_tarihi ISO). Föyler `joinedload` ile kart
  sorgusuna LEFT JOIN'lenir — kartın sorgu sayısı 6'da kaldı (G051 kilidi
  `test_kart_iliski_basina_tek_sorgu_kosuyor` yeşil; selectinload 7. sorgu açardı).
  `routes/cases.py`'a dokunulmadı (route dict'i olduğu gibi döner).
- `backend/schemas.py` — `CaseRead.foyler: List[Dict[str, Any]] = []` (belgeleme).
- `backend/tests/test_g113_kapsam_disi_foy.py` (YENİ, 22 test): şema (op + model +
  index yokluğu), okuyucu (6), aktarım sqlite (8: işaretleme + belge envanteri +
  ikinci koşu 0, Kapsam_Dışı, ATLANDI, geri dönüş + tekrar 0, düşen satır adına geri
  dönüş yok, D9 çelişki 0 + kontrol paketi çelişki 2, aşama süzgeci, kuru koşu),
  ilişki katmanı (3: hedef/kaynak tarafı + legacy kolon), `get_case` (3), dbtest
  (gerçek Postgres'te üç kolon; 3-ortam SKIP deseni).

### Kararlar + gerekçeler

- **Aynı pakette hem ana sayfada hem kapsam sayfasında olan föy:** kapsam sayfası
  kazanır (gerekçeli, bilinçli liste); ana sayfa satırı yalnız kimlik yazar. Kabul
  kriterindeki "iki föylü kart, biri kapsam dışı, künye farklı → çelişki 0" ancak bu
  yorumla anlamlı — kapsam dışı föy ana sayfada yoksa zaten uzlaşıya girmiyordu.
- **Geri dönüş yalnız BAŞARIYLA işlenen satırlar için:** savepoint'i geri alınan
  satır adına kapsam kararı verilmez (yarım föy ilkesinin kardeşi).
- **`foy_map.py`'a dokunulmadı** (dosya kapsamı dışı): kapsam kolonları
  `hukdok_aktarim.kapsam_isaretlerini_yaz` içinde ORM satırına doğrudan yazılır;
  `upsert_foy` bu kolonlara dokunmaz (`None` = koru sözleşmesi zaten kapsam
  kolonlarını atlıyor). NOT: `case_foys`'un "tek yazma yolu foy_map" ilkesi bu üç
  kolon için script'e genişledi; ileride `foy_map.set_kapsam` çıkarılabilir.
- **Migration idempotency testi sqlite'ta yazılmadı:** `check_and_migrate_tables`
  sqlite'ta `CREATE EXTENSION pg_trgm` ERROR logu üretir; kanıt gerçek Postgres'te
  iki koşu (yukarıda) + dbtest + op türünün koşullu yapısı.

### Test sonuçları

- `docker compose exec -T backend python -m pytest` → **2325 passed, 3 skipped**
  (HEAD'de 1 kırmızı olan `test_g063` dahil; yeni dosya 22 test).
- `ruff check .` → All checks passed. `mypy` → no issues (53 dosya).

### İzlenecekler

- Frontend rozeti ("kapsam dışı föy") sonraki tur: `foyler[]` sözleşmesi hazır.
- NOT (kapsam dışı): `scripts/mukerrer_kart_raporu.py` ve `services/teslim_cevap.py`
  `case_foys`'u kapsam süzgeçsiz okur — mükerrer kart raporu kapsam dışı föyün
  TKU'sunu hâlâ ortaklık sayar; ayrı karar.
- NOT: `foy_map._UPDATABLE` listesi kapsam kolonlarını içermez (bilinçli: aktarımın
  ana sayfa yolu işareti ezmesin).

### Tarihçe — 03.09 sabahı — BLOKE çözüldü (kullanıcı kararı)

Aşağıdaki önerinin iki adımı da uygulandı: `test_g063_case_foys.py:73` beklenen kümeye üç
kolon + uzunluk/nullable iddiaları eklendi (tam eşitlik korundu), dosya yukarıdaki kapsama
alındı. İşçi mevcut testi DEĞİŞTİRMEZ; kolonları sözleşmeye göre açınca test kendiliğinden
yeşile döner.

### Gece kaydı — DURUM: BLOKE — test değiştirmek gerekti (2026-09-03, işçi oturumu; kod yazılmadı, commit yok)

#### Sebep

Sözleşmenin ilk maddesi (`case_foys.kapsam_durumu` / `kapsam_gerekcesi` / `kapsam_tarihi`
kolonları) `models.CaseFoy`'a üç kolon eklemeyi gerektiriyor. Mevcut bir test bunu
mekanik olarak KİLİTLİYOR:

- `backend/tests/test_g063_case_foys.py::test_model_ve_kolonlar_gorev_taslagina_uygun`
  (satır 73-76): `set(columns.keys()) == {"id", "sistem_no", "case_id", "case_party_id",
  "tku_no", "hasar_no", "source", "created_at", "updated_at"}` — TAM eşitlik. Kolon
  eklenince test deterministik kırmızıya düşer; yeşile dönmesinin tek yolu bu beklentiyi
  güncellemek. Test G063'ten (bfffcfa, 19.08) beri hiç değişmedi; yorumu kolon setinin
  "FAZ F tam eşleme turunda kararlaştırılacağını" söylüyor — G113 tam da o karar, ama
  kilidi açma yetkisi işçide değil.

İşçi sözleşmesi (mevcut testleri değiştirme/beklentiyi gevşetme YOK; gerekiyorsa
`test-degistirmek-gerekti` ile dur) ve görevin dosya kapsamı (yalnız `test_g113_...py` YENİ;
`test_g063_case_foys.py` listede yok) birlikte bu görevi işçi oturumunda uygulanamaz kılıyor.
Kolonları modele koymadan (yalnız migration + ham SQL) sözleşmeyi karşılamak `foy_map` /
`get_case` / `case_relations_auto` yollarında ORM'siz erişim demekti — kilidi korumak
için kötü kod yazmak, sahte yeşilin ters yüzü; denenmedi.

### Sabah kararı için öneri (insan)

1. `test_g063_case_foys.py:73` beklenen kümeye üç kolonu ekle (`kapsam_durumu`,
   `kapsam_gerekcesi`, `kapsam_tarihi`) + `kapsam_durumu.type.length == 20` ve üçünün
   `nullable` olduğu iddiaları; yorumdaki "bu turda BİLİNÇLİ açılmadı" cümlesine G113
   notu. Tam eşitlik KORUNUR (gevşetme değil, sözleşmeyle güncelleme).
2. Görev dosyasının "Dosya kapsamı"na `backend/tests/test_g063_case_foys.py (yalnız kolon
   kümesi kilidi)` satırını ekle; sonra G113'ü kuyruğa geri koy.

Başka kilit taraması: `_UPDATABLE` (`foy_map.py`) testlerde doğrudan kilitli değil;
`test_g064`/`test_g104`/`test_g107`-`g112` fixture'ları `case_foys` index op'larını
olduğu gibi uyguluyor (yeni kolon `("columns", ...)` op'unda gelir, `("index", ...)`
gerekmez — G041 kuralı) — ek engel görülmedi.

NOT: çalışma ağacında kapsam dışı kirli dosyalar vardı (`.claude/settings.local.json`,
`.claude/launch.json` — oturum yapılandırması); dokunulmadı.
