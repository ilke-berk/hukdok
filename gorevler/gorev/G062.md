# G062 — case_stage_decisions: aşama/karar tarihçesi tablosu + BELİRSİZ damgası + son-aşama senkronu

- **Bant:** backend
- **Bağımlı:** G060
- **Dosya kapsamı:** backend/models.py, backend/database.py, backend/managers/stage_decisions.py
  (YENİ dosya), backend/schemas.py, backend/tests/test_g062_stage_decisions.py
- **Dokunma:** `case_esas_numbers` / `case_manager.sync_current_esas` (desen kaynağıdır,
  DAVRANIŞI DEĞİŞMEZ); `routes/**` (okuma/yazma uçları bu görevin kapsamı DEĞİL — FAZ F
  aktarımı ve sonrası UI ayrı işler); `frontend/**`.

## Hedef

Aynı yargı aşamasının birden çok kararını kaydedebilen tarihçe tablosu. Gerekçe: bizim
şemada da karar künyesi TEK SLOT (models.py:69-108 — yerel karar_no/karar_tarihi, istinaf 8,
temyiz 9, KD 6 alan); ikinci karar eskisini ezer. Kanıt vakası: id-2271 — Danıştay 2023
Bozma + 2026 Onama, tek slota sığmaz (KARAR_ASAMALARI_TASARIM_PAKETI_2026-08-17.xlsx,
VAKALAR). Alanlar prod kopyasında bugün 0 dolu (18.08 ölçümü) → veri göçü YOK, tablo
sıfırdan doğar. Desen: `case_esas_numbers` (G045/G049) — aşama etiketli kayıtlar + tek
yazma yolu + türetilmiş tek-slot fotoğraf.

## Tablo taslağı (işçi ayrıntıyı koddan doğrulayarak keskinleştirir)

`case_stage_decisions`: id · case_id FK(cases, ondelete=CASCADE — case_esas_numbers ile
aynı gerekçe) · `stage` VARCHAR(20) (etiket seti case_esas_numbers ile AYNI:
YEREL|ISTINAF|TEMYIZ|KARAR_DUZELTME; ONCEKI burada YOK — o yalnız esas kavramı) ·
`sira_no` INT (aynı aşamanın kaçıncı kararı; sıralama BUNUNLA yapılır, tarihle DEĞİL —
tasarım paketi: 170 föyde tarihler güvenilmez) · mahkeme · esas_no · karar_no ·
karar_tarihi · karar_durumu (G060 listelerinden doğrulanır; stage'e göre ilgili liste) ·
teblig_tarihi · basvuran_taraf · aciklama · `dogrulama_durumu` VARCHAR(20) NOT NULL
DEFAULT 'BELIRSIZ' (UYAP|BELGE|TURETILDI|BELIRSIZ — tahmin yasağının taşıyıcısı) ·
`kaynak_id` INT self-FK nullable (bu karar hangi karardan doğdu: bozma → yeni yerel) ·
`source` VARCHAR(100) (provenance imzası) · created_at.

- UNIQUE `(case_id, stage, sira_no)` — **DAİMA `("index", ...)` op'uyla** (G041 kuralı).
- Tek yazma yolu: `managers/stage_decisions.py` (yeni modül — case_manager 1.500+ satır,
  yeni sorumluluk yeni dosyaya). Yazım sonrası SENKRON: her stage'in en yüksek sira_no'lu
  kaydı, cases üzerindeki o aşamanın tek-slot kolonlarına "son aşama fotoğrafı" olarak
  yazılır (`sync_current_esas` deseninin karar ikizi). Fotoğraf kolonları: YEREL →
  karar_no, karar_tarihi, yerel_karar_durumu (G060) · ISTINAF → istinaf_* ailesi ·
  TEMYIZ → temyiz_* ailesi · KARAR_DUZELTME → karar_duzeltme_* ailesi.
  `karar_turu`/`karar_lehine` türetmesi BİLİNÇLİ KAPSAM DIŞI (kaba alan, ayrı karar).

## Kabul kriterleri

- [ ] Aynı davaya aynı aşamadan iki karar yazılabiliyor (id-2271 senaryosu testte:
      TEMYIZ sira 1 Bozma + sira 2 Onama; ikisi de duruyor, fotoğraf = Onama)
- [ ] UNIQUE (case_id, stage, sira_no) her kurulum yolunda doğuyor (migrasyon testi;
      `deploy.sh --gate-only` kapısının koştuğu testlerde görünür)
- [ ] Mükerrer (case_id, stage, sira_no) yazımı UniqueViolation'da düzgün geri dönüyor —
      G049 dersi: geri dönüş yolu GERÇEK kırmızıyla test edilir, yalancı yeşil değil
- [ ] `dogrulama_durumu` verilmezse BELIRSIZ; geçersiz etiket reddediliyor
- [ ] `karar_durumu` değeri stage'in G060 listesinde yoksa yazım reddediliyor (kapalı liste)
- [ ] Senkron: tek-slot fotoğraf her yazımda tazeleniyor; kayıt silinirse (admin düzeltme
      yolu) fotoğraf bir önceki sira_no'ya geri düşüyor
- [ ] Gerçek DB testleri 3-ortam kuralına uyar (to_regclass + SKIP)
- [ ] `docs/mimari/` ilgili bölümüne kısa şerh (tablo + senkron kuralı) — kod+test+doküman tek commit

## Doğrulama

- `docker compose exec -T backend python -m pytest`  (ekstra `-q` EKLEME)
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-08-19 gece, işçi oturumu)

### Yapılanlar

- `models.CaseStageDecision` (`case_stage_decisions`): görev taslağındaki 16 kolon;
  sınırlar cases'teki kardeş slot kolonlarıyla hizalı (mahkeme 200, esas/karar_no 50,
  karar_durumu 100, basvuran_taraf 50) — fotoğraf kopyası hedef kolondan uzun olamaz.
  `dogrulama_durumu` default + **server_default** BELIRSIZ (ham INSERT bile damgasız
  satır bırakamaz; dbtest'te gerçek PG'de doğrulandı). `kaynak_id` self-FK
  ondelete=SET NULL. `Case.stage_decisions` cascade="all, delete-orphan".
- `database._MIGRATIONS` madde 35: `("index", ...)` op'u (G041 kuralı) —
  `uq_case_stage_decision (case_id, stage, sira_no)` + `idx_case_stage_decisions_kaynak`.
  Tablo op'u bilinçli yok (create_all yaratır, madde 32 gerekçesi).
- `managers/stage_decisions.py` (YENİ, tek yazma yolu): `add_stage_decision` /
  `delete_stage_decision` / `get_stage_decisions`. Kapalı kümeler: DECISION_STAGES
  (ONCEKI yok), DOGRULAMA_DURUMLARI, stage→G060 listesi doğrulaması (ada göre,
  tablo içeriğinden). sira_no verilmezse max+1 (yarışta bir kez yeniden hesap);
  verilmişse doluysa `DuplicateStageDecisionError` — SAVEPOINT'li insert, oturum
  bozulmaz. Her yazım/silmeden sonra `_resync_stage_photo`: aşamanın en yüksek
  sira_no'lu satırı slot kolonlara fotoğraf; satır kalmazsa temizler. Commit yok
  (flush) — işlem sınırı çağıranın (sync_current_esas sözleşmesi).
- `schemas.CaseStageDecisionRead` (from_attributes) — uçlar FAZ F/UI işi, şema
  tabloyla birlikte doğdu.
- `docs/mimari/dava-acma-akisi.md` §9: tablo + senkron kuralı şerhi.
- Test: `tests/test_g062_stage_decisions.py` — 32 test, 3 katman (şema kilitleri /
  sqlite davranış — fixture migrasyon index'lerini uygular, G049 / dbtest gerçek PG,
  to_regclass+SKIP, dış transaction'la iz bırakmadan).

### Kararlar + gerekçeler

1. **Fotoğraf haritası** (işçi keskinleştirmesi): YEREL görev tanımındaki üçlüyle
   sınırlı (karar_no, karar_tarihi, yerel_karar_durumu) — karar_teblig_tarihi/
   karar_aciklama elle yönetilen slot olarak kaldı; karar_turu/karar_lehine kapsam
   dışı; esas_no/court'a asla yazılmaz (tek yazma yolu sync_current_esas). ISTINAF/
   TEMYIZ/KARAR_DUZELTME aileleri satır alanı ↔ slot kolonu birebir; TEMYIZ'de
   "başvuran taraf"ın şemadaki asimetrik adı `temyiz_eden_durumu` (alan prod'da 0
   dolu, tasarım paketi istinafla simetri diyor). KD'nin mahkeme/başvuran slotu şemada yok.
2. **G060 doğrulaması `active` filtresiz**: tarihçe "ne olduğunu" kaydeder;
   panelden pasifleştirilmiş resmi değer tarihsel kaydı geçersizleştirmez. Kapalı
   havuz denetimi ada göre tablo içeriğinden (boş liste = hiçbir değer geçmez).
3. **kaynak_id aynı davayla sınırlı**: çapraz dava soy bağı veri çöpü olurdu;
   silmede ORM tarafında elle NULL'lanır (sqlite FK aksiyonu koşturmaz, davranış
   iki diyalekte eşit).
4. **Mükerrer sınıflandırma**: PG'de SQLSTATE 23505 (`db_errors`); SQLSTATE
   taşımayan diyalekt (sqlite birim koşusu) için girdiler ön-doğrulandığından tek
   gerçekçi kısıt uq'dur (confirm_idempotency akıl yürütmesi). 23503 (FK) gibi
   kodlu başka hata çakışma SAYILMAZ.
5. **`idx_case_stage_decisions_kaynak` eklendi** (ilk taslakta yoktu): tam koşuda
   `test_g043_...::test_index_siz_fk_kolonu_kalmadi` bekçisi kırmızı verdi —
   index'siz FK, referans verilen satırın silinmesini seq scan'e zorlar ve G043
   bunu şema KURALI olarak kilitliyor. G042 "ölçülmeden index yok" ilkesi FK
   kapsama sınıfına uygulanmıyor; bekçiye uyuldu.

### Test sonuçları

- `pytest` (konteynerde): **1333 passed, 3 skipped** (skip'ler önceden var olan
  koşullu skip'ler; G062 dbtest'leri lokal şema göçtüğü için KOŞTU).
  G062 dosyası tek başına: 32 passed.
- `ruff check .`: All checks passed. `mypy`: Success, 40 dosya.
- Migrasyon lokalde canlı doğrulandı: backend restart → migrate.py → tablo +
  uq + kaynak index'i `to_regclass`/`pg_indexes` ile görüldü, açılış 0 ERROR.

### İzlenecekler

- FAZ F aktarımı ve okuma/yazma uçları (routes) bilinçli kapsam dışı; G065
  (yerel_karar_durumu okuma/yazma yolu) slot kolonlarının çift yazıcılığını
  (takip paneli ↔ fotoğraf) tek yola indirirken bu modülü kullanmalı.
- `karar_turu`/`karar_lehine` türetmesi ayrı karar bekliyor (görev notu).
- Deploy sonrası prod'da `uq_case_stage_decision` + `idx_case_stage_decisions_kaynak`
  varlığı `\di` ile teyit edilmeli (index op'u koşulsuz, IF NOT EXISTS).
