# G033 — B.3-1: "DB hatası → kapı KAPALI kalır" karakterizasyon testleri

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/tests/test_gate_closed_on_db_error.py (YENİ)
- **Dokunma:** backend/tests/conftest.py (DOKUNMA — G031 orada çalışıyor), backend/managers/case_manager.py + backend/routes/cases.py + backend/routes/config.py (**salt okunur**)

## Hedef

FAZ B'nin **zorunlu vakası** (plan §4, B.3). FAZ 0'da düzeltilen hata-yutan kapıların
(0.2 mükerrer dava kapısı, 0.3 `useConfig`, 0.4 ofis no sıra tahsisi) backend ayağı
bugün regresyon koruması olmadan duruyor. O düzeltmeler G014/G019 ile yapıldı ama
**geri dönerse hiçbir test bağırmaz.**

Arıza modeli sinsi: DB hatasında kapı sessizce **açılıyordu** — mükerrer dava kontrolü
boş liste dönüp "mükerrer yok" diyordu, zorunlu alan kapısı boş konfigle açılıyordu.
Bu testler tam olarak bunu kilitler: **hata → kapı KAPALI**, boş sonuç değil.

## Kabul kriterleri

- [ ] Mükerrer dava kontrolü: manager katmanı istisna fırlattığında uç **boş `matches`
      ile 200 DÖNMÜYOR** — hata durum kodu dönüyor ya da açık bir hata sinyali taşıyor.
      Bugünkü gerçek davranış önce okunup teste dönüştürülür (bu bir karakterizasyon
      testidir — davranışı DEĞİŞTİRME, kilitle)
- [ ] Ofis no sıra tahsisi: DB hatasında **sessizce `{"sequence": 1}` dönmüyor**
      (FAZ 0.4'ün düzeltmesi). Bugünkü davranış kilitlendi
- [ ] Konfig ucu: hata durumunda **boş liste dönmüyor** — zorunlu alan kapısını
      besleyen uç sessizce açılmıyor
- [ ] Her vaka için hem **başarı** hem **hata** yolu test edildi (yalnız hata yolunu
      test etmek mutasyona karşı zayıf — başarı yolu da assert'lenir)
- [ ] Testler DB'siz: manager monkeypatch'i + `TestClient`
      (desen: `tests/test_case_intake_commit.py`)
- [ ] Her testin docstring'i **hangi FAZ 0 maddesini** koruduğunu yazıyor (0.2 / 0.3 / 0.4)
- [ ] **Kaynak kod değişmedi** — diff yalnız yeni test dosyası. Bugünkü davranış yanlış
      görünüyorsa DÜZELTME; bulguyu rapora yaz, ayrı görev olur

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

### Yapılanlar

Tek yeni dosya: `backend/tests/test_gate_closed_on_db_error.py` — 11 test, üç kapıyı
FAZ 0 maddesine göre etiketli (her testin docstring'i 0.2 / 0.3 / 0.4 yazar).
**Kaynak kod değişmedi** (diff yalnız yeni test dosyası; `git status backend/` teyitli).

- **FAZ 0.2 — mükerrer dava kapısı** (4 test): eşleşme VARSA taşınıyor · gerçekten boşsa
  200 + `[]` meşru · DB hatasında 503 + gövdede `matches` YOK · "boş" ile "bilinmiyor"
  yanıtları hem durum kodu hem gövde olarak AYIRT EDİLEBİLİR.
- **FAZ 0.4 — ofis no sıra tahsisi** (4 test): isim bloğu yolu max+1 (`0007` → 8) ·
  fallback yolu da max+1 (`0012` → 13, COUNT'a dönüş yakalanır) · DB hatasında 503,
  gövdede `sequence` YOK · boş `client_name` erken çıkışı hata değil, 200 + `{"sequence": 1}`.
- **FAZ 0.3 — zorunlu alan kapısı** (3 test): uç `required_fields.py` listesini TAM döner ·
  erişilebilir tüm oturum fabrikaları patlasa da liste boşalmaz · genel referans listesi
  ucunun bugünkü hâli (aşağıdaki NOT).

### Alınan kararlar + gerekçeleri

- **`test_g014_error_gates.py` ile bilinçli kısmi örtüşme.** Orası düzeltmenin kendisini
  (manager katmanı + tek-ERROR log sözleşmesi) kilitler; burası **başarı/hata çiftini**
  kilitler. Kabul kriteri "hem başarı hem hata yolu" istiyordu ve G014 ağında başarı
  yolu yoktu: uç her koşulda 503 dönse de o testler yeşil kalırdı. Örtüşme dosya
  docstring'inde açıkça yazılı.
- **Başarı yolu değerleri bilinçli olarak 1 DEĞİL** (8 ve 13). Eski arıza sabit "1"
  döndürmekti; başarı yolu da 1 assert etseydi mutasyon ayırt edilemezdi.
- **Konfig ucu olarak `/api/config/required_case_fields` alındı** — kabul kriterindeki
  "zorunlu alan kapısını besleyen uç" tam olarak budur (`required_fields.py` tek kaynak,
  `useConfig.ts:128` bunu okur). Bu uç DB'ye hiç inmez; test bunu kilitler ki uç bir gün
  hata yutan bir DB fallback'ine çevrilirse ağ kırmızıya dönsün.
- **Bulgu düzeltilmedi, karakterize edildi** (aşağıdaki NOT) — görev "davranışı DEĞİŞTİRME,
  kilitle" diyor.

### Test sonuçları

- `docker compose exec -T backend python -m pytest` → **1008 passed, 2 skipped** (yeni
  dosya 11 test ekledi; öncesi 997+2)
- `ruff check .` → All checks passed · `mypy` → no issues in 29 source files
- **Mutasyon sınaması (ağ gerçekten ısırıyor mu?):** repoya DOKUNMADAN, konteyner içinde
  `/app`'in `/tmp/mut` kopyasına üç FAZ 0 düzeltmesi geri alındı ve ağ o kopyada koşuldu:
  - check-duplicate istisna yutup `{"matches": []}` dönerse → 2 test FAIL
  - client-sequence hata anında `{"sequence": 1}` dönerse → 1 test FAIL
  - required_case_fields boş liste dönerse → 2 test FAIL
  Kopya sonrasında silindi; `git status backend/` yalnız yeni test dosyasını gösteriyor.

### İzlenecekler

- **NOT (kapsam dışı bulgu, ayrı görev konusu):** genel referans listesi uçları
  (`/api/config/lawyers`, `statuses`, `doctypes` …) DB hatasında HTTP **200 + `[]`**
  döner — `managers/reference_lists.py:213-215` `get_items` istisnayı yutup boş liste
  veriyor. Yani "liste gerçekten boş" ile "DB çöktü" çağıran taraftan ayırt edilemez ve
  frontend'in G019 ile eklediği `CONFIG_LIST_ERROR` ayrımı bu uçlarda karşılıksız kalır.
  Bu uçlar zorunlu alan kapısını beslemediği için (o kapı statik uçtan beslenir) G033
  kapsamında düzeltilmedi; bugünkü davranış
  `test_reference_list_endpoint_swallows_db_error_today` ile kilitlendi — biri 503'e
  çevirirse testin bilinçli olarak güncellenmesi gerekir.
- `routes/cases.py`'deki 503 sözleşmesi iki uçta da `{"detail": ...}` (Faz 5-B biçimi);
  yeni bir hata-yutan kapı eklenirse aynı biçim korunmalı.
