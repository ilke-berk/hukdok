# G008 — FAZ 6-B (2/2): modül docstring'leri + proje skill'leri + sertleştirme planının kapanışı

- **Bant:** backend (ana dizin — pytest kapısı var)
- **Bağımlı:** G007
- **Dosya kapsamı:** `backend/managers/*.py`, `backend/services/*.py`, `backend/routes/*.py`
  (YALNIZ modül üstü docstring), `.claude/skills/test-kos/SKILL.md` + `.claude/skills/deploy-prosedur/SKILL.md`
  (yeni), `docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md` (kapanış)
- **Dokunma:** fonksiyon gövdeleri/davranış (HİÇBİR kod yolu değişmez), `gorevler/KUYRUK.md`

## İş 1: modül üstü docstring standardı
`managers/`, `services/`, `routes/` altındaki her modülün başına 3-5 satır: ne yapar, kimden
çağrılır, dış bağımlılığı ne. Var olan iyi docstring'leri BOZMA, eksikleri tamamla, bayat
olanları koda bakarak düzelt. Davranış değişikliği YASAK — yalnız docstring/yorum satırları.

## İş 2: iki proje skill'i
- `test-kos`: iki paketi doğru yerde koşan kısa skill (backend konteynerde pytest+ruff+mypy,
  frontend host'ta vitest; bind-mount/worktree uyarısı).
- `deploy-prosedur`: deploy öncesi kontrol listesi (aktivite/pencere kontrolü, pre-dump
  beklentisi), nohup+ayrı-ssh-poll deseni, sağlık kapısı ve deploy-sonrası doğrulama adımları,
  rollback yolu. Skill deploy'u KENDİLİĞİNDEN BAŞLATMAZ — kullanıcı istediğinde prosedürü uygular.

## İş 3: sertleştirme planının kapanışı (takip dosyası)
- 6-A ve 6-B kutularını `[x]` yap; Durum notları'na G006/G007/G008 için mevcut formatta
  tek'er satır ekle (Rapor'lardan damıt).
- Dosyanın tepesindeki banner'ı güncelle: **"PLAN TAMAMLANDI (2026-08-12) — FAZ 0-6 bitti;
  FAZ 0-5 prod'da canlı (67f1694), FAZ 6 repo-içi. Bu dosya artık ARŞİV adayıdır; güncel
  mimari docs/mimari/, güncel kararlar docs/kararlar/."**

## Kabul kriterleri
- [x] Üç dizindeki tüm modüllerde modül üstü docstring var (eksiksiz envanter Rapor'da).
- [x] Kod davranışı değişmedi: konteynerde pytest **859 passed + 2 skipped tabanının altına
      düşmedi**, ruff + mypy temiz. (Docstring dışı satır diff'i yoksa bu zaten garanti.)
- [x] İki skill çalışır talimat içeriyor (komutları koddan/gerçekten doğrulanmış).
- [x] Takip dosyası kapanışı yapıldı; KUYRUK'a dokunulmadı.

## Doğrulama
- `docker compose exec -T backend python -m pytest -q` + konteynerde ruff + mypy.

## Rapor

TAMAM — 2026-08-11. Üç elde bitti: **işçi oturumu** İş 1'i yapıp `.claude/skills/`
yazma iznine takılınca BLOKE bıraktı; iki SKILL.md'yi **dirijan** yazdı (içerik işçinin
doğrulanmış Rapor malzemesinden); **devralan bu oturum** İş 1 diff'ini ve skill
içeriklerini koda karşı doğruladı, İş 3'ü yaptı, kapıları koşup tek commit'le kapattı.

### İş 1 — modül üstü docstring standardı (işçi oturumu; bu oturum doğruladı)

`managers/`, `services/`, `routes/` altındaki TÜM modüllerde artık modül üstü docstring
var. Bu oturumun bağımsız envanteri (konteynerde `ast.get_docstring` taraması):
**36/36 modülde docstring VAR, eksik YOK** (managers 14 + services 9 + routes 13; işçi
raporundaki "34 modül" sayımı küçük hatalıydı, sonuç değişmiyor). Dokunulan 15 dosya:

| Dosya | Ne yapıldı |
| --- | --- |
| `managers/__init__.py` | eklendi (paket işaretleyici, boşluk gerekçesi) |
| `managers/cache_manager.py` | eklendi |
| `managers/client_manager.py` | 1 satırlık docstring 5 satıra genişletildi |
| `managers/config_manager.py` | eklendi |
| `managers/log_manager.py` | eklendi |
| `managers/ttl_cache.py` | eklendi |
| `services/__init__.py` | eklendi |
| `routes/__init__.py` | eklendi |
| `routes/activity.py` | 3 satırlık başlık docstring'i genişletildi |
| `routes/cases.py` | eklendi |
| `routes/clients.py` | eklendi |
| `routes/config.py` | eklendi |
| `routes/documents.py` | eklendi |
| `routes/parties.py` | eklendi |
| `routes/processing.py` | eklendi |

Zaten yeterli docstring'i olan kalan 21 modüle DOKUNULMADI (`case_manager`,
`counter_manager`, `export_publisher`, `case_intake`, `export`, `client_errors`,
`debug`, `admin` …) — işçi hepsini tek tek okudu, bayat/yanlış olan çıkmadı.

**Devralma teyidi (bu oturum):** `git diff -- backend` satır satır tarandı — 15 dosyada
104 ekleme + 3 silme (silmeler `client_manager`/`activity`'de docstring genişletilirken
düşen eski kısa satırlar), diff'in TAMAMI modül-üstü docstring; TEK kod satırı değişmedi.

### İş 1 kararları + gerekçeleri (işçi oturumundan devralındı, geçerli)

- **Docstring'ler koddan doğrulandı, arşivden/ezberden yazılmadı.** Her iddia için kaynak
  okundu: `deleted_at` soft-delete filtreleri (`routes/cases.py:283`,
  `routes/clients.py:253`), `add_client`'ın soft-delete'li kaydı diriltmemesi
  (`client_manager.py:83-95`), `cache_manager.save_cache`'in pid'li temp + `shutil.move`
  atomikliği (`cache_manager.py:57-67`), `require_admin`'in `routes/config.py`'den
  `routes/activity.py`'ye import edilmesi (`routes/activity.py:13`), router'ların
  `api.py:412-430` include_router satırları, `@router` yol listeleri.
- **`log_manager` docstring'i bilerek "docker logs'ta yok" DEMİYOR.** Bellekteki eski not
  bunu söylüyordu ama kod (Faz 3.3/K10, `log_manager.py:215-239`) kayıtları standart
  logging'e DE delege ediyor; docstring "standart logging'e delege eder VE tamponu
  SharePoint arşivine yükler" diye yazıldı. → Bellek notu güncellenmeli (izlenecek).
- **Boş `__init__.py`'lere de docstring eklendi**, "paket işaretleyici, bilinçli boş,
  re-export yok" gerekçesiyle — kabul kriteri "üç dizindeki TÜM modüller" diyor ve boş
  bırakılan bir `__init__.py` bir sonraki okuyucuya "unutulmuş" gibi görünüyordu.
- **Davranış değişikliği YOK:** diff yalnız docstring satırlarından ibaret, tek bir kod
  yolu değişmedi.

### İş 2 — proje skill'leri (dosyalar dirijandan; bu oturum koda karşı doğruladı)

İşçi oturumu `.claude/skills/` altına Write izni alamayınca (dört denemede izin kapısı)
BLOKE bırakmıştı; iki SKILL.md'yi dirijan, işçinin yukarıda doğrulanmış malzemesinden
yazdı. Bu oturum kabul kriteri 3 gereği içerikteki HER komutu ve dosya:satır atfını koda
karşı sınadı — **düzeltme gerektiren iddia ÇIKMADI**:

- **test-kos:** `addopts = "-q"` → çift `-q` uyarısı doğru (`backend/pyproject.toml:49`);
  mypy kapsamı `files = ["managers", "routes", "config"]` (`pyproject.toml:35`);
  `requirements-dev.txt` başlığı "prod imajına GİRMEZ" + pytest/httpx/ruff/mypy pinleri;
  `"test": "vitest run"` (`frontend/package.json:10`; lint/build script'leri de mevcut);
  imaj `python:3.10-slim` (`backend/Dockerfile` FROM satırı); worktree tuzağı doğru:
  lokal `docker-compose.override.yml:8` `- ./backend:/app` ANA dizini bağlar, base
  compose'ta ise kaynak mount'u YOK (yalnız `backend-data` volume,
  `docker-compose.yml:51-53`).
- **deploy-prosedur:** akış yorumu `deploy.sh:6-9`; REQUIRED_KEYS + `hukuk_shared`
  önkoşulları (`deploy.sh:44-55`); `git pull --ff-only || fail` (65); `pg_dump -Fc` +
  `MIN_DUMP_BYTES=1048576` (36, 77-79); build eski stack çalışırken (100-101);
  `APP_VERSION` + SHA etiketleri (98-104); `up -d --remove-orphans` (109); 120 sn
  `/healthz` kapısı + "bayat imaj" sürüm uyarısı (115-133); 30 sn frontend poll
  (137-145); `db-backup.timer` kontrolü (149-155); `KEEP_TAGS=3` etiket bakımı (35,
  157-171); kapanış üçlüsü "✅ Deploy tamam: … · rollback: … · DB dump: …" (179);
  "rollback İMAJI döndürür, DB'yi DÖNDÜRMEZ" (`deploy.sh:70-71` + `rollback.sh:7-8,37`);
  pencere kontrolündeki `hukdok_backend` konteyner adı (`docker-compose.yml:45`);
  doğrulama curl'ü script'in kendi `HEALTH_URL`'i (`deploy.sh:32`); ssh alias'ları
  `hukukoid` + `hukukoid-cc` `~/.ssh/config`'te mevcut.

### İş 3 — sertleştirme planının kapanışı (bu oturum)

`docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md`: 6-A ve 6-B kutuları `[x]`;
tepe banner görev dosyasındaki dondurulmuş metinle AYNEN değiştirildi ("PLAN TAMAMLANDI
(2026-08-12) — …"); Durum notları'na mevcut formatta üç satır eklendi — (24) G006
(c5aad90, denetim GECTI), (25) G007 (4fddb77), (26) G008 — içerik Rapor'lardan damıtıldı.
G007 satırına denetim iddiası bilerek YAZILMADI: kuyruk kaydında (51e1e79) yalnız
"tamam" var, denetim kaydı yok — doğrulanmayan yazılmaz.

### Test sonuçları (bu oturum, konteynerde, 2026-08-11)

- `docker compose exec -T backend python -m pytest` → **859 passed, 2 skipped**
  (14.52 sn) — taban birebir korundu (işçi koşusu da aynı sayıları vermişti).
- `docker compose exec -T backend python -m ruff check .` → **All checks passed!**
- `docker compose exec -T backend python -m mypy` → **Success: no issues found in 29
  source files**
- Dev araçları konteynerde önceki oturumdan kuruluydu (`pip list` ile teyit) — yeniden
  kurulum gerekmedi. Lokal override mount'u sayesinde konteyner çalışma ağacındaki
  düzenlenmiş dosyaları koştu (rebuild gerekmedi).

### İzlenecekler + NOT (kapsam dışı, düzeltilmedi)

- Bellek notu "TechnicalLogger ERROR'ları docker logs'ta değil" BAYAT: kod (K10,
  `log_manager.py:215-239`) kayıtları standart logging'e DE delege ediyor; yeni docstring
  doğrusunu söylüyor. Bellek notu güncellenmeli (işçinin tespiti, bu oturum katılıyor).
- `.claude/settings.local.json` (M) + `.claude/launch.json` (??) harness'ın izin/oturum
  dosyaları — koşucu düzeninde MUAF; dokunulmadı, commit'e alınmadı.
- `gorevler/KUYRUK.md`'ye dokunulmadı (işaretleme dirijanda).
- Banner tarihi "2026-08-12" görev dosyasında dondurulmuş metindir ve dirijan talimatıyla
  AYNEN kullanıldı (koşu 2026-08-11'de bitti; plan FAZ 6'yı 12'si gecesine bağladığı için
  bilinçli). Durum notu satırları gerçek koşu tarihiyle (2026-08-11) yazıldı.
