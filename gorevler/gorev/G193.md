# G193 — Performans turu dokümantasyonu: mimari dokümanlar + CLAUDE.md + karar 018 eki + denetim raporu "uygulamada değişti" şerhi

- **Bant:** docs
- **Bağımlı:** G186,G187,G188,G192
- **Dosya kapsamı:** `docs/mimari/genel-bakis.md`, `docs/mimari/deploy-ve-altyapi.md`, `docs/mimari/dava-acma-akisi.md` (arama bölümü), `docs/kararlar/018-index-temizligi-37-kalem.md`, `CLAUDE.md` (dava arama + nginx paragrafları), `docs/arsiv/performans-denetimi-2026-09-14.md` (üst şerh), `gorevler/KUYRUK.md` DOKUNMA (runner'ın)
- **Dokunma:** kod dosyaları (yalnız okunur — ALTIN KURAL: her iddia koddan doğrulanır)

## Hedef
G182-G192'nin sonucunu yaşayan dokümanlara işle; koddan doğrulanmış iddialar dışında hiçbir
şey yazma. Görev raporlarındaki (`gorevler/gorev/G18x.md` "Rapor" bölümleri) sayıları doğrudan
KOPYALAMA — dokümana giren her sayı/yol/komut yeniden koşularak ya da koddan okunarak doğrulanır.

## Kabul kriterleri
- [ ] `CLAUDE.md` "Dava arama (E8, G055)" paragrafı: tek koşu (COUNT + sayfa), `case_foys` trgm index'leri, `cases` trgm index'lerinin güncel durumu (G190'da eklenen/eklenmeyen) — koddan (`database.py::_TRGM_INDEXES`, `case_manager.get_cases`) doğrulanmış.
- [ ] `CLAUDE.md` "İki katmanlı nginx" paragrafı: `/assets/` immutable önbellek + `index.html` no-cache; host nginx'in aynı başlığı verip vermediği "prod'da doğrulanacak" notuyla.
- [ ] `docs/mimari/deploy-ve-altyapi.md`: `perf_olcum` komutu, compose Postgres parametreleri, yeni `DB_*` env'ler, `pg_stat_statements` okuma örneği, "recreate şart" notu (G183/G191 dokümanlarıyla çakışmayacak — varsa birleştir).
- [ ] `docs/mimari/genel-bakis.md` (frontend bölümü): route düzeyinde kod bölme, bayat parça yeniden yükleme davranışı, `useConfigList`, `refetchOnWindowFocus:false` kararı.
- [ ] `docs/kararlar/018-index-temizligi-37-kalem.md`: 14.09 denetimi + G189/G190 kanıtıyla nihai index listesi (eklenen/düşürülen/bekleyen) tek tabloda.
- [ ] `docs/arsiv/performans-denetimi-2026-09-14.md` başına şerh: hangi bulgular hangi görevle kapandı (F/D numarası → G numarası), hangileri bilinçli açık (F13 font, D12 jsonb, D13/D14, D3 VACUUM prod ölçümüne bağlı, D7 index düşürme prod sayacına bağlı, D9 VALIDATE).
- [ ] Her yeni doküman cümlesi için kaynak dosya:satır ya da koşulan komut rapora yazılır.

## Doğrulama
- Test yok (docs bandı). `git diff --stat` yalnız kapsamdaki dokümanlar.

## Rapor

DURUM: TAMAM (2026-09-14, işçi oturumu, worktree `gorev/G193`; başlangıç HEAD `e3ba26d`)

### Yapılanlar (dosya başına)
- **`CLAUDE.md`**
  - "İki katmanlı nginx": `/assets/` immutable + `/` no-cache, `add_header` kalıtım tuzağı, host nginx'in başlığı geçirdiğinin prod'da doğrulanacağı notu. **Düzeltme:** "host konfigi repo DIŞINDA" iddiası yanlıştı (`infra/nginx/sites-available/default` repoda, `infra/install.sh:73` kurar). `/export` atfı `nginx.conf:75` → `:114`.
  - "Dava arama": başlık G189 eklendi; tek koşu (D4) + `with_total=False` yolu + aramasız liste; `offset ≤ 10000`; `_TRGM_INDEXES` konumu (op değil, hata-toleranslı blok); sözlükteki kollar, düşmüş kalanlar, trigram'sız kollar, `notes`/`old_value` kullanıcı kararı; karar 018 tablosuna gönderme.
- **`docs/mimari/genel-bakis.md`**
  - Başlık: kapsamlı doğrulama notu.
  - §1: compose satır atıfları (G191 +11 kaydırmıştı) + Postgres `command:` paragrafı.
  - §2: önbellek başlıkları tablosu + kalıtım kuralı; bütün `nginx.conf` satır atıfları (G182 kaydırmıştı).
  - §3: `migrate.py` artık üç env'i 0'lar.
  - §7: beş alt başlık — kod bölme, bayat parça tek yenileme, `useConfigList` + tüketici tablosu, `refetchOnWindowFocus:false`, tema sağlayıcısı/depo.
- **`docs/mimari/deploy-ve-altyapi.md`**
  - Başlık: doğrulama notu.
  - §3, §4, §8, §11 satır atıfları düzeltildi; §11'de CSP artık üç satırda, geri dönüş üçü birlikte.
  - §11: "Önbellek başlıkları (G182)" (curl kontrolü, imajdan gelir, host nginx prod doğrulaması, tarayıcı dumanı).
  - §12: giriş cümlesi CHECK → VALIDATE; "Deploy sonrası prod doğrulama sırası" (G183/G191 bölümleriyle çakışmasın diye komutları tekrarlamaz, §11/§13'e gönderir).
  - §13: mevcut içerik yeniden koşularak doğrulandı, metin değişmedi.
- **`docs/mimari/dava-acma-akisi.md`** (§10 arama notu): föy trigram index'leri (G189) + CLAUDE.md/karar 018 göndermesi. Ayrı bir "arama bölümü" yok; arama yalnız §10'da anlatılıyor.
- **`docs/kararlar/018-index-temizligi-37-kalem.md`**: başlık notu; "Nihai index durumu — 2026-09-14" tablosu. Satır grupları: eklenen 7, düşürülen 3 + 20 PK ikizi, düşmüş kalan 2, ölçülüp eklenmeyen 2, bekleyen D7 7, dokunulmayan 3, sıfırdan kurulum 2. Altına `ck_cases_status_uclu` notu.
- **`docs/arsiv/performans-denetimi-2026-09-14.md`**: gövde değişmeden üste şerh. F1-F14 ve D1-D15 → durum / görev / kodda yer. Bilinçli açıklar: F13, D12, D13, D14; D3 prod ölçümüne, D7 prod sayacına bağlı; D9 VALIDATE açık; F11 AÇIK (görev açılmadı).

### Kaynaklar — her yeni cümle neye dayanıyor
Koşulan komutlar (hepsi salt okunur):
- (K1) `docker exec hukudok-postgres psql ... pg_indexes`: `gin_trgm_ops` index listesi (11 adet, `_TRGM_INDEXES` ile birebir), düşen/bekleyen adların varlığı, `^ix_*_id$` kalan 7 index'in PK olmayışı, `ck_cases_status_uclu convalidated=f`.
- (K2) `SHOW effective_cache_size / shared_preload_libraries / random_page_cost / track_io_timing / pg_stat_statements.max / shared_buffers / max_connections / work_mem` → 384MB / pg_stat_statements / 1.1 / on / 5000 / 128MB / 100 / 4MB.
- (K3) §13'teki iki psql komutu aynen koşuldu (SHOW ikilisi iki sonuç basar; en pahalı 10 sorgu satır döner).
- (K4) `curl -sI http://localhost:8080/`, `/assets/index-*.js`, `/assets/YOK-parca.js` → `Cache-Control` yok, eksik parça 200 (aşağıda NOT).
- (K5) Bağıl link + atıf yolu varlık kontrolü (6 dosya): kırık 0.

Koddan okunanlar:
| Doküman iddiası | Kaynak |
| --- | --- |
| `/assets/` immutable, `=404`, `always` değil; `/` no-cache; kalıtım yorumu; güvenlik başlıkları + CSP üç yerde | `nginx.conf:38-51`, `:53-56`, `:70`, `:77-86`, `:88-99` (`:81-85`, `:94-98`) |
| `/healthz` `/api` `/process` `/confirm` `/preview-*` `/refresh` `/export` satırları | `nginx.conf:101-111`, `:116`, `:124-131`, `:133`, `:141`, `:149-159`, `:161`, `:114-115` |
| Host konfig repoda, `add_header`/`proxy_hide_header` yok, `proxy_pass localhost:8080`, 300s | `infra/nginx/sites-available/default:24-35`, `infra/install.sh:73-78` |
| `nginx.conf` frontend imajına kopyalanır | `frontend/Dockerfile:34` |
| Compose satırları (+11), `command:` | `docker-compose.yml:4,22,26,30-33,39-43,48-55,62-65,69-71,85-88,92,103-104,110-121,130-140,146-148,163-166` |
| DB env varsayılanları, `_build_connect_args`, havuz | `backend/database.py:65-67`, `:70-86`, `:92-96` |
| migrate muafiyeti | `backend/migrate.py:19-30` |
| `_TRGM_INDEXES` girişleri, DDL `IF NOT EXISTS` (CONCURRENTLY yok), pg_trgm/pg_stat_statements log metinleri | `database.py:1387-1418`, `:1421-1426`, `:1598-1617`, `:1611`, `:1643` |
| `_DUSURULECEK_INDEXLER` (20 PK ikizi, klasor/resp, legacy üçlü), DROP op'ları | `database.py:1295-1314`, `:1324-1336`, `:1339-1342` |
| Madde 52 CHECK NOT VALID | `database.py:150-185`, `:1229` |
| Arama kolları (15/13), tek koşu, `_load_cases_in_order`, `with_total=False` | `managers/case_manager.py:740-798`, `:814-830`, `:956-974`; `search_cases` `:1463-1471` |
| offset tavanı | `routes/cases.py:92` |
| D11 / D15 | `routes/cases.py:413`, `routes/clients.py:164`, `services/upload_queue.py:506-507` |
| `ix_cases_tku_no`/`ix_cases_sistem_no` sıfırdan kurulum kaynağı | `models.py:61-62` |
| PK ikizi kapsam bekçisi | `tests/test_g192_dusuk_etkili.py:436` |
| Lazy sayfalar, tek Suspense, admin route'ları, QueryClient, ErrorBoundary/ThemeProvider | `frontend/src/App.tsx:25-47`, `:49-51`, `:105-149`, `:128-143`, `:244-250` |
| Admin bekçisi çocuğu çizmeden yönlendirir | `components/ProtectedAdminRoute.tsx` (tamamı okundu) |
| Bayat parça davranışı | `lib/chunkReload.ts:18-74` |
| Kaynak bekçisi `pages/`den türer | `App.lazy.test.tsx:181-188` |
| `useConfigList`, ortak kurucu, 32 `useQuery`, yardımcılar | `hooks/useConfig.ts:128-134`, `:137-149`, `:159-172`; `sed -n 174,260p \| grep -c useQuery(` = 32 |
| Tüketiciler | `grep "= useConfig()"` (7 dosya) + `grep useConfigList(` (6 dosya); `AdminPage.tsx:331-340`, `NewCase.tsx:89-94`, `IntakeReviewStep.tsx:131`, `QuickCaseModal.tsx:85` |
| Diğer react-query sorguları, `refetchInterval` yok, bildirim poll'u | `useClients.ts:47`, `ClientList.tsx:132`, `usePartyCheck.ts:112`, `useNotifications.ts:36,155-187` |
| Tema/yetki belgesi önbelleği | `theme-provider.tsx:14`, `YetkiBelgesiModal.tsx:32-33`, `lib/formDraft.ts:10-11` |
| Şerh F-satırları | `CaseDetails.tsx:195`, `AdminPage.tsx:366`, `Index.tsx:120,747`, `NewCase.tsx:1728`, `CaseList.tsx:169,222`, `index.html:55`, `grep 'import("sonner")'` (yalnız test yorumu) |
| Şerh D12/D14 | `models.py:380,1327,1348,1394,1433` `Column(JSON)`; `grep skip_locked\|pg_advisory backend` (test dışı 0) |
| `perf_olcum.py:413` bayat metin | `scripts/perf_olcum.py:413` |

Görev raporlarından (`G182`-`G195`) sayı KOPYALANMADI. Karar 018 tablosu yalnız satır/varlık bilgisi taşır, ölçüm sayısı için mevcut G189/G190 eklerine gönderir. Tek istisna G182 raporuna "beklenen curl çıktıları" ve "tarayıcı dumanı yapılmadı" göndermesi; dosya adıyla verildi, sayı taşımaz.

### Doğrulama
- docs bandı, test yok. Link/atıf yolu kontrolü (K5): kırık 0.
- Tur 1 yeşil. Kod ile çelişen iddialar bulunup düzeltildi:
  - CLAUDE.md "host konfigi repo dışında"
  - `nginx.conf` ve compose satır kaymaları
  - AdminPage "tüm listeler" taslağı (13 liste) — yayından önce düzeltildi
  - §12 CHECK → VALIDATE
- `git diff --stat`: yalnız kapsamdaki 6 doküman + bu görev dosyası.

### İzlenecekler / NOT
- **NOT (kod, kapsam dışı):** `nginx.conf:44` yorumu da "host nginx (konfigi repo DISINDA)" diyor. Repoda `infra/nginx/sites-available/default` var. Kod dosyası, düzeltilmedi.
- **NOT (lokal ortam):** lokal 8080 frontend konteyneri G182 öncesi imajla çalışıyor (`Cache-Control` yok, eksik parça 200). `docker compose build frontend && docker compose up -d frontend` gerekir. Docs bandında compose koşulmadı.
- **NOT:** `scripts/perf_olcum.py:413` metni "CHECK kısıtı (D9) ancak 0 iken eklenebilir" — G195 sonrası doğru ifade "VALIDATE ancak 0 iken". Dokümana şerh düşüldü, kod değişmedi.
- **NOT:** `genel-bakis.md` §3'ün `database.py`/`api.py` satır atıfları (ör. `database.py:142`, `:939`) bu turda yeniden doğrulanmadı. Başlıkta açıkça yazılı.
- **NOT:** CLAUDE.md "Komutlar" bölümündeki test sayıları (2026-09-13) bayat. Bu görevin paragraf kapsamı dışı; koşulmadan yazılamaz.
- **NOT:** CLAUDE.md doküman haritasında `docs/hukukbot-aktarim/` satırı `nginx.conf:75` atfını taşıyor. `/export` uyarısı artık `:114`. Spec dosyası atfı da bayat olabilir, kapsam dışı.
- **Açık kullanıcı kararları:** `notes`/`case_history.old_value` arama (G190 A/B/C); F11 (CaseList react-query) için görev yok.
- **Prod (insan):** `deploy-ve-altyapi.md` §12 "Deploy sonrası prod doğrulama sırası".
- Çalışma ağacı temiz başladı (worktree); ana dizindeki `.claude/*` kirliliğine dokunulmadı.
