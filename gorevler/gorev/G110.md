# G110 — Cevap paketi: eşleşme CSV (Talep #9) + raporların SharePoint'e geri yüklenmesi

- **Bant:** backend
- **Bağımlı:** G109
- **Dosya kapsamı:** backend/services/teslim_kutusu.py, backend/services/teslim_cevap.py (YENİ),
  backend/tests/test_g110_teslim_cevap.py (YENİ)
- **Dokunma:** backend/sharepoint/sharepoint_uploader_graph.py (yalnız `upload_file_to_sharepoint`
  çağrılır; değiştirme), backend/scripts/hukdok_aktarim.py (dokunma; `foy_map` ve `models`
  üzerinden okunur)

## Hedef

Plan §2.4. Veri ekibine FAZ F §3'te ve teslim paketinin `HUKDOK_TALEPLERI` #9'unda
("ÖNCELİKLİ": hangi SistemNo hangi `cases.id`/`tracking_no` ile eşleşti, hangileri eşleşmedi)
borçlu olduğumuz çıktılar her `uygulandi` teslimde kendiliğinden `03_VERI_TESLIM/cevap/<teslim>/`
altına gitsin. Döngü böylece iki yönde de kapanır.

## Yapılacaklar

1. **Eşleşme dosyası** — `teslim_cevap.eslesme_csv_uret(teslim_id, hedef: Path) -> Path`:
   teslimin `Sheet`'indeki her SistemNo için `case_foys` üzerinden `cases.id`, `tracking_no`,
   `klasor_no_2`, `case_party_id`, `tku_no`; eşleşmeyenler `case_id` boş + `sebep` (satır
   raporundaki ATLANDI/HATA sebebi). CSV: UTF-8 BOM, `;` ayraç (`hukdok_aktarim._csv_yaz` ile
   aynı biçim — o fonksiyon import edilebiliyorsa kullan, private ise deseni kopyala ve raporda
   not düş). Sütunlar: `sistem_no;dosya_no;case_id;tracking_no;klasor_no_2;tku_no;case_party_id;durum;sebep`.
2. **Özet dosyası** — `ozet_<teslim>.txt`: `hukdok_aktarim.ozet_metni(sonuc)` çıktısı kuru
   koşu ve uygulama için deftere kaydedilmeli — G107 `AktarimSonucu`'nu deftere yazmıyor; bu
   görevde `teslim_kutusu` uygulama sonrası `ozet_metni` çıktısını `rapor_dizini/ozet.txt`'ye
   yazar (spool'da kalıcı) + kapı kararı satırı ekler.
3. **Geri yükleme** — `teslim_cevap.cevap_yukle(teslim_id) -> bool`: `rapor_dizini` altındaki
   tüm CSV/TXT + eşleşme dosyası `upload_file_to_sharepoint(filepath, target_filename,
   target_folder_name=f"{SHAREPOINT_FOLDER_TESLIM_NAME}/cevap/{dosya_adi_uzantisiz}",
   content_type="text/csv")` ile gider (klasör yoksa Graph `root:/path:/content` PUT'u ara
   klasörleri kendisi açar — mevcut davranış; doğrula ve raporda yaz). Hepsi başarılıysa
   `cevap_yuklendi=True`. Kısmi başarısızlık: WARNING, `cevap_yuklendi=False` kalır, teslim
   durumu DEĞİŞMEZ (yazım zaten commit'li).
4. **Gece turuna bağlama** — `gece_turu` sonunda `cevap_yuklendi=False` olan tüm `uygulandi`
   teslimler için `cevap_yukle` denenir (retry doğal olarak ertesi geceye kalır; deneme sayısı
   `durum_gecmisi` notunda). Admin `uygula` ucu da başarı yolunda bir kez dener.

## Kabul kriterleri

- [x] Eşleşme CSV: sqlite uçtan uca — 3 eşleşen + 1 eşleşmeyen satırlı sentetik paket
      uygulandıktan sonra dosyada 4 satır, eşleşmeyenin `case_id` boş ve `sebep` dolu; BOM +
      `;` (byte düzeyinde test).
- [x] `ozet.txt` kuru koşu ve uygulama sonrası spool'da; içinde "yazıldı mı" satırı ve kapı
      kararı.
- [x] `cevap_yukle`: sahte `upload_file_to_sharepoint` (monkeypatch) her dosya için bir kez
      çağrılıyor, hedef klasör `03_VERI_TESLIM/cevap/HUKDOK_TESLIM_X`; bir dosya patlarsa
      `cevap_yuklendi=False`, durum `uygulandi` kalıyor, WARNING; ertesi `gece_turu` yeniden
      deniyor ve başarıda True oluyor.
- [x] Log sözleşmesi korunuyor (yükleme hatası WARNING, ERROR yok).
- [x] `hukdok_aktarim.py` diff'i SIFIR (import izinli).

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-03, işçi oturumu) — kabul kriterleri 5/5; Yapılacaklar 1-4 uygulandı.
Yapılacaklar #4'ün "admin `uygula` ucu" ayağı `routes/admin.py`'ye dokunmadan
`teslim_kutusu.teslim_uygula` başarı yolunda karşılandı (uç zaten bu fonksiyonu çağırır).

### Yapılanlar

- `backend/services/teslim_cevap.py` (YENİ): `cevap_klasoru(dosya_adi)`,
  `teslim_adi_uzantisiz`, `eslesme_csv_uret(teslim_id, hedef, db=None) -> Path`,
  `cevap_yukle(teslim_id, db=None) -> bool`, `cevap_bekleyen_idler(db, haric=None)`,
  `bekleyen_cevaplari_yukle(db, haric=None) -> {id: bool}`; sabitler `ESLESME_BASLIKLARI`
  (görevdeki 9 sütun, sıra sabit), `DURUM_ESLESTI`/`DURUM_ESLESMEDI`, `CEVAP_TURLERI`
  (`.csv` → `text/csv`, `.txt` → `text/plain`), `DENEME_NOTU_ONEKI`.
- `backend/services/teslim_kutusu.py`: `OZET_DOSYASI = "ozet.txt"`; `_ozet_dosyasi_yaz`
  (ozet_metni + "kapı kararı" satırı) kuru koşuda ("henüz değerlendirilmedi") ve uygulamada
  (teslimin kararı/gerekçesi) yazılır; `kapi_degerlendir` commit sonrası `_ozet_kapi_guncelle`
  ile satırı tazeler (ikilemez). `teslim_uygula` başarı yolunda `_cevap_dene` (istisna →
  WARNING, durum değişmez). `gece_turu` sonunda `_bekleyen_cevaplari_yukle(haric=uygulanan)`.
  Mevcut `kuru-kosu-ozeti.txt` / `uygulama-ozeti.txt` korundu (G107 testi bunları kilitliyor).
  Modül şerhine "Cevap paketi" maddesi eklendi.
- `backend/tests/test_g110_teslim_cevap.py` (YENİ, 20 test): birim ×3 (klasör env, CSV bayt
  eşitliği, başlık sözleşmesi), eşleşme CSV ×3 (kabul senaryosu 3+1, aynı SistemNo iki satır +
  satır-no anahtarlı sebep, spool yoksa ValueError), ozet.txt ×2, cevap_yukle ×8 (tam yükleme,
  kısmi hata + ertesi gece + üçüncü gece dokunmaz, zaten yüklü, yanlış durum, anahtar kapalı,
  env yok, eşleşme üretilemedi, rapor dizini yok, `_cevap_dene` istisnası), gece turu ×3
  (aynı gece ikinci deneme yok, çok bekleyen + tek hata turu durdurmaz + `ozet` şekli
  korunur, `cevap_bekleyen_idler(haric)`).

### Kararlar ve gerekçeleri

1. **Yüklemenin İKİ kapısı var: otomasyon anahtarı + `SHAREPOINT_FOLDER_TESLIM_NAME` env'i
   (yazma için varsayılan YOK).** Semantik gerekçe: anahtar "SharePoint ile otomatik
   alışveriş"tir — G109 gözcüsü kapalıyken bakmıyor, cevap da kapalıyken yazmıyor (elle
   "Uygula" yine çalışır, dosyalar spool'da hazır durur, anahtar açılınca ertesi gece turu
   yükler — testli). Yazma hedefi ortak arşive dosya bırakır; okuma tarafındaki
   `03_VERI_TESLIM` varsayılanı (`teslim_gelen_klasoru`, G109 testi kilitli) yazma için
   türetilmez; env yoksa INFO + `False`, deneme sayılmaz. **Dürüst ikinci gerekçe (asıl
   tetikleyici):** lokal konteyner gerçek Graph kimlik bilgilerini taşıyor
   (`SHAREPOINT_TENANT_ID`/`CLIENT_ID`/`CLIENT_SECRET` set; `SHAREPOINT_FOLDER_TESLIM_NAME`
   set DEĞİL) ve mevcut G107/G108/G109 testleri upload'ı sahtelemeden `uygulandi`ya ulaşıyor
   (`teslim_uygula`, admin `uygula` ucu, `gece_turu`). Kapısız bir tasarımda bu testler her
   koşuda prod SharePoint'e `03_VERI_TESLIM/cevap/HUKDOK_TESLIM_*/` altına dosya bırakırdı.
   Anahtar kapısı G107/G108'i (anahtar hiç açılmıyor), env kapısı G109'u (fixture env'i
   siliyor) korur; ikisi birden gelecekte `.env`'e klasör adı yazıldığında da G107/G108'i
   korumaya devam eder. Mevcut testleri değiştirmek sözleşme gereği yasaktı; conftest kapsam
   dışıydı. Bu görevin testleri env'i açıkça kurar, anahtarı açar ve upload'ı sahteler.
2. **`_csv_yaz` deseni KOPYALANDI** (görev: "private ise deseni kopyala ve raporda not düş"):
   4 satır; `test_csv_bicimi_hukdok_aktarim_csv_yaz_ile_bayt_bayt_ayni` iki fonksiyonun
   çıktısını bayt düzeyinde eşitler — biçim ayrışırsa test kırmızı.
3. **Sebep `satir_no` ile eşlenir, SistemNo ile değil:** aynı SistemNo dosyada iki kez
   geçebilir; ilk satır HATA ile düşüp föy ikinci satırdan yazılırsa CSV'de ilk satır
   `ESLESTI` + sebep dolu, ikincisi temiz görünür (testli). Kaynak rapor dizinindeki EN YENİ
   `satir-raporu_*.csv` (ad zaman damgalı); dosya yoksa/okunamazsa sebep boş, eşleşmeyen için
   "satır raporunda kayıt yok".
4. **`durum_gecmisi` notu durum DEĞİŞMEDEN düşer** (`{"durum": "uygulandi", "at", "not":
   "cevap yükleme denemesi #N: k/n dosya → <klasör>[; hatalar: …]"}`); deneme sayısı bu
   önekten sayılır. Env/anahtar kapalı atlama deneme SAYILMAZ (deftere yazılmaz — her gece
   satır şişmesin, kurulum eksiği panelde `cevap_yuklendi=false` olarak zaten görünür).
5. **Aynı gece iki deneme yok:** `gece_turu` sonundaki tarama `ozet["uygulanan"]`ı hariç
   tutar — `teslim_uygula` az önce denedi, retry ertesi geceye kalır (görev metni). `ozet`
   dönüş şekline anahtar EKLENMEDİ (G109 testi kapalı-anahtar şeklini birebir kilitliyor);
   cevap sonucu INFO satırında.
6. **Eşleşme dosyası rapor dizinine yazılır** (`eslesme_<teslim>.csv`) — spool'da kalıcı,
   G108 rapor listeleme/indirme uçları kendiliğinden gösterir, yükleme oradan yapılır.
   `ozet.txt` yüklenirken `ozet_<teslim>.txt` adını alır (plan §2.4 adı); diğer dosyalar
   kendi adlarıyla (`kuru-kosu-ozeti.txt`/`uygulama-ozeti.txt` dahil — küçük, fazlası zarar
   değil).
7. **Ara klasör doğrulaması (görev: "doğrula ve raporda yaz"):** kodda ayrı "klasör yarat"
   çağrısı YOK; `_upload_with_token` küçük dosyada `PUT /drives/{drive}/root:/{klasör}/{ad}:/content`
   atar. `cevap/<teslim>/` alt klasörlerinin açılması Graph'ın yol-adresli PUT davranışına
   dayanır (Graph belgesi). Kod tabanında bu davranışa yaslanan başka çağrı BULUNAMADI:
   mevcut yüklemeler düz klasöre gider (`01_HAM_ARSIV`, `02_YEDEK_ARSIV`), `use_date_subfolder`
   hiçbir çağıranda açık değil. Yani "mevcut davranış" iddiası koddan doğrulanamadı;
   gece oturumu Graph'a ulaşamaz → gerçek SharePoint'te ilk cevap yüklemesi insan adımı.
8. **Döngüsel import:** `teslim_cevap` → `teslim_kutusu` modül düzeyinde; ters yön
   (`_cevap_dene`, `_bekleyen_cevaplari_yukle`) fonksiyon içinde import — monkeypatch
   (`teslim_cevap.cevap_yukle`) yine görülür (testli).

### Test sonuçları (konteynerde)

- `pytest`: **2269 passed, 3 skipped** (önce 2249+3; +20 yeni). G107/G108/G109 dosyaları
  değişmeden yeşil (138 test).
- `ruff check .`: All checks passed. `mypy`: Success, 53 dosya (3 `Column[str]` arg-type +
  2 generator uyarısı ilk turda düzeltildi — `str(x) if x else None`, tipli liste).
- `git diff -- backend/scripts/hukdok_aktarim.py backend/sharepoint/sharepoint_uploader_graph.py`:
  0 satır.
- Kapsam dışı kirli dosyalar (`.claude/settings.local.json`, `.claude/launch.json`) oturum
  başında da kirliydi (araç dosyaları); dokunulmadı, commit'e alınmadı.

### İzlenecekler

- **Prod kurulum (insan):** `.env`'e `SHAREPOINT_FOLDER_TESLIM_NAME=03_VERI_TESLIM` yazılıp
  `docker compose up -d` (recreate) — yazılmazsa cevap paketi HİÇ yüklenmez (INFO
  "SHAREPOINT_FOLDER_TESLIM_NAME tanımsız"), defterde `cevap_yuklendi=false` kalır.
  SharePoint'te `03_VERI_TESLIM/cevap` klasörü ve veri ekibine paylaşım (plan insan adımı).
- **İlk gerçek cevap yüklemesi** logda "cevap paketi yüklendi: N dosya → …" satırı ve
  SharePoint'te `cevap/<teslim>/` klasörünün açıldığı gözle doğrulanmalı (karar 7: ara
  klasör davranışı koddan kanıtlanamadı). Açılmazsa hata WARNING olarak her gece tekrar
  eder; `list_folder_children` deseniyle klasörü önce yaratan küçük bir ek gerekir.
- G111 paneli `durum_gecmisi`ni gösteriyorsa "cevap yükleme denemesi #N" notları
  `uygulandi` durumuyla gelir (durum tekrarı — olay kaydı, geçiş değil).
- Cevap klasörü SharePoint'te `03_VERI_TESLIM/cevap/HUKDOK_TESLIM_X/` (uzantısız ad).
  Aynı dosya adıyla ikinci teslim `yinelenen` olduğu için klasör çakışması beklenmez; farklı
  içerik aynı adla gelirse (nadir) aynı klasöre üstüne yazılır (Graph PUT replace).
