# G073 — Takip/kart alan yerleşimi: aynı kavram iki ekrana bölünmüş

- **Bant:** backend
- **Bağımlı:** - (G072 ile paralel koşabilir; G074 ikisini de bekler)
- **Dosya kapsamı:** backend/managers/case_manager.py (`TRACKING_FIELDS`),
  backend/managers/stage_decisions.py (yalnız gerekirse `_PHOTO_COLUMNS` yorumu),
  backend/tests/test_g073_alan_yerlesimi.py
- **Dokunma:** `cases` ŞEMASI — kolon eklenmez/silinmez/taşınmaz. Bu görev
  **hangi alanın hangi yazma yolundan geçtiğini** düzeltir, veriyi taşımaz.
  `hukdok_aktarim.py`ın kart eşlemesi de bu turda değişmez (aktarım zaten
  kolonlara yazıyor; sorun kolonlarda değil ekran/yol dağılımında).

## Hedef

Bugünkü ayrım şu: **karar künyesi → takip paneli**, **statik dosya bilgisi → kart**.
Fikir doğru, uygulamada üç sapma var (2026-08-19 incelemesi):

| Alan | Bugün nerede | Sorun |
| --- | --- | --- |
| `arabuluculuk_no`, `arabuluculuk_karar_tarihi` | Kart (`PROCESS_CARD_FIELDS`) | Arabuluculuk davanın **ön aşamasıdır** (435 föy; 148 kartta dava + arabuluculuk aynı kartta birleşti). Kart alanı olarak durunca zaman çizgisinin ilk adımı ekranda başka yerde kalıyor |
| `arsiv_tarihi` | Kart | Dosyanın **kapanış olayı**. Takip panelinde `KESINLESME`/`KAPALI` aşamaları var; arşiv onların devamı. Kartta durunca yaşam çizgisi ikiye bölünüyor |
| `dosya_son_durumu` | **İkisinde de** | `TRACKING_FIELDS` içinde (panel yazıyor) ve 2026-08-19'da kartın "Büro Bilgileri" grubuna okuma amaçlı eklendi. Aynı veri iki ekranda |

Dördüncü sapma (`istinaf_basvuran_taraf` kart ile aşama fotoğrafı arasında iki
yazıcıydı, ikinci koşuda 5 kart salınıyordu) **2026-08-19'da kapatıldı** — kart
eşlemesinden çıkarıldı, değer aşama satırına besleniyor. Bu görev kalan üçünü
aynı disiplinle kapatır.

## Karar noktaları (işçi karar verir ve GEREKÇELENDİRİR)

1. **Arabuluculuk: `TRACKING_FIELDS`e mi, aşama satırına mı?** İki seçenek var:
   (a) iki kolonu `TRACKING_FIELDS`e alıp panelde "Arabuluculuk" adımı olarak
   göstermek — küçük diff, veri yerinde kalır; (b) arabuluculuğu bir
   `case_stage_decisions` aşaması yapmak — kavramsal olarak daha doğru ama
   `DECISION_STAGES` kapalı kümesini (G062: YEREL/ISTINAF/TEMYIZ/KARAR_DUZELTME)
   büyütmeyi ve dört kapalı havuza beşincisini eklemeyi gerektirir.
   **Öneri: (a)** — (b) ayrı bir karar (ADR) hak eder, bu görevde kapsam dışı.
2. **`arsiv_tarihi` takip alanı olunca kartta kalsın mı?** Öneri: **kalmasın**
   (G074 karttan kaldırır). Aynı değerin iki ekranda görünmesi bugünkü
   `dosya_son_durumu` sorununun ta kendisi.
3. **`dosya_son_durumu` tekilleşmesi hangi yönde?** Öneri: **takipte kalır**,
   karttan çıkar (G074). Panel onu zaten YAZIYOR; kart yalnız okuyor — okuma
   kopyası, çakışan iki kaynak izlenimi veriyor.
4. **Aktarım tarafı:** `hukdok_aktarim.KART_ALANLARI` bu üç kolona yazmaya devam
   eder (kolonlar yerinde kalıyor). Ama `TRACKING_FIELDS`e giren bir alanın
   aktarımdan da yazılması İKİ YAZICI mı sayılır? Hayır — `_PHOTO_COLUMNS`
   türetmesi gibi bir senkron yok, kolon serbest. Yine de görev raporunda
   AÇIKÇA gerekçelendir; ikinci yazıcı sınavı bu projede tekrar eden hata sınıfı.

## Kabul kriterleri

- [ ] `arabuluculuk_no`, `arabuluculuk_karar_tarihi`, `arsiv_tarihi`
      `TRACKING_FIELDS` içinde; takip paneli yazma yolundan güncellenebiliyor
- [ ] `TRACKING_FIELDS` ile zorunlu alan kümesinin kesişimi BOŞ kalmaya devam
      ediyor (`case_manager` bekçisi — mevcut test yeşil)
- [ ] `update_case_tracking` bu üç alanı yazınca `case_history` DAVRANIŞI
      değişmedi (bugün yazmıyor; G065'in işi — bu görev onu ÇÖZMEZ, sadece
      bozmadığını gösterir)
- [ ] Aktarım koşusu bu üç alanı yazmaya devam ediyor ve **ikinci koşu hâlâ 0
      değişiklik** (idempotency regresyonu yok) — sentetik pakette test
- [ ] Görev raporunda "ikinci yazıcı doğdu mu?" sorusu üç alan için tek tek
      cevaplanmış

## Doğrulama

```bash
docker compose exec -T backend python -m pytest tests/test_g073_alan_yerlesimi.py
docker compose exec -T backend python -m pytest
docker compose exec -T backend python -m ruff check .
docker compose exec -T backend python -m mypy
```

## Rapor

**Durum: TAMAM** (2026-08-19, lokal). Kapılar: `pytest` **1648 passed, 3 skipped**
(G073 öncesi 1634 → +14), `ruff check .` temiz, `mypy` 43 dosyada temiz.

### Karar noktalarının cevabı

1. **Arabuluculuk → `TRACKING_FIELDS` (seçenek a).** Seçenek (b) —
   arabuluculuğu bir `case_stage_decisions` aşaması yapmak — `DECISION_STAGES`
   kapalı kümesini (G062) VE dört G060 karar havuzuna beşincisini eklemeyi
   gerektirirdi; arabuluculuğun "karar durumu" havuzu diye bir şey henüz yok,
   yani liste uydurmak zorunda kalırdık. Ayrı bir ADR hak eder. Kapalı kümenin
   büyümediği testle kilitlendi.
2. **`arsiv_tarihi` takipte, karttan çıkacak** — G074 kaldırır (bu turda
   backend'de kartın yazma yolu zaten yoktu, aşağıya bak).
3. **`dosya_son_durumu` takipte kalır**, kart kopyası G074'te düşer. Panel onu
   zaten YAZIYOR; kart 19.08'de yalnız okuma amaçlı basmıştı.
4. **Aktarım tarafı değişmedi** — `KART_ALANLARI` üç kolona yazmaya devam
   ediyor; testle kilitlendi.

### "İkinci yazıcı doğdu mu?" — üç alan için tek tek

Bu projede tekrar eden hata sınıfı; üçü de tek tek sınandı.

| Alan | Yazan yollar | İkinci yazıcı mı? |
| --- | --- | --- |
| `arabuluculuk_no` | takip paneli (yeni) + aktarım | **Hayır** |
| `arabuluculuk_karar_tarihi` | takip paneli (yeni) + aktarım | **Hayır** |
| `arsiv_tarihi` | takip paneli (yeni) + aktarım | **Hayır** |

Gerekçe üç ayaklı, üçü de ölçüldü:

* **Kart yazmıyor.** `CaseCreate` (kartın PUT gövdesi) bu üç alanı HİÇ
  içermiyor — kolonlar G044'te açıldı ama yazma yolu bilinçli açılmamıştı.
  Frontend'deki `PROCESS_CARD_FIELDS`/`OFFICE_CARD_FIELDS` grupları da
  dosyanın kendi başlığıyla **SALT OKUNUR** (`caseCardFields.ts:8`). Yani
  "karttan takibe taşındı" ifadesi GÖRÜNTÜ yerini anlatıyor, yazma yolu
  devrini değil: bu üç alanın bugüne kadar hiç etkileşimli yazıcısı yoktu,
  şimdi tam olarak BİR tane var.
* **Aşama fotoğrafı dokunmuyor.** Üçü de `_PHOTO_COLUMNS` hedefi DEĞİL, yani
  `_resync_stage_photo` bu kolonlara yazmıyor — `istinaf_basvuran_taraf`ın
  19.08'de kapatılan kusuru tekrar etmiyor. Takip × fotoğraf kesişimi hâlâ
  yalnız 24 karar künyesi kolonu (G062'nin bilinçli bıraktığı, G065'in
  birleştireceği küme); test bu kümeyi tam liste olarak dondurdu, büyürse söyler.
* **Aktarım senkron DEĞİL.** `_PHOTO_COLUMNS` gibi "bir tablodan türet" bağı
  yok; aktarım toplu yükleyici, panel tekil düzeltme yolu. Biri diğerinin
  yazdığını türetmediği için ikisi de serbestçe aynı kolona yazabilir —
  idempotency testi ikinci koşunun 0 değişiklik ürettiğini gösteriyor.

### Değişen dosyalar

* `managers/case_manager.py` — `TRACKING_FIELDS`e üç alan (arabuluculuk ön
  aşama olarak listenin başına, arşiv kapanış olayı olarak Kesinleşme/İnfaz
  yanına) + listenin başına yerleşim kuralının yazılı hâli. 39 → 42 alan.
* `schemas.py` — `CaseTrackingUpdate`e aynı üç alan. Zorunluydu: mevcut
  `test_schema_covers_all_tracking_fields` şemada karşılığı olmayan takip
  alanını KIRMIZI yapıyor (alan şemada yoksa route'tan zaten geçemez). Görev
  dosyasının dosya kapsamında yazmıyordu ama kabul kriteri ("takip paneli
  yazma yolundan güncellenebiliyor") bunsuz sağlanamaz.
* `tests/test_g073_alan_yerlesimi.py` — 14 test.

### Testler

Sözleşme: üç alan `TRACKING_FIELDS` + şemada; `dosya_son_durumu` takipte kaldı;
`DECISION_STAGES` büyümedi; fotoğraf hedefi değiller; takip×fotoğraf kesişimi
tam listeyle donduruldu; zorunlu alan kesişimi hâlâ boş; `tracking_changes`
üçünü taşıyor; gönderilmeyen alan dokunulmuyor.
Davranış (sqlite): panel üçünü yazıyor; `None` gönderilen siliniyor,
gönderilmeyen korunuyor; `case_history` **0 satır** (G065'in işi — bu görev
onu çözmez, bozmadığını gösterir); aşama değişince `CaseStageLog` hâlâ düşüyor.
Aktarım (sentetik xlsx): üç alan yazılıyor, **ikinci koşu 0 alan değişikliği /
0 kart** ve kart fotoğrafı birebir aynı.

Aktarım fixture'ları (`db_env`, `uc_kart`) G064 test modülünden import edildi,
kopyalanmadı: `db_env` pysqlite'ın örtük-commit tuhaflığını kapatan reçeteyi
taşıyor (isolation_level=None + elle BEGIN); ikinci bir kopya sahte
SAVEPOINT'lerle sessizce koşardı.

### Ölçüm — lokal DB (aktarım sonrası, 14.345 aktif kart)

| Alan | Dolu kart |
| --- | --- |
| `arsiv_tarihi` | 3.346 |
| `dosya_son_durumu` | 14.239 |
| `arabuluculuk_karar_tarihi` | 1 |
| `arabuluculuk_no` | 1 |

### İzlenecek — G073'ün DIŞINDA bir bulgu

**Arabuluculuk alanları pratikte BOŞ:** 402 kart `file_type='Arabuluculuk'`
ama `arabuluculuk_no` yalnız 1 kartta dolu (`2023/92352`), karar tarihi hiç.
Aktarımın başlık takma adı tek: `"Arabuluculuk Numarası"` /
`"Arabuluculuk Karar Tarihi"` (`hukdok_aktarim.py:160-161`). İki olasılık var
ve teslim paketi elde olmadan ayırt edilemez: (a) sütun teslimde gerçekten
neredeyse boş, (b) teslimdeki başlık farklı yazılmış ve eşleşmiyor. Bu bir
AKTARIM sorusudur, alan yerleşimi sorusu değil — G073 kapsamında
DEĞİŞTİRİLMEDİ.

> **G076'da kapandı** (2026-08-19): teslim paketi ölçüldü — (a) doğruymuş,
> sütun kaynakta boş (8.409 satırın 1'i). Başlık eşleşmesi ve aktarım DOĞRU
> çalışıyor; "435 föy" ifadesi dosya TÜRÜNÜ anlatıyor, numara sütununu değil. G074'te panelde "Arabuluculuk" bölümü açılırsa kullanıcı
neredeyse hep boş bir bölüm görecek; kararı buna göre ver.
