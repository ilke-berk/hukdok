# G198 — Zamanlayıcı pytz'den kurtulur: `zoneinfo` + APScheduler 3.11.3 + import hatası yutulmaz

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** `backend/api.py` (yalnız lifespan'deki zamanlayıcı bloğu, ~`:185-253`), `backend/requirements.txt` (yalnız `apscheduler` satırı + gerekirse `tzdata`), `backend/tests/test_*scheduler*.py` / yeni bekçi testi, `docs/mimari/genel-bakis.md` (zamanlayıcı anlatımı varsa)
- **Dokunma:** `Office365-REST-Python-Client` satırı (kaldırılması AYRI iş — Faz 3), `services/deadline_scanner.py`, `managers/activity_manager.py` iş gövdeleri, `services/singleton_lock.py` (lider kilidi)

Kaynak: `docs/arsiv/bagimlilik-raporu-2026-09.md` §5.1; yol haritası Faz 1 (kullanıcı onayı 26.09).

## Hedef
`api.py:192` `import pytz` yapıyor ama `pytz` `requirements.txt`'te YOK; bugün yalnız APScheduler 3.10.4 ve
(kullanılmayan) Office365 üzerinden dolaylı geliyor. APScheduler 3.11 `pytz`'yi bağımlılıktan çıkardı. İkisi de
bırakınca `ImportError` `api.py:252-253`'te "apscheduler yüklü değil" WARNING'iyle YUTULUR → 00:00 rapor,
02:30 dönüşüm retry, 06:00 süre taraması SESSİZCE kapanır. Bu görev:
1. `pytz.timezone("Europe/Istanbul")` → `zoneinfo.ZoneInfo("Europe/Istanbul")` (kodda zaten kullanılan desen:
   `schemas_rapor.py:15`, `services/deadline_scanner.py:73`). Saat dilimi sabiti tek yerde tanımlanır.
2. `apscheduler==3.10.4` → `3.11.3` (4.x YASAK — API baştan değişti).
3. Zamanlayıcı kurulumundaki `ImportError` artık sessiz WARNING değil: tek bir `logging.error` (log sözleşmesi:
   nihai başarısızlık TEK ERROR) + istisna mesajı loga girer. Uygulama açılmaya devam eder (zamanlayıcı yokluğu
   belge akışını durdurmamalı), ama hata görünür olur.
4. `python:3.12-slim` imajında `/usr/share/zoneinfo/Europe/Istanbul` 26.09'da VAR (doğrulandı). Görev bunu
   konteynerde yeniden doğrular; sistem tz verisi yoksa `tzdata` pin'lenir ve gerekçe rapora yazılır.

## Kabul kriterleri
- [ ] `grep -rn "pytz" backend --include=*.py` (tests hariç) boş; `api.py` zamanlayıcı ve üç `CronTrigger` `ZoneInfo("Europe/Istanbul")` kullanır.
- [ ] `requirements.txt`'te `apscheduler==3.11.3`; imaj `docker compose build --progress=plain backend` ile yeniden kurulur, pip adımının KOŞTUĞU log satırıyla rapora yazılır (OneDrive cache tuzağı); `pip show apscheduler` 3.11.3.
- [ ] Yeni bekçi testi: (a) `api.py` kaynağında `pytz` importu yok; (b) zamanlayıcı bloğunda `ImportError` olursa `logging.error` düzeyinde kayıt atılır (WARNING değil) — test monkeypatch ile import'u kırar ve `caplog` ile ERROR'u görür.
- [ ] Lider worker'da üç iş (`daily_activity_report`, `conversion_retry`, `deadline_scan`) kayıtlı ve `next_run_time` TR saatiyle 00:00 / 02:30 / 06:00 — konteyner loglarından ya da testle kanıt rapora.
- [ ] Tam pytest + ruff + mypy yeşil; sayılar rapora.

## Doğrulama
- `docker compose build --progress=plain backend && docker compose up -d backend`
- `docker compose exec -T backend pip install -q -r requirements-dev.txt`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data`
- `docker compose exec -T backend python -m mypy`
- `docker logs hukdok_backend --since 2m 2>&1 | grep -iE "scheduler|zamanlay"`

## Rapor
(işçi oturumu doldurur)
