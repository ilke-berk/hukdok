# G134 — Raporlar sayfası: favori şablonlar, Excel/CSV indirme, "İndirme geçmişi" sekmesi

- **Bant:** frontend
- **Bağımlı:** G133 (backend sözleşmesi G131'de; plan §2.4 `/templates`, `/export`, `/runs` dondu)
- **Dosya kapsamı:** frontend/src/lib/reports.ts (şablon/export/koşu fonksiyonları), frontend/src/pages/ReportsPage.tsx
  (sekmeler + araç çubuğu), frontend/src/components/reports/TemplateBar.tsx, .../SaveTemplateDialog.tsx,
  .../RunsTable.tsx, .../ExportButtons.tsx, ilgili `*.test.tsx`
- **Dokunma:** frontend/src/App.tsx, frontend/src/components/shell/Sidebar.tsx, frontend/src/lib/api.ts
  (hub — G133 bitirdi, dokunma), frontend/src/components/reports/ReportBuilder*.tsx (yalnız prop ekle)

## Hedef

Kullanıcı hazırladığı rapor tanımını **adıyla kaydeder**, sonra listeden seçip yükler, günceller, siler,
paylaşımlı işaretler; **Excel** ya da **CSV** indirir; "İndirme geçmişi" sekmesinde kimin ne zaman hangi
raporu aldığını görür ve saklanan çıktıyı yeniden indirir. Backend sözleşmesi plan §2.4-2.5 (G131).

## Yapılacaklar

1. **`lib/reports.ts`:** `listTemplates/createTemplate/updateTemplate/deleteTemplate`, `exportReport(tanim,
   format, sablonId, kaynak)` → blob + `Content-Disposition` dosya adı (regex `AdminPage.tsx:427-429`) +
   `X-Rapor-Kosu-Id`; 413 gövdesi (`sebep: satir_limiti`, `toplam`, `limit`) okunur hataya çevrilir;
   `listRuns(limit, offset)`, `downloadRun(id)` (410 → "çıktı saklama süresi dolmuş").
2. **Sekmeler (`ReportsPage`):** `Tabs` (shadcn) — "Rapor" (G133 oluşturucu + önizleme) · "Şablonlar" ·
   "İndirme geçmişi"; sekme `?tab=` ile URL'de (`AdminPage.tsx:173-187` deseni).
3. **TemplateBar (Rapor sekmesi üstü):** şablon `Select` (kendi + paylaşımlı, paylaşımlıda sahibi rozeti),
   "Yükle" tanımı oluşturucuya koyar (veri kaynağı değişince kolonlar sıfırlanır — kullanıcıya uyarı), "Kaydet"
   (yeni ad → `SaveTemplateDialog`: ad, açıklama, paylaşımlı `Switch`), "Güncelle" (yalnız sahibi), "Sil"
   (`useConfirm`, yalnız sahibi). Şablonlar sekmesi: aynı listenin tablosu (ad, sahibi, paylaşım, güncellenme,
   işlemler; sağa sabit işlem kolonu `AdminPage.tsx:107` kalıbı).
4. **ExportButtons (önizleme araç çubuğu):** "Excel indir" / "CSV indir" — indirme sırasında spinner,
   başarıda `toast.success` (dosya adı + satır sayısı), 413'te tavan mesajı ve filtre daraltma önerisi;
   `kaynak:"manuel"`. Blob indirme kalıbı `AdminPage.tsx:417-444`.
5. **RunsTable (İndirme geçmişi):** sunucu sayfalı liste — zaman (`dd.MM.yyyy HH:mm`), kullanıcı, şablon adı
   (yoksa "—"), veri kaynağı, format, kaynak (manuel/asistan rozeti), satır/kolon sayısı, dosya adı + boyut,
   "İndir" (`dosya_mevcut` değilse pasif + tooltip "saklama süresi doldu"), "Tanımı yükle" (koşunun `tanim`ını
   oluşturucuya koyar → Rapor sekmesine geçer). Hata ≠ boş liste.
6. **Test:** şablon kaydetme gövdesi ve listede görünmesi; başkasının şablonunda Güncelle/Sil düğmeleri yok;
   Excel indirme `/api/reports/export`'a `format:"xlsx"` + `kaynak:"manuel"` gönderir ve `<a download>` dosya adı
   `Content-Disposition`'dan gelir; 413 mesajı; İndirme geçmişi satırları render + `dosya_mevcut=false`
   düğmesi pasif; "Tanımı yükle" oluşturucu state'ini günceller.

## Kabul kriterleri

- [ ] Export gövdesi plan §2.4 ile birebir (`tanim`, `format`, `sablon_id`, `kaynak`) (test).
- [ ] İndirilen dosya adı sunucunun `Content-Disposition`'ından alınır, istemci ad uydurmaz (test).
- [ ] Şablon sahibi olmayan kullanıcı için Güncelle/Sil UI'da yok; paylaşımlı rozeti görünür (test).
- [ ] İndirme geçmişi kullanıcı + zaman + format + satır sayısı + dosya durumu gösterir; 410 okunur mesaj (test).
- [ ] Şablon yükleme, oluşturucudaki veri kaynağı/kolon/filtre state'ini tam değiştirir (test).
- [ ] Hub dosyalara dokunulmadı (`git diff --stat` raporda).
- [ ] `npm test`, `lint`, `tsc -b --force` temiz.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

**DURUM: TAMAM** — 2026-09-06 gece koşusu, worktree `gorev/G134` (taban `380b74e`).

### Yapılanlar

- **`lib/reports.ts` (+185):** `listTemplates` / `createTemplate` / `updateTemplate` (403 → sahiplik mesajı) /
  `deleteTemplate` (204, 403); `exportReport(tanim, format, sablonId, kaynak)` → `{blob, dosyaAdi, kosuId}`
  (gövde `{tanim, format, sablon_id, kaynak}` §2.4 birebir; ad `Content-Disposition`'dan — `dispositionDosyaAdi`
  tırnaklı/tırnaksız/RFC 5987 `filename*=UTF-8''` biçimlerini okur; `X-Rapor-Kosu-Id` sayıya çevrilir); 413
  `satir_limiti` zaten `raporHatasiCevir`'de ("… (120.000 satır, limit 50.000). Filtre daraltın."); `listRuns(limit,
  offset)`, `downloadRun(id)` (410 → `RAPOR_KOSU_SURESI_DOLDU`); `dosyayiIndir` (`<a download>`, AdminPage kalıbı);
  `sablonSahibiMi` (küçük harf e-posta karşılaştırması — sunucu `require_admin` `preferred_username|upn|email`'i
  lower'lar, istemci MSAL `username`); `tarihSaatBicimle` (`dd.MM.yyyy HH:mm`, yerel), `boyutBicimle`, etiket tabloları.
- **`ReportsPage.tsx`:** Radix `Tabs` — "Rapor" · "Şablonlar" · "İndirme geçmişi"; `?tab=` girişte okunur
  (`resolveTab`, AdminPage `ADMIN_TABS` deseni) VE sekme değişince `setSearchParams(replace)` ile yazılır
  (varsayılan sekmede parametre silinir). Şablon listesi katalogdan SONRA çekilir (G133 testi `calls[0]`'a bakar);
  liste hatası çubukta satır-içi not + "Yeniden dene" (toast yok, `role=alert` yok — sayfanın tek alert'i önizleme
  şeridi). Geçmiş yalnız sekme açıkken çekilir; export sonrası `kosuSurumu` artar → sekme açılınca yeniden çekilir.
  `tanimiYukle`: veri kaynağı katalogda yoksa reddeder; kaynak değişiyorsa `confirm` (warning) sorar; `tanimdanDurum`
  filtre satırlarına React key verir. Export'a `sablon_id` yalnız taslak yüklü şablonla BİREBİR aynıyken yazılır
  (geçmişte "şablon adı" yanıltmasın). Onay diyaloğu `ConfirmContext` üzerinden; provider yoksa (izole render)
  `window.confirm` yedeği — G133 testleri provider'sız render eder, `useConfirm()` fırlatırdı.
- **`TemplateBar.tsx`:** select (kendi + paylaşımlı; başkasınınki "ad · sahibi"), seçilide paylaşım rozeti
  (paylaşımlı+başkası → sahibinin e-postası), Yükle · Kaydet (yeni ad diyaloğu) · Güncelle/Sil **yalnız sahibi**
  (aksi halde DOM'da hiç yok). Aynı dosyada `TemplatesTable` (Şablonlar sekmesi): ad+açıklama, sahibi ("Siz"),
  paylaşım rozeti, veri kaynağı, güncellenme, sağa sabit işlem kolonu (`STICKY_ACTIONS` kalıbı) — yükle / düzenle
  (künye diyaloğu) / sil yalnız sahibi.
- **`SaveTemplateDialog.tsx`:** ad (zorunlu, ≤120) + açıklama (≤500) + paylaşımlı `Switch`; mod `yeni`
  (tanım = taslak) / `duzenle` (tanım şablonunki, künye değişir). Başlangıç künyesi ref'te — yalnız açılışta forma
  yazılır (üst bileşen her render'da yeni nesne üretir; efekt bağımlılığında olsaydı yazılan form sıfırlanırdı).
- **`ExportButtons.tsx`:** "Excel indir" / "CSV indir" — spinner, `toast.success("İndirildi: <ad> · N satır")`
  (N önizlemeden, bayat değilse), 413'te ayrı başlık ("… tavanını aşıyor") + mesaj; `kaynak:"manuel"` sabit.
  `PreviewTable`'a tek `araclar?: ReactNode` yuvası eklendi (G133 raporu bunu öngörmüştü).
- **`RunsTable.tsx`:** zaman · kullanıcı · şablon (yoksa "—") · veri kaynağı · format rozeti · kaynak rozeti
  (manuel nötr / asistan marka) · satır/kolon · dosya adı + boyut (+ "süresi doldu" işareti) · İndir (`dosya_mevcut`
  değilse `disabled` + `title` "Saklama süresi doldu — …") · Tanımı yükle; sunucu sayfalı (limit 50, Geri/İleri
  yalnız toplam > limit'te); hata → `DataErrorBanner` (boş liste değil). 410 alan satır yerinde pasifleşir.

### Kararlar

- **Çubuk "Güncelle" ≠ tablo "Düzenle":** çubuktaki Güncelle seçili şablonun TANIMINI taslakla değiştirir (künye
  aynı, `confirm` info); tablodaki Düzenle künyeyi (ad/açıklama/paylaşım) diyalogla değiştirir, tanıma dokunmaz.
  İkisi de PUT tam gövde (§2.4 "kısmi değil").
- Tooltip'ler native `title` (TooltipProvider'a bağımlılık yok; jsdom'da okunur).
- `dosyayiIndir` sunucu adı yoksa `hukdok-rapor.<format>` yedeğine düşer (proxy başlığı sıyırırsa uzantı doğru
  kalsın); normal yolda hiç kullanılmaz — test "başlık kazanır"ı doğrular.
- Şablon listesi hatasında toast atılmaz: katalog toast'ını gölgelemesin, G133 "katalog hatası toast 1 kez" testi
  aynen geçer.

### Bileşen ağacı

```
ReportsPage
├─ Eyebrow + h1
├─ Tabs (value ↔ ?tab=)
│  ├─ TabsContent rapor
│  │  ├─ HairlineCard › TemplateBar (select#rapor-sablon · rozet · Yükle/Kaydet/[Güncelle/Sil])
│  │  └─ section.grid-cols-1.xl:grid-cols-[320px_1fr]
│  │     ├─ HairlineCard › ReportBuilder (G133, dokunulmadı)
│  │     └─ HairlineCard › PreviewTable(araclar=<ExportButtons/>)
│  ├─ TabsContent sablonlar › HairlineCard › TemplatesTable
│  └─ TabsContent gecmis › HairlineCard › RunsTable
└─ SaveTemplateDialog (portal)
```

### Testler

- `frontend/src/lib/reports.export.test.ts` — 14 test: şablon uçları (gövde anahtarları birebir, PUT tam gövde,
  403/204), `sablonSahibiMi`, export gövdesi + `Content-Disposition`/`X-Rapor-Kosu-Id`, `sablon_id:null`,
  413 mesajı, `dispositionDosyaAdi` biçimleri, `listRuns` sorgusu, `downloadRun` + 410, `dosyayiIndir`
  `<a download>` adı, tarih-saat/boyut biçimi.
- `frontend/src/pages/ReportsPage.sablon.test.tsx` — 13 test: kaydetme diyaloğu → POST gövdesi + select + tablo;
  başkasının şablonunda Güncelle/Sil yok + rozet (çubuk ve tablo); Excel indir gövdesi (`format:"xlsx"`,
  `sablon_id:null`, `kaynak:"manuel"`) + `<a download>` adı başlıktan; CSV + yüklü şablon `sablon_id:1` + toast
  satır sayısı; 413 tavan mesajı (dosya inmez); geçmiş satırları (kullanıcı/zaman/format/satır/dosya durumu),
  `dosya_mevcut=false` pasif + ipucu, İndir adı başlıktan; 410 mesajı + satır pasifleşir; liste hatası
  DataErrorBanner + Tekrar dene; "Tanımı yükle" state'i tümüyle değiştirir (kaynak/kolon/filtre) + Rapor sekmesi +
  URL; onay reddi state'i korur, aynı kaynakta onay yok; Sil DELETE + seçim temizlenir; Güncelle PUT tam gövde;
  `?tab=` giriş + geçersiz değer.
- Koşu (HOST, worktree): `vitest run` **58 dosya / 692 test geçti** (önce 665 → +27, tümü yeşil; G133'ün 13 testi
  değişmeden geçer), `eslint .` temiz (0 uyarı), `tsc -b --force` exit 0, `vite build` başarılı.
- Hub dosyalar: `git diff --stat -- App.tsx Sidebar.tsx api.ts ReportBuilder.tsx` → boş (dokunulmadı).

### İzlenecekler

- Backend G131 uçları bu worktree'de henüz yok (`routes/reports.py` yalnız catalog/preview); gerçek uçlarla duman
  testi gündüz: `Content-Disposition` başlığının konteyner nginx'inden geçtiği ve `X-Rapor-Kosu-Id`'nin CORS'suz
  aynı-origin'de okunduğu doğrulanmalı.
- `olusturan` sunucuda küçük harfli e-posta olarak yazılmalı (G131 sözleşmesi); farklı yazılırsa sahiplik
  rozeti/düğmeleri yanlış çıkar — tek uyum noktası `sablonSahibiMi`.
- G135: `ExportButtons` `kaynak` prop'u sabit "manuel"; asistan akışı `exportReport(..., "asistan")`'ı doğrudan çağırır.
- NOT: `PreviewTable.tsx` kapsam listesinde yoktu; yalnız `araclar` yuvası eklendi (G133 raporunun öngördüğü nokta).
