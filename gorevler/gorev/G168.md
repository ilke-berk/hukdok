# G168 — Rapor asistanı: sonuç odaklı sohbet + sohbetten şablon kaydı + sınırları açıklama

- **Bant:** frontend (+ backend yalnız prompt)
- **Bağımlı:** G167 (prod'da, Deploy #22) + G167 ek yerel onay (main 83b9d6c)
- **Dosya kapsamı:** frontend/src/lib/reportsChat.ts (`kaydetNiyeti`, `sonucSatiri`), components/reports/AssistantBar.tsx
  (`sonucBeklenen`, `onizlemeSonucu`/`onSablonKaydet` prop'ları, kaydet niyeti), pages/ReportsPage.tsx
  (`asistanSablonKaydet`, prop bağlama), backend/prompts.py (reddederken sebep + alternatif), testler
  (reportsChat.test, AssistantBar.test, ReportsPage.asistan.test — **beklenti değiştirme izni VAR**: sonuç satırı balon
  sayılarını artırdı), docs/mimari/raporlama.md §7
- **Dokunma:** sunucu sözleşmesi (`RaporTanimi`, uçlar, olaylar), K6 (asistan veri görmez — sayı önizlemeden)

## Hedef

Kullanıcı kararı (10.09 gece, öneri listesinden üçünü seçti): (1) "kaç kayıt çıktı" ve boş sonuçta gevşetme önerisi
sohbette; (2) "bunu haftalık rapor olarak kaydet" ile şablon kaydı; (3) bot reddederken sebebini kısa yazsın.

## Kabul kriterleri

- [x] Onayla / yerel onay / sözle onay sonrası önizleme gelince "N kayıt bulundu." (tr-TR); 0'da filtre listesi + öneri;
      bir kez ve yalnız uygulanan tanım için; indirmede satır yok.
- [x] `kaydetNiyeti`: adlı 4 kalıp + adsız 8 kalıp; "telefonu kaydet"/"tamam"/"kaydetme" null. Kayıt Gemini'ye gitmez,
      uygulamaz (kart onay beklemeye devam eder), ad yoksa `sablonAdiOner`, liste + seçim güncellenir, toast.
- [x] Prompt: sıralama/ikinci kademe reddinde sebep + alternatif cümlesi.
- [x] Frontend 899 passed / eslint 0 / tsc 0; backend test_g132 51 passed, ruff temiz.

## Rapor

DURUM: TAMAM (2026-09-10 gece, gündüz oturumu — doğrudan uygulandı; prod'a ÇIKMADI)

Sonuç satırı sayfanın zaten aldığı önizleme toplamından üretilir (`ReportsPage` `cevap.toplam` + `sonTanim`); asistan
hâlâ satır görmez. Boş sonuç önerisi filtreleri `tanimAyrintisi` ile sayar, kullanıcı "durum filtresini kaldır" yazınca
Gemini bekleyen tanımı günceller (G167 düzeltme yolu). Şablon kaydı `favoriEkle` ile aynı gövdeyi gönderir (tek yol).
Bilinçli: kaydetmek uygulamak DEĞİLDİR (kart onay beklemeye devam eder); kaydedilen tanım bekleyen tanımdır, yoksa
oluşturucudaki. Gemini duman testi yapılmadı (prompt cümlesi mevcut kuralları daraltmıyor, genişletiyor).
