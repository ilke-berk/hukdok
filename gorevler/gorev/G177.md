# G177 — Sohbet öncelikli rapor ekranı: raporlama.md §7/§8/§9 yeniden yazımı + CLAUDE.md paragrafı + plan şerhi + G169-G172 arşivi (koddan doğrulanmış)

- **Bant:** docs
- **Bağımlı:** G175,G176
- **Dosya kapsamı:** docs/mimari/raporlama.md (giriş ağaç şeması, §1 bileşen tablosu, §7 asistan — otomatik uygulama/değer
  eşleme/liste balonu, §8 tamamı, §9 anahtar — "kapalıyken şerit yedek", §12 farklar, §13 sınırlar, §15 test satırı, §16 nereye
  bakmalı), CLAUDE.md ("Raporlama (G130-G139)" paragrafı — ekran cümlesi + asistan cümlesi), docs/plan/raporlama-plani-2026-09-06.md
  (başa "uygulamada değişti: sohbet öncelikli ekran, G173-G177" şerhi; §4.1/§6.1/§8 gövdeleri silinmez), gorevler/gorev/G169.md,
  G170.md, G171.md, G172.md → docs/arsiv/gorevler/ (`git mv`; her dosyanın başına "İPTAL 11.09 — kullanıcı kararı: sohbet
  öncelikli ekran (G173-G177); koşulmadı" şerhi), docs/arsiv/gorevler/README.md (dört satır)
- **Dokunma:** gorevler/KUYRUK.md (DOKUNMA — runner yazar), backend/frontend kodu (sıfır)

## Hedef

G173-G176 ekranı "kaynak kartı + filtre şeridi + kolon paneli"nden "sohbet → tanım şeridi → tablo"ya çevirdi ve asistanın
teyit döngüsünü otomatik uygulamaya döndürdü. Doküman bunu anlatmalı. ALTIN KURAL: her iddia koddan okunarak yazılır
(dosya/işlev adı + satır), tahmin yazılmaz; kodla çelişen her eski cümle düzeltilir. Docs bandı, test yok.

## Kabul kriterleri

- [ ] Giriş ağaç şeması: `AssistantBar` (ya da kapalı kartı) → `TanimSeridi` → sayaç/şablon/indirme satırı → `PreviewTable`;
      gerçek bileşen/dosya adları `ls frontend/src/components/reports` ile doğrulanmış; kaldırılanlar §1 tablosunda
      "KALDIRILDI (G175)" şerhiyle (SourceCards, QuickFilters, ColumnSheet, ColumnPicker, gerekirse ui/sheet).
- [ ] §7 asistan: otomatik uygulama kuralı (`AssistantBar` complete işleyicisi, satır), `degerEsle` (kural: contains alt dize /
      eq birebir, aday ≤5, normalize), `listeNiyeti` + `DegerListesi`, `onFiltreEkle` yolu, K6'nın nasıl korunduğu (öneriler
      katalogla istemcide), prompt'un üç yeni cümlesi (`prompts.py` satırları), G167 kartının hangi durumda kaldığı.
- [ ] §8 yeniden: 8.1 yerleşim, 8.2 tanım şeridi (7 öğe, prop sözleşmesi, `builderState` yardımcıları), 8.3 çip düzenleme
      popover'ı (FilterControl yeniden kullanımı, kontrol→op tablosu AYNEN — `lib/reports.ts` satırları yeniden okunur),
      8.4 otomatik önizleme (değişmediyse "değişmedi" + güncel satır numaraları), 8.5 önizleme tablosu ve sıralama.
- [ ] §9 anahtar: "kapalıyken sayfa boş kalmaz — bilgi kartı + şerit yedek kurucu" (ReportsPage satırı); prod'da anahtarın
      AÇIK olduğu notu (Deploy #21) ve "artık ekranın birincil yolu" uyarısı.
- [ ] §12 farklar: yeni tur satırları (teyit döngüsü geri alındı — sebebi; manuel kurucu kaldırıldı; üç aşama planı iptal).
- [ ] §13 sınırlar: "aday eşleme yalnız `oneriler` taşıyan kolonlarda; 300 kesik listede aday bulunamayabilir", "liste balonu
      yalnız kalıp eşleşince (Gemini'ye gitmez)", "Gemini/anahtar kapalıyken sohbet yok — şeritle çalışılır", "sohbet geçmişi
      sayfa yenilemede sıfırlanır (değişmedi)".
- [ ] §15 test satırı: G173-G175 test dosyaları + toplam sayı (koşarak: `npm --prefix frontend test` özet satırı rapora;
      dokümana sayı + tarih); backend test_g132 sayısı.
- [ ] CLAUDE.md raporlama paragrafı: "kaynak kartı + katalogdan gelen filtre şeridiyle (operatör seçici yok…)" cümlesi ve
      "AI asistan … anahtarı ister" cümlesi yeni ekranı anlatacak şekilde değişir; başka satır dokunulmaz.
- [ ] Plan dosyasının başına "uygulamada değişti" şerhi (tarih + görev id'leri); G169-G172 dosyaları `git mv` ile arşivde,
      başlarında iptal şerhi; arşiv README'sinde dört satır.
- [ ] Doküman "Son doğrulama" başlık satırı yeni commit SHA + tarih ile güncellenir.

## Doğrulama

- `npm --prefix frontend test` (yalnız sayı için; kod değişmez)
- `git diff --stat` yalnız docs/ + CLAUDE.md + gorevler/gorev (mv)

## Rapor

DURUM: TAMAM (2026-09-11 gece, worktree `gorev/G177`, taban `c839fdb`; docs bandı)

### Yapılanlar

- `docs/mimari/raporlama.md` — koddan yeniden okunarak yazıldı (her iddia dosya/işlev + c839fdb satır numarası):
  başlık + "Son doğrulama" bloğu (hangi bölümlerin bu turda, hangilerinin a36e98f/G166'da doğrulandığı açık; eski
  "§11'de" iç tutarsızlığı §12'ye düzeltildi), giriş paragrafı (sohbet öncelikli tur) + ağaç şeması
  (`AssistantBar`/kapalı kartı → `TanimSeridi` → sayaç/şablon/indirme → `PreviewTable`, `ReportsPage.tsx:740-852`),
  §1 bileşen tablosu (güncel dosya listesi `ls` ile; KALDIRILDI (G175): SourceCards, QuickFilters, ColumnSheet,
  ColumnPicker, ui/sheet.tsx), §7 (asistan.py satırları 274-334'e taşındı; G176 üç prompt cümlesi `prompts.py:522-535`;
  yeni maddeler: otomatik uygulama `AssistantBar.tsx:244-270`, değer eşleme `degerEsle` kuralı contains alt dize /
  eq birebir, aday ≤5, `degerAnahtari` normalize, liste balonu `listeNiyeti` + `DegerListesi`, `onFiltreEkle` yolu,
  K6'nın korunması; G167 kartının hangi durumlarda kaldığı; yerel onay/G168 maddeleri güncel satırlarla),
  §8 tamamı (8.1 yerleşim, 8.2 şerit — 7 öğe + prop sözleşmesi + builderState yardımcıları, 8.3 çip popover'ı +
  kontrol→op tablosu `lib/reports.ts:489-534` yeniden okunmuş satırlarla, 8.4 otomatik önizleme "mekanizma
  değişmedi" + güncel satırlar, 8.5 tablo/sıralama "değişmedi"), §9 (kapalıyken bilgi kartı + şerit yedek kurucu;
  prod'da Deploy #21 ile AÇIK notu — bellek notu, koddan doğrulanamaz diye işaretli; "birincil yol" uyarısı),
  §12 F23-F28 (teyit döngüsü geri alındı — sebebi; manuel kurucu kalktı; üç aşama planı iptal; prompt; istemci
  eşleme), §13 dört yeni sınır + iki NOT + KAPANDI şerhi + duman testi maddesi, §15 test satırları (sayılar dosya
  başına `grep` ile; toplam koşularak), §16.
- `CLAUDE.md` — yalnız "Raporlama" paragrafının ekran cümlesi ve asistan cümlesi (etiket G130-G176); G166 cümleleri
  ve diğer paragraflar dokunulmadı.
- `docs/plan/raporlama-plani-2026-09-06.md` — başa "uygulamada değişti — sohbet öncelikli ekran (11.09; G173-G177)"
  şerhi; §4.1/§6.1/§8 gövdeleri SİLİNMEDİ (tarihsel).
- `gorevler/gorev/G169-G172.md` → `docs/arsiv/gorevler/` (`git mv`, her dosyanın başına "İPTAL 11.09 — kullanıcı
  kararı: sohbet öncelikli ekran (G173-G177); koşulmadı" şerhi); `docs/arsiv/gorevler/README.md`'ye dört satırlık
  bölüm.

### Kararlar + gerekçe

- "Son doğrulama" SHA'sı olarak kodun okunduğu HEAD (`c839fdb`) yazıldı — doküman commit'i kendi SHA'sını bilemez;
  blokta "kod HEAD'i, bu doküman commit'i onun üstündedir" diye açıklandı. Bu turda yeniden okunmayan bölümlerin
  (§2-§6, §10, §11, §14) satır numaraları önceki commit'e ait olarak bırakıldı ve blokta açıkça söylendi.
- Kontrol→op tablosuna kodda var olan `var_yok`/`bos_anahtari` (§5.3 sunum kontrolleri) satırları eklendi — "AYNEN"
  kuralı sunucu sözleşmesi içindir; tablo koddan okunduğu için eksik satırlar tamamlandı.
- İkinci tur fark satırları (F16-F21) silinmedi; silinen dosyalara atıf yaptıkları tek cümleyle işaretlendi.
- Prod anahtar durumu koddan doğrulanamaz; §9'a "bellek notu, `GET /api/admin/settings` ile bakılır" şerhiyle yazıldı.

### Doğrulama (docs bandı)

- `npm --prefix frontend test` worktree'de KOŞTU (main'in `node_modules`'ü junction ile bağlandı, iş bitince
  kaldırıldı): **916 passed / 69 dosya**, exit 0 (vitest 4.1.11, 27,8 sn) — G175 raporuyla aynı.
  Dosya başına: TanimSeridi 14, builderState 18, AssistantBar 16, reportsChat 42, ReportsPage 18, asistan 23,
  sablon 13, favori 12 (`grep -cE '^\s*(it|test)\('`). Backend: `test_g132_rapor_asistani.py` 30 test fonksiyonu
  (`grep`); konteynerde koşulmadı (docs bandı, worktree) — G176 raporu 3292 passed / 3 skipped alıntılandı.
- İç tutarlılık: yeniden yazılan bölümlerdeki her satır numarası `cat -n`/`sed -n` ile okunarak yazıldı; kalan
  `SourceCards/QuickFilters/ColumnSheet/ColumnPicker/sheet.tsx` geçişleri yalnız tarih/KALDIRILDI bağlamında
  (grep ile sayıldı); göreli linkler (`../plan/...`, `dis-bagimliliklar.md`, arşiv G169-G172) mevcut.
- `git diff --stat`: yalnız `docs/mimari/raporlama.md`, `CLAUDE.md`, `docs/plan/raporlama-plani-2026-09-06.md`,
  `docs/arsiv/gorevler/README.md`, dört `git mv` (RM) + bu dosya. Backend/frontend kodu sıfır.

### İzlenecekler / NOT (kapsam dışı)

- NOT: `FilterControl.tsx:65` yorumu bayat (QuickFilters silindi) — tek satırlık frontend düzeltmesi (§13'e yazıldı).
- NOT: "N kayıt" ekranda iki kez (sayaç satırı + `PreviewTable` `toplam-rozeti`) — G175 raporunun temizlik önerisi.
- İnsan adımı: Gemini duman testi 3 istem (G176 raporu) + tarayıcı görsel dumanı (Radix popover konumu, `lg` altı
  sarma) — §13'te.
- `docs/plan/raporlama-plani-2026-09-06.md` §4.1/§6.1/§8 gövdeleri bilinçli tarihsel; okuyan önce baştaki şerhi görür.
