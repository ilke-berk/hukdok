# G157 — Aşama çelişki raporu üreticisi repoya: yer tutucu sınıfı, E-8 etiketi, S5 satır 82 düzeltmesi

- **Bant:** backend
- **Bağımlı:** G156 (aynı aktarım/teslim zinciri; G150'nin yeni uzlaşı kuralını kullanır)
- **Dosya kapsamı:** backend/services/asama_celiski_raporu.py (YENİ; kaynak: scratchpad `rapor_uret.py` + `kanit_cikar.py`
  — repoya girmemişti), backend/scripts/asama_celiski_raporu.py (ince CLI), backend/tests/test_g157_asama_celiski_raporu.py (YENİ)
- **Dokunma:** backend/scripts/hukdok_aktarim.py (`celiskileri_bul` `:1938-1946` yalnız ÇAĞRILIR), backend/scripts/mukerrer_kart_raporu.py

## Hedef

Plan 08.09 §1.2 **A7 + A3 (ucuz karşılık)**. 06.09'da ekibe giden `HUKDOK_ASAMA_CELISKILERI_2026-09-06.xlsx`
(532 grup, 5 sınıf, sarı CEVABINIZ) tek seferlik scratch betikle üretildi. Ekip S5 satır 82'nin (id-10285/10286,
Temyiz) `Farklı alanlar`/`Değerler`/`Ana sayfa değerleri` sütunlarını boş aldı: yer tutucu değerler
(`DANIŞTAY . DAİRE` ≈ `DANIŞTAY DAİRE`, `2026/` ≈ `2026`) normalize anahtarında eşit sayılıp fark yazılmadı.
Ayrıca ekip "karar durumu müvekkil yönünden farklı" (E-8, 29 grup) gruplarının hata olmadığını teyit etti;
raporda ayrı etiketle çıkmalı (kullanıcı kararı 08.09: föy düzeyi model YOK, etiket yeter).

## Yapılacaklar

1. Üreticiyi servis olarak yaz: girdi = G150 sonrası aktarımın çelişki listesi (`celiskileri_bul` çıktısı ya da DB'den
   yeniden hesap), çıktı = 5 sınıf sayfa (S1 boşluk artık G150 ile yazılır → sayfa yalnız kalan gerçek boşluk),
   `OZET`, sarı `CEVABINIZ` sütunu; ekibin cevap sütunlarını (`CEVABINIZ`, `AÇIKLAMANIZ`) **geri okuma** yardımcısı
   (HATA/BELİRSİZ/SEBEBİ VAR sayımı → `Düzeltme_Logu` beklentisi listesi).
2. **Yer tutucu sınıfı:** mahkeme/esas değeri yer tutucu desenine (`DAİRE` öncesi numara yok, `YYYY/` numarasız, `?`)
   uyuyorsa `Farklı alanlar` boş bırakılmaz; "S6 · Daire/numara eksik (yer tutucu)" alt sınıfı.
3. **E-8 etiketi:** S5'te föyler ayrı müvekkile aitse (`case_foys.case_party_id` ya da `ham_veri["Müvekkil"]` farklı)
   satıra "müvekkil yönü farkı (E-8) — hata değil" ön etiketi; ekip cevabıyla uyumlu.
4. Testler: yer tutucu deseni; E-8 etiketi; S5 satır 82 senaryosu (iki föy, Temyiz, yalnız yer tutucu fark) →
   sütunlar dolu; xlsx başlıkları 06.09 dosyasıyla birebir (ekip aynı formatı bekliyor).
5. Rapor: lokalde üretilen dosyanın sınıf başına satır sayısı (G150 sonrası 532 → beklenen ≤ 240).

## Kabul kriterleri

- [x] Servis + CLI repoda, scratch betik referansı raporda; başlıklar 06.09 ile aynı (test).
- [x] S5 satır 82 senaryosunda sütunlar dolu (test); E-8 etiketi (test).
- [x] ruff + mypy temiz; pytest özet satırı.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-10, gece kuyruğu işçisi)

### Yapılanlar

1. **Servis** `backend/services/asama_celiski_raporu.py` (YENİ) — 06.09'un scratch üreticisi
   (`kanit_cikar.py` + `rapor_uret.py`, oturum scratchpad'i `ed2b8dab…/scratchpad/`, repoya
   girmemişti) saf çekirdek + yükleyici + yazıcı + okuyucu olarak repoya alındı:
   - `siniflandir(AsamaGrubu) -> CeliskiGrubu | None`: çelişki tanımı **aktarımın tanımı**
     (`hukdok_aktarim._asama_imzasi` doğrudan çağrılır → G150 boş hücre imzaya girmez, G151 büro
     durumu boş sayılır). İmzası aynı grup rapora düşmez. Sınıflar S2–S5 06.09 mantığıyla
     (yazım/bambaşka/mahkeme-esas/durum; S4 alt sebepleri: yazım, bayat föy, çok tur adayı, yanlış
     dosyalanmış aşama). **S1** artık yalnız *kalan gerçek boşluk*: künye alanı aşama satırlarının
     HEPSİNDE boş, ana sayfada (Sheet) dolu — aşama katmanı ana sayfadan künye almadığı için
     birleşik satır bu değeri taşımaz.
   - **S6 · Daire/numara eksik (yer tutucu)** — `yer_tutucu_mu(alan, deger)`: mahkeme için
     `DAİRE` öncesinde hiç rakam yok, esas/karar no için `^\d{4}/?$`, her alan için `?`. Farklı
     künye alanlarının hepsinde yer tutucu varsa S6; ham yazımı farklı öteki yer tutucu alanlar da
     `Farklı alanlar`a eklenir (satır 82: normalize anahtarı eşit `DANIŞTAY . DAİRE`/`DANIŞTAY DAİRE`
     artık listede). Biri gerçek biri yer tutucu (`DANIŞTAY 10. DAİRE` ⇄ `DANIŞTAY DAİRE`) da S6.
   - **E-8 etiketi** — S5'te `Bizim okuma` `müvekkil yönü farkı (E-8) — hata değil; …` ile başlar.
     Koşul: föyler ayrı müvekkile ait (`case_party_id`, yoksa `ham_veri["Müvekkil"]` /
     Sheet `Müvekkil` anahtarı) **VE** karar durumları zıt yönlü (`karar_yonu`: Kabul ⇄ Red,
     Onama ⇄ Bozma, Başvuru Ret ⇄ Kaldırma). Gerekçe aşağıda (karar 2).
   - `gruplari_topla(db, paket, yalniz_kartlar=)`: paket `Karar_Asamalari` + DB föy→kart bağı →
     (kart, aşama) grupları; kapsam dışı föy katılmaz; çok tur: satır sayısı farklıysa S4 "çok tur"
     notu, aynıysa tur tur ayrı grup. `celiskileri_bul` yalnız ÇAĞRILIR: ana sayfa karar
     no/tarihi çelişkisi olan kartlar S2/S3 sebebine "(ana sayfa karar no/tarihi de çelişiyor)"
     notu düşer; `celiski_kart_idleri(sonuc.celiskiler)` ile aktarım çıktısı filtre olarak da girer.
   - `xlsx_yaz`: `OZET` + 6 sınıf sayfası, başlıklar `BASLIKLAR` (06.09 ile birebir, test kilidi),
     sarı `CEVABINIZ`/`AÇIKLAMANIZ`; boş sınıfın sayfası da yazılır. Dosya adı
     `HUKDOK_ASAMA_CELISKILERI_<tarih>.xlsx`.
   - **Geri okuma** `cevaplari_oku` → `CevapOzeti` (sınıf → HATA/BELİRSİZ/SEBEBİ VAR/(boş)/
     (tanınmayan) sayımı, E-8 etiketli satır sayısı, HATA satırlarından `DuzeltmeBeklentisi`
     listesi = sonraki paketin `Düzeltme_Logu` beklentisi); `cevap_ozeti_metni`, `beklenti_csv_yaz`
     (UTF-8 BOM + `;`). Sınıf sayfası `S<no>_` önekiyle, sütunlar başlıkla (aksan duyarsız) bulunur.
2. **CLI** `backend/scripts/asama_celiski_raporu.py` (ince): `--paket --cikti-dizini [--tarih]`
   üretim (SALT OKUNUR), `--cevap [--beklenti-csv]` geri okuma; `main(argv, session_factory=)`.
3. **Testler** `backend/tests/test_g157_asama_celiski_raporu.py` — 76 test: yer tutucu deseni (17),
   S5 satır 82 senaryosu (S6 + üç sütun dolu + sayfa adları), gerçek↔yer tutucu, E-8 (zıt yön 5 /
   aynı yön 7 / `case_party_id` önceliği / etiketsiz), G150/G151 uyumu, kalan boşluk S1, S2/S3/S4
   + çok tur, başlık kilidi (`BASLIKLAR`, `OZET`, 06.09 sayfa adları), kanıt sütunları, DB+paket
   toplama (kapsam dışı, Önceki zinciri, müvekkil kimliği), `celiskileri_bul` çıktısı filtresi,
   cevap anahtarı (9), geri okuma (sayım + beklenti + CSV + boş satır + sınıf dışı sayfa),
   üretilen dosyanın geri okunması, CLI üretim/geri okuma/hata kodları.

### Kararlar

1. **Çelişki tanımı aktarımdan ödünç, kopya değil.** `_asama_imzasi` çağrılır; sınıflandırıcı kendi
   normalizasyonunu yalnız *sınıf* için (yazım/rakam farkı) kullanır. Böylece "rapora düşen = aşaması
   yazılmayan" eşitliği yapısal.
2. **E-8 etiketi yalnız "müvekkil farklı" ile DEĞİL.** Ekibin cevaplı dosyası ölçüldü: S5'in 82
   grubunun **82'sinde** müvekkil farklı (kardeş föy = hekim föyü + sigorta föyü; tanım gereği).
   Görev tanımındaki koşul tek başına S5'in tamamını "hata değil" etiketlerdi — ekip ise 52 grubu
   BELİRSİZ, 4'ünü HATA dedi. Ayırıcı olan sonucun **yönü**: SEBEBİ VAR'ın 26 grubunun 25'i zıt
   yönlü çift (Kabul ⇄ Red 20, Onama ⇄ Bozma 4, Başvuru Ret ⇄ Kaldırma 1; kalan 1: `Red/Feragat`
   + boş), BELİRSİZ/HATA'nın tamamı aynı yönün ayrıntı farkı (Kabul ⇄ Kabul/Kısmen 19, Red/X ⇄
   Red/Y 25, Açılmamış ⇄ Red 7 …). Yön koşulu eklendi; lokal ölçümde etiket 81 S5 grubunun 25'ine
   düşüyor — ekip cevabıyla birebir uyumlu ("ekip cevabıyla uyumlu" kabul maddesi).
3. **S1 = kalan gerçek boşluk.** Eski S1 (kardeş föyde boş) G150 ile birleşiyor, rapora düşmez.
   Kalan: tüm aşama satırlarında boş + ana sayfada dolu — birleşik satır bunu taşıyamaz, ekibin
   kendi "uzlaştırma turu" (06.09 §1) tam bu hücreleri doldurmuş; sonraki pakette sıfırlanması
   beklenir. Sayfa boş kalsa da yazılır (format sözleşmesi).
4. **Yer tutucu mahkeme deseni "DAİRE öncesinde rakam yok".** İlk deneme (`\d. <kelime> DAİRE`)
   lokalde `ANKARA 3 İDARİ DA ADAİRESİ` (yazım hatası) satırını S6 saydı; gevşetildi, o grup S4
   "mahkeme adı yazımı/eksik"e döndü (test parametresi eklendi).
5. PDF üretimi (06.09'daki reportlab çıktısı) repoya ALINMADI: görev xlsx + geri okuma istiyor,
   reportlab prod imajında yok, bağımlılık eklenmez.

### Lokal ölçüm (04.09 paketi × lokal DB, 8.386 föy; `docker cp` ile /tmp)

`python scripts/asama_celiski_raporu.py --paket /tmp/paket.xlsx --cikti-dizini /tmp/rapor_g157`
→ 1.400 (kart, aşama) grubu okundu, **342** satır:

| Sınıf | 06.09 | G157 |
|---|---:|---:|
| S1 Yalnız boşluk (yeni tanım: kalan gerçek boşluk) | 302 | 114 (Karar durumu 83 · İstinaf 61 / Yerel 38 / KD 14) |
| S2 Yazım hatası adayı | 37 | 33 |
| S3 Aynı dava, bambaşka karar künyesi | 61 | 60 (12'si "ana sayfa da çelişiyor" notlu) |
| S4 Mahkeme ya da esas farklı | 50 | 53 (yazım 22 · bayat föy 13 · bambaşka 8 · çok tur adayı 4 …) |
| S5 Yalnız karar durumu farklı | 82 | 81 (**E-8 etiketli 25**) |
| S6 Daire/numara eksik (yer tutucu) | — | 1 (kart 14213 = satır 82 ikizi, `Mahkeme, Esas` dolu) |
| Toplam | 532 | 342 — gerçek çelişki **228**, kalan boşluk 114 |

Beklenti "≤ 240" gerçek çelişki için tutuyor (228); S1'in 114'ü çelişki değil, ekibin doldurmasını
beklediğimiz boşluk. S5'in 81 (82 değil) olması G151: `Kapalı`/`Derdest` grubu artık boş sayılır.

**Geri okuma, gerçek cevap dosyası** (`--cevap /tmp/cevapli.xlsx`): S1 302 (HATA 254 · BELİRSİZ 22 ·
SEBEBİ VAR 26) · S2 37 · S3 61 · S4 50 · S5 82 (HATA 4 · BELİRSİZ 52 · SEBEBİ VAR 26) — toplam
**HATA 270 · BELİRSİZ 210 · SEBEBİ VAR 52**, ekibin cevap notundaki üç sayıyla birebir;
`Düzeltme_Logu` beklentisi 270 kalem CSV'ye yazıldı (repo dışı).

### Doğrulama (konteynerde)

- `python -m pytest` → **3094 passed, 3 skipped** (önce 3018+48 → 76 yeni test).
- `python -m ruff check .` → All checks passed. `python -m mypy` → Success: no issues found in 62 source files.

### İzlenecekler

- Sonraki pakette S1'in sıfırlanıp sıfırlanmadığı (ekibin uzlaştırma turu) ve S6'nın 19 hücrelik
  "daire numarası eksik" kuyruğuyla eşleşmesi (ekip §5).
- `karar_yonu` havuzu kapalı liste değil, ön ek listesi: yeni bir istinaf/temyiz sonucu (ör.
  `Kısmen Onama/Kısmen Bozma`) yön almaz → etiketlenmez (bilinçli: tahmin yasağı).
- NOT: çalışma ağacı başında kapsam dışı kirli dosyalar vardı (`.claude/settings.local.json` M,
  `.claude/launch.json` ??) — araç yapılandırması, dokunulmadı, commit'e alınmadı.
