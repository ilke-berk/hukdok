# G137 — Rapor kataloğu genişlemesi: grup/kontrol/hızlı filtre/kolon seti/öneriler + taraf bağlantılı filtreler

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/services/rapor/registry.py, backend/services/rapor/motor.py,
  backend/schemas_rapor.py (yalnız gerekiyorsa katalog şemaları), backend/routes/reports.py (catalog önbelleği),
  backend/services/rapor/asistan.py (yalnız `katalog_metni` — yeni alanlar prompt'a girmesin),
  backend/tests/test_g137_rapor_katalog_genisleme.py, backend/tests/test_g130_rapor_temeli.py (yalnız
  "türetilmiş filtrelenemez" varsayımını taşıyan testlerin güncellenmesi — raporda listelenir)
- **Dokunma:** backend/models.py, backend/database.py (migrasyon YOK — hepsi sorgu katmanı),
  backend/prompts.py, frontend/**

## Hedef

Kullanıcı bulgusu (07.09): filtre arayüzü anlaşılmıyor, kategorilerin içinden süzülemiyor, tarih filtresi
görünmüyor, elle yazım hata üretiyor. Frontend yeniden tasarımı (G138/G139) katalogdan **kontrol türü, grup,
hızlı filtre listesi, kolon setleri ve öneri değerleri** bekliyor; dava kaynağında **taraf adı / müvekkil
kategorisi / sigortalı adı** filtreleri gerekiyor. Sözleşme `docs/plan/raporlama-plani-2026-09-06.md` §4.2'de
DONDU; G138 buna göre paralel yazılıyor. K1 (registry beyaz listesi, serbest SQL yok) ve K2 (tenant +
soft-delete) aynen korunur.

## Yapılacaklar

1. **`Kolon` dataclass'ı:** `grup: str`, `onerili: bool = False`, `filtre_ifadesi: Optional[Callable]`
   (türetilmiş kolonda EXISTS üreten fonksiyon; `None` ise `ifade` kullanılır). `_turetilmis(...)`
   `filtrelenebilir` parametresi alır; `_kendini_denetle` "filtrelenebilir türetilmiş" yasağını
   "filtrelenebilir türetilmiş ama `filtre_ifadesi` yok" yasağına çevirir. Her kolona `grup` (plan §4.2 kümeleri;
   denetim: grup kümesi kaynağa göre kapalı, boş grup yok).
2. **Yeni/değişen dava kolonları:** `muvekkil_adlari` ve `karsi_taraf_adlari` filtrelenebilir olur
   (`contains`/`is_null`/`not_null`, EXISTS `case_parties` + `party_type`); `sigortali_adlari` (YENİ, türetilmiş
   seçim + EXISTS, `role='Sigortalı'`); `muvekkil_kategorisi` (YENİ, `liste`, seçenekler `clients.category`
   DISTINCT ∪ seed; filtre EXISTS `case_parties JOIN clients` — `clients.deleted_at IS NULL`; seçim ifadesi
   CLIENT tarafların kategorileri `aggregate_strings`). `contains` bu kolonlarda ILIKE kaçışı aynen (`motor`
   yardımcıları yeniden kullanılır — kopyalama yok). `siralanabilir=False` kalır.
3. **`motor.py`:** filtre kurarken `kolon.filtre_ifadesi` varsa onu çağırır (`op`, `deger`); `tanimi_dogrula`
   türetilmiş kolonda op listesini kolonun izin verdiği alt kümeyle sınırlar (`Kolon.izinli_oplar` ya da eşdeğer;
   tip→op tablosu §2.2 değişmez, alt küme daraltır).
4. **`VeriKaynagi`:** `hizli_filtreler: tuple[HizliFiltre, ...]` (`alan`, `alternatifler`), `kolon_setleri:
   tuple[KolonSeti, ...]` — plan §4.2 listeleri; denetim: her anahtar katalogda ve filtrelenebilir, setlerdeki
   kolonlar katalogda.
5. **`katalog()`:** kolona `grup`, `kontrol` (tipten: tarih→tarih_araligi, liste→coklu_secim, metin→metin_icerir,
   sayi/para→sayi_araligi, mantik→mantik; filtrelenemeyen → `null`), `oneriler` (onerili metin kolonlarda
   DISTINCT ≤300 + `oneri_kesik`), kaynağa `hizli_filtreler`, `kolon_setleri`. Route'ta süreç içi 60 sn önbellek
   (tenant anahtarlı — öneriler tenant kurallı; `time.monotonic`, testte sıfırlanabilir).
6. **`asistan.katalog_metni`:** yeni alanları GÖMMEZ; yeni dört kolon doğal olarak girer (test: metin uzunluğu
   önerilerle şişmez, `hizli_filtreler` kelimesi yok).
7. **Testler** (`test_g130` `env` deseni; sqlite): katalog yeni alanların şekli; her kolonun `grup`u dolu;
   `kontrol` tip eşlemesi; `hizli_filtreler`/`kolon_setleri` anahtarları katalogda; öneriler: DISTINCT, boş
   hariç, silinmiş/tenant dışı hariç, 300 kesme + bayrak; taraf filtreleri: müvekkil adı `contains` doğru
   davaları bulur (aynı adlı karşı taraf bulunmaz), `sigortali_adlari` role bazlı, `muvekkil_kategorisi in`
   silinmiş müvekkil kartını saymaz, türetilmişte izinsiz op (`eq` metinde, `gte`) 422, sıralama 422; önbellek
   60 sn (monotonic monkeypatch); G130'daki eski varsayım testleri güncellendi ve raporda gerekçesi yazıldı.

## Kabul kriterleri

- [ ] `GET /api/reports/catalog` §4.2 alanlarını döner: her kolonda `grup` + `kontrol` + `oneriler`
      (onerili metinde liste, diğerinde `null`), her kaynakta `hizli_filtreler` + `kolon_setleri` (test).
- [ ] Davalar'da `muvekkil_adlari contains "Anadolu"` yalnız CLIENT tarafında bu adı taşıyan davaları döner;
      `muvekkil_kategorisi in ["Özel Hastane"]` müvekkil kartı silinmiş tarafı saymaz (test).
- [ ] Türetilmiş kolonlarda yalnız izinli op'lar; diğerleri 422 `{"alan","sebep"}`; sıralama 422 (test).
- [ ] Öneriler tenant + soft-delete kurallı, ≤300, `oneri_kesik` doğru (test).
- [ ] Katalog önbelleği: 60 sn içinde ikinci çağrı DISTINCT sorgusu koşturmaz (test, sorgu sayacı ya da
      monkeypatch).
- [ ] Asistan katalog metni öneri/hızlı filtre alanlarını içermez (test).
- [ ] Sözleşme farkı çıktıysa plan §4.2 güncellendi ve raporda "SÖZLEŞME DEĞİŞTİ" başlığıyla yazıldı.
- [ ] Migrasyon yok; ruff + mypy temiz; tam paket yeşil.

## Doğrulama

- `docker compose exec -T backend python -m pytest tests/test_g137_rapor_katalog_genisleme.py tests/test_g130_rapor_temeli.py`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-07 gece, işçi oturumu; migrasyon YOK — hepsi sorgu katmanı)

### Yapılanlar

- **`registry.py`** — `Kolon`'a `grup`, `onerili`, `filtre_ifadesi`, `izinli_oplar`, `oneri_sorgusu`,
  `secenek_ifadesi` alanları + `oplar`/`kontrol` özellikleri; `HizliFiltre` ve `KolonSeti` dataclass'ları;
  `VeriKaynagi`'na `gruplar` (kapalı küme), `hizli_filtreler`, `kolon_setleri`. Kolon listeleri plan §4.2
  gruplarına göre `_grup("…", …)` demetleriyle yeniden dizildi (kolon kümesi aynı, yalnız sıra grup sırası).
  Dört taraf kolonu: `muvekkil_adlari`/`karsi_taraf_adlari` filtrelenebilir oldu (EXISTS `case_parties`,
  `party_type`), `sigortali_adlari` YENİ (`role='Sigortalı'`, her party_type), `muvekkil_kategorisi` YENİ
  (`liste`; seçenek = seed çekirdeği ∪ `client_categories` tablosu ∪ `clients.category` DISTINCT; filtre EXISTS
  `case_parties JOIN clients` + `clients.deleted_at IS NULL`; seçim ifadesi CLIENT tarafların canlı kart
  kategorileri `aggregate_strings`). Tenant kuralı yalnız `cases` üzerinden (K2). `onerileri_getir`: kaynağın
  `kisitlar(tenant_id)` + boş hariç + DB sırası + `ONERI_MAX=300` (301 çekilir, aşarsa `oneri_kesik`).
  `_kendini_denetle` genişledi: boş/küme dışı grup, filtre ifadesiz filtrelenebilir türetilmiş, türetilmiş
  olmayan kolonda `filtre_ifadesi`, tip tablosu dışı `izinli_oplar`, metin dışı `onerili`, hızlı filtre
  katalogda yok/filtrelenemez/tekrar, alternatifler tarih dışı, kolon seti boş/tekrar/katalog dışı.
- **`motor.py`** — `_kosul` ikiye bölündü: `_ifade_kosulu(ifade, op, deger)` atom koşul (ILIKE kaçışı, `ne`
  NULL-dahil, `in` bağlı parametre TEK yerde) + dispatch. `Kolon.filtre_ifadesi` varsa `(op, deger,
  _ifade_kosulu)` ile çağrılır — registry EXISTS'i kurar, atomu motordan alır (kopya yok). `tanimi_dogrula`
  iki katman: `TIP_OPLARI[tip]` (değişmedi) + `kolon.oplar` alt kümesi; ihlal 422 `{"alan","sebep"}` ("… kolonunda
  izinli değil (izinli: …)").
- **`routes/reports.py`** — `_katalogu_getir`: süreç içi 60 sn önbellek, anahtar `(SessionLocal, tenant_id)`,
  `threading.Lock`, saat `_saat = time.monotonic` (test monkeypatch'ler), `katalog_onbellegini_sifirla()`.
  `registry.katalog(db, limitler, tenant_id)` imzası tenant aldı (öneriler tenant kurallı).
- **`asistan.py`** — yalnız `_kolon_satiri`/`katalog_metni`: filtrelenebilir türetilmişte "türetilmiş (filtre
  yalnız: contains|is_null|not_null; sıralama yok)"; yeni alanlar (grup/kontrol/öneri/hızlı filtre/set) GÖMÜLMEZ.
  `prompts.py`'ye dokunulmadı.
- **Testler** — `tests/test_g137_rapor_katalog_genisleme.py` (32 test, parametreler dahil): şekil, grup kümesi (boş grup yok),
  kontrol eşlemesi, hızlı filtre + kolon seti plan listeleri birebir, yeni taraf kolonları, öneriler
  (DISTINCT/boş/tenant/soft-delete/300 kesme/`db=None`), taraf filtreleri (aynı adlı karşı taraf bulunmaz,
  is_null/not_null, rol bazlı sigortalı, silinmiş müvekkil kartı sayılmaz, ILIKE kaçışı + zehir string bağlı
  parametrede, `dava_sayisi` karşılaştırma), izinsiz op 7 varyant 422, sıralama 422, registry denetimi 5 ret,
  önbellek (monotonic monkeypatch + sayaç; veri değişimi 60 sn gizlenir), asistan metni (300+ değerle
  uzunluk sabit).

### G130 testlerinde güncellenenler (gerekçe: "türetilmiş filtrelenemez" varsayımı §4.2 ile kalktı)

- `test_katalog_sekli`: kolon anahtar kümesine `grup/kontrol/oneriler/oneri_kesik`, kaynak kümesine
  `hizli_filtreler/kolon_setleri` eklendi; türetilmişte yalnız `siralanabilir is False` kaldı.
- `test_422_turetilmis_filtrelenemez_siralanamaz`: örnek `muvekkil_adlari contains` (artık geçerli) →
  `foy_sayisi gte` (filtre ifadesiz türetilmiş, hâlâ "filtrelenemez" 422). Sıralama yarısı aynen.
- `test_registry_kendini_denetler`: yalnız docstring (assert'ler değişmedi).
- `test_g132` DOKUNULMADI: `muvekkil_adlari eq` örneği alt küme dışı olduğundan yine 422; prompt satırı
  öneki ("… metin · türetilmiş") korundu.

### Kararlar

- **`dava_sayisi` filtrelenebilir** (Müvekkiller hızlı filtresi plan listesinde; denetim "filtrelenebilir"
  ister): COUNT alt sorgusu `_skaler_filtre` ile doğrudan karşılaştırılır, op tablosu `sayi` ile aynı —
  `is_null` boş küme döner ama §4.3 "boş olanlar" anahtarı 422 yemez. `foy_sayisi`/`belge_sayisi` istenmedi,
  filtrelenemez kaldı.
- **Önbellek anahtarında oturum fabrikası:** prod'da tek; testlerde monkeypatch'lenir → farklı fabrika =
  farklı DB, bayat gövde başka teste sızmaz (G130 `env` fixture'ı sıfırlama çağırmaz, değiştirilmedi).
- **`muvekkil_kategorisi` seçimi DISTINCT değil** (sqlite `group_concat(DISTINCT x, ayraç)` yok); iki Doktor
  müvekkil "Doktor ; Doktor" verir — filtre EXISTS olduğundan doğruluğu etkilemez.
- **Öneri sırası DB collation'ı** ("alfabetik" = ORDER BY); Türkçe locale sırası istenirse ayrı iş.
- **Sözleşme farkı YOK:** katalog gövdesi §4.2'yle birebir (`oneri_kesik` planda var); plan dosyasına
  dokunulmadı. `gruplar` kaynağa AYRICA verilmedi (planda yok) — grup sırası kolon sırasından okunur.

### Doğrulama

- `pytest tests/test_g13*_rapor*.py` → 192 passed (G130-G137).
- `pytest` tam paket (konteyner) → **2670 passed, 3 skipped**.
- `ruff check .` → All checks passed. `mypy` → Success: no issues found in 61 source files.
- Kırmızı kapı: kod stash'lenip yeni test dosyası eski koda karşı koşuldu → fixture `katalog_onbellegini_sifirla`
  bulunamadığından tüm testler ERROR (eski kodda geçmiyor); stash geri alındı.

### İzlenecekler

- G138 tarafında: `metin_icerir` combobox'ı öneri seçince `eq` üretir — taraf kolonlarında `eq` izinli DEĞİL
  (yalnız `contains|is_null|not_null`); frontend bu dört kolonda seçimde de `contains` göndermeli ya da
  §4.3'e şerh düşülmeli (G140).
- 2 worker → önbellek worker başına; 60 sn içinde iki worker farklı fotoğraf verebilir (kabul edilebilir).
