# G060 — Karar sonucu resmi listeleri: 4 referans listesi + yerel_karar_durumu kolonu + seed

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/models.py, backend/database.py, backend/managers/reference_lists.py,
  backend/managers/seed_data.py, backend/managers/config_manager.py, backend/routes/config.py,
  backend/schemas.py, backend/tests/test_g060_karar_listeleri.py
- **Dokunma:** `cases.karar_turu` / `cases.karar_lehine` (models.py:70-71 — kaba 6+3'lük AYRI
  alanlardır, bu görevin kapsamı değildir, davranışları DEĞİŞMEZ);
  `frontend/**` (dropdown bağlama G061'in işi); `backend/scripts/import_excel_cases.py`
  (eski araç, kullanılmayacak — temizlik planı §8).

## Hedef

Karar sonuçları için büronun RESMİ değer havuzlarını (10.08 teslim paketinin DEGER_HAVUZLARI
sayfasından, aşağıda gömülü) yönetilen kapalı referans listeleri olarak kur. Desen birebir
`appealing_parties`/`alleged_faults` deseni (G044): model + `LIST_REGISTRY` + `DEPENDENCIES`
+ seed + config route + DynamicConfig cache. Yerel mahkeme karar durumunun `cases` üzerinde
kolonu YOK (mevcut `karar_turu` 6 değere ezen ayrı bir kaba alandır; kanıt:
`scripts/import_excel_cases.py:138 map_karar_turu`) — `yerel_karar_durumu VARCHAR(100)`
kolonu bu görevde açılır. Bağlam: KARAR_ASAMALARI_TASARIM_PAKETI_2026-08-17.xlsx "kapalı
havuzlar" değişmezi; alanlar bugün prod kopyasında 0 dolu (18.08 ölçümü) — normalize/backfill
YOK, temiz başlangıç.

## Yeni listeler (registry anahtarı → başlık → DepSpec bağı)

| Anahtar | LIST_TITLES | Bağlı kolon (DEPENDENCIES) |
| --- | --- | --- |
| `local_decisions` | Yerel Karar Durumları | `Case.yerel_karar_durumu` (YENİ kolon) |
| `appeal_decisions` | İstinaf Karar Durumları | `Case.istinaf_karar_durumu` (models.py:83) |
| `cassation_decisions` | Temyiz Onama Durumları | `Case.temyiz_karar_durumu` (models.py:93) |
| `revision_decisions` | Karar Düzeltme Durumları | `Case.karar_duzeltme_durumu` (models.py:103) |

Dört ayrı model (AppealingParty kalıbı, models.py:511-520 civarı): `code` (ASCII, tekil,
değişmez; üretim kuralı: tr_upper → Türkçe karakter sadeleştir → `[^A-Z0-9]+` → `_`),
`name`, `sequence`, `active`.

## Seed değerleri (resmi havuz — birebir bu yazımla, DEGER_HAVUZLARI 2026-08-10)

**Yerel (28):** Açılmamış Sayılması (HMK 150. Md) · Adli Para Cezası · Anlaşma · Anlaşmama ·
Beraat · Birleştirme · Derdest · Düşme Kararı · Hapis Cezası · Hapis Cezasının Paraya
Çevrilmesi · Hükmün Açıklanmasının Geri Bırakılması (HAGB) · İflas · Kabul · Kabul/Kısmen ·
Kapalı · Karar Verilmesine Yer Olmadığına (HMK 331 Md.) · Kovuşturmaya Yer Olmadığına (KYOK) ·
Red/Arabuluculuk Ön Şart · Red/Dilekçenin Reddi · Red/Esastan · Red/Feragat · Red/Görev ·
Red/Husumet · Red/İdari Merciye Tevdi · Red/MSK Kararı Gereği · Red/Yargı Yolu ·
Red/Yetkisizlik · Red/Zamanaşımı

**İstinaf (3):** Kaldırma · Kaldırma/Yeniden Hüküm · Başvuru Ret
**Temyiz (3):** Bozma · Onama · Düzelterek Onama
**Karar Düzeltme (2):** Karar Düzeltme Kabul · Karar Düzeltme Ret

> Not: `normalize_list_name` (tr_title) ad yazımını değiştirir ("Red/Esastan" → başharf
> kuralı). Resmi havuz yazımı KORUNMALI — seed doğrudan model satırı yazar (add_item değil),
> `_seed_appealing_parties` deseninde. Kullanıcı ekipten güncel liste alırsa panelden düzeltilir.

## Kabul kriterleri

- [ ] 4 liste `LIST_REGISTRY` + `DEPENDENCIES` + `LIST_TITLES`'ta; GET uçları değerleri
      dönüyor (appealing_parties'in route kaydı hangi yoldan geçiyorsa aynı yol)
- [ ] `yerel_karar_durumu` kolonu `("columns", ...)` op'uyla eklendi; kısıt/index gerekirse
      DAİMA `("index", ...)` op'una (G041 kuralı — `("table",...)` gövdesine gömülmez)
- [ ] Seed yarış-güvenli: `seed_data._ekle_yarissiz` (satır başına SAVEPOINT, G058 deseni);
      ikinci koşuda 0 ekleme, 0 ERROR (idempotentlik testi)
- [ ] Seed değer sayıları test sabitleriyle: 28 · 3 · 3 · 2; yazımlar yukarıdaki listeyle birebir
- [ ] `karar_turu`/`karar_lehine` davranışı değişmedi (mevcut testler yeşil; bu alanlara diff YOK)
- [ ] Gerçek DB'ye bağlanan yeni test 3-ortam kuralına uyar: `to_regclass` şema kontrolü +
      DB yoksa/şema göçmemişse SKIP (Deploy #10 dersi, KUYRUK.md "kapı merdiveninin kör noktası")
- [ ] Silme koruması çalışıyor: listedeki değer bir davada kullanılıyorken `mode=block`
      silme `ItemInUseError`/409 üretiyor (get_usage DepSpec bağı üzerinden — test)

## Doğrulama

- `docker compose exec -T backend python -m pytest`  (DİKKAT: ekstra `-q` EKLEME)
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- Dev araçları konteynerde yoksa önce: `docker compose exec -T backend pip install -r requirements-dev.txt`

## Rapor

**Durum: TAMAM** (2026-08-18 gecesi, ana oturumda — org ayarı CLI erişimini
kapattığı için koşucu yerine; işçi + doğrulama disiplini aynen uygulandı).

### Yapılanlar

- 4 model: `LocalDecision` / `AppealDecision` / `CassationDecision` /
  `RevisionDecision` (models.py, AppealingParty kalıbı birebir; tablolar
  create_all yolundan gelir, tablo op'u gerekmez — database.py madde 34 yorumu).
- `cases.yerel_karar_durumu VARCHAR(100)`: `("columns", ...)` op'u, madde 34.
  Kısıt/index YOK — bilinçli: alan bugün 0 dolu, hiçbir sorgunun filtresi değil
  (G042 dersi); gerekseydi G041 kuralı gereği ayrı `("index", ...)` op'una yazılırdı.
- `LIST_REGISTRY` + `DEPENDENCIES` + `LIST_TITLES` + isimli sarmalayıcılar
  (get/add/delete ×4, reference_lists.py).
- DynamicConfig: 4 özel alan + getter/setter çiftleri (getattr sözleşmesi).
- Seed (seed_data.py): `_karar_kodu` (görev tanımındaki kod üretim kuralı) +
  resmi havuz sabitleri (28·3·3·2) + `_seed_karar_listesi` ortak fonksiyonu
  (G058 SAVEPOINT deseni) + `seed_all_lists`e 4 çağrı. Yazımlar BİREBİR korunur:
  seed doğrudan model satırı yazar, `add_item`/`normalize_list_name`e girmez.
- routes/config.py: 4×(GET/POST/DELETE) — appealing_parties'le aynı yol; silme
  api.py'deki `ItemInUseError` handler'ıyla 409'a düşer.
- schemas.py: `CaseRead` + `CaseTrackingUpdate`e `yerel_karar_durumu` (Optional).
- Test: `tests/test_g060_karar_listeleri.py` — 17 test, üç katman (şema kilidi /
  sabit kilidi / sqlite seed + silme koruması). Gerçek DB'ye bağlanan test YOK —
  hepsi kendi in-memory sqlite şemasını kurar, üç ortamda aynı davranır.

### Kararlar + gerekçeleri

- **Kodlar elle yazılmadı**, `_karar_kodu` kuralından türetiliyor: 36 girdide el
  hatası riskine karşı kural kodda yaşıyor; test 4 örnek + ASCII deseni + tekillikle
  kilitler.
- **appeal/cassation/revision mevcut kolonlara bağlandı** (`istinaf_karar_durumu`,
  `temyiz_karar_durumu`, `karar_duzeltme_durumu`): alanlar prod kopyasında 0 dolu
  (18.08 ölçümü) → eski-değer çakışması yok, backfill yok.
- **`karar_turu`/`karar_lehine`e dokunulmadı**; test hem tanımlarını hem "yeni
  listelere BAĞLANMADI"yı kilitliyor (DepSpec taraması).

### Test sonuçları

- Yeni dosya: **17/17** geçti.
- Tam paket (konteyner): **1302 passed, 3 skipped** (önceki 1285 + 17 yeni).
- ruff: temiz · mypy: 39 dosya, 0 hata.
- Migrasyon konteyner DB'sinde koşuldu: log "Added yerel_karar_durumu to cases",
  information_schema doğrulaması `character varying(100)`. İlk tam koşuda
  test_g046 gerçek-kayıt testi `UndefinedColumn` ile kırmızıydı — sebep migrasyonun
  henüz koşmamış olmasıydı (model önde, DB geride); `python migrate.py` sonrası
  yeşil. Deploy'da entrypoint (önce migrate, sonra uvicorn) aynı sırayı garanti eder.

### İzlenecekler / NOT

- **NOT (kapsam dışı, G061/G062 girdisi):** `yerel_karar_durumu`nun okuma/yazma
  yolu henüz bağlı değil — `case_manager.get_case` alanı serialize ETMİYOR
  (managers/case_manager.py:379 civarı) ve takip güncelleme whitelist'i
  (case_manager.py:1545 civarı "Yerel Karar" bloğu) alanı İÇERMİYOR; ikisi de bu
  görevin dosya kapsamı dışındaydı. G061 dropdown'ı bağlarken ya da G062 son-aşama
  senkronunu kurarken bu iki nokta eklenmeli, yoksa alan UI'dan okunamaz/yazılamaz.
- Panelden ekleme (`add_item`) adı `normalize_list_name`den (tr_title) geçirir —
  resmi yazımdan sapabilir; ekip güncel havuz gönderirse seed sabitine de işlenmeli
  (görev notundaki karar).
- Çalışma ağacında kapsam dışı kirlilik yalnız `.claude/` dosyalarıydı
  (settings.local.json + launch.json) — koşucunun kirlilik tanımı da `.claude/`u
  dışlar; dokunulmadı, commit dışı bırakıldı.
