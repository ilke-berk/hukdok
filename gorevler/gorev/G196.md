# G196 — Dava durumu yazma yollarında üçlü kapısı: üçlü dışı değer 400, veritabanına gitmez

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** `backend/managers/case_manager.py` (yalnız `add_case`, `update_case`, `update_case_tracking`), `backend/constants.py` (yalnız YENİ hata sınıfı / kapı yardımcısı — `CASE_STATUSES` ve `normalize_case_status` davranışı değişmez), `backend/api.py` (yeni exception handler), `backend/routes/cases.py` (gerekirse), YENİ `backend/tests/test_g196_status_kapisi.py`
- **Dokunma:** `backend/database.py` (G195 kısıtı olduğu gibi kalır), `backend/models.py`, `backend/scripts/**` (yalnız okunur, rapora), frontend, mevcut test gövdeleri (değişmesi gerekirse BLOKE — test değiştirme izni YOK)

Kaynak: `otomasyon/loglar/kuyruk-workflow_2026-09-14-4.md` "Karar bekleyenler" (G195 işçi notu); kullanıcı kararı 14.09: görev açıldı.

## Hedef
G195 `cases.status` üzerine `CHECK (status IN CASE_STATUSES) NOT VALID` ekledi. Uygulama yazma yolları buna hazır değil:
- **`add_case`** (`case_manager.py:1512`) status'u normalize ETMEDEN yazar: `status=data.get("status", "DERDEST")`.
  Üçlü dışı değer → `CheckViolation` → `except IntegrityError` dalı (`:1627-1638`) tekil ihlali olmadığı için
  `logger.error` + `None` → `routes/cases.py:77` **500** "Failed to save case".
- **`update_case`** (`:1118-1123`) `normalize_case_status`'tan geçirir ama o fonksiyon tanımadığı metni olduğu gibi
  döndürür ("kapalı liste kapısı çağıranda uygulanır", `constants.py:46-53`) → commit'te `CheckViolation` →
  `except Exception` (`:1239-1242`) ERROR + `False` → `routes/cases.py:307` **500**.
- **`update_case_tracking`** (`:1983-1996`) aynı desen → `except Exception` (`:2015-2018`) ERROR + `False` →
  `routes/cases.py:593` **404** "Dava bulunamadı veya güncelleme başarısız" (yanıltıcı).
- `schemas.CaseCreate.status: str = "DERDEST"` (`schemas.py:308`) serbest metin kabul eder.

Kapı veritabanından ÖNCE: normalize sonrası değer `CASE_STATUSES` dışındaysa istemci hatası yükselir, hiçbir alan
yazılmaz, route **400** döner, ERROR basılmaz (log sözleşmesi: istemci hatası nihai sistem başarısızlığı değil).
Mevcut desen örnek alınır: `stage_decisions.InvalidDecisionStatusError(ValueError)` + `api.py:405-407` handler'ı +
`update_case_tracking`'teki `except InvalidDecisionStatusError: rollback; raise` (`:2010-2014`). `add_case` de
`update_case` gibi `normalize_case_status` kullanır: eski değer (TEMYIZ, KAPALI, …) üçlüye çevrilir, aşama boşsa
`case_stage`'e taşınır (karar 020 — bilgi kaybolmaz). `None`/boş davranışı DEĞİŞMEZ (`add_case` varsayılanı DERDEST;
`update_case` `None`'da dokunmaz; takip yolunda `None` alanı temizler — NULL CHECK'ten geçer, NOT NULL ayrı karar).

## Kabul kriterleri
- [ ] `POST /api/cases` `status="Arşivde"` (serbest metin) → **400**, anlamlı Türkçe `detail`; DB'de yeni kart YOK; `Add Case Error` ERROR logu YOK.
- [ ] `POST /api/cases` `status="TEMYIZ"` → kart `status=DERDEST`, `case_stage=TEMYIZ` (istek aşama vermediyse); `status` verilmezse DERDEST (mevcut davranış).
- [ ] `PUT /api/cases/{id}` `status="Arşivde"` → **400**; kartın HİÇBİR alanı değişmez (istekteki diğer alanlar dahil — kısmi yazım yok), tarihçe satırı yok, ERROR logu yok. `status="TEMYIZ"` → mevcut davranış (DERDEST + boşsa aşama).
- [ ] Takip güncelleme ucu (`routes/cases.py:580-593`) `status="Arşivde"` → **400** (bugünkü 404 değil); hiçbir alan yazılmaz; ERROR logu yok.
- [ ] `dbtest` (gerçek Postgres, G195 kısıtı yerinde; mevcut desen `test_migration_path._scratch_database` + `_run_init_db`): üç yolda üçlü dışı değer 400 döner ve `CheckViolation`'a HİÇ ulaşılmaz (log yakalanıp ERROR yokluğu doğrulanır); aynı test kapı olmadan (eski kod) kırmızıdır — kırmızı-yeşil kanıtı rapora.
- [ ] Tarama rapora (düzeltme KAPSAM DIŞI): `add_case`'i çağıran diğer yollar (`scripts/kartsiz_foy_kart_ac.py:199` — gönderdiği status üçlüde mi?) ve `models.Case(...)` ile doğrudan yazanlar (`scripts/birlesik_kart_ayir.py:190`, `scripts/import_excel_cases.py:248`) + `case.status =` atamaları; üçlü dışı yazabilecek yol varsa dosya:satır ile not.
- [ ] Mevcut testler gövdeleri DEĞİŞMEDEN yeşil; `update_case_tracking`'in `False`/404 dönüşünü kilitleyen bir test çıkarsa BLOKE bırak (izin yok).

## Doğrulama
- `docker compose exec -T backend python -m pytest tests/test_g196_status_kapisi.py`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data` (çıplak `ruff check .` git dışı `calibration-data/_g179/ek3_cevapla.py` yüzünden kırmızı)
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-14, işçi oturumu; başlangıç HEAD `435ba80`) — kabul maddesi 4'ün HTTP 400 kısmı yazıldığı gibi karşılanmadı (aşağıda "Kabul durumu")

### Yapılanlar
- **`constants.py`** — YENİ `InvalidCaseStatusError(ValueError)` + `validated_case_status(value)`: `normalize_case_status`
  çağırır, normalize sonrası değer `CASE_STATUSES` dışındaysa Türkçe mesajla yükseltir (değer + üçlü + "yargı aşaması
  durum değil aşamadır"). `CASE_STATUSES` ve `normalize_case_status` davranışı DEĞİŞMEDİ (tanınmayanı olduğu gibi döndürme
  sözleşmesi korunur; yeni testte kilitli).
- **`api.py`** — `InvalidCaseStatusError` → 400 handler (`InvalidDecisionStatusError` handler'ının ikizi). İmport dosya
  başında (geç import + `noqa` yerine).
- **`case_manager.add_case`** — kapı oturum açılmadan ÖNCE: eski değer üçlü + `case_stage` (istek `case_stage` verdiyse o
  kazanır), boş/None → DERDEST; üçlü dışı → yükselir, kart açılmaz. `is_consult` normalize edilmiş değerden (`danış`/`DANIS`
  de danışma sayılır).
- **`case_manager.update_case`** — `normalize_case_status` → `validated_case_status` (ilk yazımdan önce, dava bulunduktan
  sonra: olmayan dava yine `None`/404); `except InvalidCaseStatusError: rollback; raise` (ERROR yok, `False`'a yutulmaz).
- **`case_manager.update_case_tracking`** — status kapısı yazım döngüsünden ÖNCE toptan ön geçişe alındı (eskiden döngü
  içindeydi: `TRACKING_FIELDS` sırasında status'tan önce gelen `case_stage` oturum nesnesine yazılmış oluyordu); except
  dalı `(InvalidDecisionStatusError, InvalidCaseStatusError)` → rollback + raise. `None` alanı temizler (değişmedi).
- `routes/cases.py` değişmedi (handler yeter). `database.py`, `models.py`, `schemas.py`, `scripts/**`, frontend dokunulmadı.

### Kabul durumu
- [x] `POST /api/cases` `status="Arşivde"` → 400, detail Türkçe (değer + üçlü), DB'de kart yok, ERROR yok (dbtest).
- [x] `POST` `status="TEMYIZ"` → `DERDEST` + `case_stage=TEMYIZ`; status yok → DERDEST (dbtest + sqlite parametreli).
- [x] `PUT /api/cases/{id}` `"Arşivde"` + court/subject/esas_no → 400; kart satırı (updated_at dahil), `case_history`,
  `case_stage_logs`, `case_esas_numbers` sayıları birebir aynı; ERROR yok. `"TEMYIZ"` → DERDEST + aşama (dbtest).
- [ ] **Takip ucu HTTP 400 — yazıldığı gibi KARŞILANMADI.** Görev öncülü ("bugün 404") HTTP katmanında yanlış:
  `schemas.CaseTrackingUpdate` (`schemas.py:487-557`) `status` alanı TAŞIMAZ; pydantic fazla alanı sessizce düşürür →
  `PATCH /api/cases/{id}/tracking` gövdesindeki `status` yöneticiye hiç ulaşmaz. Eski kodda ölçüldü: `{"case_stage":"KARAR",
  "karar_no":"2026/7","status":"Arşivde"}` → **200** `{"status":"success"}` (status yok sayıldı, öteki alanlar yazıldı).
  `update_case_tracking`'in tek çağıranı bu route; frontend `lib/trackingDraft.ts` status göndermez. HTTP 400 için şemaya
  `status` eklemek gerekir — `schemas.py` dosya kapsamı DIŞI ve takip ucundan durumu YAZILABİLİR yapan bir API genişlemesi
  (ürün kararı). Yapılan: yönetici kapısı (fonksiyon sözleşmesi `TRACKING_FIELDS`'ta status taşıyor) + gerçek Postgres'te
  yönetici düzeyi dbtest (hiçbir alan yazılmaz, CheckViolation yok, ERROR yok). **Karar bekleyen:** takip şemasına
  `status` eklensin mi (eklenirse 400 kendiliğinden gelir), yoksa `TRACKING_FIELDS`'tan `status` çıkarılsın mı?
  **Kullanıcı kararı 14.09: DOKUNMA.** Şema ve `TRACKING_FIELDS` olduğu gibi kalır; durumun tek yazma yolu
  `PUT /api/cases/{id}` (400 kapısı orada), takip ucu status'u yok sayar, yönetici kapısı savunma katmanı olarak durur. Konu kapandı.
- [x] dbtest: scratch Postgres (`g196`, `_scratch_database` + `_run_init_db`, G195 kısıtı yerinde); engine `handle_error`
  dinleyicisi CheckViolation'a hiç ulaşılmadığını, caplog ERROR yokluğunu doğrular; iki kanıt aracı kontrol testiyle
  (`test_pg_kanit_araclari_bos_gecmez`: ham INSERT CheckViolation yakalanır, `AdminManager` ERROR yakalanır) boş geçmez.
- [x] Tarama (aşağıda) — üçlü dışı yazabilen yol bulunmadı.
- [x] Mevcut testler gövdeleri değişmeden yeşil; `update_case_tracking` `False`/404 dönüşünü kilitleyen test yok.

### Kırmızı-yeşil kanıtı
Yeni test dosyası kod değişikliğinden ÖNCE eski kodda koşuldu: **26 failed, 5 passed**. dbtest'lerin sebebi doğru:
POST serbest → 500 `Failed to save case`; POST TEMYIZ → 500 `Failed to save case` (CheckViolation); PUT serbest → 500
`Dava güncellenemedi`; takip HTTP → 200 (yukarıdaki bulgu; test bundan sonra yönetici düzeyine çevrildi). sqlite
katmanı eski kodda serbest metni yazıyordu (`DID NOT RAISE` / ImportError). Değişiklik sonrası: **31 passed**.
Not: yönetici düzeyi takip dbtest'inin son hâli eski kodda ayrıca koşulmadı (gerçek DB'ye yazma riskli bir simülasyon
gerekirdi); eski yol aynı desen (commit → CheckViolation → `update_case_tracking error` ERROR + `False`) — Kapı mekanik
doğrular.

### Tarama (düzeltme kapsam dışı)
- `scripts/kartsiz_foy_kart_ac.py:156` `status="DERDEST" if aktif else "MAHZEN"` → `:199` `add_case` üçlü alır. Temiz.
- `scripts/birlesik_kart_ayir.py:190-191` `models.Case(status=aday.status)` — aday `kfa.kart_adaylari`'ndan (aynı `:156`). Temiz.
- `scripts/import_excel_cases.py:113-116` `STATUS_MAP` {Aktif→DERDEST, Arşiv→MAHZEN}, `:246` varsayılan MAHZEN → `:248`. Temiz.
- `scripts/mukerrer_kart_birlestir.py:222` `kalan.status = "DERDEST"`. Temiz.
- `scripts/hukdok_aktarim.py:633` `DURUM_ESLEMESI` {AKTIF→DERDEST, ARSIV→MAHZEN}, `:849` `_esleme` (`:604`: tanınmayan
  değer None → yazılmaz). Temiz.
- `routes/case_intake.py:1095` `case_dict["status"] = "DERDEST"` (sunucu zorlaması, `add_case`'ten önce). Temiz.
- `routes/processing._auto_update_case_status` status'a değil `case_stage`'e yazar (test_durum_uclusu). Temiz.
- Backend'de başka `.status =` ataması yalnız export/upload outbox satırları (cases değil).

### Kararlar
- Yeni tip `constants.py`'de (kapsam böyle istedi; `stage_decisions`'a bağımlılık doğmasın); `InvalidDecisionStatusError`
  yeniden kullanılmadı — mesaj ve anlam farklı, handler ikiz.
- Kapı sırası: `add_case` oturumdan önce; `update_case`/`update_case_tracking` dava aramasından SONRA (olmayan dava + serbest
  durum → 404 anlamı korunur; G066 kapısıyla aynı sıra).
- `add_case` boş string (`""`) → DERDEST. Eskiden Postgres'te CheckViolation/500'dü; görev "add_case varsayılanı DERDEST".
- `add_case` artık `data["case_stage"]`'i yazar (yalnız legacy aşamaya karşı öncelik için). `CaseCreate`'te alan yok →
  API'den gelmez; script çağıranı göndermiyor. Pratik etki yok.

### Test sonuçları
- `pytest tests/test_g196_status_kapisi.py` → 31 passed
- `pytest` (tam) → **3567 passed, 3 skipped**
- `ruff check . --extend-exclude calibration-data` → All checks passed
- `mypy` → Success: no issues found in 62 source files

### İzlenecekler
- Takip şemasına `status` kararı (yukarıda) — karar verilene dek takip ucundan durum değişmez (bugünkü davranış).
- Çalışma ağacında kapsam dışı `.claude/settings.local.json` (M) ve `.claude/launch.json` (??) vardı; dokunulmadı, commit'e girmedi.
