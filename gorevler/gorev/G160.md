# G160 — `yazim_birligi.py`: tek seferlik yazım dönüşümü (uzmanlık, taraf adı, mahkeme, konu, rol, büro türü listesi)

- **Bant:** backend
- **Bağımlı:** G159 (`tr_title` düzeldikten sonra; hedef yazım onunla üretilir)
- **Dosya kapsamı:** backend/scripts/yazim_birligi.py (YENİ, dry-run varsayılan, `--apply`, `--kim`), backend/tests/test_g160_yazim_birligi.py (YENİ)
- **Dokunma:** backend/scripts/hukdok_aktarim.py, backend/managers/reference_lists.py (`update_item` yalnız ÇAĞRILIR),
  backend/models.py, frontend/**; `case_documents.muvekkil_adi` (deprecated kopya, DOKUNMA); `case_parties` satırı
  SİLİNMEZ/yeniden yaratılmaz (belge bağı `case_party_id`, FAZ F şartı)

## Hedef

Plan 08.09 §5.3-B (kullanıcı onayı 08.09). Ölçüm (lokal, `yazim_farki_olc.py`, ayrıntı `yalniz_harf_detay.csv`):

| Adım | Kaynak harita | Hedef | Lokal satır |
| --- | --- | --- | ---: |
| 1 | föy `ham_veri["Uzmanlık Alanı"]` ile YALNIZ_HARF çiftleri (17 tekil) | `cases.sub_type` | 4.521 |
| 2 | `ham_veri["Müvekkil"]`/`["Karşı Taraf"]` çiftleri (68 + 170 tekil) → teslim yazımı; DB-içi ikiz kalanı (111 grup) → baskın yazım, o da BÜYÜKse `tr_title` | `case_parties.name` | ~3.450 + ikiz |
| 3 | DB-içi ikiz (64 grup) → baskın Title yazım | `cases.court` | 1.115 |
| 4 | DB-içi ikiz (6 grup) → baskın | `cases.subject` | ~110 |
| 5 | `party_roles` listesindeki yazım | `case_parties.role` ("DAVALI"/"DAVACI") | 9 |
| 6 | `update_item` rename (DEPENDENCIES kartlara yayar) | `bureau_types` 8 satır BÜYÜK → kart yazımı ("Dr Özel"); "Hasta" listeye eklenir (42 kart); "Tür Seçiniz" → boş (1 kart) | 8 + 42 + 1 |
| — | **DOKUNMA** | Sigortalı, Davalı İdare (teslim BÜYÜK), `istinaf_mahkemesi`/`temyiz_mahkemesi` + `appeal_courts`/`cassation_courts` (paket kaynaklı, A sınıfı), avukat adları | — |

Kural: yalnız **Türkçe büyük-harf anahtarı aynı** (`turkish_upper`) olan çiftler dönüşür; içerik farkı asla.

## Yapılacaklar

1. Script: her adım ayrı fonksiyon, `--adim 1,2,…` seçimi; dry-run çıktısı adım başına (tekil değer, satır sayısı,
   ilk 20 örnek) + CSV (`/tmp/yazim_birligi/<adim>.csv`). `--apply`: `case_history` `source="yazim_birligi"`,
   `changed_by=--kim` (kart alanları için); taraf satırı UPDATE (id sabit). İkinci `--apply` 0 değişiklik.
2. Baskın yazım kuralı: gruptaki en çok satırlı yazım; eşitlikte teslim yazımı, o da yoksa `tr_title`.
3. Güvenlik: belge envanteri (`case_documents` sayısı + `case_party_id` NULL sayısı) önce/sonra eşit; `deleted_at`
   dolu kartlar atlanır; `--limit` yok.
4. Testler: her adım için sqlite fixture (çift + ikiz + dokunulmayacak alan); idempotency; envanter denk; içerik
   farklı çift dönüşmez.
5. Lokal koşu (raporda): adım başına sayılar, `yazim_farki_olc.py` yeniden koşusu → YALNIZ_HARF (DB-008 sütunlarında) 0.
   Prod'da paket uygulamasından SONRA, yedekle (insan).

## Kabul kriterleri

- [x] Dry-run varsayılan; `--apply` tarihçeli; ikinci koşu 0 (test + lokal).
- [x] Dokunulmayan alanlar diff'te yok (test: Sigortalı/istinaf mahkemesi değişmez).
- [x] Belge envanteri ve `case_party_id` sayıları eşit (test + lokal).
- [x] Lokal ölçüm sonrası DB-008 sütunlarında YALNIZ_HARF 0 (rapor — teslim yazımının hedef olduğu dört ölçümde
      0; `court`/`subject` için teslim BÜYÜK yazar ve bilinçli hedef değildir, DB-içi ikiz 0 — aşağıda).
- [x] ruff + mypy temiz; pytest özet satırı.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-10, gece kuyruğu işçisi)

### Yapılanlar

1. **`backend/scripts/yazim_birligi.py`** (YENİ): kuru koşu varsayılan, `--apply --kim <kullanıcı>`, `--adim 1,3`,
   `--cikti-dizini` (varsayılan `/tmp/yazim_birligi`, adım başına `<adim>.csv`: tablo/kayıt/kart/alan/eski/yeni/kaynak).
   Her adım ayrı fonksiyon (`ADIMLAR` sözlüğü), hepsi salt hesaplar; `_uygula` TEK oturumda UPDATE + `case_history`
   (`source="yazim_birligi"`, `changed_by=--kim`; kart alanı = kolon adı, taraf satırı aktarımla aynı `taraf` alanı
   "ad (rol)" biçiminde). Belge envanteri (`services/belge_envanteri`) commit'ten önce aynı oturumda ölçülür; fark
   varsa rollback + tek ERROR + çıkış kodu 2. Taraf satırı yerinde UPDATE (`id` sabit) — satır silinmez/yaratılmaz.
   - Anahtar: `turkish_upper` + boşluk normalize (`anahtar`). Yalnız aynı anahtarlı değerler dönüşür; ASCII `I` ≠
     Türkçe `İ` içerik sayılır (`SIGORTALI` rolü bu yüzden dokunulmadan kaldı — test).
   - **Baskın yazım** (`baskin_yazim`): en çok satırlı; eşitlikte teslim, o da yoksa `tr_title`; seçilen tamamı
     BÜYÜKse `tr_title`. Adım 1-2'de teslim yazımı varsa o kazanır (satır sayısına bakılmaz; teslim BÜYÜK
     gelirse `tr_title` — kaynak sütununda `tr_title` etiketi).
   - Adım 1 `sub_type`: föy `ham_veri["Uzmanlık Alanı"]`; yoksa DB-içi ikiz → baskın (föysüz kartlar da kapanır).
     Adım 2 `case_parties.name`: `ham_veri["Müvekkil"/"Karşı Taraf"]` (`_taraf_adlari` ile `;` bölmesi, aktarımla
     aynı); yoksa ikiz → baskın. Adım 3 `court` / 4 `subject`: yalnız DB-içi ikiz (teslim yazımı BÜYÜK, mahkeme adı
     kimliği bizim — kullanılmaz). Adım 5 `role`: `party_roles` (aktif) adı. Adım 6: liste adı BÜYÜKse hedef =
     kart yazımı (kartlardaki baskın; yoksa `tr_title`), `normalize_list_name`den geçirilir; kartlar bu betikte
     TARİHÇELİ hizalanır, liste `update_item` ile (commit SONRASI, kendi oturumunda; DEPENDENCIES yayılımı artık
     yalnız aynı değeri yeniden yazar + silinmiş kartlar). "Hasta" `add_item` (kod `HASTA`), "Tür Seçiniz" → NULL.
   - **Dokunulmaz:** rolü `Sigortalı`/`Davalı İdare` olan taraf satırları (`KORUNAN_ROLLER`, `TARAF_SUTUNLARI`ndan
     türetilir) — ne güncellenir ne baskınlık sayımına girer; `deleted_at` dolu kartlar ve tarafları; kapsam dışı
     föyün ham satırı; `istinaf_mahkemesi`/`temyiz_mahkemesi`, iki mahkeme listesi, avukat adları, `muvekkil_adi`
     (betik bu kolonlara hiç bakmaz). `--limit` yok.
2. **`backend/tests/test_g160_yazim_birligi.py`** (YENİ, 16 test): saf kurallar (anahtar, baskın tablosu, `--adim`
   ayrıştırma, `--kim` zorunluluğu), sqlite zemini (7 kart: teslim çifti + ikiz + silinmiş + kapsam dışı föy; 15
   taraf: teslim/ikiz/baskın-BÜYÜK/Sigortalı/silinmiş/ASCII-I rolü; 3 belge; 2 liste): kuru koşu yazmaz + CSV/özet,
   `--apply` altı adımın beklenen değerleri + tarihçe imzası + ikinci koşu 0 + envanter denk + taraf `id`/kart bağı
   aynı + istinaf/appeal_courts değişmez, `--adim 3` yalnız `court`, envanter farkı → rollback + NONZERO + tek
   ERROR + liste işlemi koşmaz, içerik farklı çift dönüşmez (yalnız boşluk farkı dönüşür).

### Lokal koşu (yedek: `C:\hukdok-veri\yedek\pre_g160_20260910.dump`, 5,1 MB)

Kuru koşu = `--apply` (aynı sayılar); `--apply --kim ilke` → YAZILDI, belge envanteri DENK (229 belge / 7 tarafa
bağlı, önce=sonra); `case_history` 6.574 satır (`taraf` 6.201 · `sub_type` 177 · `subject` 111 · `court` 83 ·
`bureau_type` 2), tamamı `source=yazim_birligi`, `changed_by=ilke`. **İkinci `--apply`: 0 satır, 0 liste işlemi.**

| Adım | Satır | Tekil | Kaynak | Görev tahmini → neden farklı |
| --- | ---: | ---: | --- | --- |
| 1 `sub_type` | 177 | 11 | teslim 177 | 4.521 — G159'un aktarım koşusu 3.590 kartı zaten yazmıştı; kalan 115 kardeş-çelişkili + 62 föysüz kart burada kapandı (`" Ve "` içeren kart 0) |
| 2 `case_parties.name` | 6.192 | 253 | teslim 6.173 · baskın 7 · tr_title 12 | ~3.450 + ikiz — harita anahtar-global (aynı ad başka kartta da dönüşür), ikizler dahil |
| 3 `court` | 83 | 64 | baskın 83 | 1.115 = ikiz gruplarındaki TOPLAM satır; yalnız azınlık yazım (83) döner |
| 4 `subject` | 111 | 6 | baskın 111 | ~110 ✓ |
| 5 `role` | 9 | 2 | liste 9 | 9 ✓ |
| 6 `bureau_type` | 2 (+8 rename +1 add) | 2 | liste 1 · boşalt 1 | 8 + 42 + 1 ✓ ("Hasta" 42 kart zaten o yazımda, listeye eklendi) |

`yazim_farki_olc.py` yeniden koşusu (salt okunur), önce → sonra:

| Ölçüm | Önce | Sonra |
| --- | ---: | ---: |
| `cases.sub_type` YALNIZ_HARF (föy çifti) | 196 | **0** |
| `case_parties[Müvekkil]` YALNIZ_HARF | 3.215 | **0** |
| `case_parties[Karşı Taraf]` YALNIZ_HARF | 237 | **0** |
| `case_parties.role` YALNIZ_HARF | 9 | **0** |
| DB-içi ikiz: `court` / `sub_type` / `subject` / `bureau_type` / `role` | 64 / 11 / 6 / 1 / 2 grup | **0 / 0 / 0 / 0 / 0** |
| DB-içi ikiz `case_parties.name` | 112 grup / 7.669 satır | 56 grup / 150 satır — tamamı Sigortalı (BÜYÜK, korunur) ↔ başka roldeki Title satır; korunan satır sayımdan çıkınca tek varyant kalır, dokunulmaz |
| `bureau_types` tamamı BÜYÜK | 8 | 0 (9 satır, "Hasta" dahil) |
| `case_parties[Sigortalı]` / `[Davalı İdare]` YALNIZ_HARF | 803 / 131 | 803 / 131 (DOKUNMA) |
| `cases.court` YALNIZ_HARF (föy çifti) | 407 | 486 — bilinçli: teslim BÜYÜK yazar, biz Title; 83 kart daha Title'a döndüğü için "yalnız harf farkı" çifti ARTAR |
| `cases.subject` YALNIZ_HARF (föy çifti) | 10 | 9 (`TAZMİNAT ` 7, `GÖREVİ İHMAL` 2 — teslim BÜYÜK, bizde Title; hedef değil) |
| avukat adları | 476 / 2.571 | aynı (kapsam dışı) |

### Kararlar

- **Adım 1-2'de teslim yazımı satır sayısına bakılmaksızın kazanır** (görev tablosu "→ teslim yazımı"); teslim
  BÜYÜK gelirse `tr_title` (DB-008 Title) — 12 satır. `court`/`subject`'te teslim BİLİNÇLİ kullanılmaz.
- **Taraf haritası anahtar-global** (aynı kartla sınırlı değil): aynı harf dizisi = aynı ad; bu yüzden adım 2
  görev tahmininin üstünde (6.192). Ölçüm betiği kart-içi çift sayıyordu.
- **Korunan roller baskınlık sayımına da girmez**: aksi hâlde Sigortalı BÜYÜK çoğunluğu Title satırları
  BÜYÜĞE çekerdi; şimdi o gruplar tek varyant sayılır ve kalır (56 grup / 150 satır, yukarıda).
- **Adım 6 kartları betik yazar, listeyi `update_item`**: tarihçe böyle tam; `update_item` yayılımı aynı değeri
  yeniden yazar ("yayılan 1520" gibi sayılar no-op UPDATE'tir; Lexis 1.963 = 1.957 + 6 silinmiş kart —
  silinmiş kartlardaki eski liste yazımı tarihçesiz döner, admin paneli davranışıyla aynı).
- Kapsam dışı föyün ham satırı teslim haritasına alınmaz (aktarımın kardeş-föy uzlaşısı kuralı).

### Test sonuçları (konteynerde)

- `python -m pytest`: ilk tam koşu **1 failed, 3215 passed, 3 skipped** — `test_no_basicconfig_left_in_backend`
  (Faz 2-B bekçisi: `main()`deki `logging.basicConfig`) → `logging_setup.configure_logging()` ile düzeltildi (aktarım
  CLI'siyle aynı); ikinci tam koşu **3216 passed, 3 skipped, 0 failed** (16'sı yeni dosyadan). Hedefli: `test_g160` 16 passed,
  `test_faz2_logging` + `test_g160` 35 passed 1 skipped.
- `ruff check .`: All checks passed. `mypy`: Success, 62 source files (scripts/ taranmaz).

### İzlenecekler / NOT

- **Prod'a UYGULANMADI.** Sıra: 04.09 paketi aktarımı (G159 sonrası) → yedek → `yazim_birligi.py` kuru koşu →
  `--apply --kim <kullanıcı>` → ikinci koşu 0 kontrolü (insan). Kuru koşu çıktısını okuyun: adım 2'de teslimin
  bağlaç-küçük yazımları ("… adına Velayeten") teslim yazımı olarak geçer.
- NOT: Git Bash `/tmp/...` argümanını Windows yoluna çevirir (konteynerde `/app/C:/...` dizini açıldı, bind-mount'ta
  `backend/C<U+F03A>/` olarak belirdi — silindi). Lokalde `MSYS_NO_PATHCONV=1` ile koşun; prod (Linux) etkilenmez.
- NOT (kapsam dışı, dokunulmadı): `scripts/README.md` tablosuna yeni betik satırı eklenmedi (dosya kapsamı dışı);
  `cases.bureau_type="Sağlık Personeli"` 1 kart listede yok (görevde yok, bırakıldı); `case_parties.role`
  `SIGORTALI` (ASCII I) 3 satır ve `SIGORTA_SIRKETI` 3 satır içerik farkı sayılır, kaldı; kalan 56 Sigortalı-ikiz
  grubu yukarıda.
- NOT: çalışma ağacında kapsam dışı kirli dosyalar vardı (`.claude/settings.local.json` M, `.claude/launch.json`
  ??) — harness dosyaları, dokunulmadı, commit'e alınmadı.
- Konteyner `/tmp/yazim_birligi/{kuru,cli}` ve `/tmp/yazim_farki/` kalıntıları recreate'te uçar.
