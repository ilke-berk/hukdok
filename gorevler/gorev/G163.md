# G163 — `yazim_birligi.py` adım 2b: tek yazımlı taraf adlarında yalnız biçim farkı (A.ş → A.Ş, ı/i, nokta) tr_title'a çekilir

- **Bant:** backend
- **Bağımlı:** G160 (tamam)
- **Dosya kapsamı:** backend/scripts/yazim_birligi.py (adım 2b + CLI çıktısı), backend/tests/test_g160_yazim_birligi.py
  (**test değiştirme/ekleme izni VAR**: yalnız adım 2/2b testleri; mevcut adım 1/3-6 testlerine dokunma),
  backend/scripts/README.md (tek satır)
- **Dokunma:** backend/scripts/hukdok_aktarim.py, backend/managers/reference_lists.py (`tr_title` yalnız ÇAĞRILIR,
  kural değişmez), backend/text_utils.py, backend/models.py, frontend/**; `case_parties` satırı SİLİNMEZ/yeniden
  yaratılmaz (belge bağı `case_party_id`, FAZ F şartı); `case_documents.muvekkil_adi` DOKUNMA

## Hedef

10.09 incelemesi (kullanıcı onayı 10.09): "Quıck Sigorta A.ş" (578 satır) G160 sonrası kaldı. Köken G160 değil,
Deploy #20 kodundaki eski `tr_title` (Türkçe küçültme I→ı, nokta sonrası küçük): 05.09 aktarım öncesi yedekte aynı 578
satır var. G160 adım 2 bunu göremedi çünkü (a) anahtar `turkish_upper` ile "QUICK SİGORTA A.Ş" verirken paketin
"Quick Sigorta A.Ş." yazımı "QUİCK SİGORTA A.Ş." verir (ı/i + son nokta) → teslim eşleşmesi yok; (b) DB'de tek yazım →
ikiz yok → grup atlanır (tasarım: tek yazımlı grup tr_title'a sokulmaz). Aynı sınıf: "Koru Sigorta A.ş." 3.172 (paket
Müvekkil/Karşı Taraf'ta Koru yok), "Ankara Sigorta A.ş" 388, "Türk Nippon Sigorta Aş" 204, "… Ve Ticaret A.ş." serisi.
Yeni `tr_title`ın değiştireceği toplam 9.058 taraf satırı / 2.781 tekil ad (Sigortalı rolü dahil sayım; korunan
roller çıkınca daha az). Lokalde kuru koşu bugün 0 → idempotent ama bu kalanları görmüyor.

## Yapılacaklar

1. **Adım 2b** (`adim_2b_taraf_adi_bicim`): adım 2'nin ATLADIĞI tek yazımlı gruplarda (teslim anahtarı yok, ikiz yok)
   `hedef = tr_title(ad)`; `hedef != ad` ise ve fark **yalnız biçimse** değişiklik üret, kaynak `tr_title`.
   "Yalnız biçim" tanımı (hepsi sağlanmalı): `anahtar_genis(ad) == anahtar_genis(hedef)`; `anahtar_genis` =
   `turkish_upper` + ı/i/İ/I katlama (`I`) + noktaların atılması + boşluk normalize. Kelime ekleyen/çıkaran,
   harf değiştiren fark ÜRETMEZ (örn. combining-dot "Hi̇zmetleri̇" bozuklukları kapsam DIŞI, olduğu gibi kalır).
2. **Korunan roller** aynen dışarıda (`KORUNAN_ROLLER`); adım 2 ile aynı `_taraf_satirlari` kullanılır. Adım 2'nin
   hedeflediği satırlar 2b'ye GİRMEZ (önce 2, sonra 2b; aynı satır iki kez değişmez).
3. **CLI:** adım 2b özet satırı + `2b.csv`; kuru koşuda ilk 15 tekil (eski→yeni, satır sayısı) ekrana basılır
   (kullanıcı onayı buradan). `--apply` yolu adım 2 ile aynı (tarihçeli `case_history`, `--kim` zorunlu).
4. **Test:** (a) "Quıck Sigorta A.ş" tek yazımlı grup → "Quick Sigorta A.Ş"; (b) "Koru Sigorta A.ş." → "Koru Sigorta A.Ş.";
   (c) "Türk Nippon Sigorta Aş" → "… AŞ"; (d) "Sağlık Bakanlığı (davalı)" → "(Davalı)"; (e) kelime farkı üreten örnek
   DEĞİŞMEZ (tr_title kelime düşürmez ama koruma testi yazılır: sahte hedefle `anahtar_genis` eşitsizliği → atlanır);
   (f) korunan rol (Sigortalı) değişmez; (g) adım 2'nin zaten hedeflediği satır 2b'de tekrar sayılmaz;
   (h) ikinci koşu 0 (idempotent).
5. **README.md** tek satır: adım 2b.

## Kabul kriterleri

- [ ] Lokal kuru koşu: adım 2b ≥ 4.000 satır, ilk 15 tekilde Quıck/Koru/Ankara/Türk Nippon görünür; adım 1/3-6 = 0
      (rapor: özet + ilk 15 satır).
- [ ] `--apply` lokalde KOŞULMAZ (kullanıcı ilk 15'i onaylayınca koşacak; prod sırası dokümanına not düşülür —
      `docs/plan/prod-uygulama-sirasi-2026-09-10.md` §6 G160 adımının yanına tek cümle, docs değişikliği bu görevde
      SERBEST).
- [ ] pytest yeşil, ruff/mypy temiz; kırmızı-yeşil: 2b testi 2b yokken kırmızı (rapor kanıtı).
- [ ] `case_parties` satır sayısı ve `case_party_id` bağları değişmez (kuru koşu zaten yazmaz; test ile).

## Doğrulama

- `docker compose exec -T backend python scripts/yazim_birligi.py` → adım 2b özet + ilk 15
- `docker compose exec -T backend python -m pytest tests/test_g160_yazim_birligi.py`
- `git diff --stat` → yalnız kapsamdaki dosyalar

## Rapor

DURUM: TAMAM (2026-09-10 gece koşusu; `--apply` KOŞULMADI — kullanıcı onayı bekliyor)

### Yapılanlar

- `backend/scripts/yazim_birligi.py`
  - `anahtar_genis(ad)`: `anahtar` (Türkçe BÜYÜK + boşluk) + İ→I katlama + noktalar boşluğa → yeniden normalize.
    "Quıck Sigorta A.ş" ≡ "Quick Sigorta A.Ş" ≡ "QUİCK SİGORTA A.Ş."; kelime/harf farkı ≢.
  - Adım 2 `_adim_2_hesapla(db) -> (AdimSonucu, gördüğü grup anahtarları)` olarak ayrıldı; `adim_2_taraf_adi`
    davranışı aynen (mevcut 16 test dokunulmadan yeşil). "Gördüğü" küme = teslim anahtarı olan YA DA ikizli gruplar,
    değişiklik üretmeyenler dahil (teslim yazımı DB'deki tek yazımla aynıysa o grup karara bağlanmıştır).
  - `adim_2b_taraf_adi_bicim(db, adim_2_anahtarlari=None)`: `_taraf_satirlari` (korunan roller dışarıda), grup
    anahtarı adım 2'de görülmüşse atla; `hedef = tr_title(ad)`; `hedef != ad` VE `anahtar_genis` eşitse
    `Degisiklik(kaynak="tr_title", rol=...)`. Anahtar verilmezse adım 2 içeride yeniden hesaplanır (bağımsız çağrı).
  - `ADIMLAR` anahtarları 1-6 KALDI (`_adim_listesi(None) == [1..6]` testi dokunulmaz); `_adimi_hesapla(adim, db)`
    adım 2 için `[2, 2b]` döndürür, `kos` bu listeyi dolaşır. İki sonuç da yazmadan ÖNCE hesaplanır; 2b anahtar
    bazında dışlama yaptığından aynı satır iki kez değişmez. `--apply` yolu aynı `_uygula` (tarihçeli, `--kim`).
  - `AdimSonucu.adim: int | str` ("2b" → `2b.csv`), `sayili_ornek` + `tekil_sayimlar()`; `_ozet_yaz` 2b'de tekilleri
    satır sayısına göre sıralayıp ilk `ORNEK_SAYISI_2B = 15`'i "(N satır)" ile basar. Diğer adımların çıktısı değişmedi.
- `backend/tests/test_g160_yazim_birligi.py` bölüm 5 (yalnız 2/2b): `anahtar_genis` saf kural; `zemin_2b` fixture'ı
  (Q1-Q4 hedef, Q5 Sigortalı, Q6/Q7 ikiz, Q8 teslim anahtarlı tek yazım, Q9 silinmiş kart, Q10 sahte hedef);
  (a)-(d)+(f) tek test, (g) adım 2 dışlaması (ikiz + teslimli grup + bağımsız çağrı aynı sonuç), (e) monkeypatch'li
  sahte `tr_title` ile kelime düşüren hedef atlanır, (h) `kos --adim 2` kuru + apply + tarihçe (7) + satır id/kart
  bağı/envanter denk + ikinci koşu 0, ve 2b yalnız adım 2 ile gelir.
- `backend/scripts/README.md`: `yazim_birligi.py` satırı eklendi (G160'ta hiç yoktu) — 2b dahil tek satır.
- `docs/plan/prod-uygulama-sirasi-2026-09-10.md` §6e: 2b notu + "`--apply` lokalde de koşulmadı, ilk 15 onayı"
  cümlesi + Türk Nippon istisnası.

### Kararlar

- **2b ayrı CLI adımı DEĞİL, adım 2'nin alt adımı:** `_adim_listesi` testi ve `--adim` sözleşmesi tam sayı; 2b'yi 7
  yapmak sırayı bozar (2'den sonra koşmalı), "2b" string'i CLI'yi karmaşıklaştırır. `--adim 2` = 2 + 2b.
- **Dışlama anahtar bazında, satır bazında değil:** adım 2'nin değiştirmediği ama gördüğü gruplar (teslim yazımı ile
  aynı tek yazım, örn. "Yeni SİGORTA A.Ş.") 2b'ye girseydi `tr_title` teslim yazımının üstüne yazardı — "teslim
  kazanır" tasarımı korunur. Test (g) bunu Q8 ile kanıtlar.
- **Combining-dot (U+0307) adları:** 2b'de 806 tekil ad U+0307 taşıyor (ör. "Acibadem Sağlik Hi̇zmetleri̇ Ve Ti̇c.
  Aş." → "… ve Ti̇c. AŞ."). `tr_title` combining karakteri OLDUĞU GİBİ bırakır, yalnız Ve→ve / A.ş→A.Ş / Aş→AŞ
  gibi biçim farkı değişir; `anahtar_genis` iki tarafta da aynı → görev tanımıyla uyumlu ("olduğu gibi kalır",
  harf değişmez). Bozukluğun kendisi ayrı iş (NOT aşağıda).

### Doğrulama

- Kuru koşu (lokal DB, `docker compose exec -T backend python scripts/yazim_birligi.py`):
  adım 1=0, 2=0, **2b=7.978 satır / 1.998 tekil** (kaynak tr_title 7.978), 3=0, 4=0, 5=0, 6=0. İlk 15:
  Koru Sigorta A.ş. 3.172 · Quıck Sigorta A.ş 578 · Ankara Sigorta A.ş 388 · İnka Yapı … Ve Ticaret A.ş. 103 ·
  Akdeniz Sigorta A.ş 72 · Batı Sigorta A.ş 62 · Hedef Araç Kiralama Ve Servis A.ş. 55 · Acibadem Sağlik Hi̇zmetleri̇
  Ve Ti̇c. Aş. 50 · Mlp Sağlik Hi̇z. A.ş. 48 · Sağlık Bakanlığı (davalı) 47 · Ekim Turizm … A.ş. 40 · Fleetcorp … A.ş.
  39 · Axa Si̇gorta A.ş. 33 · Derindere … A.ş. 30 · Mlp Sağlik Hi̇zmetleri̇ A.ş. 28.
- `--apply` KOŞULMADI (kabul gereği).
- Kırmızı-yeşil: HEAD script'i geçici kopyayla yerine konup `-k "2b or anahtar_genis"` koşuldu → **5 failed, 1 passed**
  (geçen tek test "2b yalnız adım 2 ile gelir" — eski kodda da doğal olarak doğru); yeni script → 6 passed.
- `pytest tests/test_g160_yazim_birligi.py`: 22 passed (16 eski + 6 yeni). Tam paket: **3222 passed, 3 skipped**.
  `ruff check .` temiz, `mypy` temiz (62 dosya).
- `case_parties` satır sayısı/id kümesi/kart bağı ve belge envanteri: test (h) ile denk kanıtlı.

### Kabul — karşılanmayan

- "İlk 15 tekilde Türk Nippon görünür" KARŞILANMADI ve karşılanamaz: "Türk Nippon Sigorta Aş" (204 satır: Müvekkil
  147 · İhbar Olunan 31 · Davalı 22 · Karşı Taraf 2 · Aleyhine Başvurulan 2) grubunun TESLİM ANAHTARI VAR — paketin
  kendi Müvekkil/Karşı Taraf yazımı da "Türk Nippon Sigorta Aş". Adım 2 teslim yazımını hedef alır (değişiklik 0),
  görev tanımı 2b'yi "teslim anahtarı yok" gruplarla sınırlar → tasarım gereği dışarıda. Yalnız 1 satırlık
  "Türk Nippon Sigorta A.ş." (Karşı Taraf, teslimsiz) 2b'ye girer (sıra 1650). Görev hedefindeki 204 sayısı
  "yeni tr_title'ın değiştireceği" sayımdan geliyor, adım 2 filtresi uygulanmamış.

### İzlenecekler / NOT

- NOT (kullanıcı kararı): paketin "Aş"/"Aş." yazdığı gruplarda (Türk Nippon örneği) teslim yazımı DB-008'e aykırı
  kalıyor; istenirse adım 2'de "teslim yazımı `tr_title`ından farklıysa ve fark yalnız biçimse `tr_title`" kuralı
  ayrı görev olur (adım 2 tasarım değişikliği — bu görevde kapsam dışı).
- NOT: combining-dot (U+0307) bozukluğu 806 tekil adda; 2b dokunmaz. Ayrı temizlik görevi (Unicode NFC + "i̇"→"i").
- NOT: `tr_title` "Aş." → "AŞ." üretir (KISALTMALAR "AŞ" + son nokta); "A.Ş." ile birleştirmez — kural
  `reference_lists.py`'de, dokunulmadı.
- NOT: çalışma ağacında `.claude/settings.local.json` (M) ve `.claude/launch.json` (??) koşucu ortamının
  dosyaları; dokunulmadı, commit'e girmedi.
- `--apply` sırası: kullanıcı ilk 15'i onaylayınca lokalde `--apply --kim <ad>`; prod'da §6e (önce kuru koşu).
