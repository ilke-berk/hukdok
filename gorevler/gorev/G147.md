# G147 — Teslim hattı ikinci SharePoint kimliği: `teslim` config'i (Hanyaloğlu tenant'ı), arşiv LexisBio'da kalır

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/sharepoint/auth_graph.py, backend/sharepoint/sharepoint_uploader_graph.py,
  backend/services/teslim_kutusu.py (yalnız `sharepoint_tara` çağrıları + modül şerhi),
  backend/services/teslim_cevap.py (yalnız `upload_file_to_sharepoint` çağrısı + modül şerhi),
  backend/.env.example (yeni env bloğu), backend/tests/test_g147_teslim_sharepoint_kimligi.py (YENİ),
  docs/mimari/veri-teslim-hatti.md (§1 tablo + §9 prod kurulumu maddesi — koddan satır referanslı)
- **Dokunma:** backend/email_sender.py, backend/managers/counter_manager.py, backend/managers/activity_manager.py,
  backend/scripts/repair_overwritten_documents.py (hepsi `get_graph_token()` / `_get_site_and_drive_id(token)`
  çağırır — imza geriye uyumlu kalır, bu dosyalara DOKUNMA), backend/services/upload_queue.py, backend/api.py,
  docker-compose.yml (env `env_file: .env` ile zaten geçer; compose değişmez), frontend/**

## Test değiştirme İZNİ (baştan)

**Değiştirmek İZİNLİ (yalnız sahte imzaları):** `tests/test_g109_teslim_gozcusu.py:232-233`,
`tests/test_g110_teslim_cevap.py:86,112`, `tests/test_g112_duzeltme_logu.py:668-669` — `lambda folder_name: []`
biçimindeki sahteler `config_type=` anahtar argümanını almadığı için `TypeError` verir; sahteyi
`lambda folder_name, **kw: []` (ya da `def _upload(*a, **kw)`) yap. **Beklentiler (`assert`) değişmez,
`expect`/`assert` sayısı azalmaz.** `tests/test_faz3_graph_retry.py` sahteleri zaten `config_type="default"`
kabul ediyor (`:105-106`, `:116`, `:394-446`) — dokunma; kırılırsa tasarım yanlış demektir, BLOKE bırak.
Diğer tüm testler KİLİT.

## Hedef

Veri ekibi (hanyaloglu@hanyaloglu-acar.av.tr, Entra tenant `9776cf1f…` = `hanyaloglu.sharepoint.com`)
`03_VERI_TESLIM/gelen` klasörünü göremiyor: prod arşiv site'ı LexisBio tenant'ında
(`lexisbio.sharepoint.com/sites/hukukarsivtest`, tenant `44f029f8…`; 30.07 DB kopyasındaki 176 belge URL'sinin
tamamı orada) ve 03.09'daki paylaşım tenant'lar arası misafir davetine düştü, davet ulaşmadı (06.09 e-postası §13).
Karar (kullanıcı, 08.09): **yalnız teslim hattı** (gözcü listeleme + indirme, cevap paketi yükleme)
Hanyaloğlu tenant'ındaki `hanyaloglu.sharepoint.com/sites/hukdok_arsiv` site'ına taşınır; arşiv, ofis-no sayacı,
`log` listesi, belge URL'leri ve hukukbot export'u LexisBio'da KALIR. Bugün kod tek-site/tek-kimliktir:
`auth_graph._get_msal_app(config_type)` parametreyi alır ama her config aynı env'i okur (`auth_graph.py:85-100`),
`_get_site_and_drive_id` her config'de `SHAREPOINT_SITE_URL`'i kullanır (`sharepoint_uploader_graph.py:131-142`),
üç çağrı noktası `"default"`a çivilidir (`:107`, `:115`, `:333`, `:432`, `:466`) ve lokal `.env`'deki
`UPLOAD_SHAREPOINT_*` değişkenlerini HİÇBİR kod okumaz (08.09 keşfi). Hanyaloğlu tenant'ındaki uygulama kaydı
CANLI (token alıyor), site + `Belgeler` drive + `01_HAM_ARSIV`/`02_YEDEK_ARSIV` var; `03_VERI_TESLIM` ve
`Counter`/`log` listeleri YOK (salt-okunur Graph çağrısıyla 08.09'da doğrulandı — sayaç/log listesi bu görevin
işi DEĞİL, arşiv taşınmıyor).

## Yapılacaklar

1. **`auth_graph.py` — ikinci kimlik seti.** Yeni env adları: `TESLIM_SHAREPOINT_TENANT_ID`,
   `TESLIM_SHAREPOINT_CLIENT_ID`, `TESLIM_SHAREPOINT_CLIENT_SECRET` (secret önce `vault.get_secret`, sonra env —
   default ile aynı sıra `:92-95`). `_get_msal_app("teslim")`: üçü de doluysa bu setle MSAL uygulaması kurar;
   biri bile boşsa **default sete düşer** ve BİR KEZ INFO basar ("teslim kimliği tanımsız, arşiv kimliği
   kullanılıyor") — geriye tam uyumlu, prod `.env` değişmeden davranış aynı. `_MSAL_APPS` anahtarı `config_type`
   olarak kalır (ayrı token cache). `config_type` uzayı: `"default" | "teslim"`; eski `"upload"` dalı
   (`:86-88`, `:134-135`) ölü koddur → kaldır ya da `"teslim"`e çevir, docstring'leri düzelt.
   `check_client_secret_expiry`'ye isteğe bağlı `env_name` parametresi: lifespan'da ikinci çağrı
   `TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT` ile (tanımsızsa sessiz — mevcut sözleşme). Log satırlarında
   secret'ın kendisi asla geçmez (mevcut kural).
2. **`sharepoint_uploader_graph.py` — site/drive çözümü config'e göre.**
   `_get_site_and_drive_id(token, config_type)`: `"teslim"` için `TESLIM_SHAREPOINT_SITE_URL`
   (boşsa `SHAREPOINT_SITE_URL`'e düş) ve `TESLIM_SP_DRIVE_NAME` (boşsa `SP_DRIVE_NAME`, o da boşsa
   `"Belgeler"` — hukdok_arsiv drive'ının adı `Belgeler`). **`@lru_cache(maxsize=1)` → `maxsize=4`:** anahtar
   `(token, config_type)`; iki config'in token'ı farklı olduğundan `maxsize=1` her config geçişinde
   site+drive'ı yeniden çözerdi (2 Graph GET; outbox upload ile gece gözcüsü art arda koşar). Docstring'deki
   "token anahtarlı TTL" gerekçesi korunur.
   `_with_fresh_token_on_401(fn, config_type="default")` → token'ı verilen config'le alır ve yeniler.
   `list_folder_children(folder_name, *, config_type="default")`, `download_file_from_sharepoint(folder_name,
   filename, *, config_type="default")`, `upload_file_to_sharepoint(..., config_type="default")` (keyword-only;
   `_upload_with_token`'a geçer). Varsayılanlar `"default"` → arşiv/outbox/e-posta/sayaç çağrıları DEĞİŞMEZ.
3. **Teslim tarafı tek sabit:** `teslim_kutusu.py`'de `TESLIM_SP_CONFIG = "teslim"` (modül şerhine gerekçe);
   `sharepoint_tara` iki çağrıya `config_type=TESLIM_SP_CONFIG` geçer (`:1262`, `:1275`);
   `teslim_cevap.py:524` aynı sabitle yükler (`tk.TESLIM_SP_CONFIG`). Klasör adları (`SHAREPOINT_FOLDER_TESLIM_NAME`,
   `gelen`/`cevap`) DEĞİŞMEZ — site değişir, yol aynı kalır.
4. **`.env.example`:** SharePoint bloğunun altına "Veri teslim SharePoint'i (isteğe bağlı ikinci kimlik)" bloğu:
   dört `TESLIM_SHAREPOINT_*` + `TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT` + `TESLIM_SP_DRIVE_NAME`, yorumlu
   varsayılanlar; "tanımsızsa arşiv kimliği/site'ı kullanılır; `.env` değişikliği `up -d` (recreate) ister"
   notu. `UPLOAD_SHAREPOINT_*` adlarını YAZMA (bayat ad; lokal `.env`'de duran değerler kullanıcı tarafından
   `TESLIM_*`'e elle taşınacak — insan adımı, aşağıda).
5. **Testler — `tests/test_g147_teslim_sharepoint_kimligi.py` (yeni, ağ yok):**
   - `_get_msal_app("teslim")`: üç env doluyken `msal.ConfidentialClientApplication` sahtesi `TESLIM_*` tenant/
     client ile kurulur; biri boşken default değerlerle kurulur + INFO tek sefer (caplog); `_MSAL_APPS`
     iki anahtarı ayrı tutar (test sonunda `pop`).
   - `_get_site_and_drive_id(token, "teslim")`: `TESLIM_SHAREPOINT_SITE_URL` doluyken Graph `sites/{host}:{path}`
     çağrısı o host/path ile gider (session sahtesi); boşken `SHAREPOINT_SITE_URL`; drive adı önceliği
     `TESLIM_SP_DRIVE_NAME` > `SP_DRIVE_NAME` > `Belgeler`; cache anahtarı config'i ayırır (aynı token,
     farklı config → iki ayrı çözüm; `cache_clear()` ile temiz başla).
   - `list_folder_children(..., config_type="teslim")` / `download_file_from_sharepoint` / `upload_file_to_sharepoint`:
     `get_graph_token` sahtesine `config_type="teslim"` geldiği ve 401'de `force_refresh=True` ile AYNI
     config'in yenilendiği (`_with_fresh_token_on_401` yolu).
   - `sharepoint_tara` ve `teslim_cevap` yükleme yolu: sahte `_spu` fonksiyonlarına `config_type="teslim"`
     geldiği (G109/G110 sahte kalıbıyla, anahtar kapısı açık).
   - Geriye uyum: `upload_file_to_sharepoint(...)` config'siz çağrı `get_graph_token("default")` kullanır
     (outbox yolu değişmedi — tek assert yeter).
   - `check_client_secret_expiry(env_name=...)`: ikinci env adıyla aynı üç dal (yeniden kullanılan yardımcı).
6. **`docs/mimari/veri-teslim-hatti.md`:** §1 tablosuna "Site/kimlik" satırı (`TESLIM_SHAREPOINT_*`, düşüş kuralı,
   satır referansları), §2 gözcü adımına config notu, §9 "Prod kurulumu insan adımıdır" maddesi yeni site'a göre
   yeniden yazılır (aşağıdaki insan adımları listesi). Yalnız koddan doğrulanmış satır numaraları.
7. **Lifespan:** `api.py` lifespan'daki `check_client_secret_expiry()` çağrısının yanına ikinci çağrı
   (`env_name="TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT"`) — `api.py` Dokunma listesinde ama bu tek satır
   izinlidir; başka değişiklik yok.

## İnsan adımları (ajan YAPMAZ — raporda "yapılmadı" diye listele)

1. SharePoint `hanyaloglu.sharepoint.com/sites/hukdok_arsiv` → Belgeler → `03_VERI_TESLIM/gelen` ve
   `03_VERI_TESLIM/cevap` klasörleri açılır; `gelen` veri ekibine düzenleme, `cevap` görüntüleme izniyle
   paylaşılır (aynı tenant → davet gerekmez). Doğrudan bağlantı 06.09 e-postasına cevapta iletilir.
2. Hanyaloğlu tenant'ındaki uygulama kaydının `Sites.ReadWrite.All` (ya da `Sites.Selected` + site izni) app-only
   iznine sahip olduğu Entra'da teyit edilir; secret bitiş tarihi `TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT`'a yazılır.
3. Prod `.env`: `TESLIM_SHAREPOINT_TENANT_ID/CLIENT_ID/CLIENT_SECRET/SITE_URL` (lokal `.env`'deki `UPLOAD_SHAREPOINT_*`
   değerlerinin karşılığı) + `SHAREPOINT_FOLDER_TESLIM_NAME=03_VERI_TESLIM` → `docker compose up -d` (recreate;
   `restart` env'i almaz; bayat upstream için frontend de recreate edilir).
4. Admin panelden `veri_teslim_otomasyonu` anahtarı; ilk teslim daima `inceleme_bekliyor`.
5. LexisBio site'ındaki eski `03_VERI_TESLIM` klasörü (03.09 lokal testinin izi) kullanıcı kararıyla silinir ya da
   bırakılır — ajan dokunmaz.

## Kabul kriterleri

- [ ] `TESLIM_SHAREPOINT_*` dörtlüsü doluyken gözcü listeleme/indirme ve cevap yükleme Hanyaloğlu site'ına
      (`TESLIM_SHAREPOINT_SITE_URL`) o tenant'ın token'ıyla gider; arşiv/outbox/sayaç/e-posta çağrıları
      `SHAREPOINT_*` ile aynen devam eder (test: config'e göre host + token config'i).
- [ ] Dörtlüden biri boşsa teslim hattı default kimlik/site'a düşer, tek INFO; hiçbir yerde istisna yok (test).
- [ ] `_get_site_and_drive_id` cache'i `(token, config_type)` anahtarlı ve `maxsize>=2`; iki config art arda
      çağrılınca ikinci turda Graph'a GİTMEZ (test: session sahtesi çağrı sayısı).
- [ ] 401 yenilemesi çağrının kendi config'ini yeniler (`force_refresh=True` + aynı `config_type`) (test).
- [ ] `test_faz3_graph_retry.py` DEĞİŞMEDEN geçer; G109/G110/G112'de yalnız sahte imzaları güncellendi, assert
      sayısı azalmadı (rapor: "değiştirilen test → gerekçe").
- [ ] `.env.example`'da yeni blok; `UPLOAD_SHAREPOINT_*` adı repoda geçmez (grep).
- [ ] `veri-teslim-hatti.md` §1/§2/§9 güncel, satır referansları koddan doğrulanmış; insan adımları raporda
      "yapılmadı" olarak listeli.
- [ ] Dokunma listesindeki dosyalar (tek satırlık `api.py` lifespan çağrısı hariç) diff'te yok; ruff + mypy temiz;
      pytest özet satırı raporda (`-q` EKLEME — pyproject zaten `-q`).

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- `grep -rn "UPLOAD_SHAREPOINT" backend docs --include=*.py --include=*.md --include=*.example` → boş

## Rapor

**DURUM: TAMAM** (2026-09-08, işçi oturumu; tek commit — SHA kuyruk raporunda)

### Yapılanlar

1. **`sharepoint/auth_graph.py` — ikinci kimlik seti.** `CONFIG_DEFAULT`/`CONFIG_TESLIM`/`CONFIG_TYPES`
   sabitleri + `_ENV_PREFIX` (`SHAREPOINT_` / `TESLIM_SHAREPOINT_`); `_read_credentials(config_type)`
   tenant/client env'den, secret önce `vault.get_secret` sonra env (default ile aynı sıra).
   `_get_msal_app("teslim")`: üçü doluysa teslim setiyle, biri bile boş/boşluksa **default sete düşer +
   süreç başına TEK INFO** (`_teslim_dusus_loglandi` bayrağı; app yeniden kurulsa da ikilenmez).
   `_MSAL_APPS` anahtarı `config_type` kalır — düşüşte bile `teslim` anahtarlı AYRI app (ayrı token
   cache), `force_refresh` yolu config'e sadık. Ölü `"upload"` dalı kaldırıldı; tanınmayan config
   `ValueError` (uzay `default | teslim`). `check_client_secret_expiry(today=None, env_name=...)`:
   ikinci env adıyla aynı üç dal; mesajda etiket ("Veri teslim SharePoint") ve güncellenecek env adı
   (`TESLIM_SHAREPOINT_CLIENT_SECRET ve ..._EXPIRES_AT`) — secret'ın kendisi hiç geçmez.
   `.env` yüklemesi `_load_env()`'e çıkarıldı (testler sahteler).
2. **`sharepoint/sharepoint_uploader_graph.py` — site/drive config'e göre.** `_site_url_for` (teslim:
   `TESLIM_SHAREPOINT_SITE_URL` → boşsa `SHAREPOINT_SITE_URL`), `_drive_name_for` (`TESLIM_SP_DRIVE_NAME`
   > `SP_DRIVE_NAME` > `Belgeler`; boş dize tanımsız sayılır). `_get_site_and_drive_id`
   `@lru_cache(maxsize=1)` → **`maxsize=4`**, docstring'de token-TTL gerekçesi korundu + config gerekçesi
   eklendi. `_with_fresh_token_on_401(fn, config_type="default")` token'ı o config'le alır ve 401'de
   **aynı config'i** yeniler (WARNING satırına config eklendi). `list_folder_children(folder, *,
   config_type)`, `download_file_from_sharepoint(folder, name, *, config_type)`,
   `upload_file_to_sharepoint(..., *, config_type)` keyword-only; `_upload_with_token` config'i geçirir.
   Varsayılan `"default"` → outbox/e-posta/sayaç/`repair_overwritten_documents` çağrıları değişmedi
   (imzalar geriye uyumlu; `counter_manager` `_get_site_and_drive_id(token)` konumsal çağrısı da aynen).
3. **Teslim tarafı tek sabit:** `teslim_kutusu.TESLIM_SP_CONFIG = _spu.CONFIG_TESLIM` (modül şerhine
   gerekçe maddesi); `sharepoint_tara` iki çağrıya `config_type=TESLIM_SP_CONFIG`; `teslim_cevap`
   yüklemesi `config_type=tk.TESLIM_SP_CONFIG` + modül şerhine madde. Klasör adları değişmedi.
4. **`.env.example`** (repo kökünde — görev `backend/.env.example` yazıyor ama dosya kökte, `.env.example:31-48`):
   "Veri teslim SharePoint'i (istege bagli IKINCI kimlik/site, G147)" bloğu — dört `TESLIM_SHAREPOINT_*`
   + `..._CLIENT_SECRET_EXPIRES_AT` + `TESLIM_SP_DRIVE_NAME`, hepsi yorumlu; düşüş kuralı ve
   "`up -d` (recreate) ister" notu. `UPLOAD_SHAREPOINT_*` adı YAZILMADI.
5. **`api.py` lifespan:** tek satır — `check_client_secret_expiry(env_name="TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT")`
   (`api.py:133`); başka değişiklik yok.
6. **Testler:** `tests/test_g147_teslim_sharepoint_kimligi.py` (YENİ, 42 test, ağ yok): msal app sahtesi
   ile üç env dolu/eksik/boş dize, INFO tek sefer, `_MSAL_APPS` iki anahtar, vault önceliği, secret
   loga sızmaz, `ValueError`, `get_graph_token("teslim", force_refresh=True)` doğru app; site/drive
   çözümü host+path, düşüş, drive önceliği (4 parametre), env yoksa RuntimeError, **cache anahtarı
   config'i ayırır ve ikinci turda GET sayısı sabit (4)**, `maxsize == 4`; list/download/upload
   `"teslim"` token'ı + 401'de aynı config, geriye uyum (`"default"`), keyword-only TypeError;
   `sharepoint_tara` ve `cevap_yukle` sahte `_spu`'ya `config_type="teslim"` (G109/G110 sqlite kalıbı);
   `check_client_secret_expiry(env_name=...)` üç dal + iki env bağımsız + lifespan ikinci çağrı (AST
   yerine kaynak metni, G093 deseni); bayat ad grep'i (parçalı yazılmış).
7. **`docs/mimari/veri-teslim-hatti.md`:** §1 tabloya "Site/kimlik" satırı (env'ler, düşüş kuralı,
   cache, satır referansları), §2 madde 2 config notu + madde 5 (satır numaraları koddan yeniden
   okundu: `sharepoint_tara` 1259-1305, `list_folder_children` 483-518, `download` 453-474), §9 prod
   kurulumu maddesi yeni site'a göre beş adımla yeniden yazıldı, §10'a iki satır. Başlık şerhine
   "§1/§2/§9 G147 ile yeniden doğrulandı" notu.

### Kararlar

- **Tenant/client boşken secret hiç aranmaz** (`_read_credentials`): `vault.get_secret` bulamayınca
  WARNING basıyor; teslim kimliği tanımsız prod'da her açılışta sahte "Secret not found" uyarısı
  olurdu. Tenant+client doluyken secret eksikse uyarı meşru (yanlış kurulum) ve düşüş yine çalışır.
- **Düşüşte anahtar `teslim` kalır, ayrı app kurulur:** aynı kimlikle iki MSAL app = iki token
  cache (bir fazladan token isteği); karşılığında `force_refresh`'in `_MSAL_APPS.pop(config_type)`
  yolu ve cache anahtar uzayı config'e sadık kalıyor — görev metni ("`_MSAL_APPS` anahtarı
  `config_type` olarak kalır (ayrı token cache)") bunu istiyor.
- **Bilinmeyen config `ValueError`**: eski kod her adı sessizce default'a çeviriyordu; `"upload"`
  ölü dalıyla birlikte gitti. Repo'da `default` dışı çağıran yok (grep), testler geçti.
- **Boş dize tanımsız sayılır** (`SP_DRIVE_NAME=""` → `Belgeler`): eski `os.getenv(..., "Belgeler")`
  boş dizeyi drive adı sayıp "Drive '' not found" ile düşerdi; küçük iyileştirme, geriye uyumlu.
- Lifespan ikinci çağrısı görev metnindeki gibi env adını literal taşır (sabiti import etmek için
  `api.py`'de ikinci satır gerekirdi; "tek satır" sınırı korundu).

### Değiştirilen test → gerekçe (yalnız sahte imzaları; assert sayısı değişmedi)

- `tests/test_g109_teslim_gozcusu.py:216,222` — `_list(folder_name)` / `_download(folder_name, filename)`
  → `**kw` eklendi (yeni `config_type=` anahtar argümanı `TypeError` verirdi).
- `tests/test_g110_teslim_cevap.py:86` — `lambda folder_name: []` → `lambda folder_name, **kw: []`.
- `tests/test_g112_duzeltme_logu.py:668` — aynı lambda düzeltmesi.
- `tests/test_faz3_graph_retry.py` DOKUNULMADI, değişmeden geçiyor.

### Test sonuçları (konteyner)

- `python -m pytest` → **2834 passed, 3 skipped, 3 warnings** (137 sn; önce 2792 + 42 yeni)
- `python -m ruff check .` → All checks passed!
- `python -m mypy` → Success: no issues found in 61 source files
- `grep -rn UPLOAD_SHAREPOINT backend docs` → `backend/` ve `docs/mimari/` temiz; **yalnız `docs/arsiv/`
  üç tarihli belgede geçiyor** (`gelistirme-plani-2026-07-07.md:30`, `kod-kalitesi-guvenlik-denetimi-2026-07-07.md:28`,
  `sistem-teknik-raporu-2026-08-04.md:24,572`) — arşiv tarihsel kayıttır, dosya kapsamı dışında,
  DOKUNULMADI (kabul kriterine şerh).

### Yapılmayan insan adımları (ajan yapmaz)

1. `hanyaloglu.sharepoint.com/sites/hukdok_arsiv` → Belgeler → `03_VERI_TESLIM/gelen` (veri ekibine
   düzenleme) ve `03_VERI_TESLIM/cevap` (görüntüleme) klasörleri + paylaşım; bağlantı 06.09 e-postasına
   cevapta — **yapılmadı**.
2. Hanyaloğlu tenant'ındaki uygulama kaydının app-only `Sites.ReadWrite.All` / `Sites.Selected` izni
   Entra'da teyit + `TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT` — **yapılmadı**.
3. Prod `.env`: `TESLIM_SHAREPOINT_TENANT_ID/CLIENT_ID/CLIENT_SECRET/SITE_URL` (lokal `.env`'deki bayat
   `UPLOAD_*` değerlerinin elle taşınması) + `SHAREPOINT_FOLDER_TESLIM_NAME=03_VERI_TESLIM` →
   `docker compose up -d` (recreate, frontend dahil) — **yapılmadı** (`.env` okunmadı/yazılmadı).
4. Admin panelden `veri_teslim_otomasyonu` anahtarı — **yapılmadı**.
5. LexisBio site'ındaki eski `03_VERI_TESLIM` klasörü kararı — **yapılmadı**, ajan dokunmadı.

### İzlenecekler / NOT

- NOT: çalışma ağacında görev dışı kirli `.claude/settings.local.json` (M) ve `.claude/launch.json` (??)
  vardı — koşucu/oturum dosyaları; dokunulmadı, commit'e alınmadı.
- İlk gerçek koşuda log'da "Veri teslim SharePoint kimliği tanımsız ... arşiv kimliği kullanılıyor"
  INFO'su görünürse prod `.env` dörtlüsü eksik demektir (3. insan adımı).
- Cevap klasörü ara-klasör davranışı (§6 bilinen sınır) yeni site'ta da ilk yüklemede gözle doğrulanmalı.
