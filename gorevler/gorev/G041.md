# G041 — D 6.1: eksik kısıt/index'leri çalışan op türüne taşı

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/database.py, backend/tests/test_migration_path.py
- **Dokunma:** backend/models.py (DOKUNMA — G044/G045'in bandı), diğer testler

## Hedef

`create_all()` (`database.py:107`) tabloları modelden **önce** yaratıyor; migrasyon tablo
listesini **sonra** okuyor (`:540`) → `("table", …)` op'ları `if table in tables: continue`
ile atlanıyor (`:587-588`). Sonuç: o op'ların taşıdığı UNIQUE kısıt ve index SQL'leri
**hiç çalışmamış**. `models.py`'de `__table_args__`/`UniqueConstraint`/`Index(` → **0 eşleşme**,
yani ORM da bunları ifade etmiyor.

Çözüm ucuz: `("index", tablo, [...])` op türü **zaten çalışıyor** (canlı kanıt:
`uq_cases_sistem_no`, `idx_case_docs_conversion_pending` prod'da VAR). Eksik kalemler
bu mevcut op türüne taşınır. Yeni mekanizma ya da Alembic GEREKMİYOR.

## Prod'da doğrulanmış eksik liste (2026-08-12, salt okunur ölçüm — yeniden keşfetme)

| Kalem | Nereden | Prod'da |
| --- | --- | --- |
| `uq_case_relation` UNIQUE (source_case_id, target_case_id) | `table` op | ❌ yok |
| `uq_daily_report` UNIQUE (user_email, report_date) | `table` op | ❌ yok |
| `idx_case_relations_source` (source_case_id) | `table` op | ❌ yok |
| `idx_case_relations_target` (target_case_id) | `table` op | ❌ yok |
| `idx_daily_reports_user` (user_email, is_acknowledged) | `table` op | ❌ yok |
| `idx_export_outbox_status` (status) | `table` op | ❌ yok |
| `idx_stage_logs_case` (case_id) | `table` op | ❌ yok |
| `ix_clients_name` (clients.name) | `models.py index=True` | ❌ yok |

**Mükerrer kontrolü PROD'DA KOŞULDU ve TEMİZ** (2026-08-12): `case_relations` →
0 mükerrer (tabloda toplam **1 satır**), `daily_activity_reports` → 0 mükerrer (89 satır).
Yani iki UNIQUE de risksiz eklenebilir. **Yine de migrasyon adımı savunmacı yazılmalı**
(aşağıya bak) — prod ile bu ölçüm arasında yeni satır girebilir.

## Kabul kriterleri

- [ ] Sekiz kalem `("index", tablo, [...])` op'una taşındı; hepsi `IF NOT EXISTS` ile
      idempotent. UNIQUE'ler `CREATE UNIQUE INDEX IF NOT EXISTS` biçiminde
      (`ALTER TABLE ADD CONSTRAINT` idempotent DEĞİL — tekrar koşuda patlar)
- [ ] `("table", …)` op'larının gövdesindeki artık kısıt/index SQL'leri **kaldırılmadı**
      (yeni bir kurulumda tablo sıfırdan yaratılırken hâlâ gerekli) — ama yeni `index`
      op'larıyla **çakışmadıkları** doğrulandı (`IF NOT EXISTS` ikisini de güvenli kılar)
- [ ] **Savunmacı UNIQUE:** migrasyon UNIQUE index yaratmadan önce mükerrer kontrolü
      yapıyor; mükerrer varsa **anlaşılır bir hata** ile duruyor (fail-fast doğru davranış —
      ama "duplicate key value violates unique constraint" yerine hangi tablo/kolon/kaç grup
      olduğunu söyleyen bir mesaj). Konteyner kalkmaması BİLİNÇLİ, sessiz atlama YASAK
- [ ] `test_migration_path.py`'deki iki "bugünkü kusuru kilitleyen" test **ters çevrildi**:
      `test_table_op_index_sqlleri_bugun_hic_calismiyor` artık index'lerin **oluştuğunu**
      iddia ediyor (ad da güncellendi). G031 bu devri açıkça öngörmüştü
- [ ] `test_columns_op_post_index_sqlleri_bugun_hic_calismiyor` için karar verildi ve
      gerekçesi rapora yazıldı: ölçüldü ki `columns` post_sql'leri **çoğunlukla koşmuş**
      (kolon gerçekten sonradan eklendiğinde çalışıyor); bu testin ne iddia ettiği
      gözden geçirilmeli — yanıltıcıysa daraltılmalı
- [ ] Boş DB'ye sıfırdan kurulum + ikinci koşu (idempotency) hâlâ yeşil
- [ ] `docs/mimari/` içinde migrasyon mekanizmasını anlatan yer güncellendi

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m pytest tests/test_migration_path.py -v`
- `docker compose exec -T backend python -m ruff check .` · `python -m mypy`
- Lokal stack'i **recreate** edip migrasyonun gerçekten koştuğunu gör:
  `docker compose up -d --force-recreate backend` + `docker compose logs backend | tail -40`

## Rapor

**DURUM: TAMAM** (2026-08-12)

### Yapılanlar

`backend/database.py`:

1. **28. madde eklendi** — tablo op'larına gömülü olduğu için hiç koşmamış kalemler
   koşulsuz çalışan `("index", …)` op'una taşındı, hepsi `IF NOT EXISTS`:
   `uq_case_relation` (UNIQUE), `idx_case_relations_source`, `idx_case_relations_target`,
   `uq_daily_report` (UNIQUE), `idx_daily_reports_user`, `ix_clients_name`.
   UNIQUE'ler kabul kriterine uygun olarak `CREATE UNIQUE INDEX IF NOT EXISTS` biçiminde
   (ADD CONSTRAINT idempotent olmadığı için).
2. **Tablo op'larının gövdesindeki özgün SQL'lere DOKUNULMADI** — tablo gerçekten yoksa
   hâlâ gerekliler. Çakışma yok: 28. madde listede sonra geldiği ve `IF NOT EXISTS`
   taşıdığı için tablo op'u koştuğunda ikinci yol no-op'a düşer (idempotency testi kanıt).
3. **`_MIGRATIONS` başına DİKKAT şerhi**: `("table"/"columns", …)` op'larının koşullu
   olduğu, kalıcı kısıt/index'in DAİMA `("index", …)` op'una yazılması gerektiği —
   G045/G046'nın aynı tuzağa düşmemesi için mekanizmanın kendi dosyasında yazılı.
4. **Savunmacı UNIQUE**: `_unique_index_duplicate_report` + `check_and_migrate_tables`
   içindeki `_guard_unique_index`. UNIQUE index yaratılmadan önce mükerrer sayılır;
   varsa `RuntimeError` ile **fail-fast** (konteyner kalkmaz — bilinçli), mesaj
   tablo + kolonlar + grup sayısı + örnek anahtarları (`(x, y) ×3`) verir.
   Ayrıntılar: index zaten varsa kontrol **atlanır** (her açılışta boş tarama yok);
   NULL'lu satırlar dışlanır (PG'de çoklu NULL unique'i ihlal etmez, `GROUP BY` ise
   NULL'ları eşit sayar → yanlış alarm olurdu); ön kontrolün KENDİSİ patlarsa WARNING
   ile geçilir ve DDL yine denenir (savunma, kapıyı gevşetmez). Log sözleşmesi korundu:
   yeni ERROR yok, nihai ERROR `init_db`'de tek satır.

`backend/tests/test_migration_path.py`:

5. `test_table_op_index_sqlleri_bugun_hic_calismiyor` → **ters çevrildi**:
   `test_table_op_index_niyetleri_semada_karsilikli`. Yeni `test_table_op_unique_kisitlari_semada_karsilikli`
   ile UNIQUE kısıtlar da kilitlendi.
6. `test_columns_op_post_index_sqlleri_bugun_hic_calismiyor` → **daraltıldı**:
   `test_columns_op_post_sqlleri_yalnizca_kolon_sonradan_eklendiginde_kosuyor` (gerekçe aşağıda),
   ters yönü kanıtlayan `test_columns_op_post_sqli_kolon_eklendiginde_kosuyor` eklendi.
7. `test_create_all_mevcut_tabloya_index_eklemiyor` güncellendi: create_all'ın boşluğu artık
   `create_all`ı **doğrudan** çağırarak ölçülüyor, ardından `init_db` sonrası `ix_clients_name`'in
   **oluştuğu** iddia ediliyor (G041'in kapattığı boşluk).
8. Savunmacı UNIQUE için iki test: rapor içeriği/NULL davranışı + migrasyonun gerçekten durduğu.

`docs/mimari/genel-bakis.md`: "Migrasyon op türleri ve `create_all` tuzağı" başlığı eklendi;
bayat satır atıfları (`database.py:131`, `:529`) güncel numaralara çekildi.

### Alınan kararlar + gerekçeleri

**K1 — Sekiz kalemin ÜÇÜ bilinçli olarak yaratılmadı (kapsama var, yalnız AD yok).**
Görev dosyası listesini yeniden keşfetmedim ama lokal DB'de (prod restore kopyası) ve
`models.py`'de doğruladım — üç kalem aynı kolonu ikinci kez indekslerdi:

| Kalem | Neden atlandı (ölçüm) |
| --- | --- |
| `idx_stage_logs_case` | `case_stage_logs.case_id` modelde `index=True` → `ix_case_stage_logs_case_id` **VAR** |
| `idx_export_outbox_status` | `export_outbox.status` modelde `index=True` → `ix_export_outbox_status` **VAR** |
| `idx_client_policies_client` | Bu ad zaten **VAR** — `client_policies` tablosu create_all'la değil TABLO OP'uyla doğmuş (kanıt: `ix_client_policies_client_id` yok) |

Yani bu üçünde eksik olan **kapsama değil isimdir**; eklemek birebir mükerrer index üretir ve
doğrudan G042'nin ("kullanılmayan index temizliği") listesine düşerdi. Aynı desen dosyada zaten
kayıtlı (15. madde, `hearing_dates`: "ix_ adı bilinçli yeniden kullanılıyor"). Bunun yerine test
**addan bağımsız** hale getirildi: `(tablo, kolonlar, unique?, kısmi?)` imzası üzerinden kapsama
doğrulanıyor — yani "şu adlı index" değil, "şu kolonlarda bir index" aranıyor. Böylece kabul
kriterinin asıl amacı (op'ların taşıdığı SQL'lerin niyeti şemada karşılıksız kalmasın) makineyle
kilitlendi, mükerrer index bedeli ödenmeden. Karar kodda 28. maddenin şerhinde de yazılı.

**K2 — `columns` post-SQL testi ters çevrilmedi, DARALTILDI.** Ölçüm: post SQL'ler "hiç"
çalışmıyor değil — kolon gerçekten sonradan eklendiğinde çalışıyorlar (prod/lokal DB'de
`idx_clients_tenant`, `idx_clients_deleted_at` mevcut). Eski test adı ve docstring'i bu yüzden
yanıltıcıydı; iddia "sıfırdan kurulumda koşmuyor"a daraltıldı ve ters yön ayrı testle
kanıtlandı (`clients` tablosu `tenant_id`siz yaratılıyor → init_db kolonu ekliyor →
`idx_clients_tenant` oluşuyor). Boşluğun kendisi G041 kapsamı DIŞINDA bırakıldı: görev tablo
op'larını hedefliyordu ve düzeltmesi ya SQL'leri iki yerde tutmayı ya da mekanizma değişikliğini
gerektirirdi ("yeni mekanizma GEREKMİYOR" kuralı). Devri aşağıda `NOT:` olarak yazıldı.

**K3 — `docs/mimari/genel-bakis.md` dosya kapsamının dışındaydı ama 7. kabul kriteri onu
zorunlu kılıyordu; "Dokunma" listesinde olmadığı için değiştirildi.**

### Test sonuçları

- `docker compose exec -T backend python -m pytest` → **1045 passed, 2 skipped** (41.53s)
- `tests/test_migration_path.py -v` → **11 passed** (7.04s; öncesi 6 test — hepsi gerçek
  Postgres'e karşı KOŞTU, SKIP yok)
- `ruff check .` → All checks passed · `mypy` → no issues in 39 source files
- **Canlı doğrulama** (`docker compose build backend` + `up -d --force-recreate backend`):
  migrasyon hatasız koştu, altı index de prod-restore kopyası veritabanında oluştu
  (`pg_indexes` ile teyit; iki UNIQUE mükerrer çarpmadan kuruldu). Ardından `restart` ile
  **ikinci koşu** da temiz — idempotent.

### İzlenecekler

- **NOT (G043'e devir):** sıfırdan kurulan bir veritabanında `("columns", …)` post-SQL
  index'lerinin **onu da** oluşmuyor — en kritiği `uq_cases_sistem_no`: yeni kurulumda
  `cases.sistem_no` tekilliği ZORLANMIYOR (`models.py`'de `unique=True` yok). Mevcut
  kurulumlarda kolon migrasyonla eklendiği için index var, yani prod etkilenmiyor; ama
  FAZ F sistem_no tekilliğine yaslanacak. Test bu boşluğu kilitli tutuyor.
- **NOT (G042'ye girdi):** `daily_activity_reports` artık üç index'i de `user_email` ile
  başlatıyor (`ix_daily_activity_reports_user_email` ⊂ `uq_daily_report` /
  `idx_daily_reports_user`) → tek kolonluk olan artık gereksiz, temizlik adayı.
- **NOT:** `docs/plan/temizlik-ve-yapisal-saglik-plani-2026-08-11.md:280-290` maddesi 6.1'i
  hâlâ açık kusur olarak anlatıyor ve bayat satır numaraları veriyor (`database.py:110`, `:543`);
  plan dosyası bu görevin kapsamı dışında bırakıldı.
- **NOT:** Oturum boyunca `.claude/settings.local.json` (M) ve `.claude/launch.json` (??)
  kirliydi — harness'ın kendi izin dosyaları, görev kapsamıyla ilgisiz; dokunulmadı,
  commit'e alınmadı.
- Prod'a çıkışta iki UNIQUE index ilk kez gerçek veride kurulacak: ölçüm 2026-08-12'de
  temizdi ama arada satır girmiş olabilir — savunmacı kontrol tam da bunun için var,
  Deploy #10'da `docker compose logs backend` migrasyon satırlarına bakılmalı.
