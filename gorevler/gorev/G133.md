# G133 — Raporlar sayfası iskeleti: route + menü + tipler + Rapor Oluşturucu + önizleme tablosu

- **Bant:** frontend
- **Bağımlı:** - (G130 ile PARALEL — sözleşme `docs/plan/raporlama-plani-2026-09-06.md` §2.1-2.4'te dondu)
- **Dosya kapsamı:** frontend/src/App.tsx (route), frontend/src/components/shell/Sidebar.tsx (menü öğesi),
  frontend/src/lib/api.ts (yalnız `LONG_TIMEOUT_PREFIXES`'e `/api/reports/` satırı), frontend/src/lib/reports.ts
  (tipler + API fonksiyonları), frontend/src/pages/ReportsPage.tsx, frontend/src/components/reports/**
  (ReportBuilder, ColumnPicker, FilterRow, PreviewTable), frontend/src/pages/ReportsPage.test.tsx,
  frontend/src/lib/reports.test.ts
- **Dokunma:** frontend/src/pages/AdminPage.tsx, frontend/src/components/admin/**, frontend/package.json
  (yeni paket YOK; dnd-kit ve shadcn bileşenleri zaten kurulu)

## Hedef

Yöneticiye özel `/reports` sayfası: veri kaynağı seç → kolonları seç ve sırala → filtre satırları ekle →
sıralama → **Önizle** (sunucu sayfalı tablo + toplam). Hub dosyalara (`App.tsx`, `Sidebar.tsx`, `api.ts`)
YALNIZ bu görev dokunur; G134/G135 bileşen ekler. UI, `CaseList.tsx:286-…` redesign kalıbını izler
(Eyebrow + SectionHeader, `HairlineCard`, `FlowButton`, sol filtre sütunu `xl:grid-cols-[320px_1fr]`,
tablo `overflow-x-auto`, `max-w-[1600px]` sola yaslı — sağda boşluk bırakma). Toast: `sonner`.

## Yapılacaklar

1. **Route + menü:** `App.tsx` korumalı layout bloğuna `/reports` → `ProtectedAdminRoute` + `ReportsPage`
   (`/admin` satırıyla aynı sarmalama). `Sidebar.tsx:62-64`'te admin öğeleri: `{id:"reports", label:"Raporlar",
   path:"/reports", Icon: Table2 (lucide)}` — Yönetim'den ÖNCE. `api.ts` uzun-timeout listesine
   `"/api/reports/"` (önizleme/export/chat 30 sn'yi aşabilir; şerh yaz).
2. **`lib/reports.ts`:** plan §2.1/§2.4 tiplerinin birebir TS karşılığı (`RaporTanimi`, `Filtre`, `Katalog`,
   `KatalogKolon`, `OnizlemeCevabi`, `RaporSablonu`, `RaporKosusu`); dosya başında yorum:
   "Backend sözleşmesi: backend/schemas_rapor.py + docs/plan/raporlama-plani-2026-09-06.md §2".
   Fonksiyonlar: `getCatalog()`, `previewReport(tanim, sayfa, sayfaBoyu)`; 422 gövdesindeki `alan/sebep`
   kullanıcıya okunur hata olarak çevrilir. **Şablon/koşu/chat fonksiyonları G134/G135'te eklenir** — tipleri
   burada şimdiden tanımla (tek okuma turu).
3. **Rapor Oluşturucu (sol sütun):** veri kaynağı `Select`; **ColumnPicker** — katalog kolonları checkbox
   listesi (arama kutusu + "varsayılanları seç" + "temizle"), seçilenler sıralanabilir (dnd-kit `@dnd-kit/sortable`
   kurulu; alternatif ↑↓ düğmeleri kabul); **FilterRow** — alan (yalnız `filtrelenebilir`), op (tipe göre
   §2.2 listesi), değer girdisi tipe göre (metin/ sayı/ tarih `input type=date`/ liste `Select`, `in` için çoklu
   seçim, `between` iki kutu, `is_null/not_null` değer yok); sıralama (alan + yön, ≤3). Tanım değişince
   önizleme "bayat" rozeti; Önizle düğmesi tanım geçersizken kapalı (kolon yok).
4. **PreviewTable (sağ):** kolon başlıkları etiketle, tarih `dd.MM.yyyy`, para `tr-TR` biçimi, `null` "—";
   sayfalayıcı `CaseList.tsx:692-720` kalıbı; toplam satır rozeti; yarış koruması (`reqIdRef`,
   `CaseList.tsx:167`); hata ≠ boş liste (`DataErrorBanner`). Araç çubuğuna G134 için yer tutucu bırakma —
   düğmeleri G134 ekler.
5. **Responsive:** `xl` altında oluşturucu üste düşer; tablo yatay kayar; sayfa gövdesi yatay kaydırmaz.
6. **Test:** `ReportsPage.test.tsx` (`AdminPage.test.tsx` kalıbı: `// @vitest-environment jsdom`, `createRoot`+
   `act`, `vi.hoisted` fetch mock, sabit kimlikli mock nesneleri, `MemoryRouter`): katalog yüklenir ve kaynak
   listelenir; kolon seçimi + filtre ekleme gövdeyi doğru `RaporTanimi` ile `/api/reports/preview`'a gönderir;
   422 cevabı okunur mesaja dönüşür; sayfa değişimi `sayfa` gönderir. `reports.test.ts`: tip↔op tablosu ve
   `between` değer biçimi.

## Kabul kriterleri

- [ ] `/reports` yalnız `useIsAdmin()===true` iken açılır, Sidebar'da "Raporlar" yalnız yöneticide görünür (test).
- [ ] Önizleme isteği gövdesi plan §2.1 ile birebir (test: `filtreler[].op`, `between` dizi, `siralama`).
- [ ] Filtre op listesi kolon tipine göre §2.2'yi aşmaz; türetilmiş kolon filtre alanında listelenmez (test).
- [ ] 422 `detail.alan/sebep` kullanıcıya gösterilir; ağ hatasında tablo boş listeye DÖNMEZ (DataErrorBanner).
- [ ] Tam genişlik kuralı: sayfa `mx-auto` ile ortalanmaz, sağda boşluk kalmaz; `xl` altı tek sütun.
- [ ] Hub dosyalarda değişiklik: `App.tsx` 1 route + 1 import, `Sidebar.tsx` 1 öğe, `api.ts` 1 satır — fazlası yok.
- [ ] `npm test`, `lint`, `tsc -b --force` temiz; raporda ekran görüntüsü yerine bileşen ağacı özeti.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

**DURUM: TAMAM** — 2026-09-06 gece koşusu, worktree `gorev/G133` (taban `73277b1`).

### Yapılanlar

- **Hub dosyalar (asgari):** `App.tsx` +1 import (`ReportsPage`) +1 route (`/reports`, `ProtectedAdminRoute`
  sarmalı, `/admin` ile birebir); `Sidebar.tsx` +1 ikon importu (`Table2`) +1 nav öğesi (`reports`, Yönetim'den
  önce, yalnız `isAdminUser`); `api.ts` `LONG_TIMEOUT_PREFIXES`'e tek satır `"/api/reports/"` + şerh.
- **`lib/reports.ts`:** plan §2.1/§2.2/§2.4/§2.6 tiplerinin TS karşılığı (`RaporTanimi`, `Filtre`, `Siralama`,
  `Katalog`, `KatalogKolon`, `OnizlemeCevabi`, `RaporSablonu`, `RaporKosusu`, `RaporKosuListesi`, `AsistanMesaji`,
  `AsistanOlayi`); `OP_BY_TIP` tablosu (§2.2 birebir), `opsForTip`/`opDegerSekli`/`degerSekleUyarla`,
  `filtreTamamMi`/`tanimGecerliMi` (Önizle kapısı: kolon yok / eksik filtre / türetilmiş alan / limit aşımı →
  kapalı), `hucreBicimle` (tarih dd.MM.yyyy, para/sayı tr-TR, null "—", mantık Evet/Hayır), `RaporApiError` +
  `raporHatasiCevir` (422 `detail.alan/sebep` → "Geçersiz rapor tanımı: alan — sebep"; FastAPI liste biçimi,
  413 `satir_limiti`, düz metin `detail` ve JSON-dışı gövde de ele alınır), `getCatalog()`, `previewReport()`.
  Şablon/koşu/chat fonksiyonları G134/G135'e bırakıldı (tipler hazır).
- **Bileşenler (`components/reports/`):** `builderState.ts` (yerel durum: filtre satırına yalnız React key için
  `id`; `tanimOlustur` sunucu gövdesine çevirir, değersiz op'larda `deger` anahtarı hiç yazılmaz), `ui.ts` (ortak
  sınıflar), `ColumnPicker` (arama + checkbox listesi + "Varsayılanları seç"/"Temizle" + seçilenler dnd-kit
  sortable ve ↑↓/× düğmeleri, 60 kolon tavanı), `FilterRow` (alan yalnız `filtrelenebilir && !turetilmis`; op
  listesi tipe göre; değer girdisi tip/op'a göre: metin, sayı `type=number` → JSON number, tarih `type=date`,
  liste `select`, `in` liste tipinde çip çoklu seçim / metin tipinde virgülle, `between` iki kutu, `is_null`/
  `not_null` girdisiz; eksik değerde "değer eksik" işareti), `ReportBuilder` (kaynak select → kolonlar → filtreler
  (≤20) → sıralama (≤3, yalnız `siralanabilir && !turetilmis`) → Önizle; bayat rozeti), `PreviewTable` (etiketli
  başlıklar, sayı/para sağa yaslı, sayfalayıcı CaseList kalıbı, toplam rozeti, hata → `DataErrorBanner`, boş
  liste ayrı; araç çubuğunda G134 için yer tutucu YOK).
- **`pages/ReportsPage.tsx`:** katalog yükleme (hata → şerit + sonner toast + Tekrar dene), `reqIdRef` yarış
  koruması, sayfa değişimi SON önizlenen tanımla gider (taslakla değil), `bayat = sonTanim ≠ taslak`,
  `sayfa_boyu = min(50, limitler.onizleme_sayfa_boyu_max)`. Layout `grid gap-7 max-w-[1600px]` (mx-auto YOK),
  `grid-cols-1 xl:grid-cols-[320px_1fr]`, tablo `overflow-x-auto`, sağ kart `min-w-0`.

### Kararlar

- **Radix `Select` yerine native `<select>`** (kaynak/alan/op/yön/liste-değer): filtre satırında kompakt kalır,
  jsdom'da `change` olayıyla sürülür (repo deseni `CaseTrackingPanel`; Radix Select'i süren hiçbir test yok).
  Görsel token'lar (`--bg`, `--border`, `--brand`) uygulanmış; sayfa kalıbı korunuyor.
- **Checkbox native `<input type=checkbox>`** (`accent-[var(--brand)]`): aynı gerekçe.
- **`tanimGecerliMi` türetilmiş kolonu filtre/sıralama alanında REDDEDER** — UI zaten listelemez ama kapı da
  geçirmez (ilk test turunda yakalanan boşluk, düzeltildi).
- `degerSekleUyarla`: op değişince önceki değer korunur (eq→between ilk uç, between→eq ilk eleman, →in sarılır,
  →is_null düşer); kullanıcı emeği kaybolmaz.
- Sayı/para değeri girdide anında `Number`'a çevrilir (sözleşme: JSON number); geçersiz sayı "" sayılır.

### Bileşen ağacı

```
ReportsPage
├─ Eyebrow "01 · Raporlar" + h1
├─ DataErrorBanner (katalog hatası) | HairlineCard "yükleniyor"
└─ section.grid-cols-1.xl:grid-cols-[320px_1fr]
   ├─ HairlineCard (xl:sticky)
   │  └─ ReportBuilder
   │     ├─ select#rapor-kaynak
   │     ├─ ColumnPicker (arama · checkbox listesi · DndContext/SortableContext ol>li ↑↓×)
   │     ├─ FilterRow[] (select Alan · select Operatör · değer girdisi · ×)
   │     ├─ sıralama satırları (select alan · select yön · ×)
   │     └─ FlowButton Önizle
   └─ HairlineCard(padded=false)
      └─ PreviewTable (araç çubuğu + toplam/bayat rozeti · DataErrorBanner | boş | table.overflow-x-auto · sayfalayıcı)
```

### Testler

- `frontend/src/lib/reports.test.ts` — 22 test: §2.2 tablo birebir, op şekli, between/in biçimi, `tanimGecerliMi`
  kapısı, hücre biçimlendirme, 422/413/düz metin/JSON-dışı hata çevirisi, `previewReport` gövdesi, `getCatalog`.
- `frontend/src/pages/ReportsPage.test.tsx` — 12 test: katalog → kaynaklar + varsayılan kolonlar; kolon/filtre
  (in çip, between, gte sayı)/sıralama → gövde §2.1 birebir + tablo biçimi + toplam rozeti; türetilmiş alan yok +
  op listeleri tipe göre; is_null `deger` taşımaz + kolon yokken Önizle kapalı + bayat; 422 okunur mesaj; ağ hatası
  → DataErrorBanner (boş liste değil) + Tekrar dene; sayfa 2 SON tanımla; katalog hatası şerit/toast/yeniden;
  kaynak değişimi sıfırlama; tam genişlik sınıfları; `/reports` yönetici kapısı (ProtectedAdminRoute + yönlendirme);
  Sidebar "Raporlar" yalnız yönetici + Yönetim'den önce.
- Koşu (HOST, worktree): `vitest run` **56 dosya / 663 test geçti** (önce 550 → +34 yeni, tümü yeşil; ilk turda 1
  kırmızı = kod boşluğu, düzeltildi), `eslint .` temiz, `tsc -b --force` exit 0, `vite build` başarılı.

### İzlenecekler

- G134: PreviewTable araç çubuğuna kaydet/indir düğmeleri; `lib/reports.ts`'e şablon + export + runs fonksiyonları
  (tipler hazır). G135: `AsistanOlayi` NDJSON okuyucu.
- Backend (G130) sözleşmeden saparsa uyum noktası tek yer: `lib/reports.ts` tipleri + `raporHatasiCevir`.
- NOT: gerçek `/api/reports/catalog` gelene dek sayfa boş katalogla (`veri_kaynaklari: []`) da çökmez — kaynak
  select boş kalır, Önizle kapalı; duman testi G130 sonrası gündüz.
