# G194 — Teslim cevap yüklemesi: SharePoint yüklemesinden önce transaction'ı kapat

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** `backend/services/teslim_cevap.py` (yalnız `cevap_yukle`), `backend/tests/test_g110_teslim_cevap.py` (yalnız YENİ test eklenir)
- **Dokunma:** `backend/database.py`, `backend/services/teslim_kutusu.py`, `backend/sharepoint/**`, `docker-compose.yml`, mevcut test gövdeleri (değişmesi gerekirse BLOKE bırak — test değiştirme izni YOK)

Kaynak: G191 gece kaydı "NOT (kapsam dışı bulgu)" (`gorevler/gorev/G191.md`, 2026-09-14); kullanıcı kararı
14.09: G191'den ÖNCE ayrı görevle kapatılır.

## Hedef
`cevap_yukle` (`services/teslim_cevap.py:477-553`) oturumu açar, teslimi okur, `eslesme_csv_uret(db=session)`
ile sorgu koşar ve ardından SharePoint yükleme döngüsünü (`_spu.upload_file_to_sharepoint`, uploader iç
retry'ı dahil) AYNI açık transaction içinde çalıştırır; `session.commit()` en sondadır. G191
`idle_in_transaction_session_timeout=60 s` açınca yüklemeler 60 sn'yi aşarsa sunucu oturumu keser, son commit
`OperationalError` verir, `cevap_yuklendi` yazılmaz ve `bekleyen_cevaplari_yukle` (`:567-577`) rollback +
WARNING ile turu geçer. Düzeltme: yükleme döngüsü başlamadan önce transaction kapatılır (döngünün ihtiyaç
duyduğu değerler yerel değişkenlere alınır, döngü içinde ORM nesnesine dokunulmaz — `expire_on_commit`
yeni SELECT açıp transaction'ı yeniden başlatır); döngüden sonra teslim `tk._teslim_getir` ile YENİDEN
okunur, not + `cevap_yuklendi` yazılır, commit.

## Kabul kriterleri
- [ ] Yükleme döngüsü sırasında oturumda açık transaction YOKTUR: test, sahte `upload_file_to_sharepoint` içinde `session.in_transaction()` değerini kaydeder → her dosya için `False` (hem `db` verilen hem verilmeyen yol; verilmeyen yolda `SessionLocal` taklidiyle).
- [ ] `dbtest` (mevcut desen: `tests/test_index_envanteri.py` dbtest'leri): gerçek Postgres bağlantısında `SET idle_in_transaction_session_timeout = '1s'`, sahte yükleme dosya başına 1,5 sn uyur → `cevap_yukle` `True` döner, `cevap_yuklendi=True` ve deneme notu DB'de. Aynı test ESKİ kodda kırmızıdır (rapora kırmızı-yeşil kanıtı).
- [ ] Davranış değişmez: dönüş değerleri, `durum_gecmisi` not metni (`DENEME_NOTU_ONEKI` + `basarili/toplam` + hatalar), INFO/WARNING log metinleri, erken dönüş yolları (zaten yüklü, anahtar kapalı, klasör tanımsız, rapor dizini yok, eşleşme üretilemedi) aynıdır; `test_g110_teslim_cevap.py` ve `test_g147_teslim_sharepoint_kimligi.py` mevcut testleri gövdeleri DEĞİŞMEDEN yeşil.
- [ ] Döngü sonrası yazım bayat nesneye yapılmaz: teslim yeniden okunur; bu arada durumu `uygulandi` dışına çıkmışsa ya da `cevap_yuklendi` başka yoldan `True` olmuşsa üzerine yazılmaz, yalnız not eklenir (testle).
- [ ] Log sözleşmesi: yeni ERROR yok (deneme-düzeyi WARNING kalır).
- [ ] `git diff --stat`: yalnız kapsamdaki iki dosya + görev raporu.

## Doğrulama
- `docker compose exec -T backend python -m pytest tests/test_g110_teslim_cevap.py tests/test_g147_teslim_sharepoint_kimligi.py`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data` (çıplak `ruff check .` git dışı `calibration-data/_g179/ek3_cevapla.py` yüzünden kırmızı — G183/G190 notu)
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM

### Yapılanlar

1. **`services/teslim_cevap.py::cevap_yukle`** (yalnız bu fonksiyon): eşleşme CSV'si üretildikten
   ve `_cevap_dosyalari` listesi alındıktan sonra `tid = int(teslim.id)` yerel değişkene alınır ve
   **yükleme döngüsünden ÖNCE `session.commit()`** ile transaction kapanır. Döngü ORM nesnesine
   dokunmaz (log satırları `teslim.id` yerine `tid`; `klasor`, `deneme`, `dosyalar` zaten yerel).
   Döngü sonrası teslim `tk._teslim_getir(session, tid)` ile **yeniden okunur**, not taze nesneye
   eklenir, commit.
2. **Döngü sırasında başka yol teslime yazdıysa:**
   - `cevap_yuklendi` başka yoldan True olmuşsa bayrak yeniden yazılmaz, yalnız deneme notu eklenir
     (taze `durum_gecmisi` listesine; başka yolun notu korunur) → dönüş True, mevcut INFO.
   - Durum `uygulandi` dışına çıkmışsa `cevap_yuklendi` YAZILMAZ, durum ezilmez, not yeni durumla
     eklenir → dönüş False + TEK WARNING ("… ama durum yükleme sırasında '<durum>' oldu —
     cevap_yuklendi yazılmadı").
3. Fonksiyon docstring'ine G194 paragrafı (neden + bayat nesne kuralı).
4. **Testler** (`tests/test_g110_teslim_cevap.py`, yalnız YENİ bölüm "6. G194", mevcut gövdeler
   değişmedi — diff'te bu dosyada silme satırı yok):
   - `test_g194_yukleme_sirasinda_oturumda_acik_transaction_yok[db-verilen|SessionLocal]` — sahte
     upload her dosyada `in_transaction()` kaydeder → hepsi False; SessionLocal yolunda
     `tk.SessionLocal` sayan fabrikayla (tek oturum açıldığı da doğrulanır).
   - `test_g194_baska_yol_yukleyip_not_dustuyse_uzerine_yazilmaz_yalniz_not_eklenir`
   - `test_g194_dongu_sirasinda_durum_uygulandi_disina_ciktiysa_bayrak_yazilmaz`
   - `test_g194_pg_idle_in_transaction_1sn_yavas_yukleme_cevap_yuklendi` (`dbtest`) — scratch DB
     (`test_migration_path._scratch_database` + `_run_init_db`, test_g107 `fresh_db` ikizi), her
     bağlantıda `SET idle_in_transaction_session_timeout = '1s'` (connect event'i, autocommit'te;
     test `SHOW` ile `1s` olduğunu doğrular), sahte yükleme dosya başına 1,5 sn uyur → `True`,
     DB'de `cevap_yuklendi=True` + not `cevap yükleme denemesi #1: 2/2 dosya → …`, ERROR yok.

### Kararlar

- **Döngü öncesi `commit`, `rollback` değil:** o noktada bu fonksiyonun bekleyen yazımı yok; ama
  `db` verilen yolda (teslim_uygula → `_cevap_dene`, gece turu) çağıranın oturumu kullanılır ve
  eski kod da sonda commit ediyordu — rollback çağıranın bekleyen bir değişikliğini sessizce atardı.
- **Durum dışına çıkma → False:** "dönüş True ⇔ DB'de `cevap_yuklendi=True`" değişmezi eski tüm
  yollarda geçerliydi (zaten yüklü → True; tam başarı → bayrak + True); korunmuş oldu. `uygulandi`
  nihai durumdur (`NIHAI_DURUMLAR`), kodda oradan çıkış yok — bu dal savunmacıdır (elle DB
  müdahalesi); WARNING deneme düzeyidir, ERROR eklenmedi.
- **Deneme numarası** döngü öncesi hesaplanan değerdir (eski davranış; not metni birebir).
- Erken dönüş yolları (zaten yüklü, anahtar kapalı, klasör tanımsız, rapor dizini yok, eşleşme
  üretilemedi) ve mevcut INFO/WARNING metinleri dokunulmadı.

### Test sonuçları

- **Kırmızı (eski kod, testler eklenip kod değişmeden):** `pytest tests/test_g110_teslim_cevap.py -k g194`
  → **5 failed**: iki transaction testi `[[True], …] == [[False], …]`; başka-yol testi
  `assert False is True` (sqlite tek bağlantıda iç içe BEGIN → yükleme hatası sayıldı); durum testi
  `'uygulandi' == 'basarisiz'`; dbtest `sqlalchemy.exc.OperationalError: (psycopg2.OperationalError)
  server closed the connection unexpectedly` (sunucu 1 sn'de oturumu kesti, son commit patladı).
- **Yeşil (yeni kod):**
  - `pytest tests/test_g110_teslim_cevap.py tests/test_g147_teslim_sharepoint_kimligi.py` → **67 passed**
    (önce 62; +5 yeni, skip yok — dbtest gerçekten koştu).
  - `pytest` (tam) → **3501 passed, 3 skipped**.
  - `ruff check . --extend-exclude calibration-data` → All checks passed.
  - `mypy` → Success: no issues found in 62 source files.
- `git diff --stat`: `backend/services/teslim_cevap.py`, `backend/tests/test_g110_teslim_cevap.py`
  + bu rapor (commit'e yalnız bu üçü girer).

### İzlenecekler

- G191 artık bu yoldan bağımsız: `idle_in_transaction_session_timeout` açıldığında cevap
  yüklemesi etkilenmez.
- NOT (kapsam dışı, düzeltilmedi): `eslesme_csv_uret` spool xlsx'ini (`hukdok_aktarim.xlsx_oku`)
  ve eşleşme sorgusunu `cevap_yukle`'nin açık transaction'ı içinde okur (`_teslim_getir` SELECT'i
  transaction'ı açmış olur). 8.409 satırlık gerçek pakette okuma saniyeler mertebesinde (60 sn'nin
  çok altı) ama paket büyürse aynı hata moduna girer; gerekirse `eslesme_csv_uret` çağrısı öncesi de
  commit edilebilir (bu görevin kapsamı yalnız yükleme döngüsüydü). Aynı desen
  `teslim_havuz_farki` için de geçerli.
- NOT: çalışma ağacında görev dışı kirli `.claude/settings.local.json` (M) ve `.claude/launch.json`
  (??) başta vardı (orkestrasyon ortamı); dokunulmadı, commit'e girmedi.
