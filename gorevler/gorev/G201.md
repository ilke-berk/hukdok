# G201 — SQLAlchemy 2.0.25 → 2.0.54 + arama EXPLAIN önce/sonra

- **Bant:** backend
- **Bağımlı:** G200 (`requirements.txt` ortak; backend seri)
- **Dosya kapsamı:** `backend/requirements.txt` (yalnız `sqlalchemy` satırı); kod YALNIZ yeni sürüm uyumsuzluk çıkarırsa (`managers/case_manager.py`, `services/rapor/registry.py`) — o durumda değişiklik minimum ve rapora gerekçeli
- **Dokunma:** `database.py::_MIGRATIONS` / `_TRGM_INDEXES` (migrasyon yok), arama semantiği (E8 `UNION`/`INTERSECT`), testler (gevşetilmez)

Kaynak: `docs/arsiv/bagimlilik-raporu-2026-09.md` §5.8.

## Hedef
29 yama sürümü birikti. 26-45 arası kalem kalem okunamadı; compound select (`union`/`intersect`) davranış
değişikliği görülmedi ama kanıtlanmadı. Kullanım: `managers/case_manager.py:11,810,933` (`_search_term_ids`,
`_term_case_id_selects`), `services/rapor/registry.py:99,1368`. Sürüm çekilir; arama ve rapor sorgularının ürettiği
SQL ve planın değişmediği ölçülür.

## Kabul kriterleri
- [ ] Sürüm çekilmeden ÖNCE `scripts/perf_olcum.py --term <2 örnek terim, biri çok terimli>` EXPLAIN çıktısı alınır; SONRA aynı komut tekrar koşulur; iki plan rapora yan yana (düğüm türleri + kullanılan index'ler). Plan farkı varsa açıklanır.
- [ ] `requirements.txt`'te `sqlalchemy==2.0.54`; imaj `--progress=plain` ile yeniden kurulur, `pip show sqlalchemy` 2.0.54.
- [ ] Arama bekçileri (`test_g055_arama_union.py`, `test_g190_arama_tek_kosu.py`, `test_g051_kart_ve_arama_sorgulari.py`) ve rapor testleri gövdeleri DEĞİŞMEDEN yeşil.
- [ ] Tam pytest + ruff + mypy yeşil; yeni SQLAlchemy uyarıları (`SAWarning`/`DeprecationWarning`) varsa sayısı ve kaynakları rapora.
- [ ] Rapor: 2.0.26-2.0.54 changelog'undan bizi ilgilendiren kalemler (compound select, psycopg2 dialect, Core `select` derleme).

## Doğrulama
- `docker compose exec -T backend python scripts/perf_olcum.py --term "<terim>"` (önce/sonra)
- `docker compose build --progress=plain backend && docker compose up -d backend`
- `docker compose exec -T backend pip install -q -r requirements-dev.txt`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data`
- `docker compose exec -T backend python -m mypy`

## Rapor
(işçi oturumu doldurur)
