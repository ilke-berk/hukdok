# G151 — Karar durumu havuzları: "Kapalı/Derdest" yerel havuzdan çıkar, aktarımda "karar yok" kuralı, seed genişlemesi

- **Bant:** backend
- **Bağımlı:** G150 (TAMAM 3be2a74; aynı `hukdok_aktarim.py` aşama bölümü)
- **Dosya kapsamı:** backend/managers/seed_data.py (`_YEREL_KARAR_ADLARI` `:416-445`, istinaf `:446`, temyiz `:447`),
  backend/scripts/hukdok_aktarim.py (aşama satırı yazımı çevresi — yalnız "karar yok" kuralı),
  backend/scripts/deger_havuzu_seed.py (paket elemesi + `--kaldir`), backend/tests/test_g151_havuz_kurali.py (YENİ)
- **Dokunma:** backend/managers/stage_decisions.py (doğrulama mantığı değişmez), backend/managers/reference_lists.py,
  backend/routes/config.py, frontend/**

> **YENİDEN KUYRUKTA (2026-09-08 sabah, planlayıcı).** İlk koşu BLOKE oldu: görev "Kısmen Kabul"ü istinaf
> havuzuna eklemeyi şart koşuyordu, kilitli iki test aynı değeri **havuz dışı örnek** olarak kullanıyor.
> Görev tanımı hatasıydı; aşağıda izin genişletildi ve iki kabul kriteri düzeltildi. **İşçinin taslağı
> `git stash` içinde:** `stash@{0}` "G151-wip: havuz kurali taslagi" (seed_data, deger_havuzu_seed,
> hukdok_aktarim, test_g060, test_g151). İşçi ana dizinde **önce `git stash pop`** ile başlar (çakışma
> beklenmez: G150 sonrası bu dosyalara başka görev dokunmadı), sonra yalnız aşağıdaki izinli test değişikliğini
> yapıp yeşile çeker. Önceki koşunun raporu en altta korunmuştur.

## Test değiştirme İZNİ (baştan — genişletildi)

**İZİNLİ:** `tests/test_seed_data.py` ve `tests/test_g060_karar_listeleri.py`'de yerel/istinaf/temyiz liste
**içerik** beklentileri (sayı ve ad listesi). **EK İZİN (08.09 sabah):** `tests/test_g066_karar_durumu_dogrulama.py`
(`DORT_ALAN` `:53` istinaf örnek değeri, `test_route_liste_disi_degeri_400_donuyor` `:367/:372`) ve
`tests/test_g103_belgeleme_olayi.py` (`test_karar_durumu_kapisi_hala_calisiyor` `:376/:380`): **YALNIZ havuz dışı
örnek değer** "Kısmen Kabul" → havuzda olmayan başka bir dizgi (ör. "Lehe İstinaf"); test mantığı ve assert sayısı
değişmez. **KİLİT:** `test_g062_stage_decisions.py`, `test_g076_havuz_disi_serhi.py`, `test_g124_deger_havuzlari.py`
(mekanizma), diğerleri.

## Hedef

Plan 08.09 §1.2 **A4 + A5** (kullanıcı kararı 08.09: Kapalı/Derdest havuzdan çıkar; genişleme seçici).
- Yerel havuzda `Kapalı` (`seed_data.py:431`) ve `Derdest` (`:423`) var; bunlar mahkeme kararı değil büro dosya
  durumu. Ekip 409 hücreye bu değerleri **bizim havuzumuzda var diye** yazdı (06.09 cevap notu §2-a).
- İstinaf/temyiz havuzları meşru sonuçları taşımıyor: HMK 353/1-b-2 düzelterek karar; 193+15+3 föy "tanınmayan".

## Yapılacaklar

1. **Seed:** yerel listeden `Kapalı` ve `Derdest` çıkar; `Red/Usulden` ekle. İstinafa ekle: `Düzeltilerek Karar
   Verildi`, `Düzeltilerek Kabul Edildi`, `Düzeltilerek Reddine`, `Kısmen Kabul`, `Davacı İstinaf Talebinin Kabulü`.
   Temyize ekle: `Kısmen Onama/Kısmen Bozma`. **`Karar` (74 föy) EKLENMEZ** (ekibe soruldu — plan §4-6).
   Yazım birebir (seed `add_item` normalize'ını atlar). Mevcut kurulumda seed yalnız ekler; `Kapalı`/`Derdest`
   satırlarını silmek için `deger_havuzu_seed.py --kaldir AD` (dry-run varsayılan, `--apply`): satır kartta/aşamada
   kullanılıyorsa SİLİNMEZ, raporlanır.
2. **Aktarım kuralı:** Karar_Asamalari satırında `karar_durumu` ∈ {Kapalı, Derdest} (harf/boşluk/aksan toleranslı)
   → **"karar yok"**: künye boşsa satır yazılmaz, künye doluysa durum boş + şerh "büro durumu, karar değil";
   `sonuc.buro_durumu_atlanan` sayacı; INFO (beklenen durum). Kardeş uzlaşısında büro durumu boş hücre gibi
   (imzaya girmez). Sheet'in "Yerel Mahkeme Karar Durumu" sütunu aktarımca zaten OKUNMUYOR (kart kolonunun tek
   yazıcısı aşama fotoğrafı) — ayrı yol eklenmez.
3. **Paket seed elemesi:** `deger_havuzu_seed.py` dört karar listesine paketten büro durumu sokmaz.
4. **Testler:** seed içerikleri; Kapalı/Derdest satırı yazılmaz + sayaç; yeni değerler doğrulamadan geçer;
   `--kaldir` dry-run/apply/kullanılan satır korunur; havuz dışı davranış (durum boş + şerh) değişmedi.

## Kabul kriterleri

- [x] Seed: yerel 27, istinaf 8, temyiz 4 (test); `Karar` havuzda YOK (grep seed).
- [x] Kapalı/Derdest gelen aşama satırı karar durumu üretmez, sayaç + INFO (test); künye doluysa satır durumsuz yazılır.
- [x] Kaldırma yolu dry-run varsayılan, kullanılan satırı silmez (test).
- [x] **Lokal DB:** `deger_havuzu_seed --kaldir Kapalı --kaldir Derdest --apply` koşulur (Derdest kullanımdaysa
      korunur, raporda sayı); `seed_all_lists` ile yeni 7 değer lokal listeye girer; sonuç liste sayıları raporda.
- [x] 04.09 paketiyle kuru koşu: `buro_durumu_atlanan` sayısı raporda (**bu paketin Karar_Asamalari sayfasında
      yalnız 2 Derdest satırı var → beklenen 1-2**; "≈409" ekibin Sheet sütunu sayısıdır, kriter DEĞİL); ikinci koşu 0.
- [x] `tam pytest` yeşil; g066/g103'te yalnız örnek değer değişti (rapor: değiştirilen test → gerekçe); ruff + mypy temiz.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-08 sabah, ikinci koşu, işçi)

### Yapılanlar

- `stash@{0}` (G151-wip) çakışmasız açıldı; kod taslağı (seed_data, deger_havuzu_seed, hukdok_aktarim,
  test_g060, test_g151) **değiştirilmeden** korundu — içerik aşağıdaki "Önceki koşu raporu"nda anlatıldığı gibi.
- **İzinli test değişikliği (08.09 ek izni):** yalnız havuz dışı örnek değer `"Kısmen Kabul"` → `"Lehe İstinaf"`
  (`grep` ile hiçbir seed/havuzda olmadığı doğrulandı):
  - `tests/test_g066_karar_durumu_dogrulama.py` `DORT_ALAN` `:53` (istinaf liste dışı örnek) ve
    `test_route_liste_disi_degeri_400_donuyor` `:367` (istek gövdesi) / `:372` (detail'de değer aranır) — 3 dizgi.
  - `tests/test_g103_belgeleme_olayi.py` `test_karar_durumu_kapisi_hala_calisiyor` `:376` (docstring, G151 notu
    eklendi) / `:380` (çağrı) — 2 dizgi.
  - **Gerekçe:** görev "Kısmen Kabul"ü istinaf havuzuna ekledi; iki test aynı dizgiyi *havuz dışı* örnek olarak
    kullanıyordu. Test mantığı, assert sayısı ve beklentiler AYNEN; yalnız örnek verisi değişti.
- Kilitli testlere (`test_g062`, `test_g076`, `test_g124`, diğerleri) dokunulmadı.

### Doğrulama

- `docker compose exec -T backend python -m pytest` (tam): **2869 passed, 3 skipped** (144 s) — önceki koşunun
  3 kırmızısı örnek-değer düzeltmesiyle yeşile döndü; `test_g151_havuz_kurali.py` YENİ (25 test fonksiyonu).
- `ruff check .` → All checks passed! (test düzenlemelerinden SONRA yeniden koşuldu) · `mypy` → Success: no issues
  found in 61 source files.

### Lokal DB (yedek: `C:\hukdok-veri\yedek\pre_g151_20260908.dump`, 4,9 MB, `--kaldir --apply` ÖNCESİ)

1. `deger_havuzu_seed.py --liste local_decisions --kaldir Kapalı --kaldir Derdest` kuru koşu: **Kapalı 0 kart / 0 aşama
   → silinebilir; Derdest 1 kart / 1 aşama → KULLANILIYOR, silinmedi.** `--apply`: Kapalı SİLİNDİ, Derdest korundu
   (kural çalıştı, sayı raporda).
2. `seed_all_lists`: yerel `Red/Usulden` + istinaf `Düzeltilerek Reddine`, `Kısmen Kabul`, `Davacı İstinaf Talebinin
   Kabulü` + temyiz `Kısmen Onama/Kısmen Bozma` girdi (5 yeni satır). **İstinafın 2 değeri (`Düzeltilerek Karar
   Verildi`, `Düzeltilerek Kabul Edildi`) girmedi:** 05.09 paket seed'i aynı kodları (`DUZELTILEREK_KARAR_VERILDI`,
   `DUZELTILEREK_KABUL_EDILDI`) paketin küçük harf yazımıyla (`Düzeltilerek karar verildi`) zaten eklemişti; seed
   kod-bazlı tekilleştirir (`filter_by(code=…)`), ad çakışmasına dokunmaz. Lokal DB'de bu iki değer paket yazımıyla
   duruyor — ad düzeltmesi panelden (seed yalnız ekler, yeniden adlandırmaz; taşıma değil). Prod'da paket seed'i
   koşulmadığından prod'a deploy'da 7 değerin 7'si resmi yazımla girer.
3. 04.09 paketi (`/tmp/paket_0409.xlsx`, sha256 masaüstündeki `HUKDOK_TESLIM_PAKETI_2026-09-04.xlsx` ile BİREBİR)
   `--dry-run`: okunan 8.409 · alan değişikliği 0 · aşama satırı **0 eklendi, 1 güncellendi, 0 ikinci tur, 0 belgeli
   (önceki esas: 0, büro durumu atlanan: 1)** · kardeş çelişkisi 6.610 (G150 zemini, değişmedi) · ERROR 0 · INFO:
   "Aşama karar durumu büro durumu, durum boş yazıldı: kart 13161 YEREL 'Derdest'". **Kabul beklentisi 1-2 → 1.**
   Çıkış kodu 1 = `CIKIS_SATIR_HATASI` (23 satır hatası, paketin bilinen zemini; ERROR değil).
4. Aynı paket **uygulandı** (yazıldı mı: EVET, 1 aşama satırı güncellendi, 0 ERROR) → kart 13161 `yerel_karar_durumu`
   **None** (aşama fotoğrafı kuralı izledi; YEREL satırı durumsuz + "büro durumu, karar değil: Derdest" şerhi).
5. **İkinci koşu** (`--dry-run`): aşama satırı **0 / 0 / 0 / 0**, alan değişikliği 0, büro durumu atlanan 1 (sayaç her
   koşuda yeniden sayar — tasarım; YAZMA 0), ERROR 0.
6. Paket uygulaması Derdest'in tek kullanımını kaldırdığı için `--kaldir Derdest` kuru koşu artık 0/0 → `--apply`
   ile SİLİNDİ. **Son lokal sayılar:** local_decisions **28** (27 seed + 1 paket: `Red/Usulden` dahil, Kapalı/Derdest
   YOK), appeal_decisions 25, cassation_decisions 4, revision_decisions 9.
7. `--liste appeal_decisions --kaldir Karar` kuru koşu: 0 kart / 0 aşama → silinebilir; **uygulanmadı** (görev
   kapsamı seed'de `Karar` olmamasıdır; lokal satır 05.09 paket seed'inden — silme kararı insanın).

### Kararlar + gerekçeleri

- Örnek değer olarak `"Lehe İstinaf"` seçildi: görev dosyasının önerdiği dizgi, hiçbir listede yok, testin
  "liste dışı → 400 + detail'de değer" mantığını aynen taşır.
- Derdest'in ikinci adımda silinmesi: kriter "kullanımdaysa korunur" der; paket uygulaması kullanımı sıfırladıktan
  sonra kaldırmak görevin hedefiyle (Kapalı/Derdest havuzdan çıkar) tutarlı; her iki durum da raporda.
- NOT (kapsam dışı, dokunulmadı): çalışma ağacında `.claude/settings.local.json` (M) ve `.claude/launch.json` (??)
  harness'ın kendi dosyaları; commit'e alınmadı.

### İzlenecekler

- Prod'a deploy sonrası: `deger_havuzu_seed.py --liste local_decisions --kaldir Kapalı --kaldir Derdest` önce kuru
  koşu (prod'da Derdest kullanımı lokalden farklı olabilir), sonra `--apply`; seed yeni 7 değeri açılışta ekler.
- `Karar` (74 föy) ekibin cevabını bekliyor (plan §4-6).

---

## Önceki koşu raporu (2026-09-08 gece, BLOKE — taslak `stash@{0}`'da korundu)

DURUM: BLOKE — test değiştirmek gerekti: KİLİT dosyalar `tests/test_g066_karar_durumu_dogrulama.py`
(`DORT_ALAN` `:53`, `test_route_liste_disi_degeri_400_donuyor` `:367/:372`) ve
`tests/test_g103_belgeleme_olayi.py` (`test_karar_durumu_kapisi_hala_calisiyor` `:376/:380`)
"Kısmen Kabul"ü **havuz dışı örnek değer** olarak kullanıyor; görev bu değeri istinaf havuzuna
EKLEMEYİ şart koşuyor (kabul: istinaf 8). Mekanizma bozulmadı (yalnız örnek verisi çakıştı), ama
düzeltme yalnız kilitli testteki örnek değeri değiştirmekle mümkün (ör. "Kısmen Kabul" →
"Lehe İstinaf"), o izin yok → kod COMMIT'LENMEDİ, çalışma ağacında duruyor (2026-09-08, işçi).

### Doğrulama (o oturum)

- `pytest` (tam): **3 failed, 2866 passed, 3 skipped** — üç kırmızı yukarıdaki örnek-değer
  çakışması; `test_g151_havuz_kurali.py` (29 test) + g060/g064/g072/g076/g124/g150/seed_data
  hedefli koşu 195 passed. `ruff check .` temiz; `mypy` "no issues in 61 source files".
- Lokal DB kuru koşuları (yazılmadı): `--kaldir Kapalı --kaldir Derdest` → Kapalı 0 kart/0 aşama
  **silinebilir**, Derdest 1 kart + 1 aşama **KULLANILIYOR — silinmedi** (kural çalışıyor);
  `--liste appeal_decisions --kaldir Karar` → 0/0 (05.09 paket seed'i eklemişti; seed'de yok).
  04.09 paketi `--input` kuru koşu: 13 listede 0 yeni (lokal 05.09'da paketten kurulmuştu; yerel
  30 = 28 seed + 2 paket, istinaf 25 — `Kapalı`/`Derdest` lokal yerel listede HÂLÂ var,
  `--apply` bloke sebebiyle KOŞULMADI). `seed_all_lists` de koşulmadı (yeni 7 değer lokal DB'de yok).
- **04.09 paketi ölçümü:** `Karar_Asamalari`'nda büro durumu yalnız **2 satır** (Derdest/Yerel);
  Sheet'in "Yerel Mahkeme Karar Durumu" sütununda 1.519 Derdest + 2.227 Kapalı — bu sütun aktarımca
  OKUNMUYOR (kart kolonunun tek yazıcısı aşama fotoğrafı). Görevdeki "≈409" bu paketle
  ÜRETİLEMEZ. Ekibin 409'u muhtemelen başka kesit (Sheet sütunu ya da 18.08 paketi).
- **04.09 paketi tam kuru koşu (lokal DB, yeni kodla):** okunan 8.409 · alan değişikliği 0 ·
  aşama satırı "0 eklendi, **1 güncellendi**, 0 ikinci tur, 0 belgeli korundu (önceki esas: 0,
  **büro durumu atlanan: 1**)" · yazıldı mı: HAYIR. Güncellenen satır kart 13161 YEREL: eski
  havuzla yazılmış `Derdest` → durum boş + şerh (INFO log gözlendi, WARNING yok). Kardeş
  çelişkisi 6.610 satır raporlandı — bu görevin değişikliği imzadan yalnız DEĞER ÇIKARIR (çelişki
  artıramaz); sayı G150 sonrası zeminin kendisi, karşılaştırma için eski kodla koşulmadı.

### Yapılanlar (stash'teki taslak)

- **`managers/seed_data.py`:** yerel listeden `Kapalı` ve `Derdest` çıktı, `Red/Usulden` girdi
  (alfabetik konum, Red/MSK sonrası); istinafa 5 (`Düzeltilerek Karar Verildi`, `Düzeltilerek Kabul
  Edildi`, `Düzeltilerek Reddine`, `Kısmen Kabul`, `Davacı İstinaf Talebinin Kabulü`), temyize 1
  (`Kısmen Onama/Kısmen Bozma`) eklendi → **Yerel 27 · İstinaf 8 · Temyiz 4** (KD 2 dokunulmadı).
  Yazım birebir (mevcut `_seed_karar_listesi` yolu, normalize yok); kodlar `_karar_kodu` ile
  (`RED_USULDEN`, `KISMEN_ONAMA_KISMEN_BOZMA`…), tekil. `Karar` EKLENMEDİ (grep seed: yok).
- **`scripts/hukdok_aktarim.py` (yalnız Karar_Asamalari bölümü + sayaç/özet):**
  `BURO_DURUMLARI = {KAPALI, DERDEST}` + `_buro_durumu_mu` (`_baslik_anahtari` → büyük/küçük
  harf, boşluk, aksan toleranslı), `_kunye_dolu` (mahkeme/esas/karar no/karar tarihi/tebliğ
  tarihi; yer tutucu tarih künye sayılmaz). Yazma döngüsünde **"karar yok" kuralı:** büro
  durumu taşıyan satır künyesizse HİÇ yazılmaz (sıra da tüketmez), künyeliyse durum boş +
  `"büro durumu, karar değil: <ham>"` şerhi; `sonuc.buro_durumu_atlanan` sayacı; **INFO** log
  (WARNING değil — beklenen durum). Kardeş uzlaşısında büro durumu **imzaya girmez** (boş hücre
  gibi): H-1 `Derdest` + H-2 `Kabul` → çelişki değil, `Kabul` yazılır; iki kardeş de büro
  durumuysa değer ilk dolu hâliyle taşınır ve kural uygulanır. Özet satırında
  "büro durumu atlanan: N". Havuz dışı yol (G076: durum boş + "havuz dışı durum" şerhi +
  WARNING + `havuz_disi_durum`) DEĞİŞMEDİ.
- **`scripts/deger_havuzu_seed.py`:** (a) dört karar listesine (`KARAR_LISTELERI`,
  `STAGE_DECISION_LISTS`ten türetilir) paketten büro durumu GİRMEZ — aksi hâlde paket seed'i
  `Kapalı`/`Derdest`i geri sokardı; diğer BÜYÜK/BOZUK yazımlar yine geçer (kullanıcı kararı
  05.09). (b) **`--kaldir AD` (tekrarlanabilir) + `--liste`** (varsayılan `local_decisions`):
  kuru koşu varsayılan, `--apply` ile yazar; satır kart kolonunda (`reference_lists.DEPENDENCIES`)
  ya da aşama satırında (`case_stage_decisions.karar_durumu`, ilgili stage) kullanılıyorsa
  SİLİNMEZ, kullanım sayısıyla raporlanır. `--input` artık `--kaldir` varken zorunlu değil.
- **Testler:** `tests/test_g151_havuz_kurali.py` (YENİ, 4 katman: seed sabitleri; doğrulama
  yeni değerleri geçirir/çıkarılanları reddeder; aktarım kuralı — künye boş/dolu, tolerans, sıra,
  kardeş uzlaşısı, eski kurulum `Derdest` satırının paketle durumsuza güncellenmesi (tarihçeli,
  fotoğraf izler), ikinci koşu 0, havuz dışı gerileme nöbetçisi, özet metni; seed elemesi +
  `--kaldir` kuru koşu/apply/CLI). **Değiştirilen test → gerekçe:** `test_g060_karar_listeleri.py`
  yalnız liste İÇERİK beklentileri (28→27, 3→8, 3→4, `RESMI_*` adları) — görev dosyasının baştan
  verdiği izin; assert sayısı azalmadı, mekanizma testleri aynen.

### Kararlar + gerekçeleri (o oturum)

- **Sheet'teki "Yerel Mahkeme Karar Durumu" sütunu:** aktarım bu sütunu ZATEN OKUMUYOR
  (`KART_ALANLARI`nda yok; `yerel_karar_durumu` kart kolonunun tek yazıcısı aşama fotoğrafı,
  G062). Kural tek yazma yolunda (aşama satırı) uygulanınca fotoğraf onu izler — ayrı bir kart
  yolu eklemek ikinci yazıcı doğururdu. Docstring'e yazıldı.
- **Sayaç semantiği:** `buro_durumu_atlanan` her koşuda yeniden sayar (`havuz_disi_durum` ve
  `asama_belgeli_korunan` ile aynı desen — "sessizce yutulmaz"); idempotency ölçütü YAZMA
  sayaçlarıdır (ikinci koşuda `asama_eklenen`/`asama_guncellenen`/tarihçe 0 — test bunu kilitler).
- **Silme yolu yalnız script'te**, `stage_decisions`/`reference_lists`/`routes/config` dokunulmadı
  ("Dokunma" listesi). `_kunye_dolu`da başvuran taraf künye sayılmadı (büro durumu + yalnız
  başvuran taraf anlatacak karar taşımaz).
