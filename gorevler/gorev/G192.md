# G192 — Düşük etkili backend kalemleri: offset tavanı, N+1'ler, outbox vade filtresi SQL'de, PK ikizi index'lerin düşürülmesi

- **Bant:** backend
- **Bağımlı:** G190,G191
- **Dosya kapsamı:** `backend/routes/cases.py` (offset `le`, ilişki döngüsü 391-405), `backend/routes/clients.py` (poliçe döngüsü 158-168), `backend/services/upload_queue.py` (`_scan_once` 494-500), `backend/database.py` (yalnız `_DUSURULECEK_INDEXLER`), YENİ test dosyası
- **Dokunma:** `backend/managers/case_manager.py` (G190'da bitti), `backend/models.py` (PK `index=True` KALIR), mevcut test gövdeleri (özellikle `tests/test_index_envanteri.py`, `tests/test_perf_olcum.py` — değişmesi gerekirse BLOKE), `constants.CASE_STATUSES`

> **Kullanıcı kararı 14.09 (BLOKE sonrası):**
> - **D10 → seçenek B:** `models.py` DEĞİŞMEZ (G042 deseni, `database.py:1198-1202` şerhi). Yalnız DROP yarısı:
>   20 `ix_<tablo>_id` `_DUSURULECEK_INDEXLER`'e girer; yeni kurulumda `create_all` yine yaratır, düşürme op'u
>   her `init_db`'de idempotent siler. "PK kolonunda `index=True` yok" AST kabul maddesi KALDIRILDI.
> - **Görev bölündü:** D9 (`cases.status` CHECK NOT VALID) **G195**'e taşındı. Bu görev yalnız D8, D11, D15, D10-DROP.
>   Aşağıdaki Rapor'daki "Doğrulanan ama geri alınan uygulama" yol haritası aynen uygulanabilir (o koşunun
>   scratchpad patch'i YOK sayılır, yeniden yazılır).

Kaynak: `docs/arsiv/performans-denetimi-2026-09-14.md` D8, D10, D11, D15 (D9 → G195). D12 (json→jsonb)
ve D13/D14 bilinçli KAPSAM DIŞI (aciliyet yok / tek konteyner).

## Hedef
Dört küçük, birbirinden bağımsız düzeltme; hepsi davranışı koruyarak (5. madde tarihsel — G195'e taşındı, bu görevde UYGULANMAZ):
1. **D8** `routes/cases.py:90` `offset` Query parametresine tavan (`le=10000`) ve `limit`'e mevcut
   tavan doğrulanır; tavan aşımı 422. Keyset sayfalama KAPSAM DIŞI (arama modunda relevance
   sıralaması offset ister).
2. **D11** `routes/cases.py:391-405` ilişki başına `first()` → id listesi `in_()` tek sorgu + dict;
   `routes/clients.py:158-168` poliçe başına `original_filename` sorgusu → tek `in_()` sorgu.
   `case_manager.py:1857-1858` G190 alanı, DOKUNMA.
3. **D15** `upload_queue._scan_once`: `next_attempt_at IS NULL OR next_attempt_at <= now` SQL'e
   taşınır (parçalı index `idx_upload_outbox_pending (next_attempt_at) WHERE status='pending'`
   kullanılır); `_is_due` Python yardımcısı kalabilir ama sorgu zaten eler.
4. **D10 (seçenek B)** PK ikizi `ix_<tablo>_id` adları (20 tablo: alleged_faults, appeal_courts,
   currencies, medical_events, …) `_DUSURULECEK_INDEXLER`'e (G042 deseni, `("index", ...)` op'u);
   `models.py`'deki `index=True` KALIR. `idx_case_docs_conversion_pending
   (id) WHERE pending` DOKUNULMAZ (parçalı, müşterisi var — doğrula, rapora).
5. ~~**D9**~~ (G195'e taşındı — UYGULAMA) `cases.status` için `ALTER TABLE cases ADD CONSTRAINT ck_cases_status_uclu CHECK (status
   IN ('DERDEST','DANIŞ','MAHZEN')) NOT VALID` — idempotent (`DO $$ … IF NOT EXISTS
   (pg_constraint) …`), ayrı `("index", ...)` op'u (G041 kuralı: koşulsuz op). `NOT VALID`
   mevcut satırları denetlemez, yeni yazımları zorlar → migrasyon prod'da bozuk eski değere
   TAKILMAZ; `VALIDATE CONSTRAINT` G183 ölçümü prod'da 0 ihlal gösterince ayrı görevde.
   Değer listesi `constants.CASE_STATUSES`'ten üretilir (elle yazılmaz).

## Kabul kriterleri
- [ ] Test: `GET /api/cases?offset=10001` → 422; `offset=10000` → 200.
- [ ] Test: ilişkili 5 dava için `routes/cases.py` ilişki bloğu tek SELECT koşar (cursor sayacı); poliçe listesi için `case_documents` sorgusu 1'dir.
- [ ] Test: `_scan_once` vadesi gelmemiş (`next_attempt_at` gelecekte) satırı DB'den hiç çekmez (sorgu sayacı ya da `filter` içeriği); `NULL` ve geçmiş vadeler çekilir (startup reconcile davranışı korunur).
- [ ] Test: `create_all`'ın ürettiği PK ikizi `ix_<tablo>_id` index'lerinin TAMAMI `_DUSURULECEK_INDEXLER`'dedir (`Base.metadata` taramasından türetilir, elle liste değil); `dbtest`: `init_db()` sonrası `ix_<tablo>_id` index'leri YOK, PK index'leri VAR, ikinci `init_db()` idempotent. `models.py` diff'i BOŞ.
- [ ] Mevcut testler gövdeleri DEĞİŞMEDEN yeşil — `tests/test_index_envanteri.py` (≥25 eşiği, seçenek B ile 46 ikizin hepsi listede) dahil.
- ~~CHECK kısıtı dbtest'i, `CASE_STATUSES` değer listesi testi, `dava-acma-akisi.md` notu~~ → **G195** (kullanıcı kararı 14.09).

## Doğrulama
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- `docker compose build backend && docker compose up -d backend` → migrasyon logu temiz

## Rapor

### Gece kaydı — DURUM: BLOKE — test değiştirmek gerekti (2026-09-14, işçi oturumu; kod yazıldı + doğrulandı, GERİ ALINDI, commit yok)

G191 emsali ("görev tek iş = tek commit ve kabul kriterleri bütün hâlinde — yarım görev
commit'lenmedi") ve gorev-devam BLOKE kuralı uygulandı. Çalışma ağacı HEAD `15a4378` ile birebir
(`git diff --quiet` 4 kaynak dosyada 0); bu dosya dışında kirli dosya yok.

#### Sebep 1 — D9 (`cases.status` CHECK NOT VALID) mevcut testle çelişiyor
`backend/tests/test_perf_olcum.py:293-309` `olcum_db` fixture'ı ÖNCE `mig._run_init_db(engine)`
koşar, SONRA `('G183/3', 'TEMYIZ', ...)` satırını INSERT eder (satır 302); test satır 332'de
`| TEMYIZ | 1 | 0 | evet |` bekler. `NOT VALID` yalnız mevcut satırları denetlemez, YENİ INSERT'i
zorlar → kısıt init_db'de kurulunca fixture reddedilir (dbtest'ler container'da gerçekten koşuyor,
atlanmıyor). Kabul kriteri tam da bu INSERT'in reddini istiyor → ikisi aynı anda sağlanamaz.

#### Sebep 2 — D10'un models.py yarısı (PK `index=True` kaldırma) mevcut testle çelişiyor
`backend/tests/test_index_envanteri.py:229-271` `test_dusurulen_ikizlerin_kapsamasi_semada_duruyor`,
`create_all()`in düşürme listesindeki adlardan **≥25** tanesini yarattığını varsayar (satır 258).
Container'da ölçüldü (`Base.metadata` taraması): create_all 46 PK ikizi üretiyor, 26'sı listede,
20'si eksik (görevin 20 tablosu), listede PK-dışı create_all index'i 0. `index=True` kalkarsa
sayı 26 → 0, test kırılır. G042 şerhi (`database.py:1198-1202`) de bu yüzden "models.py DEĞİŞMEZ" der.

Denenmedi (sahte yeşil): ikizleri `__table_args__`'a `Index(...)` olarak taşımak, eşiği tutturmak
için ek index düşürmek, testi değiştirmek.

#### Doğrulanan ama geri alınan uygulama (ikinci koşu için yol haritası)
- **D8** `routes/cases.py` `offset: Query(0, ge=0, le=10000)`.
- **D11** ilişki döngüsü: ONERI_RED hariç karşı id'ler toplanır → tek `Case.id.in_()` +
  `selectinload(parties)` → dict; `manual_rows` sırası korunur, boş kümede sorgu koşmaz.
  `routes/clients.py`: `source_document` adları → tek `(original_filename, sharepoint_url)`
  `in_()` sorgusu `id desc` → `setdefault` ile adın en yeni canlı URL'si.
- **D15** `upload_queue._scan_once` filtresine `or_(next_attempt_at IS NULL, next_attempt_at <= now)`;
  `_is_due` defansif kaldı. Lokal DB `EXPLAIN` (seqscan kapalı): `Index Scan using idx_upload_outbox_pending`.
- **D10 (DROP yarısı)** 20 `ix_<tablo>_id` `_DUSURULECEK_INDEXLER`e (alleged_faults, app_settings,
  appeal_courts, appeal_decisions, appealing_parties, applied_methods, cassation_courts,
  cassation_decisions, client_policies, client_types, currencies, defendant_administrations,
  event_types, judgment_roles, local_decisions, medical_events, medical_processes, patient_harms,
  revision_decisions, service_types). `idx_case_docs_conversion_pending` PK ikizi değil (kısmi),
  müşterisi `services/conversion_retry.py:385-395` gece taraması → dokunulmadı.
- Yeni test dosyası (sqlite + `before_cursor_execute` sayacı; D10 için scratch Postgres dbtest):
  offset 10001→422 / 10000→200; 5 ilişkide `cases.id IN` tek SELECT + parties tek; sorgu sayısı
  ilişki sayısından bağımsız; poliçe listesinde `case_documents` sorgusu 1; `_is_due`=True iken
  gelecek vadeli satır işlenmiyor; model PK ikizlerinin tamamı listede; init_db sonrası ikiz yok,
  PK var, ikinci koşu idempotent.

Sonuçlar (kod yerindeyken): yeni dosya **14 passed** (dbtest dahil); etkilenen mevcut dosyalar
130 passed / 1 skipped; tam koşu **3531 passed, 3 skipped**; ruff dokunulan 5 dosyada temiz
(tüm ağaçtaki 6 hata `calibration-data/_g179/ek3_cevapla.py` — repo dışı volume, görevle ilgisiz);
mypy 62 dosya temiz; `docker compose build backend && up -d` migrasyon logu temiz. Geri alma sonrası
HEAD imajı yeniden build edildi, stack sağlıklı (yalnız rutin açılış WARNING'leri: mojibake map, cache empty).

#### Yan etki (yalnız lokal DB)
Doğrulama koşusu lokal DB'de 20 `ix_<tablo>_id`'yi düşürdü; HEAD bunları geri yaratmaz (create_all
mevcut tabloyu atlar) → 0/20, 20 PK index'i yerinde. PK ikizi olduğu için plan etkisi yok; hedef
şemayla aynı son durum. Prod'a dokunulmadı.

#### Sabah kararı için öneri (insan)
1. `test_perf_olcum.py` `olcum_db`: TEMYIZ satırı "kısıttan ÖNCE var olan bozuk satır" olarak kurulsun —
   init_db → `ALTER TABLE cases DROP CONSTRAINT ck_cases_status_uclu` → INSERT → kısıt `NOT VALID`
   ile yeniden. Satır 332 beklentisi DEĞİŞMEZ (prod senaryosunun birebir taklidi, gevşetme değil).
2. `test_index_envanteri.py:258` ≥25 eşiği: "eski kurulum" elle taklit edilsin (listedeki PK ikizleri
   init_db öncesi `CREATE INDEX ix_<t>_id ON <t> (id)` ile kurulur), kapsama iddiası aynen kalır.
   Alternatif: models.py'yi değiştirmeyip (G042 deseni) yalnız DROP yarısını kabul etmek ve
   AST kabul maddesini kaldırmak.
3. Karar sonrası ikinci koşu yukarıdaki yol haritasıyla D9 + D10'u tamamlar.

#### İzlenecekler
- D9 `NOT VALID` uyarısı: kısıt yeni INSERT'i ve bozuk eski satırın HER UPDATE'ini (başka kolon
  değişse bile) reddeder. Prod'a çıkmadan G183 ölçümüyle (`perf_olcum` durum bölümü) ihlal 0 doğrulanmalı.
- Çalışma ağacında görev dışı kirli dosyalar vardı (`.claude/settings.local.json` M,
  `.claude/launch.json` ??) — koşucu yapılandırması, dokunulmadı.
