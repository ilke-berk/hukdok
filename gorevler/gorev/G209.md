# G209 — [DIŞ REPO ../hukukbot-ui] Eski hukbot arayüzü silinir

- **Çalışma dizini:** `C:\Users\ilkeb\OneDrive\Masaüstü\hukukbot-ui` (AYRI repo; gündüz oturumu, runner koşamaz).
- **Bant:** docs (kod silme; test = backend testleri değişmeden yeşil)
- **Bağımlı:** G208 + canlı geçiş tamam (HukuDok `/hukukbot` prod'da doğrulandı, hukbot.tragic.tr kapandı). Geri dönüş yolu git geçmişidir.
- **Dosya kapsamı (SİLİNİR):** `App.tsx`, `index.tsx`, `index.html`, `index.css`, `types.ts`, `vite-env.d.ts`, `components/**`, `contexts/**`,
  `services/**`, `config/authConfig.ts`, `package.json`, `package-lock.json`, `tsconfig.json`, `vite.config.ts`, `tailwind.config.js`,
  `postcss.config.js`, `metadata.json` (frontend'e aitse — önce kontrol), `Dockerfile.frontend`, `deploy/frontend-nginx.conf`; `dist/`,
  `node_modules/` (git'te değilse yalnız diskten). **Güncellenir:** `.env.example` (VITE_* kalkar), `.gitignore`, `README`/`rapor/08-vps-deploy-durum.md` (erişim artık HukuDok `/hukukbot`), `rapor/PAYLASIM-NOTLARI.md` gerekiyorsa.
- **Dokunma:** `app/**`, `tests/**`, `eval/**`, `scripts/**`, `Dockerfile` (backend), `requirements.txt`

## Kabul kriterleri
- [ ] Silinecek her dosyanın backend tarafından kullanılmadığı `grep` ile kanıtlanır (ör. `metadata.json`, `types.ts` Python'dan okunuyor mu).
- [ ] `docker compose config` temiz; `docker compose build api` ve `pytest` yeşil.
- [ ] Repoda `msal`, `vite`, `VITE_` kalıntısı yok (`grep -rn` çıktısı rapora; `rapor/` altındaki tarihli cümleler hariç).
- [ ] Sunucu tarafı İNSAN ADIMI rapora yazılır: `hukukbot_frontend` konteyneri + imajı kaldırma, `/etc/nginx/sites-enabled/hukbot` silme + `nginx -t && reload`, `certbot delete --cert-name hukbot.tragic.tr`, DNS kaydı (Namecheap), Azure'daki eski hukbot uygulama kaydı (`15c7dbd8…`) silme.

## Doğrulama
- `docker compose exec -T api python -m pytest`

## Rapor
(işçi oturumu doldurur)
