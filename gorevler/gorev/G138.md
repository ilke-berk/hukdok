# G138 — Rapor ekranı: filtre şeridi, operatörsüz kontroller, gruplu alan seçici, otomatik önizleme, başlıktan sıralama

- **Bant:** frontend
- **Bağımlı:** - (backend sözleşmesi plan §4.2'de DONDU; G137 ile PARALEL)
- **Dosya kapsamı:** frontend/src/lib/reports.ts (katalog tipleri §4.2 + kontrol↔op çevirileri §4.3),
  frontend/src/components/reports/QuickFilters.tsx (YENİ), .../FilterControl.tsx (YENİ: tarih_araligi /
  coklu_secim / metin_icerir(combobox) / sayi_araligi / mantik + "boş olanlar"), .../FieldPicker.tsx (YENİ, cmdk),
  .../FilterChip.tsx (YENİ), .../FilterRow.tsx (KALDIRILIR ya da "gelişmiş" çipe indirgenir),
  .../PreviewTable.tsx (başlıktan sıralama, "güncelleniyor" durumu), .../ReportBuilder.tsx (filtre + sıralama
  bölümleri çıkar; kolon seçici G139'a kadar yerinde kalır), frontend/src/pages/ReportsPage.tsx (otomatik
  önizleme, kaynak değişiminde anında yenileme), ilgili `*.test.ts(x)`
- **Dokunma:** frontend/src/App.tsx, Sidebar.tsx, lib/api.ts (hub — DOKUNMA), AssistantPanel.tsx,
  TemplateBar.tsx, RunsTable.tsx (davranış korunur; yalnız prop uyumu gerekiyorsa)

## Test değiştirme İZNİ ve planlayıcı kararları (BLOKE çözümü, 07.09 gece)

İlk koşuda işçi haklı olarak durdu: görev, G133-G135'in testlerle kilitlediği davranışı bilinçli kaldırıyor.
**Bu görevde aşağıdaki mevcut test dosyalarını değiştirmek, beklentilerini ters çevirmek ve gerekirse
silmek AÇIKÇA İZİNLİDİR** (kullanıcının 07.09 yeniden tasarım kararı; plan §4.1):
`frontend/src/pages/ReportsPage.test.tsx`, `ReportsPage.sablon.test.tsx`, `ReportsPage.asistan.test.tsx`,
`frontend/src/components/reports/FilterRow.test.tsx` (varsa), `ReportBuilder.test.tsx` (varsa),
`PreviewTable.test.tsx` (varsa), `frontend/src/lib/reports.test.ts` (yalnız op/kontrol tablosu ve KATALOG
sahteleri), `frontend/src/lib/builderState*.test.ts` (varsa). Kilit korunacaklar: `lib/reports.export.test.ts`
(export gövdesi, K7), `lib/reportsChat.test.ts` (NDJSON sözleşmesi), RunsTable/TemplateBar testleri (davranış
aynı; yalnız prop uyumu). Raporda "değiştirilen test → gerekçe" tablosu yazılır.

Sekiz sorunun cevabı:
1. Operatör select'i ve op-listesi testleri → yeniden yazılır (kontrol bazlı testler, §4.3).
2. Bayat rozeti → beklenti ters çevrilir: rozet DOM'da YOK.
3. Önizle düğmesi/elle istek sayısı → otomatik önizleme testleriyle değiştirilir (debounce + tek istek).
4. Asistan `eylem=null` → tanım uygulanınca OTOMATİK önizlenir (plan §4.1 madde 8); `asistan.test.tsx:387-390`
   ters çevrilir.
5. Sıralama bölümü kaldırılır; başlıktan sıralama testleri yazılır.
6-7. Tek parça görev; bölünmez.
8. Katalog artık kolon başına `oplar` veriyor (registry `_kolon_katalogu`, plan §4.3 şerhi): combobox seçimi
   `eq` `oplar`da varsa `eq`, yoksa (taraf kolonları) `contains`; "…" menüsü yalnız `oplar`dakileri gösterir.
   KATALOG test sahteleri §4.2 + `oplar` alanlarıyla güncellenir (tipler zorunlu).

## Hedef

Kullanıcı bulgusu (07.09, ekran görüntüsü): filtre üç adımlı ve anlaşılmıyor, alan listesi 80+ öğeli düz
`<select>`, elle yazım hata üretiyor, tarih filtresi görünmüyor, kaynak değişince tablo eski kaynağın
satırlarını gösteriyor (bayat). Hedef ekran plan §4.1 (madde 2, 4, 5), kontrol→op eşlemesi §4.3. Sunucu
sözleşmesi (§2.1 `RaporTanimi`) DEĞİŞMEZ — bu görev sunum katmanıdır; tanım her zaman aynı JSON'a derlenir.

## Yapılacaklar

1. **`lib/reports.ts`:** `KatalogKolon` + `grup`, `kontrol`, `oneriler`, `oneri_kesik`; `VeriKaynagi` +
   `hizli_filtreler`, `kolon_setleri`. `filtredenKontrol(filtre, kolon)` (op → kontrol durumu; çözülemeyen →
   `gelismis`) ve `kontroldenFiltre(durum)` (§4.3 tablosu; "boş olanlar" → `is_null`) saf fonksiyonlar +
   birim testleri (gidiş-dönüş: her op için `kontroldenFiltre(filtredenKontrol(f)) ≡ f`).
2. **QuickFilters (önizleme üstü şerit):** kaynağın `hizli_filtreler`i sırayla, her biri `FilterControl`;
   tarih aralığında alan değiştirici (`alternatifler`) + kısayollar (bu yıl / geçen yıl / son 30 gün / son 12 ay;
   bugünün tarihi `new Date()` — test sabitler); çoklu seçimde checkbox'lı açılır + seçilen çipleri;
   metin_icerir'de `oneriler` varsa combobox (cmdk `Command`, yazdıkça daralır, seçim → `eq`, serbest yazım →
   `contains`), yoksa metin; sayı/para aralığı "en az – en çok"; mantık Evet/Hayır. "+ Başka alan" →
   **FieldPicker** (cmdk, `grup` başlıklı, aranabilir, yalnız `filtrelenebilir`, zaten şeritte olan gizli) →
   şeride kontrol olarak eklenir. Etkin filtreler çip (`FilterChip`: etiket · özet değer · × · "…" menüsünde
   gelişmiş op'lar `ne`/`not_null`/tam eşitlik). "Temizle" hepsini siler.
3. **Otomatik önizleme (`ReportsPage`):** tanım geçerli ve son önizlenenden farklıysa yapısal değişiklikte
   hemen, metin/sayı/tarih girdilerinde 600 ms gecikme (`useDebounce` mevcut) ya da odak çıkışı; `reqIdRef`
   korunur; "Önizleme bayat" rozeti ve düğmesi KALKAR; `PreviewTable` başlığında "güncelleniyor…"; hata
   durumunda "Tekrar dene". **Kaynak değişince** taslak varsayılan kolonlara döner, filtreler/sıralama sıfırlanır,
   önizleme hemen yenilenir — eski kaynağın satırları asla görünmez (test).
4. **Başlıktan sıralama:** `siralanabilir` kolon başlığı tık → artan → azalan → kaldır; ok simgesi; en fazla 3
   (dördüncüde en eski düşer); `ReportBuilder`'daki Sıralama bölümü kaldırılır.
5. **Şablon/asistan uyumu:** yüklenen tanımın filtreleri `filtredenKontrol` ile şeride çözülür; hızlı filtre
   olmayan alanlar "+ Başka alan" ile eklenmiş gibi görünür; çözülemeyen op "gelişmiş" çip. Asistanın uyguladığı
   tanım otomatik önizlenir (`eylem=null` olsa da).
6. **Testler:** QuickFilters her kontrol türü için doğru filtre JSON'u üretir (özellikle `between` iki öğe number/
   ISO, `in` dizi, `is_null` değersiz); kısayollar sabit tarihle; combobox seçim → `eq`, yazım → `contains`;
   FieldPicker gruplu ve arama; kaynak değişimi → eski satırlar temizlenir + yeni istek; debounce (sahte
   zamanlayıcı) tek istek; başlık sıralama döngüsü ve tavan 3; şablon/asistan tanımı gidiş-dönüş.

## Kabul kriterleri

- [x] Operatör açılır listesi hiçbir yerde yok; her kontrol §4.3'e göre doğru op üretir (test, kontrol başına).
- [x] Liste tipi alanlarda serbest metin girişi YOK; metin alanlarında öneri varsa combobox (test).
- [x] Kaynak değişince eski kaynağın satırları görünmez, önizleme kendiliğinden yenilenir; "bayat" rozeti
      kodda kalmadı (grep + test).
- [x] Yazarken tek istek gider (debounce testi); geçersiz tanımda istek gitmez.
- [x] Başlıktan sıralama en fazla 3 alan; `siralanabilir=false` başlık tıklanamaz (test).
- [x] Şablon ve asistan tanımları kayıpsız çözülür; çözülemeyen op gelişmiş çip olarak korunur (gidiş-dönüş testi).
- [x] Hub dosyalara dokunulmadı; yeni npm paketi yok; `npm test`, `lint`, `tsc -b --force` temiz.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

DURUM: TAMAM (işçi oturumu, 2026-09-07 gece; worktree `C:/dev/hukudok-wt/G138`, dal `gorev/G138`)

### Yapılanlar

- **`lib/reports.ts` (§4.2 + §4.3):** `KatalogKolon`'a `grup`, `kontrol`, `oplar`, `oneriler`, `oneri_kesik`;
  `VeriKaynagi`'na `hizli_filtreler` (`{alan, alternatifler}`) ve `kolon_setleri` (tipler zorunlu — test sahteleri
  buna göre güncellendi). Yeni saf katman: `KontrolDurumu` (5 kontrol + `gelismis`), `bosKontrol`,
  `kontroldenFiltre` (§4.3 tablosu: tarih/sayı iki uç `between` / tek uç `gte`/`lte`, çoklu 1→`eq` n→`in`,
  metin `contains` (combobox `tam`→`eq`), mantık `eq bool`, "boş olanlar"→`is_null` değersiz),
  `filtredenKontrol` (op→kontrol; çözülemeyen → `gelismis`, filtre kaybolmaz), `gelismisOplar` (çip "…" menüsü =
  kolonun `oplar`ı ∖ kontrolün doğal op'ları), `kontrolOzeti`, `tarihKisayolu` (bugün parametreli),
  `kolonOplari` (katalog `oplar`; boşsa tip tablosu). `tanimGecerliMi` motorla hizalandı: filtre kapısı artık
  kolon bazında `filtrelenebilir` + kolonun `oplar`ı (taraf kolonları türetilmiş AMA filtrelenebilir, `eq` yok);
  sıralama kapısı yalnız `siralanabilir`.
- **`builderState.ts`:** filtreler `FiltreSatiri[]` yerine `SeritOgesi[]` (kontrol durumu + yuva bilgisi);
  `kaynakIcinBaslangic` hızlı filtre yuvalarını boş açar (tarih yuvasında alan değiştirici seçenekleri);
  `tanimdanDurum` sunucu tanımını yuvalara/eklenen alanlara çözer — dolu öğeler TANIMDAKİ sırayla önce, boş
  yuvalar sonra (filtre sırası korunur; şablon eşitliği `JSON.stringify` ile karşılaştırıldığından şart);
  `tanimOlustur` boş kontrolleri düşürür; `siralamaDongusu` (yok→artan→azalan→kaldır, tavan 3, dördüncüde en
  eski düşer).
- **Yeni bileşenler:** `QuickFilters` (şerit: yuvalar + eklenen alanlar, etkin filtre çipleri, sayaç, "+ Başka
  alan", "Filtreleri temizle"), `FilterControl` (tarih aralığı + alan değiştirici + kısayol `<select>`; çoklu
  seçim checkbox'lı açılır; `MetinCombobox` cmdk `Command` — yazım `contains` gecikmeli, seçim `eq` yalnız
  `oplar`da varsa; sayı/para en az–en çok; mantık Hepsi/Evet/Hayır; "boş olanlar" anahtarı girdiyi kilitler;
  gelişmiş çip için yalnız değer girdisi), `FilterChip` (etiket · özet · "…" menü · ×; metinde "Tam eşitlik"
  anahtarı; gelişmiş çipte diğer izinli op'lar + "Basit kontrole dön"), `FieldPicker` (cmdk, `grup` başlıklı,
  aranabilir, yalnız `filtrelenebilir`, şerittekiler ve tarih alternatifleri gizli). cmdk için bilinçli düz
  alt-dize filtresi (varsayılan bulanık skor "kon" için `tracking_no`yu da getiriyordu). Açılırlar portal'sız
  (`useDisariTiklama`, `ui.ts`) — jsdom'da `container` içinde kalır.
- **`PreviewTable`:** `siralanabilir` başlıklar düğme (`aria-sort`, ok, çok alanlıda sıra numarası, ipucu
  döngünün sonraki adımı); "güncelleniyor…" durumu; taslak geçersizken "taslak eksik" ipucu (son geçerli
  önizleme ekranda kalır); `bayat` prop'u ve rozeti kalktı; Önizle düğmesi yok.
- **`ReportBuilder`:** yalnız kaynak seçimi + `ColumnPicker` (G139'a kadar); filtre/sıralama/Önizle/bayat çıktı.
- **`ReportsPage`:** otomatik önizleme efekti — geçerli taslak son İSTENEN'den (`sonIstenenRef`, hata dönse de)
  farklıysa yapısal değişiklikte hemen, yazarak girilen değerde `ONIZLEME_GECIKME_MS`=600 (`gecikmeliRef`,
  her tuşta yeniden başlar) ya da odak çıkışında (`hemenOnizle`); yarış koruması `reqIdRef` korunur. Tek
  durum yazma yolu `durumDegisti`: kaynak değişince cevap/hata/`sonTanim`/bekleyen zamanlayıcı ANINDA düşer,
  süren istek yok sayılır → yeni kaynağın varsayılan tanımı hemen istenir. Şablon/koşu/asistan tanımları
  `tanimdanDurum` ile şeride çözülür; asistan `eylem=null`'da da otomatik önizlenir, `eylem:"onizle"` aynı
  tanımda bile yeniden ister (`sonIstenenRef=null`). "Tekrar dene" hata anındaki tanımla.
- **Kaldırılan:** `FilterRow.tsx` (operatör seçicili satır) — gelişmiş çipe indirgenmek yerine değer girdisi
  `FilterControl.GelismisDeger`'e taşındı.

### Kararlar + gerekçeler

1. **Şerit durumu tek doğruluk kaynağı, sunucu tanımı türetilir** (`tanimOlustur`). İki yönlü senkron yerine
   tanım yalnız yüklemede (`tanimdanDurum`) çözülür; böylece "boş kontrol tanıma girmez" doğal kalır ve
   §2.1 JSON'u hiç değişmez.
2. **Tek değerli `in` → `eq` normalizasyonu** — gidiş-dönüşün bilinen tek istisnası (eş anlamlı; test belgeler).
3. **Yüklemede dolu öğeler önce** — filtre sırası korunmazsa şablonla taslak eşitliği (`exportSablonId`,
   `onizlenenSatirSayisi`) ve asistanın "aynı tanımı yeniden isteme" kapısı bozulurdu. Görsel bedel: yüklenen
   şablonda dolu filtreler şeridin başına gelir (etkin filtreler önce — kabul edilebilir).
4. **Gelişmiş çip yuvayı ezmez:** şablondaki `opening_date not_null` yuvayı boş bırakıp eklenen alan olur;
   yuva kontrolü çözülemeyen op'la kilitlenmez.
5. **"Filtreleri temizle" etiketi** — `ColumnPicker`'daki "Temizle" ile çakışmasın (G139'da kolon paneli
   taşınınca yeniden değerlendirilebilir).
6. **`useDebounce` kancası KULLANILMADI:** plan "mevcut kancayla" diyor ama yapısal/yazım ayrımı (hemen vs 600 ms)
   ve odak çıkışında anında atma değer-debounce'la kurulamıyor; efekt + ref bayrağıyla aynı sözleşme (600 ms,
   tek istek) sağlandı ve sahte zamanlayıcıyla test edildi.
7. **Geçersiz taslakta son geçerli önizleme ekranda kalır** + başlıkta "taslak eksik" ipucu; boşa düşürmek
   kullanıcıyı verisiz bırakırdı, istek zaten gitmiyor.
8. **cmdk `List` kendi `aria-label`'ını basıyor** (props'tan sonra "Suggestions") — testler `[cmdk-list]` ile
   bulur; eşleşmeyen grup DOM'dan kalkmaz `hidden` alır — başlık sayımı `:not([hidden])`.
9. **`tanimGecerliMi`'de `turetilmis` engeli kaldırıldı** — §4.2 taraf kolonları türetilmiş ama filtrelenebilir;
   motor da yalnız `filtrelenebilir`/`siralanabilir`'a bakıyor (`motor.py:136,149`). Eski davranış
   (filtrelenemeyen türetilmiş kolon reddi) `filtrelenebilir=false` üzerinden aynen korunuyor (test).

### Değiştirilen test → gerekçe

| Test | Gerekçe |
| --- | --- |
| `lib/reports.test.ts` KAYNAK sahtesi | §4.2 zorunlu alanlar (`kolon()` helper); "türetilmiş" testi `filtrelenebilir=false` üzerinden aynı, + taraf kolonu (`oplar`) kapısı |
| `ReportsPage.test.tsx` "henüz istek yok" | ters çevrildi: açılışta otomatik önizleme (tek istek, varsayılan tanım), Önizle/Filtre ekle/Sıralama ekle/Operatör YOK |
| `…` "kolon+filtre+sıralama gövdesi" | FilterRow/op-select akışı → şerit + başlık sıralaması; gövde §2.1 birebir (between dizi, in liste, number) |
| `…` "para between düzenlenmeyen uç" | sayı aralığı kontrolü `en_az/en_cok` number tutar; birim testte (`kontroldenFiltre`) |
| `…` "türetilmiş yok / op listesi §2.2" | operatör listesi kalktı; yerine `gelismisOplar` + FieldPicker "yalnız filtrelenebilir" testleri |
| `…` "is_null … Temizle → bayat rozeti" | ters: bayat rozeti DOM'da YOK; geçersiz taslakta istek gitmez + "taslak eksik" |
| `…` "sayfa değişimi son tanımla" | taslak değişimi artık yazarak (600 ms bekleyen) — sayfa 2 son önizlenen tanımla, süre dolunca taslak sayfa 1 |
| `…` "kaynak değişince…" | + eski satırlar cevap gelmeden ANINDA kaybolur (elle çözülen promise), şerit yeni kaynağın hızlı filtreleri |
| `ReportsPage.sablon.test.tsx` | sahte katalog; "Önizle" tıklamaları kalktı (otomatik); "Tanımı yükle" filtre `Şehir içerir` girdisi + çip; sıralama tanım gövdesinden doğrulanır (`siralama-satiri` yok) |
| `ReportsPage.asistan.test.tsx` | sahte katalog; `eylem=null` → otomatik önizleme (madde 4, `:387-390` ters); `onizle` → açılış + asistan = 2 istek; "manuel akış" kolon tıkıyla; bayat beklentileri ters |

Kilit korunanlar dokunulmadı: `reports.export.test.ts`, `reportsChat.test.ts`, RunsTable/TemplateBar davranışı.

### Test sonuçları

- Yeni test dosyaları: `components/reports/builderState.test.ts` (8), `QuickFilters.test.tsx` (9),
  `PreviewTable.test.tsx` (5); `lib/reports.test.ts` +34 (kontrol başına op, 22 örnekli gidiş-dönüş, kısayol).
- `npm --prefix frontend test`: **786 passed / 63 dosya** (ikinci koşu). İlk tam koşuda `lib/api.test.ts`
  "eşzamanlı iki token'sız istek tek logout" bir kez kırmızı düştü (zamanlama; dosya bu görevde dokunulmadı,
  tek başına ve ikinci tam koşuda yeşil) — NOT: hub testinde kararsızlık izi, ayrı bakılmalı.
- `npm run lint`: 0 hata / 0 uyarı. `tsc -b --force`: 0 hata.
- `grep -rn bayat frontend/src` (test dışı): reports kodunda yalnız "rozet yok" yorumları + ExportButtons
  "geçmiş bayatladı" (İndirme geçmişi, farklı kavram); `bayat-rozeti` / `bayat` prop'u yok.
- Kapsam: hub dosyalar (`App.tsx`, `Sidebar.tsx`, `lib/api.ts`), `AssistantPanel`, `TemplateBar`, `RunsTable`
  değişmedi; `package.json` değişmedi (cmdk zaten kurulu).

### İzlenecekler

- G139: `ColumnPicker` yan panele taşınınca "Temizle"/"Filtreleri temizle" etiketleri ve şeridin yerleşimi
  (kaynak kartlarıyla) yeniden ele alınır; `ReportBuilder` yalnız kaynak+kolon kaldı.
- Gelişmiş `ne`/`in` metin girdileri gecikmeli önizleme kullanır; `in` (virgülle) girdisi `defaultValue` ile
  kontrolsüz — çip menüsünden op değişince değer `degerSekleUyarla` ile uyarlanır, girdi yeniden monte olur.
- `lib/api.test.ts` "tek logout" testi tam paket altında bir kez kırmızı düştü (bu görev dışı) — kararsızlık
  tekrarlarsa `_isLoggingOut` bekçisinin zamanlayıcı bağımlılığına bakılmalı.
- Plan §4.1 madde 6 ("açılışta dolu liste") otomatik önizlemeyle fiilen sağlandı; G139 kapsamındaki kaynak
  kartları/metin sadeleştirme bu görevde YAPILMADI (kapsam dışı).
