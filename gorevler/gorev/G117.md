# G117 — Veri teslim bildiriminden sekmeye gitme + admin aktarım uçlarına uzun zaman aşımı

- **Bant:** frontend
- **Bağımlı:** -
- **Dosya kapsamı:** frontend/src/components/notifications/NotificationBell.tsx,
  frontend/src/components/notifications/NotificationBell.test.tsx, frontend/src/pages/AdminPage.tsx
  (yalnız `Tabs` başlangıç değeri + URL okuma), frontend/src/lib/api.ts (YALNIZ
  `LONG_TIMEOUT_PREFIXES` dizisine bir satır), frontend/src/lib/api.test.ts (varsa; yoksa
  `resolveTimeoutMs` için küçük test dosyası), frontend/src/components/admin/DeliveryInboxCard.tsx
  (yalnız zaman aşımı hata metni; isteğe bağlı)
- **Dokunma:** backend/**, diğer bildirim türlerinin davranışı (`case_id`'li yönlendirme ve
  "case_id'siz bildirim gezinmez" testi KORUNUR — yalnız `veri_teslim` türü istisna olur)

## Hedef

03.09 lokal testinde iki kusur çıktı:

1. **Bildirim tıklanınca hiçbir şey olmuyor.** `NotificationBell.bildirimeTiklandi` yalnız
   `case_id` dolu bildirimleri `/cases/<id>`ye götürüyor; `veri_teslim` türü bildirimler
   davaya bağlı değil (`case_id` NULL), panel açık kalıyor. Kullanıcı bildirimi görüyor ama
   ilgili ekrana (Admin > Veri Teslimleri) tek tıkla gidemiyor.
2. **"Uygula" ve "Kuru koş" 30 saniyede kesiliyor.** `api.ts` varsayılan zaman aşımı 30 sn;
   `/api/admin/aktarim/...` yolları `LONG_TIMEOUT_PREFIXES`'te yok. Gerçek uygulama 56 sn
   sürdü: istemci kesti, sunucu bitirdi, kullanıcı ikinci tıklamada 409 "Bu durumda
   uygulanamaz" gördü ve ilk denemenin başarıyla bittiğini anlamadı.

## SÖZLEŞME

- `NotificationItem` tipinde `type: string` alanı yoksa eklenir (backend zaten döndürüyor).
- `bildirimeTiklandi`: `item.type === "veri_teslim"` ise `setOpen(false)` +
  `navigate("/admin?tab=deliveries")`; `case_id` dolu diğer türler eski yol; ikisi de
  değilse eski davranış (gezinmez, panel açık kalır — mevcut test korunur).
- `AdminPage`: `useSearchParams` (react-router v7) ile `tab` parametresi okunur; geçerli bir
  sekme değeriyse (`TabsTrigger` value'larından biri) `Tabs` başlangıç değeri ve `activeTab`
  o olur; değilse `"lawyers"`. Sekme değişince URL GÜNCELLENMEZ (kapsam dar; yalnız giriş).
- `api.ts`: `LONG_TIMEOUT_PREFIXES`'e `"/api/admin/aktarim/"` (yorum: kuru koşu/uygulama
  8.409 satırda 45-60 sn; nginx 300 sn). Yükleme ucu zaten `FormData` ile uzun.
- `DeliveryInboxCard` (isteğe bağlı): `ApiTimeoutError` yakalanınca toast "İstek zaman
  aşımına uğradı; işlem sunucuda sürüyor olabilir, listeyi yenileyin" + `load()`.

## Kabul kriterleri

- [ ] `veri_teslim` bildirimi tıklanınca `navigate("/admin?tab=deliveries")` çağrılıyor ve
      panel kapanıyor (test); `case_id`'li bildirim `/cases/42`ye gidiyor (mevcut test yeşil);
      `case_id`'siz ve `veri_teslim` olmayan bildirim gezinmiyor (mevcut test yeşil).
- [ ] `AdminPage` `?tab=deliveries` ile açılınca Veri Teslimleri sekmesi aktif (test:
      `MemoryRouter initialEntries`); geçersiz `?tab=xyz` → Avukatlar.
- [ ] `resolveTimeoutMs("/api/admin/aktarim/teslimler/1/uygula")` → 300000; `"/api/admin/settings"`
      → 30000 (test).
- [ ] lint 0 hata, `tsc -b --force` temiz, mevcut testler yeşil (596+).

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## KAPSAMADIĞI doğrulama (insan adımı)

Frontend imaj rebuild (`docker compose build frontend && docker compose up -d frontend`) +
girişli tarayıcıda bildirime tıklayıp sekmenin açıldığını görmek.

## Rapor

DURUM: TAMAM (2026-09-03, işçi oturumu, worktree `gorev/G117`)

**Yapılanlar**

- `NotificationBell.bildirimeTiklandi`: `item.type === "veri_teslim"` → panel kapanır +
  `navigate("/admin?tab=deliveries")`, erken `return`. `case_id`'li türler eski yol; ikisi de
  değilse eski davranış (gezinmez). `NotificationItem.type: string` zaten vardı, tip eklenmedi.
- `AdminPage`: `useSearchParams` ile `tab` bir kez (`useState` başlatıcısında) okunur;
  `ADMIN_TABS` listesi (17 sekme, `TabsTrigger value`'larıyla birebir) içindeyse `Tabs
  defaultValue` + `activeTab` başlangıcı o olur, değilse `"lawyers"`. Sekme değişince URL
  güncellenmez (sözleşme). Yardımcı `resolveInitialAdminTab` bilinçli olarak export EDİLMEDİ
  (`react-refresh/only-export-components` lint kuralı; davranış MemoryRouter ile sınanıyor).
- `api.ts`: `LONG_TIMEOUT_PREFIXES`'e `"/api/admin/aktarim/"` (tek satır + yorum). Liste ucu
  (`/teslimler?limit=50`) da uzun katmana girer — sözleşmedeki "şüphede uzun" kararıyla uyumlu,
  tavan bekleme değil.
- `DeliveryInboxCard` (isteğe bağlı madde): kuru koş + uygula `catch`'inde zaman aşımı
  yakalanınca toast "İstek zaman aşımına uğradı; işlem sunucuda sürüyor olabilir, listeyi
  yenileyin." + `load()`. Tanıma `e.name === "ApiTimeoutError"` ile yapılır, sınıf içe
  alınmaz: kartın mevcut testi `@/lib/api`'yi yalnız `{ apiClient }` ile mock'luyor,
  `instanceof ApiTimeoutError` orada `undefined` olup TypeError atardı (mevcut testler
  değiştirilmedi).

**Testler (yeni, eski kodda kırmızı)**

- `NotificationBell.test.tsx`: "veri_teslim bildirimi davasız olsa da ... sekmesine gider" —
  `navigate("/admin?tab=deliveries")` + panel kapalı; mevcut `/cases/42` ve "case_id'siz
  gezinmez" testleri korundu.
- `src/pages/AdminPage.test.tsx` (YENİ dosya, 4 test): `?tab=deliveries` → "Veri Teslimleri"
  aktif + DeliveryInboxCard stub'ı basılı; parametresiz → Avukatlar; `?tab=xyz` → Avukatlar;
  `?tab=features` → Özellikler. `useConfig`/`usePageTitle`/kartlar/sonner mock'lu,
  `MemoryRouter initialEntries`. Tuzak notu: `useConfig` mock'u her render'da yeni `[]`
  döndürünce `useEffect(..., [lawyers])` sonsuz döngüye girip vitest'i askıda bıraktı (>10 dk,
  çıktı yok); listeler `vi.hoisted` ile sabit kimlikli yapıldı.
- `api.test.ts`: `/api/admin/aktarim/teslimler/1/uygula|kuru-kos|?limit=50` → 300000;
  `/api/admin/settings`, `/api/admin/deleted-records` → 30000.

**Doğrulama (HOST, worktree; docker compose kullanılmadı)**

- `npm --prefix frontend test`: 49 dosya, **602 passed** (önce 596+; +6 yeni).
- `npm --prefix frontend run lint`: 0 hata.
- `npx --prefix frontend tsc -b --force frontend`: temiz (exit 0).

**İzlenecekler**

- İnsan adımı: frontend imaj rebuild + girişli tarayıcıda bildirime tıklayıp Veri Teslimleri
  sekmesinin açıldığını görmek (görev dosyasındaki "KAPSAMADIĞI doğrulama").
- NOT: Sekme değiştikçe URL güncellenmiyor (bilinçli dar kapsam); tarayıcı "geri" ile
  `?tab=deliveries`e dönülürse sayfa yeniden mount olmadığı sürece sekme değişmez.
