# G159 — `tr_title` DB-008 kuralı + `sub_type` yazım farkında paket kazanır

- **Bant:** backend
- **Bağımlı:** G157 (aynı `hukdok_aktarim.py`; `_baslik_bicimli` `:616-626`, `ICERIK_KARSILASTIRMALI_ALANLAR` `:821`)
- **Dosya kapsamı:** backend/managers/reference_lists.py (`tr_title` `:49-56`, `normalize_list_name` `:59-63`),
  backend/scripts/hukdok_aktarim.py (yalnız `:821` kümesi ve `_baslik_bicimli` docstring'i),
  backend/tests/test_g159_tr_title.py (YENİ)
- **Dokunma:** backend/text_utils.py (`turkish_upper` değişmez), backend/party_check.py, backend/managers/case_matcher.py,
  routes/**, frontend/**

## Test değiştirme İZNİ (baştan)

**İZİNLİ:** `tests/test_g064_aktarim_cekirdek.py:896` ("Çocuk Sağlığı **Ve** Hastalıkları" → "… ve …"),
`tests/test_reference_lists.py`'de `tr_title`/`normalize_list_name` çıktı beklentileri (bağlaç/kısaltma/parantez
senaryoları), `tests/test_g064`'te `sub_type` "yalnız yazım farkında dokunulmaz" beklentisi. **KİLİT:**
`test_g118_muvekkil_eslesme.py`, `test_party_check.py` (anahtar normalizasyonu ayrı yol), diğerleri.

## Hedef

Plan 08.09 §5 (kullanıcı onayı 08.09) — ölçüm: kartta uzmanlık alanı 4.521 kart "… **Ve** …", müvekkil tarafı 3.209
satır "A.**ş**.", konu "(**t**ıbbi": hepsi `tr_title`'ın naif kelime-başı büyütmesinden (`reference_lists.py:49-56`);
`normalize_list_name` ve aktarımın `_baslik_bicimli`'si aynı fonksiyonu kullanır. Ekibin DB-008 kuralı: bağlaç küçük
(ve, ile), kısaltma korunur (A.Ş. · Dr. · Ltd. Şti. · T.C.), yabancı adda I/ı kuralı yok (QUICK → Quick, HDI → HDI).
Ayrıca plan §1.3 **M6** (karar 08.09): `sub_type` yazım farkında paket yazsın (`court` içerik modunda kalır).

## Yapılacaklar

1. `tr_title` yeniden: kelime sınırları boşluk **ve** `(`, `-`, `/` sonrası; bağlaç kümesi `{ve, ile, veya, adına, vb.}`
   (ilk kelime hariç küçük); kısaltma kümesi korunur (`A.Ş.`, `Ltd.`, `Şti.`, `T.C.`, `Dr.`, `Av.`, `Hem.`, `K.H.`,
   `TCK`, `HMK`, `HD`, `İDD`, `BİM`, `BAM`, `HDI`, `AXA`?) — **AXA/Quick gibi marka adlarını listeleme**, kural:
   noktalı kısaltma (`X.Y.` deseni) ve 2-3 harfli tamamı-büyük bilinen kısaltmalar korunur; başka her kelime
   Türkçe küçük + ilk harf büyük. Docstring'de DB-008 referansı ve örnek tablosu. Kelime içi `İ/ı` mevcut kural.
2. `normalize_list_name` = yeni `tr_title` (istisna yok); `_baslik_bicimli` aynı.
3. `ICERIK_KARSILASTIRMALI_ALANLAR = {"court"}` — `sub_type` çıkar; docstring gerekçesi (paket kazanır, yazım
   standardı ekipte). `court` için mahkeme adı kimliği bizim (G067-G070).
4. Testler: tablo — `"KADIN HASTALIKLARI VE DOĞUM" → "Kadın Hastalıkları ve Doğum"`, `"AK SİGORTA A.Ş." → "Ak Sigorta
   A.Ş."`, `"TAZMİNAT (TIBBİ …)" → "Tazminat (Tıbbi …)"`, `"DR ÖZEL" → "Dr Özel"`, `"QUICK SİGORTA" → "Quick Sigorta"`,
   `"YARGITAY 3. HD" → "Yargıtay 3. HD"`, `"T.C. SAĞLIK BAKANLIĞI" → "T.C. Sağlık Bakanlığı"`, `"ali VELİ" → "Ali Veli"`;
   idempotent (`tr_title(tr_title(x)) == tr_title(x)`); `sub_type` yazım farkında paket yazar, `court` dokunmaz.
5. Lokal ölçüm (raporda): 04.09 paketiyle koşu → `sub_type` YALNIZ_HARF 4.521 → 0 (ölçüm betiği: masaüstü
   `veri-ekibi-cevap-2026-09-06/yazim_farki_olc.py`, salt okunur).

## Kabul kriterleri

- [x] Test tablosu geçer, fonksiyon idempotent; `normalize_list_name` ve `_baslik_bicimli` aynı çıktı.
- [x] `sub_type` yazım farkında paket yazar; `court` içerik modunda (test).
- [x] Lokal ölçüm 4.521 → 196 raporda (196'nın tamamı kapsam dışı iki mevcut kuralın kalıntısı: 194 kardeş
      çelişkisi + 2 kök/müvekkil çelişkisi satır hatası — aşağıda); ikinci koşu 0.
- [x] ruff + mypy temiz; pytest özet satırı; değiştirilen test → gerekçe.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-10, gece kuyruğu işçisi)

### Yapılanlar

1. **`tr_title` yeniden** (`backend/managers/reference_lists.py`): kelime sınırı boşluk + `(`, `-`, `/`
   sonrası (`_SINIR_AYRACLARI`); her parça `_parca_bicimle` ile sırayla sınanır:
   - noktalı kısaltma `X.Y.` deseni (`_NOKTALI_KISALTMA`) → tamamı büyük (`a.ş.` → `A.Ş.`, `T.C.`, `K.H.`);
   - bilinen 2-3 harfli kısaltma `KISALTMALAR` → kanonik yazım (`hd` → `HD`; küme: TC AŞ HD CD İDD BİM BAM
     HDI TCK HMK CMK TBK TMK SGK — marka adı YOK: `AXA` → `Axa`, `QUICK` → `Quick`);
   - bağlaç `BAGLACLAR` = {ve, ile, veya, adına, vb} (nokta/parantez soyulmuş çekirdek) → küçük, metnin İLK
     harfli parçası hariç (`VE DİĞERLERİ` → `Ve Diğerleri`);
   - yabancı ad izi `_YABANCI_IZI` (Q/W/X ya da noktasız I'nın ünlüye komşuluğu — Türkçede "ıa/aı" yok) →
     I/ı → i (`QUICK` → `Quick`, `Quıck` → `Quick`, `ALLIANZ` → `Allianz`);
   - kalan: Türkçe küçük + ilk harf büyük (mevcut i→İ / ı→I kuralı). Harfsiz parça (`3.`, `2023/1660`) aynen.
   Docstring'de DB-008 referansı + örnek tablosu; idempotent. `normalize_list_name` = `tr_title` (istisna
   yok, docstring güncellendi). `re` importu eklendi.
2. **`sub_type` paket kazanır** (`backend/scripts/hukdok_aktarim.py`): `ICERIK_KARSILASTIRMALI_ALANLAR =
   {"court"}`; gerekçe yorumda (M6, DB-008 ekipte, `_baslik_bicimli` biçimi kurar). `_baslik_bicimli`
   docstring'i ve modül docstring'inin iki cümlesi güncellendi.
3. **Boşaltma yasağı korundu** — kapsam parantezinin ("yalnız `:821` kümesi ve docstring") iki satır dışına
   çıkıldı, gerekçesi: `BOSALTMA_DISI_ALANLAR` bu kümeden TÜRETİLİYORDU; `sub_type` çıkınca boşaltılabilir
   olacaktı ve KİLİTLİ `test_g112_duzeltme_logu.py:143` (`BOSALTMA_DISI_ALANLAR >= {…, "court", "sub_type"}`)
   kırılacaktı. Yeni sabit `BOSALTMA_YASAK_KART_ALANLARI = ICERIK_KARSILASTIRMALI_ALANLAR | {"sub_type"}`;
   `BOSALTMA_DISI_ALANLAR` artık bundan türer; `_bosaltma_talimatlari` red sebebi `sub_type` için ayrı metin
   ("uzmanlık alanı boşaltılmaz (paket yazar, silmez)") — künye/içerik metniyle karışmaz (test).
4. **Testler:** `backend/tests/test_g159_tr_title.py` (YENİ, 106 test): 34 satırlık DB-008 tablosu × (çıktı,
   idempotentlik, `normalize_list_name` = `_baslik_bicimli`), küme kilitleri, uçtan uca aktarım (D-1: kartta
   "Ortopedi Ve Travmatoloji" + paket BÜYÜK HARF → `sub_type` "Ortopedi ve Travmatoloji", `court` bizim
   yazımda; ikinci koşu 0 alan/0 tarihçe), boşaltma reddi. Fixture'lar `test_g064`'ten modül-atamasıyla
   alındı (`# noqa` yok).
   **Değiştirilen testler (izin listesindeki ikisi):** `test_g064_aktarim_cekirdek.py:258` küme beklentisi
   `{"court"}`; `:861-896` docstring + D-1 `sub_type` "Ortopedi ve Travmatoloji" (paket kazanır) + D-2
   "Çocuk Sağlığı ve Hastalıkları" (bağlaç küçük). `test_reference_lists.py`: yalnız yorum satırı (mevcut 5
   `tr_title` testi değişmeden geçiyor). Kilitli testlere dokunulmadı.

### Lokal ölçüm (04.09 paketi, `yazim_farki_olc.py` salt okunur; DB yedeği önce alındı:
`C:\hukdok-veri\yedek\pre_g159_20260910.dump`, 4,9 MB)

| Ölçüm | Önce | 1. koşu sonrası | 2. koşu |
| --- | --- | --- | --- |
| `cases.sub_type` YALNIZ_HARF (föy çifti) | **4.521** | **196** | 196 |
| `cases.sub_type` " Ve " içeren kart (deleted_at null) | 3.766 | 177 | 177 |
| aktarım "alan değişikliği" | — | 3.717 (3.592 kart; `sub_type` 3.590) | **0 (0 kart)** |
| `cases.court` YALNIZ_HARF | 407 | 407 (içerik modu, dokunulmadı) | 407 |

Kalan 196 çiftin tamamı kapsam DIŞI iki mevcut kuralın kalıntısı, G159 kuralı değil:
- **194 çift / 115 kart:** kardeş föyler farklı uzmanlık bildiriyor (`id-14227` Perinatoloji ↔ `id-14224`
  Kadın Hastalıkları) → `celiskili_alanlar` kart alanına yazmaz (kardeş çelişkisi raporunda `sub_type` 247
  satır). Yazım kalıntısı çelişki çözülünce (ekip cevabı / G157 raporu) kendiliğinden kapanır.
- **2 çift (H-5628, H-1592):** satır hatası `kök/müvekkil çelişkisi` (G153 kuralı; 7 satırdan ikisi) — satır
  bütünüyle yazılmaz.
- Kalan 177 kartın 62'si föysüz (pakette yok; aktarım dokunamaz) — bunlar ölçüm sayısına girmiyor.
Aktarımın 1. koşusundaki `sub_type` dışı değişiklikler (ISTINAF `basvuru_tarihi` 267, `case_party_id` 8.385
bağ, 9 yeni föy…) G153-G156'nın lokal DB'ye henüz uygulanmamış etkileridir; G159 ile ilgisi yok.

### Kararlar

- **Yabancı ad tespiti sezgisel, sözlüksüz:** Q/W/X + I-ünlü komşuluğu. `GENERALI`/`ZURICH`/`UNICO` gibi
  izsiz adlar Türkçe kuralla kalır ("Generalı") — bugünkü davranışın aynısı, kayıp yok; docstring'de sınır
  yazılı. Marka listesi tutulmadı (görev kuralı).
- **Kısaltma kümesi bilinçli küçük** (görev listesi + T.C./A.Ş.'nin noktasız hâli + 5 kanun kısaltması);
  `AK` listede değil → "Ak Sigorta" (görev tablosu). Genişletme ölçümle.
- **Boşaltma yasağı `sub_type`ta sürüyor** (madde 3): görev bunu söylemiyor ama kilitli test söylüyor;
  "paket yazar, silmez" tutarlı bir kural.
- **`sub_type` ikizleri DB'de kalıyor** (`Kadın Hastalıkları Ve Doğum` 53 ↔ `ve` 1.455): aktarım föylü
  kartı yazar, föysüz/çelişkili kartı yazmaz; toplu düzeltme ayrı bir betiğin işi (aşağıda).

### Test sonuçları (konteynerde)

- `python -m pytest`: **3200 passed, 3 skipped, 0 failed** (106'sı yeni dosyadan). Hedefli 7 dosya: 325 passed.
- `ruff check .`: All checks passed. `mypy`: Success, 62 source files.

### İzlenecekler / NOT

- NOT (kapsam dışı): `cases.subject` 94 kartta "(tıbbi", 3 kartta "(tck 89)" — eski `tr_title`ın liste
  adından yayılan kalıntısı; `case_subjects`/`specialties` liste tablolarında da "… Ve …" yazımlar duruyor.
  `scripts/normalize_list_names.py` (dry-run varsayılan) artık DB-008 kuralını uygular → `--apply` ile liste
  adları; kart kolonları (`subject`/`sub_type` föysüz 62) için ayrı tek seferlik backfill gerekir (yeni görev).
- NOT: `case_parties.name` "A.ş." 3.783+1.381 satır (Axa/Ak) — taraf adı `tr_title`tan geçmiyor (party_check
  kapsam dışı); DB-008 uyumu ayrı karar.
- NOT: çalışma ağacında kapsam dışı kirli dosyalar vardı (`.claude/settings.local.json` M, `.claude/launch.json`
  ??) — koşucu/harness dosyaları, dokunulmadı, commit'e alınmadı.
- Konteyner `/tmp`'de koşu kalıntıları (`HUKDOK_TESLIM_PAKETI_2026-09-04.xlsx`, `paket_20260904.xlsx`,
  `yazim_farki_olc.py`, `g159-rapor-1/2`, `yazim_farki/`) — recreate'te uçar.
- Prod'a UYGULANMADI (deploy insan kararı); prod'da aynı 04.09 paketiyle koşu 4.521 kartı yazacak.
