# G206 — Rota + menü: Hukukbot iç sayfaya döner, dış link kalkar

- **Bant:** frontend
- **Bağımlı:** G205
- **Dosya kapsamı:** `frontend/src/App.tsx` (yalnız `/hukukbot` rotası, lazy), `frontend/src/components/shell/Sidebar.tsx` + `Sidebar.test.tsx`, `frontend/src/lib/hukukbot.ts` (SİLİNİR) + varsa testi, `.env.example` (`VITE_HUKUKBOT_URL` bloğu silinir), `frontend/src/vite-env.d.ts` (tanım varsa)
- **Dokunma:** `frontend/src/pages/HukukbotPage.tsx` ve `components/hukukbot/**` (G205), `nginx.conf`

## Hedef
Menüdeki "Araçlar → Hukukbot" yeni sekmede `hukbot.tragic.tr`'yi açmak yerine HukuDok içindeki `/hukukbot`
sayfasına gider. Dış adres ayarı (`VITE_HUKUKBOT_URL`, `HUKUKBOT_VARSAYILAN_URL`) tamamen kalkar.

## Kabul kriterleri
- [ ] `/hukukbot` korumalı rotalar altında (diğer sayfalarla aynı `ProtectedRoute`/kabuk), lazy yüklenir (G182 kod bölme deseni).
- [ ] Sidebar'da Hukukbot iç `NavLink`: `target="_blank"`/`rel` yok, aktif durum vurgusu diğer öğelerle aynı; "yeni sekmede açılır" title'ı kalkar.
- [ ] `grep -rn "hukbot.tragic.tr\|VITE_HUKUKBOT_URL\|HUKUKBOT_URL" frontend/src .env.example` boş.
- [ ] Sidebar testi: link `/hukukbot`'a gider, dış adres içermez.
- [ ] Rolü olan her kullanıcı görür (admin kısıtı YOK — kullanıcı aksini söylemedi).

## Doğrulama
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`
- `npm --prefix frontend run build`

## Rapor
(işçi oturumu doldurur)
