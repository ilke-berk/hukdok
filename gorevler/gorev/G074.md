# G074 — Takip panelinde aşama zaman çizgisi + kart gruplarının sadeleşmesi

- **Bant:** frontend
- **Bağımlı:** G072 (okuma route'u), G073 (alan yerleşimi)
- **Dosya kapsamı:** frontend/src/components/CaseTrackingPanel.tsx,
  frontend/src/lib/trackingDraft.ts (yalnız `STAGE_FIELDS`e taşınan alanlar),
  frontend/src/lib/caseCardFields.ts (`PROCESS_CARD_FIELDS` / `OFFICE_CARD_FIELDS`
  sadeleşmesi), frontend/src/pages/CaseDetails.tsx (grup render'ı),
  ilgili `*.test.ts(x)` dosyaları
- **Dokunma:** takip panelinin YAZMA akışı (taslak/dirty/beforeunload mantığı,
  Faz 4-C) — zaman çizgisi **salt okunur** bir katmandır, kaydetme akışına
  karışmaz. `TransferFieldsCard` deseni korunur (boş alan satırı basılmaz,
  grubun tamamı boşsa kart doğmaz).

## Hedef

İki iş bir arada, ikisi de aynı ilkenin sonucu — **bir kavram tek ekranda**:

**1. Aşama tarihçesi görünür olsun.** 2026-08-19 aktarımı `case_stage_decisions`e
4.971 satır yazdı (YEREL 3.098 · İSTİNAF 1.236 · TEMYİZ 574 · K.Düzeltme 63) ve
hiçbiri ekranda yok. Panel bugün yalnız **son aşamanın fotoğrafını** gösteriyor
(`cases` tek-slot kolonları). Kanıt vakası: id-2271 — Danıştay 2023 Bozma + 2026
Onama; panelde yalnız Onama görünüyor.

**2. Karttan takibe taşınan alanlar karttan çıksın.** G073 `arabuluculuk_no`,
`arabuluculuk_karar_tarihi`, `arsiv_tarihi` alanlarını takip tarafına aldı;
`dosya_son_durumu` zaten takipteydi ve 19.08'de karta da eklenmişti. Aynı değeri
iki ekranda göstermek "hangisi doğru?" sorusunu doğurur.

## Karar noktaları (işçi karar verir ve GEREKÇELENDİRİR)

1. **Zaman çizgisi nerede durur?** Öneri: seçili aşamanın alan formunun ALTINDA,
   "Bu aşamanın geçmiş kararları" başlığıyla — aşama sekmeleri zaten
   YEREL/İSTİNAF/TEMYİZ/K.DÜZELTME; her sekme kendi satırlarını gösterir.
   Alternatif (tek birleşik dikey çizgi) daha gösterişli ama sekme seçimiyle
   çelişir; seçersen gerekçelendir.
2. **`dogrulama_durumu` nasıl görünür?** 1.367 satır `BELIRSIZ`, 3.604 satır
   `TURETILDI` — hiçbiri UYAP/BELGE değil. Öneri: rozet
   (`BELIRSIZ` amber, `TURETILDI` nötr). Kullanıcı "bu bilgi nereden geldi"yi
   görmeden karara güvenmemeli; tahmin yasağının ekran karşılığı budur.
3. **Satır sayısı çok olan aşama.** Bir aşamada 2-3 satır olağan (bozma sonrası
   yeniden yargılama). Öneri: hepsi basılır, kısaltma YOK — 4.971 satırın kart
   başına ortalaması 1'in biraz üstünde, gizlemeye değecek hacim yok.
4. **Kart grubu boşalırsa?** `PROCESS_CARD_FIELDS`ten üç alan çıkınca grupta
   yalnız ne kalıyor kontrol et; grup boşalıyorsa kartı tamamen kaldır
   (`hasAnyValue` zaten boş grubu basmıyor ama ölü tanım bırakma).

## Kabul kriterleri

- [ ] Takip panelinde seçili aşamanın geçmiş kararları listeleniyor: mahkeme,
      esas/karar no, karar tarihi, karar durumu, tebliğ tarihi, başvuran taraf
- [ ] Satırlar `sira_no` sırasında; her satırda `dogrulama_durumu` rozeti var
- [ ] Tarihçesi olmayan aşamada liste hiç basılmıyor (boş kutu gürültüsü yok)
- [ ] `arabuluculuk_no`, `arabuluculuk_karar_tarihi`, `arsiv_tarihi`,
      `dosya_son_durumu` **kart gruplarında YOK**; takip panelinde var
- [ ] Aynı alan iki ekranda görünmüyor — testte kilitli (kart alan listeleri ile
      `TRACKING_DRAFT_KEYS` kesişimi BOŞ)
- [ ] `npm --prefix frontend test` + `lint` + `build` temiz; mevcut takip paneli
      testleri (taslak/dirty davranışı) DEĞİŞTİRİLMEDEN yeşil

## Doğrulama

```bash
npm --prefix frontend test
npm --prefix frontend run lint
npm --prefix frontend run build
```

Ek olarak lokal stack'te gözle: aktarım koşulmuş bir kartta (YEREL + İSTİNAF
satırı olan bir dava) takip paneli açılır, iki aşamanın da geçmiş kararı görünür;
kartta arabuluculuk/arşiv alanlarının artık basılmadığı doğrulanır.

## Rapor

**Durum: TAMAM** (2026-08-19, lokal). Kapılar: `npm test` **365 passed** (G074
öncesi 343 → +22), `npm run lint` 0 hata (22 mevcut uyarı, değişmedi),
`npm run build` temiz, `npx tsc -b --force` (G037'nin GERÇEK kapısı) temiz.

### Tasarımı belirleyen ölçüm: `case_stage` neredeyse her kartta BOŞ

Lokal kopyada (aktarım sonrası): **14.345 aktif kartın 14.344'ünde `case_stage`
NULL** (tek dolu kart ISTINAF). Panelin alan formu `isReached = selectedIdx <=
currentIdx` kapısından geçiyor ve `case_stage` boşken `currentIdx = -1` —
yani bugün **hiçbir kartta hiçbir aşama alanı düzenlenebilir değil**. Bu ölçüm
iki kararı doğrudan belirledi:

* **Aşama tarihçesi `isReached` kapısının DIŞINDA** basılır. Kapıya
  bağlansaydı aktarımın yazdığı 4.971 satırın hiçbiri ekrana gelmezdi —
  görevin varlık sebebini yok ederdi. Tarihçe zaten SALT OKUNUR; "aşamaya
  gelinmiş mi" sorusu okumayı ilgilendirmiyor.
* **G073'ün üç alanı aşama sekmesine GÖMÜLMEDİ.** `STAGE_FIELDS`e konsalardı
  3.346 dolu `arsiv_tarihi`nin hiçbiri düzenlenemezdi. `PANEL_FIELDS` adında
  ayrı bir küme açıldı: `dosya_son_durumu` ile aynı yerde, her zaman görünür.

### Karar noktalarının cevabı

1. **Zaman çizgisi nerede?** Öneri kabul: seçili aşamanın alan formunun
   ALTINDA, "Bu aşamanın geçmiş kararları (N)" başlığıyla. Sekmeli görünümün
   bedeli var — kullanıcı diğer aşamada karar olduğunu göremezdi; bu yüzden
   zaman çizelgesindeki her daireye **kayıtlı karar sayısı rozeti** eklendi.
   Böylece sekme seçimiyle çelişmeyen ama hiçbir satırı gizlemeyen bir görünüm
   çıktı.
2. **`dogrulama_durumu` rozeti** eklendi: BELIRSIZ amber, TURETILDI nötr,
   UYAP/BELGE yeşil. Ölçüm rozetin neden gerektiğini gösteriyor: 3.604
   TURETILDI + 1.367 BELIRSIZ, **hiçbiri UYAP/BELGE değil**.
3. **Kısaltma YOK** — bir aşamanın bütün satırları basılır (kart başına
   ortalama 1,50; en zengin kart 4 satır — gizlemeye değecek hacim yok).
4. **Kart grubu boşalmadı:** `PROCESS_CARD_FIELDS`te `istinaf_basvuran_taraf`
   kaldı (253 kartta dolu), grup "Kanun Yolu Bilgileri" olarak yeniden
   adlandırıldı. Kalma gerekçesi kabul kriteriyle uyumlu: bu alan takip
   panelinin YAZMA yolunda değil (aşama fotoğrafının hedefi, tek yazıcısı
   `stage_decisions`) — "hangisinden düzeltirim" belirsizliği doğurmuyor.
   Kesişim testi bunu mekanik olarak da doğruluyor.

### Yapılanlar

* **`useCases.getCaseStageDecisions`** — G072 route'unun istemcisi + üç tip
  (`CaseStageDecision`, `CaseEsasNumberEntry`, `CaseStageDecisions`).
  **G002 disiplini:** hata YUTMAZ, `CASE_STAGE_DECISIONS_ERROR` fırlatır.
  Sessiz `[]` "bu davada geçmiş karar yok" diye okunurdu — oysa aktarım 4.971
  satır yazdı; panel "tarihçe yok" ile "tarihçe alınamadı"yı ayırıyor.
* **`trackingDraft.ts`** — `PANEL_FIELDS` (3 alan) + `TRACKING_DRAFT_KEYS`e
  dahil + `DECISION_STAGE_BY_PANEL_KEY` (panelin `KARAR`ı backend'in `YEREL`i;
  `KESINLESME`/`KAPALI` haritada YOK — karar aşaması değiller).
* **`CaseTrackingPanel.tsx`** — tarihçe okuması (salt okunur, taslağa
  dokunmaz), aşama başına geçmiş karar listesi, damga rozeti, zaman
  çizelgesinde karar sayısı rozeti, önceki esas numaraları bloğu, üç yeni
  alanın girişi. Alan bloğu render'ı `renderField`e çıkarıldı — aşama alanları
  ile aşamadan bağımsız alanlar aynı kontrolü kullansın diye (iki kopya iki
  davranış olurdu); aşama alanlarının markup'ı BİREBİR aynı kaldı.
* **Kart sadeleşmesi** — `PROCESS_CARD_FIELDS`ten üç alan, `OFFICE_CARD_FIELDS`ten
  `dosya_son_durumu` çıktı; `CaseDetails.tsx`te iki grup başlığı/açıklaması
  güncellendi.
* **Önceki esas numaraları** (G072 yanıtının ikinci alanı, 517 satır) "Davanın
  Son Durumu" kartında ayrı blokta gösteriliyor. Kabul kriterlerinde yoktu ama
  aynı ilkenin kapsamında: aktarılan veri görünmüyorsa doğrulanamaz.

### Testler (+22)

`CaseTrackingPanel.test.tsx` (+11): künye alanlarının hepsi basılıyor; damga
rozeti; id-2271 iki karar + `sira_no` sırası; **`case_stage` boşken tarihçe
görünüyor** (ölçümün testi); satırı olmayan aşamada kutu hiç basılmıyor;
okuma hatası uyarı basıyor ve "tarihçe yok"a çevrilmiyor; önceki esas bloğu
var/yok; üç alan `case_stage` boşken düzenlenebiliyor; aktarım değeri alana
basılıyor; değişiklik PATCH'e giriyor.
`trackingDraft.test.ts` (+7): üç alan taslakta, hiçbir aşamaya gömülmemiş,
dirty/patch akışı, boşaltma → null, aşama etiketi eşlemesi.
`caseCardFields.test.ts` (+4): üç alan + `dosya_son_durumu` kart
listelerinden çıktı; **kart listeleri × `TRACKING_DRAFT_KEYS` kesişimi BOŞ**
(kabul kriterinin mekanik kilidi); kanun yolu grubu boşalmadı.

Mevcut panel testleri (G061 taslak/dirty davranışı) DEĞİŞTİRİLMEDİ — tek
dokunuş `useCases` mock'una yeni fonksiyonun eklenmesi ve `beforeEach`e
varsayılan boş yanıt (panel artık açılışta tarihçe okuyor); hiçbir iddia
değişmedi (`git show` diff'inde görülür).

### Doğrulama

`GET /api/cases/{id}/stage-decisions` lokal stack'te canlı: konteyner nginx
üzerinden **401** (kimlik gerekiyor), olmayan bir yol **404** — route kayıtlı.
Backend `restart` edildi (kaynak mount'lu ama uvicorn reload'suz), frontend
imajı **yeniden build** edildi (nginx imajdaki dist'i servis eder — bayat
`dist/` tuzağı). `/healthz` = ok.

**Gözle bakma kullanıcıya kaldı:** panelin kendisi Azure AD girişi arkasında;
ajan oturumu kimlik bilgisi giremez. Ekranda beklenen: `case 1103` (dört
aşama) ve `case 8` (üç aşama + önceki esas 2013/1136).

### İzlenecekler

* **833 satırda `aciklama = "havuz dışı durum: None"`** (G072'de bulundu):
  artık ekranda GÖRÜNÜR. Bu bir aktarım kusurudur (kaynak durum boşken
  f-string Python'ın `None`'unu metne basmış); UI'da gizlemek veriyi saklamak
  olurdu — düzeltme yeri `hukdok_aktarim`. Ayrı görev/karar.
* **Arabuluculuk alanları pratikte boş** (402 kart `file_type=Arabuluculuk`,
  yalnız 1 kartta `arabuluculuk_no`): alanlar panele eklendi ve düzenlenebilir,
  ama kullanıcı bugün neredeyse hep boş görecek. Sebep aktarım tarafında
  (G073 raporu).
* Panelin alan formu `case_stage` boş olduğu için 14.344 kartta hâlâ
  düzenlenemez durumda. Bu G074'ün kapsamı DEĞİL (aşama geçişi ayrı yol,
  kullanıcı "Bu Aşamaya Geç" ile açıyor) ama takip panelinin gerçek
  kullanılabilirliği buna bağlı — ayrı bir karar hak ediyor.
