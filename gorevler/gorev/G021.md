# G021 — Ölü npm bağımlılıklarını kaldır (2 high advisory'yi de düşürür)

- **Bant:** frontend
- **Bağımlı:** -
- **Dosya kapsamı:** frontend/package.json, frontend/package-lock.json
- **Dokunma:** frontend/src/** (bu görev kaynak koda DOKUNMAZ — dokunması gerekiyorsa paket ölü değildir, BLOKE bırak), frontend/vite.config.ts, frontend/vitest.config.ts

## Hedef

Plan FAZ A.5 + C. Dört paket `dependencies` altında duruyor ama `frontend/src` içinde
**tek bir import'u yok** (iki bağımsız ajan doğruladı: `from "docx"`, `from "mammoth"`,
`from "file-saver"`, `from "get-port"` ve `require(...)` biçimleri → sıfır sonuç;
`saveAs` sembolü de hiç geçmiyor):

| Paket | package.json | Not |
| --- | --- | --- |
| `docx` ^9.5.1 | `:54` | `jszip` (high advisory) bunun üzerinden geliyor |
| `file-saver` ^2.0.5 | `:56` | — |
| `get-port` ^7.1.0 | `:57` | Node/CLI paketi, tarayıcı bundle'ında işi yok |
| `mammoth` ^1.11.0 | `:60` | `@xmldom/xmldom` + `underscore` (**iki high advisory**) bunun üzerinden geliyor |

Bulunan tüm "docx" eşleşmeleri yalnızca **string sabiti** (MIME/uzantı listeleri, UI metinleri):
`lib/caseIntake.ts:169`, `lib/fileValidation.ts:23`, `pages/NewClient.tsx:306`,
`pages/Index.tsx:1181`, `pages/dashboards/IdariDashboard.tsx:50`, `lib/todayUploads.ts:14`.
Bunlar dosya uzantısı metinleridir, paket kullanımı değil — **dokunulmayacak**.
Ayrıca `lib/documentUtils.ts:93-95` DOCX için "henüz destek eklenmedi" diye throw ediyor.

Çalışma zamanı etkisi yok (tree-shaking doğrulandı: servis edilen bundle'da bu paketlerin
imzası sıfır). Kazanç: **tedarik zinciri yüzeyi + `npm install`/CI süresi + 2 high advisory**.

## Kabul kriterleri

- [ ] Dört paket `frontend/package.json` `dependencies`'ten kaldırıldı
- [ ] `package-lock.json` güncellendi (`npm install` ile, elle düzenlenmedi)
- [ ] `frontend/src` altında **hiçbir kaynak dosyası değişmedi** (`git diff --stat` ile kanıtlı)
- [ ] `npm --prefix frontend run build` başarılı; bundle boyutu önce/sonra raporda
- [ ] Mevcut 299 test yeşil (23 dosya)
- [ ] `npx tsc --noEmit` temiz
- [ ] `npm audit` önce/sonra raporlandı; `@xmldom/xmldom` ve `underscore` advisory'lerinin
      düşüp düşmediği yazılı (bugünkü taban: 22 açık, 15 high)
- [ ] Uzantı/MIME string sabitlerine dokunulmadı (yukarıdaki 6 dosya)

## Doğrulama

- `npm --prefix frontend install`
- `npm --prefix frontend test`
- `npm --prefix frontend run build`
- `npm --prefix frontend audit`

## Rapor

(işçi oturumu doldurur)
