# G026 — Ölü `ui/sidebar.tsx` ve kaskadını sil

- **Bant:** frontend
- **Bağımlı:** -
- **Dosya kapsamı:** frontend/src/components/ui/** (yalnız silinecekler), frontend/package.json (yalnız bu silmeyle sahipsiz kalan bağımlılık varsa)
- **Dokunma:** frontend/src/components/shell/** (gerçek kenar çubuğu ORADA — `shell/Sidebar.tsx` CANLI, karıştırma), frontend/src/pages/**, frontend/src/hooks/**, frontend/src/lib/**

## Hedef

Plan FAZ C. `frontend/src/components/ui/sidebar.tsx` **637 satır** ve repodaki en büyük ölü
frontend dosyası. Bağımsız iki tarama doğruladı: `ui/sidebar"` deseni `frontend/src` içinde
`ui/` dışından **0 kez** geçiyor.

**Neden özellikle bu dosya öncelikli:** tek başına beş dosyayı (`use-mobile.tsx`, `form.tsx`,
`sheet.tsx`, `separator.tsx`, `toggle.tsx`) "kullanılıyor" gibi gösteren düğüm. O yüzden
**silme sırası: önce `sidebar.tsx`, sonra kaskad** — tersi yapılırsa kaskad dosyaları
hâlâ canlı görünür.

**Karışıklık uyarısı:** uygulamanın gerçek kenar çubuğu `frontend/src/components/shell/Sidebar.tsx`
(ayrı dosya, CANLI, `App.tsx` üzerinden). Aynı isimli iki dosyanın biri ölü biri canlı —
bu, kod okuyanı doğrudan yanıltan durumun ta kendisi ve silmenin asıl gerekçesi.

> **PAZARLIKSIZ KURAL (G015/FAZ C dersi):** hiçbir sembol, `getattr`/string-dispatch/dinamik
> import taraması yapılmadan ölü sayılmaz. Bu görevde her silme adayı **yeniden** doğrulanır —
> bu görev dosyasındaki listeye güvenme, kendin tara. Gece boyunca frontend değişti
> (G019/G020/G021), envanter kaymış olabilir.

## Kabul kriterleri

- [ ] **Beklenen silme beyanı:** işe başlamadan önce silinecek dosyalar ve satır sayıları
      raporda listelenir; sonda gerçekleşen sayı ile karşılaştırılır
- [ ] `ui/sidebar.tsx` silindi (**637 satır**)
- [ ] Kaskad dosyaları **yeniden taranarak** silindi: `sidebar.tsx` gittikten sonra
      gerçekten hiçbir yerden import edilmeyenler. Her biri için kanıt (import araması)
      raporda; hâlâ kullanılan varsa BIRAKILIR ve gerekçesi yazılır
- [ ] `components/shell/Sidebar.tsx` ve tüketicileri **DEĞİŞMEDİ** (`git diff` ile kanıtlı)
- [ ] **Net satır deltası NEGATİF** (bu bir silme görevi)
- [ ] `npm --prefix frontend run build` başarılı — kırık import yok
- [ ] `npx tsc --noEmit` temiz
- [ ] Mevcut **317 test** yeşil, taban altına düşmek yasak. Test sayısı düşerse: hangi test,
      neden, beyan edilmiş miydi — raporda
- [ ] `npm --prefix frontend run lint` yeni HATA üretmiyor
- [ ] Silinen dosyaların **tek** tüketicisi olduğu için sahipsiz kalan npm bağımlılığı varsa
      raporda listelendi (silme bu görevde YAPILMAZ, G027'ye bırakılır)

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run build`
- `npx --prefix frontend tsc --noEmit`
- `npm --prefix frontend run lint`

## Rapor

### Beklenen silme beyanı (işe başlamadan önce)

| Dosya | Satır | Gerekçe |
| --- | ---: | --- |
| `frontend/src/components/ui/sidebar.tsx` | 637 | hiçbir tüketicisi yok |
| `frontend/src/components/ui/sheet.tsx` | 107 | tek tüketicisi `sidebar.tsx` |
| `frontend/src/components/ui/separator.tsx` | 20 | tek tüketicisi `sidebar.tsx` |
| **Toplam** | **764** | |

**Gerçekleşen: birebir aynı** — `git diff --stat` → `3 files changed, 764 deletions(-)`.
Net satır deltası **-764** (negatif ✓).

### Yeniden tarama kanıtı (görev dosyasındaki listeye güvenilmedi)

Silme adayları görev dosyasından DEĞİL, kendi taramamdan çıktı:

- **`ui/sidebar.tsx` ölü:** `ui/sidebar` deseni `frontend` ağacında **0 eşleşme**
  (ne alias ne göreli import). `Sidebar` kelimesinin geçtiği tüm yerler
  `components/shell/Sidebar.tsx` ya da yorum satırı.
- **Kaskad, sidebar'ın kendi import ağacından türetildi** (`sidebar.tsx:6-13`):
  `use-mobile`, `lib/utils`, `button`, `input`, `separator`, `sheet`, `skeleton`, `tooltip`.
  Her biri için tüm `frontend/src` genelinde tüketici sayıldı:

| Aday | sidebar dışı tüketici | Karar |
| --- | --- | --- |
| `separator.tsx` | yok | **SİLİNDİ** |
| `sheet.tsx` | yok | **SİLİNDİ** |
| `button.tsx` | 15 dosya (AdminPage, NewCase, EmailModal, …) | BIRAKILDI |
| `input.tsx` | 12+ dosya | BIRAKILDI |
| `skeleton.tsx` | CaseGroup, CaseDetails, UnlinkedDocuments, RelatedCasesPanel | BIRAKILDI |
| `tooltip.tsx` | `App.tsx:3` | BIRAKILDI |
| `hooks/use-mobile.tsx` | yok (sidebar TEK tüketicisiydi) | **Dokunma listesinde** → bırakıldı, aşağıda NOT |

Silinen iki dosya da yaprak: `sheet.tsx`/`separator.tsx` yalnız npm paketleri ve
`lib/utils` import eder, başka bir ui bileşenini sahipsiz bırakmaz.

### Alınan kararlar + gerekçeler

1. **`hooks/use-mobile.tsx` silinmedi.** Tarama bunun da ölüye düştüğünü gösteriyor
   (`useIsMobile`'ın tek çağıranı `sidebar.tsx:6`'ydı), ama `frontend/src/hooks/**`
   bu görevin **Dokunma** listesinde. Kural gereği kapsam dışına çıkmadım; G027'ye NOT.
2. **Görev dosyasının kaskad listesi kısmen yanlış çıktı.** Dosya `form.tsx` ve
   `toggle.tsx`'i sidebar'ın propladığı dosyalar arasında sayıyordu; `sidebar.tsx`
   bu ikisinin **hiçbirini import etmiyor**. Her ikisi de sidebar'dan BAĞIMSIZ olarak
   zaten ölü (`form.tsx`'in hiç tüketicisi yok; `toggle.tsx`'i yalnız tüketicisi
   olmayan `toggle-group.tsx` çağırıyor). Bunlar "sidebar kaskadı" olmadığı ve
   KUYRUK'ta **G027 = "kalan ölü ui/ bileşenleri"** açıkça bu işi üstlendiği için
   burada silinmedi — başka görevin dosyalarına girmemek adına.
3. **`npx tsc --noEmit` doğrulaması düzeltildi.** `frontend/tsconfig.json`
   solution-style (`"files": []` + `references`); ona karşı `--noEmit` koşmak
   **hiçbir dosyayı denetlemeden sessizce geçer**. Gerçek denetim
   `-p frontend/tsconfig.app.json` ile yapıldı.

### Test sonuçları

| Kapı | Sonuç |
| --- | --- |
| `npm --prefix frontend test` | **317 passed (25 dosya)** — taban 317 ile birebir, düşüş yok |
| `npm --prefix frontend run build` | **başarılı** (`built in 5.44s`) — kırık import yok |
| `npx tsc --noEmit -p tsconfig.app.json` | **4 hata — hepsi ÖNCEDEN VAR** (aşağıda kanıt) |
| `npm --prefix frontend run lint` | **0 error**, 25 warning — yeni hata yok |

**tsc hatalarının önceden var olduğu kanıtlandı:** üç dosya `git show HEAD:<yol>` ile
geçici geri konup tsc tekrar koşuldu; **birebir aynı 4 hata** listelendi. Yani silme
sıfır yeni tip hatası ekliyor. Hatalar dokunulmayan sayfalarda:
`AdminPage.tsx:427`, `CaseDetails.tsx:561`, `AvukatDashboard.tsx:120`,
`IdariDashboard.tsx:99` (hepsi `TS2352`/`TS2322` tip dönüşümü). Ölçüm sonrası dosyalar
tekrar silindi; nihai `git status` saf üç silmedir.

**`components/shell/Sidebar.tsx` değişmedi:** `git status --porcelain` çıktısı yalnız
üç `D` satırı içeriyor — canlı kenar çubuğuna ve tüketicilerine dokunulmadı.

### İzlenecekler

- **NOT (G027):** `hooks/use-mobile.tsx` (11 satır) artık tümüyle ölü — tek tüketicisi
  silinen `ui/sidebar.tsx`'ti. Dokunma listesi yüzünden burada silinemedi.
- **NOT (G027 — sahipsiz npm):** `@radix-ui/react-separator` (`package.json:39`) artık
  hiçbir yerden kullanılmıyor; tek tüketicisi silinen `separator.tsx`'ti. Kabul
  kriteri gereği silme bu görevde YAPILMADI.
  Buna karşılık **`@radix-ui/react-dialog` sahipsiz DEĞİL** — `dialog.tsx` ve
  `command.tsx` hâlâ kullanıyor, kaldırılmamalı.
- **NOT (kapsam dışı bulgu):** `frontend/tsconfig.json`'a karşı koşulan
  `npx tsc --noEmit` yanlış güvence veriyor (hiçbir dosyayı denetlemiyor). Başka
  görev dosyalarındaki doğrulama komutları da bu biçimdeyse aynı boşluğu taşır;
  `-p tsconfig.app.json` kullanılmalı.
- **NOT:** Mevcut 4 tip hatası main dalında canlı; bu görevin işi değil ama bir
  temizlik görevine değer.

**DURUM: TAMAM**
