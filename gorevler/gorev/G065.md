# G065 — yerel_karar_durumu okuma/yazma yolu: get_case serialize + takip whitelist

- **Bant:** backend
- **Bağımlı:** G060 (kolon + resmi liste oradan geldi; kolon prod-öncesi lokalde migrasyonlu).
  G062 ile DOSYA ÇAKIŞMASI YOK — G062 yeni modül `managers/stage_decisions.py`'de çalışır,
  `case_manager.py`'ye dokunmaz (G062 dosya kapsamı, 2026-08-18 doğrulaması).
- **Dosya kapsamı:** backend/managers/case_manager.py,
  backend/tests/test_g065_yerel_karar_yolu.py
- **Dokunma:** backend/models.py, backend/database.py, backend/schemas.py (üçü de G060'ta
  bitti — `CaseRead`/`CaseTrackingUpdate` alanı ZATEN taşıyor, şemaya iş yok);
  backend/managers/stage_decisions.py (G062'nin dosyası); routes/**; frontend/** (G061 bitti).

## Hedef

G060 kolonu + resmi listeyi açtı, G061 UI dropdown'ını bağladı; ama alanın backend
okuma/yazma yolu kopuk (işçi+denetçi ortak bulgusu — G061 raporu; G060 raporunun
"İzlenecekler" notu aynı iki noktayı önceden işaretlemişti):

1. **Serialize:** dava serialize sözlüğü `istinaf_karar_durumu`nu içeriyor ama
   `yerel_karar_durumu`nu İÇERMİYOR (`case_manager.py:379` civarı — işçi tam kapsamı
   koddan doğrular: aynı sözlüğü kullanan tüm dönüş yolları).
2. **Whitelist:** takip güncelleme whitelist'inin "Yerel Karar" bloğu
   (`case_manager.py:1544-1549` civarı) alanı içermiyor.

Sonuç: dropdown dolu ama değer GET'te dönmüyor, güncellemede yazılamıyor. Bu görev iki
noktayı bağlar; başka davranışa dokunmaz.

## Kabul kriterleri

- [x] Dava serialize çıktısında `yerel_karar_durumu` dönüyor — test, değeri yazılmış
      kayıtta alanın döndüğünü kilitler ve ESKİ kodda KIRMIZI olur (kırmızı-yeşil kanıtı)
- [x] Takip güncelleme yolu `yerel_karar_durumu` yazabiliyor (whitelist "Yerel Karar"
      bloğuna eklenerek; `CaseTrackingUpdate` şemasına dokunmadan — alan G060'tan beri var)
- [x] İstinaf/temyiz/karar-düzeltme karar alanlarının davranışı DEĞİŞMEDİ (bu alanlara
      diff yok; mevcut testler yeşil)
- [x] Whitelist dışı alan reddi/yoksayması değişmedi (mevcut davranış neyse o — işçi önce
      koddan okur, testle kilitler)
- [x] Yeni testler kendi kendine yeterli (G060'ın sqlite deseni tercih); gerçek DB
      gerekiyorsa 3-ortam kuralı: `to_regclass` şema kontrolü + SKIP

## Doğrulama

- `docker compose exec -T backend python -m pytest`  (ekstra `-q` EKLEME)
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- Dev araçları konteynerde yoksa önce: `docker compose exec -T backend pip install -r requirements-dev.txt`

## Rapor

**Durum: TAMAM** (2026-08-19 gecesi, ana dizin işçi oturumu)

### Yapılanlar

- `case_manager.py` — iki satırlık bağ, başka davranışa diff YOK (+4/-1):
  1. **Serialize:** `get_case` sözlüğüne `"yerel_karar_durumu": item.yerel_karar_durumu`
     eklendi — `karar_lehine`den sonra, models.py/schemas.py sırasıyla birebir.
     Kapsam koddan doğrulandı: `istinaf_karar_durumu` üretim kodunda YALNIZ bu
     sözlükte serialize ediliyor (grep: case_manager.py:379 tek nokta); liste
     görünümü (`get_cases`) karar alanlarını zaten taşımıyor — parite bozulmadı.
     Route (`/api/cases/{case_id}`) ham dict döndürür (`response_model` yok),
     yani eksik anahtar istemciye birebir yansıyordu; şimdi değer akıyor.
  2. **Whitelist:** `TRACKING_FIELDS` "Yerel Karar" bloğuna `yerel_karar_durumu`
     eklendi (`karar_lehine` sonrası). `CaseTrackingUpdate` şemasına dokunulmadı
     (alan G060'tan beri vardı; route → `model_dump(exclude_unset=True)` →
     `tracking_changes` zinciri artık süzmüyor).
- `tests/test_g065_yerel_karar_yolu.py` — 10 test, iki katman (G060 deseni):
  DB'siz (whitelist üyeliği + saf `tracking_changes`: yazma/None-silme/whitelist-dışı
  yoksayma) + in-memory sqlite StaticPool (`get_case` değerli + boş kayıt,
  `update_case_tracking` uçtan uca yazma/silme, kardeş istinaf alanı + whitelist-dışı
  `tracking_no` korunumu). Gerçek DB'ye bağlanan test YOK — üç ortamda aynı.

### Kararlar + gerekçeleri

- **Whitelist dışı alan davranışı = sessiz yoksayma** (koddan okundu:
  `tracking_changes` süzer, hata üretmez; route katmanında ayrıca Pydantic şeması
  var). Mevcut davranış AYNEN kilitlendi, ret'e çevrilmedi — görev "neyse o" diyor.
- **Boş kayıtta anahtar + None dönüyor** (istinaf paritesi): route ham dict
  döndürdüğü için anahtar hiç dönmeseydi frontend'in kontrollü input'u
  undefined'a düşerdi; ayrı testle kilitlendi.
- **Liste görünümüne (get_cases) alan EKLENMEDİ:** o sözlük hiçbir karar alanını
  (istinaf/temyiz/KD dahil) taşımıyor; eklemek pariteyi aşar, görev "başka
  davranışa dokunmaz" diyor.

### Test sonuçları

- Kırmızı kanıt (HEAD sürümü geçici geri konarak fiilen koşuldu): eski kodda
  **9 failed / 1 passed** — geçen tek test kardeş-alan korunum testi (iki sürümde
  de yeşil olması tasarım gereği). Serialize bacağı KeyError, whitelist bacağı
  boş-liste ile kırmızı.
- Yeni dosya: **10/10** yeşil.
- Tam paket (konteyner): **1343 passed, 3 skipped** (önceki 1333 + 10; sıfır
  regresyon — istinaf/temyiz/KD mevcut testleri dahil).
- ruff: temiz · mypy: 40 dosya, 0 hata.

### İzlenecekler / NOT

- Manager katmanı değeri resmi listeye (local_decisions) karşı DOĞRULAMAZ —
  kardeş alanlarla (istinaf/temyiz/KD) aynı sözleşme: dropdown G061'de kapalı
  liste, serbest metin yalnız API'yi doğrudan çağırana açık. Sıkılaştırma
  istenirse dört alan birlikte ele alınmalı (yeni görev konusu).
- G062'nin `stage_decisions.py` haritası (`karar_durumu` → `yerel_karar_durumu`)
  bu yoldan bağımsız kendi yazma yolunu kullanıyor; dokunulmadı.
