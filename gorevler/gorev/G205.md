# G205 — `/hukukbot` sayfası HukuDok tasarımıyla

- **Bant:** frontend
- **Bağımlı:** G203 (markdown paketleri), G204 (istemci)
- **Dosya kapsamı:** yeni `frontend/src/pages/HukukbotPage.tsx`, yeni `frontend/src/components/hukukbot/**`, bunların testleri
- **Dokunma:** `frontend/src/App.tsx`, `frontend/src/components/shell/Sidebar.tsx` (G206), `frontend/src/lib/hukukbotApi.ts` (G204 — eksik varsa BLOKE bırak), `frontend/package.json`

## Hedef
Eski hukbot arayüzünün (`../hukukbot-ui/App.tsx`, `components/HistorySidebar.tsx`, `InputArea.tsx`,
`MarkdownRenderer.tsx`, `RightPanel.tsx` — yalnız İŞLEV referansı, görsel/kod kopyalanmaz) işlevlerini HukuDok'un
kabuğu, bileşenleri (`components/ui/**`) ve tema token'larıyla (açık/koyu, koyu temada kurumsal bordo) yeniden kurar.
Giriş ekranı, MSAL, kullanıcı profili bileşeni YOK — HukuDok kabuğu zaten sağlar.

## Kabul kriterleri
- [ ] Sol: sohbet listesi — sabitlenenler üstte, yeni sohbet, başlık düzenleme, sabitle/çöz, silme ONAYLI (HukuDok'taki onay diyaloğu deseni).
- [ ] Orta: mesaj akışı; model yanıtı `react-markdown` + `remark-gfm` ile (ham HTML render EDİLMEZ — `rehype-raw` yok);
      akış sırasında kademeli yazım, "durdur" düğmesi (AbortSignal), hata/429 Türkçe mesaj.
- [ ] Kaynaklar: yanıtın `sources` alanı liste/panel; PDF kaynağı G204 `indir()` ile açılır.
- [ ] Giriş kutusu: Enter gönderir, Shift+Enter satır; boş soru gönderilmez; gönderimde kutu kilitli.
- [ ] Sayfa tam genişlik (max-w sınırı YOK — kullanıcı kuralı), 375 px'te yatay kaydırma yok (liste çekmeceye döner).
- [ ] Oturum seçimi URL'de (`/hukukbot?s=<id>`) → yenilemede aynı sohbet açılır.
- [ ] Testler: liste render + sabitleme sırası, silme onayı, gönder → akış parçaları ekrana düşer, markdown tablo render, ham `<script>` metin olarak kalır, 429 mesajı, kaynak indirme çağrısı.
- [ ] Sayfa lazy import'a uygun default export (rota G206'da bağlanır).

## Doğrulama
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`
- `npm --prefix frontend run build`

## Rapor
(işçi oturumu doldurur)
