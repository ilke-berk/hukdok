# G141 — Rapor kataloğu: veriden kapalı liste, seçenek etiketleri, `in` içinde boş, sanal arama kolonu, hızlı filtre sunumu

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/services/rapor/registry.py, backend/services/rapor/motor.py,
  backend/schemas_rapor.py (`Filtre.deger` `in` listesinde `null`; `AsistanFiltre` "(boş)"), backend/services/rapor/asistan.py
  (`katalog_metni` "yalnız filtre" şerhi + "(boş)" çevirisi), backend/prompts.py (yalnız "(boş)" kuralı tek cümle),
  backend/config/settings.py (`RAPOR_SECENEK_ESIGI`), .env.example, backend/tests/test_g141_rapor_veriden_liste.py,
  mevcut testler (aşağıdaki izin)
- **Dokunma:** backend/models.py, backend/database.py (migrasyon YOK), backend/routes/reports.py (yalnız gerekiyorsa
  önbellek anahtarı), frontend/**

## Test değiştirme İZNİ (baştan — G138/G139 dersi)

Bu görev katalog şeklini genişletir ve `Filtre.deger` doğrulamasını gevşetir (`in` içinde `null`). Aşağıdaki
mevcut testlerin ilgili beklentilerini **değiştirmek İZİNLİDİR**: `tests/test_g130_rapor_temeli.py`
(`test_katalog_sekli` anahtar kümesi; `in` içinde `null` 422 bekleyen parametre varsa), `tests/test_g137_rapor_katalog_genisleme.py`
(katalog şekli, `kontrol` tip eşlemesi — artık veriden liste kolonlarda `coklu_secim`; hızlı filtre listesi
müvekkiller), `tests/test_g132_rapor_asistani.py` (yalnız katalog metni uzunluğu/şekli testleri).
**Kilit:** 403/422 sözleşme yolları, tenant/soft-delete testleri, export/koşu logu testleri (G131) DEĞİŞMEZ;
`def test_`/`assert` sayısı net azalmaz. Raporda "değiştirilen test → gerekçe" tablosu.

## Hedef

Plan §5 (kullanıcı kararı 07.09): "birden fazla şehir seçilemiyor" sınıfı sorunun kökü, kontrol türünün
kolon TİPİNDEN türetilmesi. Yeni kural: işaretli metin kolonlarda DISTINCT değer sayısı eşiğin altındaysa
katalog `coklu_secim` + veriden seçenek (sıklık sırası) verir; müvekkil şeridi tek arama kutusu, kategori
çipleri, var/yok ve "X yok" anahtarlarıyla kurulur. Sözleşme §5.2 DONDU; G142 paralel yazılıyor. K1/K2
korunur; `/preview`/`/export` gövdesi ve DB değişmez.

## Yapılacaklar

1. **Veriden liste:** `Kolon.veriden_liste: bool = False`; `secenekleri_getir` genişler: veriden liste kolonda
   `SELECT deger, COUNT(*) … GROUP BY … ORDER BY COUNT DESC, deger` (kaynağın `kisitlar(tenant_id)` + boş hariç),
   eşik `settings.rapor_secenek_esigi` (env `RAPOR_SECENEK_ESIGI`, varsayılan 100): sayı ≤ eşik → `kontrol=
   "coklu_secim"`, `secenekler` dolu, `secenek_kaynagi="veri"`; aşarsa `kontrol="metin_icerir"`, `oneriler`
   (G137). Katalog kolonuna `secenek_kaynagi: "sabit"|"veri"|null`. İşaretler plan §5.2 listesi. Kolon `tip`
   "metin" kalır. Katalog önbelleği (60 sn, tenant anahtarlı) bu sorguları da kapsar — ek DISTINCT maliyeti raporda
   ölçülür (lokal 14.5k dava / 2k müvekkil, ms).
2. **Seçenek etiketleri:** `Kolon.secenek_etiketleri: Mapping[str,str]|None`; katalogda `secenek_etiketleri`;
   `client_type` için plan §5.2 haritası. Filtre değeri ham kalır (motor değişmez).
3. **`in` içinde boş:** `Filtre` doğrulaması `in` listesinde `None` öğesine izin verir (diğer op'larda 422
   `{"alan","sebep":"null yalnız 'in' listesinde"}`); tipli değer denetimi `None`'ı atlar; motor `_ifade_kosulu`:
   `in` → `or_(ifade.in_(dolu), ifade.is_(None))` (dolu boşsa yalnız `is_(None)`); türetilmiş EXISTS filtrelerinde
   `in` + `null` → "koşula uyan taraf yok" OR EXISTS(in) (muvekkil_kategorisi için). `AsistanFiltre.degerler`'de
   `"(boş)"` (ve "(bos)") sabiti `None`'a çevrilir; prompt'a tek cümle: "boş değeri de dahil etmek için `(boş)`".
4. **Sanal arama kolonu:** `Kolon.secilebilir: bool = True`; her kaynağa `arama` (plan §5.2 kolon listeleri;
   `_turetilmis(..., filtrelenebilir=True, secilebilir=False, izinli_oplar=("contains",))`, `filtre_ifadesi`
   = OR'lu ILIKE'lar; taraf adları EXISTS). `tanimi_dogrula`: `kolonlar`/`siralama` içinde `secilebilir=False`
   → 422 ("yalnız filtre alanı"); `_kendini_denetle`: varsayılan kolonlar ve kolon setleri `secilebilir` olmalı.
   Katalog kolonuna `secilebilir`. `katalog_metni`: "yalnız filtre" şerhi; `grup` "Arama".
5. **Hızlı filtre sunumu:** `HizliFiltre.sunum` (Literal plan §5.2) + `etiket`; denetim: `arama` yalnız `arama`
   kolonunda, `cipler` yalnız `secenekler`i olan kolonda, `var_yok` yalnız sayı/para, `bos_anahtari` her tipte.
   Müvekkiller listesi plan §5.2 sırası; Davalar/Belgeler/Föyler başına `arama(arama)`. Katalog `hizli_filtreler`
   öğesine `sunum` + `etiket`.
6. **Testler** (`test_g137` `env` deseni): veriden liste — eşik altı `coklu_secim` + sıklık sırası + tenant/
   silinmiş hariç; eşik üstü `metin_icerir` + öneriler; `RAPOR_SECENEK_ESIGI` env (monkeypatch settings);
   `secenek_etiketleri` client_type; `in` içinde `null` → IN OR IS NULL (davalar `court`, müvekkiller `il`,
   türetilmiş `muvekkil_kategorisi`); `null` başka op'ta 422; `arama` contains beş kolonda bulur, `kolonlar`da 422,
   kolon setlerinde yok, katalogda `secilebilir=false`; hızlı filtre `sunum` şekli + denetim hataları (`ValueError`
   import anında); asistan "(boş)" çevirisi; katalog şekli (anahtar kümesi güncel).

## Kabul kriterleri

- [ ] `il` kolonu katalogda `kontrol="coklu_secim"`, `secenekler` sıklık sırasında ve tenant/soft-delete kurallı,
      `secenek_kaynagi="veri"`; eşik aşımında `metin_icerir` (test, env ile iki yön).
- [ ] `{"alan":"il","op":"in","deger":["Ankara",null]}` Ankara VEYA il boş kartları döner; `null` `eq`/`contains`'te
      422 (test).
- [ ] `arama contains` beş müvekkil kolonunun herhangi birinde bulur; `arama` kolon listesine konursa 422;
      Kolonlar setlerinde ve varsayılanlarda yok (test).
- [ ] Müvekkiller `hizli_filtreler` plan §5.2 sırası ve `sunum` değerleriyle birebir (test).
- [ ] `client_type` `secenek_etiketleri` üç değer; filtre ham kodla çalışır (test).
- [ ] Asistan: `degerler:["Ankara","(boş)"]` → `in ["Ankara", null]` (test); katalog metni `arama`yı "yalnız
      filtre" şerhiyle içerir, seçenek listelerini şişirmez (test: metin uzunluğu G137 sonrası +%20'yi aşmaz).
- [ ] Migrasyon yok; `/preview`/`/export` gövdesi değişmedi; ruff + mypy temiz; tam paket yeşil.
- [ ] Sözleşme farkı çıktıysa plan §5.2 güncellendi ve raporda "SÖZLEŞME DEĞİŞTİ" başlığıyla yazıldı.

## Doğrulama

- `docker compose exec -T backend python -m pytest tests/test_g141_rapor_veriden_liste.py tests/test_g137_rapor_katalog_genisleme.py tests/test_g130_rapor_temeli.py tests/test_g132_rapor_asistani.py`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-07 gece, işçi oturumu; bant backend, ana dizinde, tek commit)

### Yapılanlar

1. **Veriden liste** (`registry.py`): `Kolon.veriden_liste` + `veriden_secenekleri_getir(kaynak, kolon, db, tenant_id)`
   — kaynağın `kisitlar(tenant_id)` (tenant + soft-delete) + boş/NULL hariç, `GROUP BY … ORDER BY COUNT DESC, deger`,
   `LIMIT eşik+1` (eşik aşımı tüm DISTINCT çekilmeden anlaşılır). `settings.rapor_secenek_esigi` (env
   `RAPOR_SECENEK_ESIGI`, varsayılan 100, `.env.example`'a yorumlu satır). `_kolon_katalogu`: sayı ≤ eşik →
   `kontrol="coklu_secim"`, `secenekler`, `secenek_kaynagi="veri"`, öneri katmanı KOŞMAZ (`oneriler=null`);
   aşarsa `metin_icerir` + G137 önerileri. Sabit liste → `secenek_kaynagi="sabit"`. `_kolon(veriden_liste=True)`
   `onerili`yi otomatik açar (eşik üstü yedek). İşaretlenenler: Müvekkiller `il, specialty`; Davalar `court,
   judicial_unit, responsible_lawyer_name, uyap_lawyer_name, sub_type`; Belgeler `uploaded_by, belge_turu_adi`.
2. **Seçenek etiketleri:** `Kolon.secenek_etiketleri` (Mapping) → katalog `secenek_etiketleri`; `client_type` için
   `MUVEKKIL_TIPI_ETIKETLERI` (Individual/Corporate/Gerçek Kişi). Filtre ham kodla (motor değişmedi).
3. **`in` içinde null:** `Filtre` validator: liste içinde `None` yalnız `op="in"`de (aksi 422 "null yalnız 'in'
   listesinde"); motor `_deger_cevir` `in` listesinde `None`'ı tip denetiminden geçirmez, tek değerli op'ta
   `None` → "değer gerekli (null yalnız 'in' listesinde)"; `_ifade_kosulu` `in` → `IN (dolu) OR IS NULL`, yalnız
   `[null]` → `IS NULL`. `_muvekkil_kategorisi_filtresi`: `in` + `null` → "kategorili canlı kartlı taraf yok" OR
   EXISTS(in dolu) (satır düzeyi `IS NULL` yanıltırdı). Asistan: `BOS_SABITLERI = {"(boş)","(bos)"}`
   (küçük harf + trim) yalnız `in` listesinde `None`'a; prompt'a tek cümle.
4. **Sanal `arama` kolonu:** `Kolon.secilebilir`; `_arama(*ifadeler, ek_filtreler=…)` → `_turetilmis(… secilebilir=False,
   izinli_oplar=("contains",))`, `ifade=null()` (SELECT'e giremez), koşul `_arama_filtresi` = OR'lu atom ILIKE'lar +
   taraf EXISTS'leri. Kolon listeleri plan §5.2 birebir (Davalar: `tracking_no|esas_no|subject|court|muvekkil_adlari
   (EXISTS)|karsi_taraf_adlari(EXISTS)`). Grup `"Arama"` her kaynağın grup kümesine başa eklendi; `motor.tanimi_dogrula`
   `kolonlar`/`siralama`da `secilebilir=False` → 422 "yalnız filtre alanı"; `_kendini_denetle` varsayılan kolon +
   kolon setlerinde seçilebilirlik zorlar; `_kolonu_denetle` seçilemeyen kolonu türetilmiş+filtrelenebilir+Arama
   grubuna, Arama grubunu yalnız seçilemeyen kolona bağlar. `katalog_metni`: "yalnız filtre (kolon listesine girmez;
   op: contains)" şerhi.
5. **Hızlı filtre sunumu:** `HizliFiltre.sunum` (`SUNUMLAR`) + `etiket`; `_hizli_filtreyi_denetle` (import anında):
   `arama` ⇔ `arama` kolonu, `cipler` yalnız liste/veriden liste, `var_yok` yalnız sayı/para, `bos_anahtari` `is_null`
   izinli kolon, `etiket` yalnız anahtar sunumlarında, alternatifler yalnız tarih+varsayılan. Müvekkiller şeridi plan
   §5.2 sırası (7 öğe); Davalar/Belgeler/Föyler `arama(arama)` başa. Katalog öğesi `{alan, alternatifler, sunum, etiket}`.
6. **Testler:** `tests/test_g141_rapor_veriden_liste.py` — 23 test / 95 assert (eşik iki yön + sınır + 0, sıklık ve
   ad sırası, tenant/soft-delete, plan işaret listesi, env, etiketler + ham filtre, `in`+null üç kaynak/türetilmiş, 5×422
   null başka op, `arama` 9 parametre müvekkil + davalar/belgeler/föyler, 422 kolon/sıralama/op, katalog `secilebilir`,
   müvekkil şeridi birebir, denetim hataları 7+1+1, kolon denetimi 6, asistan "(boş)", katalog metni şerh + uzunluk,
   `/preview` gövdesi + önbellek GROUP BY kapsamı).

### Kararlar + gerekçe

- **`secenekler` ↔ `oneriler` birbirinin yerine:** eşik altında öneri sorgusu koşmaz (aynı DISTINCT'i iki kez çekmek
  anlamsız; frontend `kontrol`e bakar). Plan §5.2'ye not düşüldü.
- **`karar_turu`/`case_stage`/`dosya_son_durumu` işaretlenmedi:** zaten `liste` (sabit + tablo + DISTINCT katmanı),
  kontrol zaten `coklu_secim`; `veriden_liste` yalnız düz+önerili METİN kolonda tanımlı (`_kolonu_denetle`).
- **Eşik `≤`:** görev metni "sayı ≤ eşik"; plan "altındaysa" yazıyordu → plan netleştirildi.
- **`ifade=null()` arama kolonunda:** `Kolon.ifade` zorunlu; SELECT'e hiç girmediği (422) için zararsız, `Kolon`
  şeklini bozmamak için `Optional` yapılmadı.
- **Sıralamada da 422:** türetilmiş zaten sıralanamazdı; "yalnız filtre alanı" mesajı daha açıklayıcı, önce o.
- **`routes/reports.py` dokunulmadı:** önbellek anahtarı `(SessionLocal, tenant)`; eşik boot'ta donan settings alanı,
  anahtara girmesi gerekmez (test monkeypatch'te önbelleği sıfırlar).

### SÖZLEŞME DEĞİŞTİ (plan §5.2 güncellendi)

- Zaten `liste` tipli üç dava kolonu + `client_type`/`muvekkil_tipi` "işaretlenecekler"den çıkarılıp "sabit kalır"
  olarak yazıldı (davranış aynı: `coklu_secim`, `secenek_kaynagi="sabit"`).
- Eşik karşılaştırması `≤`; veriden liste eşik altında `oneriler=null`; `secilebilir=false` kolon `siralama`da da 422.
- `hizli_filtreler` öğesi + katalog kolonu alanları plan §5.2 ile aynı; ek alan yok.

### Değiştirilen test → gerekçe (izin: görev dosyası "Test değiştirme İZNİ")

| Test | Değişiklik | Gerekçe |
| --- | --- | --- |
| `test_g130::test_katalog_sekli` | kolon anahtar kümesi + `secilebilir/secenek_kaynagi/secenek_etiketleri`; `secenekler` kuralı (liste → sabit; `veri` → liste; aksi None) | katalog şekli genişledi |
| `test_g137::GRUPLAR` | her kaynağa `"Arama"` | sanal `arama` kolonunun grubu |
| `test_g137::ONERILI` → `VERIDEN` + `ONERILI` | işaretli 9 kolon `secenekler`(veri), öneriler yalnız işaretsiz 5 kolonda | veriden liste eşik altında öneri koşmaz |
| `test_g137::test_katalog_yeni_alanlarin_sekli` | hızlı filtre anahtar kümesi `+sunum/etiket`; VERIDEN kolonlarda `secenekler` list + `secenek_kaynagi="veri"` (assert +1) | katalog şekli |
| `test_g137::test_katalog_kontrol_tip_eslemesi` | `secenek_kaynagi="veri"` → `coklu_secim` dalı; `court` assert'i eklendi | kontrol artık veriden |
| `test_g137::test_katalog_hizli_filtreler_plan_listesi` | listeler (`arama` başa, müvekkil şeridi yeni); `acilis` index 1 + `sunum/etiket` | plan §5.2 listesi |
| `test_g137::test_oneriler_onerili_kolonlar_ve_kurallar` | avukat/mahkeme `oneriler` → `secenekler`; `secenek_kaynagi=="veri"` kümesi assert'i eklendi | aynı kurallar, yeni alan |
| `test_g137::test_oneriler_tenant_kurali` / `..._soft_delete` / `..._60_sn_gizler` | `oneriler` → `secenekler` (il, uploaded_by, belge_turu_adi, responsible_lawyer_name) | aynı |

`def test_` sayısı: g130 32→32, g137 25→25; `assert` sayısı g130 82→84, g137 124→129 (net azalma yok). Kilitli
yollar (403/422 sözleşme, tenant/soft-delete, G131 export/koşu logu) DEĞİŞMEDİ. `test_g132` değişmedi (yeşil).

### Ölçüm (lokal Postgres: 14.555 dava / 1.998 müvekkil / 229 belge / 8.386 föy, sqlite değil)

`veriden_secenekleri_getir` kolon başına: `davalar.sub_type` 64,6 ms (73 seçenek), `responsible_lawyer_name` 6,4 ms
(17), `uyap_lawyer_name` 5,2 ms (1), `court` 8,0 ms (**eşik üstü → metin_icerir + öneriler**, >100 mahkeme),
`judicial_unit` 7,8 ms (25), `muvekkiller.il` 3,9 ms (79), `specialty` 1,8 ms (44), `belgeler.belge_turu_adi`
5,1 ms (40), `uploaded_by` 2,9 ms (3). Toplam 105,7 ms; `registry.katalog()` tam gövde 215 / 182 ms (iki tur).
60 sn tenant anahtarlı önbellek bu sorguları da kapsar (test ile doğrulandı). `katalog_metni` 9.045 → 9.345 karakter
(+%3,3; arama satırları hariç hâline göre sınır +%20).

### Test sonuçları

- Hedef dosyalar: `test_g141` (23) + `test_g137` + `test_g130` + `test_g132` → **193 passed**.
- Tam paket (konteyner): **2712 passed, 3 skipped** (önceki 2019/2352 sayıları eski; bugünkü main tabanı).
- `ruff check .` temiz; `mypy` temiz (61 dosya).
- Migrasyon yok; `/preview`/`/export` gövdesi değişmedi (test bekçisi); `routes/reports.py`, `models.py`,
  `database.py`, `frontend/**` dokunulmadı.

### İzlenecekler / NOT

- NOT: `prompts.py` "Türetilmiş kolonlar (işaretli) filtrelenemez ve sıralanamaz" cümlesi G137'den beri eksik
  (taraf kolonları filtrelenebilir) — kapsam dışı, tek cümle kuralıyla dokunulmadı; G143 docs turunda ele alınmalı.
- NOT: Lokal veride `court` 100'ü aşıyor → prod'da da muhtemelen `metin_icerir` kalır; eşik `RAPOR_SECENEK_ESIGI=150`
  ile açılabilir (ölçüm 8 ms, maliyet düşük). Kullanıcı kararı.
- NOT: `.claude/settings.local.json` (M) ve `.claude/launch.json` (??) oturum başında zaten kirliydi; kapsam dışı,
  dokunulmadı, commit'e alınmadı.
- G142 (frontend) bu sözleşmeye göre paralel: `secilebilir=false` gizleme, `secenek_kaynagi`, `sunum`, `in` içinde
  `null` çipi. G143 docs: `raporlama.md`.
