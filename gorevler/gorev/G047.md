# G047 — Deploy kapısı migrasyon testlerini koşmuyor

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** deploy.sh, docs/mimari/deploy-ve-altyapi.md
- **Dokunma:** backend/tests/conftest.py (DOKUNMA), backend/tests/test_migration_path.py (DOKUNMA), .github/workflows/ci.yml (DOKUNMA)

## Hedef

G038'in kapısı çalışıyor ama **eksik denetliyor** (2026-08-12'de ölçüldü):

| Nerede | Sonuç |
| --- | --- |
| `docker compose exec backend pytest` | 1041 passed, **2** skipped |
| `bash deploy.sh --gate-only` | 1035 passed, **8** skipped |

Aradaki 6 test `tests/test_migration_path.py`; sebep konteynerin çıktısında yazılı:
`SKIPPED: Gerçek Postgres'e ulaşılamadı (OperationalError)`.

Kapı bilinçli olarak **temiz ortamlı** `docker run` kullanıyor (G038'in doğru kararı —
`docker compose run` `.env`'i taşır ve migrasyon testi **prod Postgres'te** scratch
veritabanı yaratıp düşürebilirdi). Ama bunun bedeli: DB'ye ulaşamıyor, testler
**sessizce** atlanıyor. SKIP yeşildir, kimse fark etmez.

**Bugün zararsız, FAZ D'de değil:** D tamamen migrasyon işi ve onu koruyan testler
deploy anında koşmayacak. D'nin deploy'undan ÖNCE kapanmalı.

## Çözüm yönü

Kapı **kendi tek kullanımlık Postgres'ini** kaldırsın: `docker run -d postgres:15-alpine`
(rastgele ad, yayınlanmamış port, kendi ağı) → test konteyneri ona bağlansın →
kapı bitince **her iki konteyner de** yıkılsın. Prod veritabanına hiçbir temas yok.

## Kabul kriterleri

- [ ] `--gate-only` çıktısında `tests/test_migration_path.py`'nin **7 testi de koşuyor**
      (skip yok); skip sayısı `docker compose exec` koşusuyla aynı seviyeye indi
- [ ] Geçici Postgres konteyneri kapı bitince **her yolda** siliniyor: başarı, test
      başarısızlığı, pip başarısızlığı (91) ve `Ctrl-C`/`trap`. Dördü de denendi ve
      `docker ps -a` ile artık kalmadığı görüldü — çıktı rapora yazılır
- [ ] Konteyner adı çakışmaya dayanıklı (aynı anda iki deploy ya da kalıntı bir konteyner
      kapıyı kilitlememeli)
- [ ] Geçici Postgres **prod ağına bağlanmıyor** ve **port yayınlamıyor**;
      `docker compose run` KULLANILMIYOR (denetçi grep'ler)
- [ ] Kapının süresi yeniden ölçüldü (postgres kalkışı ekleniyor) ve rapora yazıldı;
      G038'in ölçtüğü ~46 sn ile karşılaştırıldı
- [ ] Dört yol yine ölçüldü: sağlam → 0, kırık → ≠0, `SKIP_TESTS=1` → 0 + uyarı,
      pip yok → 91 → atla
- [ ] `bash -n deploy.sh` temiz; `docs/mimari/deploy-ve-altyapi.md` aynı commit'te güncel
- [ ] Prod'a hiçbir şey koşulmadı

## Doğrulama

- `bash -n deploy.sh`
- `bash deploy.sh --gate-only` (lokal — prod DEĞİL)
- `docker ps -a` (kalıntı konteyner yok)

## Rapor

(işçi oturumu doldurur)
