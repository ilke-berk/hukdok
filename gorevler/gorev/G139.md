# G139 — Rapor ekranı: kaynak kartları, Kolonlar yan paneli (gruplar + setler + sıra), dolu açılış, sadeleştirme

- **Bant:** frontend
- **Bağımlı:** G138
- **Dosya kapsamı:** frontend/src/components/reports/SourceCards.tsx (YENİ), .../ColumnSheet.tsx (YENİ; shadcn
  `Sheet` + mevcut ColumnPicker mantığı), .../ColumnPicker.tsx (yan panele taşınır, tip rozetleri kalkar, gruplu),
  .../ReportBuilder.tsx (kaldırılır ya da ince araç çubuğuna iner), frontend/src/pages/ReportsPage.tsx (yerleşim:
  kartlar → filtre şeridi → araç çubuğu → tablo; açılışta dolu liste), .../PreviewTable.tsx (sayaç "N kayıt · M
  kolon"), ilgili `*.test.tsx`
- **Dokunma:** frontend/src/App.tsx, Sidebar.tsx, lib/api.ts (hub — DOKUNMA), QuickFilters/FilterControl/
  FieldPicker (G138 ürünleri — yalnız prop uyumu), AssistantPanel.tsx, TemplateBar.tsx

## Test değiştirme İZNİ ve planlayıcı kararları (BLOKE çözümü, 07.09 gece, ikinci koşu)

İşçi haklı olarak durdu: G138'in yeni yazdığı sayfa testleri (`#rapor-kaynak` select'i, sol sütun
`xl:grid-cols-[320px_1fr]`, sayfada doğrudan `[data-kolon]` listesi, "Toplam 120 kayıt" metni) bu görevin
bilinçle kaldırdığı arayüzü kilitliyor. **Aşağıdaki test dosyalarını değiştirmek, beklentilerini ters
çevirmek ve gerekirse silmek AÇIKÇA İZİNLİDİR** (kullanıcının 07.09 yeniden tasarım kararı, plan §4.1):
`frontend/src/pages/ReportsPage.test.tsx`, `ReportsPage.sablon.test.tsx`, `ReportsPage.asistan.test.tsx`,
`frontend/src/components/reports/PreviewTable.test.tsx`, `ColumnPicker.test.tsx` / `ReportBuilder.test.tsx`
(varsa). **Kilit korunacaklar (DOKUNMA):** `lib/reports.export.test.ts`, `lib/reportsChat.test.ts`,
`lib/reports.test.ts`, `QuickFilters.test.tsx`, `builderState.test.ts`, RunsTable/TemplateBar testleri.
Raporda "değiştirilen test → gerekçe" tablosu yazılır; `expect(` sayısı net azalmaz (kapı kuralı).

Dört sorunun cevabı:
1. Dört test dosyası için izin VERİLDİ (yukarıdaki liste).
2. Seçili kolon doğrulaması: birincil yol **"Kolonlar (N)" düğme metni + `/preview` istek gövdesindeki
   `kolonlar` listesi** (işçinin önerisi); ek olarak EN AZ bir test paneli açıp `[data-kolon]` ile grup/set
   davranışını doğrular (ColumnSheet'in kendi testi).
3. Yan panel: `frontend/src/components/ui/sheet.tsx` shadcn şablonundan eklenir — `@radix-ui/react-dialog`
   üzerine kurulu, **yeni npm paketi değil** (dialog primitive zaten kurulu; `components.json` var). Portal
   nedeniyle testler `document.body` üzerinden okur (`AdminPage.test.tsx` Dialog kalıbı). G138'in
   `useDisariTiklama` deseni yalnız küçük açılırlar için; yan panel Sheet.
4. Kabul maddeleri AYNEN korunur (dolu açılış, sol sütun yok, tip rozeti yok, tam genişlik, `lg` altı);
   çatışan testler yeniden yazılır, madde gevşetilmez.
Kaynak seçimi kartla olur; `#rapor-kaynak` select'i kalkar. Kartlarda `role="radio"` + `aria-checked`
(erişilebilirlik ve test için tek kimlik: `[data-kaynak="davalar"]`).

## Hedef

Plan §4.1 madde 1, 3, 6, 7: sol sütundaki "sorgu kurucu" kalkar; kullanıcı önce **ne** listeleyeceğini kartla
seçer, tablo hemen dolu gelir, filtre şeridi üstte (G138), kolonlar yan panelde gruplu ve hazır setli.
Ekrandaki metin azalır (açıklamalar tooltip'e, tip rozetleri kalkar). Tam genişlik kuralı korunur.

## Yapılacaklar

1. **SourceCards:** dört kart (etiket + tek satır `aciklama`, simge), seçili kart vurgulu; tıklama G138'in
   kaynak değişimi yolunu çağırır. Dar ekranda yatay kaydırılabilir.
2. **Yerleşim (`ReportsPage`, Rapor sekmesi):** kartlar → QuickFilters şeridi → araç çubuğu (sol: "Kolonlar (N)"
   düğmesi + şablon çubuğu; sağ: Excel/CSV + Asistan) → PreviewTable tam genişlik. `xl:grid-cols-[320px_1fr]`
   sol sütun kalkar. Sayfa açılışında katalog gelince Davalar + varsayılan kolonlar + filtresiz önizleme
   kendiliğinden yüklenir.
3. **ColumnSheet:** sağdan açılır panel; üstte kolon setleri (`kolon_setleri`, tık = seçimi değiştirir,
   "Temel" = varsayılan), arama kutusu, `grup` başlıklı checkbox listesi (grup başlığında "tümünü seç"),
   altta "Seçili · sıra" (mevcut dnd + ↑↓, `ColumnPicker` mantığı taşınır); tip rozetleri (`abc/123/liste`)
   kaldırılır, tip yalnız `title` tooltip'inde. Panel kapanınca önizleme G138 otomatik yoluyla yenilenir.
4. **Sadeleştirme:** kaynak açıklaması yalnız kartta; "Cari kartlar (silinmişler hariç)…" gibi açıklamalar
   tooltip; Eyebrow başlıklar kısa; tablo başlığında "3.064 kayıt · 8 kolon"; boş sonuçta "Bu filtrelerle kayıt
   yok — filtreleri gevşetin" + Temizle kısayolu.
5. **Responsive:** `lg` altında kartlar yatay kaydırma, şerit sarar, tablo `overflow-x-auto`; gövde yatay
   kaydırmaz.
6. **Testler:** açılışta otomatik ilk önizleme (`/preview` bir kez, davalar + varsayılan kolonlar); kart tıklaması
   kaynak değiştirir; ColumnSheet set seçimi kolon listesini değiştirir ve önizleme yenilenir; gruplu liste +
   grup "tümünü seç"; tip rozeti DOM'da yok; şablon yükleme + asistan uygulama akışı hâlâ çalışır (regresyon).

## Kabul kriterleri

- [x] Sayfa açılışında tablo dolu gelir (Davalar, varsayılan kolonlar), kullanıcı hiçbir şeye basmadan (test).
- [x] Sol sütun yok; kolonlar yalnız yan panelden; panelde gruplar ve en az 3 hazır set (test).
- [x] Tip rozetleri ekranda yok; kaynak açıklaması kart dışında tekrar etmiyor (test/grep).
- [x] Şablon yükleme, Excel/CSV indirme, asistan eylemi ve İndirme geçmişi G134/G135 testleriyle yeşil (regresyon).
- [x] Sağda boşluk kalmaz (`mx-auto` yok), `lg` altı düzen yatay kaydırmaz.
- [x] Hub dosyalara dokunulmadı; yeni npm paketi yok; `npm test`, `lint`, `tsc -b --force` temiz.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

DURUM: TAMAM (işçi oturumu, 2026-09-07 gece, worktree `C:/dev/hukudok-wt/G139`, dal `gorev/G139`)

### Yapılanlar

- **`components/ui/sheet.tsx` (YENİ):** shadcn `Sheet` şablonu, `@radix-ui/react-dialog` üzerine — yeni npm paketi YOK
  (`package.json` değişmedi). Kenar sınıfları `cva` yerine düz tabloda (dışa aktarılmaz; react-refresh
  "only-export-components" uyarısı üretmez). Redesign token'ları (`--bg-elevated`, `--border`, köşe yok).
  Kapat düğmesi `aria-label="Paneli kapat"`.
- **`SourceCards.tsx` (YENİ):** dört kart (`role="radiogroup"` / `role="radio"` + `aria-checked`, kimlik
  `[data-kaynak]`), etiket + tek satır `aciklama` (truncate) + simge (davalar Briefcase · müvekkiller Users ·
  belgeler FileText · föyler Folder; tanınmayan kaynak Table2). `lg` altı `flex overflow-x-auto`, `lg`+ 4 sütun grid.
  Kaynak açıklaması YALNIZ burada.
- **`ColumnPicker.tsx` (yeniden yazıldı, yan panel gövdesi):** üstte hazır setler (`kolon_setleri`; "Temel" katalogda
  yoksa `varsayilan_kolonlar`dan üretilir; tık = seçimi setin sırasıyla DEĞİŞTİRİR; `aria-pressed` sıra bağımsız aynı
  kümede), arama, `grup` başlıklı checkbox listesi (grup başlığı "tümünü seç": eksikleri tavana kadar ekler, tam
  seçiliyse grubu kaldırır, kısmi → `indeterminate`), "Seçili · sıra" (dnd-kit + ↑↓ + × + Temizle, önceki mantık
  taşındı). Tip rozetleri (`abc/123/liste/₺`) ve "türetilmiş" rozeti KALKTI — tip yalnız satırın `title` ipucunda.
- **`ColumnSheet.tsx` (YENİ):** "Kolonlar (N)" düğmesi (`data-testid="kolon-dugmesi"`, `aria-expanded`) + sağdan
  `Sheet` (`data-testid="kolon-paneli"`, portal). Her değişiklik anında sayfaya yazılır → G138 otomatik önizleme
  (yapısal = hemen); panel kapanınca ek istek yok.
- **`ReportBuilder.tsx` KALDIRILDI** (`git rm`); kaynak `select#rapor-kaynak` yok.
- **`ReportsPage.tsx`:** yerleşim kartlar → QuickFilters şeridi → araç çubuğu (`data-testid="arac-cubugu"`; sol:
  ColumnSheet + TemplateBar, sağ: ExportButtons + Asistan düğmesi — başlıktan çubuğa taşındı) → PreviewTable, hepsi
  tek `HairlineCard padded={false}` içinde tam genişlik. `xl:grid-cols-[320px_1fr]` sol sütun kalktı; kök
  `grid gap-7` (`max-w-[1600px]` de kaldırıldı — tam genişlik kuralı, `mx-auto` yok). Başlık paragrafı kaldırıldı,
  kullanım ipucu `h1 title`'ında. `onKaynakSec` (aynı kart = işlem yok), `onFiltreleriTemizle` (boş sonuç kısayolu).
  Dolu açılış: katalog gelince ilk kaynak + varsayılan kolonlar → G138 efekti önizlemeyi kendiliğinden ister
  (davranış G138'de vardı, artık sol sütunsuz ve testle kilitli).
- **`PreviewTable.tsx`:** sayaç `"N kayıt · M kolon"` (`toplam` tr-TR + `cevap.kolonlar.length`; "Toplam" ön eki
  yok); boş sonuçta `data-testid="bos-sonuc"`: filtre varken "Bu filtrelerle kayıt yok — filtreleri gevşetin." +
  "Filtreleri temizle" kısayolu (`onFiltreleriTemizle`), filtresizken "Bu kaynakta kayıt yok."; `araclar` yuvası
  kaldırıldı (indirme düğmeleri araç çubuğunda).
- **`builderState.ts`:** `seritiTemizle(serit, kaynak)` — QuickFilters'ın "Filtreleri temizle" kuralının aynısı
  (hızlı yuvalar boş kontrole döner, eklenen alanlar kalkar). QuickFilters kendi kopyasını korur (G138 ürünü,
  "yalnız prop uyumu" kuralı gereği dokunulmadı).
- **`docs/mimari/raporlama.md`:** diyagramın ilk satırı `ReportBuilder.tsx`'i gösteriyordu → yeni yerleşim
  (CLAUDE.md altın kuralı: kod ile çelişen doküman düzeltilir).

### Kararlar + gerekçeleri

1. **Kolon değişikliği panel açıkken anında önizlenir** (kapanışta toplu değil): G138 sözleşmesi "yapısal
   değişiklik hemen" der; mevcut sayfa testleri her checkbox tıklamasında bir `/preview` bekler; kullanıcı
   panelin arkasında tablonun değiştiğini görür. "Panel kapanınca yenilenir" maddesi böylece kendiliğinden sağlanır.
2. **Seçili kolon kanıtı** planlayıcı kararıyla: `"Kolonlar (N)"` düğme metni + `/preview` gövdesi `kolonlar`;
   sayfa testlerinde `seciliKolonlar()` bu ikisini birlikte doğrular. Panel içi `[data-kolon]` sırası
   ColumnSheet'in kendi testinde + sayfa testinde (set/grup senaryosu) `document.body` üzerinden okunur.
3. **`max-w-[1600px]` kaldırıldı:** kullanıcı geri bildirimi (redesign sayfaları genişlik tavanı kullanmaz, sağda
   boşluk kalmasın); Shell `<main>` zaten `px-7` verir.
4. **Asistan düğmesi araç çubuğunun sağında** (görev madde 2); `butonBul("Asistan")` konteyner genelinde arar,
   asistan testleri değişmeden geçti (yalnız `#rapor-kaynak`/`[data-kolon]` yardımcıları uyarlandı).
5. **"Filtreleri temizle" metni** boş sonuç kısayolunda da aynı (QuickFilters ile tutarlı); testte
   `[data-testid='bos-sonuc']` kökünden bulunur, çakışmaz.

### Değiştirilen test → gerekçe (izinli liste; `expect(` sayısı NET ARTTI: 345 → 411 dört dosyada + 65 yeni dosyalarda)

| Dosya | Değişiklik | Gerekçe | `expect(` |
| --- | --- | --- | --- |
| `pages/ReportsPage.test.tsx` | `#rapor-kaynak` select → `[data-kaynak]` kartlar (`seciliKaynak()`); sayfada `[data-kolon]` → `"Kolonlar (N)"` + gövde; kolon checkbox/↓/Temizle yan panelde (`kolonPaneliAc/Kapat`, `document.body`); "Toplam 120 kayıt" → "120 kayıt · 4 kolon"; yerleşim testi `xl:grid-cols-[320px_1fr]` bekler**di** → sol sütun YOK + kartlar→şerit→çubuk→tablo sırası + kaydırma sınıfları; `max-w` yok. YENİ: kart tıklaması (aria-checked, aynı kart istek üretmez), yan panel 3 set + gruplar + set→önizleme + grup "tümünü seç" + kapanışta ek istek yok, boş sonuç kısayolu, sadeleştirme (açıklama tek yer, tip rozeti yok, "Rapor Oluşturucu" yok). Katalog sahtesine 2 set + müvekkil açıklaması eklendi; `sec()` yardımcısı silindi (kullanılmıyordu). | Görev bilinçle kaldırdığı arayüzü (select, sol sütun, sayfada kolon listesi) kilitleyen beklentiler; kabul maddeleri gevşetilmedi, tersine kilitlendi | 96 → 151 |
| `pages/ReportsPage.sablon.test.tsx` | `seciliKolonlar()` = son önizleme gövdesi + düğme sayısı; `#rapor-kaynak` → `seciliKaynak()`; "reddedilirse" senaryosunda önce istek sayısı (1) sonra kolon kanıtı | Aynı sebep; şablon/indirme/geçmiş akışı değişmedi | 110 → 111 |
| `pages/ReportsPage.asistan.test.tsx` | aynı yardımcılar; `byLabel("Durum")` tıklamaları `kolonPaneliAc()` sonrası panelde | Kolon checkbox'ı artık yan panelde | 115 → 116 |
| `components/reports/PreviewTable.test.tsx` | "Toplam 1 kayıt" → "1 kayıt · 3 kolon"; YENİ: 3.064 binlik + "Toplam" yok; boş sonuç filtreli/filtresiz | Sayaç metni görev maddesi 4 | 24 → 33 |
| `components/reports/ColumnSheet.test.tsx` (YENİ) | düğme/portal/setler/gruplar/sıra/tip title; set değiştirme + sıra bağımsız seçili; grup tümünü seç (indeterminate, kaldır); arama/Temizle/tavan (60 kolon, grup ve satır pasif) + "Temel" üretimi; kapat | Görev madde 6 + planlayıcı kararı 2 (panel içi `[data-kolon]`) | 52 |
| `components/reports/SourceCards.test.tsx` (YENİ) | radiogroup/aria-checked/etiket+açıklama/boş açıklama/title; onSec | Görev madde 1 | 13 |

Kilit korunanlar DOKUNULMADI: `lib/reports.export.test.ts`, `lib/reportsChat.test.ts`, `lib/reports.test.ts`,
`QuickFilters.test.tsx`, `builderState.test.ts`, RunsTable/TemplateBar testleri, `AdminPage.test.tsx`.

### Doğrulama (HOST, worktree; docker compose kullanılmadı)

- `npm --prefix frontend test` → **65 dosya, 798 passed** (ilk tam koşuda `src/lib/api.test.ts` "eşzamanlı iki
  token'sız istek" 2000 ms `waitFor` zaman aşımıyla düştü; dosya tek başına 28/28 ve ikinci tam koşu 798/798 —
  yük kaynaklı flake, bu görevle ilgisi yok; bkz. NOT).
- `npm --prefix frontend run lint` → temiz (0 hata, 0 uyarı).
- `npx --prefix frontend tsc -b --force frontend` → temiz.
- Reports paketi tek başına: 8 dosya 74 test yeşil.

### İzlenecekler / NOT

- **NOT (kapsam dışı):** `frontend/src/lib/api.test.ts` "eşzamanlı iki token'sız istek tek logout tetikler"
  testi tam paket koşusunda yük altında 2000 ms `vi.waitFor` sınırını aşabiliyor (gerçek zamanlayıcı). Tek
  başına ve ikinci koşuda geçti; `lib/api.ts` hub olduğu için dokunulmadı. Sık tekrarlarsa `waitFor`
  süresini artırmak ya da sahte zamanlayıcıya almak ayrı iş.
- Tarayıcıda görsel duman testi yapılmadı (backend/MSAL gerektirir); yerleşim ve responsive kanıtı sınıf/sıra
  düzeyinde testle. Sabah gerçek ekranda: kart satırının `lg` altında yatay kaydırması, Sheet'in `sm:max-w-lg`
  genişliği ve araç çubuğunda TemplateBar'ın sarması göz kontrolü ister.
- TemplateBar araç çubuğunun sol yarısında `flex-1` alanda; dar ekranda çubuk `flex-wrap` ile sarar.
