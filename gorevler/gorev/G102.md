# G102 — RelatedCasesPanel CaseDetails'e monte edilir; manual-only çip kodu sökülür

- **Bant:** frontend
- **Bağımlı:** -
- **Dosya kapsamı:** frontend/src/pages/CaseDetails.tsx,
  frontend/src/components/RelatedCasesPanel.tsx (yalnız gerekirse — normalde değişmez),
  frontend/src/components/RelatedCasesPanel.test.tsx (yalnız regresyon eki gerekirse)
- **Dokunma:** frontend/src/components/AddRelationModal.tsx, frontend/src/hooks/useCases.ts,
  frontend/src/lib/api.ts, backend/** (gerekirse BLOKE bırak — backend tarafı BİTMİŞTİR,
  bu görev saf frontend montaj işidir)

## Hedef

Otomatik ilişki katmanı (TKU grubu + esas/mahkeme ikizi, `services/case_relations_auto.py`)
backend'de çalışıyor ve `GET /api/cases/{id}/relations` yanıtının `automatic` listesinde
geliyor; onu çizecek `RelatedCasesPanel` bileşeni de 20.08'de yazılıp testlendi
(commit `4e3eec4`, 9 test). **Ama o commit yalnız bileşene + testine dokundu — bileşeni
hiçbir sayfa import etmiyor.** CaseDetails'teki "Bağlantılı Davalar" alanı ise yalnız
`result.manual`'ı çiziyor (`CaseDetails.tsx` `fetchRelated`: `setRelatedBrief(result.manual ?? [])`);
elle kurulmuş bağ da 0 satır (aktarım bilinçli olarak ilişki YAZMAZ, okurken hesaplanır —
`case_relations_auto.py` docstring). Sonuç: otomatik katman uygulamanın hiçbir yerinde
görünmüyor. Kanıt (2026-08-25, lokal aktarım kopyası): kart 14142'de servis 4 ilişki
üretiyor (TKU-788 → 14139/14140/14141/14143, hepsi YENIDEN_ACILAN + "Aynı TKU grubu"
gerekçesi), UI'da 0 görünüyor.

Bu görev paneli dava detay sayfasına monte eder ve sayfadaki manual-only ikiz kodu söker —
tek yazar panel olur.

## Tasarım kararı (2026-08-25, kullanıcı "B'yi yapalım" dedi)

Panel **header kartı ile Tabs bloğu arasına, her zaman görünür tam-genişlik bölüm olarak**
konur (bir sekmenin içine DEĞİL — sekme içinde deep-link/ilk-yük durumlarında monte olmama
kenarı doğar; ayrıca sayfa tam genişlik kuralına uy, max-w cap yok). Header'daki eski inline
çip bölümü ("Bağlantılı Davalar" + mevcut-dava çipi + çip listesi + inline AddRelationModal)
KALDIRILIR: hemen altındaki panel aynı bilgiyi tür etiketi + gerekçe + Kalıcı Bağla ile
zaten basar; iki blok aynı veriyi yan yana tekrar ederdi ve iki ayrı yazar (iki modal,
iki fetch) yaşatırdı. `AYNI_DAVA` uyarı rengi ve "Sistemin bulduğu" ayrımı panelin kendi
işi — sayfa tarafında stil işi yok.

## Yapılacaklar

1. `CaseDetails.tsx`: `RelatedCasesPanel`'i import et, `caseData` yüklendiğinde header
   kartının kapanışı ile `<Tabs ...>` arasında render et (`<RelatedCasesPanel caseId={parseInt(id)} />`;
   `onCountChange` gerekmiyor — panel kendi rozetini basıyor).
2. İkiz kodu sök: `relatedBrief` state'i, `RelatedCaseBrief` arayüzü, `fetchRelated`,
   `handleAddRelation`, `handleRemoveRelation`, `addRelationOpen` state'i, sayfadaki
   `AddRelationModal` mount'u ve importu, header'daki çip bölümü JSX'i. `useCases()`
   destructure'ından sayfada artık kullanılmayan `getRelatedCases` / `addCaseRelation` /
   `removeCaseRelation` çıkar (başka kullanım yerleri varsa bırak — sökmeden önce grep'le).
   Yalnız çip bölümünün kullandığı yardımcılar/ikonlar da (ör. `Link2`, `Gavel`, `Plus`,
   `getFileTypeMeta`, `getStatusStyle` — sayfadaki KOPYALARI) başka tüketicisi kalmadıysa silinir;
   lint kullanılmayan importu yakalar ama sen grep'le doğrula.
3. Panel bileşeninde değişiklik BEKLENMİYOR. Küçük bir uyum gerekirse (ör. dış boşluk
   için sarmalayıcı) sayfada çöz, bileşene dokunma; bileşene dokunmak zorunda kalırsan
   9 mevcut testi bozmadığını göster ve raporda gerekçele.

## Kabul kriterleri

- [x] `CaseDetails.tsx` `RelatedCasesPanel`'i import ediyor ve caseData yüklü durumda
      koşulsuz render ediyor (bir sekmenin/TabsContent'in İÇİNDE DEĞİL).
- [x] `grep -n "relatedBrief\|fetchRelated\|RelatedCaseBrief\|addRelationOpen" frontend/src/pages/CaseDetails.tsx`
      → 0 satır; sayfada `AddRelationModal` importu/mount'u yok (panel kendi modalını taşır).
- [x] Header kartındaki eski inline "Bağlantılı Davalar" çip bölümü kaldırıldı.
- [x] `RelatedCasesPanel.test.tsx`'teki 9 test DEĞİŞMEDEN yeşil (bileşen davranış
      sözleşmesi bu görevde sabittir).
- [x] Vitest paketi tam yeşil, eslint 0 hata, `tsc -b --force` temiz, `npm run build` geçer.
- [x] Rapor bölümünde: silinen satır aralıkları + panelin sayfadaki yeri (satır no) +
      varsa sapmalar gerekçeli.

## KAPSAMADIĞI doğrulama (insan adımı — gece oturumu tarayıcı açamaz)

Girişli tarayıcı turu: lokal stack'te (docker compose, 8080) kart 14142
(`D1.N_GOKCINAR.0004.IDARE.00000-2`) açılır — panelde "Sistemin bulduğu" altında 4 ilişki
(2024/519, 2020/489, 2020/488, 2020/490, hepsi "Yeniden Açılan" + TKU-788 gerekçesi)
görünmeli; 13364'te İcra + Tüketici paralelleri, 4442'de Bölge İdare bağı görünmeli.
DİKKAT: lokal frontend Docker imajdan servis edilir — turdan önce
`docker compose build frontend && docker compose up -d frontend` gerekir (bayat dist tuzağı).

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`
- `npm --prefix frontend run build`

## Rapor

**DURUM: TAMAM** — 2026-08-25 gece oturumu, worktree `gorev/G102` (taban: 462f96a).
Tek kod dosyası değişti: `frontend/src/pages/CaseDetails.tsx` (+10 / −133).

### Yapılanlar

1. **Montaj:** `RelatedCasesPanel` importu satır 29; mount satır 475-480 — header
   kartının kapanışı ile `{/* Tabs Container */}` arasında, her zaman görünür
   tam-genişlik bölüm. Sayfa tarafında `bg-[var(--bg-elevated)] border p-6`
   sarmalayıcısı içinde `<RelatedCasesPanel caseId={parseInt(id!)} />`; `onCountChange`
   verilmedi (panel rozetini kendisi basar). Sekme/TabsContent İÇİNDE DEĞİL; caseData
   yüklü ana return'de koşulsuz render.
2. **Söküm** (orijinal 462f96a satır numaralarıyla):
   - 4: `Link2`, `Building2`, `Plus` lucide importları (tek tüketicileri sökülen koddu)
   - 29: `AddRelationModal` importu → `RelatedCasesPanel` importu oldu
   - 90-99: sayfadaki `fileTypeMeta` + `getFileTypeMeta` kopyası (tek tüketici çip bölümüydü)
   - 101-110: `RelatedCaseBrief` arayüzü
   - 194: `useCases()` destructure'ından `getRelatedCases`/`addCaseRelation`/`removeCaseRelation`
     çıktı (hook'ta duruyorlar; panel + useCases testleri kullanmaya devam ediyor — grep'le doğrulandı)
   - 214-215: `relatedBrief` + `addRelationOpen` state'leri
   - 222-247: `fetchRelated`, `handleAddRelation`, `handleRemoveRelation`
   - 330: useEffect içindeki `fetchRelated()` çağrısı
   - 522-592: header kartındaki inline "Bağlantılı Davalar" çip bölümü (mevcut-dava çipi,
     çip listesi, boş-durum butonu dahil)
   - 1096-1101: sayfa düzeyindeki `AddRelationModal` mount'u
3. **Bilerek kalanlar:** `statusColors`/`getStatusStyle` — header'daki statü Badge'i
   (`style`) hâlâ kullanıyor; `Gavel`/`Scale`/`BarChart3`/`FileText`/`Sparkles` importlarının
   başka tüketicileri var. `CaseDetailsData.related_cases` alanı API yanıt tipinin parçası,
   dokunulmadı.

### Kararlar

- Görsel uyum sarmalayıcısı SAYFADA çözüldü (Yapılacaklar 3. madde uyarınca): panel çıplak
  fragment döndürür, sayfadaki diğer bölümler elevated kart olduğundan kabuk sayfaya verildi.
  `RelatedCasesPanel.tsx` ve `RelatedCasesPanel.test.tsx` 0 satır değişti.
- Yeni max-w cap eklenmedi; panel `main`'in mevcut akışında tam genişlik.
- Yeni test dosyası açılmadı: dosya kapsamı CaseDetails.tsx + (yalnız gerekirse) panel
  dosyalarıyla sınırlı; montajın canlı doğrulaması görev tanımında bilinçli insan adımı
  (tarayıcı turu) olarak ayrılmış durumda.

### Test sonuçları (tek turda yeşil)

- `npm --prefix frontend test`: **550 passed / 43 dosya** (RelatedCasesPanel'in 9 testi
  değişmeden dahil)
- `npm --prefix frontend run lint`: **0 hata**, 22 uyarı (hepsi önceden var; CaseDetails'te bulgu yok)
- `tsc -b --force`: temiz (exit 0; sahte çıplak `--noEmit` biçimi KULLANILMADI)
- `npm --prefix frontend run build`: geçti (5.3 sn; chunk-size uyarısı önceden var)
- Kabul grep'i: `relatedBrief|fetchRelated|RelatedCaseBrief|addRelationOpen` → 0 satır;
  `AddRelationModal` sayfada 0 satır.

### İzlenecekler

- İnsan adımı (yukarıdaki "KAPSAMADIĞI doğrulama"): lokal stack'te kart 14142 → 4 otomatik
  ilişki, 13364 → İcra+Tüketici paralelleri, 4442 → Bölge İdare bağı. Öncesinde
  `docker compose build frontend && docker compose up -d frontend` (bayat dist tuzağı).
- NOT: eski sayfa `getRelatedCases`'i bir kez çağırıyordu, şimdi tek çağrı panelde —
  istek sayısı değişmedi, yazar tekleşti.
