# G028 — Backend ölü katmanları sil: LogManager sınıfı, DatabaseManager, SyncLog, AnalysisCache, ölü route alias'ları

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/managers/log_manager.py, backend/database.py, backend/models.py, backend/api.py, backend/routes/config.py, backend/routes/processing.py, backend/udf_converter.py, backend/tests/
- **Dokunma:** backend/managers/config_manager.py (**setter'lar CANLI — silme!** bkz. aşağıda), backend/services/html_sanitizer.py, backend/pdf/format_converter.py (gece yapıldı, dokunma)

## Hedef

Plan FAZ C'nin backend ayağı. Silinecekler ve **her birinin sınırı**:

**1. `class LogManager` (`managers/log_manager.py:26-192`, ~187 satır).**
`init_log`/`complete_log`/`fail_log` çağrısı repoda YOK; yalnız `api.py:89` instantiate ediyor.
> **DOSYA SİLİNMEZ.** Aynı dosyadaki `TechnicalLogger` (`:214-329`) ve `mask_sensitive_data`
> **CANLI** — analyzer, file_utils, counter_manager, upload_queue, document_pipeline dahil
> 20+ modül ona bağlı. Yalnız `LogManager` sınıfı ve `api.py`'deki instantiate satırı gider.
> Not: `backend/tests/test_log_manager.py` `TechnicalLogger`'ı test ediyor — **bu test kalır.**

**2. `DatabaseManager` (`database.py`, ~78 satır)** — 0 çağıran.

**3. `SyncLog` + `AnalysisCache` modelleri (`models.py`) ve ilgili ölü kod.**
İki tablo da boş (0 satır) ve tek tüketicileri ölü kod.
> **TABLO DÜŞÜRÜLMEZ.** Yalnız ORM modeli ve ona bağlı kod silinir; `DROP TABLE` **YAPILMAZ**
> (prod'da veri kaybı riski + `migrate.py` fail-fast). Tablolar prod'da kalır, zararsızdır.
> Silme sonrası `Base.metadata`'nın tutarlı kaldığı ve `migrate.py`'nin bu tablolara atıf
> yapmadığı **doğrulanmalı** (`database.py` `_MIGRATIONS` listesini tara).

**4. Ölü route alias'ları (~45 satır):** 5× `/config/*` alias'ı, `/api/refresh`,
`/api/config/refresh`. Frontend'den hiç çağrılmıyor (doğrulandı).
> `/api/config/seed` de listede geçmişti — **ayrıca doğrula**, seed yolu kurulumda
> kullanılıyor olabilir. Şüphedeysen BIRAK.

**5. `udf_converter.py` ölü async yol:** `get_process_executor`, `convert_async`,
`convert_udf_to_pdf_async` — repo genelinde 0 dış çağıran.

> **PAZARLIKSIZ KURAL — bu görevin varlık sebebi:** taslak plan `config_manager` setter'larını
> "ölü" sanıyordu. Denetim çürüttü: 14 setter'ın 8'i **yalnızca**
> `managers/reference_lists.py:532` `getattr(config, spec.setter)` üzerinden çağrılıyor,
> adlar `LIST_REGISTRY`'de **string** olarak duruyor. Silinseydi arıza sinsi olurdu:
> `refresh_cache` çağrıları DB commit'inden SONRA ve dış `except` içinde → satır DB'ye yazılır,
> kullanıcıya "başarısız" denir.
> **Bu yüzden: her silme adayı için `getattr` / string-dispatch / `globals()` / dinamik import
> taraması ZORUNLU.** Bu görev dosyasındaki listeye güvenme, kendin doğrula.

## Kabul kriterleri

- [x] **Beklenen silme beyanı:** başta silinecek semboller + satır sayıları; sonda gerçekleşenle
      karşılaştırma → Rapor §1
- [x] Her silme adayı için `getattr`/string-dispatch/dinamik import taraması yapıldı ve
      **kanıtı raporda** (komut + çıktı). Şüpheli olan BIRAKILDI, gerekçesi yazıldı → §2
      (`/api/config/seed` bırakıldı)
- [x] `TechnicalLogger` ve `mask_sensitive_data` **dokunulmadan** duruyor;
      `tests/test_log_manager.py` yeşil → §4
- [x] `config_manager` setter'larına **DOKUNULMADI** (`git diff` ile kanıtlı) → §2(b)
- [x] `SyncLog`/`AnalysisCache` için **DROP TABLE yapılmadı**; `Base.metadata` tutarlı,
      `migrate.py` bu tablolara atıf yapmıyor (tarandı, kanıt raporda) → §3
- [x] Konteyner **temiz açılıyor**: `migrate.py` + uvicorn başlangıcı hatasız
      (silme `api.py` açılış yolunu etkiliyor — bu maddenin kanıtı zorunlu) → §6
- [x] **Net satır deltası NEGATİF** → **−300** (+24 / −324)
- [x] pytest tabanı görev başındaki HEAD'de ölçülür (beklenen 975+2); **altına düşmek yasak**.
      Düşerse: hangi test, neden, beyan edilmiş miydi → taban 975+2 ölçüldü, sonuç 975+2
- [x] `ruff check .` ve `mypy` temiz → §5

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- Açılış dumanı: `docker compose exec -T backend python -c "import api"` (ya da eşdeğeri)

## Rapor

DURUM: TAMAM

### 1. Beklenen silme beyanı (uygulamadan ÖNCE, taramalar bitince yazıldı)

| # | Sembol | Yer | Beklenen satır |
| --- | --- | --- | --- |
| 1 | `class LogManager` + ölü importlar (`requests`, `get_graph_token`, `_get_site_and_drive_id`, `_headers`, `GRAPH`, `LOG_LIST_NAME`) | `managers/log_manager.py` | ~152 |
| 2 | `class DatabaseManager` + ölü importlar (`json`, `time`, `datetime/timedelta`, `Optional`) | `database.py` | ~82 |
| 3 | `SyncLog` + `AnalysisCache` ORM modelleri | `models.py` | ~24 |
| 4 | 5× `/config/*` GET alias'ı | `routes/config.py` | 5 |
| 5 | `/api/refresh` + `/api/config/refresh` alias'ları | `routes/processing.py` | 2 |
| 6 | `_process_executor`, `get_process_executor`, `convert_async`, `convert_udf_to_pdf_async`, `import asyncio` | `udf_converter.py` | ~30 |
| — | `LogManager()` instantiate + import | `api.py` | 6 |
| — | **`/api/config/seed`** | — | **SİLİNMEYECEK** (aşağıda gerekçe) |

**Gerçekleşen** (`git diff --numstat`, `.claude/` hariç):

```
1	6	backend/api.py
1	83	backend/database.py
12	170	backend/managers/log_manager.py
7	23	backend/models.py
0	5	backend/routes/config.py
3	3	backend/routes/processing.py
0	34	backend/udf_converter.py
```

Toplam **+24 / −324 → net −300 satır**. Beklentiyle uyumlu; sapmalar: `log_manager.py`
(−158 net) beklenenden büyük çünkü dosya docstring'i de LogManager'ı anlatıyordu ve
yeniden yazıldı; `models.py` (−16 net) beklenenden küçük çünkü "tablolar DB'de duruyor"
notu için 7 satırlık modül docstring'i eklendi; `processing.py` net 0 çünkü 2 alias
satırının yerine niçin tek yol kaldığını anlatan 2 satır yorum kondu.

### 2. Dinamik dispatch taraması (kabul kriteri — kanıt)

Görev dosyasının "PAZARLIKSIZ KURAL"u gereği her aday, **düz ad araması + `getattr` /
`globals()` / `importlib` / `__import__` taraması** ile ayrı ayrı doğrulandı.

**(a) Sembol adı taraması** — `Grep "LogManager|log_manager|init_log|complete_log|fail_log"`
(repo geneli): canlı kodda tek üretim atfı `api.py:69` (import) + `api.py:89` (instantiate).
Diğer 20+ isabetin tamamı `TechnicalLogger` import'u ya da doküman/arşiv metni.
`init_log`/`complete_log`/`fail_log` **çağrısı sıfır**.

`Grep "DatabaseManager|SyncLog|AnalysisCache|analysis_cache|sync_log"`: `DatabaseManager`
tek isabet = kendi tanımı (`database.py:628`); `AnalysisCache` isabetleri yalnız kendi
tanımı + `DatabaseManager` gövdesi (ölü kodun ölü kodu); `SyncLog` tek isabet = kendi
tanımı. `sync_logs` tablosuna ORM dışı erişim yok.

`Grep "get_process_executor|convert_async|convert_udf_to_pdf_async|ProcessPoolExecutor"`:
tüm isabetler `udf_converter.py` içi (kendi tanımları) + 1 arşiv dokümanı. Dış çağıran 0.

**(b) `getattr`/`globals()`/`importlib`/`__import__` taraması** (`backend/`, testler hariç,
54 isabet). Tamamı okundu; hiçbiri bu görevdeki adları string olarak dispatch etmiyor.
İki kategoriye ayrılıyor:
- SQLAlchemy kolon/alan erişimi (`getattr(spec.model, spec.key)`, `getattr(case, field)`) —
  model *kolonlarına* bakar, model *sınıflarına* değil;
- `getattr(sys, 'frozen', False)`, `getattr(response, "usage_metadata", None)` gibi
  üçüncü-parti nesne yoklamaları.

**Tek kritik isabet** `managers/reference_lists.py:532` → `getattr(config, spec.setter)`:
görev dosyasının uyardığı `config_manager` setter'ları. **DOKUNULMADI** — `git diff`
çıktısında `managers/config_manager.py` ve `managers/reference_lists.py` **hiç yok**.

**(c) Route alias'ları — üç bağımsız kanıt:**
1. Frontend taraması: `Grep` ile `frontend/src` (dist hariç) → yalnız `/api/config/*`
   biçimleri çağrılıyor (`hooks/useConfig.ts:114-118, 186-194`). `/config/*` çağrısı 0.
2. **Konteyner nginx'te `/config` location'ı YOK** (`nginx.conf` location listesi:
   `/`, `= /healthz`, `/api`, `/process`, `/confirm`, `/preview-email-body`,
   `/preview-client-email-body`, `/refresh`). Yani `/config/*` alias'ları SPA'nın
   `try_files`'ına düşüyordu — tarayıcıdan **erişilebilir bile değildi.**
3. Backend testlerinde atıf 0 (`Grep '"/config/|api/refresh|convert_async|DatabaseManager'`
   → `backend/tests` içinde "No matches found").

`/refresh` **KALDI**: `frontend/src/lib/api.ts:72` çağırıyor, `nginx.conf:109` proxy'liyor,
`vite.config.ts:40` dev proxy'sinde var. Yalnız `/api/refresh` + `/api/config/refresh`
alias'ları gitti. Canlı route tablosundan doğrulandı:
`refresh yollari: ['/refresh']`, `/config/ alias kalan: []`.

**(d) ŞÜPHELİ → BIRAKILDI: `/api/config/seed`.** Görev dosyası "ayrıca doğrula, şüphedeysen
BIRAK" diyordu. Frontend'den çağrılmıyor (tarandı) **ama ölü değil**: `require_admin`
korumalı, `Base.metadata.create_all` + `seed_all_lists()` koşuyor, yani boş/eksik referans
listelerini elle onarma yolu. Açılıştaki otomatik `seed_all_lists()` (`api.py:141-147`)
çağrısından bağımsız bir operatör kurtarma kapısı; kazanç 10 satır, risk "kurulum yolunu
kesmek". BIRAKILDI. Canlı route tablosunda duruyor: `seed: ['/api/config/seed']`.

### 3. `SyncLog`/`AnalysisCache` — DROP TABLE yapılmadı, şema tutarlı

- Silinen **yalnız ORM modelidir**; `DROP TABLE` **yok** (diff'te tek DDL satırı yok).
- `Base.metadata` tutarlı: iki modele de `ForeignKey`/`relationship` ile bağlı başka model
  yok (`Grep "SyncLog|AnalysisCache"` → yalnız kendi tanımları).
- `migrate.py` bu tablolara **atıf yapmıyor**: `Grep "sync_log|analysis_cache|SyncLog|AnalysisCache"`
  → `backend/migrate.py`'de "No matches found". `database.py::_MIGRATIONS` ve
  `_TRGM_INDEXES` listelerinde de geçmiyorlar (aynı grep, `database.py` isabetleri yalnız
  silinen `DatabaseManager` gövdesindeydi).
- Canlı doğrulama: `migrate.py` konteynerde koşturuldu → `✅ Tables created/verified.` +
  `Running migrations for postgresql` + `✅ Database ready!`, hata yok. `create_all`
  artık bu iki tabloyu üretmez ama **mevcut tablolar olduğu gibi duruyor** (silinmedikleri
  için migrasyon onları görmezden gelir).
- `models.py` başına kalıcı bir not eklendi: DB'de artık modeli olmayan iki tabloyu gören
  gelecekteki okuyucu, bunun bilinçli olduğunu orada görecek.

### 4. `TechnicalLogger` / `mask_sensitive_data` dokunulmadı

`log_manager.py` diff'i yalnız (i) modül docstring'i, (ii) `class LogManager` bloğu,
(iii) artık kullanılmayan `requests` + sharepoint auth importları ve `GRAPH`/`LOG_LIST_NAME`
sabitleri, (iv) `# --- TECHNICAL LOGGER MERGE ---` başlığının adı ("MERGE" edilecek bir şey
kalmadı) satırlarına dokunuyor. `TechnicalLogger` gövdesi ve `mask_sensitive_data` **bit
düzeyinde aynı**. `logger = logging.getLogger("LogManager")` **bilinçli KORUNDU**:
`_sync_to_cloud_locked`'ın except'i onu kullanıyor ve logger adı bir grep sözleşmesidir —
yeniden adlandırmak silme görevinin kapsamı değil.

`os`, `socket`, `datetime` importları da korundu (TechnicalLogger `os.getpid`/`os.getenv`,
`socket.gethostname`, `datetime.now` kullanıyor).

`tests/test_log_manager.py` yeşil (tüm paket yeşil, aşağıda).

### 5. Test sonuçları

| Kapı | Görev başı (HEAD `ffd50b8`) | Görev sonu |
| --- | --- | --- |
| `pytest` | **975 passed, 2 skipped** (31.13s) | **975 passed, 2 skipped** (33.17s) |
| `ruff check .` | — | `All checks passed!` |
| `mypy` | — | `Success: no issues found in 29 source files` |

Taban korundu, tek test kaybı yok (kabul kriteri: "altına düşmek yasak").

**Konteynerin değişikliği gerçekten gördüğü doğrulandı** (yanıltıcı yeşil tuzağı):
`docker compose exec -T backend python -c "..."` →
`LogManager attr: False`, `DatabaseManager attr: False`,
`SyncLog attr: False AnalysisCache attr: False`.

### 6. Açılış dumanı (kabul kriteri — zorunlu kanıt)

`api.py`'nin açılış yoluna dokunulduğu için üç kademeli doğrulandı:

1. `python -c "import api"` → `api import OK - route sayisi: 128` (import zinciri sağlam).
2. `python migrate.py` → `✅ Tables created/verified.` … `✅ Database ready!` (hatasız).
3. `docker compose restart backend` → `Up (healthy)`; loglarda **iki worker da**
   `Application startup complete.`, lider worker'da (pid 10) APScheduler
   (`generate_daily_reports`, `retry_pending_conversions`) + upload outbox worker'ı +
   catch-up thread başladı, takipçi worker (pid 11) yalnız refresh thread'i koştu —
   lider kilidi beklendiği gibi çalışıyor. **Başlangıçta tek ERROR/WARNING yok.**

### 7. Alınan kararlar ve gerekçeleri

1. **`/api/config/seed` bırakıldı** — §2(d). Görev dosyasının açık talimatı + kendi
   doğrulamam aynı yöne çıktı: admin-korumalı kurtarma yolu, ölü değil.
2. **`logger` adı `"LogManager"` bırakıldı** — canlı `TechnicalLogger` yolu kullanıyor;
   ad değişimi log grep'lerini ve olası izleme sorgularını sessizce bozardı.
3. **`log_manager.py`'de dosya-ortası importlar taşınmadı** — `pyproject.toml`
   `per-file-ignores` bu dosyaya E402 muafiyeti veriyor; taşımak sırf kozmetik olur ve
   silme diff'ini gürültüyle büyütürdü. Yerine niçin orada durdukları yorumla açıklandı.
4. **`models.py`'ye modül docstring'i eklendi (+7 satır)** — net deltayı azalttı ama
   "DB'de modeli olmayan iki tablo" sorusunun cevabını koda gömmek, gelecekteki bir
   okuyucunun yanlışlıkla DROP etmesinden ucuz.

### 8. NOT (kapsam dışı bulgular — düzeltilmedi)

- **`backend/scripts/migrate_from_staging.py`** `analysis_cache` tablosuna **ham SQL** ile
  yazıyor (`:169-181`, `:232`). ORM modeline bağlı olmadığı için bu silme onu KIRMAZ ve
  tablo da yerinde duruyor. Ancak bu script artık şemada karşılığı olmayan bir tabloyu
  taşıyor: staging→local tek seferlik göç aracı, muhtemelen tümüyle ölü. Kapsam dışı
  (`scripts/` bu görevin dosya listesinde yok) → dokunulmadı, bir sonraki temizlik
  turunda değerlendirilmeli.
- **`backend/REFACTOR_PLAN.md`** hâlâ `api.py:58 → LogManager` ve
  `routes/processing.py:499 → from managers.log_manager import LogManager` satırlarını
  taşıyor (ikincisi zaten önceden temizlenmişti — dosya bayat). Tarihsel plan belgesi,
  kapsam dışı.
- **`pyproject.toml`'daki `"managers/log_manager.py" = ["E402"]`** muafiyeti hâlâ gerekli
  (dosya-ortası importlar duruyor, §7.3) — kaldırılacak bir şey yok, bilgi amaçlı not.
