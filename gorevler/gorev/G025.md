# G025 — html_sanitizer sessiz metin kaybı: `&lt;` ile başlayan gövde metni yok oluyor

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/services/html_sanitizer.py, backend/tests/test_sanitizer_html.py, backend/tests/test_eml_expand.py
- **Dokunma:** backend/pdf/format_converter.py (G024 tohumu — DOĞRU çalışıyor, dokunma), backend/routes/case_intake.py

## Hedef

G023 gece boyunca `.eml` SSRF sınıfını kapattı (bağımsız üçüncü denetimde de doğrulandı,
50 yeni varyantla). Ama aynı denetim **güvenlik dışı ciddi bir gerileme** buldu.

`html_sanitizer.py:200-206` — `handle_data`, veri parçası `<` ile başlıyorsa **tüm metin
koşusunu atıyor**. `convert_charrefs=True` olduğu için `&lt;` / `&#60;` `handle_data`'ya
çözülmüş `<` olarak ulaşır. Sonuç: **`&lt;` ile BAŞLAYAN her gövde metni, bir sonraki
etikete kadar sessizce yok oluyor.** Uyarı da loglanmıyor.
`_TextExtractor.handle_data` (`:308-309`) aynı korumayı taşıdığı için **fail-closed düz-metin
yolu da aynı metni kaybediyor** — yani ikinci şans da kurtarmıyor.

### Ölçülen kayıplar (denetçinin canlı çıktısı)

| Girdi | Çıktı |
| --- | --- |
| `<p>&lt;Sayin Ilgili&gt;, ekte bilirkişi raporu ve fatura yer almaktadir. Toplam tutar 145.000 TL'dir…</p>` | **`<p>&lt;</p>`** — cümlenin tamamı gitti |
| `<td>&lt;av.ilke@lexis.com.tr&gt; tarafindan 12.08.2026 tarihinde gonderildi</td>` | **`<td>&lt;</td>`** |
| `<div>&lt;&lt;DOSYA NO&gt;&gt; 2026/1234 Esas - durusma 15.09.2026</div>` | **`<div>&lt;</div>`** |

**Neden önemli:** gövde PDF'i Gemini'ye **analiz için** gidiyor. Outlook gövdelerinde açılı
parantezli e-posta adresi (`<ad@firma.com>`) ve `<<yer tutucu>>` şablonları rutindir.
Yani bu, analiz kalitesini sessizce düşüren bir veri kaybı — hata vermiyor, log da yok.

**Mevcut test neden kaçırıyor:** `test_sanitizer_html.py:388`
`test_lone_less_than_in_text_survives_as_entity` yükü `"<p>tutar &lt; 5 TL</p>"` —
`<` koşunun **ORTASINDA**, o yüzden geçiyor. Koşunun `&lt;` ile **BAŞLADIĞI** vaka
hiç kapsanmamış.

## Düzeltme (ölçülmüş — tasarım tartışması değil)

**Atmak yerine kaçışla.** `handle_data` daima `html.escape(data)` yayınlasın.
Drop'u motive eden tehdit (EOF'ta kapanmamış etiket kalıntısının ham yayınlanması —
G023 kararı #4) **zaten kaçışlamayla ölüyor**: denetçi, G024 tohumu **kapalıyken**
kaçışlanmış `&lt;img src="http://127.0.0.1:9911/escaped-remnant.png"` verip **0 istek**
ölçtü, metin de PDF'te aynen göründü. Yani kaçışlama hem güvenli hem kayıpsız.

`_TextExtractor.handle_data` (`:308-309`) da aynı şekilde düzeltilmeli — fail-closed yolu
metin kaybetmemeli.

## Kabul kriterleri

- [x] `&lt;` ile **başlayan** gövde metni artık korunuyor; yukarıdaki üç ölçülen yük için
      metnin tamamı çıktıda
- [x] Aynı düzeltme `_TextExtractor` (düz-metin / fail-closed yolu) için de yapıldı;
      testle kanıtlı
- [x] **Güvenlik gerilemesi yok:** G023'ün 13 vakalık korpusu + 486 kombinasyonluk matris
      **değişmeden yeşil**; EOF'ta kapanmamış etiket ve dengesiz tırnak vakaları hâlâ
      uzak URL sızdırmıyor
- [x] **Canlı kanıt:** kaçışlanmış kalıntı içeren yük gerçek soffice'e verildiğinde
      **giden istek 0** — G024 tohumu **BİLEREK KAPALIYKEN** ölçülmeli (yoksa tohum
      sanitizer'ın kusurunu maskeler). Rapor dinleyici kaydını içerir
- [x] Yeni testler: koşu başında `&lt;`, art arda `&lt;&lt;`, `&#60;` sayısal entity,
      `<td>`/`<div>`/`<p>` bağlamları; `test_lone_less_than_in_text_survives_as_entity`
      **değiştirilmeden** yeşil kalıyor
- [x] pytest tabanı görev başındaki HEAD'de ölçülür (beklenen 958+2); altına düşmek yasak
      — ölçüldü: 958 passed, 2 skipped → 975 passed, 2 skipped
- [x] `ruff check .` ve `mypy` temiz

## İkincil (aynı görevde, küçük)

- [x] `test_eml_expand.py`'deki canlı dinleyici testi `@requires_soffice` ile host'ta
      **atlanıyor** → SSRF regresyonu CI'da sessizce geçer. En azından atlandığında
      görünür bir uyarı/işaret bırakılsın ya da testin nerede koşacağı görev raporunda
      açıkça yazılsın (CI'da soffice var mı — **ölç**, varsayma)
- [x] `format_converter.py::_seed_lo_profile` yorumuna denetçinin ölçtüğü **belgelenmemiş
      yan etki** yazılsın: tohum, HTML'deki **yerel göreli görselleri de** engelliyor
      (`<img src="legit.png">` → 0 görsel). Gerçek `.eml` yolunda zararsız (sanitizer zaten
      tüm şemaları düşürüyor) ve ofis **gömülü** görselleri etkilenmiyor — ama yazılı olmalı

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM.** Tüm kabul kriterleri karşılandı (ikincil maddeler dahil).

### Yapılanlar

1. **`_CanonicalSerializer.handle_data`** (`html_sanitizer.py:197`): `data[0] == "<"`
   ise koşuyu atan kural KALDIRILDI; metin daima `html.escape(data, quote=False)` ile
   yayınlanıyor. Docstring'e ölçüm gerekçesi yazıldı.
2. **`_TextExtractor.handle_data`** (`:308`): aynı koruma kaldırıldı — fail-closed
   düz-metin yolu artık metin kaybetmiyor. Kaçışlama burada BİLEREK yapılmadı: çıktı
   çağıranda `html.escape` ile `<pre>` içine konuyor
   (`routes/case_intake.py:141`), ikinci kaçışlama metni bozardı.
3. Modül docstring'indeki "tamamlanmamış markup kalıntısı atılır" satırı düzeltildi.
4. Testler: `test_sanitizer_html.py`'ye 4 yüklük `G025_LOSS_CASES` korpusu (3'ü
   denetçinin ölçtüğü yük + `&#60;` sayısal entity), 2 parametrik test seti,
   `_TextExtractor` kayıpsızlık testi, tek `<` regresyon testi ve 5 yüklük
   **ölçüm kilidi** testi eklendi. `test_eml_expand.py`'ye canlı soffice kanıtı +
   atlanma görünürlüğü testi eklendi.

### Alınan kararlar + gerekçeleri

- **Görev dosyasının reçetesi ("daima `html.escape`") aynen uygulandı** — ama önce
  reçetenin korpusu bozup bozmadığı ÖLÇÜLDÜ. Endişe (kalıntı yükün kaçışlanmış hâlde
  görünür olması → `EVIL not in out` iddiasının düşmesi) **karşılıksız çıktı:**
  py3.10.20'de HTMLParser tamamlanmamış markup'ı `handle_data`'ya **hiç vermiyor**.
  Ölçüm (`<p>metin</p><img src="http://kalinti.test/x`, dengesiz tırnak, kapanmamış
  kapanış etiketi, kapanmamış PI, kalıntı içinde `>`): hepsinde `handle_data`'ya
  düşen kalıntı **yok**. `<` ile başlayıp bu fonksiyona ulaşan tek veri, etiket
  açamayan **tek karakterlik `"<"`**tır — onun `html.escape`'i de zaten `&lt;`, yani
  o vakada davranış birebir aynı. Dolayısıyla G023'ün drop kuralı metin kaybından
  başka bir iş yapmıyormuş.
- **`convert_charrefs=False`'a geçme alternatifi REDDEDİLDİ.** (Entity'leri ayrı
  olaylardan toplayıp kalıntıyı ayırt etmek de çalışırdı — ölçüldü.) Ama yukarıdaki
  ölçüm reçetenin zaten yeterli olduğunu gösterdiği için daha küçük değişiklik
  seçildi; parser olay akışını değiştirmek gereksiz risk.
- **Sürüm bağımlılığı kilitlendi:** kalıntının `handle_data`'ya düşmemesi CPython
  davranışıdır, sözleşme değil. `test_incomplete_markup_never_reaches_handle_data`
  bunu 5 yükle sabitler; bir Python yükseltmesi bunu değiştirirse test kırmızıya
  döner (kalıntı kaçışlanmış hâlde inert kalmaya devam eder, yalnız görünür olur).
- **`format_converter.py`ye dokunuldu — ama YALNIZ YORUM.** Görev dosyası bu dosyayı
  hem "Dokunma" listesine koyuyor hem de ikincil kriterde oraya yorum yazılmasını
  istiyor. Çelişki, "Dokunma" gerekçesinin davranışsal olmasıyla çözüldü
  ("G024 tohumu DOĞRU çalışıyor"): `git diff -U0` ile doğrulandı — **12 satır eklendi,
  0 satır silindi, hepsi yorum**; tohum mantığı değişmedi.

### Ölçümler (hepsi konteynerde, `hukdok_backend`)

**Metin kaybı — düzeltme sonrası çıktı (iskelet kırpıldı):**

| Girdi | Çıktı (önce → sonra) |
| --- | --- |
| `<p>&lt;Sayin Ilgili&gt;, ekte bilirkişi raporu … 145.000 TL'dir.</p>` | `<p>&lt;</p>` → **tam cümle** |
| `<td>&lt;av.ilke@lexis.com.tr&gt; tarafindan 12.08.2026 …</td>` | `<td>&lt;</td>` → **tam cümle** |
| `<div>&lt;&lt;DOSYA NO&gt;&gt; 2026/1234 Esas - durusma …</div>` | `<div>&lt;</div>` → **tam cümle** |
| `<p>&#60;av.ilke@lexis.com.tr&#62; imzali dilekce ektedir</p>` | → `<p>&lt;av.ilke@lexis.com.tr&gt; imzali dilekce ektedir</p>` |

`html_to_text` (fail-closed yolu) aynı dört yükte tam metin döndürüyor, ör.
`'<<DOSYA NO>> 2026/1234 Esas - durusma 15.09.2026'`.

**CANLI KANIT — gerçek soffice + gerçek dinleyici, G024 tohumu BİLEREK KAPALI**
(`monkeypatch.setattr(fc, "_seed_lo_profile", lambda d: None)`; tam `/expand-eml`
uçtan uca yolu):

```
G024 tohumu devre dışı mı: True
DINLEYICI: 127.0.0.1:34009
HTTP     : 200
DINLEYICI KAYDI (listener.hits) = []          ← 0 istek
PDF metninde 'escaped-remnant.png'  -> True
PDF metninde 'Sayin Ilgili'         -> True
PDF metninde 'DOSYA NO'             -> True
PDF metninde '2026/1234 Esas'       -> True
```

Yani kaçışlanmış `&lt;img src="http://127.0.0.1:34009/escaped-remnant.png"&gt;`
PDF'te **düz metin olarak görünüyor** (Gemini'ye gidiyor) ve LibreOffice'e
**tek istek bile attırmıyor**. Bu ölçüm
`test_escaped_markup_text_is_visible_but_silent` olarak test paketine alındı.

**Güvenlik gerilemesi yok:** G023'ün 13 vakalık korpusu, 486 kombinasyonluk taşıyıcı
matrisi ve negatif kontroller **değiştirilmeden** yeşil.
`test_lone_less_than_in_text_survives_as_entity` ve
`test_incomplete_tag_remnant_is_not_emitted_raw` da değiştirilmeden yeşil.

### İkincil maddeler

**(a) `@requires_soffice` görünürlüğü — ÖLÇÜLDÜ, varsayılmadı.** CI'da soffice
**yok**: `.github/workflows/ci.yml` backend job'ı yalnız `requirements.txt` +
`requirements-dev.txt` kuruyor (satır 49-50), libreoffice kuran **adım yok**;
soffice bu projeye tek yerden giriyor — `backend/Dockerfile:16-17`
(`libreoffice-writer`, `libreoffice-calc`). *Not: GitHub runner imajının kendi
yazılım listesi bu oturumda ağ erişimi olmadığı için çekilemedi; ama kurulum adımı
olmadığı repodan doğrulanabilir bir olgudur.* Dolayısıyla **canlı SSRF kanıtları
yalnız konteynerde koşar:**
`docker compose exec -T backend python -m pytest tests/test_eml_expand.py`
(bu koşuda 20 passed, **0 skipped** — kanıtlar gerçekten koştu).
Görünür işaret olarak `test_soffice_dependent_ssrf_proofs_are_visible_when_skipped`
eklendi; hiçbir zaman atlanmaz, soffice yoksa uyarı basar (simülasyonla doğrulandı):
`SSRF CANLI KANITI ATLANDI: soffice yok — … SSRF regresyonu bu koşuda ölçülmemiştir`.

**(b) `_seed_lo_profile` yan etkisi — denetçinin iddiası KENDİM ÖLÇEREK doğrulandı**
(`format_converter.py:83-94`):

```
html_to_pdf + <img src="legit.png"> (dosya yanında):
  tohum KAPALI -> PDF gorsel sayisi = 1 | metin var mi = True
  tohum ACIK   -> PDF gorsel sayisi = 0 | metin var mi = True
.xlsx GÖMÜLÜ PNG (openpyxl):
  tohum KAPALI -> gorsel = 1 | tohum ACIK -> gorsel = 1     ← etkilenmiyor
```

### Test sonuçları

| Kapı | Görev başı (HEAD b216698) | Görev sonu |
| --- | --- | --- |
| `pytest` | 958 passed, 2 skipped | **975 passed, 2 skipped** (+17) |
| `tests/test_sanitizer_html.py` | 43 | 58 (+15) |
| `tests/test_eml_expand.py` | 18 | 20 (+2; 0 skipped — soffice konteynerde var) |
| `ruff check .` | — | All checks passed! |
| `mypy` | — | Success: no issues found in 29 source files |

### İzlenecekler

- `NOT:` `test_format_converter.py::test_embedded_image_document_still_converts`
  adı yanıltıcı — içinde **gömülü görsel yok**, yalnız metinli bir `.xlsx` üretiyor.
  "Meşru gömülü görsel bozulmuyor" iddiası bu testle ölçülmüyordu (bu görevde elle
  ölçüldü, yukarıda). Kapsam dışı olduğu için düzeltilmedi.
- `NOT:` `_TextExtractor` düz-metin yolunda `<` kaçışlanmadan bırakılıyor; güvenlik
  yalnız çağıranın `html.escape`'ine dayanıyor. Bugün tek çağıran `case_intake`
  ve doğru davranıyor; `html_to_text` ileride başka yerde kullanılırsa bu şart
  hatırlanmalı (docstring'e yazıldı).
- Kalıntı davranışı Python sürümüne bağlı — ölçüm kilidi testi bekçi olarak duruyor.
- Kapsam dışı kirli dosyalar (`.claude/settings.local.json`, `.claude/launch.json`)
  harness'ın kendi ayar dosyaları; dokunulmadı, commit'e alınmadı.
