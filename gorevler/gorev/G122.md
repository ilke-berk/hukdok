# G122 — Docs: Bilgilendirme sürüm 1.1 (DB-2026-001…010 sonrası) + mimari dokümanlar

- **Bant:** docs
- **Bağımlı:** G120,G121
- **Dosya kapsamı:** docs/veri-teslim/BILGILENDIRME_2026-09-03.md (yerinde sürüm 1.1;
  dosya adı DEĞİŞMEZ — veri ekibine gönderilen yol sabit), docs/veri-teslim/SOZLESME.md,
  docs/mimari/veri-teslim-hatti.md, docs/mimari/dava-acma-akisi.md, CLAUDE.md (yalnız
  veri teslim paragrafı gerekiyorsa)
- **Dokunma:** backend/**, frontend/** (docs bandı kod değiştirmez), docs/arsiv/**

## Hedef

Veri ekibinin 04.09.2026 Format Değişiklik Bildirimi (REV-2, on kalem DB-2026-001…010)
ve HukuDok'un aynı gün verdiği cevap ("hazır, bırakın" + beş cevap) sonrasında bizim
dokümanlarımız bayat kaldı. ALTIN KURAL: her iddia koddan okunarak yazılır
(`backend/scripts/hukdok_aktarim.py` SUTUN_ADAYLARI, `seed_data.py` liste sabitleri,
`services/teslim_cevap.py` HAVUZ_LISTE_ESLEMESI). Tarihli anlatı `docs/arsiv/`e değil,
bu yaşayan belgelerin ilgili bölümlerine kısa şerh olarak girer.

Bildirimin kalemleri ve bizdeki karşılığı (planlayıcının 04.09 kod kontrolü — işçi
yeniden doğrular):

| Kalem | Bizde | Doküman etkisi |
| --- | --- | --- |
| DB-001 kusur listesi 9 değer | seed'lendi (9608031, `seed_data.ALLEGED_FAULTS`) | "alleged_faults seed'lenmez" ifadeleri kalkar; kapalı liste tablosu 9 değer |
| DB-002 Müvekkil Tipi + Hizmet Türü | G119 şema + G120 aktarım + G121 UI | §3.2 okunan başlık listesi +2; kapalı liste tablosuna iki liste (5 + 9) |
| DB-003 on dört sütun gelmiyor (on biri kaynaktan silindi) | kod dokunulmadı; eksik sütun "bu teslimde yok" | §3.2'de bu başlıklar "artık gelmiyor (04.09)" şerhiyle işaretlenir; Arabuluculuk Numarası kimlik alanı DEĞİL, köprü Dosya No |
| DB-004 Karar_Asamalari ilk kez | zaten okunuyor; `Önceki` etiketi ONCEKI esas olarak işlenir (`ASAMA_ONCEKI`) | Aşama değer havuzu tablosuna `Önceki` (kabul edilir, karar aşaması değil) eklenir — veri ekibinden bu satırları GÖNDERMESİ istendi |
| DB-005 karar durumu havuzu dağınık | kanonik listemiz cevapla gönderildi | §6'ya "kanonik listemiz = seed_data dört liste" atfı; lehe/aleyhe ekseni yok notu |
| DB-006 Olay Türü / Hükümdeki Rol henüz üretilmiyor | alanlar açık, boş meşru | §3.2'de "veri ekibinde henüz üretilmiyor (04.09)" şerhi |
| DB-007 Dava Türü Alt Kırılımı → Uzmanlık Alanı | iki başlık da tanınıyor (`hukdok_aktarim.py:189`) | §3.2 başlık listesi: iki ad birlikte |
| DB-008 ad alanları İlk Harf Büyük | anahtar harf duyarsız (`party_check.normalize_person_name`) | §5 tablosu: büyük/küçük "SERBEST" satırı ad alanlarını da kapsar, açık yaz |
| DB-009 tarih/tutar tipi | bizim istediğimiz biçim | değişiklik yok, teyit cümlesi |
| DB-010 dört açık havuz kanonik yazım | metin alanı, sözlük yok | değişiklik yok |

## Yapılacaklar

1. **BILGILENDIRME_2026-09-03.md → sürüm 1.1:** başlıktaki sürüm + "değişiklik geçmişi"
   satırı; §3.2 okunan başlık listesi (sayıyı KODDAN say — `SUTUN_ADAYLARI`; 37 bayat
   olabilir), §5 kırıcı/bildirilmeli tablosu, §6 örnek bildirim no'su (DB-2026-001 artık
   gerçek bir bildirim — örnek numarayı DB-2026-0XX gibi değiştir), §9 makine-okur özet
   (`okunan_baslik`, kapalı listeler, `bildirim_kanali`). Veri ekibine giden belge
   olduğu için üslup: ikinci çoğul, kısa, tablo.
2. **SOZLESME.md §6:** kapalı listeli alanlar cümlesine Müvekkil Tipi + Hizmet Türü;
   kısa kontrol listesi (§9) güncel.
3. **veri-teslim-hatti.md:** `HAVUZ_LISTE_ESLEMESI` satırı (G119 iki listeyi eşlemeye
   ekledi mi? — koddan bak; eklemediyse "eşlemede yok, fark raporu üretmez" yaz),
   aktarım yazılan alanlar bölümü (+2), DB-2026 bildirimi + cevap tarihli şerh.
4. **dava-acma-akisi.md:** G119 zaten şema bölümüne iki alanı yazdı — çelişki/mükerrer
   varsa tekle; kapalı liste envanteri (kaç liste, hangileri seed'li) koddan sayılıp
   düzeltilir.
5. **CLAUDE.md:** yalnız veri teslim paragrafı bayat bir sayı/iddia taşıyorsa düzelt;
   başka dokunma.

## Kabul kriterleri

- [ ] Bilgilendirme'deki okunan başlık sayısı ve listesi `SUTUN_ADAYLARI` ile birebir
      (işçi raporunda karşılaştırma komutu + sonucu).
- [ ] Kapalı liste tablosu: alleged_faults 9 · client_types 5 · service_types 9 ·
      event_types 3 · judgment_roles 4 · dört karar listesi — sayılar seed sabitlerinden.
- [ ] Hiçbir dokümanda "alleged_faults seed'lenmez / boş doğar" ifadesi güncel durum olarak
      kalmadı (`grep -rn "seed'lenmez" docs/ CLAUDE.md` temiz ya da tarihli şerh).
- [ ] `docs/arsiv/`e dosya EKLENMEDİ; kod dosyası DEĞİŞMEDİ.
- [ ] Dosya adları değişmedi (BILGILENDIRME_2026-09-03.md yolu veri ekibinde).

## Doğrulama

- `git diff --stat` yalnız docs/ + CLAUDE.md
- Raporda: başlık sayısı karşılaştırma çıktısı (kod vs doküman)

## Rapor

DURUM: TAMAM (2026-09-04, gece kuyruğu işçisi; worktree `gorev/G122`, önceki HEAD 88409da)

**Yapılanlar** (yalnız docs; kod ve `docs/arsiv/` dokunulmadı, dosya adları değişmedi):

1. `docs/veri-teslim/BILGILENDIRME_2026-09-03.md` → **sürüm 1.1** (yerinde): başlık +
   değişiklik geçmişi tablosu; §3.2 okunan başlık listesi koddan yeniden sayıldı — **42 alan
   / 54 kabul edilen yazım** (1.0'daki "68 sütunun 37'si" bayattı; `TKU No.` yazımı da
   eklendi), `Müvekkil Tipi`/`Hizmet Türü` okunan listeye girdi, "Bekleyen alanlar" satırı
   kalktı (okunmayan 31 → 29); DB-2026-002/003/006/007 şerhleri §3.2'ye, DB-008 (ad
   alanları harf duyarsız) ve DB-009 (tarih/tutar teyit) hücre değerleri tablosuna + §5'e,
   DB-004 (`Önceki` etiketi: karar aşaması değil, esas tarihçesine; satırları gönderin) ve
   DB-005 (havuz dışı karar durumu: satır düşmez, açıklamaya; lehe/aleyhe ekseni yok) §3.5'e;
   §3.7'ye "Müvekkil Tipi/Hizmet Türü havuzu karşılaştırılmaz" notu; **yeni §3.8 kapalı
   liste envanteri** (9 · 5 · 9 · 3 · 4 · 28/3/3/2 · 3, değerler seed sabitlerinden birebir,
   alan başına "Uygulama" sütunu); §4 belirsiz eşleşme sebebine üçüncü anahtar (Müvekkil,
   G118); §5 tablosu (42 alan, yeni kapalı listeler, İddia Edilen Kusur ayrı satır, Hizmet
   Türü artık kapalı liste, ek sayfalar serbest); §6 örnek no `DB-2026-0NN` + "sıradaki
   DB-2026-011"; §8 kontrol listesi; §9 yaml (`surum`, `degisiklik_gecmisi`,
   `islenen_bildirimler`, `okunan_baslik` 42 grup, `kapali_listeler`, `asama_onceki`,
   `karsilastirilmayan`, `bildirim_gerektiren`, `bildirim_kanali`).
2. `docs/veri-teslim/SOZLESME.md`: sürüm 04.09 (1.1) + bilgilendirme belgesine atıf; §2
   `Sheet` satırından "68 sütun, sırası sabit" kalktı (kod ada göre okur, sıra serbest — 1.0
   Bilgilendirme ile de çelişiyordu); §6 kapalı liste cümlesine Müvekkil Tipi + Hizmet Türü,
   alan başına uygulama farkı, havuz farkı yalnız altı havuz; §9 kontrol listesi (7 madde:
   sütun sırası serbest, listeler, `Önceki` satırları).
3. `docs/mimari/veri-teslim-hatti.md`: son doğrulama 88409da; §7 `HAVUZ_LISTE_ESLEMESI`
   satırı düzeltildi — **yedi başlık → altı liste, `client_types`/`service_types` eşlemede
   YOK (fark raporu üretmez)**, satır atıfları güncel (`:116-124`, `:133-136`, `:306-331`);
   yeni "Sheet föy düzeyi kapalı listeler (G120)" paragrafı (+2 alan, 42/54 sayısı,
   Bilgilendirme §3.2 ile birebir); yeni tarihli şerh "DB-2026 bildirimi ve cevabı
   (04.09.2026)" on kalemin bizdeki karşılığıyla; §10'a Bilgilendirme satırı +
   `test_g120_aktarim_muvekkil_hizmet.py`.
4. `docs/mimari/dava-acma-akisi.md`: son doğrulama 88409da (§1-§10 eski tarih korunarak);
   §12 sonundaki "G120/G121'in işidir" ifadesi "uygulandı" olarak tekleştirildi (G119'un
   yazdığı şema bölümüyle mükerrer/çelişki yok, olduğu gibi kaldı); **yeni §13 kapalı liste
   envanteri**: `LIST_REGISTRY` 23 liste, 10'u kart havuzu ve hepsi seed'li (tablo seed
   sabitlerinden), kalan 13'ün 5'i seed'siz.
5. `CLAUDE.md`: veri teslim paragrafı bayat sayı/iddia taşımıyor — **dokunulmadı**.

**Doğrulama (docs bandı — koddan sayım):**

- Karşılaştırma scripti (`SUTUN_ADAYLARI` ast ile okundu ↔ Bilgilendirme §9
  `okunan_baslik` + §3.2 tablosu): `kod alan: 42 kod yazim: 54 · dok grup: 42 dok yazim: 54 ·
  kodda var dokta yok: [] · dokta var kodda yok: [] · grup farki: [] · 3.2 tablosunda
  gecmeyen yazim: []` — **birebir**.
- Seed sabitleri (ast): `ALLEGED_FAULTS 9 · CLIENT_TYPES 5 · SERVICE_TYPES 9 · EVENT_TYPES 3
  · JUDGMENT_ROLES 4 · APPEALING_PARTIES 3 · yerel 28 · istinaf 3 · temyiz 3 · karar
  düzeltme 2` — §3.8 ve §13 tabloları bu sayılarla.
- `grep -rn "seed'lenmez" docs/ CLAUDE.md` (arşiv hariç): tek hit, dava-acma-akisi §13'teki
  tarihli şerh ("G044'ten 04.09'a kadar bilinçli boştu … ifadesi tarihseldir").
- `git diff --stat`: yalnız 4 docs dosyası + bu görev dosyası; `docs/arsiv/`e dosya
  eklenmedi, backend/frontend değişmedi.
- İç tutarlılık: yeni bağlantılar (`../veri-teslim/BILGILENDIRME_2026-09-03.md`,
  `veri-teslim-hatti.md`) mevcut dosyalara; 37/31 kalıntısı yok; `HAVUZ_LISTE_ESLEMESI`,
  `ASAMA_ONCEKI`, `_esleme`, `_kapali_liste_parcalari`, `_taraflari_yaz`
  (`normalize_party_key`), `_metin_alan` koddan okunarak yazıldı.

**Kararlar + gerekçeler:**

- **`İddia Edilen Kusur` "kapalı liste" değil "metin" olarak yazılır** — kod:
  `KART_ALANLARI["iddia_edilen_kusur"] = ("iddia_edilen_kusur", _metin_alan)` (`:761`) ve
  seed yorumu ("Aktarım bu alanı METİN olarak yazar"). 1.0 Bilgilendirme §5 ve SOZLESME §6
  bu alan için "değer yazılmaz, rapora düşer" diyordu — kodla çelişiyordu; ALTIN KURAL gereği
  üç belgede de düzeltildi (liste kart seçimini + `DEGER_HAVUZLARI` farkını besler, liste dışı
  yazım karta girer). Kod değiştirilmedi (docs bandı) — doğrulama istenirse ayrı görev.
- **DB-2026-003'ün on dört sütunu adıyla işaretlenmedi:** REV-2 bildirim metni repoda,
  scratchpad'de, Downloads/Masaüstü'nde yok (yalnız cevabımız `HUKDOK_CEVAP_2026-09-04.md`
  var); adları uydurmak yerine §3.2'ye genel şerh yazıldı ("gelmeyen sütun = bu teslimde
  yok, kaynak bildirimin `Kaldirilan_Sutunlar` listesi"). Görev tanımındaki "bu başlıklar
  şerhle işaretlenir" maddesi bu yüzden kısmen karşılandı.
- `Önceki` satırı için "Başka değer → sessizce atlanır" kod davranışıdır
  (`ASAMA_ESLEMESI.get(...) is None: continue`); Bilgilendirme'ye açık yazıldı.
- Bilgilendirme'de `Klasör No.2` ↔ `Klasör No` ayrımı korundu (kodda iki farklı anahtar).
- SOZLESME'deki "sütun sırası sabit" ifadesi kaldırıldı: kod ada göre okur, Bilgilendirme
  1.0 zaten "sıra serbest" diyordu — iki dış belge birbiriyle çelişmesin.

**İzlenecekler:**

- NOT: `iddia_edilen_kusur` aktarımda liste doğrulamasız METİN yazılıyor; veri ekibine
  "kapalı liste" denen alan kart ekranında liste dışı değerle görünebilir. İstenirse G104
  deseniyle doğrulamalı yazıma geçmek küçük bir backend görevidir (gündüz kararı).
- NOT: REV-2 bildirim metninin (DB-2026-003 sütun listesi dahil) repoya ya da SharePoint
  arşivine konması insan adımı; konunca §3.2'deki genel şerh adlı listeye çevrilebilir.
- NOT: Bilgilendirme 1.1'in veri ekibine gönderilmesi ve KUYRUK'taki insan adımları
  (SharePoint klasör/izin teyidi, anahtar, ilk paketin elle "Uygula"sı) bu görevin dışında.
- NOT: `.claude/settings.local.json` ve `.claude/launch.json` ana dizinde kirli (harness
  dosyaları); worktree'de yok, dokunulmadı.
