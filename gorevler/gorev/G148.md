# G148 — Teslim hattı yeni site dokümanları: dış bağımlılıklar, kimlik, deploy, sözleşme, plan §8 şerhi

- **Bant:** docs
- **Bağımlı:** G147 (satır referansları G147 sonrası koddan okunur)
- **Dosya kapsamı:** docs/mimari/dis-bagimliliklar.md (§2), docs/mimari/kimlik-ve-token.md (app-only kimlik
  bölümü), docs/mimari/deploy-ve-altyapi.md (env listesi), docs/veri-teslim/SOZLESME.md (§1 klasör),
  docs/plan/veri-teslim-otomasyonu-plani-2026-09-03.md (§8 tablo "Prod kurulumu" satırı + yeni satır),
  CLAUDE.md ("Veri teslim hattı" paragrafına tek cümle)
- **Dokunma:** docs/mimari/veri-teslim-hatti.md (G147 güncelledi — yalnız çelişki varsa düzelt),
  backend/**, frontend/**, docs/arsiv/** (arşive yazma yok)

## Hedef

G147 ile teslim hattı ikinci bir Graph kimliği/site'ı kullanır hale geldi (Hanyaloğlu tenant'ı,
`hanyaloglu.sharepoint.com/sites/hukdok_arsiv`); arşiv LexisBio'da kaldı. Yaşayan dokümanlar hâlâ "tek site,
tek kimlik" anlatıyor (`dis-bagimliliklar.md:85-90`, `kimlik-ve-token.md:170`, `deploy-ve-altyapi.md:31`).
ALTIN KURAL: her iddia G147 sonrası koddan satır numarasıyla doğrulanır; `docs/arsiv/`den kopya yok.

## Yapılacaklar

1. **`dis-bagimliliklar.md` §2:** iki config (`default` arşiv / `teslim` veri teslimi), env adları, düşüş kuralı
   (`TESLIM_*` eksikse default), cache anahtarı `(token, config_type)` + `maxsize`, hangi çağrıların hangi config'i
   kullandığı (tablo: outbox/e-posta/sayaç/log → default; gözcü listeleme+indirme, cevap yükleme → teslim).
   Klasör adları paragrafına (`:129-133`) "teslim klasörü ayrı site'ta" notu.
2. **`kimlik-ve-token.md`:** app-only kimlik maddesine ikinci MSAL uygulaması (`_MSAL_APPS["teslim"]`), secret bitiş
   izleme (`TESLIM_SHAREPOINT_CLIENT_SECRET_EXPIRES_AT`) ve iki tenant'ın hangisi olduğu (LexisBio `44f029f8…` arşiv;
   Hanyaloğlu `9776cf1f…` teslim — `ALLOWED_TENANTS` ile ilişkisi: giriş tenant'ları aynı ikili, ama SharePoint
   kimlikleri bağımsız).
3. **`deploy-ve-altyapi.md`:** env listesine `TESLIM_SHAREPOINT_*` (isteğe bağlı; boşsa davranış değişmez) +
   `.env` değişikliğinin `up -d` istediği hatırlatması (mevcut kural).
4. **`SOZLESME.md` §1:** klasörün hangi SharePoint site'ında olduğu (site adı + "Belgeler" kütüphanesi altında
   `03_VERI_TESLIM/gelen`), aynı kuruluş hesabıyla (`@hanyaloglu-acar.av.tr`) "Benimle paylaşılanlar"da göründüğü;
   e-posta/WhatsApp yolunun kapalı olduğu cümlesi korunur. Doğrudan bağlantı yazılmaz (insan adımı — kullanıcı
   cevap e-postasında verir).
5. **Plan §8:** "Prod kurulumu" satırı yeni site'a göre yeniden yazılır; yeni satır "Tenant ayrımı (08.09)": neden
   (06.09 e-postası §13 + 30.07 kopyasındaki belge URL'leri), karar (yalnız teslim hattı taşındı), G147 referansı.
6. **`CLAUDE.md`** "Veri teslim hattı" paragrafına tek cümle: teslim klasörü ayrı SharePoint kimliği/site'ında
   (`TESLIM_SHAREPOINT_*`, `services/teslim_kutusu.py::TESLIM_SP_CONFIG`).

## Kabul kriterleri

- [ ] Beş dokümanda "tek site/tek kimlik" ifadesi kalmadı (grep `Single-Site` / "tek site" / `SHAREPOINT_SITE_URL`
      bağlamı); her satır referansı G147 sonrası kodla eşleşiyor (rapor: referans → dosya:satır doğrulama listesi).
- [ ] SOZLESME.md'de doğrudan bağlantı YOK, site adı + klasör yolu VAR.
- [ ] Plan §8'de yeni satır + güncellenmiş "Prod kurulumu" satırı.
- [ ] `docs/arsiv/` değişmedi; kod/test değişmedi (diff yalnız .md).

## Doğrulama

- `git diff --stat` yalnız `docs/**` + `CLAUDE.md`
- `grep -rn "Single-Site\|tek site" docs/mimari docs/veri-teslim CLAUDE.md` → G147 sonrası kodla çelişen satır yok

## Rapor

**DURUM: TAMAM** (2026-09-08, işçi oturumu; worktree `gorev/G148`, tek commit)

### Yapılanlar

1. **`docs/mimari/dis-bagimliliklar.md` §2:** yeni alt başlık "İki config: `default` (arşiv) ve `teslim`
   (veri teslim hattı) — G147": iki config'in env/secret sırası/site/drive/secret-bitiş/`.env` şablonu tablosu,
   düşüş kuralı (üçlüden biri boşsa default set + tek INFO; site için `TESLIM_SHAREPOINT_SITE_URL` boşken),
   düşüşte bile ayrı `_MSAL_APPS["teslim"]`, cache anahtarı `(token, config_type)` + `maxsize=4` gerekçesi,
   `_with_fresh_token_on_401` aynı config'i yeniler; "hangi çağrı hangi config" tablosu (outbox / belge
   indirme / e-posta / sayaç / onarım → `default`; gözcü listeleme+indirme, cevap yükleme → `teslim`) her satır
   koddan satır numaralı. Klasörler paragrafına "teslim klasörü arşivle aynı site'ta DEĞİLDİR" notu + sayacın
   arşiv site'ında kaldığı. §2'nin G147 ile kayan **eski** satır referansları da düzeltildi (retry bloğu,
   sabitler, SSL, `list_folder_children`). Başlık şerhine "§2 G148 ile yeniden doğrulandı" notu.
2. **`docs/mimari/kimlik-ve-token.md` §4:** ikinci MSAL uygulaması maddesi (`config_type` uzayı, `_ENV_PREFIX`,
   LexisBio `44f029f8…` = arşiv / Hanyaloğlu `9776cf1f…` = teslim, düşüş + tek INFO, ayrı token cache),
   `ALLOWED_TENANTS` ilişkisi (giriş tenant'ları aynı ikili; SharePoint app-only kimlikleri bağımsız),
   `/healthz` sayacının config ayırmadığı; secret bitiş paragrafı iki env'li (lifespan iki çağrı `api.py:131-133`).
   Aynı dosyada §3.1 "Graph token yeniden deneme" satır referansı (`:138-149` → `:185-197`) ve §6 "Secret ömrü
   izleme" satırı güncellendi — §4 dışı ama aynı kaymanın ürünü, ALTIN KURAL gereği düzeltildi.
   §4'ün tüm eski satır numaraları (`:102-108`, `:93-95`, `:15/:110`, `:115-153`, `:143-146`, `:126-135`,
   `sharepoint_uploader_graph.py:102-117`, `:25-65`, `:21`, `:34-36`) G147 sonrası koda göre yeniden yazıldı.
3. **`docs/mimari/deploy-ve-altyapi.md` §1 Güvenlik kapıları:** yeni madde "İsteğe bağlı: veri teslim
   SharePoint'i (`TESLIM_SHAREPOINT_*`)" — zorunlu listede YOK (`deploy.sh:260-262`), boşsa davranış değişmez,
   `.env` değişikliği `restart` ile gelmez → `up -d` (recreate, stack'in tamamı). Başlık şerhine not.
4. **`docs/veri-teslim/SOZLESME.md` §1:** klasörün yeri — Hanyaloğlu Acar SharePoint'i, `hukdok_arsiv` site'ı,
   `Belgeler` kütüphanesi, `03_VERI_TESLIM/gelen`; kuruluş hesabıyla (`@hanyaloglu-acar.av.tr`) "Benimle
   paylaşılanlar"da görünür, davet/misafir hesabı gerekmez; doğrudan bağlantı cevap e-postasında (yazılmadı).
   Sürüm satırı 1.2 (08.09). WhatsApp/e-posta kapalı cümlesi (giriş paragrafı) aynen korundu.
5. **Plan §8:** "Prod kurulumu" satırı yeni site'a göre beş adımla yeniden yazıldı; yeni satır "Tenant ayrımı
   (08.09)": neden (06.09 e-postası §13, 30.07 kopyasındaki belge URL'leri LexisBio'da, misafir daveti), karar
   (yalnız teslim hattı taşındı), G147 kod referansları, G148 doküman listesi. §8 başlığına tarih şerhi.
6. **`CLAUDE.md`** "Veri teslim hattı" paragrafına tek cümle (ayrı kimlik/site, `TESLIM_SHAREPOINT_*`, düşüş,
   `services/teslim_kutusu.py::TESLIM_SP_CONFIG`, `sharepoint/auth_graph.py`; arşiv/sayaç/export LexisBio'da).

### Referans → dosya:satır doğrulama listesi (hepsi `sed -n` ile okundu, HEAD `11150b4`)

- `sharepoint/auth_graph.py`: `_MSAL_APPS` `:16`; `CONFIG_*` `:25-27`; `_ENV_PREFIX` `:29`;
  `SECRET_EXPIRY_WARN_DAYS` `:36`; `TESLIM_SECRET_EXPIRES_AT_ENV` `:39`; `check_client_secret_expiry` `:42-88`
  (etiket/env adı `:61-63`, healthz şerhi `:54-56`); `_read_credentials` `:101-115` (secret aranmaz `:104-105`,
  `:111`); `ValueError` `:130-131`; düşüş + INFO `:136-143`; ayrı app gerekçesi `:121-125`;
  `ConfidentialClientApplication` `:150-156`; cache yazımı `:158`; `get_graph_token` `:163-201`
  (force_refresh `:174-183`, iki deneme `:185-197`, healthz `:190-194`).
- `sharepoint/sharepoint_uploader_graph.py`: `_site_url_for` `:27-32`; `_drive_name_for` `:35-40`; retry şerhi
  `:42-48`; DİKKAT `:50-54`; `_RETRY_STATUS_CODES` `:55`; sabitler `:56-58`; SSL `:78-83`; `_build_retry`
  `:86-96` (`allowed_methods` `:91-93`, Retry-After `:94-95`); `_with_fresh_token_on_401` `:125-142`;
  `lru_cache(maxsize=4)` `:145-146`, gerekçe `:150-156`; `upload_file_to_sharepoint` `:395-403`;
  `download_file_from_sharepoint` `:453-455`; `_LIST_PAGE_SIZE/_LIST_SELECT` `:477-480`;
  `list_folder_children` `:483-518`.
- `services/teslim_kutusu.py:174-178` (`TESLIM_SP_CONFIG`), `:1274`, `:1287`; `services/teslim_cevap.py:529-532`;
  `api.py:131-133`; `auth_verifier.py:73-75`; `deploy.sh:260-262`; `.env.example:9-17`, `:15`, `:31-48`, `:45`.
- Default çağıranlar: `services/upload_queue.py:275-276`, `routes/documents.py:312,415`, `routes/export.py:252`,
  `services/conversion_retry.py:210`, `services/document_pipeline.py:243,308`, `scripts/upload_db_backup.py:32`,
  `email_sender.py:147`, `managers/activity_manager.py:244`, `managers/counter_manager.py:189-190,239-240`,
  `scripts/repair_overwritten_documents.py:68-77`.
- Tenant GUID kısaltmaları (`44f029f8…`, `9776cf1f…`) kodda YOKTUR (`.env`); kaynak G147 görev kaydı + 08.09 Graph
  doğrulaması — dokümanda bu şekilde işaretlendi.

### Kararlar

- SOZLESME'de host adı (`hanyaloglu.sharepoint.com`) da yazılmadı: site URL'sinin kendisi bağlantıdır; "site adı"
  = `hukdok_arsiv` + kütüphane + klasör yolu yeterli (görev: doğrudan bağlantı insan adımı).
- `dis-bagimliliklar.md`'de "tek site / tek kimlik" ifadesi kendi negasyon cümlemde bile bırakılmadı (mekanik
  grep temiz kalsın).
- `docs/mimari/veri-teslim-hatti.md`'ye dokunulmadı (G147 güncelledi; çelişki bulunmadı — §1 "Site/kimlik",
  §2, §9 ile yeni metinler tutarlı, satır referansları aynı ağaç).

### Doğrulama

- `git diff --stat` → yalnız `CLAUDE.md` + `docs/mimari/{dis-bagimliliklar,kimlik-ve-token,deploy-ve-altyapi}.md`
  + `docs/plan/veri-teslim-otomasyonu-plani-2026-09-03.md` + `docs/veri-teslim/SOZLESME.md` (+ bu görev dosyası).
  `docs/arsiv/`, `backend/`, `frontend/` diff'te YOK.
- `grep -rn "Single-Site\|tek site\|tek-site\|tek kimlik" docs/mimari docs/veri-teslim CLAUDE.md` → 0 satır.
  `docs/plan/` içinde tek eşleşme `veri-ekibi-cevabi-karsilastirma-plani-2026-09-08.md:36` ("Kod tek-site …
  YAP → G147/G148 (kuyrukta)") — G147 öncesi tespit satırı, dosya kapsamı dışı, DOKUNULMADI (NOT aşağıda).
- Bant docs: test yok; iç bağlantılar (`dis-bagimliliklar.md`, `kimlik-ve-token.md`, `veri-teslim-hatti.md`)
  aynı dizinde mevcut dosyalara gider.

### NOT (kapsam dışı bulgular, düzeltilmedi)

- `.env.example:17` `SHAREPOINT_DRIVE_NAME=Documents` adını hiçbir kod okumaz; kod `SP_DRIVE_NAME` okur
  (`sharepoint_uploader_graph.py:40`). Şablon bayat ad taşıyor — `.env.example` bu görevin kapsamı dışı.
- `docs/plan/veri-ekibi-cevabi-karsilastirma-plani-2026-09-08.md:36` K1 satırı "kuyrukta" diyor; G147 tamam,
  G148 bu commit — o plan dosyası kapsam dışı, küçük durum güncellemesi ayrı iş.
- Çalışma ağacı temizdi (worktree); ana dizindeki `.claude/settings.local.json` / `.claude/launch.json`
  kirliliği bu worktree'ye yansımadı.
