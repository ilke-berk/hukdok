# G066 — Dört karar durumu alanında kapalı liste doğrulaması (takip yazma yolu)

- **Bant:** backend
- **Bağımlı:** - (ön koşulların hepsi bitti: G060 resmi listeler · G062 `stage_decisions`
  doğrulayıcısı · G065 `yerel_karar_durumu` yazma yolu)
- **Dosya kapsamı:** backend/managers/case_manager.py, backend/managers/stage_decisions.py
  (YALNIZ ortak doğrulayıcı paylaşıma açılacaksa), backend/routes/cases.py **ya da**
  backend/api.py (yalnız hata→HTTP eşlemesi gerekiyorsa, aşağıya bak),
  backend/tests/test_g066_karar_durumu_dogrulama.py
- **Dokunma:** backend/models.py, backend/database.py (şema DEĞİŞMEZ — kolonlar
  `String(100)` kalır, kısıt DB'ye taşınmaz: değerler ada göre denormalize ve resmi liste
  panelden düzenlenebilir); `frontend/**` (G061'in "(liste dışı)" davranışı korunur);
  backend/scripts/hukdok_aktarim.py (koddan doğrulandı: bu 4 alana YAZMIYOR).

## Hedef

Kapalı havuz kuralı bugün **yalnız arayüzde** duruyor. `cases` üzerindeki dört karar durumu
alanı manager katmanında serbest metin kabul ediyor: `case_manager.py:371` yalnızca
"kapalı liste (local_decisions, G060)" diye **yorum** taşıyor, doğrulama yok. Panelden
kapalı liste zorunlu ama API'yi doğrudan çağıran istediğini yazabilir.

Yazma yolları koddan tarandı (2026-08-19) — doğrulanmayan **tek** nokta var:

| Yol | Durum |
| --- | --- |
| `update_case_tracking` ← `TRACKING_FIELDS` (`case_manager.py:1546-1562`) | ❌ **doğrulanmıyor** — bu görevin hedefi |
| `stage_decisions.add_stage_decision` | ✅ `_validated_karar_durumu` (`stage_decisions.py:170`) |
| `stage_decisions._resync_stage_photo` (slot kolonlara fotoğraf) | ✅ dolaylı — yazdığı değer yukarıda doğrulanmış |
| `scripts/hukdok_aktarim.py` (G064) | — bu 4 alana yazmıyor (grep: 0 eşleşme) |

Kaynak: KARAR_ASAMALARI tasarım paketi (17.08) "4 · Kapalı havuzlar" değişmezi + 18.08
boşluk analizinin 3. maddesi; G065 raporunun "İzlenecekler" notu dört alanın **birlikte**
ele alınmasını şart koşuyor (tek alan sıkılaştırılırsa kardeş alanlar arasında asimetri doğar).

## Kapsanan alanlar ve listeleri

| Alan (`cases`) | Resmi liste | Seed |
| --- | --- | --- |
| `yerel_karar_durumu` | `local_decisions` | 28 |
| `istinaf_karar_durumu` | `appeal_decisions` | 3 |
| `temyiz_karar_durumu` | `cassation_decisions` | 3 |
| `karar_duzeltme_durumu` | `revision_decisions` | 2 |

Eşleme zaten iki yerde yaşıyor: `reference_lists.DEPENDENCIES` (:155-158) ve
`stage_decisions` STAGE haritaları (:81-107). **Üçüncü bir kopya çıkarma** — birini
kaynak seç, gerekçesini rapora yaz.

## Karar noktaları (işçi karar verir ve GEREKÇELENDİRİR)

1. **Doğrulayıcı paylaşımı mı, ikinci uygulama mı.** `_validated_karar_durumu` hazır ve
   sözleşmesi doğru. Öneri: paylaşıma aç (tek doğruluk kaynağı). Ama import yönünü
   ölç — `case_manager` → `stage_decisions` bağı döngü üretiyorsa ortak yardımcıyı
   uygun bir yere taşı; döngüyü "sonra import" hilesiyle gizleme.
2. **`active` filtresi.** `stage_decisions` bilinçli olarak filtresiz doğruluyor
   (tarihçe "ne olduğunu" kaydeder; pasifleştirilmiş değer geçmişi geçersizleştirmez).
   Canlı takip yazımında pasif değer kabul edilmeli mi? Öneri: **simetri** — aynı kural,
   çünkü iki yol aynı kolona yazıyor ve fotoğraf senkronu ikisini birbirine bağlıyor.
   Ne seçersen testle kilitle.
3. **Liste BOŞSA ne olur.** `stage_decisions` semantiği "boş liste = hiçbir değer geçmez".
   Aynısı takip yolunda uygulanırsa, seed koşmamış bir kurulumda panel karar alanlarını
   kaydedemez hale gelir. Bilinçli karar ver (seed açılışta koşar → boş liste
   yapılandırma hatasıdır, sert davranmak savunulabilir) ve **hangisini seçtiysen onu**
   testle kilitle.
4. **Hata → HTTP.** Ham `ValueError` route'tan 500 olarak çıkarsa bu bir gerileme olur
   (G003 durum kodu disiplini). 4xx'e düşmeli. `api.py`'de `DuplicateItemError` /
   `ItemInUseError` için hazır handler deseni var (`api.py:315-320`) — ya o desene
   uy ya da route'ta yakala. Hangi yolu seçersen seç, **testle kanıtla**.

## Kabul kriterleri

- [ ] Dört alanın **hepsinde**: listede olmayan değer `update_case_tracking` yolundan
      REDDEDİLİYOR (dördü için ayrı test — asimetri kalmasın)
- [ ] Listede olan değer yazılabiliyor; `None`/boş alanı TEMİZLİYOR (G065'in kilitlediği
      davranış — gerileme yok, o dosyanın testleri yeşil kalır)
- [ ] Ad normalizasyonu `_validated_karar_durumu` ile aynı: baştaki/sondaki ve içteki
      fazla boşluklar sadeleşir, sonra ada göre eşleşir
- [ ] Reddediliş istemciye **4xx** olarak dönüyor (500 DEĞİL) ve mesaj hangi alan/liste
      olduğunu söylüyor — route seviyesinde test
- [ ] Whitelist dışı alan davranışı ve diğer takip alanlarının yazımı DEĞİŞMEDİ
      (G065'in sessiz-yoksayma kilidi dahil)
- [ ] `stage_decisions` yolunun davranışı DEĞİŞMEDİ (mevcut 32 test yeşil); fotoğraf
      senkronunun yazdığı değer yeni doğrulamaya TAKILMIYOR (senkron yolu testi)
- [ ] Karar noktaları 2 ve 3 için seçilen davranış testle kilitli + görev raporunda gerekçeli
- [ ] Mevcut veri: dört alanın prod kopyasında 0 dolu olduğu ölçümü (18.08) **yeniden
      doğrulanır**; doluysa liste dışı değer sayısı raporlanır ve backfill YAPILMAZ
      (kapsam dışı; bulgu olarak yazılır)
- [ ] Gerçek DB'ye bağlanan yeni test varsa 3-ortam kuralı: `to_regclass` + SKIP

## Doğrulama

- `docker compose exec -T backend python -m pytest`  (ekstra `-q` EKLEME)
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- Dev araçları konteynerde yoksa önce: `docker compose exec -T backend pip install -r requirements-dev.txt`

## Rapor

(işçi oturumu doldurur)
