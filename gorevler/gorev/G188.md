# G188 — localStorage dayanıklılığı: theme-provider try/catch + ErrorBoundary içine; YetkiBelgesi TC/sicil önbelleği (KVKK)

- **Bant:** frontend
- **Bağımlı:** G184
- **Dosya kapsamı:** `frontend/src/components/theme-provider.tsx`, `frontend/src/App.tsx` (yalnız provider sıralaması), `frontend/src/components/YetkiBelgesiModal.tsx`, `frontend/src/components/YetkiBelgesiModal*.test.tsx`, yeni `frontend/src/components/theme-provider.test.tsx`
- **Dokunma:** `frontend/src/lib/formDraft.ts` (kural kaynağı, DEĞİŞMEZ), `frontend/src/hooks/useDashboardView*` (desen kaynağı)

Kaynak: `docs/arsiv/performans-denetimi-2026-09-14.md` F6, F7. F6 performans değil KVKK
maddesidir; ayrı ele alınması kullanıcı kararı (14.09).

## Hedef
1. **F7** `theme-provider.tsx:18,42` — `localStorage.getItem/setItem` try/catch'siz ve
   `ThemeProvider` `App.tsx:213-225`'te `ErrorBoundary`'nin DIŞINDA → storage kapalı/kota dolu
   (Safari private, kurumsal politika) → açılışta beyaz ekran. `DashboardViewProvider.tsx:7-14`'teki
   `readStored` desenini uygula; provider'ı boundary içine al.
2. **F6** `YetkiBelgesiModal.tsx:27,43-45,201-204` — avukat TC + sicil no sürümsüz anahtarla
   KALICI localStorage'a yazılıyor, `setItem` try/catch'siz (kota → `goToStep3` fırlatır, 3. adım
   açılmaz). `lib/formDraft.ts:10-11` "TC içeren veri localStorage'a yazılmaz" kuralıyla çelişir.
   Karar: TC hiç önbelleklenmez; sicil no ve ad `sessionStorage`'da `:v1` anahtarla, try/catch'li.
   Eski kalıcı anahtar ilk açılışta SİLİNİR (temizlik migrasyonu).

## Kabul kriterleri
- [ ] Test: `localStorage` erişimi fırlatan (getter `throw`) ortamda `App` boyanır (beyaz ekran yok), tema varsayılana düşer.
- [ ] Test: `ThemeProvider` render ağacında `ErrorBoundary`'nin altındadır (App test'inde ağaç sırası ya da provider testi).
- [ ] Test: yetki belgesi akışında 3. adıma geçince `localStorage`'da TC İÇEREN hiçbir anahtar yoktur; sicil/ad `sessionStorage` `:v1` anahtarındadır; `setItem` fırlatınca akış devam eder (3. adım açılır).
- [ ] Test: eski kalıcı anahtar (`localStorage`) varsa modal açılınca silinir.
- [ ] `formDraft.ts:10-11` kuralına atıf yorumu YetkiBelgesiModal'da durur.
- [ ] Mevcut `YetkiBelgesiModal.print.test.tsx` yeşil.

## Doğrulama
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor
(işçi oturumu doldurur)
