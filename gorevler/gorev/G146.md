# G146 — Rapor şeridi: sayı rozetleri, sıfırlılar soluk sonda, "Boş" birinci sınıf seçenek, her kontrolde boş çipi, "Eksik bilgi" hücresi

- **Bant:** frontend
- **Bağımlı:** - (sözleşme plan §7.2'den; G145 ile PARALEL)
- **Dosya kapsamı:** frontend/src/lib/reports.ts (`KatalogKolon.secenek_sayilari`, `bos_sayisi`; `BOS_ETIKETI="Boş"`;
  `secenekSayisi`, `bosSayisi` yardımcıları; tarih/sayı/metin kontrol durumlarına `bos: boolean` → `is_null`),
  frontend/src/components/reports/ChipSelect.tsx, .../FilterControl.tsx (çoklu seçim listesi + tarih/sayı/metin
  kontrollerine boş çipi), .../ToggleFilter.tsx (rozet), .../QuickFilters.tsx ("Eksik bilgi" hücresi: `bos_anahtari`
  yuvaları gruplanır), .../FilterChip.tsx (özet "boş"), .../builderState.ts (gidiş-dönüş), ilgili `*.test.ts(x)`
- **Dokunma:** frontend/src/App.tsx, Sidebar.tsx, lib/api.ts (hub — DOKUNMA), AssistantBar/AssistantThread,
  FavoritePrompt, TemplateBar, SourceCards, ColumnSheet (yalnız prop uyumu), backend/**

## Test değiştirme İZNİ (baştan)

**Değiştirmek İZİNLİ:** `components/reports/QuickFilters.test.tsx`, `ChipSelect.test.tsx`, `FilterControl*.test.tsx`,
`ToggleFilter.test.tsx`, `builderState.test.ts`, `lib/reports.test.ts` (KATALOG sahteleri — yeni alanlar,
"(boş)" → "Boş" etiketi, kontrol durumları), `pages/ReportsPage*.test.tsx` (katalog sahteleri + boş çipi akışı),
`lib/reports.favori.test.ts` (yalnız "(boş)" özeti). **Kilit:** `lib/reports.export.test.ts`, `lib/reportsChat.test.ts`,
AssistantBar/AssistantThread/FavoritePrompt/TemplateBar/SourceCards/RunsTable testleri. `expect(` sayısı net
azalmaz; raporda "değiştirilen test → gerekçe".

## Hedef

Plan §7.1/§7.3 (kullanıcı bulgusu): seçeneklerin yanında kayıt sayısı, sıra katalogdan (sayıya göre), sıfırlılar
soluk ama seçilebilir; "(boş)" yerine parantezsiz **Boş** birinci sınıf seçenek (kendi stili + boş sayısı);
boş seçimi tarih/tutar/metin kontrollerinde de tek tık; "E-postası yok / Cep telefonu yok" tek "Eksik bilgi"
hücresinde. Sunucu filtre sözleşmesi değişmez (`in` içinde `null`, `is_null`).

## Yapılacaklar

1. **Tipler/yardımcılar (`lib/reports.ts`):** `secenek_sayilari`, `bos_sayisi` (isteğe bağlı, eski katalog sahteleri
   için `null` kabul); `BOS_ETIKETI = "Boş"` tek sabit (tüm "(boş)" metinleri buradan; `secenekEtiketi(null)` →
   "Boş"); `sayiRozeti(n)` tr-TR binlik; kontrol durumlarına `bos` bayrağı (tarih_araligi/sayi_araligi/metin_icerir):
   `kontroldenFiltre` → `bos` ise `is_null` (girdi değerleri yok sayılır), `filtredenKontrol` tersi (`is_null` →
   ilgili kontrol `bos=true`, artık "gelişmiş çip" değil); `kontrolOzeti` "boş".
2. **ChipSelect:** her çipte etiket + sayı rozeti (`secenek_sayilari?.[deger] ?? null`; rozet yoksa sadece
   etiket); `0` → `opacity-60` ama tıklanır; sıra katalogdan; listenin sonunda ince ayırıcı + **Boş** çipi
   (italik, rozet `bos_sayisi`); "+N" daraltma sıfırlıları önce gizler.
3. **Çoklu seçim açılırı (FilterControl):** satırlarda sayı rozeti; "Sık" katalog ilk 8; "Tümü" bölümünde
   sıfırlılar sonda soluk; **Boş** satırı ayırıcıyla en altta; seçili çiplerde "Boş" etiketi.
4. **Tarih/sayı/metin kontrolleri:** girdinin sağında küçük "Boş" toggle çipi (`aria-pressed`, rozet
   `bos_sayisi`); açıkken girdiler `disabled`; kapatınca eski girdi değerleri geri (durumda saklanır). Arama
   kutusunda YOK (sanal kolon, `bos_sayisi=null`).
5. **QuickFilters "Eksik bilgi" hücresi:** `sunum=bos_anahtari` yuvaları tek hücrede (`data-testid="eksik-bilgi"`),
   başlık "Eksik bilgi", kutucuklar yan yana; her kutucuk "<etiket> · N" (`bos_sayisi`); hücre yalnız böyle yuva
   varsa render.
6. **FilterChip/özet:** `in` içindeki `null` "Boş" diye; `is_null` "boş"; "(boş)" metni koddan tamamen kalkar (grep).
7. **Testler:** rozet metinleri (1.443 biçimi), sıra katalogdan (istemci yeniden sıralamaz), 0 soluk + tıklanır,
   Boş çipi/satırı + rozet, tarih/sayı/metin boş toggle → `is_null` + girdiler kilitli + geri açınca değer geri,
   şablon `is_null` → boş toggle (gidiş-dönüş), Eksik bilgi hücresi + rozetler, eski katalog sahtesi (alanlar
   yok) çökmez, "(boş)" DOM'da yok.

## Kabul kriterleri

- [x] Çip ve listelerde sayı rozeti; sıfırlılar soluk, sonda ve seçilebilir; sıra katalogdan (test).
- [x] "Boş" parantezsiz, kendi stiliyle, boş sayısı rozetli; "(boş)" kodda kalmadı (grep + test).
- [x] Tarih/sayı/metin kontrollerinde boş toggle `is_null` üretir, girdileri kilitler; şablon/asistan `is_null`
      aynı toggle'a çözülür (gidiş-dönüş testi).
- [x] `bos_anahtari` yuvaları tek "Eksik bilgi" hücresinde, rozetli (test).
- [x] Katalogda yeni alanlar yoksa (eski backend) ekran rozetsiz çalışır (test).
- [x] Hub dosyalara dokunulmadı; yeni npm paketi yok; `npm test`, `lint`, `tsc -b --force` temiz.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

**DURUM: TAMAM** (2026-09-07, gece işçisi; worktree `C:/dev/hukudok-wt/G146`, dal `gorev/G146`, taban 2cf04bb)

### Yapılanlar

1. **`lib/reports.ts`** — `KatalogKolon.secenek_sayilari?: Record<string, number> | null` ve `bos_sayisi?: number | null`
   (isteğe bağlı: eski katalog sahteleri ve eski sunucu cevabı derlenir/çalışır). `BOS_ETIKETI = "Boş"` tek sabit
   (`BOS_SECENEK_ETIKETI` kalktı; `secenekEtiketi(null)` → "Boş"). Yardımcılar `secenekSayisi(kolon, deger)`
   (`null` → boş sayısı; yoksa `null`; `0` gerçek sayı), `bosSayisi(kolon)`, `sayiRozeti(n)` (tr-TR binlik, "1.443").
   Kontrol durumlarına `bos: boolean` (tarih_araligi/sayi_araligi/metin_icerir): `bosKontrol` `bos:false` açar;
   `kontroldenFiltre` `bos` ise girdileri yok sayıp `is_null`; `filtredenKontrol` `is_null` → ilgili kontrol `bos:true`
   (gelişmiş çip değil) — yalnız kolon `is_null` izinliyse (sanal `arama` kolonu gelişmiş kalır, filtre kaybolmaz);
   `KONTROL_DOGAL_OPLARI`'na tarih/sayı/metin için `is_null` eklendi → "…" menüsünden "boş" düştü (mantıkta kaldı);
   `kontrolOzeti`: `bos` → "boş", `in` içindeki `null` → "Boş", yalnız `null` seçili → "boş"; `sablonAdiOner` boş çipli
   tarih/metin için "<Kolon> boş".
2. **`ToggleFilter.tsx`** — `SayiRozeti` (ortak, `data-testid="sayi-rozeti"`, sayı `null` ise hiç basılmaz),
   `BosKutucugu` (başlıksız kutucuk, "<etiket> · N"), `BosAnahtari` onu sarar, yeni `BosCipi` (`aria-pressed`,
   erişilebilir ad "<Kolon> boş", rozet `bos_sayisi`, 0 → soluk).
3. **`ChipSelect.tsx`** — çipte etiket + rozet; sayı 0 → `opacity-60` + `data-sifir` ama tıklanır; sıra katalogdan;
   "+N" daraltması önce sıfırlıları gizler (seçililer daima görünür); sonda ince ayırıcı (`data-testid="bos-ayirici"`)
   + italik **Boş** çipi, sayıma girmez ve daima görünür (`bos_sayisi=0` iken soluk).
4. **`FilterControl.tsx`** — çoklu seçim açılırında satır rozeti, "Sık" = katalog sırasında sıfır olmayan ilk 8,
   "Tümü" katalog sırası (sıfırlılar soluk), **Boş** satırı üst ayırıcıyla en altta; seçili çiplerde/özet düğmesinde
   "Boş". Tarih/sayı/metin kontrollerinde girdinin sağında `BosCipi` (kolon `is_null` izinliyse): açıkken girdiler
   (+ tarih kısayolu, cmdk combobox) `disabled`, değerler durumda kalır, kapatınca eski filtre geri gelir.
5. **`QuickFilters.tsx`** — `sunum=bos_anahtari` yuvaları ızgarada TEK `data-testid="eksik-bilgi"` hücresinde
   ("Eksik bilgi" başlığı, kutucuklar yan yana, her biri rozetli), ilk yuvanın yerinde; her kutucuk kendi
   `data-testid="filtre-kontrolu"` sarmalını korur (sıra/sunum testleri değişmedi). Hücre yalnız böyle yuva varsa.
6. **`FilterChip.tsx` / `builderState.ts`** — yorum güncellemesi; `tekilDeger` `bos` açıkken değer taşımaz. Gidiş-dönüş
   `filtredenKontrol` üzerinden kendiliğinden çalışır (builderState kodu değişmedi).

### Kararlar + gerekçeleri

- **`bos` zorunlu alan (`bos: boolean`)**, isteğe bağlı değil: eksik ele alma derlemede yakalansın (testlerdeki
  durum sahteleri güncellendi). Katalog alanları ise isteğe bağlı (eski backend/sahteler için).
- **`is_null` "…" menüsünden kalktı** (tarih/sayı/metin): kontrolün kendi ürettiği op menüde de dursaydı iki farklı
  durum (gelişmiş çip vs boş çipi) aynı JSON'u üretirdi; gidiş-dönüş tek yola indirildi. Mantık kolonunda boş çipi
  görev kapsamında değildi (plan §7.1 madde 3: tarih/tutar/metin) → menüde kaldı.
- **"Boş" çipi "+N" sayımının dışında ve daima görünür**: plan §7.3 "`bos_sayisi=0` iken de görünür (soluk)" — sayıma
  girse daraltmada gizlenebilirdi. Bu yüzden 13 kategoride "+2" → "+1" (test güncellendi).
- **İstemci yeniden sıralamaz** (görev kuralı): sıfırlıların "sonda" olması katalog sözleşmesinden (§7.2) gelir;
  istemci yalnız soluklaştırır ve daraltmada önce onları gizler (katalog tutarsızsa bile sıra bozulmaz — test var).
- Çip özeti: `in` içindeki `null` seçenek adıyla "Boş" ("Ankara, Boş"), salt boşluk (`is_null`) op etiketiyle "boş"
  (görev madde 6 birebir).
- `is_null` izinsiz kolonda (`arama`, oplar `[contains]`) `filtredenKontrol` boş çipe DEĞİL gelişmişe çözer: çip
  gösterilmeyeceği için kapatılamayacak bir durum üretmemek; filtre yine kaybolmaz (test var).

### Değiştirilen testler → gerekçe

- `lib/reports.test.ts`: `"(boş)"` → `"Boş"` etiket beklentileri; "boş olanlar YOK" testi §7.1 boş çipi testine
  dönüştü (`bos` bayrağı artık VAR, is_null üretimi); `ORNEKLER` tablosunda tarih/sayı/metin/email `is_null` →
  `gelismis` yerine kontrolün kendisi (+1 satır: maddi `is_null`); `gelismisOplar` beklentilerinden `is_null` düştü;
  `KONTROL_DOGAL_OPLARI` tarih/sayı/metin/çoklu için `is_null` içerir, mantık içermez; özet "Ankara, boş" →
  "Ankara, Boş", gelişmiş `in` "…, Boş"; durum sahtelerine `bos:false`. `expect(` 191 → 223, `it` 48 → 50.
- `QuickFilters.test.tsx`: tarih testi "…" menüsü `["eşittir","dolu"]` + boş çipi akışı; Karşı Taraflar/Mahkeme menüleri
  "boş"suz; tüm `"(boş)"` etiketleri "Boş"; Kategori "+2" → "+1" (Boş sayım dışı); §5.1 sıra testine Eksik bilgi
  hücresi beklentileri; +6 yeni test (sayılı katalog). `expect(` 179 → 260, `it` 17 → 23.
- `FilterControl.test.tsx`: "(boş)" → "Boş"; tarih testi boş çipi akışıyla genişledi; +4 yeni test (sayılı çoklu seçim,
  sayılı çip satırı, sayı/metin boş çipi, combobox kilidi, sanal kolonda çip yok). `expect(` 34 → 106, `it` 7 → 11.
- `builderState.test.ts`: +1 gidiş-dönüş testi (tarih/sayı/metin `is_null` → yuva `bos:true`, gelişmiş değil, JSON aynı).
  `expect(` 41 → 49.
- Kilitli testlere (`reports.export`, `reportsChat`, AssistantBar/…/RunsTable, `ReportsPage*`, `reports.favori`)
  DOKUNULMADI; eski katalog sahteleriyle rozetsiz çalışırlar (bu, "eski backend çökmez" kabulünün doğal kanıtı).
  Hiçbir test silinmedi/gevşetilmedi; skip/only yok.

### Test sonuçları (HOST, worktree; docker compose kullanılmadı)

- `npx vitest run`: **70 dosya, 887 passed, 0 failed** (tur 1: 886/887 — kendi yeni testimde JSON anahtar sırası;
  tur 2 ve son koşu yeşil).
- `npx tsc -b --force`: 0 hata. `npm run lint`: 0 hata/uyarı.
- `grep "(boş)"` kaynak kodda: 0 (testlerde yalnız `not.toContain("(boş)")` negatif beklentileri ve başlıklar).

### İzlenecekler / NOT

- NOT: Tarayıcıda görsel doğrulama YAPILMADI (worktree'de backend yok; katalog + MSAL gerekir). jsdom testleri
  davranışı kapatıyor; görsel ayar (rozet boyutu, ayırıcı, `opacity-60`) sabah gerçek katalogla bakılmalı.
- NOT: `secenek_sayilari`/`bos_sayisi` G145 (backend) ile gelir; G145 deploy edilmeden ekran rozetsiz çalışır.
- NOT: Gelişmiş çipin "…" menüsünde `is_null` (gelişmiş op olarak) hâlâ listelenir — o çipten "boş" seçilirse gelişmiş
  `is_null` çipi oluşur; şablona kaydedilip geri yüklenince boş çipine normalize olur (gidiş-dönüş eş anlamlı).
- NOT: Mantık kolonunda boş çipi yok (plan §7.1 madde 3 kapsamı tarih/tutar/metin); istenirse ayrı görev.
- NOT (yan etki): HEAD'deki `ChipSelect.tsx` 1 NUL bayt içeriyordu (git `i/-text`, diff "Bin"); dosya bütünüyle
  yeniden yazıldığı için NUL kalktı — bu commit'te diff ikili görünür, sonrakilerde metin.
