# G104 — Aktarım eşlemesi: Olay Türü + Hükümdeki Rol sütunları teslimden kartlara

- **Bant:** backend
- **Bağımlı:** G103
- **Dosya kapsamı:** backend/scripts/hukdok_aktarim.py, backend/tests/** (mevcut
  test_g064_aktarim_cekirdek.py'ye ekleme ya da yeni test dosyası)
- **Dokunma:** backend/models.py, backend/managers/** (G103 bitmiş şemayı KULLAN,
  değiştirme; eksik görürsen BLOKE bırak)

## Hedef

Veri ekibi iki yeni alanı partili xlsx teslimleriyle gönderecek (25.08 belgesi §5: föy
düzeyinde yazılır, kimlik şeması değişmez). `hukdok_aktarim.py` yazma yoluna iki sütunun
eşlemesi eklenir ki gelecek ilk teslimde alanlar elle iş çıkarmadan otomatik aksın.
Sütun başlıkları karşı tarafla henüz yazılı olarak sabitlenmedi — eşleme başlık YOKKEN
davranışı değiştirmeyecek biçimde toleranslı kurulur.

## SÖZLEŞME (G103 ile aynı; sapma gerekiyorsa BLOKE bırak)

- Hedef kolonlar: `cases.olay_turu`, `cases.hukumdeki_rol` (ad denormalize).
- Beklenen teslim başlıkları: `Olay Türü` ve `Hükümdeki Rol` (KART_ALANLARI
  başlık-demeti desenine uygun; `hukdok_aktarim.py:170` civarı).
- Değer eşlemesi AD bazlı, kapalı listeye karşı: "Tıbbi Olay" · "Belgeleme Olayı" ·
  "Tıbbi + Belgeleme" · "Tek Gerekçe" · "Yan Gerekçe" · "Yalnız Saptama" ·
  "Reddedilmiş İddia".
- Çok değerli hücre (` ; ` ayraçlı, 31.08 teslim kuralı): olay türünde
  {Tıbbi Olay, Belgeleme Olayı} birlikte gelirse "Tıbbi + Belgeleme"ye normalize
  edilir (yazım birebir kapalı liste adı). Hükümdeki rolde çok değer TANIMSIZDIR →
  yazılmaz, satır rapora düşer.

## Pazarlıksız kurallar (mevcut hattın sözleşmesi — bozma)

- **Başlık teslimde yoksa alan atlanır** ("None = bu teslimde yok, boşalt değil").
  Mevcut testlerin tamamı yeşil kalır; eski paketlerle koşu davranış değiştirmez.
- **Tanınmayan değer YAZILMAZ** (tahmin yasağı): kapalı listede karşılığı olmayan değer
  satır raporuna düşer, föyün diğer alanları normal işlenir (SAVEPOINT izolasyonu
  gerekmez — alan düzeyi atlama yeter; hangi yolu seçtiğini raporda gerekçele).
- **Dolu hücre korunur mu?** Mevcut hat hangi kuralı uyguluyorsa (İÇERİK farkı /
  üzerine yazma sınıfları) iki alan METİN alanlarıyla aynı sınıfa girer — özel kural
  İCAT ETME, mevcut sınıflandırmayı kullan ve raporda hangi sınıfa girdiğini yaz.
- **İdempotency:** aynı girdiyle ikinci koşu 0 değişiklik, `case_history` şişmez.

## Kabul kriterleri

- [ ] Başlıksız (eski format) girdiyle koşu: davranış birebir eski — mevcut aktarım
      testleri değişiklik gerektirmeden geçiyor.
- [ ] Başlıklı girdiyle kuru koşu (`--dry-run`): iki alan fark raporunda görünüyor,
      DB'ye yazılmıyor.
- [ ] Gerçek koşu: geçerli değerler yazılıyor; "Aydınlatma" gibi listede OLMAYAN bir
      değer yazılmıyor ve satır raporunda gerekçesiyle listeleniyor (test).
- [ ] ` ; ` ile {Tıbbi Olay; Belgeleme Olayı} → "Tıbbi + Belgeleme" (test);
      hukumdeki_rol'de çok değer → yazılmaz + rapor (test).
- [ ] İkinci koşu 0 değişiklik (idempotency testi iki yeni alanı da kapsıyor).
- [ ] Docstring'deki "YAZILAN kart alanları" bölümü güncellendi (tek doğruluk kaynağı
      KART_ALANLARI sözlükleri kuralı korunarak).

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-02)

**Yapılanlar** (`backend/scripts/hukdok_aktarim.py` + yeni test dosyası):

1. `SUTUN_ADAYLARI`na `olay_turu` → ("Olay Türü",) ve `hukumdeki_rol` →
   ("Hükümdeki Rol",) eklendi. Tolerans mevcut mekanizmadan: `_baslik_anahtari`
   aksan/büyük-küçük/boşluk farklarını zaten yutar ("OLAY TURU" da eşleşir,
   testli); başlık teslimde YOKSA alan `satir.degerler`e hiç girmez → None
   sözleşmesi → davranış birebir eski.
2. Yeni istisna `AlanHatasi` (bilinçle `SatirHatasi`ndan TÜREMEZ): TEK alanın
   yazımını düşürür, satır işlenmeye devam eder. `kart_degerleri` alan
   düzeyinde yakalar; sebepler `atlanan_alanlar` toplayıcısıyla (yeni opsiyonel
   parametre, `_kart_alanlarini_yaz` üzerinden) `_satiri_isle`ye taşınır ve
   satır raporuna `tur=HATA`, sebep `"<alan> yazılmadı: …"` olarak düşer.
   Çelişki ön geçişi toplayıcısız çağırır — aynı kusur rapora iki kez düşmez.
3. Değer eşlemesi AD bazlı: kanonik adların tek kaynağı G103 seed sabitleri
   (`seed_data.EVENT_TYPES`/`JUDGMENT_ROLES` — literal kopya yok); anahtar
   normalizasyonu `_baslik_anahtari` (değer tarafında da toleranslı:
   "TIBBİ OLAY" → "Tıbbi Olay" kanonik yazımla yazılır, testli).
4. Çok değerli hücre ` ; `: `_AYRAC` (';' + satır sonu) ile parçalanır,
   mükerrer yazım tek değer sayılır. Olay türünde yalnız {Tıbbi Olay,
   Belgeleme Olayı} kümesi "Tıbbi + Belgeleme"ye normalize edilir; BAŞKA her
   kombinasyon tanımsız → yazılmaz + rapor. Hükümdeki rolde 2+ değer tanımsız
   → yazılmaz + rapor.
5. `KART_ALANLARI`na iki kayıt; docstring'in "YAZILAN kart alanları" bölümü
   güncellendi (KART_ALANLARI tek doğruluk kaynağı kuralı korunarak; mekanik
   kilit testte).

**Kararlar + gerekçeler** (görev "raporda gerekçele" dedi):

- **Tanınmayan değer yolu = alan-düzeyi atlama, SAVEPOINT değil.** Dönüşüm
  yazımdan ÖNCE koşar; alan sözlüğe hiç girmediği için geri alınacak kısmi
  yazım oluşmaz — izolasyon mekanizmasına gerek yok. Satırın föyü ve diğer
  alanları normal işlenir (kabul kriterindeki yol).
- **Rapor türü = HATA (koşu NONZERO).** `RaporSatiri` sözleşmesinde ATLANDI
  "beklenen sebep" demek (kart yok); kapalı listede karşılığı olmayan değer
  ise düzeltme listesi konusudur — `SatirHatasi` sınıfının "insan müdahalesi
  gerektirir" anlamıyla eş. `_esleme`nin sessiz boşluğu (İstinaf, 17 yazım
  ölçülüp bilinçli kabul) burada tercih EDİLMEDİ: bu iki alanın değer havuzu
  henüz ölçülmedi, sessizlik kusuru gizlerdi.
- **Dolu hücre sınıfı = METİN alanlarının varsayılan üzerine-yazma sınıfı**
  (İÇERİK-karşılaştırmalı modu DEĞİL; özel kural icat edilmedi): fark varsa
  teslim kazanır + tarihçeye eski→yeni düşer. İÇERİK modu bu alanlarda
  anlamsız — dönüştürücü çıktısı zaten kanonik ad, "yalnız yazım farkı"
  sınıfı oluşamaz (testte kilitli).
- **Yer tutucular** ("-", "YOK"…) D5'in ORTAK sözlüğüyle NULL sayılır (tarih
  yoluyla aynı davranış + WARNING) — rapora hata olarak düşmezler.
- **Kardeş föy çelişkisi**: iki alan mevcut ön-geçiş mekanizmasına
  kendiliğinden girdi; AYRI satırlardaki {Tıbbi, Belgeleme} çelişkidir,
  KARMA'ya NORMALIZE EDİLMEZ (tek hücre kuralından ayrımı testli).

**Test sonuçları:** yeni dosya `backend/tests/test_g104_aktarim_belgeleme_olayi.py`
— 35 test (dönüştürücü birimleri, sözleşme/kayıt kilitleri, başlık toleransı,
sqlite uçtan uca: eski format dokunmaz, kuru koşu, tanınmayan değer raporu,
KARMA + çok rol, idempotency, kardeş çelişkisi, üzerine-yazma sınıfı). Eski
kodda dosya kırmızı (import `AlanHatasi` — HEAD'de 0 geçiş, doğrulandı).
Tam paket konteynerde: **2131 passed, 3 skipped** (önceki 2096+35, mevcut
testlerde SIFIR değişiklik); ruff `All checks passed!`; mypy `Success: no
issues found in 51 source files`. Üç kapı da 1. turda yeşil.

**Kabul kriterleri:** 6/6 karşılandı. "Dokunma" listesi korundu: models.py ve
managers/** DEĞİŞMEDİ (seed sabitleri yalnız import edildi).

**NOT (kapsam içi düzeltme):** docstring'in YAZILAN bölümündeki iki bayat kalem
kodla eşitlendi — `last_status` → `dosya_son_durumu` (modelde olmayan kalıntı
ad) ve `istinaf_basvuran_taraf` listeden çıktı (kart yolundan yazılmıyor, tek
yazıcısı aşama fotoğrafı; KART_ALANLARI yorumu zaten böyle diyor).

**İzlenecekler:** başlıklar karşı tarafla yazılı sabitlenince `SUTUN_ADAYLARI`
demetine varyant eklemek tek satırlık iş; ilk gerçek partide satır raporundaki
`yazılmadı` kayıtları değer-havuzu ölçümü olarak okunmalı (havuz genişletme
kararı oradan beslenir).
