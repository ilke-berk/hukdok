# G106 — Takip paneli: Olay Türü + Hükümdeki Rol yazma arayüzü (G103'ün UI eşi)

- **Bant:** frontend
- **Bağımlı:** - (G103 + G105 main'de: e6d44ed / a6e13eb — sözleşme onların dondurduğu)
- **Dosya kapsamı:** frontend/src/components/CaseTrackingPanel.tsx,
  frontend/src/components/CaseTrackingPanel.test.tsx, frontend/src/lib/trackingDraft.ts,
  frontend/src/lib/trackingDraft.test.ts, frontend/src/lib/caseCardFields.test.ts
  (YALNIZ aşağıdaki kilit istisnası), frontend/src/hooks/useCases.ts (YALNIZ
  CaseTrackingUpdate'e iki alan bildirimi)
- **Dokunma:** frontend/src/hooks/useConfig.ts (eventTypes/judgmentRoles G105'te ZATEN
  eklendi — kullan, değiştirme), frontend/src/pages/CaseDetails.tsx ve
  frontend/src/lib/caseCardFields.ts (kart görünümü G105'in işi — dokunma)

> **02.09b koşusu notu:** İlk koşu BLOKE bıraktı — uygulama sözleşmeye birebir uygun
> yapıldı ama G073 kesişim kilidi (`caseCardFields.test.ts:115-122`) yapısal olarak
> kırmızıya düştü ve işçi kapsam dışı testi haklı olarak değiştirmedi. TAM UYGULAMA
> `C:/dev/hukudok-wt/G106` worktree'sinde KOMİTSİZ hazır duruyor (4 kapsam dosyası +
> 13 yeni test). Bu tur o worktree'nin üstüne kapatma turudur: aşağıdaki kilit
> istisnasını uygula, paketi yeşile getir, commit'le.

## Hedef

G103 iki alanın yazma yolunu backend'de takip paneline bağladı (`TRACKING_FIELDS` +
`CaseTrackingUpdate` + G066 kapalı-liste doğrulaması) ama panelin ARAYÜZÜNDE bu iki alan
yok — avukatın elle giriş/düzeltme yolu eksik kaldı (2026-09-02 koşusunun tespit ettiği
boşluk). G061/G065 emsalinin aynısı: kartta salt-okunur görünüm (G105 yaptı), takip
panelinde düzenleme (bu görev). Ekip bilgilendirme PDF'indeki deneme adımı 2 bu görevle
çalışır hâle gelir.

## SÖZLEŞME (G103/G105 ile ORTAK — değiştirilemez)

- Alanlar: `olay_turu` ("Olay Türü") ve `hukumdeki_rol` ("Hükümdeki Rol") —
  `CaseTrackingUpdate` bunları zaten kabul ediyor; liste dışı değer backend'de 400.
- Değer kaynakları: `useConfig().eventTypes` (3 değer) ve `useConfig().judgmentRoles`
  (4 değer) — G105 ekledi, mevcut karar dropdown'larının beslenme deseniyle aynı.
- Boş seçim alanı TEMİZLER (backend None → NULL yazar; "karar okunmadı"na dönüş meşru).

## Yapılacaklar

1. `trackingDraft.ts`: iki alan taslak alan listesine — mevcut alanların (örn.
   `dosya_son_durumu` / arabuluculuk alanları) deseniyle; taslak kalıcılığı ve kirlilik
   denetimi aynı davranır.
2. `CaseTrackingPanel.tsx`: iki `Select` — AŞAMAYA BAĞLI DEĞİL, genel dosya alanları
   bölümüne (dosya_son_durumu/arabuluculuk/arşiv grubu neredeyse oraya; aşama
   sekmelerinin içine KOYMA — değer tek slot, "güncel kademeye göre" kuralı etikette/
   tooltip'te söylenir). Karar dropdown'larının bileşen deseni birebir (boş "—" seçeneği
   dahil, liste boşsa mevcut davranış neyse o).
3. Kaydetme yükü: iki alan gönderilen payload'a yalnız DEĞİŞTİYSE girer (panelin mevcut
   sözleşmesi neyse ona uy — özel kural icat etme).
4. **G073 kilit istisnası** (`caseCardFields.test.ts:115-122`, planlayıcı kararı 02.09):
   kesişim beklentisi `toEqual([])` yerine TAM LİSTE olur — yalnız
   `["hukumdeki_rol", "olay_turu"]` (iki taraf da sıralanarak karşılaştırılır).
   Teste yorum: kural ("aynı alan iki ekranda DÜZENLENEMEZ") korunuyor — kart tarafı
   salt-okunur gösterimdir (TransferFieldsCard düzenleme kontrolü taşımaz), düzenleme
   TEK yerde (takip paneli); tam-liste kilidi gelecekteki kazara eklemeleri yakalamaya
   devam eder. `istinaf_basvuran_taraf` testindeki (:124) gerekçe üslubu örnek alınsın.
5. `useCases.ts::CaseTrackingUpdate` arayüzüne iki Optional alan bildirimi (02.09b
   koşusunun notu: bugün cast sayesinde çalışıyor, tip bildirimi eksik).

## Kabul kriterleri

- [x] Panelde iki dropdown genel bölümde görünüyor; değerler useConfig listelerinden
      geliyor (mock'lu test).
- [x] Seçim + kaydet → payload'da `olay_turu`/`hukumdeki_rol` doğru adlarla (test);
      dokunulmadıysa payload'a GİRMİYOR (test).
- [x] Boş seçim alanı temizliyor (payload'da null/boş sözleşmesi panelin mevcut temizleme
      davranışıyla aynı — test).
- [x] G073 kilidi tam-liste istisnasıyla güncellendi (yalnız bu iki anahtar; gerekçe
      yorumu testte); BUNUN DIŞINDA mevcut testlerde gerileme yok; taslak kalıcılığı
      iki alanı da kapsıyor.
- [x] `CaseTrackingUpdate` iki alanı tipte bildiriyor; `tsc -b` temiz.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

DURUM: TAMAM

Paket iki oturumda kapandı: 02.09b gece koşusu uygulamayı sözleşmeye birebir yazıp
G073 kilidi yüzünden haklı olarak BLOKE bıraktı (test bütünlüğü — kapsam dışı test
değiştirilmedi); planlayıcı 02.09'da kilit istisnasını görev tanımına yazdı; bu
kapatma turu istisnayı uygulayıp paketi yeşile getirdi ve commit'ledi.

### Yapılanlar

1. `trackingDraft.ts` — `EVENT_FIELDS` sabiti (Olay Türü → `event_types`, Hükümdeki
   Rol → `judgment_roles`), `EventListKey`/`PanelListKey` tip genişletmesi
   (`FieldDef.optionsFrom` artık karar + belgeleme liste ailelerini alır), iki
   anahtar `TRACKING_DRAFT_KEYS`e — taslak kalıcılığı/kirlilik denetimi kendiliğinden
   aynı davranır. `PANEL_FIELDS`ten bilinçli AYRI sabit: o grubun envanteri G073
   testiyle fotoğraflı.
2. `CaseTrackingPanel.tsx` — `decisionLists` → `configLists` (iki yeni liste girdisi);
   genel karta ("Dosya Son Durumu" bölümünün kardeşi, aşama sekmelerinin DIŞINA)
   "Belgeleme Olayı" alt bloğu; `renderField` select yolu birebir (Seçiniz + liste
   dışı damgası + boş liste davranışı); tek-slot / "güncel kademedeki hükme göre"
   kuralı görünür metin + title tooltip ile.
3. Kaydetme yükü panelin mevcut sözleşmesiyle: `buildPatch` yalnız dirty anahtarları
   koyar, boş seçim `null` gider (backend None → NULL). Özel kural eklenmedi.
4. G073 kilit istisnası (`caseCardFields.test.ts:115`): kesişim beklentisi tam-liste —
   iki taraf da sıralanıp `["hukumdeki_rol", "olay_turu"]` ile karşılaştırılır;
   başlık gerçeğe çevrildi, gerekçe yorumu testte (kural korunuyor: kart salt-okunur
   gösterim, düzenleme TEK yerde — takip paneli; tam-liste kilidi kazara eklemeleri
   yakalamayı sürdürür, yeni ortak anahtar ancak bilinçli istisnayla girer).
5. `useCases.ts::CaseTrackingUpdate` — `olay_turu`/`hukumdeki_rol` Optional bildirimi;
   panelin mevcut `patch as CaseTrackingUpdate` cast'i artık tipçe de doğru.

### Testler

- Yeni 13 test: `trackingDraft.test.ts` +6 (sözleşme kilidi anahtar/etiket/liste,
  taslak anahtarları, aşamaya gömülmeme + PANEL_FIELDS'e karışmama, yalnız-değişen
  patch, boş seçim → null, rebase kalıcılığı); `CaseTrackingPanel.test.tsx` +7
  (sıralı dropdown içerikleri, tek-slot kuralı ekranda, payload adları + dokunulmayan
  alan girmez, çift alan birlikte, boş seçim → null, liste dışı kayıtlı değer damgalı
  korunur, boş liste davranışı). configMock'a `eventTypes`/`judgmentRoles` eklendi
  (fixture yüzeyi eşitleme, beklenti değişikliği değil).
- Değişen mevcut test: YALNIZ görev tanımındaki kilit istisnası.
- Doğrulama (kapatma turu, TUR 1'de yeşil): vitest **580 passed (580, 47 dosya)** —
  02.09b'deki tek kırmızı kapandı; eslint **0 error / 22 warning** (hepsi kapsam dışı
  dosyalarda önceden vardı); `tsc -b --force` **temiz**.

### Kararlar

- Blok sekmeye değil genel karta kondu: değer tek slot, sekmeye koymak "aşama başına
  değer" iddiası doğururdu (görev tanımı kararı; yorumda gerekçeli).
- Kilit istisnasında beklenti boş-değil-tam-liste: kilidin koruma işlevi sürer,
  istisna görünür ve gerekçeli.

### İzlenecekler

- Dropdown değerleri `name` ile gider (karar dropdown deseniyle aynı); G103 backend
  doğrulaması da name kabul ediyor — uyumlu, ek iş yok.
- G105 kart gösterimi + bu yazma yolu birlikte ekip PDF'indeki deneme adımı 2'yi
  çalışır kılar; prod çıkışında config seed'lerinin (3+4 değer) canlıda görünmesini
  kontrol etmek yeterli.
