# G100 — Yazdırma popup'larında inline script/handler kaldırılır (CSP enforce ön koşulu)

- **Bant:** frontend
- **Bağımlı:** -
- **Dosya kapsamı:** frontend/src/components/dashboard/DashboardCalendar.tsx
  (yalnız `printReport`), frontend/src/components/YetkiBelgesiModal.tsx (yalnız
  `handlePrint`), frontend/src/components/dashboard/DashboardCalendar.print.test.tsx
  (YENİ), frontend/src/components/YetkiBelgesiModal.print.test.tsx (YENİ)
- **Dokunma:** frontend/src/pages/CaseDetails.tsx (DOKUNMA — blob sekmesi CSP'ye
  tabi değil), nginx.conf (DOKUNMA — G101'in işi), frontend/index.html (DOKUNMA)

## Hedef

2026-08-22 CSP ihlal turu (lokal 8080, Report-Only): uygulamanın kendisi
politikayla uyumlu — `script-src`/`style-src`/`font-src`/`frame-src` ihlali
sıfır (tek ihlal sınıfı lokal `VITE_API_URL` artefaktıydı, prod'da yok). Kod
okumasında turun tetiklemediği **iki enforce kırıcı** bulundu; ikisi de
`window.open("", "_blank")` + `document.write` ile açılan yazdırma popup'ı:

1. `DashboardCalendar.tsx` `printReport`: yazılan HTML'de
   `<body onload="window.print()">` — inline event handler (`script-src-attr`).
2. `YetkiBelgesiModal.tsx` `handlePrint`: yazılan HTML'de
   `<script>window.onload=function(){window.print()}</script>` — inline script
   (`script-src-elem`).

Kontrol deneyi ana dokümanda aynı iki deseni `script-src-attr|report` ve
`script-src-elem|report` olarak yakaladı — zorlayıcı politika altında kesin
engellenir. `about:blank` popup'ın bu politikayı miras alıp almadığı lokal
panelde doğrulanamadı (popup engelleyici); spec mirası öngörür. Belirsizliği
taşımaya gerek yok: düzeltme ucuz ve her CSP altında çalışır.

## Yapılacak

İki yerde de yazdırma tetiği popup'ın İÇİNDEN değil **açandan** verilir:

```ts
w.document.open(); w.document.write(html); w.document.close();
w.addEventListener("load", () => w.print(), { once: true });
// document.close() senkron yükleme bitirmiş olabilir: readyState zaten
// "complete" ise load hiç gelmez → o durumda doğrudan w.print().
```

Yazılan HTML'den `onload=` ve `<script>` tamamen çıkar. `<style>` bloğu kalır
(`style-src 'unsafe-inline'` bilinçli açık, G091). `window.print` çağrısının
popup kapanmadan önce koşması kritik — `readyState` kontrolü bu yüzden.

## Kabul kriterleri

- [ ] İki dosyada da popup'a yazılan HTML'de `<script` ve `onload=` YOK
      (test: `document.write`'a giden string assert edilir)
- [ ] `print()` açandan tetikleniyor: `readyState === "complete"` ise hemen,
      değilse `load` olayında — iki dal da testli (popup `window.open` mock'u ile)
- [ ] Popup engellenince (`window.open` null) mevcut toast davranışı AYNEN
      korunuyor (test)
- [ ] Yazdırılan içerik değişmedi: `DashboardCalendar` tablo gövdesi ve
      `YetkiBelgesiModal` `printRef` içeriği aynen HTML'e gidiyor (test, snapshot
      DEĞİL — `<table`/`.yb-title` gibi işaretler assert edilir)
- [ ] `grep -rnE "onload=|<script>" frontend/src --include=*.tsx` yalnız test
      dosyalarında eşleşiyor (ya da hiç)
- [ ] Bu iki bileşenin mevcut testleri (varsa) aynen geçiyor

## Doğrulama

```
npm --prefix frontend test
npm --prefix frontend run lint
npx --prefix frontend tsc -b --force frontend
```

## Bu görevin KAPSAMADIĞI doğrulama (kullanıcıya kalır)

Gerçek yazdırma diyaloğu gece oturumunda açılamaz. G101 enforce'tan sonra
insan turu: Takvim raporu "Yazdır" + Yetki Belgesi "Yazdır" → diyalog geliyor
mu; ayrıca dava kartından bir PDF'in yeni sekmede açılması.

## Rapor

(işçi oturumu doldurur)
