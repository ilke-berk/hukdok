# G009 — Denetim: bayat yol referansları + docstring/şerh düzeltmeleri (davranışsız)

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/case_intake_analyzer.py, backend/services/case_intake.py, backend/routes/case_intake.py, backend/services/confirm_idempotency.py, docs/arsiv/README.md, docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md
- **Dokunma:** gorevler/gorev/G001.md–G005.md (tarihsel görev kaydı — ASLA düzeltilmez), backend'de yukarıdakiler dışında HER ŞEY (bu görev davranış değiştirmez)

## Hedef

2026-08-11 bağımsız denetimi, G006 reorg'undan kalan bayat yol referanslarını ve iki
yanıltıcı doküman cümlesini buldu. Bu görev YALNIZ yorum/docstring/md satırlarına dokunur;
`git diff`te tek bir çalışır kod satırı değişmez.

Düzeltilecekler (satır numaraları 5abb934 itibarıyla; kaymış olabilir, içerikle bul):

1. `backend/case_intake_analyzer.py:7` ve `backend/services/case_intake.py:7` —
   yorumdaki `docs/otonom-dava-acma-gelistirme-plani-2026-07-24.md` yolu artık
   `docs/arsiv/otonom-dava-acma-gelistirme-plani-2026-07-24.md` (dosyanın orada
   olduğunu doğrula, yolu güncelle).
2. `backend/routes/case_intake.py:86` — `docs/eml-intake-...` referansı; gerçek dosyayı
   `docs/arsiv/` altında bul, yolu güncelle.
3. `backend/services/confirm_idempotency.py:56-57` — "yaratıldıysa ... satır kalıcı kilit
   görevi görür" cümlesi kodla örtüşmüyor: kilit kalıcı DEĞİL, 30 dk bayat eşiğinden sonra
   `begin()` devralır ve pipeline yeniden koşabilir (bilinçli takas). Docstring'i
   `routes/processing.py:907-908` yorumundaki doğru anlatıma hizala.
4. `docs/arsiv/README.md:31-37` — üç backend referansı için "düzeltilmeleri G007
   kapsamındadır" diyor; G007 kod değişikliğini yasaklamıştı ve kapandı. Bu görevle
   referanslar düzeldiğine göre istisna tablosunu kaldır ya da "G009'da düzeltildi"
   olarak güncelle.
5. `docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md:67` (2026-08-11 (26) notu) —
   "eski yollara işaret eden referans 0" iddiası yanlıştı (9 satır vardı: 3 backend +
   6 gorevler/gorev). Nota tek satırlık düzeltme şerhi ekle (mevcut metni silme,
   "DÜZELTME (G009):" öneki ile ekle). G001-G005 içindeki 6 satıra DOKUNMA.

## Kabul kriterleri

- [ ] `grep -rn "docs/otonom-dava-acma" backend/` ve `grep -rn "docs/eml-intake" backend/` yalnız `docs/arsiv/...` biçimli yollar döndürüyor (eski düz `docs/<dosya>` biçimi backend'de 0)
- [ ] Güncellenen her yolun hedef dosyası gerçekten var (Read ile doğrulanmış)
- [ ] `confirm_idempotency.py` docstring'i 30 dk devralma gerçeğini anlatıyor; "kalıcı kilit" ifadesi kalmadı
- [ ] `docs/arsiv/README.md` ölü G007 yönlendirmesi içermiyor
- [ ] Takip dosyasında düzeltme şerhi var; eski not metni korunmuş
- [ ] `git diff` yalnız yorum/docstring/md satırları — çalışır kod satırı 0 (Rapor'a diff özeti)
- [ ] pytest tabanı korunmuş (859 passed + 2 skipped altına düşmek yasak)

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`

## Rapor

(işçi oturumu doldurur)
