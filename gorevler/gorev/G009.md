# G009 — Denetim: bayat yol referansları + docstring/şerh düzeltmeleri (davranışsız)

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/case_intake_analyzer.py, backend/services/case_intake.py, backend/routes/case_intake.py, backend/services/confirm_idempotency.py, docs/arsiv/README.md, docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md
- **Dokunma:** gorevler/gorev/G001.md–G005.md (tarihsel görev kaydı — ASLA düzeltilmez), backend'de yukarıdakiler dışında HER ŞEY (bu görev davranış değiştirmez)

## Hedef

2026-08-11 bağımsız denetimi, G006 reorg'undan kalan bayat yol referanslarını ve iki
yanıltıcı doküman cümlesini buldu. Bu görev YALNIZ yorum/docstring/md satırlarına dokunur;
`git diff`te tek bir çalışır kod satırı değişmez.

Düzeltilecekler (satır numaraları 5abb934 itibarıyla; kaymış olabilir, içerikle bul):

1. `backend/case_intake_analyzer.py:7` ve `backend/services/case_intake.py:7` —
   yorumdaki `docs/otonom-dava-acma-gelistirme-plani-2026-07-24.md` yolu artık
   `docs/arsiv/otonom-dava-acma-gelistirme-plani-2026-07-24.md` (dosyanın orada
   olduğunu doğrula, yolu güncelle).
2. `backend/routes/case_intake.py:86` — `docs/eml-intake-...` referansı; gerçek dosyayı
   `docs/arsiv/` altında bul, yolu güncelle.
3. `backend/services/confirm_idempotency.py:56-57` — "yaratıldıysa ... satır kalıcı kilit
   görevi görür" cümlesi kodla örtüşmüyor: kilit kalıcı DEĞİL, 30 dk bayat eşiğinden sonra
   `begin()` devralır ve pipeline yeniden koşabilir (bilinçli takas). Docstring'i
   `routes/processing.py:907-908` yorumundaki doğru anlatıma hizala.
4. `docs/arsiv/README.md:31-37` — üç backend referansı için "düzeltilmeleri G007
   kapsamındadır" diyor; G007 kod değişikliğini yasaklamıştı ve kapandı. Bu görevle
   referanslar düzeldiğine göre istisna tablosunu kaldır ya da "G009'da düzeltildi"
   olarak güncelle.
5. `docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md:67` (2026-08-11 (26) notu) —
   "eski yollara işaret eden referans 0" iddiası yanlıştı (9 satır vardı: 3 backend +
   6 gorevler/gorev). Nota tek satırlık düzeltme şerhi ekle (mevcut metni silme,
   "DÜZELTME (G009):" öneki ile ekle). G001-G005 içindeki 6 satıra DOKUNMA.

## Kabul kriterleri

- [x] `grep -rn "docs/otonom-dava-acma" backend/` ve `grep -rn "docs/eml-intake" backend/` yalnız `docs/arsiv/...` biçimli yollar döndürüyor (eski düz `docs/<dosya>` biçimi backend'de 0)
- [x] Güncellenen her yolun hedef dosyası gerçekten var (Read ile doğrulanmış)
- [x] `confirm_idempotency.py` docstring'i 30 dk devralma gerçeğini anlatıyor; "kalıcı kilit" ifadesi kalmadı
- [x] `docs/arsiv/README.md` ölü G007 yönlendirmesi içermiyor
- [x] Takip dosyasında düzeltme şerhi var; eski not metni korunmuş
- [x] `git diff` yalnız yorum/docstring/md satırları — çalışır kod satırı 0 (Rapor'a diff özeti)
- [x] pytest tabanı korunmuş (859 passed + 2 skipped altına düşmek yasak)

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`

## Rapor

**Sonuç: TAMAM.** Beş düzeltmenin hepsi uygulandı; `git diff`te tek bir çalışır kod satırı yok.

### Yapılanlar

1. **`case_intake_analyzer.py:7` + `services/case_intake.py:7`** — yol
   `docs/arsiv/otonom-dava-acma-gelistirme-plani-2026-07-24.md` yapıldı. Hedef dosya Read ile
   doğrulandı (başlık: "Otonom Dava Açma — Geliştirme Planı", tarih 2026-07-24).
2. **`routes/case_intake.py:86`** — kısaltılmış `docs/eml-intake-gelistirme-plani` referansı
   gerçek dosyaya bağlandı: `docs/arsiv/eml-intake-gelistirme-plani-2026-08-01.md` (Read ile
   doğrulandı: "Sihirbaza .eml Desteği — Geliştirme Planı (2026-08-01)"). Tam ad satırı 120
   sınırına yaklaştırdığı için yorum iki satıra bölündü.
3. **`services/confirm_idempotency.py:52-58`** — "satır kalıcı kilit görevi görür" ifadesi
   kaldırıldı. Yerine kodun gerçeği yazıldı: belge yaratılmadan düşerse route release eder;
   yaratıldıysa satır `in_progress` kalır ve retry'lar 409 alır, **ama kilit kalıcı değildir** —
   eşik dolunca `begin()` satırı devralıp `proceed` döner.
4. **`docs/arsiv/README.md:28-40`** — ölü G007 yönlendirmesi ve istisna tablosu kaldırıldı;
   yerine tek paragraflık güncel kayıt ("G006'da altyapı, G009'da üç backend yorumu").
   Arşiv dosyalarının **kendi içindeki** eski yazımlara dair not korundu (hâlâ geçerli).
5. **`docs/plan/guvenilirlik-sertlestirme-uygulama-takibi.md`** — düzeltme şerhi eklendi.
   **Not:** iddia görev dosyasında satır 67 / "(26) notu" olarak gösteriliyordu; gerçekte
   satır **69**, **2026-08-11 (24)** (G006) notundaydı. İçerikle bulundu, eski metin silinmedi.

### Alınan kararlar + gerekçeleri

- **Madde 3'te docstring'i `processing.py:907-908` ile hizalarken "bilinçli takas" cümlesi
  eklendi.** Salt "30 dk sonra devralınır" demek, okuyucuda "mükerrer belge riski gözden mi
  kaçmış?" sorusu bırakırdı; takasın bilinçli olduğunu yazmak yorumun asıl işi.
- **Madde 4'te tablo kaldırıldı, "G009'da düzeltildi" seçeneği tercih edilmedi.** Tablonun
  başlığı "hâlâ eski yolla atıf alan dosyalar" idi; artık böyle dosya yok, tabloyu doldurmak
  yanlış olurdu. Tarihsel iz paragrafta korundu.
- **Madde 5'te G009 için yeni durum notu EKLENMEDİ.** Plan 26. notta "PLAN KAPANDI" damgasını
  yedi; kapanmış plana yeni oturum satırı eklemek banner'la çelişirdi. Şerh, hatalı iddianın
  bulunduğu satıra iliştirildi — yanlış iddiayı okuyan düzeltmeyi aynı yerde görür.
- **`gorevler/gorev/G001-G005` içindeki 6 satıra dokunulmadı** (dokunma listesi): tarihsel
  görev kaydı, yazıldığı günün doğrusu.

### Doğrulama

Denetimin "9 satır (3 backend + 6 gorevler/gorev)" sayımı **bağımsız doğrulandı**: backend'deki
3'ü doğrudan görüldü; `gorevler/gorev/G001-G005` içindeki 6 satır ise `docs/otonom-dava-acma`
deseninde değil, **`docs/guvenilirlik-sertlestirme-uygulama-takibi.md`** yolunda çıktı
(G001:6, G002:13, G003:11, G004:10, G005:5, G005:80). Denetim haklıydı.

README'ye yazdığım "kod tarafında düz `docs/<ad>.md` kalmadı" cümlesi de — kapsayıcı bir iddia
olduğu için — `*.{py,sh,yml,yaml,ps1,ts,tsx}` üzerinde arsiv/plan/mimari/kararlar dışlanarak
grep'lendi: **0 eşleşme**.

Kapılar (konteynerde, `docker-compose.override.yml` aktif → bind-mount ile değişiklikler görülüyor;
konteynerdeki `case_intake_analyzer.py:7`'nin yeni satırı taşıdığı ayrıca teyit edildi):

| Kapı | Sonuç |
| --- | --- |
| `pytest` | **859 passed, 2 skipped** (16.16 sn) — taban korundu |
| `ruff check .` | All checks passed |
| `mypy` | Success: no issues found in 29 source files |

### Diff özeti (çalışır kod satırı: 0)

| Dosya | Değişim | Tür |
| --- | --- | --- |
| `backend/case_intake_analyzer.py` | 1 satır | modül docstring'i |
| `backend/routes/case_intake.py` | 1 → 2 satır | blok yorumu |
| `backend/services/case_intake.py` | 1 satır | modül docstring'i |
| `backend/services/confirm_idempotency.py` | 2 → 6 satır | sabit üstü yorum |
| `docs/arsiv/README.md` | −13 / +7 | md |
| `docs/plan/guvenilirlik-...-takibi.md` | 1 satır (ekleme) | md |

`git diff -U0 -- backend` çıktısının tamamı yorum/docstring satırlarından oluşuyor; hiçbir
ifade, imza, sabit değeri veya kontrol akışı değişmedi. `STALE_IN_PROGRESS_SECONDS = 30 * 60`
dokunulmadan duruyor.

### İzlenecekler

- **NOT (kapsam dışı, düzeltilmedi):** çalışma ağacında `.claude/settings.local.json` (M) ve
  `.claude/launch.json` (??) kirli. İkisi de harness'ın kendi izin/oturum kayıtları, görev
  kapsamı dışı — G005 ve G008'deki emsale uyularak BLOKE sayılmadı, commit'e de girmedi.
- Denetimin bu turda yakaladığı desen (G006'nın "referans 0" iddiası) genel bir risk:
  kapsayıcı grep iddiaları desen + dosya kapsamıyla birlikte raporlanmalı. Şerhte ders olarak
  yazıldı; ileride benzer iddia yazan görevler bunu uygulamalı.
- `docs/arsiv/README.md` artık G006 + G009'a atıf yapıyor; arşiv yolları yeniden taşınırsa bu
  paragraf da tazelenmeli.
