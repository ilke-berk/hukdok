# G175 — Sohbet öncelikli rapor ekranı: yerleşim — manuel kurucu kalkar, sohbet → tanım şeridi → tablo; anahtar kapalı durumu

- **Bant:** frontend
- **Bağımlı:** G173,G174
- **Dosya kapsamı:** frontend/src/pages/ReportsPage.tsx (yerleşim + `TanimSeridi`/`onFiltreEkle` bağlama + anahtar kapalı
  kartı), frontend/src/pages/ReportsPage.test.tsx, ReportsPage.sablon.test.tsx, ReportsPage.favori.test.tsx,
  ReportsPage.asistan.test.tsx (**beklenti değiştirme izni VAR**: `SourceCards` radiogroup, `etkin-filtreler`, `kolon-dugmesi`,
  `kolon-paneli` kimlikleri kalkar, `tanim-seridi` gelir), KALDIRILANLAR: frontend/src/components/reports/SourceCards.tsx (+test),
  QuickFilters.tsx (+test), ColumnSheet.tsx (+test), ColumnPicker.tsx, frontend/src/components/ui/sheet.tsx (başka kullanıcı
  yoksa — `grep` ile doğrula, raporda), frontend/src/components/reports/TemplateBar.tsx (kompakt mod — yalnız görsel),
  frontend/src/components/reports/ExportButtons.tsx (sayaç satırına taşınırsa)
- **Dokunma:** AssistantBar*/AssistantMessage/AssistantThread/reportsChat.ts (G174 bitti — DEĞİŞMEZ), TanimSeridi.tsx /
  builderState.ts (G173 bitti — DEĞİŞMEZ; eksik varsa BLOKE bırak), FilterChip/FilterControl/FieldPicker/SearchBox/ChipSelect/
  ToggleFilter (şerit kullanıyor — KALIR), PreviewTable.tsx (sıralama/sayfalama aynen), lib/reports.ts (yalnız okunur), backend
  (sıfır), routes/sekme yapısı (Rapor / Şablonlar / İndirme geçmişi AYNEN)

## Hedef

Kullanıcı kararı (11.09 gündüz): "arayüz deli gibi sadeleşsin". Rapor sekmesi üç bloğa iner:

```
[ AssistantBar — tam genişlik, örnek istemler, inline konuşma ]          ← tek giriş noktası (G174 davranışı)
[ TanimSeridi — Davalar ▾ · kolon çipleri · filtre çipleri · sıralama · Temizle ]   ← şeffaflık + düzeltme (G173)
[ sayaç satırı: "1.284 kayıt" · ★ şablon (kompakt) · Excel · CSV ]   +   [ PreviewTable ]
```

Kaynak kartları, filtre şeridi, "Kolonlar (N)" yan paneli, `HairlineCard` sarmalları KALKAR. Sayfa dolu açılır (katalog
gelince ilk kaynak + varsayılan kolonlar + filtresiz önizleme — G139 davranışı aynen). Şeritteki her değişiklik
`durumDegisti` üzerinden otomatik önizlemeyi tetikler (G138 sözleşmesi: yapısal hemen, yazarak 600 ms, odak çıkışı hemen).

**Anahtar kapalı / 409 durumu:** sohbet artık birincil yol olduğundan `rapor_asistani` kapalıyken sayfa boş KALMAZ: asistan
satırının yerinde tek satırlık bilgi kartı ("Rapor asistanı kapalı — yönetici panelinden `rapor_asistani` anahtarını açın";
`ASISTAN_KAPALI_MESAJI`), şerit + tablo + şablonlar çalışmaya devam eder (şerit tam bir yedek kurucudur — Gemini düşse de
rapor alınır; kullanıcı bunu bilerek kabul etti, ama yedek bedavaya geliyor).

## Kabul kriterleri

- [ ] JSX sırası (Rapor sekmesi): `AssistantBar` (ya da kapalı kartı) → `TanimSeridi` → sayaç/şablon/indirme satırı →
      `FavoritePrompt` (koşullu, aynen) → `PreviewTable`. `SourceCards`, `QuickFilters`, `ColumnSheet` import'u YOK.
- [ ] `TanimSeridi` prop bağlama: `durum`, `onChange=durumDegisti`, `onHemen=hemenOnizle`, `onKaynakSec` (mevcut; kaynak
      değişince `kaynakIcinBaslangic` + önizleme anında düşer — "kaynak değişimi" testleri kimlik değişimi dışında aynen geçer),
      `bugun`.
- [ ] `AssistantBar` yeni prop `onFiltreEkle(alan, deger)` bağlanır: mevcut duruma `filtreEkle` + değer (op: kolonda `eq`
      varsa `eq`, yoksa `contains`) → `durumDegisti` → önizleme; toast "Filtre eklendi · <etiket>".
- [ ] Şablon yükleme (`tanimiYukle`) ve favori akışı (`favoriOner`/`favoriEkle`/`FavoritePrompt`) aynen; `TemplateBar` kompakt:
      tek satırda seçim + Yükle/Kaydet/… menüsü, "★ Kayıtlı"/"☆ Favorilere ekle" korunur (`TemplateBar.test.tsx` 4 test geçer).
- [ ] Anahtar kapalı (`asistanAnahtari === false` ya da `asistan409`) → `data-testid="asistan-kapali-karti"` görünür, şerit ve
      tablo çalışır; anahtar okunana dek iskelet (aynen).
- [ ] Kaldırılan dosyalar ve testleri silinir; `ui/sheet.tsx` başka kullanıcı yoksa silinir (raporda `grep` kanıtı), varsa kalır.
      `ColumnPicker.tsx` yalnız `ColumnSheet` kullanıyorsa silinir; `TanimSeridi` bir parçasını kullanıyorsa o parça kalır.
- [ ] Sayfa testleri: `ReportsPage.test.tsx` "sayfa dolu açılır", "kaynak değişimi", "otomatik önizleme 600 ms", "sıralama
      döngüsü", "sayfalama" beklentileri yeni kimliklerle geçer; yeni testler: şeritten filtre kaldırınca önizleme hemen istenir,
      `onFiltreEkle` ile filtre eklenir, anahtar kapalı kartı + şerit çalışır. Toplam frontend testi: kaldırılan QuickFilters(23)/
      ColumnSheet(6)/SourceCards(2) testleri düşer, buna rağmen toplam ≥ 899 − 31 + (G173/G174 eklemeleri) — raporda sayı.
- [ ] Responsive: `lg` altında sayaç satırı sarar, şerit sarar, sayfa yatay kaydırmaz; max-w cap YOK.
- [ ] `git diff --stat`: AssistantBar*/AssistantMessage/AssistantThread/reportsChat.ts/TanimSeridi.tsx/builderState.ts
      DEĞİŞMEDİ; backend sıfır.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

(işçi oturumu doldurur)
