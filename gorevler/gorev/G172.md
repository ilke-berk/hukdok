# G172 — Rapor ekranı üç aşama: raporlama.md §8 yeniden yazımı + CLAUDE.md paragrafı (koddan doğrulanmış)

- **Bant:** docs
- **Bağımlı:** G171 (ekranın son hali)
- **Dosya kapsamı:** docs/mimari/raporlama.md (§1 bileşen tablosu frontend satırı, giriş ağaç şeması, §8 tamamı, §13
  sınırlar, §15 test satırı, §16 "nereye bakmalı"), CLAUDE.md ("Raporlama (G130-G139)" paragrafı — ekran cümlesi),
  gorevler/KUYRUK.md (DOKUNMA — runner yazar)
- **Dokunma:** docs/plan/raporlama-plani-2026-09-06.md (§4.1 yerleşimi artık geçerli değil → dosyanın başına
  "uygulamada değişti: üç aşama, bkz. raporlama.md §8" şerhi; plan gövdesi yeniden yazılmaz), docs/arsiv (yazma)

## Hedef

G169-G171 ekranı "süz ve gör"den "üç aşamalı tek sayfa"ya çevirdi; §8 (8.1-8.5) ve giriş şeması artık eski
yerleşimi anlatıyor. ALTIN KURAL: her iddia koddan okunarak yazılır (dosya/işlev adı + satır), tahmin yazılmaz;
kodla çelişen her eski cümle düzeltilir. Bu görev gece koşusunda docs bandında, test yok.

## Kabul kriterleri

- [ ] Giriş ağaç şeması: `AssistantBar` → sayfa başlığı + `TemplateBar` → `StageSection` ×3 (1: `SourceCards` + sekmeler
  + `ColumnPicker`; 2: kart ızgarası A + görünmeyen alan B; 3: sayaç/lejant/`ExportButtons` + `PreviewTable`) —
  gerçek bileşen/dosya adlarıyla (`ls frontend/src/components/reports` ile doğrulanmış; kaldırılan dosyalar
  "KALDIRILDI (G169/G170)" şerhiyle §1 tablosunda).
- [ ] §8 yeniden: 8.1 aşama kabuğu (`StageSection`, tek açık, özetler, huniden açılma API'si), 8.2 kaynak + sekmeler +
  kolon seçimi (ilişki sekmeleri `iliskiler`/`bag`'den, renk eşlemesi `ui.ts`, hazır setler, 60 tavanı), 8.3 filtre
  kartları (A/B bölümleri, `hizli_filtreler` = öneri çipleri, `serit` semantiği, A↔B taşınma, 20 tavanı, temizle,
  kontrol→op tablosu AYNEN — `lib/reports.ts` satırları yeniden okunur), 8.4 otomatik önizleme (değişmediyse
  "değişmedi" + satır numaraları güncel), 8.5 önizleme başlığı (huni, kaynak şeridi, lejant, sıralama döngüsü).
  Her başlık "hangi dosya, hangi işlev, hangi satır" taşır.
- [ ] §12 "Plan ile kod arasındaki farklar"a üçüncü tur satırları (F23+): yan panel kalktı, yuvalar öneriye döndü,
  şablon çubuğu sekme üstüne çıktı, sebebiyle.
- [ ] §13 sınırlara: "arama yalnız açık sekmeyi süzer", "B bölümündeki filtre tabloda huni göstermez", 145 kolonlu
  sekme listesinin render maliyeti (ölçüm varsa yaz, yoksa "ölçülmedi").
- [ ] §15 test satırı: G169-G171 test dosyaları + toplam sayı (koşarak: `npm --prefix frontend test` özet satırı
  rapora, dokümana sayı + tarih).
- [ ] CLAUDE.md raporlama paragrafı: "kaynak kartı + katalogdan gelen filtre şeridiyle" cümlesi üç aşamayı anlatacak
  şekilde tek cümle değişir; başka satır dokunulmaz.
- [ ] Plan dosyasının başına "uygulamada değişti" şerhi (tarih + görev id'leri); §4.1 gövdesi silinmez.
- [ ] Doküman "Son doğrulama" başlık satırı yeni commit SHA + tarih ile güncellenir.

## Doğrulama

- `npm --prefix frontend test` (yalnız sayı için; kod değişmez)
- `git diff --stat` yalnız docs/ + CLAUDE.md

## Rapor

(işçi oturumu doldurur)
