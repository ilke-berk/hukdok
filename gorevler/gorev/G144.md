# G144 — Rapor ekranı: favori (şablon) önerisi — "bu formatı '…' adıyla ekleyeyim mi?"

- **Bant:** frontend
- **Bağımlı:** G143 (ReportsPage ortak)
- **Dosya kapsamı:** frontend/src/components/reports/FavoritePrompt.tsx (YENİ), frontend/src/lib/reports.ts
  (`sablonAdiOner` saf fonksiyon), frontend/src/pages/ReportsPage.tsx (tetik + durum), .../TemplateBar.tsx
  ("☆ Favorilere ekle" bağlantısı), ilgili `*.test.ts(x)`
- **Dokunma:** frontend/src/App.tsx, Sidebar.tsx, lib/api.ts (hub — DOKUNMA), ExportButtons.tsx (yalnız başarı
  geri çağrısı gerekiyorsa prop), backend/**

## Test değiştirme İZNİ (baştan)

`pages/ReportsPage.sablon.test.tsx`, `ReportsPage.asistan.test.tsx` (export sonrası akış), `ReportsPage.test.tsx`,
`components/reports/TemplateBar.test.tsx` (yeni bağlantı). **Kilit:** `lib/reports.export.test.ts` (export
gövdesi), `lib/reportsChat.test.ts`, QuickFilters/FilterControl/ColumnSheet/SourceCards/RunsTable testleri.
`expect(` sayısı net azalmaz.

## Hedef

Plan §6.2: kullanıcı bir format kurup indirdiğinde sistem sorar: "Bu formatı favorilere **'Müvekkiller ·
Doktor · Ankara'** adıyla ekleyeyim mi?" — ad düzenlenebilir, Ekle → şablon kaydedilir ve seçili olur; "Şimdi
değil" → o tanım için bir daha sorulmaz. Şablon çubuğunda kalıcı "☆ Favorilere ekle" bağlantısı indirme
yapmadan da aynı kartı açar. Backend değişmez (`POST /api/reports/templates`).

## Yapılacaklar

1. **`sablonAdiOner(tanim, katalog, mevcutAdlar)`** (`lib/reports.ts`, saf): "<Kaynak etiketi> · <en fazla iki
   filtre özeti>" — özet `kontrolOzeti`/etiketlerden (ör. "Doktor", "Ankara", "2025", "davası var"); arama
   filtresi özete girmez; filtre yoksa "<Kaynak> · Temel"; 60 karakter tavanı; aynı ad varsa " (2)", " (3)".
   Birim testleri.
2. **FavoritePrompt:** araç çubuğunun altında tek satır kart (yıldız simgesi + metin + ad girdisi + "Ekle" +
   "Şimdi değil" + ×); Enter = Ekle; kaydetme sırasında kilit; hata toast. Kaydedince `createTemplate({ad,
   aciklama:"", tanim, paylasimli:false})`, listeye eklenir, şablon çubuğunda seçili olur, toast "Favorilere
   eklendi".
3. **Tetik (`ReportsPage`):** başarılı export (manuel ExportButtons ve asistan `indir_*` yolu) sonrasında
   `sablonlar.some(s => ayniTanim(s.tanim, tanim))` yanlışsa ve tanım `reddedilenler` kümesinde değilse kart açılır.
   "Şimdi değil"/× → tanımın JSON anahtarı `reddedilenler`e (sayfa ömrü). Kart Rapor sekmesi dışında görünmez;
   tanım değişirse kart kapanır.
4. **TemplateBar "☆ Favorilere ekle":** mevcut tanım kayıtlı değilken görünür, tıklayınca aynı kart açılır
   (`reddedilenler`i geçersiz kılar); kayıtlıysa yerine "★ Kayıtlı: <ad>".
5. **Testler:** ad önerisi (kaynak + iki filtre, tavan, tekrar eki, arama hariç); export sonrası kart çıkar
   (kayıtsız tanım) / çıkmaz (kayıtlı tanım); Ekle → `POST /templates` gövdesi + seçili + toast; "Şimdi değil"
   → ikinci export'ta sorulmaz; tanım değişince kart kapanır; TemplateBar bağlantısı kartı açar; asistan
   `indir_xlsx` sonrası da kart.

## Kabul kriterleri

- [x] Kayıtsız tanımla export sonrası kart çıkar, önerilen ad "<Kaynak> · <filtreler>" biçiminde (test).
- [x] Ekle → şablon oluşur (`paylasimli:false`), çubukta seçili; "Şimdi değil" → aynı tanım için tekrar sorulmaz (test).
- [x] Kayıtlı tanımla export sonrası kart çıkmaz; TemplateBar "★ Kayıtlı" gösterir (test).
- [x] Hub dosyalara dokunulmadı; yeni npm paketi yok; `npm test`, `lint`, `tsc -b --force` temiz.

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

DURUM: TAMAM (2026-09-07, worktree `gorev/G144`, taban `251ff59`)

### Yapılanlar

- **`lib/reports.ts` — `sablonAdiOner(tanim, katalog, mevcutAdlar=[])`** (saf, `SABLON_AD_ONERI_MAX=60`):
  "<Kaynak etiketi> · <en fazla iki filtre özeti>", filtresiz "<Kaynak> · Temel". Özet `filtreAdOzeti` ile
  çip özetinden (`kontrolOzeti`) daha kısa: çoklu seçim değeri ("Doktor", §5.2 etiketiyle "Gerçek kişi"), metin
  yalnız aranan ("Ankara" — "içerir" yok), tarih aralığı aynı yılda yıl ("2025", iki yıl "2024–2025", tek uç
  "Kayıt Tarihi ≥ 01.03.2025"), var/yok ve boş anahtarı hızlı filtre etiketiyle küçük harf ("davası var",
  "e-postası yok"; etiket yoksa "<kolon> var/yok/boş"), sayı/para ve gelişmiş op kolon etiketiyle ("Maddi ≥ 1.000",
  "Meslek eşit değil Doktor"), mantık "Aktif" / "Aktif değil". Sanal arama kolonu (`secilebilir=false`) ve
  katalogda olmayan alan girmez; aynı özet tekrar etmez. Tavan: 60 karakter "…" ile; aynı ad (tr-TR
  küçük harf, trim) varsa " (2)", " (3)"… — ek dahil tavan korunur (taban ekin boyu kadar kısalır).
- **`components/reports/FavoritePrompt.tsx` (YENİ):** tek satır `form` (`data-testid="favori-onerisi"`): yıldız +
  "Bu formatı favorilere [ad girdisi, `aria-label="Favori adı"`] adıyla ekleyeyim mi?" + "Ekle" (primary,
  `type=submit`) + "Şimdi değil" (ghost) + × (`aria-label="Öneriyi kapat"`). Enter = Ekle (girdi `onKeyDown` +
  form `onSubmit`; jsdom örtük gönderimi yapmadığından ikisi de). Ad boş/120+ → Ekle kapalı; `kaydediliyor`
  girdi ve üç düğmeyi kilitler + spinner. Öneri değişince girdi yeni öneriyle başlar (`useEffect`).
- **`pages/ReportsPage.tsx` — tetik + durum:** `favoriOnerisi: RaporTanimi | null` (kartın açıldığı tanım),
  `reddedilenlerRef: Set<string>` (JSON anahtarı, sayfa ömrü), `sablonlarRef` (export cevabı beklerken liste
  değişmiş olabilir; karar anındaki listeyle bakılır). `kayitliSablon = sablonlar.find(ayniTanim(s.tanim, tanim))`
  (seçili olması şart değil — başkasının paylaşımlı şablonu da sayılır). Görünürlük
  `favoriOnerisi && !kayitliSablon && ayniTanim(favoriOnerisi, tanim)`; tanım değişince efekt kartı kapatır.
  `favoriOner(hedef, zorla)`: kayıtlı → açılmaz; `zorla` değilse reddedilen → açılmaz. Tetikler: (1) manuel
  `ExportButtons.onIndirildi` → `favoriOner(tanim)` (aynı render'ın tanımı = export edilen); (2) asistan
  `indir_*` başarılı export sonrası `favoriOner(tanimOlustur(uygulanan))` — şeride çözülüp geri derlenen hal,
  bir sonraki render'daki `tanim` memo'suna eşit (413/hata → tetik yok). "Şimdi değil"/× → `favoriReddet`
  (anahtar kümeye + kapat). "Ekle" → `createTemplate({ad, aciklama:"", tanim, paylasimli:false})` → listeye
  başa eklenir, `seciliSablonId`, toast "Favorilere eklendi" (+ad); hata → `RAPOR_SABLON_KAYIT_HATASI` toast,
  kart açık kalır. Kilit `sablonIsleniyor` (çubukla ortak). Kart araç çubuğunun altında, önizlemenin üstünde,
  Rapor sekmesi ağacında (Radix `TabsContent` diğer sekmede render etmez).
- **`TemplateBar.tsx`:** isteğe bağlı `kayitli?: RaporSablonu | null` + `onFavoriEkle?: () => void`. Kayıtlıysa
  seçimin yanında "★ Kayıtlı: <ad>" (`data-testid="favori-kayitli"`, title ile tam ad); değilse ve `onFavoriEkle`
  verilmişse "☆ Favorilere ekle" bağlantısı (`data-testid="favori-ekle-baglantisi"`, `LINK_BTN_CLS`; taslak
  geçersiz/işleniyorken pasif). Sayfa `onFavoriEkle={() => favoriOner(tanim, true)}` verir — ret geçersiz kılınır,
  indirme gerekmez. Eski çağıran (prop yok) → ikisi de yok (geriye uyumlu).
- ExportButtons'a DOKUNULMADI (mevcut `onIndirildi` geri çağrısı yetti). Backend/hub dosyaları değişmedi, yeni
  npm paketi yok.

### Kararlar

- **Öneri kartı ayrı bileşen, diyalog değil:** plan §6.2 "tek satır, kapatılabilir, konuşma alanı/toast ile
  çakışmaz" — `SaveTemplateDialog` (modal, açıklama + paylaşım) yerine satır-içi form; `paylasimli:false` ve
  `aciklama:""` sabit. Paylaşım/açıklama isteyen kullanıcı yine "Kaydet" diyaloğunu kullanır.
- **× = "Şimdi değil":** görev metni "'Şimdi değil'/× → reddedilenler" — kapatma da ret sayılır; aksi halde
  her export'ta kart geri gelir ve rahatsız eder. "☆ Favorilere ekle" reddi geçersiz kılar (kalıcı yol).
- **Kayıtlı kontrolü tüm listede** (seçili şablonla sınırlı değil; `exportSablonId` seçiliyle sınırlı KALDI —
  koşuya şablon kimliği yazmak ayrı bir anlam taşır, değiştirilmedi).
- **Ad özeti çip özetinden ayrı fonksiyon** (`filtreAdOzeti`, dosya-içi): çip "içerir \"Ankara\"" /
  "01.01.2025 – 31.12.2025" yazar, ad için "Ankara" / "2025" istenmişti; `kontrolOzeti` sözleşmesi değişmedi.
- **Asistan yolunda `sablonlarRef`:** `asistanTanimiUygula` closure'ı akış başladığı render'a ait olabilir;
  export cevabı gelene dek şablon listesi değişmişse (ör. aynı anda Ekle) bayat listeyle karar verilmesin.
- **Tanım değişince kart kapanır, aynı tanıma dönülse de kendiliğinden açılmaz** (yeni export ya da bağlantı
  gerekir) — beklenmedik kart belirmesi olmasın. Sekme değişimi tanımı değiştirmez → dönünce kart yerinde.

### Testler (yeni dosyalar, mevcut testlere dokunulmadı)

- `lib/reports.favori.test.ts` (10): Temel/etiket/bilinmeyen kaynak; Doktor · Ankara (içerir yok, eq de metin);
  en fazla iki (üçüncü düşer); arama + katalog dışı alan girmez; tarih yıl / iki yıl / tek uç; var/yok + boş
  anahtarı küçük harf; §5.2 etiket, sayı, mantık, gelişmiş op; 60 tavanı "…" + ek dahil tavan; " (2)"/" (3)" +
  büyük/küçük harf ve boşluk duyarsız; saflık.
- `pages/ReportsPage.favori.test.tsx` (12): kayıtsız tanımla Excel sonrası kart (metin, ad "Davalar · Temel",
  konum çubuk–tablo arası, POST yok, tek toast); Ekle → gövde `{ad, aciklama:"", paylasimli:false, tanim}`
  birebir + seçili "99" + kart kapanır + toast + "★ Kayıtlı"; ad düzenleme + Enter + boş ad kilidi; "Şimdi
  değil" → ikinci export'ta sorulmaz + bağlantı yine açar (export'suz); × ret; kayıtlı tanım (başkasının paylaşımlı
  + kendi yüklenen) → kart yok + rozet + bağlantı yok; kaynak değişince kart kapanır, yeni tanımda ad
  "Müvekkiller · Temel"; Şablonlar sekmesinde yok, dönünce var; " (2)" eki; 422 → toast + kart açık + liste aynı;
  asistan `indir_xlsx` → kart "Davalar · Derdest" + Ekle asistan tanımını kaydeder; asistan 413 → kart yok.
- `components/reports/TemplateBar.test.tsx` (4): bağlantı + tık; rozet; pasif halleri; prop'suz eski çağıran.

Koşular (HOST, worktree): `npm test` → **70 dosya / 873 passed** (öncesi 847 + 26 yeni), `npm run lint` temiz,
`npx tsc -b --force` exit 0. docker compose kullanılmadı (bant kuralı). Tur sayısı: 1 (ilk koşuda yeşil).

### İzlenecekler

- NOT: `sablonAdiOner` çoklu seçimde tüm değerleri virgülle yazar ("Derdest, Karar, …"); uzun listede 60 tavanı
  "…" ile keser. İlk 2-3 değer + "+N" biçimi istenirse `filtreAdOzeti` `coklu_secim` dalı yeter.
- NOT: Kart `sablonIsleniyor` kilidini çubukla paylaşır — Ekle sürerken Yükle/Kaydet de pasif (bilinçli, tek
  yazma anı). Ayrı kilit istenirse `favoriKaydediliyor` state'i eklenir.
- Gerçek kullanımda ölçülecek: kartın ne sıklıkla "Şimdi değil" aldığı — yüksekse tetik yalnız ikinci
  export'a alınabilir (sayfa ömrü sayaç). Şimdilik plan §6.2 birebir.
