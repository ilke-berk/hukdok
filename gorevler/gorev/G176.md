# G176 — Rapor asistanı prompt'u: teyit döngüsü → otomatik uygulama; yaklaşık değerde `contains`; liste sorusunda ekrana yönlendirme

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/prompts.py (`get_rapor_asistani_instruction` — yalnız kural cümleleri), backend/tests/test_g132_rapor_asistani.py
  (prompt metni doğrulayan testler — **beklenti değiştirme izni VAR**), backend/tests/test_g166_rapor_bagli_kaynaklar.py (prompt
  cümlesine bakan test varsa aynı izin)
- **Dokunma:** backend/services/rapor/asistan.py (akış/olaylar DEĞİŞMEZ), backend/schemas_rapor.py (`RaporAsistanCevabi`,
  `SohbetIstegi` DEĞİŞMEZ), frontend (G174 tarafı — burada sıfır), K6/K7 (asistan veri görmez, indirme frontend'den) KORUNUR

## Hedef

G174 tanımı düğme beklemeden uyguluyor ve düzenlenebilir şerit (G173) her an ekranda. Prompt'un "TEYİT DÖNGÜSÜ" kuralı
("onay sorusuyla bitir, hemen uygulanmaz") artık ekranla çelişir. Üç cümle değişir, gerisi aynen:

1. **TEYİT DÖNGÜSÜ → UYGULAMA:** "Hazırladığın tanım ekranda hemen uygulanır ve düzenlenebilir bir şeritte görünür; cevabında
   ne yaptığını 1-2 cümleyle söyle, onay SORMA. Belirsizlikte (hangi kolon/hangi tarih) tanim=null ve tek soru." Düzeltme
   isteğinde ("telefonu da ekle") mevcut tanımı o kadar değiştir, gerisine dokunma — bu kural kalır. "Kullanıcı 'tamam/evet'
   derse aynen döndür" cümlesi kalır (G167 yerel onay yolu `onayNiyeti` bekleyen tanım varken hâlâ kullanılıyor).
2. **Yaklaşık değer → `contains`:** "Kullanıcı bir mahkeme, il, kurum ya da taraf adını yaklaşık/kısaltarak yazarsa (`Ankara
   3. Ticaret`), `eq` DEĞİL `contains` kullan ve en ayırt edici parçayı yaz; tam liste değerini bilmiyorsan uydurma." Kapalı
   liste (`secenekler`) kolonlarında mevcut "aynen kopyala" kuralı aynen.
3. **Liste sorusu:** "Kullanıcı 'hangi mahkemeler var', 'il seçenekleri neler' gibi liste isterse listeyi SEN VEREMEZSİN (veriyi
   görmüyorsun): tanim=null, cevapta ilgili filtre çipine tıklayarak ya da 'hangi <alan>lar var' yazarak listeyi ekranda
   görebileceğini söyle." (Frontend `listeNiyeti` çoğunu yakalar; bu, kaçanlar için güvenlik ağıdır.)

## Kabul kriterleri

- [ ] `get_rapor_asistani_instruction(...)` çıktısında "TEYİT DÖNGÜSÜ" başlığı yok; "hemen uygulanır" + "onay sorma" +
      "belirsizlikte tanim=null" cümleleri var; "Mevcut tanım verilmişse SIFIRDAN ÜRETME" kuralı aynen.
- [ ] `contains` yaklaşık değer cümlesi ve liste sorusu cümlesi var; `mevcut_tanim_json` yerleşimi (prompt sonu, katalogdan
      sonra) değişmedi.
- [ ] `test_g132_rapor_asistani.py`: prompt metnini doğrulayan testler yeni cümlelere göre güncellenir; akış/olay/409/403/422
      testleri DOKUNULMADAN geçer; G166 bağlı kaynak prompt cümlesi testi aynen geçer.
- [ ] `docker compose exec -T backend python -m pytest tests/test_g132_rapor_asistani.py tests/test_g166_rapor_bagli_kaynaklar.py`
      yeşil; tam paket yeşil; ruff/mypy temiz.
- [ ] Gemini duman testi bu görevde YAPILMAZ (gece, anahtar gerektirir) — raporda "insan adımı: 3 istem" notu (ör. "2025'te açılan
      Ankara ticaret davaları", "hangi mahkemeler var", "telefonu da ekle").

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-11, gece işçisi)

**Yapılanlar**

- `backend/prompts.py::get_rapor_asistani_instruction` — yalnız kural cümleleri değişti (`sections` listesi):
  - "TEYİT DÖNGÜSÜ" maddesi → "UYGULAMA" maddesi: "tanım ekranda hemen uygulanır ve düzenlenebilir bir şeritte görünür;
    cevabında ne yaptığını 1-2 cümleyle söyle, onay SORMA ('Doğru mu, uygulayayım mı?' gibi soru YOK). Belirsizlikte
    (hangi kolon, hangi tarih alanı) tanim=null ve TEK soru." Kalanlar aynen: "tamam/evet/doğru/uygula/onayla/göster →
    tanımı AYNEN döndür + onizle", "indir/Excel/CSV → aynen + indirme eylemi" (G167 `onayNiyeti` yolu), düzeltme
    isteğinde "mevcut tanımı o kadar değiştir, gerisine dokunma" (sondaki "ve yine onay iste" kalktı).
  - Yeni "YAKLAŞIK AD" maddesi: mahkeme/il/kurum/taraf adı yaklaşık/kısaltılmış yazılırsa metin kolonunda `eq` DEĞİL
    `contains` + en ayırt edici parça; tam liste değerini bilmiyorsa uydurma; kapalı liste (seçenekli) kolonlarda
    mevcut "aynen kopyala" kuralı geçerli (o madde dokunulmadı).
  - Yeni "LİSTE SORUSU" maddesi: "hangi mahkemeler var / il seçenekleri neler" → asistan listeyi VEREMEZ (veri
    görmez, K6): tanim=null + eylem=null, cevapta ilgili filtre çipine tıklayarak ya da "hangi <alan>lar var" yazarak
    ekranda görebileceğini söyle (frontend `listeNiyeti` kaçırırsa güvenlik ağı).
  - "Mevcut tanım verilmişse SIFIRDAN ÜRETME" maddesi, TİP→OPERATÖR tablosu, KATALOG ve `mevcut_tanim_json`
    yerleşimi (prompt sonu, katalogdan sonra) DEĞİŞMEDİ. Docstring aynen (kuralları özetleyen cümle hâlâ doğru).
- `backend/tests/test_g132_rapor_asistani.py` — mevcut testlere dokunulmadı (hiçbiri "TEYİT DÖNGÜSÜ" metnini
  doğrulamıyordu; beklenti değiştirme izni kullanılmadı). İki yeni test eklendi, ikisi de eski prompt'ta KIRMIZI
  (stash ile doğrulandı):
  - `test_prompt_g176_uygulama_kurali_teyit_dongusu_yok`: "TEYİT DÖNGÜSÜ"/"hemen uygulanmaz"/"onay bekler"/"yine onay
    iste" YOK; "hemen uygulanır", "düzenlenebilir bir şeritte", "onay SORMA", belirsizlik cümlesi, sözlü onay cümlesi,
    "SIFIRDAN ÜRETME" ve düzeltme cümlesi VAR.
  - `test_prompt_g176_yaklasik_ad_contains_ve_liste_sorusu`: `contains` ve liste sorusu cümleleri; "aynen kopyala"
    kuralı korunmuş; yerleşim (kurallar < KATALOG < MEVCUT TANIM, prompt `mevcut_tanim_json` ile biter; verilmezse
    bölüm yok).
- `test_g166_rapor_bagli_kaynaklar.py`: prompt cümlesine bakan test ("BAĞLI KAYNAK KOLONLARI", `muvekkil.phone`)
  değişmeden geçiyor — dosyaya dokunulmadı.

**Kararlar**

- Başlık "UYGULAMA" seçildi (görev metnindeki "TEYİT DÖNGÜSÜ → UYGULAMA"); model için kısa ve ekrandaki davranışı
  adlandırıyor.
- `contains` kuralı "metin kolonunda" diye sınırlandı: `liste` tipinde `contains` izinli değil (TİP→OPERATÖR tablosu),
  kapalı listede "aynen kopyala" kuralına açık gönderme var; böylece iki kural çelişmiyor.
- Liste sorusu cevabında `eylem=null` da açıkça yazıldı: sunucu tanim=null iken eylemi zaten düşürür
  (`test_tanim_null_ile_gelen_eylem_sunucuda_dusurulur`), prompt bunu baştan istiyor.
- `docs/mimari/raporlama.md` (satır 43 ve 411) hâlâ "G167 TEYİT DÖNGÜSÜ ... UYGULANMAZ" anlatıyor; dosya bu görevin
  kapsamında değil, DOKUNULMADI. NOT: G174 (düğme beklemeden uygulama) ile birlikte bu iki satır güncellenmeli —
  kapsamda olan görev ya da ayrı bir docs kalemi.

**Test sonuçları (konteyner)**

- `pytest tests/test_g132_rapor_asistani.py tests/test_g166_rapor_bagli_kaynaklar.py` → 65 passed
- `pytest` (tam paket) → 3292 passed, 3 skipped
- `ruff check .` → All checks passed; `mypy` → Success: no issues found in 62 source files

**İzlenecekler — insan adımı (Gemini duman testi, anahtar gerekir, gece yapılmadı)**

Rapor asistanı açıkken 3 istem:
1. "2025'te açılan Ankara ticaret davaları" → mahkeme filtresi `contains` "Ankara" + "Ticaret" (tek `eq` değil),
   tanım şeritte hemen uygulanır, cevap onay sorusu içermez.
2. "hangi mahkemeler var" → tanim=null, cevap çipe/`hangi mahkemeler var` yazımına yönlendirir, liste uydurmaz
   (frontend `listeNiyeti` bunu zaten yakalıyorsa "hangi mahkeme adları mevcut" gibi bir varyantla dene).
3. Tanım uygulandıktan sonra "telefonu da ekle" → mevcut tanım korunup yalnız `muvekkil.phone` eklenir, onay istenmez.
