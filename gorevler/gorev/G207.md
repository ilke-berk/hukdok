# G207 — Doküman + infra: karar 021, hukbot sitesi host nginx'ten kalkar

- **Bant:** docs
- **Bağımlı:** G202 (CLAUDE.md ortak), G203, G206
- **Dosya kapsamı:** yeni `docs/kararlar/021-hukukbot-hukudok-girisi.md`, `docs/kararlar/README.md` (dizin satırı), `CLAUDE.md`, `docs/mimari/genel-bakis.md`, `docs/mimari/kimlik-ve-token.md`, `docs/hukukbot-aktarim/**` (yalnız erişim/mimari cümleleri), `infra/nginx/sites-available/hukbot` (SİLİNİR), `infra/install.sh`, `infra/README.md`
- **Dokunma:** kod dosyaları, `nginx.conf`, `docker-compose.yml`

## Hedef
Kararı ve yeni mimariyi yaşayan dokümanlara işler; hukbot.tragic.tr'nin sunucu konfig kopyasını repodan kaldırır
(VM yeniden kurulumunda geri gelmesin). Her iddia G203-G206 koduna karşı OKUNARAK yazılır (ALTIN KURAL).

## Kabul kriterleri
- [ ] Karar 021: bağlam (Hanyaloğlu hesabı hukbot'a giremedi — hukbot Azure kaydı tek kiracılı), karar (tek giriş
      HukuDok; hukbot HukuDok token'ını `aud=api://<HukuDok client>`, `scp=access_as_user`, `ALLOWED_TENANTS`, v1/v2 iss ile
      doğrular; API yalnız `/hukukbot-api/` allowlist'i; sohbetler oid anahtarlı → LexisBio geçmişi korunur),
      reddedilenler (misafir kullanıcı daveti; hukbot'u çok kiracılı ayrı site olarak bırakmak; HukuDok backend'i
      üzerinden proxy — uzun LLM akışı 2 worker'ı meşgul eder + güvenilir iç başlık riski), açık konular.
- [ ] CLAUDE.md "Mimari özet": iki katmanlı nginx paragrafına `/hukukbot-api` + gecikmeli DNS notu; hukbot erişim cümlesi.
- [ ] `genel-bakis.md` port haritası: hukbot 3000 (frontend) kalkar; 8010 yalnız `hukuk_shared` + loopback; hukbot.tragic.tr yok.
- [ ] `kimlik-ve-token.md`: aynı access token'ın iki backend'de doğrulandığı + scope/aud notu.
- [ ] `infra/nginx/sites-available/hukbot` silinir; `install.sh` 74/76. satırları ve `infra/README.md` tablosu/sertifika cümlesi güncellenir.
      Sunucudaki `/etc/nginx/sites-enabled/hukbot` + certbot sertifikası İNSAN ADIMI olarak README'ye yazılır (otomasyon ssh yapmaz).
- [ ] `grep -rn "hukbot.tragic.tr" --include=*.md --include=*.sh --include=*.conf . | grep -v docs/arsiv` yalnız karar 021'in tarihsel cümlesini döndürür.

## Doğrulama
- Test yok (docs bandı). `bash -n infra/install.sh`.

## Rapor
(işçi oturumu doldurur)
