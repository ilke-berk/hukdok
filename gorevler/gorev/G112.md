# G112 — Düzeltme_Logu provenance + açık boşaltma yolu + DEGER_HAVUZLARI fark raporu

- **Bant:** backend
- **Bağımlı:** G110
- **Dosya kapsamı:** backend/scripts/hukdok_aktarim.py, backend/services/teslim_kutusu.py
  (fark raporu çağrısı), backend/services/teslim_cevap.py (fark raporu dosyası),
  backend/tests/test_g112_duzeltme_logu.py (YENİ), mevcut aktarım testleri (yalnız ekleme)
- **Dokunma:** backend/managers/reference_lists.py (yalnız OKU — listeye değer EKLEME),
  backend/managers/seed_data.py (dokunma), backend/models.py (şema değişikliği YOK)

## Hedef

Plan §4. Teslim paketinin `Düzeltme_Logu` sayfası (20.042 satır: Excel Satırı, SistemNo,
DosyaNo, Eski Değer, Yeni Değer, Gerekçe, Tarih) veri ekibinin **gerekçesini** taşıyor; bugün
okunmuyor ve `case_history`'ye yalnız "eski→yeni" düşüyor. Ayrıca script alan boşaltmayı
bilerek yapmıyor ("None = bu teslimde yok"); `Düzeltme_Logu`'nda Yeni Değer `(boş)` olan satır
**açık boşaltma talimatıdır** ve bu görev o yolu açar. Üçüncü parça: `DEGER_HAVUZLARI`
sayfasındaki kapalı liste değerlerinin bizim referans listelerimizle farkı raporlanır.

## Pazarlıksız kurallar

- **Sütun eşlemesi tek kaynak:** `Düzeltme_Logu`'ndaki sütun ADI (ör. "İstinaf Karar Durumu")
  → kart alanı çevirisi `KART_ALANLARI`'nın ters haritasıdır; yeni sözlük İCAT ETME.
  Bilinmeyen sütun adı → satır yok sayılır (DEBUG), rapora düşmez (sayfa 20k satır, gürültü).
- **Provenance:** bir kart alanı bu teslimde değişiyorsa ve `Düzeltme_Logu`'nda aynı
  (SistemNo, alan) için satır varsa `case_history` kaydının açıklamasına `gerekçe` eklenir
  (mevcut `source` imzası KORUNUR; `source` kolonu G-kuralı gereği `HUKDOK_TESLIM_*` ile
  başlamaya devam eder — `required_fields.AKTARIM_SOURCE_PREFIX` kovası bozulmaz). Aynı
  (SistemNo, alan) için birden çok log satırı → en yeni `Tarih`.
- **Açık boşaltma:** Yeni Değer `(boş)` VE `Sheet`'te o hücre gerçekten boş VE bizde dolu →
  alan NULL yazılır, `case_history`'ye "boşaltıldı: <gerekçe>" düşer. Bu ÜÇLÜ şart olmadan
  boşaltma YOK (partili teslim güvencesi korunur). Boşaltma `ICERIK_KARSILASTIRMALI_ALANLAR`
  ve karar künyesi (`stage_decisions` tek yazıcı) için de GEÇERSİZ — o alanlar rapora düşer.
- **İdempotency:** aynı girdiyle ikinci koşu 0 değişiklik (boşaltılan alan zaten NULL).
- **DEGER_HAVUZLARI:** "Havuz / Sütun" → referans listesi eşlemesi sabit küçük sözlük
  (`İddia Edilen Kusur` → `alleged_faults`, `İstinaf Karar Durumu` → `appeal_decisions`,
  `Yargıtay Onama Durumu` → `cassation_decisions`, `Yerel Mahkeme Karar Durumu` →
  `local_decisions`, `Olay Türü` → `event_types`, `Hükümdeki Rol` → `judgment_roles`;
  eşlemesi olmayan havuz atlanır). Fark = teslimde var/bizde yok + bizde var/teslimde yok.
  Yalnız RAPOR (`deger-havuzu-farki_<teslim>.csv`, cevap paketine girer) + fark varsa admin
  bildirimi (`type="veri_teslim"`, dedupe `teslim:{id}:havuz:{email}`). **Listeye yazma YOK**
  (tahmin yasağı; alleged_faults seed'lenmez kararı korunur).

## Kabul kriterleri

- [x] `Düzeltme_Logu` okuyucu: sayfa yoksa boş (hata değil); bilinmeyen sütun adı yok sayılıyor;
      aynı (SistemNo, alan) için en yeni tarih kazanıyor (birim testleri).
- [x] Provenance: sentetik pakette değişen alanın `case_history` açıklamasında gerekçe metni
      var; `source` `HUKDOK_TESLIM_` ile başlıyor (kova testi); log satırı olmayan değişiklikte
      açıklama eski biçimde.
- [x] Boşaltma: üçlü şart sağlanınca NULL + tarihçe; `Sheet` hücresi doluysa boşaltma YOK;
      log yoksa boşaltma YOK; künye alanı için boşaltma YOK + rapor (dört test).
- [x] İkinci koşu 0 değişiklik (boşaltma dahil).
- [x] DEGER_HAVUZLARI: fark CSV'si iki yönü de listeliyor; fark yokken dosya üretilmiyor ve
      bildirim yok; referans tablolarında satır sayısı koşu öncesi/sonrası AYNI (test).
- [x] Mevcut G064/G104 testleri değişiklik gerektirmeden yeşil; docstring "Bilinçli
      YAZILMAYANLAR / boşaltma" cümleleri güncellendi.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-03, gece kuyruğu işçi oturumu)

### Yapılanlar

- `backend/scripts/hukdok_aktarim.py` — yeni "Düzeltme_Logu (G112)" bölümü:
  - `DUZELTME_ALAN_HARITASI`: sütun adı → kart alanı çevirisi `KART_ALANLARI` +
    `SUTUN_ADAYLARI`'ndan TÜRETİLİR (yeni sözlük yok); künye kaynakları (`karar_no`,
    `karar_tarihi`, `istinaf_basvuran` → `istinaf_basvuran_taraf`) yalnız RAPORLANABİLSİN
    diye haritada, `BOSALTMA_DISI_ALANLAR` = künye ∪ `ICERIK_KARSILASTIRMALI_ALANLAR`.
  - `duzeltme_logunu_oku(yol)` → `{(SistemNo, alan): DuzeltmeKaydi}`: sayfa yoksa `{}` (INFO);
    zorunlu başlık (SistemNo / sütun adı / Yeni Değer) yoksa TEK WARNING + `{}` (koşu durmaz;
    boşaltma talimatı okunamadığı için uygulanmaz — güvenli taraf); bilinmeyen sütun adı DEBUG;
    aynı anahtar için en yeni `Tarih` kazanır (tarihsiz satır en eski, eşitlikte sonraki satır).
  - `_bosalt_isareti_mi`: `(boş)` parantezli, büyük/küçük + iç boşluk toleranslı; çıplak
    "boş" bir METİN değeridir (`_baslik_anahtari` noktalamayı attığı için burada kullanılmadı).
  - `_bosaltma_talimatlari(satir, sistem_no, duzeltmeler)` → (uygulanabilir, reddedilen):
    üçlü şartın ilk iki ayağı burada — log `(boş)` VE `Sheet`'te sütun VAR ve hücre gerçekten
    boş; sütun paketten büsbütün eksikse "hücre boş" DEĞİLDİR (partili teslim güvencesi).
    Üçüncü ayak (bizde dolu) `_kart_alanlarini_yaz`'da karta bakılarak ölçülür; bizde zaten
    boşsa hiçbir şey yazılmaz (ikinci koşu 0 değişiklik).
  - `_provenance_imzasi`: değişen alanın `case_history.source`u `"<HUKDOK_TESLIM_imza> ·
    gerekçe: <Gerekçe>"` (kolon sınırı modelden, 300); boşaltmada `"… · boşaltıldı: <gerekçe>"`.
    Log satırı yoksa imza eski biçimde; `(boş)` satırının gerekçesi bir DEĞER yazımına
    iliştirilmez (çelişkili talimat). `required_fields.is_aktarim_source` kovası bozulmaz.
  - `kart_alan_celiskileri` boşaltmayı bir DEĞER (`BOSALTMA_ISARETI`) olarak uzlaşı kümesine
    sokar: kardeş föy değer taşırken diğeri `(boş)` derse alan çelişkili → yazılmaz, rapora
    düşer (aksi hâlde kart koşudan koşuya salınırdı — G064 kabul kriteri).
  - `esas_no` boşaltması tek yoldan (`sync_current_esas(None)`: kolon NULL, tarihçe satırı
    kalır, güncel işaret düşer). Künye / içerik-modu talimatı `atlanan_alanlar`a
    `"<alan> yazılmadı: boşaltılmadı — …"` olarak düşer (HATA, koşu NONZERO — G104 sınıfı).
  - `AktarimSonucu.bosaltilan` sayacı + `ozet_metni` "boşaltılan alan" satırı; modül
    docstring'inin "None = bu teslimde yok" ve "Karar künyesi" paragrafları güncellendi.
- `backend/services/teslim_cevap.py` — `DEGER_HAVUZLARI` bölümü: `HAVUZ_LISTE_ESLEMESI`
  (altı havuz → `LIST_REGISTRY` anahtarı, yalnız OKUMA), `havuz_degerlerini_oku` (uzun biçim
  "Havuz / Sütun"+"Değer"; yoksa geniş biçim — eşlemesi olan başlık = havuz; `;`/satır
  sonu/`|` ile birleşik hücre bölünür, `/` ayraç değil), `havuz_farki` (iki yön, `_anahtar`
  ile yazım toleranslı, `active` filtresi yok), `teslim_havuz_farki` / `havuz_farki_csv_yaz`
  / `havuz_farki_csv_uret` (fark yoksa dosya YOK, bayat kopya silinir), `havuz_farki_ozeti`.
- `backend/services/teslim_kutusu.py` — `_havuz_farki_dene` kuru koşu ve uygulama başarı
  yolunda defter commit'inden SONRA: `deger-havuzu-farki_<teslim>.csv` rapor dizinine (cevap
  paketi `_cevap_dosyalari` ile otomatik alır); fark varsa `havuz_bildir` → ADMIN_EMAILS,
  `type="veri_teslim"`, dedupe `teslim:<id>:havuz:<alıcı>` (kuru koşu + uygulama ikilemez).
  `BILDIRIM_OLAYLARI` DEĞİŞMEDİ (G108 kilidi) — havuz bildirimi ayrı fonksiyon. Her istisna
  WARNING, durum makinesi bozulmaz; okuma transaction'ı `commit` ile kapatılır (sqlite
  StaticPool'da sonraki BEGIN patlıyordu — ilk koşuda öğrenildi).
- `backend/tests/test_g112_duzeltme_logu.py` (YENİ, 29 test): ters harita kilidi, okuyucu
  (sayfa yok / bilinmeyen sütun / en yeni tarih / zorunlu başlık yok / `(boş)` toleransı),
  provenance (+ kırpma), boşaltma dörtlüsü (üçlü şart, Sheet dolu, log yok, sütun eksik,
  bizde boş, künye+içerik rapor), kardeş föy çelişkisi, `esas_no` tek yol, kapsam kilidi,
  havuz okuyucu (uzun/geniş/yok), fark iki yön + liste sayısı sabit, teslim uçtan uca (CSV
  içeriği bayt düzeyinde, bildirim dedupe, uygulama ikilemez), fark yokken dosya/bildirim yok
  + bayat kopya temizliği, fark istisnası kuru koşuyu düşürmez.

### Kararlar

- **Sütun adı başlığı**: plan §1 `Düzeltme_Logu`'nu 7 sütunla listeliyor (Excel Satırı,
  SistemNo, DosyaNo, Eski, Yeni, Gerekçe, Tarih) — değişen SÜTUNUN ADI için ayrı bir başlık
  yazılı değil; görev sözleşmesi ise sütun adını esas alıyor. Aday başlıklar toleranslı
  (`Sütun`, `Sütun Adı`, `Alan`, `Alan Adı`, `Kolon`, `Değişen Sütun/Alan`); başlık yoksa
  sayfa TEK WARNING ile okunmaz (uydurma eşleme yok). Gerçek başlık G114 sözleşmesine yazılmalı.
- Görevin örneği "İstinaf Karar Durumu" `KART_ALANLARI`'nda DEĞİL (tek yazıcı
  stage_decisions) → ters harita gereği bilinmeyen sütun, yok sayılır. Bilinçli: sözleşme
  "yeni sözlük icat etme" diyor.
- Künye/içerik boşaltma talimatı `AlanHatasi` sınıfıyla HATA olarak raporlanır (koşu
  NONZERO): uygulanamayan açık talimat, tanınmayan kapalı liste değeriyle aynı sınıftır —
  insanın düzeltme listesi konusu, sessiz yutulmaz.
- DEGER_HAVUZLARI sayfa düzeni de yazılı sabit değil: uzun biçim ("Havuz / Sütun" görev
  metninden) önce, geniş biçim yedek. İkisi de havuz vermezse INFO + atlanır.

### Test sonuçları (konteyner)

- `pytest`: **2298 passed, 3 skipped** (önce 2269 + 29 yeni); ruff: All checks passed;
  mypy: Success (53 dosya). Mevcut G064/G104/G107/G108/G110 testleri değişmeden yeşil.
- Kırmızı-yeşil: ilk turda 3 kırmızı (çıplak "boş" `_baslik_anahtari` ile `(boş)`a eşit
  çıkıyordu → parantezli karşılaştırma; sqlite StaticPool açık okuma transaction'ı →
  `_havuz_farki_dene` commit ile kapatır; test kendi oturumunu kapatmadan ikinci oturum
  açıyordu → test düzeni), ikinci tur yeşil.

### İzlenecekler

- G114 sözleşmesine: `Düzeltme_Logu` sütun-adı başlığının ve `DEGER_HAVUZLARI` düzeninin
  resmî adı; `(boş)` işaretinin yazımı. İlk gerçek paketle `duzeltme_logunu_oku` INFO satırı
  ("N kayıt okundu, M bilinmeyen sütun") kontrol edilmeli — bilinmeyen sayısı yüksekse başlık
  eşlemesi tutmamış demektir.
- Boşaltma yalnız `KART_ALANLARI` üyeleri için; `case_parties`/`case_lawyers` (yalnız ekleme
  yolu) ve aşama satırları kapsam dışı kaldı — talimat gelirse ayrı görev.
- NOT: çalışma ağacında kapsam dışı kirli dosyalar vardı (`.claude/settings.local.json`,
  `.claude/launch.json` — oturum yapılandırması); dokunulmadı, commit'e girmedi.

### Onarım (denetim RET sonrası, 2026-09-03)

Denetimin üç bulgusu gerçek paketle (masaüstü `HUKDOK_TESLIM_PAKETI_2026-08-18.xlsx`,
konteynerde `/tmp`'ye kopyalanıp koşularak) yeniden üretildi; üçü de DOĞRU çıktı. Yukarıdaki
"Kararlar"ın "sayfa düzeni yazılı sabit değil" cümleleri bu yüzden geçersiz — düzen dosyadan okundu:

1. **[kritik] `DEGER_HAVUZLARI` başlığı 3. satırda** (1. satır "RESMİ DEĞER HAVUZLARI", 2. boş).
   `havuz_degerlerini_oku` artık başlığı ilk `HAVUZ_BASLIK_TARAMA` (10) satırda arar
   (`_havuz_basligi_bul`: önce uzun, sonra geniş biçim). Gerçek pakette 4 havuz okunuyor:
   İddia Edilen Kusur 7 · Yerel Mahkeme Karar Durumu 28 · Temyiz Onama Durumu 3 · İstinaf
   Karar Durumu 3.
2. **[yüksek] Havuz adı ekleri + "Temyiz Onama Durumu".** Eşleme anahtarı `_havuz_anahtari`
   parantezli eki atar ("İddia Edilen Kusur (kapalı, 7)" → İDDİA EDİLEN KUSUR; etiket
   temizliği, değer tahmini değil; sözlük anahtarı ve CSV `havuz` sütunu sayfadaki yazımı
   korur). `HAVUZ_LISTE_ESLEMESI`'ne "Temyiz Onama Durumu" → `cassation_decisions` takma adı
   eklendi (sözleşmedeki "Yargıtay Onama Durumu" da tanınır; altı liste değişmedi, kilit test
   yeşil). Gerçek pakette fark: **7 satır, hepsi `alleged_faults` "teslimde var / bizde yok"**
   (seed'lenmemiş kusur değerleri — görevin asıl senaryosu); diğer üç havuz 0 fark.
3. **[yüksek] `Düzeltme_Logu` sütun-adı başlığı yok.** Gerçek sayfada sütun adı Gerekçe'nin
   köşeli parantezli ÖNEKİNDE geliyor ("[Hükmedilen Manevi] Outlook otomasyonu …" — 20.042
   satırın 10.662'si). `duzeltme_logunu_oku` sütun başlığı yoksa adı bu önekten okur (çeviri
   yine `KART_ALANLARI` ters haritası — yeni sözlük yok; başlık varsa o öncelikli), TEK WARNING
   ile öneksiz satırların uygulanamayacağını söyler; hem başlık hem Gerekçe yoksa eski davranış
   (WARNING + boş). Gerçek pakette: **1.453 (SistemNo, alan) provenance kaydı** (hukmedilen_manevi
   436 · hukmedilen_maddi 356 · hukmedilen_toplam 311 · dosya_son_durumu 100 · karar_no 81 ·
   sub_type 75 …), 9.208 satır bilinmeyen sütun (İstinaf Karar Durumu, Ek Alt Kırılım, mahkeme
   adları — `KART_ALANLARI` dışı, sözleşme gereği yok sayılır), 9.380 satır öneksiz.
   **Giderilmeyen kısım (kullanıcı kararı):** 229 `(boş)` satırının HİÇBİRİ önek taşımıyor
   (218'i "Mhzn → (boş), Format: geçersiz değer silindi") → boşaltılacak sütun bilinmiyor;
   tahmin yasağı gereği boşaltma yolu gerçek pakette hâlâ uygulanmıyor (kod yolu sentetik
   testlerle kilitli, WARNING bunu her koşuda söyler). Çözüm karşı tarafta: G114 sözleşmesine
   `Düzeltme_Logu` için sütun-adı başlığı (ya da her satırda `[Sütun]` öneki) yazılmalı.

Testler: +5 (gerçek başlık düzeninde havuz okuyucu, parantezli ek + temyiz adı farkı, gerekçe
öneki okuyucu, başlık önceliği, uçtan uca provenance) — eski kodda kırmızı, yenisinde yeşil.
