# G105 — Dava kartı UI: Olay Türü + Hükümdeki Rol alanları, belgeleme rozeti, liste filtresi

- **Bant:** frontend
- **Bağımlı:** - (sözleşme aşağıda dondurulmuş — G001/G002 deseni; backend G103 ile
  paralel koşabilir, vitest mock'larla çalışır)
- **Dosya kapsamı:** frontend/src/lib/caseCardFields.ts, frontend/src/lib/caseCardFields.test.ts,
  frontend/src/hooks/useConfig.ts, frontend/src/pages/CaseDetails.tsx,
  frontend/src/pages/CaseList.tsx, ilgili test dosyaları
- **Dokunma:** frontend/src/lib/api.ts (yalnız liste çağrısına `olay_turu` parametresi
  gerekiyorsa; başka değişiklik BLOKE bırak)

## Hedef

25.08 belgeleme olayı bulgusunun arayüz karşılığı (kullanıcı kararı 02.09): iki yeni
kapalı-liste alanı dava kartının "Tıbbi Bilgiler" kartına girer, "maddi red + manevi
kabul" imzası kartta uyarı rozeti olur, dava listesine Olay Türü filtresi eklenir.
Yeni ekran YOK; üç mevcut yere ekleme.

## SÖZLEŞME (dondurulmuş — G103 ile ORTAK; değiştirilemez, sapma gerekiyorsa BLOKE bırak)

- Kart alan anahtarları: `olay_turu` (etiket **"Olay Türü"**) ve `hukumdeki_rol`
  (etiket **"Hükümdeki Rol"**); ikisi de `type: "closedList"`.
- Liste anahtarları ve uçları: `event_types` → `GET /api/config/event_types`
  (3 değer: "Tıbbi Olay" · "Belgeleme Olayı" · "Tıbbi + Belgeleme");
  `judgment_roles` → `GET /api/config/judgment_roles`
  (4 değer: "Tek Gerekçe" · "Yan Gerekçe" · "Yalnız Saptama" · "Reddedilmiş İddia").
  Uç cevabı alleged_faults ile aynı biçimdedir (code/name/active/sequence, sıralı).
- Liste filtresi query param adı: `olay_turu` (değer = listenin ADI, ör.
  "Belgeleme Olayı").

## Yapılacaklar

1. **caseCardFields.ts:** `MEDICAL_CARD_FIELDS`e iki satır (beşlinin ALTINA — G048
   kuralı: tıbbi alanlar TEK grupta kalır, başka karta dağıtma). `ClosedListKey`
   tipine iki anahtar. Test: closedListState iki yeni liste için mevcut davranışın
   eşini veriyor.
2. **useConfig.ts:** `CONFIG_KEYS` + iki query (allegedFaultsQ kalıbı: queryKey,
   fetchJson, isLoading/isError agregasyonlarına dahil).
3. **CaseDetails.tsx:** `closedLists` objesi ÜÇ kullanım yerinde de (≈585/597/606
   satırları) iki yeni anahtarla genişler — tek yerde bırakmak diğer kartlarda boş
   dropdown üretir. Alanlar "Tıbbi Bilgiler" kartında görünür/düzenlenir (kartın
   mevcut düzenleme mekanizması neyse o — yeni mekanizma yazma).
4. **Rozet:** dava kartı başlık bölümünde uyarı rozeti — koşul (NULL ≠ 0 kuralına
   birebir dikkat): `hukmedilen_maddi === 0` (sayısal sıfır; null/undefined DEĞİL)
   VE `hukmedilen_manevi > 0` VE `olay_turu` boş → "Belgeleme olayı olabilir" rozeti.
   Rozet bilgilendirmedir, tıklanınca bir şey yapması gerekmez (title/tooltip ile kısa
   açıklama yeterli). `olay_turu` doluysa rozet KAYBOLUR.
5. **CaseList.tsx:** filtre çubuğuna "Olay Türü" seçimi (mevcut filtre dropdown'ları
   hangi desendeyse o — event_types listesinden beslenir, "Tümü" varsayılan);
   seçim `olay_turu` param'ıyla liste isteğine eklenir.

## Kabul kriterleri

- [ ] "Tıbbi Bilgiler" kartında yedi alan sıralı görünüyor; iki yenisi kapalı liste
      değerlerini backend listesinden alıyor (mock'lu test).
- [ ] Rozet testleri: (maddi=0, manevi>0, olay_turu boş) → VAR;
      (maddi=null, manevi>0) → YOK (NULL≠0!); (maddi=0, manevi>0, olay_turu dolu) → YOK;
      (maddi>0) → YOK.
- [ ] Filtre seçimi API çağrısına `olay_turu` param'ını ekliyor; "Tümü" seçiliyken
      param gönderilmiyor (test).
- [ ] Mevcut testlerde gerileme yok; yeni alanlar hiçbir yerde zorunlu değil, boşken
      kart bugünkü gibi görünüyor (filledFields davranışı).

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

**DURUM: TAMAM** (2026-09-02 gece koşusu)

### Yapılanlar

1. **caseCardFields.ts** — `ClosedListKey`e `event_types` / `judgment_roles` eklendi;
   `MEDICAL_CARD_FIELDS`e beşlinin ALTINA `olay_turu` ("Olay Türü") ve `hukumdeki_rol`
   ("Hükümdeki Rol") satırları girdi (ikisi de `closedList`). Rozet koşulu saf fonksiyon
   olarak buraya kondu: `isDocumentationEventCandidate` — `hukmedilen_maddi === 0`
   (SAYISAL sıfır; null/undefined "girilmedi"dir, imza SAYILMAZ) VE `hukmedilen_manevi > 0`
   VE `olay_turu` boş.
2. **useConfig.ts** — `CONFIG_KEYS.eventTypes/judgmentRoles` + iki query (allegedFaultsQ
   kalıbı birebir: fetchJson, `listQueries` hata/refetch agregasyonu, isLoading zinciri);
   `eventTypes` / `judgmentRoles` dışa verildi.
3. **CaseDetails.tsx** — `closedLists` objesi ÜÇ kullanım yerinde de (Tıbbi/Kanun
   Yolu/Büro kartları) iki yeni anahtarla genişledi (tip `Record<ClosedListKey,...>`
   olduğundan tsc eksiği zaten yakalar). Başlık bölümüne (statü rozetinin yanı) amber
   "Belgeleme olayı olabilir" bilgi rozeti eklendi — tıklanmaz, `title` tooltip'i
   açıklamayı taşır; `olay_turu` dolunca kaybolur. `CaseDetailsData`ya dört alan bildirimi
   eklendi (olay_turu, hukumdeki_rol, hukmedilen_maddi/manevi).
4. **CaseList.tsx** — filtre rail'ine "Olay Türü" Select'i (Dosya Türü deseni birebir;
   event_types listesinden beslenir, "Tümü" varsayılan); seçim `fetchCases` → `getCases`
   çağrısına `olayTuru` olarak gider; sayfa sıfırlama, Temizle ve aktif filtre sayacına
   bağlandı.
5. **useCases.ts** — `getCases` options'a `olayTuru?` eklendi; `ALL` değilse istek
   URL'ine `olay_turu` param'ı (değer = listenin ADI) yazılır — status/lawyer deseni.

### Kararlar + gerekçeler

- **Liste çağrısı api.ts'te değil useCases.ts'te yaşıyor.** Görevin "Dokunma" notu
  `olay_turu` param istisnasını api.ts için tanımlıyordu; api.ts'te dava liste çağrısı
  YOK (grep: `api/cases` eşleşmesi sıfır). İstisnanın hedeflediği değişiklik (liste
  çağrısına tek param) gerçek yerinde, `useCases.ts::getCases`e minimal olarak yapıldı;
  api.ts'e dokunulmadı.
- **Kart düzenleme mekanizması:** kartın mevcut mekanizması SALT OKUNUR gösterimdir
  (G048 tasarımı — TransferFieldsCard düzenleme kontrolü taşımaz); yeni alanlar da aynı
  yolla gösteriliyor, yeni düzenleme mekanizması YAZILMADI (görev talimatı).
- **caseCardFields.test.ts'te iki envanter-fotoğrafı testi güncellendi** ("beş tıbbi
  alan..." → yedi alanlı dizi; "iki kapalı liste..." → dört girdili dizi). Bu testler
  alan envanterinin birebir fotoğrafı; görev envanteri bilinçli değiştiriyor, test
  dosyası görev kapsam listesinde ve kabul kriteri "yedi alan" diyor. Beklenti
  GEVŞETİLMEDİ: kesin `toEqual` dizileri korunup yeni alanlarla genişledi.
- Rozet eşiği testlerle sabitlendi: `"   "` (yalnız boşluk) olay_turu BOŞ sayılır
  (isFilled kuralı) → rozet durur; "Tıbbi Olay" seçimi de rozeti düşürür (koşul
  "boş", "belgeleme değil" değil).

### Test sonuçları

- `npm --prefix frontend test`: **47 dosya, 567 test GEÇTİ** (önce 550 idi; +17 yeni,
  gerileme yok). TUR 1'de yeşil.
- `npm --prefix frontend run lint`: **0 hata**, 22 uyarı — tümü önceden var,
  dokunulan dosyalarda uyarı yok.
- `npx tsc -b --force`: **exit 0**.
- Yeni test dosyaları (eski kodda KIRMIZI olacak şekilde):
  `frontend/src/lib/caseCardFields.documentationEvent.test.ts` (alan tanımları +
  closedListState eşdavranışı + rozetin 4 kabul senaryosu ve kenarları; eski kodda
  import hatasıyla düşer), `frontend/src/hooks/useConfig.eventTypes.test.tsx`
  (iki uç mock'la sözleşme değerlerini sıralı verir + G019 hata bayrağı; eski kodda
  `eventTypes` undefined), `frontend/src/hooks/useCases.olayTuru.test.tsx` (param
  eklenir / ALL'da ve verilmeyince GÖNDERİLMEZ; eski kod param'ı yok sayar).

### İzlenecekler

- Backend eşi **G103**: `/api/config/event_types` + `/api/config/judgment_roles` uçları,
  `cases.olay_turu/hukumdeki_rol` kolonları ve `GET /api/cases`in `olay_turu` query
  filtresi orada açılacak — sözleşme bu görevle ORTAK ve dondurulmuş. G103 gelene dek
  uçlar 404 döner: useConfig G019 gereği config hata şeridi gösterir (boş listeyle
  karışmaz), kartta alanlar boş olduğundan görünmez, rozet `olay_turu` yokken de
  hükmedilen tutar imzasıyla çalışır.
- Kartta düzenleme yolu bilinçli yok (mevcut mekanizma salt okunur); alanların yazma
  yolu G103/FAZ F hattının işi.
