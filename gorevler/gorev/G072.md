# G072 — Aşama tarihçesinin okuma yolu: 4.971 satır yazdık, hiçbiri görünmüyor

- **Bant:** backend
- **Bağımlı:** - (tablo G062'de açıldı, veri 2026-08-19 aktarımıyla doldu)
- **Dosya kapsamı:** backend/routes/cases.py (yeni okuma route'u),
  backend/schemas.py (yanıt şeması), backend/managers/stage_decisions.py
  (yalnız `get_stage_decisions` gerekiyorsa sarmalayıcı),
  backend/tests/test_g072_asama_okuma.py
- **Dokunma:** `managers/stage_decisions.py`ın YAZMA yolu (`add_stage_decision`,
  `delete_stage_decision`, `_resync_stage_photo`) — bu görev SALT OKUMA.
  `case_stage_decisions` şeması değişmez. Takip panelinin mevcut yazma yolu
  (`update_case_tracking`) da bu turda ellenmez (G073'ün işi).

## Hedef

`case_stage_decisions` tablosunun **ne route'u ne ekranı var**. G062 tabloyu ve tek
yazma yolunu kurdu, bilinçli olarak arayüzsüz bıraktı. 2026-08-19 HUKDOK aktarımı
tabloyu doldurdu — lokal ölçüm:

| Aşama | Satır | Kart |
| --- | --- | --- |
| YEREL | 3.098 | 3.098 |
| İSTİNAF | 1.236 | 1.236 |
| TEMYİZ | 574 | 574 |
| KARAR DÜZELTME | 63 | 63 |
| **Toplam** | **4.971** | — |

Damga dağılımı: 3.604 `TURETILDI`, 1.367 `BELIRSIZ`. Ayrıca 517 satır önceki esas
(`case_esas_numbers.stage='ONCEKI'`).

Kullanıcı bugün takip panelinde yalnız **son aşamanın fotoğrafını** görüyor
(`cases` üzerindeki tek-slot kolonlar). Tarihçe ekranda yok. Kanıt vakası (G062
docstring'i): id-2271 — Danıştay 2023 **Bozma** + 2026 **Onama**; panelde yalnız
Onama görünür, Bozma tabloda saklı kalır.

## Karar noktaları (işçi karar verir ve GEREKÇELENDİRİR)

1. **Ayrı route mu, `CaseRead`e gömme mi?** Öneri: **ayrı route**
   (`GET /api/cases/{case_id}/stage-decisions`). Gerekçe: dava kartı yanıtı zaten
   büyük (taraflar + belgeler + geçmiş) ve aşama tarihçesi yalnız takip sekmesi
   açılınca gerekiyor; kart açılışını 4.971 satırlık tablonun JOIN'iyle
   yavaşlatmanın karşılığı yok. Ölç ve gerekçelendir.
2. **Önceki esas satırları bu yanıta girer mi?** `case_esas_numbers.stage='ONCEKI'`
   bir karar değil ama aynı zaman çizgisinin parçası (görevsizlik → yeni esas).
   Öneri: **ayrı alanda** (`onceki_esaslar`) döndür, karar satırlarıyla
   KARIŞTIRMA — biri kararın künyesi, diğeri numaranın tarihçesi.
3. **Tenant filtresi.** Dava görünürlüğü `_apply_tenant_filter` deseninden geçmeli;
   route önce davayı bu filtreyle çözmeli, sonra satırları okumalı (G016 dersi:
   bağlantısız kaynakta tenant izolasyonu unutulmuştu).

## Kabul kriterleri

- [ ] `GET /api/cases/{id}/stage-decisions` aşama satırlarını `sira_no` sırasıyla
      döndürüyor; her satırda mahkeme, esas/karar no, tarihler, karar durumu,
      başvuran taraf, açıklama, `dogrulama_durumu`, `source`
- [ ] Yanıt Pydantic şemasıyla tiplenmiş (`schemas.py`), route dönüşü `response_model` ile bağlı
- [ ] Başka tenant'ın davası → 404 (kayıt sızmıyor); testi var
- [ ] Kartı olmayan/silinmiş dava → 404
- [ ] Aşama tarihçesi boş dava → boş liste (200), hata değil
- [ ] `onceki_esaslar` ayrı alanda; is_current satırı bu listeye GİRMİYOR
- [ ] Yazma yolu dosyalarında diff YOK (salt okuma görevi)

## Doğrulama

```bash
docker compose exec -T backend python -m pytest tests/test_g072_asama_okuma.py
docker compose exec -T backend python -m pytest
docker compose exec -T backend python -m ruff check .
docker compose exec -T backend python -m mypy
```

Ek olarak lokal veriyle elle bir çağrı: aktarım koşulmuş bir kartta (örn. YEREL +
İSTİNAF satırı olan bir dava) yanıtın iki satır döndüğü ve sıralamanın `sira_no`
olduğu gözlenir.
