# G023 — .eml gövde temizliğini tokenizer'a taşı (G015'in RET'ini kapatır)

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/services/html_sanitizer.py (YENİ), backend/routes/case_intake.py (yalnız gövde temizliği bölümü ~:95-180, :201-236), backend/tests/test_sanitizer_html.py (YENİ), backend/tests/test_eml_expand.py
- **Dokunma:** backend/pdf/format_converter.py (**G024'ün işi**), backend/file_utils.py, case_intake.py'deki intake/commit mantığı

## Hedef

G015 aynı sorunu regex ile çözmeye çalıştı ve **denetimden RET aldı**: `_TAG_RE` etiketi
`[^>]*>` ile kesiyor, oysa LibreOffice'in tokenizer'ı tırnak içindeki `>` karakterini etiket
sonu saymıyor. 13 ajanlı bir bypass avı bu sınıfın **kapanmadığını** kanıtladı.

### Ayakta kalan bypass'lar (hepsi gerçek soffice GET'i üretti, probe kayıtlı)

| # | Yük | Neden geçiyor |
| --- | --- | --- |
| P1 | `<table dummy='a>b' background='http://…'><tr><td>x</td></tr></table>` | tek tırnak içinde `>` |
| P6 | `<img alt="a>b" src="http://…">` | `_REMOTE_IMG_RE`'yi **de** deliyor |
| P7 | `<table dummy="a>\nb" background="http://…">…` | değer içinde newline + `>` |
| P8 | `<body foo="a>b" background="http://…">x</body>` | body background taşıyıcısı |
| P9 | `<table a="x>y" b="p>q" background="http://…">…` | art arda iki tırnaklı `>` |
| P11 | `<table><tr><td dummy="a>b" background="http://…">x</td></tr></table>` | td background — hiçbir denylist'te yok |
| P2 | `<table/background="http://…"><tr><td>x</td></tr></table>` | `_ATTR_RE` ad öncesi `\s+` şart koşuyor, `/` boşluk değil |
| BASE | `<base dummy="a>b" href="http://…"><img src="BIMG.png">` | **bağımsız doğrulandı**; tüm göreli referansları uzak base'e çözdürüyor |

> **Denetçinin önerdiği düzeltilmiş regex TEK BAŞINA YETMİYOR** — ölçüldü: P2 ayakta kalıyor
> (`_ATTR_RE` de düzeltilmeli) ve düzeltilmiş hâli bile iki vakada saldırgan baytını **aynen
> geçiriyor**: dosya sonunda kapanmamış etiket (`<img src="http://…"` — `>` yok) ve dengesiz
> tırnak. Öneriyi olduğu gibi uygulayan bir görev **yine RET yerdi**.

### Kapalı çıkan sınıflar (tekrar araştırma — bunlara zaman harcama)

- **Entity/kodlama tamamen kapalı.** `_normalize_attr_value` zinciri ondalık/hex/adlı entity,
  şema-içi boşluk, büyük-küçük harf, NUL/DEL ve Unicode boşlukları yakalıyor. Kaçan
  zero-width karakterler soffice için de şemayı bozuyor → istek doğmuyor.
- **CSS bağlamları gerçek istek üretmiyor.** LibreOffice HTML import'u CSS-referanslı uzak
  kaynakları çekmiyor (`@import`, `url()`, `background-image`, `@font-face` — hepsi boş probe).
  Yine de derinlik-savunması olarak politikaya dahil (renderer değişirse canlıya döner).
- **Alternatif çekiciler etkisiz:** `object data`, `embed src`, `iframe/frame src`,
  `input[type=image]`, `video poster`, `audio src`, inline SVG `image href`, `meta refresh`,
  `form action` — süzgeci geçiyorlar ama soffice çekmiyor. **Tek istisna `<base href>`.**

## Yaklaşım (karar verilmiş — tartışma değil)

**`html.parser.HTMLParser` ile yeniden serileştirme.** Gerekçe, "şu yükü de yakaladık" değil:
regex yaklaşımının yapısal kusuru **saldırganın baytlarını çıktıya aynen geçirmesi**.
Yeniden serileştirmede çıktıdaki her etiket ve attribute **bizim** kurallarımızla, çift
tırnaklı ve escape'li olarak yeniden yazılır — LibreOffice'in tokenizer'ı bizimkinden farklı
düşünse bile ortada tokenize edilecek belirsiz bayt kalmaz. İddia sınıf düzeyinde olur:
*"çıktı belirsizlik içermeyen kanonik HTML'dir."*

Sanitizer `backend/services/html_sanitizer.py`'ye taşınır (FastAPI/eml hattı kurmadan birim
+ fuzz testi yazılabilsin). Eski desenler (`_TAG_RE`, `_ATTR_RE`, `_SCRIPT_RE`, `_LINK_TAG_RE`,
`_REMOTE_IMG_RE`, `_STYLE_BLOCK_RE`, `_META_CHARSET_RE`) **silinir** — iki tokenizer'ı üst üste
koymak yeni ayrışma yüzeyi üretir.

**Politika: isim bazlı allowlist + korunan alanlarda değer kapısı.**
- Komple düşürülen etiketler/alt ağaçlar: `script, style, link, base, meta, iframe, frame,
  frameset, noframes, object, embed, applet, param, source, track, video, audio, canvas,
  svg, math, template, input, image`. (`base` attribute soymakla yetmez — komple gitmeli.)
- `<img>`: `src` kapıdan geçemezse **etiket komple düşer** (yoksa soffice kırık görsel kutusu
  çizer ve `test_cid_image_still_removed` kırılır — G015'in doğru kararı korunuyor).
- Attribute allowlist'i: yapı (`colspan, rowspan, border, cellpadding, cellspacing, align,
  valign, width, height, nowrap, span`), anlam (`lang, dir, class, id, title, alt, start,
  value, type`), biçim (`bgcolor, color, face, size`), artı değer kapılı `src`, `href`, `style`.
  Allowlist dışı her attribute düşer → taşıyıcı uzayını tek tek saymadan kapatır (`on*` dahil).
- Değer kapıları: `src`/`href` normalize edildikten sonra `:` içeriyorsa **veya** `//`/`\\`
  ile başlıyorsa düşer (yalnız saf göreli yol kalır). `style` içinde `url`, `@import`,
  `expression`, `image-set` ya da `\` varsa attribute **komple** düşer (`none` ile ikame etme —
  `url(` literalini aramak CSS kaçışına kör).
- Yorum, PI, bogus decl, CDATA düşer; `handle_decl` **sabit** `<!DOCTYPE html>` yayınlar.
- Değersiz attribute yalnız `^[a-z_:][-.\w:]*$` kalıbına uyuyorsa yazılır.

**Fail-closed zorunlu:** sanitizer istisna atarsa ya da boyut tavanı aşılırsa ham HTML
soffice'e **asla** gitmez — mevcut düz-metin yoluna düşülür.

## Kabul kriterleri

- [ ] **AC1 Mekanizma:** temizlik regex taramasıyla değil tokenizer + yeniden serileştirmeyle.
      `case_intake.py`'de `_TAG_RE`/`_ATTR_RE`/`_SCRIPT_RE`/`_LINK_TAG_RE`/`_REMOTE_IMG_RE`/
      `_STYLE_BLOCK_RE` KALMAZ; çıktıdaki her attribute yeniden alıntılanmış/escape'li
- [ ] **AC2 Politika:** attribute allowlist'i ve düşürülen etiket kümesi **tek birer sabitte**;
      değer kapısı yalnız `src`/`href`/`style` için
- [ ] **AC3 Fail-closed:** istisna ya da boyut tavanı → düz-metin yolu; testle kanıtlı
      (`test_sanitizer_failure_falls_back_to_plaintext`)
- [ ] **AC4 Korpus:** yukarıdaki 8 bypass + kapanmamış etiket (EOF) + dengesiz tırnak +
      CSS kaçışlı `style` + `meta refresh` + koşullu yorum = **13 vaka** testte.
      Her vaka için **ÇİFT İDDİA**: (a) saldırgan host çıktının hiçbir yerinde yok,
      (b) çıktı `HTMLParser` ile **yeniden ayrıştırıldığında** uzak şema/`//` eşleşmesi ve
      allowlist ihlali yok. Ayrıca `test_carrier_matrix_no_remote_url_survives` özellik testi:
      {tırnak türü} × {ayırıcı: boşluk, `/`, newline} × {tuzak `>` var/yok} × {taşıyıcı:
      background, src, poster, srcset, data, href, xml:base, longdesc, usemap} ×
      {şema: `http:`, `//`, `HTTP:`} kartezyeni, **≥200 kombinasyon**
- [ ] **AC5 Negatif kontrol (zorunlu):** aynı korpus **G015 sonrası mevcut zincire** karşı
      koşulur ve **en az 8 vakanın DELDİĞİ** raporda kanıtlanır. Testin iş yaptığının kanıtı
      olmadan AC4 sayılmaz
- [ ] **AC6 Canlı kanıt:** gerçek soffice + gerçek dinleyiciyle uçtan uca test; **giden istek
      kaydı boş**. Rapor port, dinleyici kaydı ve PDF boyutunu içerir
- [ ] **AC7 Bozmama:** `test_eml_expand.py`'deki mevcut 14 test **değiştirilmeden** yeşil —
      özellikle `test_local_attributes_preserved`, `test_cid_image_still_removed`, iki soffice
      testi. Mevcut bir assertion'ı değiştirmek **RET sebebidir** (gerekçesi önceden yazılmadıkça)
- [ ] **AC8 Metin sadakati:** Türkçe entity'ler, tablo yapısı, koşullu-yorum içindeki görünür
      metin korunuyor; bozuk markup (`<td>a<td>b<p>c</div>`) metin kaybı olmadan geçiyor
- [ ] **AC9 Performans:** 1 MB temsili gövde için süre ölçülüp rapora yazıldı; >250 ms ise
      boyut tavanı buna göre ayarlandı
- [ ] **AC10 Kalan açık beyanı:** rapor **"SSRF kapandı" DEMEZ**. `.docx/.xlsx/.doc/.xls`
      harici bağlı görsel sınıfının E2E kanıtlı ve AÇIK olduğu, `format_converter.py::_soffice_to_pdf`
      hattının temizliksiz olduğu yazılır ve **G024'e devredilir**
- [ ] **AC11 Kapılar:** pytest tabanı görev başındaki HEAD'de **ölçülür** (ezberden yazma);
      altına düşmek yasak, yeni testlerle net artı. `ruff check .` ve `mypy` temiz

> **DENETÇİYE NOT:** kriterleri "yük listesi" olarak değil **sınıf iddiası** olarak sına.
> Kendi ürettiğin YENİ bir etiket-sınırı / ayırıcı / kaçış varyantını sanitizer'a ver ve
> çıktıda uzak URL ara; bulursan AC4 sağlanmamıştır. G015 tam da bu yüzden RET aldı.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

### Yapılanlar

`backend/services/html_sanitizer.py` (YENİ, 300 satır) — `html.parser.HTMLParser`
üzerine kurulu tokenizer + yeniden serileştirici. `routes/case_intake.py`'deki
regex zinciri (`_TAG_RE`, `_ATTR_RE`, `_SCRIPT_RE`, `_LINK_TAG_RE`,
`_REMOTE_IMG_RE`, `_STYLE_BLOCK_RE`, `_META_CHARSET_RE`, `_BODY_OPEN_RE`,
`_ANY_TAG_RE`, `_REMOTE_SCHEME_RE`, `_PROTO_RELATIVE_RE`, `_ATTR_STRIP_RE`,
`_CSS_IMPORT_RE`, `_CSS_URL_RE`) ve yardımcıları (`_normalize_attr_value`,
`_strip_css_remote`, `_clean_tag_attrs`, `_strip_remote_refs`) tamamen silindi.

Testler: `backend/tests/test_sanitizer_html.py` (YENİ, 43 test),
`backend/tests/test_eml_expand.py` (+4 test; mevcut 14 test **satırı satırına
değişmedi**).

### Alınan kararlar + gerekçeleri

1. **Belge iskeleti girdiden bağımsız sabittir** (`DOC_PREFIX`/`DOC_SUFFIX`).
   Görev "handle_decl sabit `<!DOCTYPE html>` yayınlar" diyordu; bir adım
   ilerisi uygulandı: `handle_decl` hiçbir şey yayınlamaz, DOCTYPE + `<html>` +
   `<head><meta charset>` + `<body>` iskeletini **biz** bir kez yazarız.
   Gerekçe: (a) girdideki bildirim baytı çıktıyı hiç etkilemez, (b) `<body>`
   nerede diye regex aramaya gerek kalmadığı için `_BODY_OPEN_RE` de silindi —
   başlık tablosu `body_prefix` olarak iskeletin içine konur.
2. **`html`/`head`/`body`/`title` "iskelet etiketi" sayılır: etiket düşer,
   İÇERİK kalır.** Alt ağaç bastırması bilinçli kullanılmadı: gerçek maillerde
   `</head>` sıklıkla eksik ve bastırma tüm gövdeyi yutardı.
3. **`<img>` src kapıdan geçemezse etiket komple düşer** — G015'in doğru kararı
   korundu (boş iskelet soffice'te kırık görsel kutusu çizer,
   `test_cid_image_still_removed` bunu sabitliyor).
4. **Tamamlanmamış markup kalıntısı yayınlanmaz.** HTMLParser, kapanmamış
   etiketi ve dengesiz tırnaklı etiketi EOF'ta ham METİN olarak geri verir;
   olduğu gibi yazmak tam da G015'in kusuruydu. `<` ile başlayan veri parçasında
   yalnız `&lt;` yayınlanır, gerisi atılır (metindeki yalnız `<` karakteri
   HTMLParser'da kendi başına tek karakterlik parça olarak geldiği için korunur —
   `test_lone_less_than_in_text_survives_as_entity`).
5. **Koşullu yorumun içeriği aynı temizlikten geçirilip yayınlanır** (derinlik
   sınırı 3). AC8 "koşullu-yorum içindeki görünür metin korunuyor" diyor; içerik
   yorumun içinde bırakılsa soffice de render etmez ve metin modele ulaşmazdı.
   **Yan etki (bilinçli):** Outlook'a özel `[if mso]` blokları artık PDF'e
   giriyor; mso blokları çoğu şablonda ana içeriğin kopyası olduğu için gövde
   metni tekrarlanabilir. Hedef görsel sadakat değil metnin modele ulaşması
   olduğundan tekrar, kayıp riskine tercih edildi.
6. **Style kapısı ad eşlemesi değil sınıf kuralıdır.** Görev `url`/`@import`/
   `expression`/`image-set`/`\` diyordu; bypass avımda `background:u/**/rl(…)`
   (CSS yorumu adı bölüyor) bu listeyi deldi. Kapıya `(` (fonksiyon çağrısı
   sözdizimi) ve `/*` (CSS yorumu) eklendi — artık ad hiç okunmadan tüm çağrı
   sınıfı kapanıyor. `calc()` gibi meşru fonksiyonların kaybı önemsiz (görsel
   sadakat hedef değil).
7. **`src`/`href` kapısı `\\` yerine "herhangi bir `\`"e bakar.** Göreli yolda
   ters bölü hiçbir zaman meşru değil; UNC'nin tek varyantını saymak yerine sınıf
   kapatıldı.
8. **Metin değerli allowlist attribute'ları (`title`, `alt`, `class`, `id`,
   `value`, `type`, `lang`, `dir`, `bgcolor`, `color`, `face`) kapılı DEĞİL.**
   Kaynak çekmedikleri **canlı dinleyiciyle ölçüldü** (aşağıdaki tabloda
   `ALLOWLIST_METIN_ATTR` satırı: ham hâlde bile 0 GET). İçlerindeki URL benzeri
   dizge, gövde metnindeki adresle aynı sınıftadır — o da bilinçli korunuyor.
9. **Fail-closed düz-metin yolu `html_to_text` ile beslenir** (o da tokenizer;
   asla istisna atmaz), sonuç `html.escape` ile `<pre>` içine konur → ham HTML
   soffice'e hiçbir yoldan ulaşmaz.

### Test sonuçları

| Kapı | Sonuç |
| --- | --- |
| pytest tabanı (görev başı, HEAD `bc35eb1`) | **905 passed, 2 skipped** — ölçüldü |
| pytest (görev sonu) | **952 passed, 2 skipped** (net **+47** test) |
| `ruff check .` | All checks passed! |
| `mypy` | Success: no issues found in 29 source files |
| `test_eml_expand.py` mevcut 14 test | 14/14 yeşil, **assertion değiştirilmedi** |

**AC4 korpus (13 vaka):** hepsi çift iddiayla geçiyor — (a) saldırgan host
çıktıda yok, (b) çıktı `HTMLParser` ile yeniden ayrıştırıldığında kapılı
attribute'larda uzak şema/`//`/`\` yok ve allowlist ihlali yok. Denetim, çıktıyı
regex'le taramak yerine **gerçek bir tokenizer'a** veriyor (G015'in kusuru tam da
temizleyici ile denetçinin ayrışmasıydı).

**AC4 matris:** `test_carrier_matrix_no_remote_url_survives` — {3 tırnak} × {3
ayırıcı} × {2 tuzak `>`} × {9 taşıyıcı} × {3 şema} = **486 kombinasyon**, hepsi
temiz; `test_carrier_matrix_keeps_cell_text` aynı 486'da hücre metninin durduğunu
gösteriyor (aşırı temizlik kontrolü).

**AC5 negatif kontrol (korpus iş yapıyor mu?):** aynı korpus, G015 sonrası
zincirin dondurulmuş kopyasına (`_legacy_clean`, `bc35eb1` birebir) verildi:

- **13 vakanın 10'u eski zinciri deliyor** (≥8 şartı sağlandı):
  `P1_tek_tirnak_gt, P2_slash_ayirici, P6_img_alt_gt, P7_deger_icinde_newline,
  P8_body_background, P9_iki_tuzakli_attr, P11_td_background, BASE_href,
  DENGESIZ_TIRNAK, EOF_kapanmamis_etiket`
- Eski zincirin tuttuğu 3 vaka: `CSS_KACISLI_STYLE`, `META_REFRESH`,
  `KOSULLU_YORUM` (derinlik-savunması sınıfları).
- Matriste **486 kombinasyonun 324'ü** eski zinciri deliyor.
- İki test kalıcı: `test_negative_control_legacy_chain_is_pierced`,
  `test_negative_control_matrix_is_pierced`.

**AC6 canlı kanıt (gerçek soffice + gerçek dinleyici, konteyner içi):**
dinleyici `127.0.0.1:36145` (efemeral port), her yük tek tek gerçek
`soffice`'e verildi; ham hâl ile temizlenmiş hâl ayrı sayıldı.

| Vaka | ham GET | temiz GET | PDF bayt |
| --- | --- | --- | --- |
| P1_tek_tirnak_gt | 1 | **0** | 13.359 |
| P6_img_alt_gt | 2 | **0** | 6.547 |
| P7_deger_icinde_newline | 1 | **0** | 12.379 |
| P8_body_background | 0 | **0** | 13.960 |
| P9_iki_tuzakli_attr | 1 | **0** | 12.379 |
| P11_td_background | 1 | **0** | 13.359 |
| P2_slash_ayirici | 1 | **0** | 12.379 |
| BASE_href | 2 | **0** | 6.913 |
| CSS_KACISLI_STYLE | 0 | **0** | 13.008 |
| META_REFRESH | 0 | **0** | 6.547 |
| KOSULLU_YORUM | 0 | **0** | 14.039 |
| DENGESIZ_TIRNAK | 0 | **0** | 6.547 |
| EOF_kapanmamis_etiket | 2 | **0** | 12.661 |
| ALLOWLIST_METIN_ATTR | 0 | **0** | 14.566 |
| **TOPLAM** | **11 istek / 8 vaka** | **0** | — |

Dinleyici kaydı temizlenmiş hâlde **tamamen boş**. `P8_body_background`'ın ham
hâlde GET üretmemesi fikstür farkıdır (yükü `<html><body>` ile sardığım için iç
`<body>` yuvalanıyor); görev dosyasındaki özgün probe'da tek başınaydı. Diğer üç
sıfır satırı (`CSS_KACISLI_STYLE`, `META_REFRESH`, `KOSULLU_YORUM`,
`DENGESIZ_TIRNAK`) zaten "renderer bugün çekmiyor" sınıfı — politikada
derinlik-savunması olarak duruyorlar.

Bu düzenek **kalıcı iki teste** çevrildi (`requires_soffice` kapılı):
`test_probe_listener_records_raw_payload_positive_control` (pozitif kontrol —
ham yük istek doğurur, düzenek çalışıyor) ve
`test_no_outbound_request_after_sanitize` (uçtan uca `/expand-eml`, giden istek
kaydı boş). Pozitif kontrol düşerse asıl testin "boş kayıt" sonucu anlamsızdır;
ikisi bilerek birlikte duruyor.

**Bağımsız bypass avı (denetçi notu gereği, korpus/matris DIŞI 47 yeni varyant):**
NUL enjeksiyonu, `=` çevresi boşluk, `/` ayırıcı, entity/hex/tab/ZWSP şema
kaçışları, iç içe `<`, `<svg><image>`, downlevel-revealed koşullu yorum,
`<![if]>` bogus decl, backtick tırnak, mükerrer attribute, CR/FF ayırıcı,
`xmp`/`textarea`/`noscript` sarmalayıcı, 40 seviye yuvalama, iç içe koşullu
yorum vb. **İlk koşuda 1 gerçek gedik çıktı** (`style` içinde CSS yorumu) →
kapatıldı ve kalıcı parametrik teste çevrildi
(`test_style_gate_is_class_level_not_name_matching`). Kalan 8 "sağ kalan"ın
tamamı kaçışlanmış METİN sınıfı: 6'sı metin değerli allowlist attribute'ı
(canlı ölçümle 0 GET), 1'i düz metindeki URL (bilinçli korunuyor), 1'i
`<img <!--x--> src="…">` varyantının `&lt;`-kaçışlı metin kalıntısı — hiçbiri
markup olarak yorumlanmıyor, politika ihlali üretmiyor.

**AC9 performans:** 1 MB temsili gövde (tablo satırları + `style`/`class`/`href`)
→ **236,7 ms** (konteyner, 5 koşunun en iyisi). 250 ms eşiğinin altında olduğu
için tavan bu ölçümle belirlendi: `MAX_HTML_CHARS = 2_000_000` (≈0,5 sn en kötü
durum). Temizlik istek işleyicisinde satır içi koştuğu için tavan bilinçli dar
tutuldu; aşan gövde istisna alır ve düz-metin yoluna düşer.
`test_one_megabyte_body_under_budget` bunu CI payıyla (3 sn) sabitliyor,
`test_default_size_cap_is_documented_value` tavanın sessizce büyütülmesini
engelliyor.

### AC10 — kalan açık beyanı (SSRF KAPANMADI)

**Bu görev "SSRF kapandı" DEMEZ.** Kapanan, `.eml` gövdesinin HTML temizliği
sınıfıdır. Açık kalanlar:

1. **`.docx/.xlsx/.doc/.xls` harici bağlı görsel sınıfı AÇIK ve E2E KANITLI.**
   Office belgeleri `/process` ve commit hattında `pdf/format_converter.py`
   üzerinden soffice'e **hiçbir temizlikten geçmeden** gidiyor; harici bağlı
   görsel/`INCLUDEPICTURE` alanları uzak kaynak çektiriyor.
2. **`format_converter.py::_soffice_to_pdf` hattı temizliksizdir** — bu görevin
   "Dokunma" listesinde olduğu için elle sürülmedi.
3. Her ikisi de **G024'e devredilir.**

Ayrıca bilinçli açık bırakılanlar (risk değil, karar): gövde METNİNDEKİ URL'ler
ve metin değerli attribute'lardaki URL benzeri dizgeler korunuyor (istek
doğurmadıkları canlı ölçümle doğrulandı, modele bilgi taşıyorlar).

### İzlenecekler

- **G024**: `format_converter.py` hattı + Office harici bağlı görsel sınıfı.
- **Koşullu yorum tekrarı**: `[if mso]` blokları artık render ediliyor; gerçek
  bir Outlook mailinde gövde metninin ikizlenmesi çıkarım kalitesini etkilerse
  karar 5 gözden geçirilmeli (metin kaybı ↔ tekrar dengesi).
- **`_legacy_clean` dondurulmuş kopyası** yalnız AC5 kanıtı içindir; G015 zinciri
  artık üretimde yok. Silinirse korpusun iş yaptığının kanıtı da gider.
- Canlı soffice testleri host'ta (soffice yok) atlanır; kanıt yalnız konteynerde
  koşarken üretilir.

**DURUM: TAMAM**
