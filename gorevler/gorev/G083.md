# G083 — Topbar zil paneli: bildirim listesi + okunmamış rozeti

- **Bant:** frontend
- **Bağımlı:** G081
- **Dosya kapsamı:** frontend/src/components/shell/Topbar.tsx,
  frontend/src/components/notifications/** (YENİ),
  frontend/src/hooks/useNotifications.ts (YENİ), ilgili test dosyaları
- **Dokunma:** frontend/src/components/PlaceholderBadge.tsx (SİLİNMEZ — süre panelleri
  G086'ya kadar kullanmaya devam eder), frontend/src/lib/api.ts (`apiClient` deseni yeterli)

## Hedef
`Topbar.tsx:126-135`'teki zil butonu bugün `onClick={() => { /* TODO: bildirim paneli */ }}`
ve yanında `PlaceholderBadge position="corner"` duruyor. Buton gerçek panele bağlanır.

Kanal kararı: **yalnız uygulama içi**. SSE/WebSocket YOK — 2 worker + nginx
`proxy_read_timeout` karmaşasına değmez; 60 sn polling yeterli.

## Kabul kriterleri
- [ ] Zile tıklayınca dropdown açılır; dışarı tıklama/Escape kapatır
- [ ] Okunmamış sayısı rozette görünür (tavan "9+"); 0 ise rozet hiç çizilmez
- [ ] 60 sn'de bir `GET /api/notifications/count` ile tazelenir; **`document.hidden` iken
      polling durur** ve sekme geri görünür olunca hemen bir kez tazeler
- [ ] Bildirime tıklama: okundu işaretler **ve** `case_id` doluysa `/cases/:id`ye gider
- [ ] "Tümünü okundu işaretle" düğmesi çalışır ve rozeti sıfırlar
- [ ] Zilin `PlaceholderBadge`ı kaldırıldı (Topbar'da başka kullanımı yok)
- [ ] Boş durum ("bildirim yok") ve hata durumu ayrı ayrı gösterilir — hata sessizce boş
      liste gibi görünmez
- [ ] Test: hook'un polling/görünürlük davranışı + panelin üç durumu (dolu, boş, hata)

## Doğrulama
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor
(işçi oturumu doldurur)
