# G120 — Aktarım eşlemesi: Müvekkil Tipi + Hizmet Türü sütunları teslimden kartlara

- **Bant:** backend
- **Bağımlı:** G119
- **Dosya kapsamı:** backend/scripts/hukdok_aktarim.py, backend/tests/** (yeni test
  dosyası `test_g120_aktarim_muvekkil_hizmet.py` ya da test_g104'e ekleme)
- **Dokunma:** backend/models.py, backend/managers/** (G119 bitmiş şemayı KULLAN,
  değiştirme; eksik görürsen BLOKE bırak)

## Hedef

DB-2026-002 (04.09.2026): `Sheet` sayfasında `Müvekkil Tipi` ve `Hizmet Türü` sütunları
HUKDOK_TESLIM_PAKETI_2026-09-04.xlsx'ten itibaren geliyor, ikisi de kapalı liste ve
föylerin tamamında dolu. `hukdok_aktarim.py` yazma yoluna iki sütunun eşlemesi G104
deseninin birebir kopyasıyla eklenir; başlık YOKKEN davranış değişmez.

Bildirimin örnek satırı: `SistemNo=H-11636, Müvekkil Tipi="Sigorta", Hizmet Türü="Lexis Rapor"`.

## SÖZLEŞME (G119 ile aynı; sapma gerekiyorsa BLOKE bırak)

- Hedef kolonlar: `cases.muvekkil_tipi`, `cases.hizmet_turu` (ad denormalize).
- Beklenen teslim başlıkları: `Müvekkil Tipi` ve `Hizmet Türü` (`SUTUN_ADAYLARI`
  demeti; `_baslik_anahtari` toleransı aksan/büyük-küçük farkını zaten yutar).
- Değer eşlemesi AD bazlı, kanonik adların tek kaynağı G119 seed sabitleri
  (`seed_data.CLIENT_TYPES` / `SERVICE_TYPES` — literal kopya YOK); `_kapali_liste_parcalari`
  yolu (G104) kullanılır.
- Çok değerli hücre (` ; `): iki alanda da TANIMSIZ → yazılmaz, `AlanHatasi` ile satır
  raporuna düşer (hukumdeki_rol kuralı). KARMA benzeri normalizasyon YOK.

## Pazarlıksız kurallar (G104 ile aynı — bozma)

- **Başlık teslimde yoksa alan atlanır** ("None = bu teslimde yok"). Mevcut testler
  değişmeden yeşil.
- **Tanınmayan değer YAZILMAZ** (tahmin yasağı): kapalı listede karşılığı olmayan değer
  alan-düzeyi atlanır, satır raporuna `tur=HATA` düşer, föyün diğer alanları işlenir.
- **Dolu hücre sınıfı:** METİN alanlarının varsayılan üzerine-yazma sınıfı (G104 raporu);
  özel kural icat etme.
- **İdempotency:** aynı girdiyle ikinci koşu 0 değişiklik.
- **Kardeş föy çelişkisi:** iki alan mevcut ön-geçiş mekanizmasına girer; aynı davanın
  föyleri farklı müvekkili takip edebildiği için (bildirim: "föy başına değişir") bu
  alanlarda çelişki BEKLENEN durumdur — mekanizma neyse o (rapora düşer), özel istisna
  yazma; raporda kaç kardeş çelişkisi ürettiğini yaz.

## Kabul kriterleri

- [ ] Başlıksız (eski format) girdiyle koşu: davranış birebir eski — mevcut aktarım
      testleri değişiklik gerektirmeden geçiyor.
- [ ] Başlıklı girdiyle kuru koşu: iki alan fark raporunda görünüyor, DB'ye yazılmıyor.
- [ ] Gerçek koşu: "Sigorta" / "Lexis Rapor" yazılıyor; listede OLMAYAN değer (ör.
      "Sigorta Şirketi") yazılmıyor ve satır raporunda gerekçesiyle listeleniyor (test).
- [ ] ` ; ` ile çok değer → yazılmaz + rapor (test).
- [ ] İkinci koşu 0 değişiklik (idempotency testi iki alanı kapsıyor).
- [ ] `KART_ALANLARI` + docstring "YAZILAN kart alanları" bölümü güncellendi (mekanik
      kilit testi yeşil).

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-04, gece kuyruğu işçisi)

**Yapılanlar** (`backend/scripts/hukdok_aktarim.py` + yeni test dosyası; G104
deseninin birebir kopyası):

1. `SUTUN_ADAYLARI`na `muvekkil_tipi` → ("Müvekkil Tipi",) ve `hizmet_turu` →
   ("Hizmet Türü",). Tolerans mevcut `_baslik_anahtari`ndan ("MUVEKKIL TIPI" /
   "hizmet türü" de eşleşir, testli). Başlık anahtarı MUVEKKILTIPI ≠ MUVEKKIL:
   taraf sütunu "Müvekkil" ile çapraz bağlanmaz (testli). Başlık teslimde
   YOKSA alan `satir.degerler`e girmez → None sözleşmesi → davranış birebir eski.
2. `MUVEKKIL_TIPI_ESLEMESI` / `HIZMET_TURU_ESLEMESI`: kanonik adların tek
   kaynağı G119 seed sabitleri (`seed_data.CLIENT_TYPES` / `SERVICE_TYPES`,
   literal kopya YOK); anahtar `_baslik_anahtari` — 5 ve 9 ad 5 ve 9 ayrık
   anahtara çözülür (parantezli "Takip (…)" adları da; kilit testte).
3. Dönüştürücüler `_muvekkil_tipi` / `_hizmet_turu`: ortak `_tekil_kapali_liste`
   yardımcısı üzerinden `_kapali_liste_parcalari` (G104) yolu — tanınmayan
   parça `AlanHatasi` (alan yazılmaz, satır raporuna `tur=HATA`, föyün diğer
   alanları işlenir); ` ; ` ile 2+ değer iki alanda da TANIMSIZ → `AlanHatasi`
   (hukumdeki_rol kuralı; KARMA benzeri normalizasyon yok); mükerrer yazım tek
   değer; yer tutucular D5 ortak sözlüğüyle NULL + WARNING.
4. `KART_ALANLARI`na iki kayıt (aynı sınıf: METİN alanlarının varsayılan
   üzerine-yazma sınıfı, İÇERİK moduna girmez). Docstring'in "YAZILAN kart
   alanları" bölümü güncellendi; "Bilinçli YAZILMAYANLAR"daki `service_type`
   kalemine `Hizmet Türü` sütununun ORAYA DEĞİL `hizmet_turu`ya gittiği notu
   eklendi (G119 ayrımı; `service_type` yazılmaya başlanmadı — testle kilitli).

**Kararlar + gerekçeler:**

- `_hukumdeki_rol` gövdesi ortak yardımcıya TAŞINMADI: davranışı aynı olsa da
  G104 kodu/testleri dokunulmadan kalsın diye; yeni iki alan tek yardımcıyı
  paylaşır (kopya iki kez tekrarlanmadı).
- Kardeş föy çelişkisi: iki alan mevcut ön-geçiş mekanizmasına kendiliğinden
  girdi, özel istisna YAZILMADI. Test: aynı kartın iki föyü farklı müvekkil
  tipi anlatınca `celiskiler`e 1 kayıt (kume=KART), alan yazılmaz; uzlaşan
  `hizmet_turu` normal yazılır. Bu gece gerçek paket KOŞULMADI (görev kapsamı
  kod+test) — gerçek koşudaki kardeş çelişki sayısı ilk teslim raporundan
  okunacak; bildirim gereği ("föy başına değişir") yüksek çıkması beklenir.
- "Dokunma" listesi korundu: models.py ve managers/** DEĞİŞMEDİ (seed
  sabitleri yalnız import edildi).

**Test sonuçları (konteynerde):** yeni dosya
`backend/tests/test_g120_aktarim_muvekkil_hizmet.py` — 37 test (dönüştürücü
birimleri 5+9 değer, tanınmayan/çok değer AlanHatasi, sözleşme/kayıt kilitleri,
başlık toleransı ve çapraz bağlanmama, sqlite uçtan uca: eski format dokunmaz,
kuru koşu, "Sigorta Şirketi" raporu, çok değer raporu, idempotency, kardeş
çelişkisi, üzerine-yazma sınıfı). Eski kodda (script değişikliği stash'lenmiş):
**35 failed, 2 passed** — kırmızı-yeşil mekanik olarak tutuyor.
Tam paket: **2435 passed, 3 skipped** (önceki 2398+37; mevcut testlerde
SIFIR değişiklik); ruff `All checks passed!`; mypy `Success: no issues found
in 53 source files`. Üç kapı da 1. turda yeşil.

**Kabul kriterleri:** 6/6 karşılandı.

**İzlenecekler:**

- NOT: `services/teslim_cevap.py::HAVUZ_LISTE_ESLEMESI`ne yeni iki liste
  EKLENMEDİ (G119 notu; kapsam dışı — bildirim sütunları `Sheet`te föy
  düzeyinde, havuz sayfasında değil; gerekirse gündüz kararı).
- İlk gerçek partide satır raporundaki `yazılmadı` kayıtları değer-havuzu
  ölçümü olarak okunmalı (bildirim "kapalı liste, tamamı dolu" diyor —
  sıfır beklenir); kardeş çelişki sayısı da aynı rapordan.
- NOT: Çalışma ağacında oturum başından beri kirli olan
  `.claude/settings.local.json` ve `.claude/launch.json` (harness dosyaları,
  kod değil) commit'e ALINMADI, dokunulmadı.
