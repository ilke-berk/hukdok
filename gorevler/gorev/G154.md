# G154 — Cevaplı xlsx ile 20 föyü bağlama: `--kart-esleme` seçeneği + ekip cevabını okuyan script

- **Bant:** backend
- **Bağımlı:** G153 (aynı `hukdok_aktarim.py` çözümleme bölümü)
- **Dosya kapsamı:** backend/scripts/hukdok_aktarim.py (yalnız `_kart_coz` giriş noktası: dışarıdan verilen
  `SistemNo → tracking_no` haritası önce denenir), backend/scripts/cevapli_kart_eslemesi.py (YENİ: xlsx → CSV harita),
  backend/tests/test_g154_cevapli_esleme.py (YENİ)
- **Dokunma:** backend/scripts/kartsiz_foy_kart_ac.py, backend/scripts/mukerrer_kart_birlestir.py, frontend/**

## Hedef

Plan 08.09 §1.3 **M1**. Ekip `HUKDOK_MUKERRER_VE_YENI_KARTLAR_20260905_CEVAPLI.xlsx` (masaüstü
`veri-ekibi-cevap-2026-09-06/`) `MUKERRER_ESLESME` sayfasında 29 grubun `CEVABINIZ (hangi kart / not)` sütununu
doldurdu: 27 satırda kart no (ör. `S3.AXA........2915.IDARE.00000`), H-6589 "BAĞLAMAYIN — önce bizde düzeltilecek",
id-1012 "MÜVEKKİL: Quick Sigorta A.Ş." (öneri yoktu; kök 2 → #786 Quick kartı). Bu 20 föy bugüne kadar hiçbir karta
yazılmadı (rapor 06.09 §2.1). G153'ün kök kuralı çoğunu kendiliğinden çözer; kalanlar ve ekip cevabı açık harita
olarak aktarıma verilir.

## Yapılacaklar

1. `scripts/cevapli_kart_eslemesi.py`: xlsx'i okur (`Satır türü` = "FÖY (paket)" satırları), `CEVABINIZ`'daki kart
   numarasını (`tracking_no` biçimi, regex) ya da "MÜVEKKİL: <ad>" notunu ayrıştırır; "BAĞLAMAYIN" → atla ve raporla.
   Çıktı CSV `sistem_no,tracking_no,kaynak_not`. "MÜVEKKİL:" satırı için aday kartlar arasında CLIENT anahtarı eşleşen
   TEK kart varsa onun `tracking_no`su, değilse boş + uyarı. Salt okunur.
2. `hukdok_aktarim.py`: `--kart-esleme <csv>` (CLI) ve `aktarimi_kos(kart_eslemesi=...)`: `_kart_coz` önce bu
   haritaya bakar; `tracking_no` DB'de yoksa satır hata (rapor). Harita yalnız verilen SistemNo'ları etkiler.
3. Testler: xlsx ayrıştırma (3 desen), harita önceliği, bilinmeyen kart no hata, BAĞLAMAYIN atlanır.
4. Lokal koşu (raporda): CSV üret → 04.09 paketi `--kart-esleme` ile kuru koşu → 19 föy bağlanır (H-6589 hariç),
   ikinci koşu 0, belge envanteri denk. Prod'da aynı adım paket uygulamasından sonra (insan).

## Kabul kriterleri

- [x] CSV üretimi 3 deseni ayrıştırır, BAĞLAMAYIN atlanır (test).
- [x] `--kart-esleme` haritası `_kart_coz`'da önceliklidir, bilinmeyen kart hata (test).
- [x] Lokal kuru koşu: 19 föy bağlandı, H-6589 bağlanmadı; ikinci koşu 0 (rapor).
- [x] ruff + mypy temiz; pytest özet satırı.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-08)

### Yapılanlar

1. **`scripts/cevapli_kart_eslemesi.py` (YENİ, salt okunur):** `MUKERRER_ESLESME` sayfasını okur (başlıklar
   `_baslik_anahtari` ile aksan/boşluk duyarsız; zorunlu: Satır türü / SistemNo / CEVABINIZ), "FÖY (paket)" satırını
   izleyen "KART (HukuDok)" satırlarını grubun adayları sayar (Grup sütunu varsa onunla). `cevabi_coz` üç deseni ayırır:
   **kart no** (`TRACKING_NO_DESENI`: `S3.AXA........2915.IDARE.00000`, `…HUKUK.00000-2`; hücrede not olsa da ilk numara),
   **`MÜVEKKİL: <ad>`** (`muvekkille_kart_sec`: önce müvekkili YALNIZ o ad olan kart — G153'ün `_kokun_karti_mi` ilkesi,
   ikiz hekim kartı sigortayı ortak müvekkil olarak taşır —, tek değilse adı içeren kartlar; sigorta adları
   `_sigorta_markasi` ile karşılaştırılır: "Quick" = "Quıck"), **BAĞLAMAYIN** (aksansız/küçük harf de). Çıktı CSV
   `sistem_no,tracking_no,kaynak_not` (',' ayraç, UTF-8 BOM); BAĞLAMAYIN CSV'ye girmez, özette listelenir; boş/tanınmayan
   cevap boş `tracking_no` ile düşer (insan gözden geçirsin). `kaynak_not` öneriyle karşılaştırır ("öneriyle aynı" /
   "öneri farklıydı: X" / "öneri yoktu" / "grubun aday kartları dışında" + WARNING).
2. **`scripts/hukdok_aktarim.py`:** `kart_eslemesini_oku(csv)` (boş kart no WARNING ile atlanır; aynı SistemNo iki farklı
   karta → `AktarimHatasi`, koşu başlamaz; başlık/dosya eksik → `AktarimHatasi`), `_eslemeden_kart` (tracking_no →
   silinmemiş kart; yoksa `SatirHatasi` "--kart-esleme: … DB'de yok ya da silinmiş" — kart yaratılmaz, köprüye düşülmez),
   `_kart_coz` sırası artık **harita → föy kaydı → Dosya No köprüsü**; ön geçiş ikizi `_kart_id_tahmini` de haritayı
   görür (bilinmeyen kartta None). `aktarimi_kos(kart_eslemesi=…)`, `_satiri_isle`/`kart_alan_celiskileri` geçişi, CLI
   `--kart-esleme <csv>`, modül docstring'ine bölüm. Teslim hattı (`teslim_kutusu`) imzası değişmedi (keyword, varsayılan
   None).
3. **`tests/test_g154_cevapli_esleme.py` (YENİ, 22 test):** `cevabi_coz` 10 param (kart no, `-2` eki + not, MÜVEKKİL tek
   aday/id-1012 deseni, hekim kendi kartı, aday yok, iki aday, BAĞLAMAYIN iki yazım, boş, tanınmayan), kaynak_not/öneri,
   xlsx → CSV → `kart_eslemesini_oku` uçtan uca (H-6589 CSV'de yok), sayfa/sütun kilitleri, cevaplı CLI; CSV okuyucu (boş,
   tekrar, çelişen, başlıksız, yok); sqlite: haritasız belirsiz → HATA, haritayla İKİNCİ karta + haritasız satır köprüden;
   bilinmeyen + silinmiş kart HATA (föy yazılmaz, ötekiler işlenir); harita föy kaydından önce (föy taşınır); ön geçiş
   haritalı föyleri uzlaşıya katar (çelişen alan yazılmaz); çelişki kapısı haritadan önce; ikinci koşu 0 (tarihçe dahil);
   aktarım CLI `--kart-esleme` + yok dosya `CIKIS_GIRDI`.

### Kararlar

- **Harita ön geçişe de girer** (görev "yalnız `_kart_coz`" dedi, ikiz de aynı bölümde): ikiz haritayı görmeseydi
  haritayla bağlanan föy kardeş-föy uzlaşısına katılmaz, satır satır yazımda son föy kazanır ve ikinci koşu salınırdı
  (G064 dersi). Test `test_on_gecis_haritali_foyleri_uzlasiya_katar`.
- **Harita föy kaydından da önce:** ekibin cevabı yanlış karta bağlı föyü doğru karta taşıyabilmeli;
  `foy_map._apply_update` bunu zaten WARNING'li yapar, yeni yol açılmadı.
- **Kök/müvekkil çelişkisi kapısı haritadan ÖNCE kalır** (H-6589: harita verilse de yazılmaz). Paketin kendi içinde
  çelişen satır ekibin "önce bizde düzeltilecek" sınıfıdır; cevaplı script zaten BAĞLAMAYIN'ı CSV'ye almaz. Ekip bir
  çelişkili satır için kart verirse önce paket düzelmeli — rapor bunu HATA olarak gösterir.
- **MÜVEKKİL çözümü xlsx'in KART satırlarından** (DB'ye bakmaz): script salt okunur ve sentetik test edilebilir kalır; kart
  numarasının DB doğrulaması aktarımda (`_eslemeden_kart`).
- Aynı karta tekrar yazım (28 satırın 9'u zaten G127 ile bağlıydı) zararsız: CSV'de tutuldu, ekibin teyidi haritada
  görünür; ikinci koşu bunu 0 sayar.

### Test sonuçları

- Konteynerde: **pytest 2977 passed, 3 skipped** (önce 2955) · **ruff temiz** · **mypy temiz (61 dosya)**.
- Kırmızı-yeşil: eski kodda `scripts.cevapli_kart_eslemesi` modülü ve `kart_eslemesini_oku` yok (dosya ImportError);
  `aktarimi_kos(kart_eslemesi=…)` TypeError.
- Mevcut test DEĞİŞTİRİLMEDİ.

### Lokal koşu (cevaplı xlsx 06.09 + 04.09 paketi, lokal DB, `--kesim-tarihi 30.07.2026`)

CSV üretimi: 29 föy satırı → **27 kart no + 1 müvekkil adıyla (id-1012 → `S4.QUICK......0060.IDARE.00000` #786, ekibin ve
G153 kök adımının seçtiği kart) + 1 BAĞLAMAYIN (H-6589, CSV'de yok)**; 27'sinin tamamı "öneriyle aynı", 0 çözülemedi.

| | haritasız (HEAD 98fdbac) | `--kart-esleme` |
| --- | --- | --- |
| yeni föy | 9 | **19** |
| satır hatası | 20 (10 belirsiz + 7 çelişki + 3 boş DosyaNo) | **10** (7 çelişki + 3 boş DosyaNo; belirsiz **0**) |
| alan değişikliği / avukat / taraf / aşama | 127 (9 kart) / 20 / 4 / 13 | 227 (17 kart) / 28 / 6 / 24 |
| föy↔müvekkil bağı | 8.385 | 8.395 |
| harita föylerinde HATA | — | **0** (28/28) |
| H-6589 | çelişki HATA | çelişki HATA (bağlanmadı) |
| belge envanteri | DENK | DENK |

**İkinci koşu 0:** dış transaction içinde (`join_transaction_mode="create_savepoint"`, sonunda ROLLBACK — lokal DB'ye
yazılmadı, doğrulandı: 8.386 föy, id-14271 hâlâ bağsız, tarihçe 112.171) iki YAZMA koşusu: 1. koşu haritalı kuru koşuyla
birebir (19 yeni föy), 2. koşu **yeni föy 0 · alan 0 · avukat 0 · taraf 0 · bağ 0 · aşama 0**, envanter DENK. Kalan 7
çelişki + 3 boş DosyaNo her koşuda yeniden raporlanır (tasarım).

### İzlenecekler

- **Prod uygulaması (insan):** paket uygulamasından SONRA `cevapli_kart_eslemesi.py --input <CEVAPLI.xlsx> --output
  kart_eslemesi.csv` → `hukdok_aktarim.py --input <paket> --kart-esleme kart_eslemesi.csv --dry-run` → apply. Lokal DB'ye
  YAZILMADI (kuru koşu + geri alınan transaction).
- H-6589 ekibin paket düzeltmesini bekler; Corpus ×4 / Ergo / DN-11927 çelişkileri G153 izlenecekleriyle aynı (ekibe soru).
- `docs/mimari/veri-teslim-hatti.md` / `dava-acma-akisi.md`ye `--kart-esleme` ve cevaplı script anlatımı (docs kapsam
  dışıydı, dokunulmadı).
- NOT (kapsam dışı, dokunulmadı): `.claude/settings.local.json` (M) ve `.claude/launch.json` (??) oturum başında kirliydi
  — harness dosyaları, commit'e girmedi.
