# G145 — Rapor kataloğu: seçenek sayıları + sıklık sırası (sabit listeler dahil) + kolon boş sayısı

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/services/rapor/registry.py (`secenekleri_getir` → sayılı sürüm, `bos_sayilari`,
  `_kolon_katalogu`), backend/routes/reports.py (yalnız önbellek gerekiyorsa), backend/services/rapor/asistan.py
  (yalnız `katalog_metni` sayıları gömmesin — test), backend/tests/test_g145_rapor_secenek_sayilari.py, mevcut
  testler (aşağıdaki izin)
- **Dokunma:** backend/models.py, backend/database.py (migrasyon YOK), motor.py (filtre semantiği değişmez),
  frontend/**

## Test değiştirme İZNİ (baştan)

Katalog şekli genişliyor ve `secenekler` sırası değişiyor (sabit listelerde artık sıklık sırası). **Değiştirmek
İZİNLİ:** `tests/test_g130_rapor_temeli.py` (`test_katalog_sekli` anahtar kümesi; seçenek sırası bekleyen
iddialar), `tests/test_g137_rapor_katalog_genisleme.py` ve `tests/test_g141_rapor_veriden_liste.py` (seçenek
sırası/şekli iddiaları — sıra artık sayıya göre; `oneriler` davranışı değişmez), `tests/test_g132_rapor_asistani.py`
(yalnız katalog metni testleri). **Kilit:** 403/422 yolları, tenant/soft-delete, `in` içinde `null` semantiği,
export/koşu logu testleri. `def test_`/`assert` sayısı net azalmaz; raporda "değiştirilen test → gerekçe".

## Hedef

Plan §7.2: kullanıcı seçeneklerin yanında kayıt sayısını görmek, listeleri kayıt sayısına göre sıralı bulmak
ve boş kayıtların kaçta kaç olduğunu bilmek istiyor. Sıfır kayıtlı sabit seçenekler listede KALIR (ileride
veri dolar), sonda. Backend bunları katalogda verir; sözleşme dışı hiçbir şey değişmez.

## Yapılacaklar

1. **`secenek_sayilari`:** `secenekler` dolu her kolonda (`liste` tipi sabit/tablo kaynaklı VE `veriden_liste`)
   `{deger: n}`; sabit liste için kaynağın `kisitlar(tenant_id)` ile `GROUP BY kolon` (tek sorgu, eşleşmeyen
   sabit değer `0`); veriden listede G141'in zaten hesapladığı sayı. Türetilmiş `liste` kolonlarda (ör.
   `muvekkil_kategorisi`, `secenek_ifadesi` olanlar) sayı **hesaplanmaz → `null`** (EXISTS başına GROUP BY
   pahalı; raporda not). `secenekler` sırası: sayıya göre azalan; eşitlikte sabit listenin kendi sırası
   (veriden: alfabetik); `0`'lar sonda.
2. **`bos_sayisi`:** filtrelenebilir, `is_null` izinli, türetilmiş OLMAYAN her kolonda; kaynak başına TEK sorgu:
   `select(*(func.sum(case((kosul, 1), else_=0)).label(k) for k …)).where(*kisitlar)` — metin kolonda
   `IS NULL OR TRIM(col) = ''`, diğerlerinde `IS NULL`. Türetilmişte `null`.
3. **Katalog:** `_kolon_katalogu` iki alanı ekler; `katalog()` kaynağın boş sayılarını bir kez hesaplayıp
   kolonlara dağıtır. Önbellek (60 sn, tenant) değişmez; `katalog_onbellegini_sifirla` testlerde.
4. **Ölçüm (raporda):** lokal DB'de (14.5k dava / 2k müvekkil / 229 belge / 8.4k föy) `katalog()` süresi önbelleksiz
   — hedef < 300 ms; aşarsa `bos_sayisi` yalnız hızlı filtre + `bos_anahtari` kolonlarına daraltılır ve plan §7.2'ye
   şerh düşülür.
5. **Asistan:** `katalog_metni` sayıları gömmez (test: metin uzunluğu değişmez).
6. **Testler:** sabit listede sayı + sıra + sıfırlılar sonda (Durum: DERDEST 2, KARAR 1, diğerleri 0); tenant/
   silinmiş kayıt sayılmaz; veriden listede sayı = G141 sayısı; türetilmiş liste kolonda `null`; `bos_sayisi`
   metin boş string dahil, tarih NULL, türetilmişte `null`; kaynak başına tek boş-sayısı sorgusu (sorgu sayacı
   ya da `echo` yakalama); katalog anahtar kümesi güncel; önbellek ikinci çağrıda sorgu koşturmaz.

## Kabul kriterleri

- [ ] Her `secenekler` dolu kolonda `secenek_sayilari` (türetilmişte `null`), sıra sayıya göre azalan, sıfırlılar
      sonda ve listede (test).
- [ ] `bos_sayisi` metin/tarih/sayı/mantık kolonlarında doğru; metin boş string boş sayılır; türetilmişte `null`
      (test).
- [ ] Kaynak başına boş sayıları TEK sorguda (test); katalog süresi lokalde < 300 ms (raporda ölçüm).
- [ ] Asistan katalog metni değişmedi (test); `/preview`/`/export` gövdesi ve DB değişmedi.
- [ ] ruff + mypy temiz; tam paket yeşil.

## Doğrulama

- `docker compose exec -T backend python -m pytest tests/test_g145_rapor_secenek_sayilari.py tests/test_g141_rapor_veriden_liste.py tests/test_g137_rapor_katalog_genisleme.py tests/test_g130_rapor_temeli.py`
- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-07, işçi oturumu)

### Yapılanlar

- `services/rapor/registry.py`:
  - `liste_sayilari(kaynak, db, tenant_id)` — kaynağın düz `liste` kolonlarında veride görülen değerlerin
    sayıları, kaynak başına **TEK** sorgu (kolon başına `GROUP BY` parçaları `UNION ALL` ile birleşik; tenant +
    soft-delete kısıtı, NULL/"" hariç). `secenekleri_sayili_getir(kaynak, kolon, db, tenant_id, veri_sayilari)`
    sabit çekirdek + referans tablosu + bu sayıları birleştirir; sıra `_sayiya_gore_sirala` (sayıya göre azalan,
    kararlı → eşitlikte sabit/tablo/alfabetik sıra korunur, sıfırlılar sonda ve listede). Bu GROUP BY, G130'un
    DISTINCT katmanının yerine geçti — veride görülen ek değerler artık tenant + soft-delete kurallı (K2).
  - `veriden_secenekleri_getir(..., sayili=True)` (overload) aynı G141 sorgusunun `(deger, sayi)` çiftlerini
    verir; katalog `secenek_sayilari`ni ek sorgu olmadan buradan alır; `sayili=False` davranışı aynen.
  - `bos_sayilari(kaynak, db, tenant_id)` — filtrelenebilir + `is_null` izinli + türetilmiş olmayan her kolon
    için `COUNT(*) FILTER (WHERE …)` tek sorgu; metin/liste kolonda `IS NULL OR TRIM(col) = ''`, diğerlerinde
    `IS NULL`; satırsız kaynakta `0`.
  - `_kolon_katalogu` iki alan ekler: `secenek_sayilari` (`{deger: n}` | `null`) ve `bos_sayisi` (`int` | `null`);
    `_kaynak_katalogu` boş ve liste sayılarını kaynak başına bir kez hesaplayıp dağıtır. Türetilmiş liste
    (`muvekkil_kategorisi`) ve türetilmiş kolonlarda her ikisi `null`.
  - `_kolonu_denetle`: düz liste kolonun DB tipi metin olmalı (UNION ALL tip uyumu) — import anında.
- `routes/reports.py`: DEĞİŞMEDİ — 60 sn tenant anahtarlı önbellek yeni sorguları da kapsıyor (test).
- `services/rapor/asistan.py`: DEĞİŞMEDİ — `katalog_metni` `secenekleri_getir(db=None)` yolunu kullanıyor,
  sayı gömmez (test).
- `docs/plan/raporlama-plani-2026-09-06.md` §7.2: G145 şerhi (türetilmiş listede `null`, K2 kuralı, ölçüm).

### Kararlar + gerekçe

- **Kaynak başına tek UNION ALL (kolon başına GROUP BY yerine):** ilk sürüm kolon başına ayrı GROUP BY ile
  katalog medyanı 306-309 ms çıktı (hedef < 300). Ölçüm (davalar, 18 liste kolonu): ayrı GROUP BY ×18 = 95 ms,
  UNION ALL tek sorgu = 42 ms, Postgres GROUPING SETS = 23 ms. GROUPING SETS sqlite'ta yok (testler sqlite) →
  dialect-bağımsız UNION ALL seçildi; iki motorda aynı kod yolu.
- **`COUNT(*) FILTER` (SUM(CASE) yerine):** davalar boş sayıları 44 → 34 ms; sqlite ≥3.30 destekler
  (konteyner 3.46.1). Plan §7.2'deki "sqlite'ta SUM(CASE)" notu bu yüzden gereksizleşti (şerhte belirtildi).
- **Türetilmiş listede sayı yok:** `muvekkil_kategorisi` EXISTS(case_parties JOIN clients) başına GROUP BY
  pahalı; görev tanımı gereği `null`, sıra sabit. Plan §7.2 "sabit ya da veriden" diyordu → şerh düşüldü
  (G146 frontend `null`u ele almalı — plan tipi zaten `| null`).
- **Boş koşulunda TRIM:** katalog sayısı "NULL ya da boş/boşluk" anlamında; motorun `is_null` filtresi
  yalnız `IS NULL` (filtre semantiği değişmedi — motor.py dokunulmadı). Docstring'de açık.
- **DISTINCT katmanı tenant kurallı oldu:** sayım sorgusu değerleri de getirdiği için ayrı DISTINCT sorgusu
  kaldırıldı; sonuç: silinmiş/başka tenant kaydının değeri artık listeye girmiyor (veriden liste ve öneri
  katmanıyla tutarlı, K2). Lokal/prod'da tüm kayıtlar `tenant_id=NULL` → pratik fark yok.

### Ölçüm (lokal DB: 14.555 dava / 1.998 müvekkil / 229 belge / 8.386 föy, Postgres konteyner)

- `registry.katalog()` önbelleksiz, 9 koşu: `[394, 242, 242, 238, 252, 255, 248, 238, 240]` ms —
  **medyan 242 ms** (ilk koşu soğuk 394). Taban (HEAD, G145 öncesi): 304/192/182 ms → ~185 ms.
- Kaynak başına: davalar liste_sayilari 42 ms (18 kolon) + bos_sayilari 34 ms; foyler 36 + 17; muvekkiller 3 + 8;
  belgeler 4 + 5. Kalan ~100 ms G141 veriden GROUP BY + G137 öneri DISTINCT'leri (değişmedi).
- Hedef < 300 ms KARŞILANDI; `bos_sayisi` daraltmasına gerek kalmadı.

### Testler

- Yeni `tests/test_g145_rapor_secenek_sayilari.py` — 14 test: sabit liste sayı/sıra/sıfırlılar sonda, tenant
  (T1/T2) + silinmiş kayıt sayılmaz, tablo katmanı (aktif 0'lı listede, pasif yok, silinmiş davanın değeri yok),
  `client_type` sayıları + etiketler, veriden liste sayısı = G141 `sayili=True` çiftleri, türetilmiş listede
  `null`, `secenek_sayilari` yalnız seçenekli kolonlarda ve azalan, `bos_sayisi` metin("" + boşluk)/liste/tarih/
  para/mantık(NULL, False değil)/id + türetilmişte `null`, yalnız filtrelenebilir+is_null+düz kolonlarda,
  kaynak başına tek FILTER + tek UNION ALL sorgusu (`before_cursor_execute` sayacı; davalar 18 GROUP BY),
  önbellekte ikinci çağrı 0 sorgu, `bos_sayilari` doğrudan (satırsız kaynak 0, db=None `{}`), denetim (metin
  dışı DB tipli düz liste ValueError), asistan metni sayı gömmez ve DB verisiyle değişmez, `/preview` gövdesi aynı.
- **Değiştirilen testler (izin kapsamında):** `tests/test_g130_rapor_temeli.py::test_katalog_sekli` — kolon
  anahtar kümesine `secenek_sayilari` + `bos_sayisi` eklendi (şekil genişledi). `::test_katalog_secenekler_tablo_ve_distinct_katmani` —
  `durum[:2] == ["DANIŞ","DERDEST"]` iddiası sıklık sırasıyla çelişti; T1 görünümünde KARAR (c2 legacy) ve
  OZEL_DURUM (c1) 1'er → `durum[:2] == ["KARAR","OZEL_DURUM"] and durum[2:4] == ["DANIŞ","DERDEST"]` (sıfırlılar
  sonda, kendi sırasıyla). `def test_`/`assert` sayısı azalmadı. G137/G141/G132 testleri DEĞİŞMEDİ (veriden
  sıra zaten sayıya göreydi; `client_type` sırası Individual 2 / Corporate 2 / Gerçek Kişi 1 ile aynı kaldı).
- Koşular (konteyner): görev seti 156 passed; tam paket **2726 passed, 3 skipped**; ruff temiz; mypy temiz
  (61 dosya).

### İzlenecekler / NOT

- NOT: `bos_sayisi` TRIM'li boş sayarken motor `is_null` filtresi yalnız NULL'u süzer — frontend "Boş" çipi
  `is_null` gönderdiğinde satır sayısı rozetten küçük çıkabilir (boş string kayıtlar). Kapsam dışı (motor.py
  dokunulmadı); istenirse `is_null` metin semantiğini genişletmek ayrı görev.
- NOT: `muvekkil_kategorisi` için sayı yok — G146 frontend `secenek_sayilari=null`da rozetsiz göstermeli.
- Katalog süresi lokal ölçümdür; prod (aynı veri, e2-medium) benzer beklenir; ilk soğuk çağrı ~400 ms.
