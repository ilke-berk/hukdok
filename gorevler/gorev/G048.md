# G048 — Frontend: `Uzmanlık Alanı` + 11 yeni alanın arayüz karşılığı

- **Bant:** frontend
- **Bağımlı:** G044
- **Dosya kapsamı:** frontend/src/pages/**, frontend/src/components/**, frontend/src/types/**, frontend/src/lib/**
- **Dokunma:** backend/** (DOKUNMA), .github/workflows/ci.yml (DOKUNMA)

## Hedef

G044 backend tarafında 11 kolonu açıp `Dava Türü Alt Kırılımı` → `Uzmanlık Alanı`
adlandırmasını yapıyor. Bu görev arayüz karşılığını kuruyor: alanlar dava kartında
**görünmezse** aktarımla gelen veri kullanıcıya ulaşmaz.

## Kabul kriterleri

- [ ] `Dava Türü Alt Kırılımı` → **`Uzmanlık Alanı`** her göründüğü yerde değişti
      (form etiketleri, tablo başlıkları, filtre adları, dışa aktarma başlıkları).
      Tarama `grep` ile yapılır, tek tek gözle değil
- [ ] 11 yeni alan dava kartında görünüyor; boş olanlar kartı kirletmiyor
      (mevcut boş-alan gösterim deseni neyse aynısı — yeni desen İCAT ETME)
- [ ] `iddia_edilen_kusur` ve `istinaf_basvuran_taraf` **kapalı liste** olarak
      render ediliyor (serbest metin DEĞİL); değerler backend'den geliyor, frontend'de
      sabit liste tutulmuyor
- [ ] Beş tıbbi alan (`tibbi_surec`, `tibbi_olay`, `iddia_edilen_kusur`,
      `hastada_olusan_zarar`, `uygulanan_yontem`) mantıklı bir grupta toplandı —
      dava kartına dağıtılmadı
- [ ] `npx tsc --noEmit -p tsconfig.app.json` **0 hata** (çıkış kodu ayrıca ölçülür —
      çıplak `npx tsc --noEmit` bu repoda hiçbir şey denetlemez, G037 dersi)
- [ ] `npm --prefix frontend test` sayısı düşmedi (317 taban)
- [ ] `npm run lint` temiz, `npm run build` geçiyor
- [ ] **Sayfa tam genişlik kuralı korundu** — redesign sayfaları `max-w` cap kullanmaz,
      sağda boşluk kalmaz

## Doğrulama

- `npx --prefix frontend tsc --noEmit -p frontend/tsconfig.app.json`
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npm --prefix frontend run build`

## Rapor

(işçi oturumu doldurur)
