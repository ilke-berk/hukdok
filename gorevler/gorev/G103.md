# G103 — Belgeleme olayı alanları: iki kolon + iki kapalı liste + yazma/okuma yolu

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/models.py, backend/database.py, backend/schemas.py,
  backend/managers/reference_lists.py, backend/managers/seed_data.py,
  backend/managers/config_manager.py, backend/managers/case_manager.py,
  backend/routes/config.py, backend/routes/cases.py, backend/tests/**
- **Dokunma:** backend/required_fields.py (alanlar zorunlu YAPILMAZ — dosyaya dokunma),
  backend/scripts/hukdok_aktarim.py (G104'ün işi — dokunma)

## Hedef

Veri ekibinin 25.08 ölçümü (HUKDOK_BELGELEME_OLAYI_BULGUSU_2026-08-25.md): bağlı föylerin
~%14'ünde tazminatın kaynağı tıbbi olay değil **belgeleme olayı** (aydınlatma ihlali /
tıbbi kayıt eksikliği); 45 dosyada "kusur yok ama tazminat var" görünümü doğdu. Ayrıca aynı
olgu yargı kademesine göre rol değiştiriyor ("saptandı" ≠ "kazandırdı"). Kullanıcı kararı
(02.09): iki alan açılır, prensiplere en uygun yolla — kapalı liste mekanizmasının kopyası,
zorunluluk yok, tahmin yazılmaz.

Bu görev şema + backend yollarını kurar: `cases`e iki kolon, iki seed'li kapalı referans
listesi, config uçları, kart okuma/yazma yollarında eş davranış, liste filtresi parametresi.

## SÖZLEŞME (dondurulmuş — G105 ile ORTAK; değiştirilemez, sapma gerekiyorsa BLOKE bırak)

- Kolonlar: `cases.olay_turu` VARCHAR(100) NULL, `cases.hukumdeki_rol` VARCHAR(100) NULL.
  Ad denormalize taşınır (iddia_edilen_kusur ile aynı desen). NULL = "karar okunmadı" —
  meşru durumdur, backfill YOK.
- Liste 1 — `event_types` (Olay Türleri), model `EventType`, seed'li 3 değer:
  `TIBBI` → "Tıbbi Olay" · `BELGELEME` → "Belgeleme Olayı" · `KARMA` → "Tıbbi + Belgeleme".
  KARMA gerekçesi: ölçümün "yan gerekçe" sınıfında iki tür birlikte görülüyor; kart alanı
  tek slot olduğundan karma durum açık değerle taşınır, tahminle tekilleştirilmez.
- Liste 2 — `judgment_roles` (Hükümdeki Roller), model `JudgmentRole`, seed'li 4 değer:
  `TEK-GEREKCE` → "Tek Gerekçe" · `YAN-GEREKCE` → "Yan Gerekçe" ·
  `YALNIZ-SAPTAMA` → "Yalnız Saptama" · `REDDEDILMIS-IDDIA` → "Reddedilmiş İddia".
  Anlam: belgeleme olgusunun **güncel kademedeki** hükümde oynadığı rol (E-9/bayat hüküm
  kuralıyla uyumlu; kademe değişince değer düzeltme partisiyle güncellenir).
- Uçlar: `GET/POST/DELETE /api/config/event_types` ve `/api/config/judgment_roles`
  (alleged_faults kalıbının birebir kopyası, POST/DELETE admin).
- Liste filtresi: `get_cases(..., olay_turu: str = None)` — `file_type` kalıbıyla eşitlik
  filtresi; `routes/cases.py` liste ucuna aynı adla query param.

## Yapılacaklar

1. **Modeller:** `EventType` + `JudgmentRole` — `AppealingParty`nin birebir kopyası
   (code unique, name, active, sequence). `cases`e iki kolon (`models.py:133` tasnif
   bloğunun yanına, yorumuyla).
2. **Migration:** `database._MIGRATIONS`a iki kolon `("columns", ...)` op'u (mevcut tasnif
   kolonları sözlüğü `database.py:649` civarındaki desenle). Tablolar `create_all` ile
   doğar; kalıcı kısıt/index GEREKMİYOR (unique code model tanımında — appealing_parties
   emsali). G041 kuralı: kısıt eklemek gerekirse DAİMA ayrı `("index", ...)` op'u.
3. **Seed:** `seed_data.py`ya iki seed fonksiyonu, satır başına `_ekle_yarissiz`
   (G058 dersi — worker yarışı ERROR basmamalı). Kod ASCII ve değişmez kimlik.
   NOT: `alleged_faults` seed'lenmez (bilinçli, ellemeden bırak); bu iki liste seed'lenir
   çünkü değerleri karşı taraf teyidi beklemiyor — 25.08 belgesi + 02.09 kullanıcı kararı
   ile bizde sabitlendi.
4. **Referans listesi kaydı:** `reference_lists.py` LIST_REGISTRY + DEPENDENCIES
   (`event_types` → Case.olay_turu "dava", `judgment_roles` → Case.hukumdeki_rol "dava")
   + görünen ad haritası; `config_manager.py` cache alanları; `routes/config.py` uçları —
   hepsi alleged_faults satırlarının ikizi.
5. **Kart yolları — eş davranış kuralı:** `grep -rn "iddia_edilen_kusur" backend/`
   çıktısındaki her dosyada (hukdok_aktarim.py HARİÇ) iki yeni alan eş muamele görür:
   `schemas.py` (Optional[str], ~:394 bloğu), `case_manager.py` serializer(lar)ı
   (~:454), kart güncelleme yolu. Güncelleme yolunda kapalı liste doğrulaması G066
   deseniyle: değer listede yoksa RET (400'e çıkan yol), liste BOŞSA WARNING'le geç.
6. **Liste filtresi:** `get_cases`e `olay_turu` parametresi (case_manager.py:729
   file_type kalıbı) + route query param + `X-Total-Count` davranışı bozulmaz.

## Kabul kriterleri

- [ ] İki kolon migration'la geliyor; migrasyon yolu testi (gerçek Postgres, üç-ortam
      kuralı: `to_regclass` yoksa SKIP) iki kolonu ve iki tabloyu doğruluyor; ikinci
      koşu idempotent (0 değişiklik).
- [ ] Seed: temiz DB'de 3+4 satır; ikinci açılışta 0 ekleme, 0 ERROR (log sözleşmesi).
- [ ] `GET /api/config/event_types` → 3 kayıt sıralı; `judgment_roles` → 4 kayıt;
      POST/DELETE admin kapılı (alleged_faults test kalıbı).
- [ ] Kart güncelleme: listede olan değer yazılıyor; olmayan değer reddediliyor (test);
      liste boşaltılınca WARNING'le geçiyor (G066 davranış eşliği).
- [ ] `get_case` çıktısında iki alan var; `get_cases(olay_turu="Belgeleme Olayı")`
      yalnız o değerli kartları döndürüyor (testli, tenant filtresiyle birlikte).
- [ ] required_fields.py DEĞİŞMEDİ; iki alan hiçbir bağlamda zorunlu değil.
- [ ] Doküman: docs/mimari'de dava şemasını anlatan ilgili bölüme iki alan + gerekçe
      (25.08 bulgusuna atıf) eklendi; koddan doğrulanmış ifadelerle.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-02)

**Yapılanlar** — sözleşme birebir uygulandı:

1. `models.py`: `cases.olay_turu` + `cases.hukumdeki_rol` (VARCHAR(100) NULL,
   tıbbi analiz bloğunun yanına yorumuyla) + `EventType`/`JudgmentRole` modelleri
   (AppealingParty'nin birebir kopyası, G060 bloğundan sonra).
2. `database.py`: `_MIGRATIONS` madde 38 — iki kolon `("columns", ...)` op'u.
   Tablolar create_all ile doğar (madde 31 yolu), kısıt/index bilinçli yok
   (unique code model tanımında; G042 dersi yorumda).
3. `seed_data.py`: `EVENT_TYPES` (3) + `JUDGMENT_ROLES` (4) sabitleri +
   `_seed_event_types`/`_seed_judgment_roles` → `_seed_karar_listesi` çağrısı
   (jenerik gövde, satır başına `_ekle_yarissiz` SAVEPOINT — G058; fonksiyon adı
   testle kilitli olduğundan yeniden adlandırılmadı, yorumla açıklandı).
   `alleged_faults` ellenmedi (seed'siz kalmaya devam).
4. `reference_lists.py` LIST_REGISTRY + DEPENDENCIES (`olay_turu`/`hukumdeki_rol`
   → "dava") + LIST_TITLES ("Olay Türleri"/"Hükümdeki Roller") + get/add/delete
   sarmalayıcıları; `config_manager.py` iki cache alanı + getter/setter;
   `routes/config.py` GET/POST/DELETE ×2 (alleged_faults kalıbının ikizi,
   POST/DELETE admin).
5. Kart yolları: `schemas.py` CaseRead + CaseTrackingUpdate'e iki Optional[str];
   `case_manager.get_case` serializer'ına iki alan. **Yazma yolu takip paneli
   seçildi** (karar: `iddia_edilen_kusur`ın bugün kullanıcı-yüzlü yazma yolu YOK
   — yalnız aktarım scripti yazıyor; alanlar karar/hüküm bağlamlı ve G066'nın 400
   mekanizması `update_case_tracking`te kurulu): iki alan `TRACKING_FIELDS`e
   girdi, yazım öncesi `_EVENT_LIST_COLUMNS` + `validated_event_list_value`
   kapısından geçer — G066 davranış eşi (liste dışı → `InvalidDecisionStatusError`
   → api.py 400; liste BOŞSA WARNING'le geç; None temizler; active filtresi yok).
   Yeni exception tipi + api.py handler'ı AÇILMADI (api.py kapsam dışı) — mevcut
   `InvalidDecisionStatusError` yeniden kullanıldı, mesaj alan/liste/değer söyler.
6. Liste filtresi: `get_cases(..., olay_turu=None)` file_type kalıbıyla eşitlik
   (docstring'li) + `routes/cases.py` liste ucuna aynı adla query param;
   X-Total-Count davranışı korunmuş (testli).
7. Doküman: `docs/mimari/dava-acma-akisi.md` bölüm 11 (25.08 bulgusuna atıf,
   koddan doğrulanmış ifadelerle).

**Test sonuçları:** yeni dosya `backend/tests/test_g103_belgeleme_olayi.py` —
35 test (şema kilitleri, sözleşme sabitleri, sqlite seed idempotency + 0 ERROR,
silme koruması ItemInUseError, kapı davranışı 2 alan × yaz/ret/temizle + boş
listede WARNING + toptan ret, get_case/get_cases tenant'lı filtre, route
400/403/200 + X-Total-Count, gerçek Postgres'te migrasyon: iki kolon +
to_regclass ile iki tablo + ikinci koşu idempotent — DB yoksa SKIP).
Tam paket konteynerde: **2096 passed, 3 skipped** (önceki 2061+35); ruff temiz;
mypy `Success: no issues found in 51 source files`. Backend restart sonrası
migrasyon canlı lokal DB'ye uygulandı, açılış loglarında 0 ERROR; psql ile
doğrulandı: 2 kolon + 2 tablo + 3/4 seed satırı sıralı ve yazımlar birebir.

**Kabul kriterleri:** 7/7 karşılandı (`required_fields.py` ve
`scripts/hukdok_aktarim.py` dosyalarına dokunulmadı — git diff'te yoklar).

**İzlenecekler:** G105 (frontend) bu sözleşmenin UI bacağını bağlayacak;
`GET /api/config/event_types` cevabı alleged_faults ile aynı biçimde (code/name).
Takip panelinin arayüzü bugün bu iki alanı göndermiyor — G105'e kadar tek yazma
yolu API'dir; alan doluluğu ölçülünce index kararı yeniden değerlendirilir
(G042 kuralı).
