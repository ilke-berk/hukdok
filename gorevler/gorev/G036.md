# G036 — Main dalında canlı 4 tip hatası (sahte tsc kapısının bıraktığı borç)

- **Bant:** frontend
- **Bağımlı:** -
- **Dosya kapsamı:** frontend/src/pages/AdminPage.tsx, frontend/src/pages/CaseDetails.tsx, frontend/src/pages/dashboards/AvukatDashboard.tsx, frontend/src/pages/dashboards/IdariDashboard.tsx, frontend/src/types/**
- **Dokunma:** .github/workflows/ci.yml (**DOKUNMA** — kapı çevirmesi G037'nin işi), frontend/tsconfig*.json (DOKUNMA)

## Hedef

`npx tsc --noEmit` bu repoda **hiçbir şey denetlemiyor**: `frontend/tsconfig.json`
solution-style (`"files": []` + `references`) olduğu için komut yardım metnini basıp
**exit 0** dönüyor. Bu gecenin keşfinde yeniden doğrulandı. Gerçek kapı
`tsc --noEmit -p tsconfig.app.json` ve koşturulduğunda **4 hata** çıkıyor:

```
src/pages/AdminPage.tsx(427,65)              TS2352  ConfigItem → Record<string, unknown>
src/pages/CaseDetails.tsx(561,85)            TS2322  unknown → ReactNode
src/pages/dashboards/AvukatDashboard.tsx(120,24)  TS2352  Record<string, unknown>[] → DashboardCase[]
src/pages/dashboards/IdariDashboard.tsx(99,24)    TS2352  Record<string, unknown>[] → DashboardCase[]
```

Bu hatalar FAZ C'nin ürünü değil — **önceden vardı**, kapı sahte olduğu için görünmüyordu.
G037 kapıyı gerçeğe çevirecek; bu görev önce borcu kapatır (sıra tersine olursa CI kırmızıya düşer).

## Kabul kriterleri

- [ ] `npx tsc --noEmit -p tsconfig.app.json` **0 hata** (çıkış kodu ayrıca doğrulandı —
      pipe'a sokma, `; echo $?` ile ölç)
- [ ] Üç `TS2352` **`as unknown as X` çift-cast ile susturulmadı.** Doğru çözüm tip
      tarafında: `DashboardCase`/`ConfigItem` tiplerine uygun dönüşüm ya da kaynağın
      doğru tiple yazılması. Çift-cast kaçınılmazsa her biri gerekçeli yorumla ve
      rapora sayıyla yazılır
- [ ] `CaseDetails.tsx:561` `unknown → ReactNode`: değer render edilmeden önce daraltıldı
      (`String(...)` ile körlemesine sarma DEĞİL — değerin gerçekte ne olduğu okundu)
- [ ] Davranış değişmedi: `npm --prefix frontend test` sayısı düşmedi (317 taban)
- [ ] `npm --prefix frontend run lint` temiz, `npm --prefix frontend run build` geçiyor
- [ ] `frontend/tsconfig*.json` ve `.github/workflows/ci.yml` **değişmedi** (denetçi ölçer)

## Doğrulama

- `npx --prefix frontend tsc --noEmit -p frontend/tsconfig.app.json`
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npm --prefix frontend run build`

## Rapor

(işçi oturumu doldurur)
