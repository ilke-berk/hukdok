# G108 — Teslim admin uçları + bildirim + özellik anahtarı

- **Bant:** backend
- **Bağımlı:** G107
- **Dosya kapsamı:** backend/routes/admin.py, backend/schemas.py, backend/services/teslim_kutusu.py
  (bildirim yardımcısı eklenir), backend/services/app_settings.py,
  backend/tests/test_g108_teslim_admin_uclari.py (YENİ)
- **Dokunma:** backend/api.py (G109), backend/scripts/hukdok_aktarim.py (dokunma),
  backend/services/notifications.py (yalnız çağır)

## Hedef

Plan §2 "yedek giriş yolu" + §2.3 gündüz işlemleri. Admin paneli için defteri okuyan, dosya
yükleyen, tarama/kuru koşu/uygulama tetikleyen uçlar; eşik dışı ve nihai durumlarda
ADMIN_EMAILS'e uygulama içi bildirim; tüm hattı kapatan tek anahtar.

## SÖZLEŞME (G111 frontend bu sözleşmeye göre PARALEL yazılıyor — DEĞİŞTİRME)

Hepsi `require_admin` kapılı. Tarih alanları ISO 8601 string.

| Uç | Gövde / Parametre | Dönüş |
| --- | --- | --- |
| `GET /api/admin/aktarim/teslimler?limit=50` | — | `{"teslimler": [Teslim…], "esikler": {"hata_orani": 0.02, "eslesmeyen_orani": 0.05, "alan_degisikligi": 10000}, "etkin": bool}` — en yeni önce |
| `GET /api/admin/aktarim/teslimler/{id}` | — | `Teslim` (tam; `durum_gecmisi` dahil) |
| `POST /api/admin/aktarim/teslimler` | multipart `file` (.xlsx, ≤ 50 MB) | `201 {"id": int, "durum": str}` — `teslim_kaydet(kaynak="yukleme")` + `teslimi_isle(otomatik_uygula=False)`; `.xlsx` değilse 400 |
| `POST /api/admin/aktarim/teslimler/{id}/kuru-kos` | — | `{"id", "durum", "kapi_karari", "kapi_gerekcesi"}` — `alindi`/`dogrulandi`/`kuru_kosuldu`/`inceleme_bekliyor` durumlarında yeniden doğrula+kuru koş+kapı; diğer durumda 409 |
| `POST /api/admin/aktarim/teslimler/{id}/uygula` | `{"onay": true}` | `{"id", "durum"}` — `teslim_uygula(uygulayan=<admin e-posta>)`; uygun olmayan durumda 409; `onay` false ise 400 |
| `GET /api/admin/aktarim/teslimler/{id}/raporlar` | — | `{"dosyalar": [{"ad": str, "boyut": int}]}` |
| `GET /api/admin/aktarim/teslimler/{id}/raporlar/{ad}` | — | CSV/TXT indirme (`text/csv; charset=utf-8`); yol dışına çıkış (`..`, `/`) 400 |
| `POST /api/admin/aktarim/tara` | — | `{"yeni": int, "yinelenen": int}` — G109 gelene kadar `{"yeni": 0, "yinelenen": 0, "not": "SharePoint gözcüsü henüz yok"}` döner (uç ŞİMDİ açılır ki frontend sözleşmesi tamam olsun) |

`Teslim` şeması (`schemas.AktarimTeslimiOut`): `id, dosya_adi, sha256, kaynak, durum,
onceki_teslim_adi, zincir_tamam, okunan, islenen, atlanan, hata_sayisi, alan_degisikligi,
kart_degisen, envanter_denk, kapi_karari, kapi_gerekcesi, cevap_yuklendi, uygulayan,
hata_mesaji, created_at, updated_at, done_at` (+ liste dışı: `durum_gecmisi`, `spool_path`).

**Anahtar:** `app_settings.SETTINGS_REGISTRY["veri_teslim_otomasyonu"]` — label "Veri teslim
otomasyonu", varsayılan **False**. Kapalıyken: yükleme ucu çalışır (elle yol), `tara` ve gece
job'ı (G109) hiçbir şey yapmaz, gece otomatik uygulama YAPILMAZ. Elle "Uygula" anahtardan
bağımsız çalışır (admin bilinçli tıklıyor).

**Bildirim** (`teslim_kutusu.bildir(teslim_id, olay)`): alıcılar `routes.config._admin_emails()`
kümesi; `type="veri_teslim"`, `severity`: `inceleme_bekliyor`/`basarisiz`/`reddedildi` →
"warning", `uygulandi` → "info"; `dedupe_key=f"teslim:{id}:{durum}:{email}"` (G082 dersi —
alıcı sonda); `title` örn. "Veri teslimi inceleme bekliyor: HUKDOK_TESLIM_PAKETI_2026-09-10.xlsx";
`body` kapı gerekçesi ya da sayaç özeti. `teslimi_isle` ve `teslim_uygula` nihai/inceleme
durumlarında bunu çağırır; bildirim hatası yutulur (WARNING), akış bozulmaz.

## Kabul kriterleri

- [x] Yedi uç `require_admin` ile kapılı (admin olmayan 403, test).
- [x] Yükleme: sentetik mini xlsx → 201, defterde `kuru_kosuldu` ya da `inceleme_bekliyor`;
      `.txt` → 400; aynı dosya ikinci kez → 201 + `durum="yinelenen"`.
- [x] `kuru-kos` yalnız izin verilen durumlarda; `uygulandi` satırında 409.
- [x] `uygula`: `{"onay": true}` ile `uygulandi`, `uygulayan` = admin e-postası; `onay`
      yoksa 400; `reddedildi` satırında 409.
- [x] Rapor indirme: listelenen ad iniyor; `..%2F` gibi yol 400.
- [x] Anahtar `GET /api/admin/settings` listesinde görünüyor, varsayılan False.
- [x] Bildirim: `inceleme_bekliyor` geçişinde her admin e-postasına bir satır; aynı geçiş
      ikinci kez bildirim ÜRETMİYOR (dedupe testi); `create_notification` patlayınca akış
      devam ediyor (monkeypatch, WARNING).
- [x] `hukdok_aktarim.py` diff'i SIFIR.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-03, işçi oturumu)

### Yapılanlar

- `backend/routes/admin.py`: sözleşmedeki yedi uç (sekiz yol) — liste (`limit` 1–500, en yeni
  önce, `esikler` = `tk.kapi_esikleri()`, `etkin` = anahtar), tek teslim (`durum_gecmisi` +
  `spool_path` dahil), multipart yükleme (`.xlsx` dışı/boş → 400, 50 MB üstü → 413,
  `teslim_kaydet(kaynak="yukleme")` + `teslimi_isle(otomatik_uygula=False)` → 201),
  `kuru-kos` (yalnız `tk.ISLENEBILIR_DURUMLAR`, aksi 409), `uygula` (`onay` şart → aksi 400;
  yalnız `kuru_kosuldu`/`inceleme_bekliyor`, aksi 409; `uygulayan` = admin e-postası),
  rapor listesi (yalnız `.csv`/`.txt`, ad sırasıyla), rapor indirme (`FileResponse`,
  csv → `text/csv; charset=utf-8`, txt → `text/plain; charset=utf-8`), `tara` yer tutucusu.
  İstek başına TEK oturum açılır ve her servis çağrısına `db=` ile verilir.
- `backend/schemas.py`: `AktarimTeslimiOzetOut` (liste, 22 alan) + `AktarimTeslimiOut`
  (+ `durum_gecmisi`, `spool_path`) + `TeslimUygulaRequest(onay: bool = False)`.
- `backend/services/app_settings.py`: `SETTINGS_REGISTRY["veri_teslim_otomasyonu"]` (label
  "Veri teslim otomasyonu", varsayılan False) + `veri_teslim_otomasyonu_etkin(db=None)`
  yardımcısı (G109 gece job'ı bunu sorar).
- `backend/services/teslim_kutusu.py`: `bildir(teslim_id, olay, *, db=None) -> list[int]`,
  `bildirim_dedupe_key`, `BILDIRIM_TURU="veri_teslim"`, `BILDIRIM_OLAYLARI` (durum →
  başlık fiili + severity). `teslimi_isle` doğrulama/kuru koşu sonucu nihai ise ve kapı
  `inceleme` dediğinde; `teslim_uygula` `uygulandi`/`basarisiz` sonrasında çağırır.
  `teslim_uygula` gövdesi `_teslim_uygula`ya taşındı (sarmalayıcı yalnız bildirir), durum
  makinesi mantığı DEĞİŞMEDİ (G107'nin 51 testi olduğu gibi geçiyor).
- `backend/tests/test_g108_teslim_admin_uclari.py` (YENİ, 35 test): 403 (parametrik 8 yol),
  yükleme ×6, liste/tek/eşik ×3, kuru-kos ×2, uygula ×4, raporlar ×3, anahtar+tara ×2,
  bildirim ×7. Sentetik paketler test_g107'nin `_paket` üreticisinden.
- `hukdok_aktarim.py`, `api.py`, `notifications.py` diff'i SIFIR (yalnız çağrılır).

### Kararlar ve gerekçeleri

1. **Rapor indirme yolu `{ad:path}` dönüştürücüsüyle**: Starlette yolu çözümlenmiş
   (`%2F` → `/`) eşler; tek segment kalıbında `..%2Fetc` hiçbir route'a uymayıp 404'e
   düşerdi. `:path` ile ad yakalanır ve yol bileşeni (`..`, `/`, `\`, öncü `.`) 400 ile
   AÇIKÇA reddedilir (kabul kriteri). İkinci savunma: `resolve()` sonrası dosyanın rapor
   kökünün altında olması + yalnız `.csv`/`.txt` uzantısı; aksi 404. Çıplak `..` segmenti
   test edilmedi — HTTP istemcisi (httpx/tarayıcı) nokta segmentini sunucuya göndermeden
   çözer, sunucu onu hiç görmez (ilk koşuda 200 = detay ucu döndü; bu istemci davranışı).
2. **Yükleme ucu senkron `def`**: `UploadFile.file` parça parça okunur (sınır aşımı 413,
   tamamı belleğe alınmadan), ardından kuru koşu saniyeler sürer — `async def` olsaydı event
   loop'u tutardı; threadpool'da koşar. Uzun teslimlerde konteyner nginx'in 300 sn
   `proxy_read_timeout`'u sınırdır (20.08 ölçümü: 8.409 satır 93 sn — sığar).
3. **Yapı hatası HTTP hatası DEĞİL**: bozuk/Sheet'siz dosya 201 + `durum="reddedildi"`
   döner (defter izi + bildirim); 400 yalnız istek biçimi hataları (uzantı, boş gövde, onay).
4. **Bildirim çağrı noktaları**: `teslimi_isle` dogrula→`reddedildi`, kuru koşu→`basarisiz`
   ve kapı→`inceleme_bekliyor` sonuçlarını bildirir; `teslim_uygula` kendi sonucunu
   (`uygulandi`/`basarisiz`) bildirir — gece yolu (`teslimi_isle(otomatik_uygula=True)`)
   `teslim_uygula`dan geçtiği için çift bildirim yok. `yinelenen` bildirim ÜRETMEZ
   (bilgi defterde; iki kez bırakılan dosya alarm değil). Tek adım fonksiyonları
   (`teslim_dogrula`, `teslim_kuru_kos` doğrudan çağrı) bildirmez — sözleşme yalnız
   `teslimi_isle`/`teslim_uygula` diyor.
5. **Bildirim gövdesi**: `inceleme_bekliyor` → "Kapı: <gerekçe>"; `reddedildi`/`basarisiz`
   → `hata_mesaji`; `uygulandi` → sayaç özeti + uygulayan. `tenant_id=None` (ortak havuz).
   `routes.config._admin_emails` ve `create_notification` fonksiyon içinde import edilir
   (servis→route bağımlılığı modül yüklenirken döngü kurmasın); alıcı kümesi boşsa WARNING.
6. **`tara` kapalıyken de 200** döner (`not` = "otomasyon kapalı — tarama yapılmadı");
   açıkken G109 gelene kadar "SharePoint gözcüsü henüz yok". Panel `not` alanını toast'a
   basıyor (G111), 4xx olsaydı hata gibi görünürdü.
7. **Uygulama uçlarında `ValueError` → 409**: durum ön kontrolü geçse de aynı satıra
   eşzamanlı ikinci istek serviste `_durum_kontrol`a takılır; bu yarış 500 değil 409'dur.

### Test sonuçları (konteynerde)

- `pytest`: **2217 passed, 3 skipped** (önce 2182+3; +35 yeni). İlk tam koşuda
  `test_bildirim_uygulandi_info_reddedildi_warning` bir kez kırmızıydı: test aynı paketi
  iki kez ÜRETİYORDU (`_paket()` ×2) ve openpyxl her üretimde `docProps/core.xml`e zaman
  damgası yazdığı için saniye sınırını aşan iki üretim farklı sha256 veriyordu
  (konteynerde 1,1 sn arayla "farkli" doğrulandı) → "yinelenen" yerine yeni teslim.
  Düzeltme testte: aynı BAYTLAR yeniden yüklenir. Sonra dosya 3 kez + tam paket 2 kez
  üst üste yeşil.
- `ruff check .`: All checks passed. `mypy`: Success, 52 dosya.
- `git diff -- backend/scripts/hukdok_aktarim.py backend/api.py backend/services/notifications.py`: 0 satır.

### İzlenecekler

- G109: gece job'ı ve boot telafisi `app_settings.veri_teslim_otomasyonu_etkin()` kapalıysa
  hiçbir şey yapmasın; `tara` ucunun yer tutucu gövdesi gözcü gelince gerçek sayaçla
  değiştirilecek (`{"yeni","yinelenen"}` sözleşmesi aynı, `not` alanı isteğe bağlı).
- Yükleme ucu kuru koşuyu İSTEK İÇİNDE koşturur; teslim büyürse (300 sn üstü) arka plan
  işine taşınmalı — G111 paneli zaten `load()` ile listeyi tazeliyor, sözleşme değişmez.
- Rapor indirme `FileResponse` ile; spool `/app/data` volume'ünde olduğundan worker'lar
  arası paylaşımlı (2 worker). Spool janitor'ı hâlâ ayrı görev (G107 notu).
- `BILDIRIM_TURU="veri_teslim"` yeni bir bildirim türüdür; frontend'de türe göre etiket/filtre
  haritası YOK (`belge_islendi` de `frontend/src` içinde geçmiyor — grep ile bakıldı), bu
  yüzden ek iş çıkmadı. İleride tür filtresi yazılırsa bu etiket listeye girer.
