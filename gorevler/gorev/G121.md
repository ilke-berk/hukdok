# G121 — Dava kartı UI: Müvekkil Tipi + Hizmet Türü alanları, Hizmet Türü liste filtresi

- **Bant:** frontend
- **Bağımlı:** - (sözleşme aşağıda dondurulmuş — G103/G105 deseni; backend G119 ile
  paralel koşabilir, vitest mock'larla çalışır)
- **Dosya kapsamı:** frontend/src/lib/caseCardFields.ts, frontend/src/lib/caseCardFields.test.ts,
  frontend/src/hooks/useConfig.ts, frontend/src/hooks/useCases.ts,
  frontend/src/pages/CaseDetails.tsx, frontend/src/pages/CaseList.tsx, ilgili test dosyaları
- **Dokunma:** frontend/src/lib/api.ts (yalnız liste çağrısına `hizmet_turu` parametresi
  gerekiyorsa; başka değişiklik BLOKE bırak)

## Hedef

DB-2026-002'nin (04.09.2026) arayüz karşılığı: iki yeni kapalı-liste alanı dava kartına
girer, dava listesine Hizmet Türü filtresi eklenir ("Lexis Rapor" 2.218 föy dava takibi
değil; istatistik ve liste bu ayrımı görebilmeli). Yeni ekran YOK; G105'in üç ekleme
noktasının aynısı. Rozet YOK.

## SÖZLEŞME (dondurulmuş — G119 ile ORTAK; değiştirilemez, sapma gerekiyorsa BLOKE bırak)

- Kart alan anahtarları: `muvekkil_tipi` (etiket **"Müvekkil Tipi"**) ve `hizmet_turu`
  (etiket **"Hizmet Türü"**); ikisi de `type: "closedList"`.
- Liste anahtarları ve uçları: `client_types` → `GET /api/config/client_types`
  (5 değer: "Sigorta" · "Doktor" · "Kurum" · "Hasta" · "Diğer Sağlık Çalışanı");
  `service_types` → `GET /api/config/service_types`
  (9 değer: "Takip (doktor müvekkil)" · "Lexis Rapor" · "Vekaletsiz Takip" ·
  "Vekaletli Takip" · "Vekalet Ücreti Alacağı" · "Takip (hasta vekilliği)" ·
  "Takip (kurum vekilliği)" · "Danışmanlık" · "Takip (sağlık personeli)").
  Uç cevabı event_types ile aynı biçimdedir (code/name/active/sequence, sıralı).
- Liste filtresi query param adı: `hizmet_turu` (değer = listenin ADI, ör. "Lexis Rapor").

## Planlayıcı istisnası (04.09 gece, 1. tur BLOKE sonrası — TEK izinli mevcut-test değişikliği)

1. tur `test-degistirmek-gerekti` ile durdu: `frontend/src/lib/caseCardFields.test.ts`
içindeki "dosya_son_durumu karttan çıktı" testi `OFFICE_CARD_FIELDS`'i TAM LİSTE olarak
`["acceptance_date", "bureau_type"]` diye kilitliyor; görevin 1. maddesi bu listeye iki
satır eklediği için test zorunlu kırılıyor. Karar: G105'in `MEDICAL_CARD_FIELDS` kilidini
güncellediği emsalle **bu tek beklenti dört anahtara güncellenir**
(`["acceptance_date", "bureau_type", "muvekkil_tipi", "hizmet_turu"]`, yorumuna
"G121 ile dört alan" notu). Aynı dosyadaki "kart-taslak kesişimi TAM LİSTE" testi
DEĞİŞMEZ (iki yeni alan `TRACKING_DRAFT_KEYS`'e girmez). Başka mevcut test değiştirilmez.

1. turun commit'siz emeği yama olarak duruyor:
`C:/Users/ilkeb/AppData/Local/Temp/claude/C--Users-ilkeb-OneDrive-Masa-st--hukudok-automator-main/b74e62c8-d18f-4b26-9c03-c2dadeca63fd/scratchpad/G121-tur1.patch`
(5 dosya + 3 yeni test dosyası, main'e `git apply --check` temiz). Worktree'yi kurduktan
sonra `git apply <yama>` ile başlayıp yalnız yukarıdaki test güncellemesini ekleyebilirsin;
yama içeriğini yine de kabul kriterlerine karşı kendin doğrula.

## Yapılacaklar

1. **caseCardFields.ts:** iki satır `OFFICE_CARD_FIELDS`e, `bureau_type` ("Büro Özel
   Türü") satırının ALTINA — büro/hizmet bilgisi tek grupta kalır, tıbbi karta KOYMA.
   `ClosedListKey` tipine iki anahtar. Test: closedListState iki yeni liste için mevcut
   davranışın eşini veriyor.
2. **useConfig.ts:** `CONFIG_KEYS` + iki query (eventTypes kalıbı: queryKey, fetchJson,
   isLoading/isError agregasyonlarına dahil).
3. **CaseDetails.tsx:** `closedLists` objesi G105'in genişlettiği HER kullanım yerinde iki
   yeni anahtarla genişler — tek yerde bırakmak diğer kartlarda boş dropdown üretir.
   Alanlar bulundukları kartın mevcut düzenleme mekanizmasıyla düzenlenir (yeni mekanizma
   yazma).
4. **CaseList.tsx + useCases.ts:** filtre çubuğuna "Hizmet Türü" seçimi (Olay Türü
   filtresi hangi desendeyse o — service_types listesinden beslenir, "Tümü" varsayılan);
   seçim `hizmet_turu` param'ıyla liste isteğine eklenir (`olay_turu` ile aynı yol).

## Kabul kriterleri

- [ ] Büro kartında `bureau_type`'ın altında iki yeni alan görünüyor; kapalı liste
      değerlerini backend listesinden alıyor (mock'lu test).
- [ ] Filtre seçimi API çağrısına `hizmet_turu` param'ını ekliyor; "Tümü" seçiliyken
      param gönderilmiyor (test, useCases.olayTuru testinin ikizi).
- [ ] Mevcut testlerde gerileme yok; yeni alanlar hiçbir yerde zorunlu değil, boşken
      kart bugünkü gibi görünüyor (filledFields davranışı).
- [ ] Tam genişlik kuralı korunuyor (filtre çubuğu sağda boşluk bırakmıyor).

## Doğrulama

- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

**DURUM: TAMAM** (2. tur, 04.09.2026 gece; 1. tur yaması `git apply` ile alındı, kabul
kriterlerine karşı yeniden okundu, üç boşluk kapatıldı)

### Yapılanlar

1. **caseCardFields.ts** — `OFFICE_CARD_FIELDS`'e `muvekkil_tipi` ("Müvekkil Tipi",
   `client_types`) ve `hizmet_turu` ("Hizmet Türü", `service_types`) `bureau_type`'ın
   hemen altına eklendi; `ClosedListKey` iki anahtarla genişledi. Tıbbi/kanun yolu
   kartlarına konmadı.
2. **useConfig.ts** — `CONFIG_KEYS.clientTypes/serviceTypes` + iki `useQuery`
   (`/api/config/client_types`, `/api/config/service_types`), `listQueries` (isConfigError)
   ve `isConfigLoading` agregasyonlarına dahil, dönüş nesnesine `clientTypes`/`serviceTypes`.
3. **CaseDetails.tsx** — `closedLists` tek bir nesnede toplandı ve ÜÇ `TransferFieldsCard`
   kullanımına aynı nesne verildi (G105 dersi: tek yerde genişletmek diğer kartlarda boş
   dropdown üretir). `CaseDetailsData`'ya iki opsiyonel alan; büro kartı açıklaması
   güncellendi. Yeni düzenleme mekanizması YAZILMADI — kart mevcut `ClosedListValue`
   salt-okunur gösterimini (liste-dışı damgası) kullanır.
4. **useCases.ts** — `getCases({ hizmetTuru })` → `hizmet_turu` param'ı; "ALL"/boşken
   gönderilmez (olay_turu ile aynı satır deseni).
5. **CaseList.tsx** — filtre sütununa "Hizmet Türü" Select'i (Olay Türü'nün altında, aynı
   desen, `serviceTypes`'tan beslenir, "Tümü" varsayılan). 1. tur yamasının ATLADIĞI üç
   yer tamamlandı: sayfa sıfırlama effect'inin bağımlılıkları, `clearFilters`, ve
   `activeFilterCount` ("hizmetturu"). Filtre çubuğu mevcut `xl:grid-cols-[260px_1fr]`
   sütunu içinde; yeni bir genişlik sınırı eklenmedi (tam genişlik kuralı korunuyor).

### Kararlar

- **Planlayıcı istisnası uygulandı:** `caseCardFields.test.ts`'teki "dosya_son_durumu
  karttan çıktı" testi dört anahtara güncellendi (yorumunda "G121 ile dört alan"); "kart-taslak
  kesişimi TAM LİSTE" testi DEĞİŞMEDİ (yeni alanlar `TRACKING_DRAFT_KEYS`'e girmiyor —
  yeni testte de kilitli). Başka mevcut test değiştirilmedi.
- `api.ts`'e dokunulmadı: liste çağrısı `useCases.getCases` içinde `URLSearchParams` ile
  kuruluyor, ayrı bir API fonksiyonu gerekmedi.
- Sözleşmeden sapma yok: anahtarlar, etiketler, liste adları, uç yolları, param adı ve
  değer biçimi (liste ADI) görev dosyasındaki dondurulmuş sözleşmeyle birebir; yeni testler
  sözleşmedeki 5+9 değeri sırasıyla kilitler.

### Test sonuçları (HOST, worktree `C:/dev/hukudok-wt/G121`)

- `npm --prefix frontend test`: **52 dosya, 614 passed** (+3 dosya, +12 yeni test; yeni
  testler düşülünce 49 dosya / 602 = mevcut paket, gerileme yok) — 1. turda yeşil.
- `npm --prefix frontend run lint`: 0 hata, 0 uyarı.
- `npx tsc -b --force frontend`: 0 hata.
- Yeni test dosyaları: `frontend/src/lib/caseCardFields.serviceTypes.test.ts` (7),
  `frontend/src/hooks/useConfig.serviceTypes.test.tsx` (2),
  `frontend/src/hooks/useCases.hizmetTuru.test.tsx` (3). İlk ikisi eski kodda kesin kırmızı
  (alanlar/listeler yok); üçüncüsü eski kodda param'ı hiç göndermediği için kırmızı.

### İzlenecekler

- Backend G119 ile birleşince canlıda doğrulanacak: kartta iki alan gerçek uçtan doluyor mu,
  `hizmet_turu` filtresi liste sayısını değiştiriyor mu ("Lexis Rapor" ≈ 2.218 föy).
- Kart alanları salt-okunur (G105 emsali); kartta düzenleme istenirse ayrı görev.
- Docker compose ile DOĞRULANMADI (bant kuralı: konteyner ana dizini mount eder).
