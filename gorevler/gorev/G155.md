# G155 — Karar_Asamalari `Başvuru Tarihi`: aşama tablosuna kolon, okuyucu, kart fotoğrafı

- **Bant:** backend
- **Bağımlı:** G154 (aynı `hukdok_aktarim.py`; migrasyon zinciri)
- **Dosya kapsamı:** backend/models.py (`CaseStageDecision.basvuru_tarihi` Date), backend/database.py (`_MIGRATIONS`
  koşullu `("columns", ...)` — kısıt/index yok), backend/managers/stage_decisions.py (`_PHOTO_COLUMNS` `:88-128`:
  ISTINAF → `cases.istinaf_basvuru_tarihi`, TEMYIZ → `cases.temyiz_basvuru_tarihi`), backend/scripts/hukdok_aktarim.py
  (`ASAMA_SUTUNLARI` `:1687-1700` + `Başvuru Tarihi` adayı; Sheet `İstinaf Mahkeme Başvuru Tar.` yedek kaynak kalır),
  backend/routes/case_tracking.py ve frontend/src/**/CaseTracking* (yalnız alanı göstermek gerekiyorsa),
  backend/tests/test_g155_basvuru_tarihi.py (YENİ)
- **Dokunma:** backend/schemas*.py (yalnız gerekli alan), backend/required_fields.py, docs/**

> **YENİDEN KUYRUKTA (2026-09-10, planlayıcı).** İlk koşu BLOKE oldu: `test_g062_stage_decisions.py`
> `CaseStageDecision` kolon kümesini `==` ile tam kilitliyor, görevin istediği `basvuru_tarihi` kolonu bu
> kilide takıldı. Görev tanımı hatasıydı; izin aşağıda. **İşçinin taslağı `git stash` içinde:** `stash@{0}`
> "G155 taslak - test izni bekliyor (test_g062 kolon kilidi)" (models, database, stage_decisions,
> hukdok_aktarim, schemas, useCases, CaseTrackingPanel(+test), test_g155). İşçi ana dizinde **sırayla**:
> (1) `git stash pop` (çakışma beklenmez: G154 sonrası bu dosyalara başka görev dokunmadı; `stash@{1}` eski ve
> İLGİSİZ, dokunma); (2) **pytest'ten ÖNCE** `docker compose exec -T backend python migrate.py` — dbtest
> lokal Postgres'te yeni kolonu arıyor (önceki koşuda `test_mukerrer_yazim_gercek_postgreste_duzgun_donuyor`
> bu yüzden kırmızıydı, kod kusuru değil); (3) yalnız aşağıdaki izinli test değişikliği; (4) tam pytest +
> ruff + mypy + frontend vitest/lint/tsc. Önceki koşunun raporu en altta korunmuştur.

## Test değiştirme İZNİ (baştan)

**İZİNLİ:** `tests/test_g062_stage_decisions.py::test_model_ve_kolonlar_gorev_taslagina_uygun` kolon kümesine
**yalnız** `'basvuru_tarihi'` eklenir (tek satırlık kilit güncellemesi; assert sayısı ve test mantığı değişmez).
`frontend/src/components/CaseTrackingPanel.test.tsx`'e yeni test EKLENEBİLİR. **KİLİT:** test_g062'nin diğer
testleri, `test_g123_*` (`KART_ALANLARI["istinaf_basvuru_tarihi"]` kalır), `test_g150_*`, `test_g064_*`, diğerleri.

## Hedef

Plan 08.09 §1.2 **A6**. Ekip (06.09 §7) `Karar_Asamalari`'na `Başvuru Tarihi` ekliyor (21 → 22 sütun): istinaf
başvuru tarihi tek kaynaktan gelsin (bizim 06.09 §2.8 isteğimiz). Bugün okuyucu tanımıyor (`ASAMA_SUTUNLARI` yalnız
`Tebliğ Tarihi` ve `Başvuran Taraf`); `istinaf_basvuru_tarihi` Sheet'ten doğrudan karta gider (`:238-240`, `:807`);
`case_stage_decisions`'ta kolon yok.

## Yapılacaklar

1. Model + migrasyon (`("columns", "case_stage_decisions", ...)`; CLAUDE.md tuzağı: kalıcı kısıt yok, index gerekmez).
2. Okuyucu: `Başvuru Tarihi` → `basvuru_tarihi` (`_tarih` ile); imzaya girer (G150 kuralıyla boşsa katılmaz).
3. Fotoğraf: ISTINAF satırının `basvuru_tarihi` → `cases.istinaf_basvuru_tarihi`, TEMYIZ → `temyiz_basvuru_tarihi`;
   Sheet sütunu yalnız aşama satırı boşsa yazar (öncelik aşama sayfası). Yerel/karar düzeltme için fotoğraf yok.
4. Takip paneli aşama satırında alan (varsa mevcut tarih kolonlarının yanında; minimal).
5. Testler: sütun okunur; sütun yokken eski davranış; fotoğraf önceliği; migrasyon idempotent (çift koşu).

## Kabul kriterleri

- [ ] Kolon + migrasyon; `Başvuru Tarihi` okunur ve fotoğraflanır; sütun yok paketle geriye uyumlu (test).
- [ ] Sheet yedek kaynağı yalnız aşama değeri boşken yazar (test).
- [ ] ruff + mypy temiz; frontend değiştiyse vitest/lint/tsc temiz; pytest özet satırı.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`
- (frontend dokunulduysa) `npm --prefix frontend test` · `npm --prefix frontend run lint` · `npx --prefix frontend tsc -b --force frontend`

## Rapor

(işçi doldurur — ikinci koşu)

---

## Önceki koşu raporu (2026-09-08 gece, BLOKE — taslak `stash@{0}`'da korundu)

DURUM: BLOKE — `backend/tests/test_g062_stage_decisions.py::test_model_ve_kolonlar_gorev_taslagina_uygun`
`CaseStageDecision`'ın kolon kümesini `==` ile TAM kilitliyor (16 ad); görev kolonu (`basvuru_tarihi`) modele
eklenince test kırmızı oluyor ve testi değiştirmeden yeşile çekmenin yolu yok. Görev dosyasında test
değiştirme izni yok → işçi sözleşmesi gereği durdum (G138/G139/G151 dersi: test-kilidi kaldıran göreve
baştan "test değiştirme izni" yazılmalı). Kod commit'lenmedi; taslak `git stash` içinde (aşağıda).

### Taslakta yapılanlar (stash: `G155 taslak — test izni bekliyor`)

- `backend/models.py`: `CaseStageDecision.basvuru_tarihi = Column(Date, nullable=True)` (yorumla).
- `backend/database.py`: madde 47 `("columns", "case_stage_decisions", {"basvuru_tarihi": "DATE"})` — koşullu
  op, kısıt/index yok (G042), post-SQL yok.
- `backend/managers/stage_decisions.py`: `_PHOTO_COLUMNS` ISTINAF → `istinaf_basvuru_tarihi`, TEMYIZ →
  `temyiz_basvuru_tarihi`; `basvuru_tarihi` `CONTENT_FIELDS`e girdi; `_content_values` /
  `stage_decision_diff` / `update_stage_decision` / `add_stage_decision` imzalarına parametre; docstring.
- `backend/scripts/hukdok_aktarim.py`: `ASAMA_SUTUNLARI["basvuru_tarihi"] = ("Başvuru Tarihi",)`; alan
  `_IMZA_ALANLARI`ne girdi (G150 kuralıyla boş hücre imzaya katılmaz; `_kunye_dolu` da sayar);
  `_basvuru_tarihi_uzlasi` (önce aşama sayfası, İstinaf'ta boşsa kardeş föylerin Sheet "İstinaf Mahkeme
  Başvuru Tar." değeri — `_basvuran_taraf_uzlasi` deseni); `asama_kaynakli_kart_alanlari` + kart yolu kapısı
  (`_kart_alanlarini_yaz(asama_kaynakli=…)`): paketin İstinaf satırı Başvuru Tarihi taşıyorsa Sheet sütunu
  karta YAZILMAZ — aksi hâlde kart yolu Sheet değerini, fotoğraf aşama değerini yazar ve kolon her koşu
  salınırdı (basvuran_taraf'ta ölçülen kusur). `KART_ALANLARI["istinaf_basvuru_tarihi"]` KALDI (G123 testi
  kilitliyor; aşama satırı olmayan föyde Sheet karta gitmeye devam eder).
- `backend/schemas.py`: `CaseStageDecisionRead.basvuru_tarihi` (yalnız gerekli alan).
- `frontend/src/hooks/useCases.ts` + `components/CaseTrackingPanel.tsx`: geçmiş karar satırında
  `Başvuru: dd.mm.yyyy` (tebliğ/başvuranın yanında, minimal); `CaseTrackingPanel.test.tsx`e 1 test.
- `backend/tests/test_g155_basvuru_tarihi.py` (YENİ, 16 test): okuyucu + fotoğraf (İstinaf/Temyiz/Yerel),
  21 sütunlu eski paket geriye uyumlu, Sheet yedeği yalnız aşama değeri boşken, aşama önceliği + ikinci
  koşu 0 (tarihçe büyümez), temyizin Sheet yedeği yok, yerinde güncelleme tarihçeli, kardeş föy imza
  çelişkisi / boş hücre toleransı, yönetici içerik alanı (fark/fotoğraf/silme), migrasyon op'u tekil +
  sqlite'ta çift koşu idempotent (`_MIGRATIONS` tabloya süzülerek, test_migration_path deseni).

### Ölçümler (taslak, konteynerde)

- `pytest tests/test_g155_basvuru_tarihi.py`: 16 passed. ruff: temiz. mypy: temiz (61 dosya).
- Hedef dosyalar (g150/g123/g062/g064/g072 + g155): 174 passed, 3 failed —
  (1) `test_g062::test_model_ve_kolonlar_gorev_taslagina_uygun` = BLOKE sebebi;
  (2) `test_g062::test_mukerrer_yazim_gercek_postgreste_duzgun_donuyor` = dbtest, lokal Postgres'e
  kolon henüz migrasyonla gelmedi (`docker compose exec -T backend python migrate.py` ya da backend
  recreate çözer; ortam, kod kusuru değil — yeniden koşuda pytest'ten ÖNCE yapılmalı);
  (3) `test_g155::test_migrasyon_idempotent_cift_kosu` ilk sürümde sqlite'ta Postgres DDL'e takıldı,
  `_MIGRATIONS` süzgeciyle düzeltildi (yeşil).
- `vitest src/components/CaseTrackingPanel.test.tsx`: 34 passed. Frontend lint/tsc tam koşulmadı
  (BLOKE'de erken durdum).

### Yeniden kuyruğa alınırken

- Göreve açık izin: `test_g062_stage_decisions.py::test_model_ve_kolonlar_gorev_taslagina_uygun` kolon
  kümesine `basvuru_tarihi` eklenebilir (tek satırlık kilit güncellemesi; test başka bir şeyi gevşetmez).
- `git stash list` → `G155 taslak` girdisi `git stash pop` ile geri alınır; sonra migrate + tam pytest +
  `npm --prefix frontend run lint` + `npx --prefix frontend tsc -b --force frontend`.
- NOT (kapsam dışı): `.claude/settings.local.json` (M) ve `.claude/launch.json` (??) çalışma alanında
  kirliydi — dokunulmadı, harness dosyaları.
