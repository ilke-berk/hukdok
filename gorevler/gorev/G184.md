# G184 — useConfig: liste başına hook (`useConfigList`) + QueryClient odak yenilemesi kapalı

- **Bant:** frontend
- **Bağımlı:** G182
- **Dosya kapsamı:** `frontend/src/hooks/useConfig.ts`, `frontend/src/hooks/useConfig*.test.tsx`, `frontend/src/App.tsx` (yalnız `new QueryClient(...)` satırı)
- **Dokunma:** `useConfig` tüketicileri (16 dosya — bu görevde DEĞİŞMEZ, G185'te taşınır), `frontend/src/lib/api.ts`

Kaynak: `docs/arsiv/performans-denetimi-2026-09-14.md` F3.

## Hedef
`useConfig()` tek hook'ta 32 ayrı `useQuery` kuruyor (`useConfig.ts:147-188`); 1-2 liste
kullanan 14+ tüketici 32 observer'a abone oluyor. `App.tsx:34` `new QueryClient()` varsayılan
`refetchOnWindowFocus: true` → 5 dk staleTime dolunca her pencere odağında 32 istek. Bu görev
hook katmanını hazırlar: mevcut `useConfig()` sözleşmesi (dönüş şekli, `requiredFieldsError`
vb.) birebir korunur, yanına liste başına abone olan `useConfigList(key)` gelir; tüketici
taşıma G185'te.

## Kabul kriterleri
- [ ] `useConfigList(key: ConfigListKey)` dışa verilir: tek `useQuery`, `CONFIG_KEYS[key]` ile aynı queryKey ve aynı `queryOpts` (staleTime, retry:false, enabled) → `useConfig()` ile önbelleği PAYLAŞIR (aynı anahtar, çift istek yok). Dönüş: `{ data, isLoading, isError, error }` + listeye özgü türetilmiş yardımcılar gerekiyorsa (örn. `lawyers` için mevcut sıralama/filtreleme aynı fonksiyonla).
- [ ] `useConfig()` dönüş tipi ve alan adları DEĞİŞMEZ (16 tüketici derlenmeye devam eder); içeride ister eski 32 `useQuery` kalsın ister `useQueries` — davranış aynı.
- [ ] `App.tsx`'te `new QueryClient({ defaultOptions: { queries: { refetchOnWindowFocus: false } } })`; gerekçe yorumu (32 liste × odak). `useNotifications` 60 sn poll'u ve diğer `refetchInterval` kullanımları etkilenmez (doğrula, rapora yaz).
- [ ] Test: `useConfigList("lawyers")` monte edilince yalnız `/api/config/lawyers` çağrılır (fetch mock sayacı = 1); aynı bileşende `useConfig()` + `useConfigList("lawyers")` birlikte → `/api/config/lawyers` yine 1 kez.
- [ ] Test: pencere odağı olayında (`window.dispatchEvent(new Event("focus"))` + `visibilitychange`) bayat sorgu YENİDEN çekilmez.
- [ ] Mevcut `useConfig*.test.tsx` testleri değişmeden yeşil.

## Doğrulama
- `npm --prefix frontend test`
- `npm --prefix frontend run lint`
- `npx --prefix frontend tsc -b --force frontend`

## Rapor

DURUM: TAMAM

### Yapılanlar
- `frontend/src/hooks/useConfig.ts`
  - Modül düzeyine `configListQueryOptions(key, authRequest, enabled)` kurucusu çıktı: `queryKey = CONFIG_KEYS[key]`,
    uç `/api/config/${CONFIG_KEYS[key][1]}` (31 anahtarın hepsinde ikinci parça = eski elle yazılmış uç yolu, tek tek
    karşılaştırıldı), `staleTime` 5 dk, `retry:false`, `enabled`. `useConfig()` içindeki 31 liste `useQuery`'si ve yeni
    hook AYNI kurucuyu kullanır → önbellek paylaşımı yapısal olarak garanti.
  - `export type ConfigListKey = keyof typeof CONFIG_KEYS` ve `export const useConfigList(key)`: tek `useQuery`; dönüş
    `{ data, isLoading, isError, error, isFetching, refetch }`. `data` boşken modül sabiti `EMPTY` (useConfig ile aynı
    referans davranışı).
  - Türetilmiş yardımcılar saf fonksiyon olarak dışa verildi: `groupCourtTypesByParent`, `splitPartyRoles`; `useConfig()`
    `courtTypesByParent` / `mainPartyRoles` / `thirdPartyRoles`'u artık bunlarla hesaplar (davranış birebir).
  - `useConfig()` dönüş tipi ve alan adları DEĞİŞMEDİ; zorunlu alan sorgusu (`required_case_fields`) aynı `queryOpts` ile
    olduğu yerde kaldı. `fetchJson` → modül düzeyi `fetchConfigList` (G019 yorumu taşındı, hata mesajı aynı).
- `frontend/src/App.tsx`: yalnız `new QueryClient(...)` satırı →
  `new QueryClient({ defaultOptions: { queries: { refetchOnWindowFocus: false } } })` + gerekçe yorumu (32 liste × odak).
- `frontend/src/hooks/useConfig.list.test.tsx` (YENİ, 10 test).

### Kararlar + gerekçe
- **Eski 32 `useQuery` yapısı korundu, `useQueries`'e geçilmedi:** kabul ikisine de izin veriyor; ortak kurucu çift kaynak
  riskini zaten kapatıyor, `useQueries`'e geçmek `isLoading`/`refetchConfig` birleşimini yeniden yazmayı gerektirirdi
  (gereksiz risk, G185 tüketici taşımasından sonra ölçülerek düşünülebilir).
- **`useConfigList().error` bir MESAJ (string | undefined), Error nesnesi değil:** `useConfig().configError` ile aynı
  sözleşme (G019 "hata ≠ boş liste" şeridi doğrudan bunu gösterir); G185'te tüketici taşıması bire bir eşleşir.
- **`isFetching` + `refetch` eklendi:** mevcut tüketiciler `isRefetchingConfig` / `refetchConfig` kullanıyor; G185'te
  tek listeye taşınırken karşılıkları hazır olsun.
- **Lawyers için özel sıralama/filtre YOK:** kabulde örnek verilen "mevcut sıralama" `useConfig`'te bulunmuyor (liste
  backend sırasıyla döner); türetilmiş yardımcı yalnız mahkeme türü grubu ve taraf rolü ayrımı için var.
- **Odak testi App ayarını literal kopyalar + kaynak bekçisi:** `queryClient` App.tsx'te modül içi ve dışa verilmiyor;
  dosya kapsamı App.tsx'te yalnız bu satıra izin verdiği için export eklenmedi. Bunun yerine test aynı ayarla kurulur ve
  `@/App.tsx?raw` üzerinden App.tsx'in bu ayarı taşıdığı regex bekçisiyle doğrulanır.

### refetchInterval / poll doğrulaması
- `frontend/src` altında `refetchInterval` kullanımı YOK (grep: 0 eşleşme); `refetchOnWindowFocus` / `focusManager`
  başka yerde ayarlanmıyor.
- `useNotifications` 60 sn poll'u react-query DEĞİL: `setInterval(NOTIFICATION_POLL_MS)` + `document`
  `visibilitychange` dinleyicisi (`hooks/useNotifications.ts:154-187`) → QueryClient varsayılanından etkilenmez; kendi
  testi (`useNotifications.test.tsx`) değişmeden yeşil.
- react-query 5.83.0 `focusManager` yalnız `window` `visibilitychange` dinler (`focus` olayı değil); test ikisini de yayar.

### Test sonuçları
- `npm --prefix frontend test`: **76 dosya, 963 passed** (0 fail). Başlangıç: mevcut 3 useConfig test dosyası 10/10 yeşil;
  değişiklikten sonra da değişmeden yeşil.
- `npm --prefix frontend run lint`: temiz.
- `npx tsc -b --force` (frontend): çıkış 0.
- Yeni testler (10): yalnız `/api/config/lawyers` 1 çağrı; `emailRecipients` → `/api/config/email_recipients`;
  `useConfig()` + `useConfigList("lawyers")` birlikte → lawyers 1 çağrı + aynı dizi referansı; hata state'i
  (`CONFIG_LIST_ERROR`, boş liste); refetch hata temizler; App ayarıyla bayat sorgu odakta çekilmez (sayaç 1);
  **kontrol** varsayılan QueryClient'ta aynı olay yeniden çeker (sayaç 2 — bayatlatma ve olaylar gerçekten etkili, negatif
  test boşa çalışmıyor); App.tsx kaynak bekçisi; iki türetilmiş yardımcı. Eski kodda `useConfigList` / yardımcılar yok ve
  App.tsx `new QueryClient()` → yeni testler kırmızı.

### İzlenecekler
- NOT (davranış değişikliği, bilinçli): global varsayılan config dışındaki 3 sorguyu da etkiler — `useClients`
  (`["clients"]`, 5 dk stale), `ClientList` poliçeler (`["client-policies", id]`, 60 sn stale), `usePartyCheck`
  (`["party-check", …]`). Bunlar artık odakta tazelenmez; mount/yeniden bağlanma ve mutasyon invalidate'i ile tazelenir.
  Uzun süre açık bırakılan müvekkil listesinde başka kullanıcının eklediği kayıt, sayfa yeniden açılana dek görünmez.
  Gerekirse ilgili sorguda yerel `refetchOnWindowFocus: true` verilir.
- G185: 16 `useConfig` tüketicisi `useConfigList`'e taşınacak; `groupCourtTypesByParent` / `splitPartyRoles` hazır.
