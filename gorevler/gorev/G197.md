# G197 — Bayat metin ve atıflar: perf_olcum VALIDATE ifadesi, nginx.conf host yorumu, `nginx.conf:N` atıfları, CLAUDE.md test sayıları

- **Bant:** backend (kod dosyası `perf_olcum.py` + test sayıları konteyner pytest'iyle ölçülür)
- **Bağımlı:** G196 (test sayıları G196'dan SONRA ölçülür; backend seri)
- **Dosya kapsamı:** `backend/scripts/perf_olcum.py` (yalnız kullanıcıya basılan metinler: `:413`, gerekirse `:7`, `:603`), `nginx.conf` (yalnız `:44` yorum satırları), `CLAUDE.md` (Komutlar bölümündeki iki test sayısı + doküman haritası + bulunan bayat `nginx.conf:N` atıfları), `docs/mimari/**`, `docs/kararlar/**`, `docs/hukukbot-aktarim/**` (yalnız bayat `nginx.conf:N` atıfları ve `deploy-ve-altyapi.md` perf_olcum şerhi)
- **Dokunma:** `docs/arsiv/**`, `gorevler/**` (tarihsel kayıt — atıflar yazıldığı günün fotoğrafıdır), `nginx.conf` direktifleri (yalnız `#` yorum değişir), `perf_olcum.py` mantığı/sorguları, testler

Kaynak: `otomasyon/loglar/kuyruk-workflow_2026-09-14-4.md` "Karar bekleyenler" (G193 + G195 işçi notları);
kullanıcı kararı 14.09: görev açıldı.

## Hedef
Performans turu (G182–G195) sonrası yaşayan dokümanda ve kullanıcıya basılan metinde kalan dört bayatlık:
1. **`perf_olcum.py:413`** durum bölümü "CHECK kısıtı (D9) ancak 0 iken eklenebilir" diyor. G195 kısıtı `NOT VALID`
   ile ekledi; doğru ifade: `VALIDATE CONSTRAINT ck_cases_status_uclu` ancak üçlü dışı = 0 iken. Dosyadaki genel
   "CHECK" anıları (`:7`, `:603`) aynı gözle okunur, gerekiyorsa "VALIDATE"e çevrilir.
   `docs/mimari/deploy-ve-altyapi.md:459-460` şerhi ("script ... der; doğru okuma VALIDATE") metin düzelince güncellenir.
2. **`nginx.conf:44`** yorumu "TLS'i sonlandiran host nginx (konfigi repo DISINDA)" diyor — yanlış: host konfigi
   `infra/nginx/sites-available/default` olarak repoda (G193 raporu: `infra/install.sh:73` kurar; doğrula).
   Dosya yorumları ASCII'dir, ASCII kalır.
3. **`nginx.conf:N` atıfları** — G182 nginx.conf'u uzattı, satırlar kaydı. Bilinen bayatlar: `CLAUDE.md:247`
   (`:75` → `/export` uyarısı artık `:114-115`), `docs/kararlar/010-export-nginxe-acilmaz.md:14` (`:75-76`),
   `docs/mimari/raporlama.md:74` (`:77 location /api` — `location /api` artık `:116`),
   `docs/kararlar/013-bagimlilik-yamalama-ve-calisma-zamani.md:151` (`:7,91` `client_max_body_size`). Liste
   eksik olabilir: `CLAUDE.md` + `docs/mimari` + `docs/kararlar` + `docs/hukukbot-aktarim` içindeki TÜM
   `nginx.conf:N` atıfları grep'le taranır, her biri güncel dosyada açılıp doğrulanır.
4. **`CLAUDE.md` Komutlar** bölümündeki test sayıları (`2026-09-13: 3390 passed, 3 skipped` / `940 passed (74 dosya)`)
   bayat. Bu görevde KOŞULAN komutların çıktısıyla, tarihli yazılır — ezberden/rapordan kopyalanmaz (ALTIN KURAL).

## Kabul kriterleri
- [ ] `perf_olcum.py` basılan metni `VALIDATE` koşulunu söyler; `tests/test_perf_olcum.py` gövdesi DEĞİŞMEDEN yeşil; `deploy-ve-altyapi.md` şerhi güncel metinle çelişmez.
- [ ] `git diff nginx.conf` yalnız `#` ile başlayan satırları değiştirir; yorum `infra/nginx/sites-available/default` yolunu doğru anar.
- [ ] Rapora tablo: taranan her yaşayan `nginx.conf:N` atfı → dosya:satır · eski · yeni · satırdaki içerik (kanıt). Görev sonunda aynı grep'te bayat atıf 0; `docs/arsiv/**` ve `gorevler/**` diff'i BOŞ.
- [ ] `CLAUDE.md` iki test sayısı bu oturumda koşulan `docker compose exec -T backend python -m pytest` ve `npm --prefix frontend test` özet satırlarıyla birebir, tarihli; çıktılar rapora.
- [ ] `git diff --stat`: yalnız kapsamdaki dosyalar + görev raporu; kod mantığı değişikliği yok.

## Doğrulama
- `docker compose exec -T backend python -m pytest tests/test_perf_olcum.py`
- `docker compose exec -T backend python -m pytest` (sayı CLAUDE.md'ye)
- `npm --prefix frontend test` (sayı CLAUDE.md'ye)
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data` (çıplak `ruff check .` git dışı `calibration-data/_g179/ek3_cevapla.py` yüzünden kırmızı)
- `docker compose exec -T backend python -m mypy`

## Rapor

DURUM: TAMAM (2026-09-14, işçi oturumu; önceki HEAD `4245979`)

### Yapılanlar
1. **`backend/scripts/perf_olcum.py`** — yalnız metin, mantık/sorgu değişmedi:
   - `:413` (bölüm 5, basılan): "CHECK kısıtı (D9) ancak 0 iken eklenebilir" →
     "`VALIDATE CONSTRAINT ck_cases_status_uclu` (D9) ancak 0 iken koşulabilir".
   - `:7` (docstring): "CHECK kısıtı gibi kararlar" → "`ck_cases_status_uclu` VALIDATE'i gibi kararlar".
   - `:603` (başlık şerhi, basılan): "(VACUUM FULL, index düşürme, CHECK)" → "(…, CHECK VALIDATE)".
2. **`docs/mimari/deploy-ve-altyapi.md:458-460`** — "script metni hâlâ CHECK der" şerhi kaldırıldı; yerine
   script'in `:413` metninin aynı koşulu söylediği yazıldı (satır sayısı aynı).
3. **`nginx.conf:44-45`** — yalnız `#` satırları: "host nginx (konfigi repo DISINDA)" →
   "host nginx (repoda infra/nginx/sites-available/default, infra/install.sh kurar) sunucuda …".
   Doğrulama: `infra/nginx/sites-available/default` var (TLS 443, `proxy_pass http://localhost:8080`,
   `add_header`/`proxy_hide_header` YOK); `infra/install.sh:73` `inst nginx/sites-available/default
   /etc/nginx/sites-available/default 0644`. Eklenen metin ASCII.
4. **Bayat `nginx.conf:N` atıfları** — 4 kalem düzeltildi (tablo aşağıda).
5. **`CLAUDE.md` Komutlar** — iki test sayısı bu oturumda koşulan komutların özet satırlarıyla, 2026-09-14 tarihli.

### Karar: satır numarası kaydırmama
İlk denemede `nginx.conf` yorumunu 4 satıra ve `perf_olcum.py` metinlerini 2 satıra yaydım; bu `nginx.conf:48+`
atıflarının (CLAUDE.md, genel-bakış, deploy-ve-altyapı — ~30 atıf) ve `perf_olcum.py:413` atfının hepsini
yeniden bayatlatırdı. Geri alındı: her değişen yorum/metin ORİJİNAL satır sayısında tutuldu. Sonuç
`git diff nginx.conf` = `@@ -44,2 +44,2 @@`, `perf_olcum.py` 3 satır ↔ 3 satır; diğer tüm atıflar geçerli kaldı.

### `nginx.conf:N` atıf tablosu
Tarama: `rg -n 'nginx\.conf:\d'` → `CLAUDE.md`, `docs/mimari/**`, `docs/kararlar/**`, `docs/hukukbot-aktarim/**`
(hukukbot-aktarim'de satır atfı yok; `nginx.conf#L`/"satır N" biçimleri de tarandı — yaşayan dokümanda yok).
Kanıt sütunu güncel `nginx.conf` (commit sonrası; 44-45 değişikliği satır kaydırmaz).

| Dosya:satır | Eski | Yeni | Güncel `nginx.conf` satırındaki içerik (kanıt) |
| --- | --- | --- | --- |
| `CLAUDE.md:247` | `:75` | `:114-115` | 114-115 `# DIKKAT: /export buraya ASLA eklenmez — …` (75 artık `# (frontend/src/lib/chunkReload.ts). Cache-Control…`) |
| `docs/kararlar/010-export-nginxe-acilmaz.md:14` | `:75-76` | `:114-115` | aynı `/export` uyarısı (alıntılanan metin birebir) |
| `docs/mimari/raporlama.md:74` | `:77` `location /api` | `:116` | 116 `location /api {` (77 artık `location ~* ^/assets/ {`) |
| `docs/kararlar/013-bagimlilik-yamalama-ve-calisma-zamani.md:151` | `:7,91` | `:7,130` | 7 `client_max_body_size 50M;` (server), 130 `client_max_body_size 50M;` (`/process`); 91 artık `try_files $uri $uri/ /index.html;` |
| `CLAUDE.md:25` | `:77-99` | değişmedi | 77-86 `/assets/` + 88-99 `location /` önbellek blokları |
| `CLAUDE.md:30` | `:48-51` | değişmedi | 48-51 add_header kalıtım uyarısı |
| `CLAUDE.md:33` | `:10-14` | değişmedi | 10-12 gerekçe, 13-14 `proxy_read/send_timeout 300s` |
| `CLAUDE.md:36` | `:114` | değişmedi | 114 `/export` ASLA eklenmez |
| `docs/kararlar/009-confirm-idempotency-anahtari.md:10` | `:10-12` | değişmedi | GhostScript 504 / mükerrer kayıt yorumu |
| `docs/mimari/belge-isleme-hatti.md:208` | `:13` | değişmedi | 13 `proxy_read_timeout 300s;` |
| `docs/mimari/deploy-ve-altyapi.md:166` | `:101-111` | değişmedi | 101-104 yorum + 105-111 `location = /healthz` |
| `docs/mimari/deploy-ve-altyapi.md:312` | `:53-56` | değişmedi | 53-56 dört güvenlik `add_header` |
| `docs/mimari/deploy-ve-altyapi.md:314` | `:70` | değişmedi | 70 `add_header Content-Security-Policy …` |
| `docs/mimari/deploy-ve-altyapi.md:317` | `:81-85`, `:94-98` | değişmedi | `/assets/` ve `/` içindeki tekrarlanan beş başlık |
| `docs/mimari/deploy-ve-altyapi.md:318` | `:48-51` | değişmedi | kalıtım uyarısı |
| `docs/mimari/deploy-ve-altyapi.md:345` | `:70`, `:85`, `:98` | değişmedi | üç CSP satırı |
| `docs/mimari/deploy-ve-altyapi.md:372` | `:77-99` | değişmedi | iki önbellek location'ı |
| `docs/mimari/genel-bakis.md:48` | `:8` | değişmedi | 8 `listen 80;` |
| `docs/mimari/genel-bakis.md:50` | `:88-91` | değişmedi | `location /` … `try_files … /index.html` |
| `docs/mimari/genel-bakis.md:52` | `:38-51` | değişmedi | G182 önbellek gerekçe yorumu |
| `docs/mimari/genel-bakis.md:56` | `:77-86` | değişmedi | `location ~* ^/assets/` bloğu |
| `docs/mimari/genel-bakis.md:57` | `:88-99` | değişmedi | `location /` bloğu |
| `docs/mimari/genel-bakis.md:60` | `:53-56`, `:70` | değişmedi | güvenlik başlıkları + CSP |
| `docs/mimari/genel-bakis.md:61` | `:48-51` | değişmedi | kalıtım uyarısı |
| `docs/mimari/genel-bakis.md:69` | `:101-111` | değişmedi | `/healthz` |
| `docs/mimari/genel-bakis.md:70` | `:116` | değişmedi | `location /api {` |
| `docs/mimari/genel-bakis.md:71` | `:124-131` | değişmedi | `location /process` + 130 `client_max_body_size 50M` |
| `docs/mimari/genel-bakis.md:72` | `:133` | değişmedi | `location /confirm {` |
| `docs/mimari/genel-bakis.md:73` | `:141` | değişmedi | `location /preview-email-body {` |
| `docs/mimari/genel-bakis.md:74` | `:149-159` | değişmedi | yorum + `location /preview-client-email-body` |
| `docs/mimari/genel-bakis.md:75` | `:161` | değişmedi | `location /refresh {` |
| `docs/mimari/genel-bakis.md:79` | `:114-115` | değişmedi | `/export` uyarısı |
| `docs/mimari/genel-bakis.md:81` | `:13-14` | değişmedi | iki timeout |
| `docs/mimari/genel-bakis.md:83` | `:10-12` | değişmedi | gerekçe yorumu |
| `docs/mimari/genel-bakis.md:87` | `:12` | değişmedi | 12 `# Host nginx zaten 300s — iki katman esit olmali.` |

Görev sonu aynı grep: 35 atıf satırı (bazı satırda birden çok atıf; hepsi yukarıda), bayat **0**. `git status --porcelain -- docs/arsiv gorevler` → yalnız bu rapor
dosyası (`gorevler/gorev/G197.md`); `docs/arsiv/**` diff BOŞ, başka görev dosyası değişmedi.

### Test sonuçları (bu oturumda koşuldu)
- `docker compose exec -T backend python -m pytest tests/test_perf_olcum.py` → `32 passed in 4.61s`
  (test dosyası değişmedi; basılan metne kesin eşitlik iddiası yok).
- `docker compose exec -T backend python -m pytest` → `3567 passed, 3 skipped, 3 warnings in 213.24s (0:03:33)` (çıkış 0)
- `npm --prefix frontend test` → `Test Files  88 passed (88)` · `Tests  1005 passed (1005)` (çıkış 0;
  stderr'deki "tema sağlayıcı çöktü" `theme-provider.test.tsx`'in kasıtlı hata sınırı testi).
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data` → `All checks passed!`
- `docker compose exec -T backend python -m mypy` → `Success: no issues found in 62 source files`

### NOT (kapsam dışı, düzeltilmedi)
- Backend kod yorumlarında iki bayat `nginx.conf:N` atfı var (dosya kapsamı dışında, "aynı grep"in dizinlerinde değil):
  `backend/routes/processing.py:378` `nginx.conf:109` → `/refresh` artık `:161`;
  `backend/routes/notifications.py:28` `nginx.conf:62` → `location /api` artık `:116`. Ayrı küçük görev olabilir.
- Çalışma ağacında görevden ÖNCE kirli olan `.claude/settings.local.json` ve izlenmeyen `.claude/launch.json`
  koşucu ortamına ait; dokunulmadı, commit'e girmedi.

### İzlenecekler
- `perf_olcum` prod koşusu: bölüm 5 "üçlü dışı = 0" → `VALIDATE CONSTRAINT` ayrı görev (deploy-ve-altyapı §12 sıra 4).
- `nginx.conf` her satır eklemede CLAUDE.md + genel-bakış + deploy-ve-altyapı atıfları yeniden taranmalı.
