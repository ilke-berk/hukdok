# G197 — Bayat metin ve atıflar: perf_olcum VALIDATE ifadesi, nginx.conf host yorumu, `nginx.conf:N` atıfları, CLAUDE.md test sayıları

- **Bant:** backend (kod dosyası `perf_olcum.py` + test sayıları konteyner pytest'iyle ölçülür)
- **Bağımlı:** G196 (test sayıları G196'dan SONRA ölçülür; backend seri)
- **Dosya kapsamı:** `backend/scripts/perf_olcum.py` (yalnız kullanıcıya basılan metinler: `:413`, gerekirse `:7`, `:603`), `nginx.conf` (yalnız `:44` yorum satırları), `CLAUDE.md` (Komutlar bölümündeki iki test sayısı + doküman haritası + bulunan bayat `nginx.conf:N` atıfları), `docs/mimari/**`, `docs/kararlar/**`, `docs/hukukbot-aktarim/**` (yalnız bayat `nginx.conf:N` atıfları ve `deploy-ve-altyapi.md` perf_olcum şerhi)
- **Dokunma:** `docs/arsiv/**`, `gorevler/**` (tarihsel kayıt — atıflar yazıldığı günün fotoğrafıdır), `nginx.conf` direktifleri (yalnız `#` yorum değişir), `perf_olcum.py` mantığı/sorguları, testler

Kaynak: `otomasyon/loglar/kuyruk-workflow_2026-09-14-4.md` "Karar bekleyenler" (G193 + G195 işçi notları);
kullanıcı kararı 14.09: görev açıldı.

## Hedef
Performans turu (G182–G195) sonrası yaşayan dokümanda ve kullanıcıya basılan metinde kalan dört bayatlık:
1. **`perf_olcum.py:413`** durum bölümü "CHECK kısıtı (D9) ancak 0 iken eklenebilir" diyor. G195 kısıtı `NOT VALID`
   ile ekledi; doğru ifade: `VALIDATE CONSTRAINT ck_cases_status_uclu` ancak üçlü dışı = 0 iken. Dosyadaki genel
   "CHECK" anıları (`:7`, `:603`) aynı gözle okunur, gerekiyorsa "VALIDATE"e çevrilir.
   `docs/mimari/deploy-ve-altyapi.md:459-460` şerhi ("script ... der; doğru okuma VALIDATE") metin düzelince güncellenir.
2. **`nginx.conf:44`** yorumu "TLS'i sonlandiran host nginx (konfigi repo DISINDA)" diyor — yanlış: host konfigi
   `infra/nginx/sites-available/default` olarak repoda (G193 raporu: `infra/install.sh:73` kurar; doğrula).
   Dosya yorumları ASCII'dir, ASCII kalır.
3. **`nginx.conf:N` atıfları** — G182 nginx.conf'u uzattı, satırlar kaydı. Bilinen bayatlar: `CLAUDE.md:247`
   (`:75` → `/export` uyarısı artık `:114-115`), `docs/kararlar/010-export-nginxe-acilmaz.md:14` (`:75-76`),
   `docs/mimari/raporlama.md:74` (`:77 location /api` — `location /api` artık `:116`),
   `docs/kararlar/013-bagimlilik-yamalama-ve-calisma-zamani.md:151` (`:7,91` `client_max_body_size`). Liste
   eksik olabilir: `CLAUDE.md` + `docs/mimari` + `docs/kararlar` + `docs/hukukbot-aktarim` içindeki TÜM
   `nginx.conf:N` atıfları grep'le taranır, her biri güncel dosyada açılıp doğrulanır.
4. **`CLAUDE.md` Komutlar** bölümündeki test sayıları (`2026-09-13: 3390 passed, 3 skipped` / `940 passed (74 dosya)`)
   bayat. Bu görevde KOŞULAN komutların çıktısıyla, tarihli yazılır — ezberden/rapordan kopyalanmaz (ALTIN KURAL).

## Kabul kriterleri
- [ ] `perf_olcum.py` basılan metni `VALIDATE` koşulunu söyler; `tests/test_perf_olcum.py` gövdesi DEĞİŞMEDEN yeşil; `deploy-ve-altyapi.md` şerhi güncel metinle çelişmez.
- [ ] `git diff nginx.conf` yalnız `#` ile başlayan satırları değiştirir; yorum `infra/nginx/sites-available/default` yolunu doğru anar.
- [ ] Rapora tablo: taranan her yaşayan `nginx.conf:N` atfı → dosya:satır · eski · yeni · satırdaki içerik (kanıt). Görev sonunda aynı grep'te bayat atıf 0; `docs/arsiv/**` ve `gorevler/**` diff'i BOŞ.
- [ ] `CLAUDE.md` iki test sayısı bu oturumda koşulan `docker compose exec -T backend python -m pytest` ve `npm --prefix frontend test` özet satırlarıyla birebir, tarihli; çıktılar rapora.
- [ ] `git diff --stat`: yalnız kapsamdaki dosyalar + görev raporu; kod mantığı değişikliği yok.

## Doğrulama
- `docker compose exec -T backend python -m pytest tests/test_perf_olcum.py`
- `docker compose exec -T backend python -m pytest` (sayı CLAUDE.md'ye)
- `npm --prefix frontend test` (sayı CLAUDE.md'ye)
- `docker compose exec -T backend python -m ruff check . --extend-exclude calibration-data` (çıplak `ruff check .` git dışı `calibration-data/_g179/ek3_cevapla.py` yüzünden kırmızı)
- `docker compose exec -T backend python -m mypy`

## Rapor
(işçi oturumu doldurur: yapılanlar, kararlar, test sonuçları, izlenecekler)
