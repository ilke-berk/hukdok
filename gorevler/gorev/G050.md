# G050 — Deploy kapısı migrasyon/DB testlerini koşmuyor (G047 yeniden denemesi)

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** deploy.sh, docs/mimari/deploy-ve-altyapi.md
- **Dokunma:** backend/tests/conftest.py (DOKUNMA), backend/tests/test_migration_path.py (DOKUNMA), .github/workflows/ci.yml (DOKUNMA)

## Hedef

G047'nin devamı. **Görev kendi hatasıyla düşmedi:** claude süreci 60 sn sonra dışarıdan
sonlandırıldı (çıkış kodu `0x40010004` = DBG_TERMINATE_PROCESS), her iki log dosyası da
**tamamen boş**, commit üretilmedi. Yani iş hiç başlamadı — bu temiz bir yeniden deneme.

## ÖLÇÜM GÜNCELLENDİ — sorun G047'de yazandan büyük

G047 "6 test atlanıyor" diyordu. FAZ D bittikten sonra 2026-08-12'de yeniden ölçüldü:

| Nerede | Sonuç |
| --- | --- |
| `docker compose exec -T backend python -m pytest` | **1184 passed, 3 skipped** |
| `bash deploy.sh --gate-only` | **1156 passed, 31 skipped** (çıkış 0) |

Toplam ikisinde de 1187 — yani deploy kapısında **28 test sessizce atlanıyor**, 6 değil.
FAZ D'nin eklediği DB-bağımlı testler (migrasyon yolu, index envanteri, esas tarihçesi)
aynı deliğe düştü. **Kapı, FAZ D'yi koruyan testlerin çoğunu koşmadan yeşil diyor.**

Kapı bilinçli olarak temiz ortamlı `docker run` kullanıyor (G038'in doğru kararı —
`docker compose run` `.env`'i taşır ve migrasyon testi **prod Postgres'te** scratch veritabanı
yaratıp düşürebilirdi). Bedeli: DB'ye ulaşamıyor, testler SKIP oluyor. SKIP yeşildir.

## Çözüm yönü

Kapı **kendi tek kullanımlık Postgres'ini** kaldırsın: `docker run -d postgres:15-alpine`
(rastgele ad, yayınlanmamış port, kendi ağı) → test konteyneri ona bağlansın → kapı bitince
**her iki konteyner de** yıkılsın. Prod veritabanına hiçbir temas yok.

## Kabul kriterleri

- [ ] `--gate-only` çıktısındaki skip sayısı `docker compose exec` koşusuyla **aynı seviyeye**
      indi (taban: 3). Kalan her skip için sebep rapora tek tek yazılır — "yaklaştı" yetmez,
      **sayı yazılır**
- [ ] `tests/test_migration_path.py`'nin **7 testi de koşuyor** (bugün 6'sı SKIP; 7.'si
      `test_scratch_isim_korumasi_gercek_veritabanini_reddeder`, DB'siz koşan tek test)
- [ ] Geçici Postgres konteyneri kapı bitince **her yolda** siliniyor: başarı, test
      başarısızlığı, pip başarısızlığı (91) ve `Ctrl-C`/`trap`. Dördü de denendi ve
      `docker ps -a` ile artık kalmadığı görüldü — çıktı rapora yazılır
- [ ] Konteyner adı çakışmaya dayanıklı (aynı anda iki deploy ya da kalıntı bir konteyner
      kapıyı kilitlememeli)
- [ ] Geçici Postgres **prod ağına bağlanmıyor** ve **port yayınlamıyor**;
      `docker compose run` KULLANILMIYOR (denetçi grep'ler)
- [ ] Kapının süresi yeniden ölçüldü (postgres kalkışı ekleniyor) ve rapora yazıldı;
      bugünkü **44 sn** ile karşılaştırıldı
- [ ] Dört yol yine ölçüldü: sağlam → 0, kırık → ≠0, `SKIP_TESTS=1` → 0 + uyarı,
      pip yok → 91 → atla
- [ ] `bash -n deploy.sh` temiz; `docs/mimari/deploy-ve-altyapi.md` aynı commit'te güncel
- [ ] Prod'a hiçbir şey koşulmadı

## Doğrulama

- `bash -n deploy.sh`
- `bash deploy.sh --gate-only` (lokal — prod DEĞİL)
- `docker ps -a` (kalıntı konteyner yok)

## Rapor

(işçi oturumu doldurur)
