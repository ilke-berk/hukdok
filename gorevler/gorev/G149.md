# G149 — 370 derdest kart listesi: föysüz + DERDEST kartların xlsx raporu (ekibe ek)

- **Bant:** backend
- **Bağımlı:** -
- **Dosya kapsamı:** backend/scripts/foysuz_derdest_kartlar.py (YENİ), backend/tests/test_g149_foysuz_derdest_kartlar.py (YENİ)
- **Dokunma:** backend/scripts/hukdok_aktarim.py, backend/scripts/mukerrer_kart_raporu.py (yalnız desen için oku),
  backend/models.py, backend/managers/**, frontend/**

## Hedef

Plan `docs/plan/veri-ekibi-cevabi-karsilastirma-plani-2026-09-08.md` §1.5 **P1**: veri ekibi (06.09 §1.4) 370
derdest kartın listesini istiyor; icra/tahkim/vergi olanları ayırıp kalanın malpraktis olanlarını listelerine
alacaklar. Sayı 06.09 ad-hoc ölçümüdür (`veri-kalitesi-duzenleme-plani-2026-09-06.md` §2.6 #31: föysüz 7.906
aktif kart, 370 DERDEST: İcra 195, Hukuk 139); üreten script repoda yok. Salt okunur bir script yazılır.

## Yapılacaklar

1. `scripts/foysuz_derdest_kartlar.py`: `cases` içinde `deleted_at IS NULL`, `status='DERDEST'` ve
   `case_foys`'ta hiç föyü olmayan kartlar. İki sayfa: **`MALPRAKTIS_ADAYI`** (`file_type` ∉ {İcra, Tahkim, Vergi})
   ve **`KAPSAM_DISI_TUR`** (İcra/Tahkim/Vergi — bilgi). Kolonlar: HukuDok Kart No (`tracking_no`), DosyaNo
   (`klasor_no_2`), Müvekkil(ler) (`case_parties` CLIENT adları `; ` ile), Ana Tür (`file_type`), Yargı Birimi
   (`judicial_unit`), Esas (`esas_no`), Mahkeme (`court`), Dava Tarihi (`opening_date`), İş Kabul (`acceptance_date`),
   Karşı Taraf, Sorumlu Avukat, Son Güncelleme (`updated_at`). Üçüncü sayfa `OZET`: tür × durum sayıları.
   Çıktı adı `HUKDOK_DERDEST_KARTLAR_<YYYY-MM-DD>.xlsx`, `--out` ile yol; openpyxl (repoda var).
2. Salt okunur: hiçbir yazma; `argparse`, `--limit` yok (tam liste). Konteynerde koşar
   (`docker compose exec -T backend python scripts/foysuz_derdest_kartlar.py --out /tmp/...`).
3. Test: sqlite fixture'ında 3 kart (föylü DERDEST, föysüz DERDEST Hukuk, föysüz DERDEST İcra, föysüz MAHZEN) →
   yalnız ikisi listelenir, doğru sayfalarda; kolon başlıkları; OZET sayıları.
4. Raporda lokal koşu sonucu: sayfa başına satır (beklenen ~175 / ~195; sapma varsa yaz).

## Kabul kriterleri

- [ ] Script salt okunur (grep: `commit(`/`add(`/`delete(`/`update(` yok), test geçer.
- [ ] Lokal koşu çıktısı raporda (satır sayıları + dosya yolu); föylü kart listede yok (test).
- [ ] Hub dosyalara dokunulmadı; ruff + mypy temiz; pytest özet satırı raporda.

## Doğrulama

- `docker compose exec -T backend python -m pytest`
- `docker compose exec -T backend python -m ruff check .`
- `docker compose exec -T backend python -m mypy`

## Rapor

**DURUM: TAMAM** (2026-09-08, gece kuyruğu işçisi)

### Yapılanlar

- `backend/scripts/foysuz_derdest_kartlar.py` (YENİ, salt okunur): `cases` içinde `deleted_at IS NULL` +
  `status='DERDEST'` + `case_foys`'ta satırı olmayan kartlar (`NOT EXISTS`, taraflar `selectinload` ile tek
  seferde). Üç sayfa: `MALPRAKTIS_ADAYI` (tür ∉ {İcra, Tahkim, Vergi}; tür boş olanlar da burada — bilinmeyen
  türü gizlememek için), `KAPSAM_DISI_TUR` (İcra/Tahkim/Vergi, bilgi), `OZET` (Ana Tür × sayfa + TOPLAM).
  12 kolon görev tanımındaki sırayla; müvekkil/karşı taraf `; ` ile alfabetik. `--out` dosya ya da dizin
  (dizinse `HUKDOK_DERDEST_KARTLAR_<YYYY-MM-DD>.xlsx` içine düşer); `--limit` yok.
- `backend/tests/test_g149_foysuz_derdest_kartlar.py` (YENİ, 23 test): sqlite fixture'ında föylü DERDEST,
  föysüz DERDEST Hukuk, föysüz DERDEST İCRA, föysüz MAHZEN + soft-silinmiş DERDEST + tür boş DERDEST → yalnız
  üçü listelenir, doğru sayfalarda; föylü/MAHZEN/silinmiş kart listede yok; kolon başlıkları; satır
  değerleri; OZET sayıları; Türkçe harfe dayanıklı tür eşlemesi (`İCRA`/`icra`/`TAHKİM`); dosya adı
  sözleşmesi; kaynak grep'i (`commit(`/`add(`/`delete(`/`update(` yok); boş DB'de üç sayfa yine yazılır.

### Kararlar

- **Kapsam işaretli föy de föydür:** `case_foys.kapsam_durumu` dolu olan föyün kartı "föysüz" sayılmaz —
  kart teslimde bir kez görünmüştür, ekibin listesinde zaten vardır (test: `test_kapsam_isaretli_foy_de_foydur`).
- **Tür eşlemesi Türkçe-dayanıklı:** `str.lower()` "İ"yi bozar; `_tur_anahtari` önce İ→i, I→ı çevirir. Lokal
  veride `İcra` tek yazımla geliyor ama teslim/panel yazımı değişebilir.
- **`OZET` "tür × durum":** listedeki tüm kartlar DERDEST olduğundan "durum" ekseni sayfa (kapsam durumu)
  olarak yorumlandı: Ana Tür × {MALPRAKTIS_ADAYI, KAPSAM_DISI_TUR} + üç TOPLAM satırı.
- **Excel saat dilimi bilmez:** tz'li `updated_at` TR saatine çevrilip tz'siz yazılır (`_excel_zamani`);
  tarihler `yyyy-mm-dd` biçiminde.
- Hub dosyalara (`hukdok_aktarim.py`, `mukerrer_kart_raporu.py`, `models.py`, `managers/**`) dokunulmadı;
  `mukerrer_kart_raporu.py` yalnız desen için okundu.

### Lokal koşu (2026-09-08, lokal DB = 04.09 paketi uygulanmış kopya)

```
docker compose exec -T backend python scripts/foysuz_derdest_kartlar.py --out /tmp/g149/
  föysüz DERDEST kart : 370
  MALPRAKTIS_ADAYI    : 175   (Hukuk 139 · İdare 14 · Ceza 7 · Arabuluculuk 6 · Danışmanlık 3 ·
                              (boş) 3 · Hukuk Dava 2 · Savcılık 1)
  KAPSAM_DISI_TUR     : 195   (İcra 195; Tahkim/Vergi 0)
```

Sapma YOK: 06.09 ad-hoc ölçümüyle (370; İcra 195, Hukuk 139) birebir. xlsx doğrulandı: sayfa satırları
175 / 195 / OZET 12, 12 kolon, örnek satırda 12/12 hücre dolu. Dosya host'a kopyalandı (repo dışı):
`C:\hukdok-veri\rapor\HUKDOK_DERDEST_KARTLAR_2026-09-08.xlsx` (41,6 KB) — ekibe ek olarak bu gönderilir.
Prod'daki sayı bu koşuyla farklı olabilir (lokal DB kopyası + lokal G126/G127 sonuçları); ekibe göndermeden
önce prod'da koşulmalı: `docker compose exec -T backend python scripts/foysuz_derdest_kartlar.py --out /tmp/`
+ `docker compose cp`.

### Doğrulama

- `pytest`: **2750 passed, 3 skipped** (159 s) — G149 dosyası 23 test.
- `ruff check .`: All checks passed. `mypy`: Success: no issues found in 61 source files.
- Salt okunurluk grep'i: `commit(`/`add(`/`delete(`/`update(` — 0 eşleşme (test de kilitler).

### İzlenecekler / NOT

- NOT (tuzak): Git Bash `docker compose exec ... --out /tmp/x/` argümanını Windows yoluna çevirir
  (`C:/Users/.../Temp/x/`) ve konteyner bu yolu `/app/C:/...` altında yaratır — bind-mount yüzünden repoya
  `backend/C:/` dizini düşer. Çözüm `MSYS_NO_PATHCONV=1` (bu koşuda görüldü, dizin silindi, commit'e girmedi).
- Prod'da koşu + ekibe gönderim insan adımı (plan §6 "370 liste eki (P1)").
- "Hukuk Dava" (2) ve "Savcılık" (1) türleri `file_types` kanonik listesinde değil — yazım temizliği
  plan §6.2 kapsamında, bu görevin dışında.
