# G006 — FAZ 6-A: repo kökü CLAUDE.md + docs/ reorganizasyonu

- **Bant:** backend (ana dizin — taşımalar repo genelinde referans günceller, worktree'de yapılamaz)
- **Bağımlı:** -
- **Dosya kapsamı:** `CLAUDE.md` (yeni), `docs/**` (taşıma + README'ler), referans güncellemesi
  gereken dosyalar: `otomasyon/gece-kosusu.ps1`, `.claude/skills/faz-devam/SKILL.md`,
  `.claude/skills/faz-denetle/SKILL.md` (takip dosyası yolu geçiyor)
- **Dokunma:** `backend/**`, `frontend/**` (kod DEĞİŞMEZ), `gorevler/KUYRUK.md`,
  `otomasyon/kuyruk-kosusu.ps1` (takip yoluna referans VERMEZ — dokunma)

## Hedef
Plan FAZ 6.1 + 6.2: AI-dostu repo. Her sıfır-context oturum mimariyi CLAUDE.md'den alsın;
37 tarihli plan/rapor dosyası arşive insin ve **eski bulgular güncel bilgi sanılmasın**.

## Adımlar
1. **docs/ reorganizasyonu (git mv ile — HİÇBİR ŞEY SİLME):**
   - `docs/mimari/` (boş iskelet + kısa README: "yaşayan dokümanlar, G007 dolduracak"),
     `docs/plan/`, `docs/arsiv/`, `docs/kararlar/` (boş iskelet + README).
   - Biten tarihli plan/rapor/denetim dosyaları → `docs/arsiv/`.
   - YAŞAYAN iki dosya → `docs/plan/`: `guvenilirlik-sertlestirme-uygulama-takibi.md` +
     `guvenilirlik-sertlestirme-plani-2026-08-04.md`. (KARAR verildi: taşınacaklar.)
   - `docs/arsiv/README.md`: "Bu klasör tarihsel kayıttır; plan/rapor/bulgular yazıldıkları
     günün fotoğrafıdır ve GÜNCEL DURUMU YANSITMAZ. Güncel mimari: docs/mimari/. Bu klasörü
     güncel bilgi kaynağı olarak KULLANMA."
2. **Referans güncelleme (taşımayla AYNI commit):** yukarıdaki üç dosyadaki takip yolu +
   taşınan md'lerin birbirine göreli linkleri. Bekçi kanıtı: repo genelinde eski yollara
   (`docs/guvenilirlik-...`) işaret eden referans kalmadığını grep ile göster, Rapor'a yaz.
3. **CLAUDE.md (repo kökü):** Bölümler — mimari özet (iki katmanlı nginx, 2 worker + lider
   kilidi, tenant_id=NULL havuz, process→confirm→outbox→hukukbot akışı, failed/error stream
   sözleşmesi); komutlar (backend pytest KONTEYNERDE / frontend vitest HOST; lokal stack;
   deploy prosedürü işaretçisi); kritik tuzaklar (PS5.1 UTF-8, OneDrive+Docker build cache,
   doctype `_` padding, AVG TLS, backend bind-mount gerçeği, log sözleşmesi WARNING/tek-ERROR);
   doküman haritası (mimari=güncel, arsiv=TARİHSEL — okumaya karşı uyarı, gorevler/otomasyon);
   kod konvansiyonları. **ALTIN KURAL: yazdığın her operasyonel iddiayı koddan/koşarak doğrula;
   eski md'lerden veya ezberden KOPYALAMA. Doğrulayamadığını yazma, Rapor'da listele.**

## Kabul kriterleri
- [x] Taşımalar git mv ile; silinen dosya YOK; arsiv README şerhi var.
- [x] Eski takip/plan yollarına işaret eden referans kalmadı (grep kanıtı Rapor'da).
- [x] CLAUDE.md'deki her komut/yol/port iddiası doğrulanmış; doğrulanamayanlar yazılMAMIŞ.
- [x] Kod dosyalarında davranış değişikliği yok (backend/frontend diff'i BOŞ).

## Doğrulama
- Test koşusu gerekmez (kod değişmiyor); `git diff --stat` yalnız docs/CLAUDE.md/referans
  dosyalarını göstermeli. Şüphen varsa konteynerde pytest koşabilirsin (859+2 taban).

## Rapor

TAMAM — 2026-08-11. İki oturumda bitti: ilk oturum (izin kapısına takılıp BLOKE bıraktı)
README iskeletlerini yazdı ve 5 kararı aldı; bu oturum işi devraldı, kararları bağımsız
doğrulayıp uyguladı, taşımaları + referansları + CLAUDE.md'yi tamamladı.

### Yapılanlar
1. **Taşımalar (git mv, silinen dosya YOK):** `docs/` kökündeki 39 md → 37'si
   `docs/arsiv/`, 2 yaşayan dosya (`guvenilirlik-sertlestirme-uygulama-takibi.md`,
   `guvenilirlik-sertlestirme-plani-2026-08-04.md`) `docs/plan/`. docs kökünde 0 md kaldı;
   `docs/hukukbot-aktarim/` yerinde (karar 2). git status: 39 satırın tümü `R` (rename).
2. **Referans güncellemeleri (aynı commit):**
   - `otomasyon/gece-kosusu.ps1:6` **ve :33** → `docs/plan/...` (33. satır önceki oturumun
     listesinde YOKTU ve fonksiyoneldi: `$takipDosya = Join-Path ... "docs\guvenilirlik-..."` —
     güncellenmeseydi gece koşucusu takip dosyasını bulamazdı; backslash'li olduğu için
     düz `docs/` grep'ine takılmamış).
   - `otomasyon/README.md:5`, `.claude/skills/faz-devam/SKILL.md:11,16`,
     `.claude/skills/faz-denetle/SKILL.md:13` → `docs/plan/...`
   - `.github/workflows/ci.yml:1`, `infra/scripts/net-watchdog.sh:3`,
     `infra/scripts/mem-watch.sh:7` → `docs/arsiv/...`
   - Arşive inen 6 dosyada koda giden göreli linkler bir seviye derinleştiği için kırılmıştı →
     `](../` → `](../../` (dava-baglanti-gelistirme-raporu, guvenlik-incelemesi-2026-05-10,
     idor-incelemesi-2026-05-10, regex-denetim-raporu-2026-07-10, toplu-yukleme-duzeltme-plani,
     toplu-yukleme-incelemesi-2026-05-17). Arşiv dosyalarının birbirine linkleri aynı klasöre
     birlikte indikleri için zaten geçerli; metin içi eski `docs/...` yazımları tarihsel içerik
     olarak bilerek bırakıldı (arsiv README'ye okuma notu eklendi).
3. **`docs/arsiv/README.md` güncellendi:** eşleme tablosuna üçüncü backend atfı eklendi
   (`backend/routes/case_intake.py:86` → eml-intake planı; önceki oturum bunu kaçırmıştı),
   tablo yalnız HÂLÂ bayat atıf taşıyan `backend/**` satırlarına indirildi (ci.yml/infra
   güncellendiği için), arşiv-içi eski yol yazımları için okuma notu eklendi.
4. **`CLAUDE.md` (repo kökü, yeni):** mimari özet (iki katmanlı nginx, 2 worker + lider
   kilidi, tenant_id=NULL havuz, process→confirm→outbox→hukukbot, failed/error stream
   sözleşmesi), komutlar (pytest konteynerde / vitest host'ta / deploy işaretçisi),
   kritik tuzaklar (PS5.1 UTF-8, bind-mount YOK, OneDrive build cache, doctype padding,
   AVG TLS, pytest çift -q, log sözleşmesi), doküman haritası (arsiv=TARİHSEL uyarısı),
   kod konvansiyonları, ALTIN KURAL.

### Önceki oturumun kararları — doğrulama sonucu
Karar 1 (2+37 taşıma listesi), 2 (hukukbot-aktarim yerinde), 4 (G001..G005'e dokunma)
bağımsız grep'le doğrulandı ve aynen uygulandı. Karar 3 ve 5 EKSİKTİ, düzeltilerek uygulandı:
karar 3'te backend'in üçüncü atfı (`routes/case_intake.py:86`), karar 5'te
`gece-kosusu.ps1:33` (fonksiyonel yol) atlanmıştı — ikisi de yukarıda kapatıldı.
Ayrıca `deploy.sh:11` plan adını yol ÖNEKSİZ anıyor (kırılan referans değil) — dokunulmadı.
G007/G008 görev dosyaları zaten yeni yapıya (`docs/plan/`, `docs/mimari/`) referans veriyor.

### İşaretlenen üç şüpheli iddianın koddan/koşarak teyidi (ALTIN KURAL)
1. **Konteyner nginx portu:** `listen 80` (`nginx.conf:8`); 8080 host portudur
   (`docker-compose.yml` frontend `8080:80`). "Konteyner nginx 8080 dinler" yazımı YANLIŞ
   olurdu — CLAUDE.md'ye doğrusu yazıldı.
2. **Backend kaynak bind-mount'u:** prod compose'ta YOK — yalnız `backend-data` volume'u;
   compose yorumu açık ("Kaynak kod mount'u YOK (G6): prod imajdaki kodu çalıştırır",
   `docker-compose.yml:54-56`). Hot-reload yalnız gitignore'lu override ile.
3. **Test sayıları:** koşularak alındı (aşağıda). Ek kanıtlanmış tuzak: `pyproject.toml`
   `addopts="-q"` + komuttaki `-q` = `-qq` özet satırını tamamen yutuyor — üç koşu
   "sayısız" bitti, sebep bu çıktı; CLAUDE.md'ye tuzak olarak işlendi.

### Test sonuçları
- Backend (konteynerde, `python -m pytest`): **859 passed, 2 skipped** — görevdeki
  "859+2 taban" ile birebir; exit 0 (üç koşu, 2026-08-11).
- Frontend (host, `npm --prefix frontend test`): **299 passed, 23 test dosyası**, 10.1s.
- `git diff --stat -- backend frontend` (staged+unstaged): **BOŞ** — kod davranışı değişmedi.

### Grep kanıtları (kabul kriteri 2)
- `git grep -n "docs.guvenilirlik-sertlestirme" -- ':!docs' ':!gorevler'` → **0 eşleşme**
  (desen `/` ve `\` ayracını birlikte yakalar; `gorevler/` hariç tutuldu — G001..G005 + bu
  dosyanın görev metni kapanmış işlerin tarihsel kaydıdır, karar 4).
- Eski arşiv yolları (`docs/otonom-dava-acma...`, `docs/eml-intake...`,
  `docs/gelistirme-plani-2026-07-07`, `docs/sunucu-sertlestirme...`, `docs/toplu-yukleme...`,
  `docs/guvenlik-incelemesi...`, `docs/idor-incelemesi...`, `docs/sistem-teknik-raporu...`)
  için `-- ':!docs/arsiv' ':!gorevler'` taraması → yalnız 3 bilinçli istisna:
  `backend/case_intake_analyzer.py:7`, `backend/services/case_intake.py:7`,
  `backend/routes/case_intake.py:86` (dokunma listesi; eşleme `docs/arsiv/README.md`
  tablosunda, düzeltme G007'ye devirli).

### İzlenecekler
- G007: `docs/mimari/` içeriğini koddan türetip dolduracak + 3 backend yorumundaki eski
  `docs/...` yollarını düzeltecek (eşleme tablosu hazır).
- CLAUDE.md'deki test sayıları 2026-08-11 fotoğrafıdır; sayı değişince güncellemek commit
  sahibinin işi (CLAUDE.md bunu zaten tarihle işaretliyor).
- Lokal konteynerde ruff/mypy kurulu DEĞİLDİ (yalnız pytest 8.3.5 elle kurulmuş);
  dev araçları gerektiğinde `pip install -r requirements-dev.txt` konteynerde koşulmalı —
  komut CLAUDE.md'ye yazıldı. (Bu görev kod değiştirmediği için lint koşusu gerekmedi.)
