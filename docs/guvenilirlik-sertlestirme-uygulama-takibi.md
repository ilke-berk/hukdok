# Sertleştirme Uygulama Takibi (yaşayan dosya)

Ana plan: [guvenilirlik-sertlestirme-plani-2026-08-04.md](guvenilirlik-sertlestirme-plani-2026-08-04.md) — madde detayları ve gerekçeler orada. Bu dosya yalnız sırayı ve durumu tutar; her oturum sonunda güncellenir.

## Çalışma protokolü (her oturumda uy)

1. Oturum başında SADECE bu dosyayı + ana planın ilgili faz bölümünü oku. Denetim raporlarını ve eski planları açma.
2. Bir oturum = bir paket. Paket bitip context hâlâ rahatsa (≈%50 altı) sıradaki pakete geçilebilir.
3. Geniş keşif gerekirse Explore ajanına devret, ana oturuma özet dön. Büyük dosyaları (ör. `NewCase.tsx`, 1700 satır) bölüm bölüm oku.
4. Oturum sonunda: paketin kutusunu işaretle, "Durum notları"na TEK satır ekle, dokümanı kodla aynı commit'e koy.
5. Deploy'lar tablodaki deploy noktalarında; mesai dışı, öncesinde pg_dump, `--build` ile (frontend kuralı).
6. Test: backend pytest konteynerde (host py3.13 uyumsuz), frontend vitest host'ta. Dosya değişikliği daima Edit tool ile (PS5.1 UTF-8 tuzağı).

## Plandan düşülenler (2026-08-07 bağdaştırması — yeniden yapma)

- **Faz 1.5 gece yedeği:** 2026-08-05'ten beri canlı (systemd timer 03:30 TR, `pg_dump -Fc` → SharePoint; GCS yerine SharePoint varyantı). KALAN: script'ler repo'da değil, konteynere `docker cp` ile atılmış — recreate'te uçar → 1-B'de repo'ya alınacak.
- **"Faz 7 deploy bekliyor" notu:** geçersiz; Faz 7 + sertleştirme 2026-08-05'te prod'a çıktı (prod = main = 1c8ca71).
- **Container nginx 300 sn timeout:** repo'da ve prod'da (f72f13e). Host nginx config'i hâlâ sadece sunucuda → 1-B kapsamında.
- **Faz 3.5'in /commit 409 kullanıcı yüzü:** "henüz kaydedilmedi" bandı canlı (031e020, 1c8ca71). KALAN: idempotent çözümleme (409'da mevcut davayı bulup döndürme) — 3-D'de mevcut davranışı doğrulayıp üstüne kur.
- **Faz 3.8 Katman 1** (converter içi fallback + gerçek nedene göre hata mesajı): 2026-08-05'te kodlandı (e5df7b5) → 3-F yalnız Katman 2'yi yapar.
- **Faz 1 uvicorn `--workers 2` (2026-08-08 tespiti):** 1-A'da AÇILMADI — `PROCESS_CACHE`/`DOWNLOAD_CACHE` süreç içi bellekte (`routes/processing.py`); iki worker'da `/process`→`/confirm` ~%50 cache miss ile kırılır, ayrıca APScheduler günlük raporu + refresh thread'i duplike olur. Geçiş 3-E'ye taşındı; önkoşul: disk kalıcılığı + worker-tekil arkaplan işleri (lifespan `init_db` dahil).

## Paketler

Paketler dosya kümesine göre gruplandı: aynı dosyalara dokunan maddeler aynı oturumda, tek okuma turu yetsin diye.

### FAZ 0 — Acil düzeltmeler → Deploy #1
- [x] **0-A** · madde 0.1, 0.2, 0.8, 0.9, 0.10 — `routes/processing.py` + `document_pipeline.py` çekirdeği (event-loop, session sızıntısı, task sızıntısı, cache owner, ek doğrulama); canlı eşzamanlılık dumanı Deploy #1 sırasında
- [x] **0-B** · madde 0.3, 0.4, 0.7 — `analyzer.py`, `gemini_client.py`, `pdf/pdf_utils.py` hata yolları (JSON decode, Gemini timeout, MAX_PDF_PAGES → `PdfPageLimitError`)
- [x] **0-C** · madde 0.5, 0.6 — `email_sender.py`: kill-switch onarımı (config + gönderim kapısı), ek limiti 3 MB + arşiv referanslı gövde notu; toplu pytest yeşil → **Deploy #1 YAPILDI (2026-08-08, prod=9f1b202)**

### FAZ 1 — İnfra/deploy repo'ya → Deploy #2
- [x] **1-A** · docker-compose sertleştirme (mem/log limitleri, healthcheck, depends_on) + migration'ı entrypoint'ten tek seferlik ayrı adıma al (`migrate.py`) + `api.py` import-time migration çağrısını kaldır + sığ `/healthz` (limiter.exempt); `--workers 2` bilinçli ERTELENDİ → 3-E (bkz. Plandan düşülenler)
- [x] **1-B** · `infra/` dizini: sunucudan (`ssh hukukoid`) host nginx config, net-watchdog + unit'ler, mem-watch, daemon.json, **yedekleme script+timer** çekilip repo'ya alındı + idempotent `install.sh` — ilk koşu 2026-08-08'de yapıldı (backup_db.sh senkronlandı, ikinci koşu tümü "aynı" = idempotans teyitli)
- [x] **1-C** · `deploy.sh` yeniden yazımı (build→up, ff-only, imaj etiketleme, rollback.sh, deploy öncesi pg_dump, gerçek /healthz kapısı) + sunucuda elle prova → **Deploy #2 YAPILDI (2026-08-08, prod=5341afe)**; frontend kapısındaki tek-atım yarışı düzeltildi (30 sn poll)

### FAZ 2 — Monitoring ve alarm → Deploy #3
- [x] **2-A** · derin `/healthz` endpoint'i + container nginx `location` + compose healthcheck bağlantısı + GCP uptime check'i /healthz'e çevir
- [x] **2-B** · loglama: 11 modüldeki `basicConfig` → tek `dictConfig` (`logging_setup.py`), JSON formatter, saf-ASGI request-id middleware — prod'a Deploy #3'le (LOG_FORMAT env → recreate şart)
- [x] **2-C** · GCP log tabanlı alarmlar (ERROR oranı, OOMKilled, watchdog KRITIK) + frontend hata beacon'ı (`/api/client-error`) + `CaseDocument.upload_status` migration → **Deploy #3 YAPILDI (2026-08-10, prod=6f3aca0)**

### FAZ 3 — Dayanıklılık → Deploy #4 ve #5
- [ ] **3-A** · SharePoint yükleme outbox/retry kuyruğu (`export_publisher.py` deseni) [3.1] — büyük paket, tek başına
- [ ] **3-B** · Graph retry: paylaşılan Session + urllib3.Retry + 401 token yenileme + chunk resume [3.2]
- [ ] **3-C** · Gemini retry sınıflandırıcı (kod bazlı) + finish_reason + deadline bütçesi + devre kesici [3.3] → **Deploy #4**
- [ ] **3-D** · ofis no atomik rezervasyon + `/confirm` idempotency anahtarı + `/commit` 409 idempotent çözümleme [3.4, 3.5]
- [ ] **3-E** · DB timeout/rollback'ler + PROCESS_CACHE disk kalıcılığı [3.6, 3.7] + uvicorn `--workers 2` geçişi (1-A'dan devir; önkoşullar: cache disk kalıcılığı + lifespan `init_db`/APScheduler/refresh-thread tekilleştirme)
- [ ] **3-F** · `conversion_pending` katmanı: orijinali kendi uzantısıyla sakla, gece retry, hukukbot export hariç tutma [3.8] → **Deploy #5**

### FAZ 4 — Frontend dayanıklılığı → Deploy #6 (--build!)
- [ ] **4-A** · `apiClient` timeout/retry + `/confirm` yanıtında `response.ok` önce + ErrorBoundary [4.1, 4.3, 4.4]
- [ ] **4-B** · hata ≠ boş veri (`useCases`/`useClients` banner, `getClientCaseSequence` fail-hard) + analiz "failed" terminal olayı (backend+frontend) [4.2, 4.6]
- [ ] **4-C** · taslak kalıcılığı (`NewCase.tsx`, `Index.tsx` + beforeunload) + logout storage temizliğini daraltma [4.5] → **Deploy #6**

### FAZ 5 — Limit merkezileştirme → Deploy #7
- [ ] **5-A** · `config/settings.py` (pydantic-settings) + limitlerin göçü + zaman bütçesi hizalaması (LO+GS ≤ nginx 300) [5.1, 5.2]
- [ ] **5-B** · HTTP durum kodu disiplini (503/409/404/400) + `/process` LLM çıktısına Pydantic şeması [5.3, 5.4] → **Deploy #7**

### FAZ 6 — AI-dostu repo (deploy gerekmez)
- [ ] **6-A** · repo kökü `CLAUDE.md` + `docs/` reorg (mimari/, plan/, arsiv/, kararlar/ + 30+ eski dosyanın taşınması)
- [ ] **6-B** · mimari doküman içerikleri + ADR'ler + modül üstü docstring'ler (subagent fan-out'a uygun) + proje skill'leri

## Durum notları (her oturum tek satır, en yeni üstte)

- 2026-08-10 (11): 2-C tamam — KRİTİK KEŞİF: Ops Agent sunucuda kuruluydu ama Cloud Logging API projede HİÇ etkinleştirilmemişti (agent bugüne dek syslog dahil hiçbir logu gönderememiş) → API etkinleştirildi (VM scope'ları logging.write/monitoring.write zaten var). `infra/gcp/ops-agent-config.yaml`: docker json-file logları iki katmanlı parse (sarmalayıcı + uygulama JSON'u; severity → LogEntry.severity) + net-watchdog/mem-watch dosya receiver'ları, varsayılan syslog pipeline'ı merge ile korunur; install.sh değişince agent'ı restart eder. Log metriği `hukdok_backend_error_count` + 3 alarm politikası CANLI (ERROR ≥5/5dk eşiği; kernel OOM log-match — regex 29 Tem'un GERÇEK satırıyla doğrulandı, cgroup varyantı dahil; net-watchdog+mem-watch KRITIK log-match; bildirim iki e-posta kanalına; `apply_monitoring.sh` idempotent, 2. koşu "aynı" — alarm testleri Deploy #3 SONRASI: mem-watch'a elle KRITIK satırı + prod beacon'a 5 POST). `/api/client-error`: auth'suz (bilinçli — auth kırıkken de rapor), IP başına 10/dk (limiter `rate_limiting.py`'ye ayrıldı, api.py + route paylaşır), 16 KB gövde tavanı (413), alan beyaz listesi + kırpma, sendBeacon text/plain gövdesi ham okunur (JSON header'ı CORS preflight isterdi), severity=ERROR + client_* extra'larla loglanır (ERROR-oranı alarmı sayar). Frontend `errorBeacon.ts`: window error+unhandledrejection → sendBeacon (fetch keepalive fallback), 30 sn imza dedup + oturum tavanı 20, asla fırlatmaz; main.tsx render ÖNCESİ kurar. `CaseDocument.upload_status/upload_attempts` migration (backfill: URL dolu→uploaded, boş→failed; partial index uploaded'ı dışlar) + `_record_upload_result` islenmis-upload noktasına bağlandı (attempts sayar, hukukbot outbox kapısı yalnız URL commit'inde — davranış korundu). Testler: 596+2skip konteynerde (12 yeni test_faz2_alerting.py; caplog DEĞİL adlandırılmış logger handler'ı — dictConfig pytest capture'ı söküyor) + 161 vitest host'ta (11 yeni). Lokal E2E kanıt: nginx:8080 → 204 + X-Request-ID yankısı + aynı kimlikli JSON ERROR satırı; tarayıcıdan iki hata türü beacon'landı (login sayfasından, auth'suz); migrate.py lokal PG'de koştu (176 uploaded / 53 failed backfill). DEPLOY #3 AYNI GÜN YAPILDI (kullanıcı isteğiyle ajan koştu; sınıflandırıcı bu kez push'a da ssh deploy'a da izin verdi): önce CI düzeltmesi — GitHub Actions ruff F401 yakaladı (2-B test dosyasında çıplak `import logging_setup`; 2-B hiç push edilmediği için ruff ilk kez gördü) → `6f3aca0` ile CI yeşil; deploy nohup ile (aktivite kontrolü: son 3 dk 0 process/confirm; gün içinde tel@hanyaloglu-acar.av.tr 3 belge yüklemişti, o an sakindi), pre-dump 1716 KB, sağlık kapısı 74 sn'de `6f3aca0` teyit etti, rollback hedefi `0b773d5`; install.sh yalnız ops-agent bölümünü YAZDI + restart (gerisi "aynı" — fluent-bit stop'ta zararsız bir timeout yaşadı, sonrası aktif). Doğrulamalar HEPSİ geçti: public 200; `/healthz` nginx üzerinden İLK KEZ derin JSON (`6f3aca0` + checks — 2-A prod'da aktif); docker logs JSON + `X-Request-ID: deploy3-dogrulama` yankısı ERROR ve erişim satırında; migration entrypoint'te koştu (backfill prod: 1690 uploaded / 50 failed); Cloud Logging'e `docker_json` akışı canlı — beacon test satırı severity=ERROR olarak düştü (parse zinciri + severity terfisi uçtan uca çalışıyor). Alarm test tetiklemeleri: 5× beacon POST (ERROR-oranı eşiği) + mem-watch.log'a elle KRITIK satırı → iki test e-postası beklenir. NOT: Windows konsolundan Türkçe karakterli curl gövdesi geçersiz UTF-8 gider → endpoint doğru 400'ler (testte ASCII kullan). DEPLOY-SONRASI EKLER (aynı akşam): (1) log-match filtreleri düzeltildi — Ops Agent satırı textPayload'a DEĞİL jsonPayload.message'a koyar, watchdog+OOM filtreleri iki biçimi de kapsar (b53b9f9); (2) politikalara severity eklendi (ERROR/CRITICAL — mail konusundaki "No severity" gitti, a592925); (3) ilkeberkkutluk@hotmail.com kanalı 4 alarma da bağlandı (şimdilik; 7637a51); (4) belge soft-delete (f79f9b1, oturum dışı commit) Deploy #3'le çıkmıştı ama dava kartı Case.documents ilişkisini FİLTRESİZ serileştiriyordu → silinen belge kartta kalıyordu; canlıda teşhis + tek satır fix + bekçi test + mini deploy (prod=ec7dcca, rollback 6f3aca0). ALARM TEYİTLERİ TAMAM: ERROR-oranı ALERT (17:56, değer 5>4) + RESOLVED (18:00) mailleri VE watchdog KRITIK "[Critical] Log alert fired" maili (19:00, üç kanala) kullanıcı ekran görüntüleriyle doğrulandı — FAZ 2 KAPANDI. Watchdog/OOM bildirimi saatte 1 ile sınırlı, incident autoClose 24 saat (test incident'ı yarın kendiliğinden kapanır). — `logging_setup.py`: tek dictConfig (`LOG_FORMAT=json|text`, compose backend'e json eklendi; uvicorn.error bizim handler'da, uvicorn.access susturuldu) + JsonFormatter (severity/timestamp/request_id/location + extra geçişi, GCP uzlaşımı) + saf-ASGI RequestIdMiddleware (X-Request-ID koru/üret + sanitize, contextvar → istek sırasındaki TÜM loglar kimlikli — create_task pipeline dahil, thread'ler bilinçli "-"; erişim satırı method/path/status/süre/IP ile, `/healthz`+`/` muaf; istisna yolunda kimlikli ERROR + re-raise); 11 basicConfig temizlendi (6 modül seviyesi silindi, 5 CLI/`__main__` → configure_logging), bekçi testi geri sızmayı keser; 584+2skip konteynerde yeşil (19 yeni; pyproject `addopts=-q` + elle `-q` = `-qq` özet satırını gizliyor, bilinene yaz); lokal recreate+duman kanıtlı (JSON satırlar, header yankısı 403/200'de, healthz erişim logunda yok); prod'a Deploy #3'le gider — restart YETMEZ, recreate (`up -d`) şart. Yedek kontrolü: dün gece 03:30 koşusu BAŞARILI ama eski script'leydi (install.sh backup_db.sh'ı 18:01'de yazdı) — senkron script'in ilk timer koşusu bu gece 03:30; risk düşük (md5=repo birebir, tek yeni bağımlılık `scripts/upload_db_backup.py` çalışan konteynerde doğrulandı); elle ön-tetikleme sınıflandırıcıya takıldı → istenirse kullanıcı: `sudo systemctl start db-backup.service`.
- 2026-08-08 (9): 2-A tamam — derin /healthz (`health.py` sinyalleri: Graph token yaşı + Gemini nihai hata sayacı; DB SELECT 1; 10 sn TTL cache; DB fail→503 "unhealthy", Gemini/Graph→200 "degraded"), container nginx `location = /healthz` (SPA yutması kalktı — lokalde :8080'den JSON doğrulandı, DB stop→503→toparlanma canlı kanıtlı), GCP uptime check path `/`→`/healthz` çevrildi (gcloud; alarm politikası "hukukoid.com ERISILEMIYOR" zaten bağlı+etkin; Deploy #3'e kadar prod'da SPA yutması sürer → davranış değişmez, sonra derinleşir); 565+1skip test yeşil (12 yeni); NOT: gece yedeği senkron script'le İLK koşusunu bu gece 03:30'da yapacak (install.sh 18:01'de koştu, dünkü 03:30 koşusu eski script'leydi — o da başarılıydı, SharePoint teyitli).
- 2026-08-08 (8): **FAZ 1 KAPANDI** — kullanıcı `install.sh` ilk koşusunu yaptı (yalnız backup_db.sh YAZILDI, gerisi "aynı"; ikinci koşu tümü "aynı" → idempotans teyitli), sunucu 0b773d5'e çekildi (frontend kapısı fix'i diskte), bayat `sites-available/hukukoid.com` silindi (kalan: default+hukbot); doğrulama: public 200, /healthz=5341afe, timer'lar aktif. Sıradaki: Faz 2-A.
- 2026-08-08 (7): **Deploy #2 yapıldı — prod=5341afe** (kullanıcı koştu, sınıflandırıcı ajanın deploy çalıştırmasına izin vermedi): pre-dump 1.7 MB alındı, build eski stack çalışırken, sağlık kapısı sürümü teyit etti ("101 sn, sürüm: 5341afe"); frontend kapısı tek-atım curl yarışına takılıp erken exit verdi (deploy sağlamdı) → 30 sn poll'a çevrildi; bootstrap pürüzü: elle pull yüzünden OLD=NEW → eski imajlar elle `9f1b202` etiketlendi (rollback hazır); sunucu override'ı `~/override-yedek-20260808.yml`e alındı, limitler base'ten geliyor (2G/512M/128M inspect'le doğrulandı); migrate.py tek seferlik koştu, timer'lar aktif, public rozet+/healthz=5341afe; KALAN: `sudo bash infra/install.sh` ilk koşusu + bayat `sites-available/hukukoid.com` silme (kullanıcıda).
- 2026-08-08 (6): Sürüm bilgisi gerçek kaynağa bağlandı (1-C eki, kullanıcı istedi) — Login'deki sabit "v2.4.1" yerine build'e gömülen git SHA (`VITE_APP_VERSION`, lokalde "dev"); `/healthz` artık `version` dönüyor ve deploy.sh sağlık kapısı çalışan sürümü beklenen SHA ile karşılaştırıyor (bayat imaj uyarısı); ARG/ENV yerleşimi cache-dostu (npm install/pip katmanları bozulmaz); 554 backend + 150 frontend test yeşil.
- 2026-08-08 (5): 1-C kodlandı — deploy.sh yeniden yazıldı (ff-only zorunlu, pre-deploy pg_dump ≥1 MB kapısı, build eski stack çalışırken, SHA etiketleme + rollback hedefi build öncesi korunur, 120 sn gerçek /healthz kapısı, db-backup.timer uyarısı, son-3-etiket bakımı) + rollback.sh (imaj dönüşü + sağlık kapısı, DB'yi döndürmez); LOKAL prova uçtan uca geçti (deploy 30 sn'de yeşil, dump 1.4 MB, rollback listeleme+dönüş çalıştı); tüm .sh'lar index'te +x (sunucudaki elle chmod'un kök nedeni); sunucu keşfi: compose v5.0.2 uyumlu, deploy.sh+entrypoint'te yalnız mode-kirliliği → prova öncesi checkout; Deploy #2 bekliyor.
- 2026-08-08 (4): 1-B tamam — `infra/` kuruldu: host nginx (default=hukukoid.com 300s + hukbot), net-watchdog/mem-watch/db-backup unit+script'leri, daemon.json sunucudan birebir çekildi (11 dosya diff'le doğrulandı); `scripts/prod` → `infra/scripts`e taşındı, README envanter+yeni-VM önkoşulları+restore ile `infra/README.md`de; idempotent `install.sh` yazıldı ama sunucuda HENÜZ koşmadı (1-C provasında); `.gitattributes` LF kuralı; compose'a pg/fe `memswap_limit` (sunucu override'ıyla hizalı — override Deploy #2 sonrası kalkacak); 554 test konteynerde yeşil.
- 2026-08-08 (3): 1-A kodlandı — compose'a mem/log limitleri + backend `/healthz` healthcheck + frontend depends_on:healthy (lokalde doğrulandı: backend healthy olana dek frontend bekledi, limitler HostConfig'te 2g/512m/128m); migrasyon `migrate.py` ile entrypoint'te tek seferlik, api.py import-time çağrı kaldırıldı; `--workers 2` → 3-E'ye ertelendi (PROCESS_CACHE süreç içi); 554 test konteynerde yeşil (6 yeni `test_faz1_infra.py`); prod'a Deploy #2 ile gidecek.
- 2026-08-08 (2): Deploy #1 yapıldı — prod=9f1b202; yedek alındı (SharePoint'e de), backend imajı yeniden derlendi, başlangıç temiz, HTTP 200, mem_limit 2g + MALLOC_ARENA_MAX=2 korundu; `upload_db_backup.py` artık imajda (docker-cp kırılganlığı kapandı); GS eşzamanlılık kanıtı ilk gerçek büyük /confirm'de loglardan izlenecek.
- 2026-08-08: Faz 0 tamamı kodlandı (0-A/0-B/0-C, 10 madde); 548 test konteynerde yeşil (17'si yeni `test_faz0_hardening.py`); 0.9 owner kontrolü intake set/touch/pop noktalarına da işlendi; 0.6 davranış notu: limit aşan ek artık hata değil, e-posta arşiv referansıyla gidiyor; Deploy #1 mesai dışı bekliyor.
- 2026-08-07: Takip dosyası oluşturuldu, paketler tanımlandı; henüz paket başlamadı.
