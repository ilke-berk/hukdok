# Sertleştirme Uygulama Takibi (yaşayan dosya)

Ana plan: [guvenilirlik-sertlestirme-plani-2026-08-04.md](guvenilirlik-sertlestirme-plani-2026-08-04.md) — madde detayları ve gerekçeler orada. Bu dosya yalnız sırayı ve durumu tutar; her oturum sonunda güncellenir.

## Çalışma protokolü (her oturumda uy)

1. Oturum başında SADECE bu dosyayı + ana planın ilgili faz bölümünü oku. Denetim raporlarını ve eski planları açma.
2. Bir oturum = bir paket. Paket bitip context hâlâ rahatsa (≈%50 altı) sıradaki pakete geçilebilir.
3. Geniş keşif gerekirse Explore ajanına devret, ana oturuma özet dön. Büyük dosyaları (ör. `NewCase.tsx`, 1700 satır) bölüm bölüm oku.
4. Oturum sonunda: paketin kutusunu işaretle, "Durum notları"na TEK satır ekle, dokümanı kodla aynı commit'e koy.
5. Deploy'lar tablodaki deploy noktalarında; mesai dışı, öncesinde pg_dump, `--build` ile (frontend kuralı).
6. Test: backend pytest konteynerde (host py3.13 uyumsuz), frontend vitest host'ta. Dosya değişikliği daima Edit tool ile (PS5.1 UTF-8 tuzağı).

## Plandan düşülenler (2026-08-07 bağdaştırması — yeniden yapma)

- **Faz 1.5 gece yedeği:** 2026-08-05'ten beri canlı (systemd timer 03:30 TR, `pg_dump -Fc` → SharePoint; GCS yerine SharePoint varyantı). KALAN: script'ler repo'da değil, konteynere `docker cp` ile atılmış — recreate'te uçar → 1-B'de repo'ya alınacak.
- **"Faz 7 deploy bekliyor" notu:** geçersiz; Faz 7 + sertleştirme 2026-08-05'te prod'a çıktı (prod = main = 1c8ca71).
- **Container nginx 300 sn timeout:** repo'da ve prod'da (f72f13e). Host nginx config'i hâlâ sadece sunucuda → 1-B kapsamında.
- **Faz 3.5'in /commit 409 kullanıcı yüzü:** "henüz kaydedilmedi" bandı canlı (031e020, 1c8ca71). KALAN: idempotent çözümleme (409'da mevcut davayı bulup döndürme) — 3-D'de mevcut davranışı doğrulayıp üstüne kur.
- **Faz 3.8 Katman 1** (converter içi fallback + gerçek nedene göre hata mesajı): 2026-08-05'te kodlandı (e5df7b5) → 3-F yalnız Katman 2'yi yapar.
- **Faz 1 uvicorn `--workers 2` (2026-08-08 tespiti):** 1-A'da AÇILMADI — `PROCESS_CACHE`/`DOWNLOAD_CACHE` süreç içi bellekte (`routes/processing.py`); iki worker'da `/process`→`/confirm` ~%50 cache miss ile kırılır, ayrıca APScheduler günlük raporu + refresh thread'i duplike olur. Geçiş 3-E'ye taşındı; önkoşul: disk kalıcılığı + worker-tekil arkaplan işleri (lifespan `init_db` dahil).

## Paketler

Paketler dosya kümesine göre gruplandı: aynı dosyalara dokunan maddeler aynı oturumda, tek okuma turu yetsin diye.

### FAZ 0 — Acil düzeltmeler → Deploy #1
- [x] **0-A** · madde 0.1, 0.2, 0.8, 0.9, 0.10 — `routes/processing.py` + `document_pipeline.py` çekirdeği (event-loop, session sızıntısı, task sızıntısı, cache owner, ek doğrulama); canlı eşzamanlılık dumanı Deploy #1 sırasında
- [x] **0-B** · madde 0.3, 0.4, 0.7 — `analyzer.py`, `gemini_client.py`, `pdf/pdf_utils.py` hata yolları (JSON decode, Gemini timeout, MAX_PDF_PAGES → `PdfPageLimitError`)
- [x] **0-C** · madde 0.5, 0.6 — `email_sender.py`: kill-switch onarımı (config + gönderim kapısı), ek limiti 3 MB + arşiv referanslı gövde notu; toplu pytest yeşil → **Deploy #1 YAPILDI (2026-08-08, prod=9f1b202)**

### FAZ 1 — İnfra/deploy repo'ya → Deploy #2
- [x] **1-A** · docker-compose sertleştirme (mem/log limitleri, healthcheck, depends_on) + migration'ı entrypoint'ten tek seferlik ayrı adıma al (`migrate.py`) + `api.py` import-time migration çağrısını kaldır + sığ `/healthz` (limiter.exempt); `--workers 2` bilinçli ERTELENDİ → 3-E (bkz. Plandan düşülenler)
- [x] **1-B** · `infra/` dizini: sunucudan (`ssh hukukoid`) host nginx config, net-watchdog + unit'ler, mem-watch, daemon.json, **yedekleme script+timer** çekilip repo'ya alındı + idempotent `install.sh` — ilk koşu 2026-08-08'de yapıldı (backup_db.sh senkronlandı, ikinci koşu tümü "aynı" = idempotans teyitli)
- [x] **1-C** · `deploy.sh` yeniden yazımı (build→up, ff-only, imaj etiketleme, rollback.sh, deploy öncesi pg_dump, gerçek /healthz kapısı) + sunucuda elle prova → **Deploy #2 YAPILDI (2026-08-08, prod=5341afe)**; frontend kapısındaki tek-atım yarışı düzeltildi (30 sn poll)

### FAZ 2 — Monitoring ve alarm → Deploy #3
- [x] **2-A** · derin `/healthz` endpoint'i + container nginx `location` + compose healthcheck bağlantısı + GCP uptime check'i /healthz'e çevir
- [x] **2-B** · loglama: 11 modüldeki `basicConfig` → tek `dictConfig` (`logging_setup.py`), JSON formatter, saf-ASGI request-id middleware — prod'a Deploy #3'le (LOG_FORMAT env → recreate şart)
- [x] **2-C** · GCP log tabanlı alarmlar (ERROR oranı, OOMKilled, watchdog KRITIK) + frontend hata beacon'ı (`/api/client-error`) + `CaseDocument.upload_status` migration → **Deploy #3 YAPILDI (2026-08-10, prod=6f3aca0)**

### FAZ 3 — Dayanıklılık → Deploy #4 ve #5
- [x] **3-A** · SharePoint yükleme outbox/retry kuyruğu (`export_publisher.py` deseni) [3.1] — büyük paket, tek başına; **Deploy #4 ÖN-ADIMI:** prod'da tek seferlik `docker compose exec -u root backend chown -R appuser:appuser /app/data` (volume kökü büyük olasılıkla root:root — appuser spool dizinini açamaz, kuyruk fallback'te kalır; Dockerfile G6 yorumu bunu zaten belgeliyordu)
- [x] **3-B** · Graph retry: paylaşılan Session + urllib3.Retry + 401 token yenileme + chunk resume [3.2]
- [x] **3-C** · Gemini retry sınıflandırıcı (kod bazlı) + finish_reason + deadline bütçesi + devre kesici [3.3] → **Deploy #4 YAPILDI (2026-08-10, prod=bfaca9d)**
- [x] **3-D** · ofis no atomik rezervasyon + `/confirm` idempotency anahtarı + `/commit` 409 idempotent çözümleme [3.4, 3.5]
- [x] **3-E** · DB timeout/rollback'ler + PROCESS_CACHE disk kalıcılığı [3.6, 3.7] + uvicorn `--workers 2` geçişi (1-A'dan devir; önkoşullar: cache disk kalıcılığı + lifespan `init_db`/APScheduler/refresh-thread/**upload-outbox worker** tekilleştirme — worker süreç-tekil olmazsa aynı satır N kez yüklenir)
- [x] **3-F** · `conversion_pending` katmanı: orijinali kendi uzantısıyla sakla, gece retry, hukukbot export hariç tutma [3.8] → **Deploy #5** (kod+test+duman tamam; deploy kullanıcı onayı bekliyor — prod İLK KEZ 2 worker'a geçecek, izlenecekler durum notu 16 sonu + 17)

### FAZ 4 — Frontend dayanıklılığı → Deploy #6 (--build!)
- [ ] **4-A** · `apiClient` timeout/retry + `/confirm` yanıtında `response.ok` önce + ErrorBoundary [4.1, 4.3, 4.4]
- [ ] **4-B** · hata ≠ boş veri (`useCases`/`useClients` banner, `getClientCaseSequence` fail-hard) + analiz "failed" terminal olayı (backend+frontend) [4.2, 4.6]
- [ ] **4-C** · taslak kalıcılığı (`NewCase.tsx`, `Index.tsx` + beforeunload) + logout storage temizliğini daraltma [4.5] → **Deploy #6**

### FAZ 5 — Limit merkezileştirme → Deploy #7
- [ ] **5-A** · `config/settings.py` (pydantic-settings) + limitlerin göçü + zaman bütçesi hizalaması (LO+GS ≤ nginx 300) [5.1, 5.2]
- [ ] **5-B** · HTTP durum kodu disiplini (503/409/404/400) + `/process` LLM çıktısına Pydantic şeması [5.3, 5.4] → **Deploy #7**

### FAZ 6 — AI-dostu repo (deploy gerekmez)
- [ ] **6-A** · repo kökü `CLAUDE.md` + `docs/` reorg (mimari/, plan/, arsiv/, kararlar/ + 30+ eski dosyanın taşınması)
- [ ] **6-B** · mimari doküman içerikleri + ADR'ler + modül üstü docstring'ler (subagent fan-out'a uygun) + proje skill'leri

## Durum notları (her oturum tek satır, en yeni üstte)

- 2026-08-11 (17): 3-F tamam (kod+test+push; DEPLOY #5 NOKTASI — deploy kullanıcıya soruldu, bensiz başlatılmadı) — KATMAN: `convert_pdfa_and_queue_uploads` dönüşüm hatasını (tüm iç yollar + "dosya oluşturulamadı") artık 500'e çevirmez → `_register_pending_conversion`: orijinal KENDİ uzantısıyla HEM ham HEM islenmis arşive enqueue edilir (".pdf adıyla sızma" korunur — stored_filename da pending ad, belge kartı/hukukbot indirmesi arşivde GERÇEKTEN duran adı görür), CaseDocument `conversion_status='pending'` + `conversion_attempts` + `conversion_spool_path` ile açılır, results'a conversion_pending/conversion_warning (gerçek nedenli) /archived_filename yazılır, akış sürer; katman kurulamazsa (spool kopyası ya da DB kaydı yok) ESKİ 500 taban çizgisi aynen. MODEL KARARI: upload_status'a yeni değer DEĞİL ayrı kolon — upload_status pending belgede orijinalin yükleme durumunu izlemeye devam eder (dik boyutlar; kart göstergesi + partial index bozulmaz); migration op 27 (3 kolon + `idx_case_docs_conversion_pending` WHERE pending; backfill YOK — 3-A kararı korundu). SPOOL KARARI: ayrı `conversion_spool` dizini (get_spool_dir deseni, volume) — upload_outbox spool'u yükleme başarısında silindiğinden kaynak olamazdı. E-POSTA KARARI: mevcut fallback korunur (cache-hit'te analiz PDF'i = gerçek PDF, miss'te orijinal) ama ek/indirme ADI dosyanın gerçek uzantısıyla gider (`effective_filename`; email ana ek contentType + download media_type uzantıya duyarlı yapıldı — maske yok); intake yolu aynı fonksiyondan otomatik kapsandı (entry.conversion_pending + error_ozet, status "queued" kalır). GECE JOB [services/conversion_retry.py]: MEVCUT APScheduler'a job (02:30 TR — 00:00 rapor + 03:30 pg_dump'la çakışmaz; yalnız lider, AST bekçi needle'ına eklendi); YÜKLEME KARARI senkron (outbox'a bağlamak statü-düşürme/stored_filename/hook sıralamasını worker'ın başarı yoluna sızdırırdı; SharePoint hıçkırığında ertesi gece dönüşüm dahil yeniden = idempotent+ucuz, 3-B iç retry zaten var); deneme sayacı dönüşümden ÖNCE commit (zehirli dosya bekçisi, upload_queue disiplini), dönüşüm-başarılı-yükleme-başarısız'da sayaç İADE edilir (kesintili geceler MAX'ı yemesin); MAX 5 gece → TEK nihai ERROR (TechnicalLogger, plan 3.8c; spool elle kurtarma için kalır: status='pending'+attempts=0), spool kayıpsa anında nihai; başarıda PDF/A üret → .pdf adıyla yükle → stored_filename/.sharepoint_url/upload_status güncelle + statü NULL (tek transaction) → hukukbot ANCAK O ZAMAN (URL yoksa hook yok — BULGULAR #1 disiplini); janitor: failed 35 gün (uploaded_at yaklaşıklığı, MAX payı dahil), soft-delete 30 gün, sahipsiz dosya 2 gün. YARIŞ KAPANDI: gece başarısı öncesi bekleyen islenmis-ORİJİNAL outbox satırları `superseded` yapılır (`supersede_pending_uploads`) — yoksa geciken orijinal yüklemesi `_record_upload_result`'la sharepoint_url'i .udf'e GERİ EZERDİ; upload sırasında yakalanırsa worker'ın sonuç-yazma adımındaki guard belgeye işlemez/hook açmaz, purge superseded spool'u bekletmeden siler. HUKUKBOT DIŞLAMA iki yüzey + savunma: enqueue_document + `_doc_passes_filters` + 2 tekil endpoint (404) `conversion_status IS NOT NULL` dışlar (140+ failed birikimi önlemi; 'failed' de dışarıda — arşiv kopyası hâlâ orijinal). LOG SÖZLEŞMESİ DÜZELTMESİ (dumanda yakalandı): converter-İÇİ deneme-düzeyi ERROR'lar WARNING'e indirildi (pdf_converter: decode/generic/GS-timeout/UDF-import/UDF-hata; format_converter: görüntü/LO-timeout; udf_converter: eleman-başı işleme hatası — tek belgede eşiği dolduruyordu) — 3-F sonrası dönüşüm hatası belge için nihai değil, 5+ bekleyen belge gece taramasında ERROR-oranı alarmını (≥5/5dk) her gece çaldırırdı; nihai ERROR tek noktada (gece MAX / pipeline katman-arızası / analiz stream-intake handler'ları). Testler: 770+2skip konteynerde (28 yeni: test_faz3_f_conversion_pending.py 25 — katman/route-maske/gece job birimleri/superseded/enqueue filtresi/bekçiler; test_export_filters.py +3, `_doc` helper'ına conversion_status eklendi), ruff+mypy temiz. LOKAL DUMAN (2 worker; override'a geçici smoke-invalid tenant + dev-bypass, gerçek arşive SIFIR yazma): migrate 3 kolonu+index'i lokal PG'de kurdu, scheduler'a job YALNIZ liderde eklendi ("lider değil" öbür worker'da); bozuk-ama-zip-geçerli .udf ile /confirm → 200 + conversion_pending + gerçek neden ("'content' element not found") + spool dosyası + ham/islenmis outbox satırları (.udf adıyla) + yalnız WARNING zinciri; gece FAIL koşusu → deneme 1/5 WARNING, attempts=4'ten koşuda TEK nihai ERROR + status=failed + spool korundu; gece BAŞARI koşusu (spool'a gerçek PDF, GERÇEK GS dönüşümü, sahte uploader) → completed: stored .udf→.pdf, url+uploaded, statü NULL, spool+pdfa temizlendi, islenmis-orijinal satırı superseded (ham dokunulmadı), hook EN SON ve statü-düşmüşken çağrıldı (kanıtlı sıralama); artıklar silindi (2 TEST belgesi CASCADE'le outbox dahil + 5 spool + 1 download meta + /tmp scriptleri), override geri alındı, restore sonrası açılış ERROR'suz + healthz 200. İzlenecek (Deploy #5 sonrası): ilk gerçek conversion_pending vakasında WARNING zinciri + results uyarısının kullanıcıya görünürlüğü (banner Faz 4), 02:30 taramasının INFO özeti ("Gece dönüşüm taraması"), superseded satır sıklığı, conversion spool dizini boyutu.
- 2026-08-11 (16): 3-E tamam (kod+test+push; DEPLOY YOK — Deploy #5 3-F ile) — DB [3.6]: create_engine'e pool_timeout=10 + connect_timeout=5 + statement_timeout=30 sn (libpq options → bağlantı bazlı, YALNIZ app engine'i kapsar: gece pg_dump kendi bağlantısını kurar=etkilenmez; migrate.py database import'undan ÖNCE DB_STATEMENT_TIMEOUT_MS=0 set eder → create_all + backfill UPDATE'ler sınırsız, `_build_connect_args(0)` seçeneği hiç göndermez, bekçi testli); get_db istisna yolunda rollback (idle-in-transaction-aborted birikimine karşı); rollback taraması — document_pipeline save_case_document/_update_email_status/save_hearing_date (guard'lı: rollback'in kendisi düşse de "hata=None/False döner" sözleşmeleri bozulmaz), processing _auto_update/_auto_enrich (guard'lı), upload_queue enqueue + _attempt_upload sayaç-commit'i (düşerse upload'a GEÇİLMEZ — zehirli-dosya bekçisi sayaca dayanır) + janitor, confirm_idempotency complete/release; case_manager'ın 4 commit'i ZATEN rollback'liydi, dokunulmadı. — PROCESS_CACHE [3.7]: `managers/ttl_cache.py`'ye DiskTTLCache — TASARIM KARARI "diskten lazy okuma" (sidecar-senkron DEĞİL): bellekte state hiç yok, her işlem diski okur (hacim kullanıcı-eylemi mertebesinde, meta birkaç yüz bayt) → worker'lar arası bayatlama sınıfı hiç doğmaz; girdi=`<key>.json` meta, TTL saati=dosya mtime'ı (touch=os.utime, içerik yazılmaz, restart'ta kalıcı; süre dolumu TTLCache gibi YALNIZ cleanup'ta), pop süreçler-arası ATOMİK (os.replace ile rastgele adlı claim'e taşı — yarışan iki pop'tan biri kazanır; confirm_receipts kapısının bypass moduna emniyet), `adopt_file_fields`: set() payload'ı (path/original_path, uzantı korunarak) volume'deki cache dizinine TAŞIR (env `PROCESS_CACHE_DIR` | `<backend>/data/process_cache`, 3-A get_spool_dir deseni; taşıma hatasında özgün yol korunur=aynı konteynerde /tmp zaten ortak, bugünden kötü değil), set+süpürme event-loop'ta DEĞİL executor'da (0-A disiplini); owner semantiği/original_ext meta'da aynen — accept_incoming_file/_touch_owned İMZASI DEĞİŞMEDİ (intake dahil otomatik kapsandı); boot'ta lifespan süpürmesi = "TTL indeksini yeniden kur" adımı (indeks diskin kendisi); YAKALANAN YARIŞ: eviction claim'i rename'le eski mtime'ı MİRAS alıyordu → eşzamanlı ikinci süpürücü "bayat claim" sanıp AYNI girdiyi ikinci kez evict ediyordu (testte kanıtlandı) → claim adına oluşturulma zamanı gömüldü (`.claim-<epoch>-<uuid>`), pencere sıfır; bozuk meta=miss+sessiz düşer, meta'sız payload 2×TTL orphan süpürmesi. DOWNLOAD_CACHE KARARI: kapsama ALINDI ama yalnız İNDEKS disk destekli (payload'ı taşımak anlamsız: dosya /confirm'den ~30 sn sonra zaten silinir; indeks süreç içi kalsaydı 2 worker'da işlenmiş-PDF indirmesi ~%50 sessiz 404/orijinal-fallback'ti — geçiş davranış-nötr kalsın diye kapatıldı; restart sonrası bayat girdi mevcut exists-kontrolü+404 yoluyla zaten temiz). Testler gerçek /app/data volume'ünü kirletmesin diye conftest, app import'undan ÖNCE iki cache dizinini tmp'ye yönlendirir (ilk koşuda konteynerde birikinti tespit edilip temizlendi). — --WORKERS 2: entrypoint `--workers ${UVICORN_WORKERS:-2}` + compose'da açık `UVICORN_WORKERS=2` (1'e çekmek = imajsız anında geri dönüş, recreate yeter); lifespan yedek init_db KALDIRILDI (AST bekçili; host-run için önce `python migrate.py`); süreç-tekil işler (APScheduler + catch-up + upload-outbox worker) YENİ `services/singleton_lock.py` lider kilidiyle kapılı (flock LOCK_EX|LOCK_NB, konteyner-YEREL tempdir — volume'e bilerek konmaz; dosya lifespan'de/fork-SONRASI açılır, import'ta açılsa fork'ta herkes lider olurdu; süreç ölünce kernel bırakır → yeniden doğan worker lifespan'inde devralır); SAPMA NOTU (bilinçli karar): refresh thread'i talimat listesindekinin AKSİNE worker-BAŞINA bırakıldı — DynamicConfig/matcher/searcher süreç İÇİ singleton (lazy-init koddan doğrulandı), lider-tekil yapılsa diğer worker taze cache dosyası yokken BOŞ listelerle kalırdı; duplikasyonun tek gerçek zararı cache dosyası yazma yarışıydı → cache_manager.save_cache pid'li tekil temp adla atomik yazar; bilinen sınır (kabul): /refresh yalnız isteği işleyen worker'ı tazeler, liste değişiklikleri nadir. 2-worker teşhisi için JSON log satırlarına `pid` alanı + technical_log senkron dosya adına `_p<pid>` eklendi (aynı-saniye SharePoint overwrite kaybı önlenir). Testler: 742+2skip konteynerde (40 yeni `test_faz3_e_hardening.py`: engine sabitleri/connect_args/get_db, rollback taraması fake-session'larla, DiskTTLCache birimleri — iki-instance=iki-worker paylaşımı, thread'li atomik-pop yarışı, eşzamanlı süpürmede tek-evict, adopt+evict payload silme, bozuk meta/bayat claim/orphan/sanitize; kilit tekliği+devralma; AST bekçileri: lifespan'de init_db çağrısı yok, süreç-tekil işler is_leader kapısı İÇİNDE, refresh kapı DIŞINDA, entrypoint --workers) + faz0'ın 3 sahiplik testi adopt semantiğine uyarlandı; ruff+mypy temiz. LOKAL DUMAN (compose, 2 worker; override'a geçici SHAREPOINT_TENANT_ID=smoke-invalid + dev-bypass konarak gerçek arşive SIFIR yazmayla, sonra geri alındı): migrate tek sefer entrypoint'te + iki "Application startup complete" (pid 9/10) + süreç-tekil işler YALNIZ liderde + öbüründe "lider değil" satırı; 5× gerçek /process→/confirm çifti (Gemini analizli) + aynı process_id'ye ikinci /confirm'de idempotent_replay=True; ÇAPRAZ KANIT: saklayan worker SIGSTOP'lanınca uvicorn supervisor'ı onu öldürüp yeniden doğurdu ve YENİ worker lider kilidini DEVRALDI (failover canlı gözlendi — scheduler+outbox yeni pid'de yeniden başladı), ölü worker'ın yazdığı girdiyi hayattaki worker /app/data/process_cache'ten POP edip pipeline'ı tamamladı (stored pid=9 → hit pid=10, PDF/A dönüşümü adopt edilmiş dosyadan koştu); RESTART KANITI: /process → docker restart → ESKİ process_id ile /confirm diskten hit (yeni süreçte); outbox retry'ının deneme-başı WARNING+backoff sözleşmesi kör-SharePoint'te canlı görüldü; duman artıkları (7 TEST belgesi + 7 receipt + 14 outbox satırı CASCADE + spool/cache dosyaları) silindi, override eski haline döndü. İzlenecek (Deploy #5 sonrası): 2 worker'ın bellek tabanı (mem_limit 2g, mem-watch canlı), lider failover'ın prod'da ilk görülüşü ("lider değil" + tek "Upload outbox worker başladı" beklenir), DOWNLOAD_CACHE çapraz indirmelerin 404'süz seyri, healthz/devre-kesici sinyallerinin worker-başına doğası (3-C/3-D notlarıyla kabul edilmişti).
- 2026-08-11 (15): 3-D tamam (kod+test+push; DEPLOY YOK — Deploy #5 3-F ile) — OFİS NO [3.4]: tahsis tek atomik işlem `counter_manager.reserve_next_counter()` (oku + ETag/If-Match PATCH + OKUNANI döndür) ve /process'in fetch_counter'ına bağlandı; /confirm'deki koşulsuz arka plan artırması + `async_increment_counter` KALDIRILDI (numara /confirm formunda hiç taşınmıyordu — tek tüketimi /process'te kullanıcıya gösterilmesi, yani tahsis anı = /process; eski oku-şimdi-artır-sonra deseni dakikalarca açık yarış penceresiydi). 412 döngüsüne jitter'lı üstel backoff (0.3→2.0 sn tavan, RESERVE_MAX_ATTEMPTS=4, son denemeden sonra uyumaz — 10 sn wait_for bütçesine sığar; sleep executor thread'inde). `get_counter_manager` süreç-tekil singleton + list_id/field_map cache → sıcak yolda tahsis 2 HTTP turu; 412-dışı hatada cache'ler düşer (liste yeniden kurulursa 404'e saplanmaz). 3-B paylaşılan Session'ına BAĞLANDI — güvenliği KODDAN doğrulandı (talimattaki iddia): urllib3.Retry forcelist'i {429,500,502,503,504}, 412 YOK → If-Match'li PATCH'in transport tekrarı (ilk deneme sunucuda işlenmiş, yanıt kayıp) bayat ETag'le 412'ye düşer → çakışma yolu yeni değeri okuyup TAZE numarayla devam eder → en kötü durum numara ATLANIR, iki kullanıcıya aynı numara imkânsız; GET'ler idempotent, 429/5xx retry bedava. Bilinçli takas: analiz edilip onaylanmayan belge artık numara yakar (boşluk) — eski akış da /confirm başına koşulsuz artırdığından boşluk yeniydi değil; TIMEOUT___/XXXXXXXXX sözleşmesi ve 10 sn timeout korundu (timeout'ta thread tahsisi arkada bitirebilir → boşluk, mükerrer değil). — /CONFIRM İDEMPOTENCY [3.5]: ANAHTAR KARARI process_id — doğrulama: file_hash /confirm'e ULAŞMIYOR (form alanı yok, PROCESS_CACHE girdisi path/original_path/original_ext/owner saklıyor), frontend Index.tsx /confirm'e process_id + dosya fallback'ini HEP birlikte gönderiyor ve kullanıcı retry'ında ikisi de aynen kalıyor → backend-only çözüm; file_hash semantik olarak YANLIŞ anahtar (aynı dilekçe iki ayrı davaya meşru tekrar), Idempotency-Key header'ı frontend paketi (Faz 4) beklerdi; sihirbaz baştan koşulursa yeni process_id = bilinçli yeni işlem (kabul edilen sınır). KAYIT YERİ DB: `confirm_receipts` (PK=process_id, owner/status/response_json; create_all'dan gelir, migration op GEREKMEDİ) — süreç içi dict DEĞİL, gerekçe 3-E: uvicorn restart'ı ve --workers 2'de worker'lar arası tutarlılık (karar notu models docstring'inde). Akış: kapı İLK yan etkiden (cache pop/tenant sorgusu) ÖNCE; begin→proceed (satır açıldı) / replay (completed satırın yanıtı AYNEN + results.idempotent_replay=True, pipeline+e-posta koşmaz) / in_progress (409 "TEKRAR GÖNDERMEYİN") / bypass (DB arızası → korumasız devam, bugünden kötü değil); başarıda yanıt JSON'u satıra yazılır; istisna yolunda İNCE AYRIM — belge yaratılmadan düşen istek satırı SİLER (dönüşüm 500'ünde anında retry serbest), `results.case_document_id` varsa satır in_progress KALIR (yeniden koşmak belgeyi mükerrer yaratırdı; retry 409, kilit 30 dk bayat eşiğinde optimistic-guard'lı devralmayla çözülür — eşik PROCESS_CACHE TTL'iyle hizalı, canlı istek tavanı ~300 sn); 7 gün fırsatçı temizlik; log sözleşmesi: modül hiç ERROR üretmez (kapı arızaları WARNING). — /COMMIT 409 [3.5]: önce mevcut davranış doğrulandı (frontend CommitConflictError'da sequence yenileyip BİR kez otomatik retry — kaybolan-yanıt senaryosunda kendi davasına çarpıp yeni numarayla MÜKERRER DAVA açıyordu); `case_manager.find_idempotent_commit_match` MUHAFAZAKÂR eşleşme (hepsi birden: tracking_no birebir + soft-delete DEĞİL + tenant damgası uyumlu + created_at ≤24 sa + esas_no VE court normalize eşit (boş=boş dahil) + taraf kümesi {(party_type, normalize_party_key)} eşit ve BOŞ DEĞİL; şüphede None → 409 sürer, yanlış pozitif yeni davayı yutmasın) → eşleşmede mevcut dava idempotent döner: `case.idempotent_reuse=true` + mevcut statü, davada stored_filename'i (sanitize edilmiş) zaten kayıtlı belgeler YENİDEN ARŞİVLENMEZ (mevcut doc_id ile "queued"; ilk commit'in yarım bıraktıkları normal döngüde cache yaşıyorsa tamamlanır — gerçek idempotent tamamlama), poliçe beslemesi zaten dedupe'lu, yanıt istek sırasını korur; eşleşmezse 409 aynı mesajla (frontend'in bump+retry'ı gerçek çakışmada — sayaç count-bug'ı dahil — doğru davranış, bozulmadı; silinen kayıtla çakışma soft-delete filtresi sayesinde bilerek eşleşme DIŞI). Log sözleşmesi: `[TRACKING_NO_COLLISION]` ERROR add_case'ten NİHAİ noktalara taşındı (intake commit gerçek-409'u + cases.py api_add_case), add_case'te WARNING'e indi — idempotent çözülen çakışma artık ERROR-oranı alarmını (≥5/5dk) beslemiyor. Testler: 702+2skip konteynerde (35 yeni: `test_faz3_counter_reserve.py` 9 — gerçek ETag semantiği uygulayan sahte sunucu, eşzamanlı-iki-tahsis-farklı-numara kanıtı, tek-ERROR sözleşmesi, bekçi: processing'te reserve var/increment yok; `test_faz3_confirm_idempotency.py` 26 — servis birimleri in-memory sqlite'la (StaticPool; aware→naive round-trip konteynerde önceden doğrulandı), /confirm route replay/409/release/kilit-tutma/bypass, match birimleri, commit route çözümleme+belge-dedup) + mevcut 409 testi yeni akışa uyarlandı (match=None monkeypatch — testte DB'ye çıkılmaz); ruff+mypy temiz (mypy için 3 tip daraltması + 1 bilinçli tipsiz param). Lokal duman: restart sonrası migrate `confirm_receipts`'i PG'de kurdu (PK psql teyitli), açılış ERROR'suz; GERÇEK-PG servis dumanı proceed→in_progress→replay→owner-mismatch→release zinciriyle kanıtlı. İzlenecek (Deploy #5 sonrası): ilk gerçek 504-retry'da "idempotent replay" INFO'su, tahsiste ETag conflict WARNING sıklığı, [TRACKING_NO_COLLISION]'ın yeni (yalnız-gerçek-çakışma) oranı.
- 2026-08-10 (14): 3-C tamam + **DEPLOY #4 AYNI GÜN YAPILDI (prod=bfaca9d; 3-A+3-B+3-C birlikte çıktı)** — kullanıcı isteğiyle ajan koştu (Deploy #3'teki gibi sınıflandırıcı izin verdi): aktivite kontrolü son 10 dk 0 istek, ÖN-ADIM chown idempotent koştu ama GEREKSİZDİ (prod /app/data volume kökü zaten appuser:appuser — lokaldeki root:root durumu prod'da yok, yazılabilirlik touch ile teyitli), deploy nohup ile, pre-dump predeploy_bfaca9d_20260810-175042.dump, sağlık kapısı 79 sn'de bfaca9d teyit etti, rollback hedefi ec7dcca (etiket bakımı son-3: bfaca9d/ec7dcca/6f3aca0, 5341afe silindi). Doğrulamalar HEPSİ geçti: public 200; /healthz nginx üzerinden "ok"+bfaca9d; startup ERROR'suz (yalnız bilinen 2 zararsız WARNING); migrate koştu → `upload_outbox` tablosu + `idx_upload_outbox_pending` partial index prod PG'de psql ile teyitli (3-A migration op 26); "Upload outbox worker başladı (startup reconcile ilk taramada)" prod'da İLK KEZ canlı; db-backup.timer aktif. İzlenecek: ilk gerçek /confirm'de outbox enqueue + spool dizini oluşumu, SharePoint hıçkırığında WARNING-retry-nihai-ERROR zinciri, Gemini fırtınasında kesici WARNING'i. — Paket içeriği: Retry sınıflandırması KOD bazlı `gemini_client.classify_transient`: `APIError.code ∈ {429,500,502,503,504}` + `httpx.TransportError`; string eşleme tamamen kaldırıldı. SDK hiyerarşisi (google-genai 2.11.0) konteynerde KODDAN doğrulandı: ClientError(4xx)/ServerError(5xx) ⊂ APIError, `.code` her zaman int dolu; httpx hataları (TimeoutException/ConnectError/RemoteProtocolError → hepsi TransportError altı) SDK'dan sarmalanmadan geçer; SDK'nın kendi tenacity retry'ı `retry_options` verilmediğinde "never retry" → çifte-retry çarpanı YOK (bilerek kapalı bırakıldı, gemini_client docstring'inde). DEADLINE BÜTÇESİ `GEMINI_RETRY_DEADLINE_SECONDS=170`: /process NDJSON stream'inde retry penceresi boyunca event akmaz → sessiz pencere nginx proxy_read_timeout 300'ü aşarsa 504; bütçe "yeni bekleme/deneme BAŞLATMA" kapısı, en kötü durum 170 + tek deneme HTTP tavanı 120 = 290 < 300 (aritmetik bekçi testi var; wait_for_files_active'in ayrı 120 sn tavanı kendi yield'inden sonra). DEVRE KESİCİ MODEL-BAŞINA (kritik keşif: Gemini rate-limit/overload model bazlı; intake ayrı model `GEMINI_INTAKE_MODEL` kullanıyor — tek global kesici bir modelin fırtınasında diğerini de keserdi, yanlış pozitif): art arda 5× SADECE 429/503 (`SATURATION_API_CODES`; 500/502/504+ağ retry edilir ama kesici SAYMAZ) → 60 sn açık; yarı-açık: süre dolunca izin, sayaç sıfırlanmadığından ilk 429/503 ANINDA yeniden açar, başarı tam kapatır; kesici kontrolü çağrı GİRİŞİNDE (döngü içinde değil — eldeki çağrı bütçeyle zaten sınırlı, amaç yeni yığılmayı önlemek); state süreç içi (tek worker; 3-E 2-worker'da worker-başına kabul, health.py gerekçesiyle kodda not); açılış WARNING'i TEK noktadan gemini_client'ta. Hızlı-fail kullanıcı yüzü: `GeminiCircuitOpenError` → `_api_error_ozet` artık exception-aware ("koruma devrede, 1 dk sonra deneyin"); HTTP kodu değişmedi (stream complete+ozet kanalı — durum kodu disiplini 5-B'nin işi); intake tüm-koşular-geçersiz terminaline kesiciye özel mesaj + log sözleşmesi gereği eksik olan tek ERROR eklendi. HEALTH SÖZLEŞMESİ korundu ve gerekçelendi: `record_gemini_error` yalnız nihai; hızlı-fail NİHAİ SAYILIR (kullanıcıya yansıyan son durum; kesinti sürerken degraded sinyali doğru); atlatılan retry'lar sayılmaz (testle kanıtlı); soft-fail yollarının kesici-atlamaları SAYILMAZ (Gemini'ye gidilmedi, kesiciyi açan hata zaten sayıldı). FINISH_REASON: `_ensure_response_text` — MAX_TOKENS → `GeminiTruncatedError` ("uzunluk sınırı" mesajı; kısmi metin gelse bile kesik JSON parse'a sokulmaz), SAFETY/RECITATION/BLOCKLIST/PROHIBITED_CONTENT/SPII/IMAGE_SAFETY veya prompt_feedback.block_reason → `GeminiBlockedError` (güvenlik filtresi teşhisi artık YALNIZ kanıtla; bare `except ValueError`'dan filtre iddiası kaldırıldı — UnicodeDecodeError tarzı sızıntılar yanlış etiketlenmez), nedensiz boş → generic "tekrar deneyin"; intake'in 3 yanıt kontrolü (koşu/doğrulayıcı/hakem) aynı yardımcıya bağlandı. KAPSAM TARAMASI: intake `analyzer._gemini_call_with_retry` üzerinden otomatik kapsandı (hakem route'ta zaten soft-fail ARBITER_FAILED); `email_sender` (2 üretim fonksiyonu, `_breaker_guarded_generate`) + `date_extractor.ask_llm_referee` tek-deneme+soft-fail KORUNDU (retry/bütçe bilerek eklenmedi: şablon/regex fallback'leri zarif, retry beklemesi /confirm zincirini ve preview'u geciktirirdi) ama kesiciye İKİ yönde bağlandı — danışma: açıkken çağrı hiç yapılmaz, anında fallback; besleme: gerçek 429/503 sayaca yazar (`GEMINI_MODEL_NAME` analyzer'la ortak olduğundan sinyal ortak); `upload_to_gemini`/`wait_for_files_active`/`files.delete` retry dışı (kendi deadline/soft-fail'leri var). Log sözleşmesi: deneme WARNING (etiket artık kod: "503", "ağ/ReadTimeout"), deadline-stop WARNING, kesici açılışı tek WARNING, nihai tek ERROR çağıran handler'da. httpx requirements'a bilerek eklenmedi (google-genai'nin zorunlu transport bağımlılığı — 3-B'nin urllib3 kararıyla aynı gerekçe). Testler: 667+2skip konteynerde (28 yeni `test_faz3_gemini_retry.py`: sınıflandırma/kesici birim+yarı-açık/retry+health/bütçe/model-izolasyon/finish_reason/ozet/soft-fail kapıları; fake aio client + `_monotonic` saat dolaylaması; conftest'e kesici reset'i eklendi), ruff+mypy temiz; konteyner restart sonrası gerçek ortamda import zinciri + kesici fast-fail dumanı kanıtlı (healthz sayacı 1'i gördü).
- 2026-08-10 (13): 3-B tamam (kod+test+push; DEPLOY YOK — Deploy #4 3-C'den sonra) — Graph retry'ın İÇ katmanı `sharepoint_uploader_graph.py`'de: süreç-tekil paylaşılan Session (`_get_shared_session`; kurulum sonrası state değişmez → thread-safe, verify ilk kullanımda donar) + urllib3.Retry(total=3, backoff_factor=1, forcelist 429/500/502/503/504, respect_retry_after_header) — urllib3 2.7'de PUT/POST/PATCH varsayılan listede DEĞİL, `allowed_methods` ile açıkça izinli; loopback dumanıyla kanıtlı (PUT 503×2→retry→200, bytes gövde her denemede tam gitti). Küçük dosya PUT gövdesi BİLEREK belleğe okunur (≤4 MB sınırlı: data=<dosya> stream'i transport retry'ında geri sarılamaz, ikinci deneme boş giderdi; 45 MB dosyalar chunk yolunda, OOM riski yok). 401: `get_graph_token(force_refresh=True)` (msal 1.34 `remove_tokens_for_client`, konteynerde teyitli; yoksa/atarsa app düşürülüp yeniden kurulur) + `_with_fresh_token_on_401` upload VE download'da BİR kez zorla yenileyip tekrar dener — healthz `record_graph_token_ok` damgası korunur, 401 transport retry'ına bırakılmaz (aynı header'la tekrar sonuç değiştirmez). Chunk resume: parça iç retry'a rağmen düşerse ya da 416 gelirse upload session URL'ine AUTH'SUZ GET → nextExpectedRanges ofsetinden `f.seek` ile devam (bütçe 3, upload baştan BAŞLAMAZ); 202'de sıradaki ofsetin otoritesi sunucu yanıtı + ilerleme-yoksa-hata sonsuz döngü koruması; ranges boşsa nihai hata → dış katman (outbox, conflictBehavior=replace) baştan yükler. TASARIM KARARI: `_get_site_and_drive_id` lru_cache anahtarı TOKEN olarak KORUNDU — token rotasyonu bedava TTL, force-refresh yeni anahtarla ID'leri yeni token'la çözer; ayrı TTL cache aynı davranışa fazladan kod olurdu. LOG SÖZLEŞMESİ: uploader'ın deneme-başı "SharePoint Upload Error" ERROR'u WARNING'e İNDİRİLDİ (metadata soft-fail ERROR'u da) — nihai ERROR'u üst katmanlar üretiyor (upload_queue nihai failed / async_* fallback); eski haliyle SharePoint kesintisinde deneme başına ERROR, ERROR-oranı alarmını (≥5/5dk) hıçkırıkta çaldırırdı. KAPSAM TARAMASI: `sync_to_cloud` + `scripts/upload_db_backup.py` uploader üzerinden iç retry'ı bedava aldı; `email_sender`/`activity_manager` sendMail'i ve `log_manager` liste-item POST'ları idempotent DEĞİL → paylaşılan session'ı bilerek KULLANMAZLAR (çift e-posta/çift kayıt riski — modül başı yorumunda uyarı var); `counter_manager` ETag döngüsü 3-D'nin işi, dokunulmadı. Timeout'lar (connect,read) tuple oldu — kara-delik ağda deneme 10 sn'de düşer, tek-thread worker'ın seri bütçesi korunur. Testler: 639+2skip konteynerde (19 yeni `test_faz3_graph_retry.py`: politika/küçük-PUT/401/chunk-resume/bütçe/416/no-progress/download/force-refresh), ruff temiz.
- 2026-08-10 (12): 3-A tamam (kod+test+push; DEPLOY YOK — Deploy #4 3-C'den sonra) — `upload_outbox` tablosu (create_all) + partial index `idx_upload_outbox_pending` (migration op 26) + `services/upload_queue.py`: enqueue payload'ı `/app/data/upload_spool`'a (backend-data volume; env `UPLOAD_SPOOL_DIR`) KOPYALAR ve pending satırı /confirm yanıtından ÖNCE commit'ler; tek worker thread (Event ile uyanır, 60 sn poll) satırları sırayla işler — backoff 60sn→300→900→1sa→3→6→12sa (MAX 8 deneme, ~23 sa pencere), deneme sayacı upload'dan ÖNCE commit (crash-loop'ta zehirli dosya MAX'ta nihai failed'a düşer), ilk tarama = startup reconcile (süreç ölümünde satır + spool kalır), janitor failed spool'ları 30 gün sonra siler (satır denetim izi kalır; elle kurtarma: status='pending', attempts=0). `convert_pdfa_and_queue_uploads` artık enqueue kullanır; enqueue None dönerse (DB/disk arızası) ESKİ tek-denemeli BackgroundTasks fallback'i devrede — hiçbir durumda bugünden kötü değil. islenmis denemeleri `_record_upload_result`'a işlenir (upload_attempts artar, kart failed/uploaded görür), hukukbot outbox hook'u YALNIZ URL commit'inde (davranış korundu); ham satırları belgeye dokunmaz. Log sözleşmesi: geçici hata WARNING + extra doc_id/outbox_id/kind, yalnız NİHAİ başarısızlık tek ERROR (ERROR-oranı alarmı hıçkırıkta çalmaz). TASARIM KARARI: backfill'in 50 eski failed belgesi retry kapsamı DIŞI — outbox satırı/spool dosyası yok, kaynak temp'ler /confirm+30sn'de silinmişti, sunucuda yeniden yüklenecek içerik yok; upload_status=failed dürüst gösterge olarak kalır, işaretleme değişikliği gerekmez. KRİTİK KEŞİF 1: migrate.py YENİ tablo oluşturamıyordu — init_db'de "Import models" YORUMU vardı ama import yoktu → Base.metadata boş, create_all sessiz no-op; bugüne dek maskeliydi (lifespan yedek init_db'si modeller route'lardan yüklüyken koşuyor), 3-E o yedeği kaldırınca migrasyon kör kalacaktı → `import models` eklendi + AST bekçi testi. KRİTİK KEŞİF 2: backend-data volume kökü root:root (G6 öncesi init) → appuser `/app/data/upload_spool` mkdir EDEMEZ, kuyruk sessizce fallback'te kalırdı → lokalde belgelenmiş tek seferlik chown yapıldı + yazılabilirlik doğrulandı; PROD İÇİN DEPLOY #4 ÖN-ADIMI olarak paket satırına yazıldı. Testler: 620+2skip konteynerde (20 yeni `test_faz3_upload_outbox.py`; fake SessionLocal + adlandırılmış logger handler kalıbı), ruff temiz. Lokal duman: migrate.py tabloyu lokal PG'de kurdu (partial index + FK CASCADE psql'le teyit); restart sonrası "Upload outbox worker başladı" + startup reconcile spool'suz pending duman satırını AĞA ÇIKMADAN nihai failed yaptı — JSON ERROR satırında outbox_id/kind/attempts extra'ları görüldü, duman satırı silindi. 3-E notuna upload-outbox worker tekilleştirmesi eklendi.
- 2026-08-10 (11): 2-C tamam — KRİTİK KEŞİF: Ops Agent sunucuda kuruluydu ama Cloud Logging API projede HİÇ etkinleştirilmemişti (agent bugüne dek syslog dahil hiçbir logu gönderememiş) → API etkinleştirildi (VM scope'ları logging.write/monitoring.write zaten var). `infra/gcp/ops-agent-config.yaml`: docker json-file logları iki katmanlı parse (sarmalayıcı + uygulama JSON'u; severity → LogEntry.severity) + net-watchdog/mem-watch dosya receiver'ları, varsayılan syslog pipeline'ı merge ile korunur; install.sh değişince agent'ı restart eder. Log metriği `hukdok_backend_error_count` + 3 alarm politikası CANLI (ERROR ≥5/5dk eşiği; kernel OOM log-match — regex 29 Tem'un GERÇEK satırıyla doğrulandı, cgroup varyantı dahil; net-watchdog+mem-watch KRITIK log-match; bildirim iki e-posta kanalına; `apply_monitoring.sh` idempotent, 2. koşu "aynı" — alarm testleri Deploy #3 SONRASI: mem-watch'a elle KRITIK satırı + prod beacon'a 5 POST). `/api/client-error`: auth'suz (bilinçli — auth kırıkken de rapor), IP başına 10/dk (limiter `rate_limiting.py`'ye ayrıldı, api.py + route paylaşır), 16 KB gövde tavanı (413), alan beyaz listesi + kırpma, sendBeacon text/plain gövdesi ham okunur (JSON header'ı CORS preflight isterdi), severity=ERROR + client_* extra'larla loglanır (ERROR-oranı alarmı sayar). Frontend `errorBeacon.ts`: window error+unhandledrejection → sendBeacon (fetch keepalive fallback), 30 sn imza dedup + oturum tavanı 20, asla fırlatmaz; main.tsx render ÖNCESİ kurar. `CaseDocument.upload_status/upload_attempts` migration (backfill: URL dolu→uploaded, boş→failed; partial index uploaded'ı dışlar) + `_record_upload_result` islenmis-upload noktasına bağlandı (attempts sayar, hukukbot outbox kapısı yalnız URL commit'inde — davranış korundu). Testler: 596+2skip konteynerde (12 yeni test_faz2_alerting.py; caplog DEĞİL adlandırılmış logger handler'ı — dictConfig pytest capture'ı söküyor) + 161 vitest host'ta (11 yeni). Lokal E2E kanıt: nginx:8080 → 204 + X-Request-ID yankısı + aynı kimlikli JSON ERROR satırı; tarayıcıdan iki hata türü beacon'landı (login sayfasından, auth'suz); migrate.py lokal PG'de koştu (176 uploaded / 53 failed backfill). DEPLOY #3 AYNI GÜN YAPILDI (kullanıcı isteğiyle ajan koştu; sınıflandırıcı bu kez push'a da ssh deploy'a da izin verdi): önce CI düzeltmesi — GitHub Actions ruff F401 yakaladı (2-B test dosyasında çıplak `import logging_setup`; 2-B hiç push edilmediği için ruff ilk kez gördü) → `6f3aca0` ile CI yeşil; deploy nohup ile (aktivite kontrolü: son 3 dk 0 process/confirm; gün içinde tel@hanyaloglu-acar.av.tr 3 belge yüklemişti, o an sakindi), pre-dump 1716 KB, sağlık kapısı 74 sn'de `6f3aca0` teyit etti, rollback hedefi `0b773d5`; install.sh yalnız ops-agent bölümünü YAZDI + restart (gerisi "aynı" — fluent-bit stop'ta zararsız bir timeout yaşadı, sonrası aktif). Doğrulamalar HEPSİ geçti: public 200; `/healthz` nginx üzerinden İLK KEZ derin JSON (`6f3aca0` + checks — 2-A prod'da aktif); docker logs JSON + `X-Request-ID: deploy3-dogrulama` yankısı ERROR ve erişim satırında; migration entrypoint'te koştu (backfill prod: 1690 uploaded / 50 failed); Cloud Logging'e `docker_json` akışı canlı — beacon test satırı severity=ERROR olarak düştü (parse zinciri + severity terfisi uçtan uca çalışıyor). Alarm test tetiklemeleri: 5× beacon POST (ERROR-oranı eşiği) + mem-watch.log'a elle KRITIK satırı → iki test e-postası beklenir. NOT: Windows konsolundan Türkçe karakterli curl gövdesi geçersiz UTF-8 gider → endpoint doğru 400'ler (testte ASCII kullan). DEPLOY-SONRASI EKLER (aynı akşam): (1) log-match filtreleri düzeltildi — Ops Agent satırı textPayload'a DEĞİL jsonPayload.message'a koyar, watchdog+OOM filtreleri iki biçimi de kapsar (b53b9f9); (2) politikalara severity eklendi (ERROR/CRITICAL — mail konusundaki "No severity" gitti, a592925); (3) ilkeberkkutluk@hotmail.com kanalı 4 alarma da bağlandı (şimdilik; 7637a51); (4) belge soft-delete (f79f9b1, oturum dışı commit) Deploy #3'le çıkmıştı ama dava kartı Case.documents ilişkisini FİLTRESİZ serileştiriyordu → silinen belge kartta kalıyordu; canlıda teşhis + tek satır fix + bekçi test + mini deploy (prod=ec7dcca, rollback 6f3aca0). ALARM TEYİTLERİ TAMAM: ERROR-oranı ALERT (17:56, değer 5>4) + RESOLVED (18:00) mailleri VE watchdog KRITIK "[Critical] Log alert fired" maili (19:00, üç kanala) kullanıcı ekran görüntüleriyle doğrulandı — FAZ 2 KAPANDI. Watchdog/OOM bildirimi saatte 1 ile sınırlı, incident autoClose 24 saat (test incident'ı yarın kendiliğinden kapanır). — `logging_setup.py`: tek dictConfig (`LOG_FORMAT=json|text`, compose backend'e json eklendi; uvicorn.error bizim handler'da, uvicorn.access susturuldu) + JsonFormatter (severity/timestamp/request_id/location + extra geçişi, GCP uzlaşımı) + saf-ASGI RequestIdMiddleware (X-Request-ID koru/üret + sanitize, contextvar → istek sırasındaki TÜM loglar kimlikli — create_task pipeline dahil, thread'ler bilinçli "-"; erişim satırı method/path/status/süre/IP ile, `/healthz`+`/` muaf; istisna yolunda kimlikli ERROR + re-raise); 11 basicConfig temizlendi (6 modül seviyesi silindi, 5 CLI/`__main__` → configure_logging), bekçi testi geri sızmayı keser; 584+2skip konteynerde yeşil (19 yeni; pyproject `addopts=-q` + elle `-q` = `-qq` özet satırını gizliyor, bilinene yaz); lokal recreate+duman kanıtlı (JSON satırlar, header yankısı 403/200'de, healthz erişim logunda yok); prod'a Deploy #3'le gider — restart YETMEZ, recreate (`up -d`) şart. Yedek kontrolü: dün gece 03:30 koşusu BAŞARILI ama eski script'leydi (install.sh backup_db.sh'ı 18:01'de yazdı) — senkron script'in ilk timer koşusu bu gece 03:30; risk düşük (md5=repo birebir, tek yeni bağımlılık `scripts/upload_db_backup.py` çalışan konteynerde doğrulandı); elle ön-tetikleme sınıflandırıcıya takıldı → istenirse kullanıcı: `sudo systemctl start db-backup.service`.
- 2026-08-08 (9): 2-A tamam — derin /healthz (`health.py` sinyalleri: Graph token yaşı + Gemini nihai hata sayacı; DB SELECT 1; 10 sn TTL cache; DB fail→503 "unhealthy", Gemini/Graph→200 "degraded"), container nginx `location = /healthz` (SPA yutması kalktı — lokalde :8080'den JSON doğrulandı, DB stop→503→toparlanma canlı kanıtlı), GCP uptime check path `/`→`/healthz` çevrildi (gcloud; alarm politikası "hukukoid.com ERISILEMIYOR" zaten bağlı+etkin; Deploy #3'e kadar prod'da SPA yutması sürer → davranış değişmez, sonra derinleşir); 565+1skip test yeşil (12 yeni); NOT: gece yedeği senkron script'le İLK koşusunu bu gece 03:30'da yapacak (install.sh 18:01'de koştu, dünkü 03:30 koşusu eski script'leydi — o da başarılıydı, SharePoint teyitli).
- 2026-08-08 (8): **FAZ 1 KAPANDI** — kullanıcı `install.sh` ilk koşusunu yaptı (yalnız backup_db.sh YAZILDI, gerisi "aynı"; ikinci koşu tümü "aynı" → idempotans teyitli), sunucu 0b773d5'e çekildi (frontend kapısı fix'i diskte), bayat `sites-available/hukukoid.com` silindi (kalan: default+hukbot); doğrulama: public 200, /healthz=5341afe, timer'lar aktif. Sıradaki: Faz 2-A.
- 2026-08-08 (7): **Deploy #2 yapıldı — prod=5341afe** (kullanıcı koştu, sınıflandırıcı ajanın deploy çalıştırmasına izin vermedi): pre-dump 1.7 MB alındı, build eski stack çalışırken, sağlık kapısı sürümü teyit etti ("101 sn, sürüm: 5341afe"); frontend kapısı tek-atım curl yarışına takılıp erken exit verdi (deploy sağlamdı) → 30 sn poll'a çevrildi; bootstrap pürüzü: elle pull yüzünden OLD=NEW → eski imajlar elle `9f1b202` etiketlendi (rollback hazır); sunucu override'ı `~/override-yedek-20260808.yml`e alındı, limitler base'ten geliyor (2G/512M/128M inspect'le doğrulandı); migrate.py tek seferlik koştu, timer'lar aktif, public rozet+/healthz=5341afe; KALAN: `sudo bash infra/install.sh` ilk koşusu + bayat `sites-available/hukukoid.com` silme (kullanıcıda).
- 2026-08-08 (6): Sürüm bilgisi gerçek kaynağa bağlandı (1-C eki, kullanıcı istedi) — Login'deki sabit "v2.4.1" yerine build'e gömülen git SHA (`VITE_APP_VERSION`, lokalde "dev"); `/healthz` artık `version` dönüyor ve deploy.sh sağlık kapısı çalışan sürümü beklenen SHA ile karşılaştırıyor (bayat imaj uyarısı); ARG/ENV yerleşimi cache-dostu (npm install/pip katmanları bozulmaz); 554 backend + 150 frontend test yeşil.
- 2026-08-08 (5): 1-C kodlandı — deploy.sh yeniden yazıldı (ff-only zorunlu, pre-deploy pg_dump ≥1 MB kapısı, build eski stack çalışırken, SHA etiketleme + rollback hedefi build öncesi korunur, 120 sn gerçek /healthz kapısı, db-backup.timer uyarısı, son-3-etiket bakımı) + rollback.sh (imaj dönüşü + sağlık kapısı, DB'yi döndürmez); LOKAL prova uçtan uca geçti (deploy 30 sn'de yeşil, dump 1.4 MB, rollback listeleme+dönüş çalıştı); tüm .sh'lar index'te +x (sunucudaki elle chmod'un kök nedeni); sunucu keşfi: compose v5.0.2 uyumlu, deploy.sh+entrypoint'te yalnız mode-kirliliği → prova öncesi checkout; Deploy #2 bekliyor.
- 2026-08-08 (4): 1-B tamam — `infra/` kuruldu: host nginx (default=hukukoid.com 300s + hukbot), net-watchdog/mem-watch/db-backup unit+script'leri, daemon.json sunucudan birebir çekildi (11 dosya diff'le doğrulandı); `scripts/prod` → `infra/scripts`e taşındı, README envanter+yeni-VM önkoşulları+restore ile `infra/README.md`de; idempotent `install.sh` yazıldı ama sunucuda HENÜZ koşmadı (1-C provasında); `.gitattributes` LF kuralı; compose'a pg/fe `memswap_limit` (sunucu override'ıyla hizalı — override Deploy #2 sonrası kalkacak); 554 test konteynerde yeşil.
- 2026-08-08 (3): 1-A kodlandı — compose'a mem/log limitleri + backend `/healthz` healthcheck + frontend depends_on:healthy (lokalde doğrulandı: backend healthy olana dek frontend bekledi, limitler HostConfig'te 2g/512m/128m); migrasyon `migrate.py` ile entrypoint'te tek seferlik, api.py import-time çağrı kaldırıldı; `--workers 2` → 3-E'ye ertelendi (PROCESS_CACHE süreç içi); 554 test konteynerde yeşil (6 yeni `test_faz1_infra.py`); prod'a Deploy #2 ile gidecek.
- 2026-08-08 (2): Deploy #1 yapıldı — prod=9f1b202; yedek alındı (SharePoint'e de), backend imajı yeniden derlendi, başlangıç temiz, HTTP 200, mem_limit 2g + MALLOC_ARENA_MAX=2 korundu; `upload_db_backup.py` artık imajda (docker-cp kırılganlığı kapandı); GS eşzamanlılık kanıtı ilk gerçek büyük /confirm'de loglardan izlenecek.
- 2026-08-08: Faz 0 tamamı kodlandı (0-A/0-B/0-C, 10 madde); 548 test konteynerde yeşil (17'si yeni `test_faz0_hardening.py`); 0.9 owner kontrolü intake set/touch/pop noktalarına da işlendi; 0.6 davranış notu: limit aşan ek artık hata değil, e-posta arşiv referansıyla gidiyor; Deploy #1 mesai dışı bekliyor.
- 2026-08-07: Takip dosyası oluşturuldu, paketler tanımlandı; henüz paket başlamadı.
