"""`managers` paketi işaretleyicisi — sembol RE-EXPORT ETMEZ, bilinçli boştur.

Alt modüller (case_manager, config_manager, reference_lists…) tam yolla import edilir;
buraya toplu import konursa açılışta tüm zincir (DB + SharePoint istemcileri) yüklenir.
"""
